---
title: "Nowcasting PM2.5 in Beijing Using Synchronous Covariates and Lagged Features: Model Comparison and Variable Selection Stability"
title_zh: 利用同步协变量和滞后特征进行北京PM2.5临近预报：模型比较与变量选择稳定性
authors: "Yusheng Wang, Ruiyan Du"
date: 2026-07-08
pdf: "https://arxiv.org/pdf/2607.07279v1"
tags: ["query:aq-forecast"]
score: 9.0
evidence: 使用滞后特征和深度学习进行PM2.5现在预测
tldr: "PM2.5临近预报对空气质量管理至关重要。本文比较正则化回归（Ridge、Lasso、Elastic Net）与深度学习（MLP、LSTM）模型，基于北京12站点3年小时观测数据，利用滞后项和同步协变量。MLP性能最优（RMSE=13.65，R²=0.972），比线性模型降低约13%误差；LSTM因计算限制表现不佳。变量选择稳定性分析表明，Lasso稀疏选择4个稳定变量，Elastic Net保留11个，其中CO、NO2、PM10和PM2.5一阶滞后为核心稳健集。站点和月度误差分析揭示空间与季节性异质性。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.07279v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1392, \"height\": 667}, {\"url\": \"assets/figures/arxiv/2607.07279v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1487, \"height\": 534}, {\"url\": \"assets/figures/arxiv/2607.07279v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1484, \"height\": 694}, {\"url\": \"assets/figures/arxiv/2607.07279v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1214, \"height\": 598}, {\"url\": \"assets/figures/arxiv/2607.07279v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1391, \"height\": 515}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.07279v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1657, \"height\": 372}, {\"url\": \"assets/tables/arxiv/2607.07279v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1415, \"height\": 462}, {\"url\": \"assets/tables/arxiv/2607.07279v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1500, \"height\": 218}, {\"url\": \"assets/tables/arxiv/2607.07279v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1656, \"height\": 362}, {\"url\": \"assets/tables/arxiv/2607.07279v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1655, \"height\": 704}, {\"url\": \"assets/tables/arxiv/2607.07279v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1501, \"height\": 607}]"
motivation: 验证不同模型在同步协变量和滞后特征下的PM2.5临近预报性能，并评估变量选择稳定性，以支持实际监测。
method: 采用Ridge、Lasso、Elastic Net和MLP、LSTM，基于北京12站点3年小时数据，通过时间序列划分和交叉验证进行模型比较与特征消融。
result: MLP最优（RMSE=13.65，R²=0.972），线性模型相近（RMSE≈15.6，R²≈0.964），LSTM因子样本训练表现差（RMSE=26.55，R²=0.889）。
conclusion: MLP适合PM2.5临近预报；Lasso稀疏选择4个稳定变量，Elastic Net保留11个；CO、NO2、PM10和PM2.5一阶滞后为稳健特征集。
---

## 摘要
可靠的PM2.5临近预报对日常空气质量监测和城市管理具有实际重要性。PM2.5浓度受排放源、气象条件、时间模式和站点异质性的共同影响，且解释变量之间存在强相关性。本研究比较了正则化回归方法（Ridge、Lasso、Elastic Net）与深度学习模型（MLP和LSTM）在北京市12个监测站2013年3月至2017年2月逐时观测数据上的同小时PM2.5估计效果。由于特征集同时包含PM2.5滞后项和同步共污染物测量值，该任务被定义为临近预报而非严格预测。模型评估采用基于时间戳的时间顺序训练/测试划分和TimeSeriesSplit交叉验证。MLP表现最佳（RMSE=13.651 μg/m³，R²=0.972），相比所有正则化回归模型（RMSE约15.6，R²约0.964），RMSE降低了约13%。三个线性模型表现几乎相同。作为探索性补充，LSTM——由于CPU计算限制而使用子采样训练集——表现较差（RMSE=26.553，R²=0.889），不将其作为主要结论。特征组消融实验表明，滞后项和同步污染物承载了主要的估计信息。基于1SE规则的变量选择稳定性分析显示，Lasso倾向于稀疏选择（4个稳定变量），而Elastic Net保留了相关变量组（11个稳定变量）；CO、NO₂、PM10和一阶PM2.5滞后项构成了稳健的核心集。逐站点和逐月误差分析进一步揭示了模型误差中的空间和季节异质性。

## Abstract
Reliable nowcasting of PM2.5 is of practical importance for daily air-quality monitoring and urban management. PM2.5 concentrations are jointly influenced by emission sources, meteorological conditions, temporal patterns, and station heterogeneity, and the explanatory variables exhibit strong correlations. This study compares regularized regression methods (Ridge, Lasso, Elastic Net) with deep learning models (MLP and LSTM) for same-hour PM2.5 estimation using hourly observations from 12 monitoring stations in Beijing from March 2013 to February 2017. Because the feature set includes both PM2.5 lagged terms and synchronous co-pollutant measurements, the task is framed as nowcasting rather than strict forecasting. Model evaluation employs timestamp-based chronological train/test splitting and TimeSeriesSplit cross-validation. The MLP achieves the best performance (RMSE = 13.651 ug/m3, R^2 = 0.972), reducing RMSE by approximately 13% relative to all regularized regression models (RMSE approximately 15.6, R^2 approximately 0.964). The three linear models perform nearly identically. As an exploratory supplement, the LSTM--constrained by CPU computational limitations to a subsampled training set--underperforms (RMSE = 26.553, R^2 = 0.889) and is not treated as a primary conclusion. Feature-group ablation shows that lagged terms and synchronous pollutants carry the dominant estimation information. Variable selection stability analysis under the 1SE rule reveals that Lasso favors sparse selection (4 stable variables) while Elastic Net retains correlated variable groups (11 stable variables); CO, NO2, PM10, and the first-order PM2.5 lag form a robust core set. Per-station and monthly error analyses further reveal spatial and seasonal heterogeneity in model errors.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）

*   **核心问题**：如何准确、稳定地对北京市PM2.5浓度进行**临近预报（nowcasting）**，即利用当前可获得的同步协变量（如其他污染物、气象数据）和PM2.5的历史滞后特征，估计当前小时或近期的PM2.5浓度。
*   **研究动机与背景**：PM2.5是衡量城市空气污染的关键指标，对健康和环境有重大影响。北京人口密集、交通繁忙，冬季供暖期雾霾频发，实时准确的PM2.5估计对空气质量管理、交通调控和政策评估至关重要。实际数据中存在**时序依赖、多重共线性和站点异质性**，普通最小二乘回归（OLS）在高度相关的解释变量下系数不稳定。因此，需要比较和评估不同模型（正则化回归 vs. 深度学习）的表现，并探讨变量选择的可靠性。

### 2. 论文提出的方法论：核心思想、关键技术细节

*   **核心思想**：
    *   **任务框架**：将PM2.5估计定义为**临近预报**，因为特征集同时包含当前时刻的协变量（如同步污染物）和PM2.5的历史滞后项，而非仅基于过去预测未来（严格预报）。
    *   **模型对比**：比较**正则化线性回归**（Ridge、Lasso、Elastic Net）与**深度学习模型**（多层感知机MLP、长短期记忆网络LSTM），评估估计精度和变量选择稳定性。
    *   **稳定性分析**：针对相关变量集，通过**稳定性选择**衡量所选变量在子样本中的重现性，增强结论可信度。

*   **关键技术细节**：
    *   **正则化回归（公式1）**：
        $$
        \min_{\beta_0, \beta} \left( \sum_{i=1}^n (y_i - \beta_0 - \mathbf{x}_i^\top \beta)^2 + \lambda \left[ \frac{1-\alpha}{2} \|\beta\|_2^2 + \alpha \|\beta\|_1 \right] \right)
        $$
        - $\lambda$ 为正则化强度，$\alpha \in [0,1]$ 平衡L1和L2惩罚。Ridge仅L2（$\alpha=0$），Lasso仅L1（$\alpha=1$），Elastic Net同时包含两者。使用坐标下降法求解。
    *   **深度学习架构**：
        - **MLP**：输入56个特征 → 全连接128 (ReLU, Dropout 0.3) → 全连接64 (ReLU, Dropout 0.2) → 输出1。训练超参数：MSE损失、Adam优化器、学习率$10^{-3}$、批量256、20个epoch，最后10%训练集作验证。
        - **LSTM**（探索性）：24小时滑动窗口 → LSTM(64) → Dropout(0.3) → LSTM(32) → Dropout(0.2) → 全连接16 (ReLU) → 输出1。训练超参数：MSE损失、Adam优化器、学习率$10^{-3}$、批量512、20个epoch。**因计算资源限制，仅在80000个随机子采样序列上训练，且全局构建序列（未按站点分离）**。
    *   **变量选择稳定性**：
        - 采用**1SE规则**：选择使交叉验证误差最小的$\lambda_{\min}$加上其一个标准误差对应的$\lambda$，得到更稀疏的模型。
        - **稳定性选择**：从训练集开头取连续时间块（约6000个观测），50%重叠滑动，生成B=50个子样本。对每个子样本用1SE规则重新拟合，记录每个变量被选中的频率$\pi_j$，将$\pi_j \ge \pi_0$（默认0.8）的变量视为稳定。

### 3. 实验设计

*   **数据集**：**北京多站点空气质量数据集**（UCI Machine Learning Repository），包含2013年3月至2017年2月北京市12个监测站逐时观测（约42万条记录）。特征包括6种污染物（PM2.5、PM10、SO₂、NO₂、CO、O₃）、5种气象变量（气温、气压、露点、降雨量、风速）、分类变量（站点、风向）及时间字段。PM2.5作为响应变量。
*   **数据预处理**：去除PM2.5缺失样本（2.08%），用训练集众数/中位数填补其他缺失值。构造56个特征，包括**同步污染物、气象变量、时间变量、循环编码（sin/cos转换小时和月份）、PM2.5滞后特征（1小时、3小时、24小时滞后和24小时滚动均值）**。所有预处理参数仅基于训练集拟合。
*   **基准（Baseline）**：
    *   **训练均值（TrainMean）**：始终预测训练集PM2.5均值。
    *   **滞后1步持久化（Lag1Persistence）**：用前1小时PM2.5作为当前预测。
    *   **对比模型**：RidgeCV、LassoCV、ElasticNetCV、MLP、LSTM（探索性）。
*   **评估与划分**：
    *   **时间序列划分**：按时间戳排序，前80%作训练集，后20%作测试集。
    *   **交叉验证**：正则化模型在训练集内使用TimeSeriesSplit（k=5）选择$\lambda$和$\alpha$网格。深度学习模型使用最后10%训练集作验证（严格时间顺序，无打乱）。
    *   **评估指标**：RMSE、MAE、R²。

### 4. 资源与算力

*   **未明确GPU**：论文明确指出所有模型（包括MLP和LSTM）均在**CPU**上训练，未使用GPU。
*   **训练时长**：LSTM在CPU上训练耗时约为MLP的8-10倍（单次运行）。
*   **数据限制**：LSTM仅使用80,000个随机子采样序列（总可用290,272个），且全局构建序列（未按站点分离），导致表现不佳。作者承认这些限制使LSTM结果仅具探索性，不纳入核心结论。

### 5. 实验数量与充分性

*   **实验数量**：
    *   **模型性能对比**：5个模型（Ridge、Lasso、Elastic Net、MLP、LSTM）在测试集上比较。
    *   **特征组消融实验**：以RidgeCV为基础，去除不同特征组（仅滞后、不含滞后、仅气象+时间、仅时间等），共6种设置。
    *   **变量选择稳定性分析**：Lasso和Elastic Net在1SE规则下，对50个时间块子样本进行稳定性选择，并在7个阈值（$\pi_0$）下检验稳健性。
    *   **空间/时间异质性**：12个站点和9个月份（2016年4月至2017年1月）的逐站点/逐月误差分析。
*   **充分性与公平性**：
    *   **优点**：使用时间序列划分（避免未来信息泄露），正则化模型通过TimeSeriesSplit调参，深度学习使用严格时间顺序验证集。特征工程合理（包括滞后防止目标泄漏）。稳定性分析采用时间块子采样（保证时序依赖）。基准设置合理。
    *   **不足**：
        - LSTM实验不充分：子采样、全局序列、仅20个epoch且未收敛（训练损失仍在下降），导致可比性差。作者明确指出LSTM不用于核心比较，但将明显较差的结果直接展示可能产生误导。
        - 深度学习模型（MLP和LSTM）**仅各训练一次**（单次运行），无重复试验或平均，无法评估随机性带来的方差。
        - 未进行超参数搜索（如MLP的层数、dropout比例等）或学习率调度，可能未达最优。

### 6. 论文的主要结论与发现

1.  **MLP表现最优**：测试集RMSE=13.651 μg/m³，R²=0.972，比所有正则化回归模型（Ridge、Lasso、Elastic Net的RMSE约15.6，R²≈0.964）降低约13%误差。
2.  **正则化模型性能相近**：三种线性惩罚结构（L2、L1、混合）在点估计精度上几乎相同，说明在当前精心设计的特征集下，线性部分已提取大部分可解释方差。
3.  **LSTM因计算限制表现差**：RMSE=26.553，R²=0.889，甚至低于滞后1步持久化基准（RMSE=19.869）。作者强调这一结果不代表LSTM方法本身无效，而是反映了实际部署中CPU训练的高壁垒。
4.  **特征重要性排序**：滞后项和同步污染物是最重要的特征组。仅使用滞后变量即可接近全特征模型性能（RMSE=19.99, R²=0.943）。去除滞后项后RMSE大幅上升至24.776。气象和时间变量仅起辅助作用。
5.  **变量稳定性差异**：在1SE规则下，Lasso稀疏选择4个稳定变量（其中3个始终被选），Elastic Net保留11个（全部始终被选）。**核心稳定变量**为：**CO、NO₂、PM10和PM2.5一阶滞后项**，具有合理物理含义（燃烧排放、同源、短期自相关）。
6.  **空间和季节异质性**：北部郊区站点误差较低（如怀柔RMSE=14.424），中心城区站点误差较高（如万寿西宫RMSE=18.618）。冬季月份误差显著增大（2017年1月RMSE=25.98），夏季最低（2016年8月RMSE=8.232）。

### 7. 优点

*   **任务定义清晰**：区分“nowcasting”与“forecasting”，符合同步协变量可用的实际场景。
*   **稳定性分析完善**：引入1SE规则和稳定性选择，增强了变量选择结论的可靠性，适用于高相关特征场景。
*   **特征工程细致**：构造了多种滞后特征（多阶滞后+滚动均值）、循环编码、站点标识等，避免目标泄露（滚动均值基于滞后1步后的窗口）。
*   **消融实验完整**：系统性移除不同特征组，揭示滞后项和同步污染物的主导作用。
*   **空间和时间误差分析**：揭示聚合指标掩盖的异质性，为站点/季节特定建模提供依据。
*   **实验设计严谨**：严格时间顺序划分、训练集外预处理、TimeSeriesSplit交叉验证，避免数据泄漏。

### 8. 不足与局限

*   **LSTM实验不充分**：因CPU限制仅用子采样数据、全局序列（未按站点分离）、仅20个epoch且未收敛，导致结果远差于基线，不能作为与MLP公平对比的结论。
*   **深度学习模型无重复试验**：MLP和LSTM均仅运行一次，未报告随机种子下的多次结果，无法评估估计的稳定性。
*   **超参数优化有限**：正则化模型有网格搜索，但MLP和LSTM的架构（层数、节点数）、学习率、dropout比率等未经系统调优，可能未达到各自最优性能。
*   **模型范围较窄**：仅限于线性正则化回归和浅层MLP/LSTM，未尝试集成方法、树模型（如XGBoost）或更复杂的时间序列模型（如Transformer）。
*   **仅限北京数据**：结论可能依赖特定城市的气象、排放和站点布局，普遍性需在其他城市验证。
*   **与严格预报的差距**：使用同步污染物（需要实时观测）意味着这用于当前小时估计，而非未来几小时的预报，实际应用中需考虑同步数据的可用性。
*   **计算资源影响结论**：作者明确指出CPU限制影响了LSTM的训练，但对MLP和其他模型同样在CPU上运行（但MLP训练更高效），这一点未深入讨论。

（完）
