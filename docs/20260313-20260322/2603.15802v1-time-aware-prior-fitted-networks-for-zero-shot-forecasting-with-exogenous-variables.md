---
title: Time-Aware Prior Fitted Networks for Zero-Shot Forecasting with Exogenous Variables
title_zh: 时间感知的先验拟合网络：支持外生变量的零样本预测
authors: "Andres Potapczynski, Ravi Kiran Selvam, Tatiana Konstantinova, Shankar Ramasubramanian, Malcolm Wolff, Kin G. Olivares, Ruijun Ma, Mengfei Cao, Michael W. Mahoney, Andrew Gordon Wilson, Boris N. Oreshkin, Dmitry Efimov"
date: 2026-03-16
pdf: "https://arxiv.org/pdf/2603.15802v1"
tags: ["query:ts"]
score: 10.0
evidence: 带外生变量的高级零样本预测
tldr: 针对现有时间序列基础模型普遍忽略外部变量（如促销、天气）导致预测精度受限的问题，本文提出了ApolloPFN。这是一种时间感知的先验拟合网络（PFN），通过创新的合成数据生成程序和时间感知架构修改，实现了对外部协变量的原生支持。实验表明，ApolloPFN在M5和电价预测等依赖外部信息的任务中达到了SOTA水平，显著提升了零样本预测的准确性。
selection_source: fresh_fetch
motivation: 现有时间序列基础模型大多忽略外部变量，导致在处理受外部因素驱动的剧烈波动或相位变化时预测精度不足。
method: 提出ApolloPFN，通过改进合成数据生成流程并引入具备时间归纳偏置的架构，使PFN能够原生处理外部协变量。
result: 在M5零售需求和电力价格预测等包含外部信息的基准测试中，ApolloPFN 取得了当前最先进的零样本预测结果。
conclusion: ApolloPFN 证明了结合时间感知架构与外部变量建模是提升时间序列基础模型预测性能的关键路径。
---

## 摘要
在许多时间序列预测场景中，目标时间序列通常伴随着外生协变量，例如零售需求中的促销和价格；能源负荷中的温度；交通或销售中的日历和节假日指标；以及电力定价中的电网负荷或燃料成本。忽略这些外生信号会显著降低预测准确性，特别是当它们导致目标序列出现峰值、不连续性或状态与相位变化时。目前大多数时间序列基础模型（如 Chronos、Sundial、TimesFM、TimeMoE、TimeLLM 和 LagLlama）都忽略了外生协变量，仅根据数值型时间序列历史进行预测，从而限制了它们的性能。在本文中，我们开发了 ApolloPFN，这是一种先验数据拟合网络（PFN），它具有时间感知能力（不同于以往的 PFN），并且原生支持外生协变量（不同于以往的单变量预测器）。我们的设计引入了两项主要进展：(i) 一种专门定制的合成数据生成程序，旨在解决将表格（非时间性）PFN 应用于时间序列时出现的失效模式；(ii) 时间感知的架构修改，嵌入了利用时间序列上下文所需的归纳偏置。我们证明了 ApolloPFN 在包含外生信息的基准测试（如 M5 和电价预测）中取得了最先进的结果。

## Abstract
In many time series forecasting settings, the target time series is accompanied by exogenous covariates, such as promotions and prices in retail demand; temperature in energy load; calendar and holiday indicators for traffic or sales; and grid load or fuel costs in electricity pricing. Ignoring these exogenous signals can substantially degrade forecasting accuracy, particularly when they drive spikes, discontinuities, or regime and phase changes in the target series. Most current time series foundation models (e.g., Chronos, Sundial, TimesFM, TimeMoE, TimeLLM, and LagLlama) ignore exogenous covariates and make forecasts solely from the numerical time series history, thereby limiting their performance. In this paper, we develop ApolloPFN, a prior-data fitted network (PFN) that is time-aware (unlike prior PFNs) and that natively incorporates exogenous covariates (unlike prior univariate forecasters). Our design introduces two major advances: (i) a synthetic data generation procedure tailored to resolve the failure modes that arise when tabular (non-temporal) PFNs are applied to time series; and (ii) time-aware architectural modifications that embed inductive biases needed to exploit the time series context. We demonstrate that ApolloPFN achieves state-of-the-art results across benchmarks, such as M5 and electric price forecasting, that contain exogenous information.

---

## 论文详细总结（自动生成）

以下是对论文《Time-Aware Prior Fitted Networks for Zero-Shot Forecasting with Exogenous Variables》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
在现实世界的预测任务（如零售、能源、交通）中，**外生变量**（如价格、促销、天气、节假日）对准确性至关重要。忽略这些信号会导致模型无法捕捉由外部因素驱动的突发峰值或趋势转变。
然而，现有的时间序列基础模型（如 Chronos, TimesFM 等）大多是单变量的，无法原生处理外生变量。虽然基于先验拟合网络（PFN）的 TabPFN-TS 尝试解决此问题，但它本质上是为独立同分布（i.i.d.）的表格数据设计的，缺乏时间感知的归纳偏置，导致其在处理有序模式、趋势外推和近期偏差方面存在显著缺陷。

### 2. 方法论：核心思想与关键技术
论文提出了 **ApolloPFN**，通过改进合成数据生成流程和模型架构，使其具备原生处理外生变量的能力：
*   **核心思想**：利用 PFN 框架，通过在大量合成的时间序列数据上进行预训练，使模型能够学习如何根据历史观测值和未来外生变量进行贝叶斯推断。
*   **合成数据生成（SRNGN）**：
    *   提出了**单根节点随机生长网络（SRNGN）**算法，确保生成的结构因果模型（SCM）具有单一根节点和较长的因果路径，相比传统 RGN 算法能更有效地模拟复杂的特征依赖。
    *   引入**时间序列根节点激励**：根节点不再是独立采样，而是通过随机频率和振幅的正弦/余弦函数组合生成，从而在合成数据中引入时间相关性。
*   **架构修改**：
    *   **位置编码**：引入旋转位置嵌入（RoPE）捕捉相对距离，并结合绝对位置编码（正弦/余弦编码）来增强模型对时间顺序的感知。
    *   **全注意力机制（Full Attention）**：打破了传统 PFN 中测试点互不干扰的限制，允许所有时间点（包括预测区间）相互关注，从而使未来的外生信息能充分指导当前的预测。

### 3. 实验设计
*   **数据集**：
    *   **含外生变量场景**：电力价格预测（EPF，包含 5 个欧洲主要市场）、M5 竞赛数据集（零售需求，包含价格和促销信息）。
    *   **单变量场景**：M1、M3、M4 竞赛系列，以及 Tourism 数据集。
*   **Benchmark 与对比方法**：
    *   **支持外生变量的模型**：TabPFN-TS、Moirai-Large。
    *   **单变量基础模型**：Chronos-Large、Sundial-Base、LagLlama 等。
*   **评估指标**：sCRPS（概率预测准确性）、RMSSE（点预测准确性）。

### 4. 资源与算力
*   **模型规模**：ApolloPFN 仅有 **1100 万（11M）参数**，远小于对比的 Large 级别模型（通常为数亿参数）。
*   **训练细节**：模型训练了 400K 步，Batch Size 为 64，学习率采用余弦退火策略。
*   **算力说明**：文中**未明确提及**具体的 GPU 型号、数量及总训练时长，但提到其参数量较小，推理成本相对较低。

### 5. 实验数量与充分性
*   **实验规模**：测试涵盖了超过 **9 万条** 真实时间序列，跨越了不同的频率（小时、日、周、月、年）和领域。
*   **消融实验**：针对数据生成算法（SRNGN vs RGN）、位置编码（RoPE）、注意力机制（Full Attention）分别做了详细的消融对比。
*   **客观性**：实验对比了当前最先进的基础模型，并区分了“纯零样本”和“训练中见过测试集（如 Moirai）”的情况，评价标准较为客观公平。

### 6. 主要结论与发现
*   **性能领先**：在电价预测任务中，ApolloPFN 比次优模型（TabPFN-TS）平均提升了 **12%**；在单变量基准测试中也比 TabPFN-TS 提升了 **10%**。
*   **零样本能力**：尽管参数量仅为其他大模型的 1/30 到 1/70，ApolloPFN 在零样本设置下依然达到了 SOTA 水平，证明了 PFN 范式在时间序列领域的潜力。
*   **失效模式修复**：通过引入时间感知设计，ApolloPFN 成功解决了 TabPFN-TS 无法外推趋势、缺乏近期偏差和置信区间校准差等问题。

### 7. 优点
*   **原生支持外生变量**：无需针对特定任务进行微调即可直接利用外部信号。
*   **高效轻量**：以极小的参数量实现了超越大规模 Transformer 模型的性能。
*   **数据驱动的先验**：通过改进的合成数据生成，使模型在没有见过真实数据的情况下也能理解复杂的时间依赖关系。

### 8. 不足与局限
*   **计算复杂度**：采用标准平方复杂度的注意力机制，限制了其处理**超长序列**的能力。
*   **上下文依赖**：作为一种上下文学习（In-context Learning）模型，在观测值极少的情况下可能难以推断出序列特有的结构。
*   **合成数据偏差**：模型的性能高度依赖于合成数据分布的质量，如果真实数据的模式完全超出了合成先验的覆盖范围，模型表现可能会受限。

（完）
