---
title: Accurate and Efficient Multi-Channel Time Series Forecasting via Sparse Attention Mechanism
title_zh: 基于稀疏注意力机制的高效准确多通道时间序列预测
authors: "Lei Gao, Hengda Bao, Jingfei Fang, Guangzheng Wu, Weihua Zhou, Yun Zhou"
date: 2026-03-19
pdf: "https://arxiv.org/pdf/2603.18712v1"
tags: ["query:ts"]
score: 10.0
evidence: 捕获通道间依赖的多通道时序预测
tldr: 本研究针对多通道时间序列预测中通道间交互建模不足的问题，提出了Li-Net架构。该模型通过动态压缩序列与通道表示，结合可配置的非线性模块进行预测。核心创新在于引入稀疏Top-K Softmax注意力机制与多尺度投影框架，并融合多模态嵌入以引导注意力聚焦关键特征。Li-Net在保持高预测精度的同时，显著降低了内存占用并提升了推理速度，实现了性能与效率的优异平衡。
selection_source: fresh_fetch
motivation: 传统多通道时间序列预测方法往往忽视了通道间复杂的线性与非线性动态依赖关系。
method: 提出Li-Net架构，利用稀疏Top-K注意力机制和多模态信息融合技术，动态捕捉跨维度特征。
result: 在多个真实数据集上的实验表明，Li-Net在达到SOTA性能的同时，具有更低的计算负担和更快的推理速度。
conclusion: Li-Net证明了结合稀疏注意力和多模态融合能有效提升多通道时间序列预测的效率与准确性。
---

## 摘要
多通道时间序列预测任务在金融、供应链管理和能源规划等众多领域广泛存在。有效捕捉通道内部及通道之间的复杂动态依赖关系对于实现准确预测至关重要。然而，传统方法较少关注通道间相互作用的学习。本文提出了线性网络（Li-Net），这是一种专为多通道时间序列预测设计的新型架构，可捕捉通道间的线性和非线性依赖关系。Li-Net 在序列和通道维度上动态压缩表示，通过可配置的非线性模块处理信息，并随后重建预测。此外，Li-Net 在多尺度投影框架中集成了稀疏 Top-K Softmax 注意力机制以应对这些挑战。其核心创新在于能够无缝整合并融合多模态嵌入，引导稀疏注意力机制聚焦于最具信息量的时间步和特征通道。在多个真实世界基准数据集上的实验结果表明，与最先进的基准方法相比，Li-Net 取得了具有竞争力的性能。此外，Li-Net 在预测准确性与计算负担之间实现了卓越的平衡，具有显著更低的内存占用和更快的推理速度。详细的消融实验和参数敏感性分析验证了所提架构中各关键组件的有效性。关键词：多变量时间序列预测，稀疏注意力机制，多模态信息融合，非线性关系

## Abstract
The task of multi-channel time series forecasting is ubiquitous in numerous fields such as finance, supply chain management, and energy planning. It is critical to effectively capture complex dynamic dependencies within and between channels for accurate predictions. However, traditional method paid few attentions on learning the interaction among channels. This paper proposes Linear-Network (Li-Net), a novel architecture designed for multi-channel time series forecasting that captures the linear and non-linear dependencies among channels. Li-Net dynamically compresses representations across sequence and channel dimensions, processes the information through a configurable non-linear module and subsequently reconstructs the forecasts. Moreover, Li-Net integrates a sparse Top-K Softmax attention mechanism within a multi-scale projection framework to address these challenges. A core innovation is its ability to seamlessly incorporate and fuse multi-modal embeddings, guiding the sparse attention process to focus on the most informative time steps and feature channels. Through the experiment results on multiple real-world benchmark datasets demonstrate that Li-Net achieves competitive performance compared to state-of-the-art baseline methods. Furthermore, Li-Net provides a superior balance between prediction accuracy and computational burden, exhibiting significantly lower memory usage and faster inference times. Detailed ablation studies and parameter sensitivity analyses validate the effectiveness of each key component in our proposed architecture.   Keywords: Multivariate Time Series Forecasting, Sparse Attention Mechanism, Multimodal Information Fusion, Non-linear relationship

---

## 论文详细总结（自动生成）

这是一份关于论文《Accurate and Efficient Multi-Channel Time Series Forecasting via Sparse Attention Mechanism》（基于稀疏注意力机制的高效准确多通道时间序列预测）的结构化分析报告：

### 1. 核心问题与研究动机
*   **核心问题**：如何在保证高预测准确性的同时，有效捕捉多通道（多变量）时间序列中复杂的通道内动态和通道间相互依赖关系，并解决传统注意力机制带来的高计算复杂度问题。
*   **研究动机**：
    *   **通道交互缺失**：传统方法往往将各通道视为独立序列，忽视了它们之间的耦合关系（如金融指标间的关联）。
    *   **计算瓶颈**：标准自注意力机制的复杂度随序列长度呈平方级增长（$O(n^2)$），难以处理长序列。
    *   **辅助信息利用不足**：现实场景中存在丰富的多模态信息（如节假日、类别语义、门店属性），现有模型难以高效融合这些异构数据。

### 2. 方法论
论文提出了 **Li-Net（Linear-Network）** 架构，核心思想是“编码-处理-解码”的压缩与重建流程：
*   **多模态嵌入模块**：利用 `text2vec`（基于 BERT）将文本信息（如门店 ID、商品描述）转化为高维向量，并使用 **CoSENT 损失函数** 优化向量空间，使语义相近的特征在向量方向上更接近。
*   **特征编码器（Feature Encoder）**：
    *   **Top-K Sparse Softmax 注意力**：这是核心创新。通过 MLP 生成 Logits，仅保留前 K 个最大值的权重，其余置为负无穷。这实现了动态特征筛选，过滤了噪声并显著降低了计算量。
    *   **多尺度投影**：按照“时间维度 $\rightarrow$ 通道维度”的顺序进行压缩，将高维输入映射到抽象的低维空间。
*   **非线性变换模块**：提供可配置的 MLP 或 Transformer 块，用于在压缩空间内捕捉高阶非线性模式。
*   **特征解码器（Feature Decoder）**：采用对称的“通道 $\rightarrow$ 时间”顺序，将抽象特征映射回原始实体空间，生成最终预测。

### 3. 实验设计
*   **数据集**：涵盖了 5 个主流基准数据集：**ETTh2/ETTm2**（电力变压器）、**Electricity**（用电量）、**Weather**（气象）、**Traffic**（交通流）以及 **M5**（沃尔玛零售数据）。
*   **对比方法（Baselines）**：
    *   **Transformer 类**：iTransformer, PatchTST。
    *   **MLP/线性类**：TimeMixer。
    *   **卷积/概率类**：TCN, TFT。
*   **评估指标**：准确性（MAE, MSE）、效率（训练/推理时间、内存占用、模型参数量）。

### 4. 资源与算力
*   **硬件环境**：使用了一台配备 **NVIDIA L40S GPU** 的服务器。
*   **软件环境**：PyTorch 2.6.0, CUDA 12.4, Python 3.11.11。
*   **训练细节**：采用 AdamW 优化器，Batch Size 为 16，训练 10 个 Epoch，设置早停机制（Patience=3）。论文明确记录了不同数据集下的训练时长（如 ETTh2 约 38-60 秒）和内存占用（最低仅约 27MB）。

### 5. 实验数量与充分性
*   **实验规模**：在 8 个子数据集设置下进行了 32 组整体性能对比。
*   **消融实验**：设计了三种变体（Li-Net-softmax, Li-Net-Primitive, Li-Net-MLP），分别验证了稀疏注意力、多模态嵌入和编码器结构的必要性。
*   **参数敏感性分析**：针对稀疏保留率（Retention Rate）、压缩等级、嵌入维度和非线性模块选择进行了详细测试。
*   **评价**：实验设计非常充分且客观。通过跨领域数据集验证了泛化性，并从精度和工程落地（内存/显存）两个维度进行了公平对比。

### 6. 主要结论与发现
*   **性能领先**：Li-Net 在绝大多数任务中 MAE 和 MSE 均排名第一或第二，平均指标显著优于 iTransformer 和 PatchTST。
*   **极致效率**：Li-Net 的模型大小平均仅为 **0.5MB**（对比 TFT 的 26.8MB），内存占用极低，推理速度极快，非常适合资源受限的工业环境部署。
*   **稀疏性优势**：实验证明中等程度的稀疏保留率（0.3-0.5）在长序列预测中效果最好，能有效过滤冗余信息。

### 7. 优点
*   **平衡性极佳**：在保持 SOTA 级精度的同时，实现了极小的参数量和极高的运算效率。
*   **多模态融合创新**：将 NLP 领域的 `text2vec` 和 `CoSENT` 引入时序预测，增强了模型对业务背景信息的理解。
*   **架构灵活性**：非线性模块可插拔（MLP/Transformer），允许根据算力资源灵活调整模型复杂度。

### 8. 不足与局限
*   **超参数依赖**：Top-K 的 K 值和压缩等级对不同数据集较为敏感，可能需要针对新场景进行较多调优。
*   **极端长序列挑战**：虽然稀疏注意力降低了复杂度，但在处理超大规模通道（如数万个变量）时的表现尚未在文中深入探讨。
*   **文本依赖性**：模型性能部分取决于多模态嵌入的质量，若原始业务文本描述缺失或质量较差，模型优势可能会削弱。

（完）
