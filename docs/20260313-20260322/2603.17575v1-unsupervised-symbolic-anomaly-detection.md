---
title: Unsupervised Symbolic Anomaly Detection
title_zh: 无监督符号异常检测
authors: "Md Maruf Hossain, Tim Katzke, Simon Klüttermann, Emmanuel Müller"
date: 2026-03-18
pdf: "https://arxiv.org/pdf/2603.17575v1"
tags: ["query:ts"]
score: 9.0
evidence: 通过符号回归实现可解释的异常检测
tldr: 针对传统异常检测模型不透明的问题，本文提出SYRAN，一种基于符号回归的无监督异常检测方法。该方法不依赖高维黑盒模型，而是学习一组描述正常数据规律的可解释数学方程（符号不变式）。当新数据偏离这些方程时即判定为异常。SYRAN在保持与最先进方法相当的检测性能的同时，提供了具有科学或医学意义的可解释逻辑，实现了检测过程的透明化。
selection_source: fresh_fetch
motivation: 传统的异常检测模型通常是黑盒，缺乏可解释性，难以让用户理解其背后的检测逻辑。
method: 利用符号回归学习正常数据中近似恒定的数学方程组作为符号不变式，并以数据对这些方程的偏离程度作为异常评分。
result: 实验证明SYRAN不仅能提取出符合科学或医学规律的方程，且其检测性能与当前最先进的方法相当。
conclusion: 该研究展示了符号回归在构建高性能且本质可解释的异常检测系统方面的有效性与潜力。
---

## 摘要
我们提出了 SYRAN，一种基于符号回归的无监督异常检测方法。我们的方法不是将正常模式编码在不透明的高维模型中，而是学习一组描述符号不变性（symbolic invariants）的人类可读方程：即在正常数据上近似为常数的函数。与这些不变性的偏差会产生异常评分，从而使检测逻辑在构建时就具有可解释性，而非通过事后解释。实验结果表明，SYRAN 具有高度的可解释性，能够提供与已知科学或医学关系相对应的方程，并保持了与最先进方法相当的强大异常检测性能。

## Abstract
We propose SYRAN, an unsupervised anomaly detection method based on symbolic regression. Instead of encoding normal patterns in an opaque, high-dimensional model, our method learns an ensemble of human-readable equations that describe symbolic invariants: functions that are approximately constant on normal data. Deviations from these invariants yield anomaly scores, so that the detection logic is interpretable by construction, rather than via post-hoc explanation. Experimental results demonstrate that SYRAN is highly interpretable, providing equations that correspond to known scientific or medical relationships, and maintains strong anomaly detection performance comparable to that of state-of-the-art methods.

---

## 论文详细总结（自动生成）

### 论文总结：无监督符号异常检测 (SYRAN)

#### 1. 核心问题与研究动机
*   **核心问题**：现代异常检测（AD）模型（如深度神经网络、森林集成等）虽然性能强大，但通常是“黑盒”模型，其内部逻辑对人类不可见。在医疗、金融和预防性维护等高风险领域，缺乏可解释性限制了模型的信任度、公平性评估以及对底层科学规律的进一步探索。
*   **研究动机**：受物理学中“守恒定律”（不变性）的启发，作者希望开发一种**本质可解释**的异常检测方法。该方法不依赖事后解释技术，而是直接学习描述正常数据规律的数学方程。

#### 2. 方法论
SYRAN 的核心思想是将“正常”定义为满足一组**符号不变式（Symbolic Invariants）**。
*   **核心思想**：寻找函数 $f(x)$，使得在正常训练数据上 $f(x) \approx 1$。当新样本使该函数值大幅偏离 1 时，即判定为异常。
*   **关键技术细节**：
    *   **损失函数设计**：包含三部分：
        1.  **不变性损失 ($L_1$)**：确保函数在正常数据上接近常数 1。
        2.  **噪声对比项 ($L_{noise}$)**：引入随机生成的背景噪声，防止模型学习到全局恒等的平凡解（如 $f(x)=1$）。
        3.  **复杂度惩罚 ($L_c$)**：使用双重对数函数惩罚表达式的复杂度，确保生成的方程简短且人类可读。
    *   **优化算法**：采用基于树结构的**进化符号回归算法**，通过变异和交叉操作在离散的数学算子空间中搜索最优方程。
    *   **集成策略**：使用**特征袋装（Feature Bagging）**，让每个集成成员只关注一小部分特征子集（如 2 个特征），以提高多样性和鲁棒性。
    *   **评分机制**：通过逻辑回归函数（Sigmoid）对各方程的偏差进行校准和平均，得出最终异常评分。

#### 3. 实验设计
*   **数据集**：
    *   **物理案例**：开普勒第三定律数据集（验证能否从行星轨道数据中重新发现 $T^2/a^3 \approx 1$）。
    *   **基准测试**：来自 ADBench 的 19 个真实世界数据集（如 pima, breastw, cardio, wine 等），涵盖医疗、生化和科学领域。
*   **对比方法**：对比了 14 种主流算法，包括经典方法（KNN, LOF, IForest, PCA, OCSVM）和现代深度学习方法（DeepSVDD, DEAN, DAGMM 等）。
*   **评估指标**：AUC-ROC 曲线下面积（包括集成平均性能和最佳成员性能）。

#### 4. 资源与算力
*   **算力说明**：论文未明确提及具体的 GPU 型号或 CPU 核心数。
*   **训练时长**：文中提到在进行实验筛选时，为每个数据集的优化设置了 **2 小时的时间预算**。如果模型无法在 2 小时内完成优化（如特征数极多的数据集），则予以排除。
*   **代码实现**：提供了 GitHub 开源地址，表明其算法具有较好的复现性。

#### 5. 实验数量与充分性
*   **实验规模**：在 19 个不同领域的基准数据集上进行了测试，并进行了统计显著性检验（Friedman-Wilcoxon 协议）。
*   **消融实验**：针对复杂度权重 $\gamma$、特征袋装大小 $K$ 和噪声边界 $\Delta$ 进行了详细的参数敏感性分析。
*   **充分性评价**：实验设计较为充分且客观。通过 CD 图（关键差异图）展示了 SYRAN 与 14 种基线的对比，证明了其在保持可解释性的同时，性能与最先进的黑盒模型处于同一梯队。

#### 6. 主要结论与发现
*   **性能竞争力**：SYRAN 的集成平均性能与 LOF、DEAN 等强基线相当；而其“最佳成员”（Max）的平均排名甚至位居所有方法之首。
*   **科学发现能力**：SYRAN 在一分钟内重新发现了开普勒第三定律，而人类历史为此花费了十年。
*   **领域洞察**：在乳腺癌和脊柱病变数据集中，SYRAN 提取出的简单方程（如 $x_2 + |1-x_2|$）与医学直觉高度吻合，甚至能揭示某些基准数据集过于简单（单特征即可完美分类）的缺陷。

#### 7. 优点
*   **本质可解释性**：输出的是闭式数学方程，无需事后解释，用户可以直接审查、验证或将其嵌入到其他系统中。
*   **轻量化与移植性**：训练后的模型只是几个简单的数学公式，几乎可以在任何设备（如嵌入式传感器）和任何编程语言中运行。
*   **专家参与**：允许领域专家从生成的方程池中挑选最符合物理/医学逻辑的方程，从而进一步提升检测精度。

#### 8. 不足与局限
*   **高维扩展性**：由于符号回归的搜索空间随特征数量呈指数级增长，SYRAN 目前依赖特征袋装（每次只看 2-5 个特征），可能难以捕捉极高维特征间的复杂非线性交互。
*   **计算开销**：进化算法的搜索过程比简单的梯度下降更耗时，且在大规模特征集上可能面临收敛困难。
*   **平凡解风险**：尽管引入了噪声对比，但在某些极端分布的数据上，仍可能生成缺乏实际物理意义的复杂方程。

（完）
