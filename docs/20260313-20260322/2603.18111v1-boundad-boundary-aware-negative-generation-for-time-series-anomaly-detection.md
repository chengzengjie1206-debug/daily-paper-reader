---
title: "BoundAD: Boundary-Aware Negative Generation for Time Series Anomaly Detection"
title_zh: BoundAD：面向时间序列异常检测的边界感知负样本生成
authors: "Xiancheng Wang, Lin Wang, Zhibo Zhang, Rui Wang, Minghang Zhao"
date: 2026-03-18
pdf: "https://arxiv.org/pdf/2603.18111v1"
tags: ["query:ts"]
score: 10.0
evidence: 用于时序异常检测的边界感知负样本生成
tldr: 针对时间序列异常检测中对比学习依赖负样本质量的问题，本文提出BoundAD框架。该方法通过重构驱动的边界负样本生成机制，利用强化学习自适应调整优化步长，从正常样本的重构轨迹中诱导产生靠近数据流形边界的硬负样本。这种无需预定义异常模式的方法增强了边界监督，显著提升了异常表示学习能力和检测性能。
selection_source: fresh_fetch
motivation: 现有对比学习方法依赖随机扰动或伪异常注入，难以兼顾时间语义一致性并提供有效的决策边界监督。
method: 提出一种重构驱动的框架，结合强化学习自适应调整更新幅度，从正常样本的重构过程中挖掘流形边界附近的硬负样本。
result: 实验结果表明，该方法在多个数据集上有效提升了异常表示学习效果，并取得了具有竞争力的检测性能。
conclusion: BoundAD通过挖掘模型学习动态中的边界负样本，无需先验异常知识即可实现高效的时间序列异常检测。
---

## 摘要
时间序列异常检测（TSAD）中的对比学习方法严重依赖于负样本构建的质量。然而，现有的基于随机扰动或伪异常注入的策略往往难以同时保持时间语义一致性并提供有效的决策边界监督。大多数现有方法依赖于先验异常注入，而忽视了直接从正常样本自身在数据流形边界附近生成难负样本（hard negatives）的潜力。为解决这一问题，我们提出了一种重构驱动的边界负样本生成框架，通过正常样本的重构过程自动构建难负样本。具体而言，该方法首先采用重构网络捕捉正常的时序模式，然后引入强化学习策略，根据当前的重构状态自适应地调整优化更新幅度。通过这种方式，可以沿着重构轨迹诱导出靠近正常数据流形的边界偏移样本，并进一步用于后续的对比表示学习。与依赖显式异常注入的现有方法不同，所提出的框架不需要预定义的异常模式，而是从模型自身的学习动态中挖掘更具挑战性的边界负样本。实验结果表明，该方法有效地改进了异常表示学习，并在当前数据集上实现了具有竞争力的检测性能。

## Abstract
Contrastive learning methods for time series anomaly detection (TSAD) heavily depend on the quality of negative sample construction. However, existing strategies based on random perturbations or pseudo-anomaly injection often struggle to simultaneously preserve temporal semantic consistency and provide effective decision-boundary supervision. Most existing methods rely on prior anomaly injection, while overlooking the potential of generating hard negatives near the data manifold boundary directly from normal samples themselves. To address this issue, we propose a reconstruction-driven boundary negative generation framework that automatically constructs hard negatives through the reconstruction process of normal samples. Specifically, the method first employs a reconstruction network to capture normal temporal patterns, and then introduces a reinforcement learning strategy to adaptively adjust the optimization update magnitude according to the current reconstruction state. In this way, boundary-shifted samples close to the normal data manifold can be induced along the reconstruction trajectory and further used for subsequent contrastive representation learning. Unlike existing methods that depend on explicit anomaly injection, the proposed framework does not require predefined anomaly patterns, but instead mines more challenging boundary negatives from the model's own learning dynamics. Experimental results show that the proposed method effectively improves anomaly representation learning and achieves competitive detection performance on the current dataset.

---

## 论文详细总结（自动生成）

### BoundAD：面向时间序列异常检测的边界感知负样本生成 —— 论文总结

#### 1. 核心问题与研究动机
*   **研究背景**：时间序列异常检测（TSAD）在工业监控、医疗和网络安全等领域至关重要。目前，对比学习已成为该领域的主流范式，其效果高度依赖于负样本的质量。
*   **核心问题**：现有的负样本构建方法（如随机扰动、伪异常注入）存在两个主要缺陷：
    1.  难以同时保持时间语义的一致性。
    2.  无法提供有效的决策边界监督。
*   **研究动机**：大多数方法依赖于先验的异常注入，而忽视了从正常样本自身出发、在数据流形边界附近生成“难负样本”（Hard Negatives）的潜力。作者希望通过模型自身的学习动态，自适应地挖掘更具挑战性的边界负样本。

#### 2. 方法论
BoundAD 框架分为三个阶段，旨在从重构驱动的角度实现边界感知：
*   **阶段 1：重构模型预训练**
    *   使用基于 Transformer 和 ExtremeKAN 非线性增强模块的重构网络 $M_\theta$。
    *   目标是捕捉正常的时序模式，为后续阶段提供基础表示和重构基准。
*   **阶段 2：强化学习（RL）引导的伪异常生成与三元组表示学习**
    *   **核心思想**：引入 RL 代理（Actor 网络）来控制重构模型的参数更新步长。
    *   **状态与动作**：状态 $s_t$ 包含历史重构损失；动作 $a_t$ 映射为有符号步长 $u_t$。
    *   **边界诱导**：当重构误差过低时，RL 驱动模型向反方向微调，从而在正常流形边缘诱导出“边界偏移”的伪异常样本。
    *   **对比学习**：利用生成的伪异常作为负样本，通过三元组损失（Triplet Loss）训练编码器，拉大正常与异常在嵌入空间中的距离。
*   **阶段 3：基于原型的判别式细化**
    *   引入一组可学习的**原型向量（Prototypes）**。
    *   通过联合优化正常样本的紧凑性、异常样本的分离性、原型分散性和平衡性约束，进一步细化正常数据流形。
    *   最终异常评分基于样本嵌入到最近原型的距离。

#### 3. 实验设计
*   **实验场景**：主要针对两类典型异常场景进行验证：
    1.  **点异常（Point Anomalies）**：包括全局和上下文异常。
    2.  **集体/模式异常（Collective/Pattern Anomalies）**：包括 Shapelet（形状）、Seasonal（季节性）和 Trend（趋势）异常。
*   **评估指标**：AUC、TP（真阳性）、TN（真阴性）、FP（假阳性）、FN（假阴性）。
*   **对比分析**：通过可视化异常评分曲线和特征分布图，分析模型在不同异常类型下的定位能力和延迟表现。

#### 4. 资源与算力
*   **算力说明**：论文中**未明确提及**具体的 GPU 型号、数量、内存占用或具体的训练时长。
*   **实现细节**：提到 Actor 网络采用多层感知机（MLP），重构网络包含 Transformer 编码器，属于深度学习常规开销。

#### 5. 实验数量与充分性
*   **实验规模**：论文进行了初步的实验验证，涵盖了 5 种细分异常类型。
*   **充分性评价**：
    *   **优点**：实验涵盖了从点到模式的多种异常，并提供了详细的训练过程分析（如三元组损失的收敛情况）。
    *   **局限**：作者在文中坦承这属于“初步可行性研究”，尚未在 SMD、MSL 等大规模异构基准数据集上进行系统性的横向对比。
    *   **客观性**：通过可视化手段展示了检测延迟和特征分离趋势，分析较为客观，未回避点异常下误报率较高的问题。

#### 6. 主要结论与发现
*   **性能表现**：BoundAD 在季节性异常（AUC 0.9606）和点异常（AUC 0.9438）任务上均表现出较强的判别能力。
*   **模式敏感性**：该方法对**模式异常（Pattern Anomalies）**更为有效，因为这类异常在时间窗口内具有持续的结构偏差，更易被对比学习捕捉。
*   **边界价值**：RL 驱动的负样本生成比随机扰动更能提供有价值的边界监督信号，有助于形成更紧凑的正常流形。

#### 7. 优点（亮点）
*   **自适应性**：无需预定义异常模式，通过 RL 动态调整更新步长，实现了“模型驱动”的负样本挖掘。
*   **端到端细化**：阶段 3 的原型学习是可训练的神经模块，而非简单的静态聚类后处理，增强了表示空间的判别力。
*   **结构可解释性**：异常评分曲线与时序结构变化高度相关，特征分布图直观展示了正常与异常的分离过程。

#### 8. 不足与局限
*   **检测延迟**：由于采用滑动窗口机制，模式异常的评分上升往往滞后于异常发生点（需等待异常信号占满窗口）。
*   **点异常挑战**：在处理极短的点异常时，边界容易受正常波动干扰，导致误报率相对较高。
*   **超参数敏感性**：RL 的奖励函数设计、更新步长范围以及原型的数量等超参数可能对最终效果有较大影响，且调优过程可能较复杂。
*   **验证范围**：缺乏在更多真实世界复杂多变量场景下的泛化性验证。

（完）
