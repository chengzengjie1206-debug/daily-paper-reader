---
title: "Baguan-TS: A Sequence-Native In-Context Learning Model for Time Series Forecasting with Covariates"
title_zh: Baguan-TS：一种用于带协变量时间序列预测的原生序列上下文学习模型
authors: "Linxiao Yang, Xue Jiang, Gezheng Xu, Tian Zhou, Min Yang, ZhaoYang Zhu, Linyuan Geng, Zhipeng Zeng, Qiming Chen, Xinyue Gu, Rong Jin, Liang Sun"
date: 2026-03-18
pdf: "https://arxiv.org/pdf/2603.17439v1"
tags: ["query:ts"]
score: 9.0
evidence: 时间序列预测的上下文学习模型
tldr: Baguan-TS是一个统一的时间序列预测框架，旨在解决现有上下文学习（ICL）方法依赖人工特征或缺乏推理自适应的问题。它通过3D Transformer直接在原始序列上进行表征学习与ICL，并引入局部校准和上下文过拟合策略来提升稳定性和预测精度。实验证明其在含协变量的基准测试和能源数据集中表现优异，实现了高效的零样本自适应预测。
selection_source: fresh_fetch
motivation: 现有的时间序列上下文学习方法大多依赖手工特征，而端到端序列模型在推理阶段缺乏自适应能力。
method: 提出一种基于3D Transformer的统一框架，通过协同处理时间、变量和上下文轴，并结合检索式局部校准与上下文过拟合策略来优化预测。
result: 在含协变量的公开基准和真实能源数据集上，Baguan-TS在点预测和概率预测指标上均显著优于现有主流模型。
conclusion: 该研究成功将原始序列表示学习与上下文学习相结合，为复杂协变量下的时间序列预测提供了一种稳健且高效的新方案。
---

## 摘要
Transformer 使得时间序列预测中的上下文学习（ICL）能够实现快速、无梯度的自适应，然而，大多数 ICL 风格的方法依赖于表格化的手工特征，而端到端序列模型则缺乏推理时的自适应能力。我们通过一个统一的框架 Baguan-TS 弥补了这一差距，该框架将原始序列表示学习与 ICL 相结合，并由一个在时间、变量和上下文轴上进行联合注意力的 3D Transformer 实例化。为了使这一高容量模型具有实用性，我们解决了两个关键障碍：(i) 校准和训练稳定性，通过一种与特征无关、基于目标空间检索的局部校准来改进；(ii) 输出过度平滑，通过上下文过拟合策略来缓解。在带有协变量的公开基准测试中，Baguan-TS 始终优于现有的基准模型，实现了最高的胜率，并在点预测和概率预测指标上均有显著下降。在多种真实世界能源数据集上的进一步评估证明了其鲁棒性，并带来了实质性的改进。

## Abstract
Transformers enable in-context learning (ICL) for rapid, gradient-free adaptation in time series forecasting, yet most ICL-style approaches rely on tabularized, hand-crafted features, while end-to-end sequence models lack inference-time adaptation. We bridge this gap with a unified framework, Baguan-TS, which integrates the raw-sequence representation learning with ICL, instantiated by a 3D Transformer that attends jointly over temporal, variable, and context axes. To make this high-capacity model practical, we tackle two key hurdles: (i) calibration and training stability, improved with a feature-agnostic, target-space retrieval-based local calibration; and (ii) output oversmoothing, mitigated via context-overfitting strategy. On public benchmark with covariates, Baguan-TS consistently outperforms established baselines, achieving the highest win rate and significant reductions in both point and probabilistic forecasting metrics. Further evaluations across diverse real-world energy datasets demonstrate its robustness, yielding substantial improvements.

---

## 论文详细总结（自动生成）

以下是对论文《Baguan-TS: A Sequence-Native In-Context Learning Model for Time Series Forecasting with Covariates》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：如何在不依赖人工特征工程的前提下，让时间序列模型既能直接处理原始序列（Sequence-Native），又具备上下文学习（In-Context Learning, ICL）的快速自适应能力。
*   **研究动机**：
    *   **现有模型局限**：端到端序列模型（如 PatchTST）擅长提取原始序列特征但缺乏推理时的无梯度自适应；而现有的 ICL 预测模型（如 TabPFN-TS）通常将时间序列“表格化”，依赖手工特征，忽略了原始序列的结构信息。
    *   **协变量挑战**：在处理包含天气、节假日等协变量的复杂多变量预测时，现有模型难以平衡全局规律学习与局部任务自适应。

### 2. 方法论
Baguan-TS 提出了一个统一的框架，将原始序列表示学习与 ICL 集成：
*   **3D Transformer 架构**：
    *   模型将输入视为三维张量：**时间轴（Temporal）**、**变量轴（Variable）**和**上下文轴（Context）**。
    *   通过专门的注意力机制交替处理这三个维度，捕捉时间演化、跨变量相关性和跨样本（支持集与查询集）的对齐关系。
*   **Y空间检索式预测（Y-space RBfcst）局部校准**：
    *   不同于传统的基于协变量（X空间）的检索，该方法基于历史目标序列（Y空间）的相似性检索最相关的上下文样本。
    *   这种方法与特征无关，能提供针对特定任务的分布调整，增强了模型在分布偏移下的稳定性。
*   **上下文过拟合策略（Context-Overfitting Strategy）**：
    *   **目的**：解决 ICL 模型容易产生的“输出过度平滑”问题（即模型倾向于预测平滑趋势而忽略周期性尖峰）。
    *   **手段**：在训练中加入辅助任务，强制模型识别并精确重建上下文中出现的特定模式（如复制一段序列到查询集中），引导模型学会“模板匹配”。
*   **自适应推理模式**：支持 3D 序列模式和 2D 表格模式的集成（Ensemble），利用两者的互补性提升预测精度和校准度。

### 3. 实验设计
*   **数据集**：
    *   **fev-bench-cov**：包含 30 个具有已知动态协变量的任务。
    *   **fev-bench-uni**：包含 10 个单变量/多变量基准任务。
    *   **真实世界能源数据**：27 个涉及电力负荷和光伏发电的实际应用数据集。
*   **对比方法（Benchmark）**：包括主流的时间序列大模型（Chronos-Bolt, Moirai-2.0, TimesFM-2.5, TiRex, Toto-1.0, Sundial-Base）以及领先的 ICL 模型（TabPFN-TS）。
*   **评估指标**：SQL（缩放分位数损失）、MASE（平均绝对缩放误差）、WAPE、WQL 等。

### 4. 资源与算力
*   **模型规模**：参数量约为 **2240 万（22.4M）**。
*   **训练细节**：模型在混合了合成数据和真实数据（GIFT-Eval 语料库）的平台上训练，训练步数约为 220K 步。
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量及总训练时长。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 67 个不同的预测任务（30+10+27），并针对检索策略、过拟合策略、推理模式进行了详细的消融实验。
*   **充分性与公平性**：
    *   实验对比了当前最先进的多个基础模型，且在公开基准和私有真实数据集上均进行了验证。
    *   引入了噪声注入实验（高斯噪声、随机游走、周期噪声）来测试鲁棒性，实验设计较为客观、全面。

### 6. 主要结论与发现
*   **性能领先**：Baguan-TS 在 `fev-bench-cov` 上取得了最高的胜率，SQL 指标比 TabPFN-TS 降低了 4.8%。
*   **检索优势**：证明了基于目标序列相似性（Y空间）的检索比基于协变量（X空间）的检索在时间序列 ICL 中更有效。
*   **平滑抑制**：上下文过拟合策略显著提升了模型对周期性尖峰信号的捕捉能力，降低了训练损失。
*   **集成效应**：2D 模式（关注特征间关系）与 3D 模式（关注时序结构）的集成能有效抵消各自的偏差，使概率预测更接近理想的均匀分布。

### 7. 优点
*   **原生序列支持**：摆脱了对繁琐手工特征工程的依赖，直接从原始数据中学习。
*   **创新的 3D 注意力**：将 ICL 的样本对齐逻辑与时序建模逻辑在架构层面进行了深度融合。
*   **鲁棒性强**：通过局部校准和过拟合策略，模型在面对噪声和分布偏移时表现出极强的稳定性。

### 8. 不足与局限
*   **计算复杂度**：3D Transformer 涉及三个维度的注意力计算，尽管采用了因子化设计，但在处理超长上下文或极多变量时，计算开销可能较高。
*   **离散化限制**：模型采用 5000 个 Bin 的离散化输出，虽然支持概率预测，但对于极值或超出预设范围（[-10, 10]）的数据可能存在截断风险。
*   **算力透明度**：缺乏具体的训练资源消耗说明，难以评估其训练成本。

（完）
