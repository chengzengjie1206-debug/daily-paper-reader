---
title: Routing Channel-Patch Dependencies in Time Series Forecasting with Graph Spectral Decomposition
title_zh: 基于图谱分解的时间序列预测中通道-补丁依赖关系路由
authors: "Dongyuan Li, Shun Zheng, Chang Xu, Jiang Bian, Renhe Jiang"
date: 2026-03-14
pdf: "https://arxiv.org/pdf/2603.13702v1"
tags: ["query:ts"]
score: 10.0
evidence: 预测中通道-补丁依赖的高级处理方法
tldr: 针对时间序列预测中通道独立（CI）与通道依赖（CD）策略的局限性，本文提出通用插件 xCPD。该方法基于图谱分解，将多变量信号投影至频域并划分为不同频带，利用通道自适应路由机制动态调节各 patch 的通道交互程度。xCPD 能够灵活平衡 CI 与 CD 策略，实现对平滑趋势和局部波动的精细建模，显著增强了现有预测模型的准确性与泛化性能。
selection_source: fresh_fetch
motivation: 现有的 CI 策略忽略通道间交互，而 CD 策略易引入无关噪声，且缺乏能自适应平衡两者的灵活机制。
method: 提出 xCPD 插件，通过图谱分解将信号转为频域表示，并利用通道自适应路由动态调整不同频率 patch 的通道交互强度。
result: 实验证明 xCPD 可无缝集成至现有模型，在多个基准数据集上一致提升了预测精度和泛化能力。
conclusion: xCPD 通过输入感知的细粒度建模，有效解决了通道依赖性建模的灵活性难题，是提升时间序列预测性能的有效工具。
---

## 摘要
时间序列预测在人工智能领域引起了广泛关注。先前的研究表明，通道独立（CI）策略通过对每个通道进行单独建模来提高预测性能，但它往往面临泛化能力差的问题，并忽略了有意义的通道间交互。相反，通道依赖（CD）策略聚合了所有通道，这可能会引入无关信息并导致过度平滑。尽管最近取得了进展，但现有的方法中很少有能够灵活地根据变化的通道依赖关系自适应地平衡 CI 和 CD 策略。为此，我们提出了一个通用插件 xCPD，它可以从图谱分解的角度自适应地建模通道-补丁（channel-patch）依赖关系。具体而言，xCPD 首先使用共享的图傅里叶基将多元信号投影到频域，并根据其谱能量响应将补丁（patches）分为低频、中频和高频段。随后，xCPD 应用了一种通道自适应路由机制，动态调整每个补丁的通道间交互程度，从而实现对特定频率专家的选择性激活。这有助于对平滑趋势、局部波动和突发转变进行细粒度的输入感知建模。xCPD 可以无缝集成到现有的 CI 和 CD 预测模型之上，在各个基准测试中持续提高准确性和泛化能力。代码可在 https://github.com/Clearloveyuan/xCPD 获取。

## Abstract
Time series forecasting has attracted significant attention in the field of AI. Previous works have revealed that the Channel-Independent (CI) strategy improves forecasting performance by modeling each channel individually, but it often suffers from poor generalization and overlooks meaningful inter-channel interactions. Conversely, Channel-Dependent (CD) strategies aggregate all channels, which may introduce irrelevant information and lead to oversmoothing. Despite recent progress, few existing methods offer the flexibility to adaptively balance CI and CD strategies in response to varying channel dependencies. To address this, we propose a generic plugin xCPD, that can adaptively model the channel-patch dependencies from the perspective of graph spectral decomposition. Specifically, xCPD first projects multivariate signals into the frequency domain using a shared graph Fourier basis, and groups patches into low-, mid-, and high-frequency bands based on their spectral energy responses. xCPD then applies a channel-adaptive routing mechanism that dynamically adjusts the degree of inter-channel interaction for each patch, enabling selective activation of frequency-specific experts. This facilitates fine-grained input-aware modeling of smooth trends, local fluctuations, and abrupt transitions. xCPD can be seamlessly integrated on top of existing CI and CD forecasting models, consistently enhancing both accuracy and generalization across benchmarks. The code is available https://github.com/Clearloveyuan/xCPD.

---

## 论文详细总结（自动生成）

# 论文总结：基于图谱分解的时间序列预测中通道-补丁依赖关系路由 (xCPD)

## 1. 核心问题与整体含义
论文针对多元时间序列预测（MTSF）中**通道依赖建模的灵活性与粒度问题**展开研究。
*   **背景**：现有的建模策略分为通道独立（CI，鲁棒但忽略交互）和通道依赖（CD，捕获全局关系但易引入噪声和过度平滑）。
*   **核心痛点**：现有的折中方案（通道部分依赖，CP）存在两个局限：一是**粒度过粗**（仅在通道级别建模，忽略了局部补丁的动态变化）；二是**频率耦合**（在时域建模，导致趋势、波动和噪声的依赖关系混杂在一起）。
*   **研究动机**：提出一种能够自适应、细粒度且在频域解耦的插件模块，以提升现有预测模型的准确性和泛化能力。

## 2. 方法论
论文提出了 **xCPD** 插件，其核心思想是将“通道-补丁”（Channel-Patch）作为图节点，利用图谱分解在频域进行依赖路由。

### 核心技术细节：
1.  **谱通道-补丁嵌入 (Spectral Channel-Patch Embedding)**：
    *   将预测输出划分为非重叠补丁（Patching），每个补丁视为图中的一个节点。
    *   **共享图傅里叶基**：为了解决不同批次间谱空间不一致的问题，论文提出并证明了学习一个共享的图傅里叶基（Shared Basis），确保所有时间步都在统一的频域内处理。
2.  **谱通道-补丁分组 (Spectral Channel-Patch Grouping)**：
    *   根据节点的**谱能量响应**（Spectral Energy Response），利用可学习的边界将节点自动划分为低频（趋势）、中频（局部波动）和高频（突变/噪声）三组。
3.  **谱通道-补丁路由 (Spectral Channel-Patch Routing)**：
    *   **动态专家混合 (DyMoE)**：设计了三个频率特定的过滤器作为“专家”。
    *   路由网络根据节点的频率特性，动态选择一个或多个专家来构建稀疏的邻接矩阵，实现输入感知的细粒度交互。
4.  **预测与优化**：
    *   采用门控双路径残差修正，将 GNN 路径（捕获通道依赖）与线性路径（保留 CI 特性）结合，对原始模型的预测结果进行精细化修正。

## 3. 实验设计
*   **数据集**：
    *   **长线预测**：9 个主流基准数据集（ETTh1/2, ETTm1/2, Weather, Electricity, Traffic, Exchange, Solar-Energy）。
    *   **短线预测**：M4（单变量多领域）、Stock（高波动股票数据）。
*   **Benchmark 架构**：
    *   选择了四种代表性模型作为 Backbone：DLinear（线性）、PatchTST（Transformer）、TSMixer（MLP）、TimesNet（卷积）。
*   **对比方法**：
    *   包括 LIFT、CCM、PCD、PRReg 等最新的通道依赖优化策略。

## 4. 资源与算力
*   **硬件环境**：单张 **NVIDIA RTX A100 80GB GPU**。
*   **软件框架**：基于 PyTorch 和 Time-Series-Library 实现。
*   **效率表现**：论文明确指出 xCPD 具有线性时间复杂度。在 Traffic 数据集上，相比于其他插件（如 CCM），xCPD 的显存占用更低（约 7GB），训练耗时仅增加约 9%-11%，体现了极高的计算效率。

## 5. 实验数量与充分性
*   **实验规模**：总计进行了 **144 组**长线预测实验设置。
*   **充分性**：
    *   涵盖了从线性到 Transformer 的多种架构。
    *   包含了消融实验（验证共享基、频率分区、节点分组等模块的有效性）。
    *   进行了**零样本（Zero-shot）迁移实验**，验证了模型在未见数据集上的泛化能力。
    *   提供了可视化分析（过滤器响应与时间模式的对应关系）。
*   **客观性**：所有实验均运行 5 次并取平均值，对比基准均在相同框架下重新训练以确保公平。

## 6. 主要结论与发现
1.  **一致性提升**：xCPD 在所有测试的 Backbone 和数据集上均实现了性能提升，尤其在变量多、交互复杂的 Electricity 和 Traffic 数据集上效果最显著。
2.  **频域建模的优越性**：通过谱分解解耦不同频率的依赖，比单纯在时域使用注意力机制更能有效区分有意义的趋势和无意义的噪声。
3.  **零样本迁移能力**：xCPD 学习到的频率过滤器具有通用性，能够显著提升模型在跨域场景下的预测表现。
4.  **轻量化**：作为插件，它在不改变 Backbone 权重的情况下，以极小的计算开销实现了显著的精度增益。

## 7. 优点
*   **模型无关性 (Model-agnostic)**：可无缝集成至任何现有预测模型，具有极强的通用性。
*   **细粒度与自适应**：将建模单元从“通道”细化到“通道-补丁”，并能根据输入动态调整交互策略。
*   **理论支撑**：为共享图傅里叶基和谱能量响应提供了数学证明，增强了方法的严谨性。
*   **可解释性**：通过频域分组，直观展示了模型如何处理平滑趋势与剧烈波动。

## 8. 不足与局限
*   **超参数敏感性**：虽然论文进行了分析，但补丁大小（Patch Size）和路由阈值等参数仍需根据具体任务进行一定程度的微调。
*   **非平稳性挑战**：尽管提出了共享基，但在面对极端分布偏移（Distribution Shift）的非平稳序列时，频域特征的稳定性可能仍面临挑战。
*   **应用范围**：目前主要验证了其在预测任务中的有效性，对于分类、异常检测等其他时间序列任务的适用性尚待进一步探索。

（完）
