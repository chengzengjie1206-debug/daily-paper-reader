---
title: "L2GTX: From Local to Global Time Series Explanations"
title_zh: L2GTX：从局部到全局的时间序列解释
authors: "Ephrem Tibebe Mekonnen, Luca Longo, Lucas Rizzo, Pierpaolo Dondio"
date: 2026-03-13
pdf: "https://arxiv.org/pdf/2603.13065v1"
tags: ["query:ts"]
score: 9.0
evidence: 时间序列分类的可解释机器学习
tldr: 针对深度学习时间序列分类模型缺乏全局解释性的问题，本文提出 L2GTX 框架。该框架采用模型无关的方法，通过聚合局部解释中的时间事件原语（如趋势、极值）及其重要性评分，生成类别级的全局解释。L2GTX 利用实例-聚类重要性矩阵选择代表性实例，在保持解释简洁性的同时，实现了较高的全局忠实度，为理解模型在时间序列上的决策行为提供了新途径。
selection_source: fresh_fetch
motivation: 现有的时间序列解释方法多为局部或模型特定，难以提供模型无关且能捕捉跨实例重复模式的类别级全局解释。
method: 提出 L2GTX 框架，通过聚类局部解释中的时间事件原语并利用重要性矩阵选择代表性实例，从而合成简洁的全局解释。
result: 在六个基准数据集上的实验表明，L2GTX 能够生成紧凑且可解释的全局解释，并保持了稳定的全局忠实度。
conclusion: L2GTX 有效填补了时间序列全局解释领域的空白，证明了通过聚合局部特征原语来构建模型无关全局解释的可行性。
---

## 摘要
深度学习模型在时间序列分类中取得了极高的准确率，但理解其类级别的决策行为仍然具有挑战性。时间序列的解释必须遵循时间依赖性，并识别在不同实例中重复出现的模式。现有方法面临三个局限性：为图像和表格数据开发的模型无关的可解释人工智能（XAI）方法无法轻易扩展到时间序列；时间序列的全局解释合成仍未得到充分探索；且大多数现有的全局方法是模型特定的。我们提出了 L2GTX，这是一个模型无关的框架，通过聚合来自代表性实例集的局部解释来生成类级别的全局解释。L2GTX 从 LOMATCE 生成的实例级解释中提取参数化时间事件原语（如上升或下降趋势以及局部极值）的聚类及其重要性得分。这些聚类在不同实例间进行合并以减少冗余，并使用实例-聚类重要性矩阵来估计全局相关性。在用户定义的实例选择预算下，L2GTX 选择能够最大化覆盖影响力聚类的代表性实例。随后，来自所选实例的事件被聚合成简洁的类级别全局解释。在六个基准时间序列数据集上的实验表明，L2GTX 产生了紧凑且可解释的全局解释，同时保持了稳定的全局忠实度（以平均局部代理保真度衡量）。

## Abstract
Deep learning models achieve high accuracy in time series classification, yet understanding their class-level decision behaviour remains challenging. Explanations for time series must respect temporal dependencies and identify patterns that recur across instances. Existing approaches face three limitations: model-agnostic XAI methods developed for images and tabular data do not readily extend to time series, global explanation synthesis for time series remains underexplored, and most existing global approaches are model-specific. We propose L2GTX, a model-agnostic framework that generates class-wise global explanations by aggregating local explanations from a representative set of instances. L2GTX extracts clusters of parameterised temporal event primitives, such as increasing or decreasing trends and local extrema, together with their importance scores from instance-level explanations produced by LOMATCE. These clusters are merged across instances to reduce redundancy, and an instance-cluster importance matrix is used to estimate global relevance. Under a user-defined instance selection budget, L2GTX selects representative instances that maximise coverage of influential clusters. Events from the selected instances are then aggregated into concise class-wise global explanations. Experiments on six benchmark time series datasets show that L2GTX produces compact and interpretable global explanations while maintaining stable global faithfulness measured as mean local surrogate fidelity.

---

## 论文详细总结（自动生成）

这是一份关于论文《L2GTX: From Local to Global Time Series Explanations》的结构化深入分析报告：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：深度学习模型在时间序列分类（TSC）中表现卓越，但其决策逻辑如同“黑盒”。现有的可解释 AI（XAI）方法主要集中在**局部解释**（解释单个样本），而**全局解释**（理解模型对整个类别的决策逻辑）面临三大挑战：
    1.  传统图像/表格的 XAI 方法难以处理时间序列的非独立观测和时间结构。
    2.  时间序列的全局解释合成研究较少。
    3.  现有的少数全局方法通常是模型特定的（Model-specific），缺乏通用性。
*   **研究动机**：开发一种**模型无关（Model-agnostic）**的方法，能够将局部的时间模式聚合为简洁、具有代表性且符合人类直觉的类别级全局解释。

### 2. 方法论：L2GTX 框架
L2GTX 的核心思想是通过聚合代表性实例的局部特征来构建全局视图，具体分为五个步骤：
1.  **局部归因（Local Attribution）**：利用 LOMATCE 算法为一批样本生成局部解释。该算法将时间序列转化为**参数化事件原语（PEPs）**，如上升/下降趋势（由开始时间、持续时间、平均梯度定义）和局部极值（由时间、数值定义）。
2.  **元聚类（Meta-Clustering）**：使用层次聚类（Agglomerative Clustering）将不同实例中相似的 PEP 聚类合并，以减少冗余并形成全局聚类。
3.  **计算全局重要性**：构建“实例-聚类重要性矩阵”，借鉴 SP-LIME 的思路，计算每个全局聚类在整个数据集中的贡献度。
4.  **子模选择（Submodular Selection）**：在给定的预算 $B$ 下，利用贪心算法选择一组最具代表性的实例，这些实例能够最大化覆盖那些最具影响力的全局聚类。
5.  **全局合成（Global Synthesis）**：提取所选实例中属于关键聚类的事件，计算其属性（如时间、幅值）的均值和标准差，生成最终的类别级可视化解释。

### 3. 实验设计
*   **数据集**：使用了 6 个 UCR 存档的基准数据集，涵盖医疗（ECG200）、动作识别（GunPoint）、食品（Coffee）、传感器（FordA/B）和合成数据（CBF）。
*   **黑盒模型**：测试了两种主流架构：全卷积网络（FCN）和长短期记忆全卷积网络（LSTM-FCN）。
*   **对比与评估指标**：
    *   **Benchmark**：由于同类模型无关的全局时间序列解释方法极少，实验重点在于验证 L2GTX 在不同合并比例（Merge Percentiles $p \in \{25, 50, 75, 95\}$）下的表现。
    *   **核心指标**：**全局忠实度（Global Faithfulness, GF）**，定义为所选代表性实例的平均局部代理模型拟合优度（$R^2$）。

### 4. 资源与算力
*   **算力说明**：论文**未明确列出**具体的 GPU 型号或总训练时长。
*   **训练细节**：提到为了确保模型稳定性，每个黑盒模型在随机划分的数据集上训练了 100 次。在解释生成阶段，对小型数据集每类采样 15 个实例，大型数据集采样 30 个实例。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 6 个不同领域的数据集和 2 种不同的深度学习架构，每组实验运行了 3 个随机种子并报告了 95% 置信区间。
*   **充分性评价**：实验设计较为充分。通过改变合并百分比（$p$）展示了模型在解释压缩（更少的聚类）与忠实度之间的平衡。
*   **客观性**：通过宏平均（Macro-averaged）指标和统计显著性分析，证明了该方法在不同模型和数据集上的稳健性。

### 6. 主要结论与发现
*   **稳定性**：L2GTX 在大幅压缩解释空间（提高 $p$ 值减少聚类数）时，全局忠实度（GF）保持稳定，没有显著下降。
*   **模型无关性**：该方法能有效捕捉不同架构（FCN vs LSTM-FCN）的决策差异，同时识别出它们共同关注的时间区域。
*   **语义一致性**：在 Coffee 和 ECG200 的案例研究中，生成的全局解释（如特定的波峰、波谷或趋势）与领域知识（如光谱特征、心电图形态）相吻合。

### 7. 优点（亮点）
*   **结构化解释**：不同于仅提供“显著性图”的方法，L2GTX 提供的是基于“事件”（趋势、极值）的解释，更符合人类对信号的认知。
*   **代表性选择**：引入子模优化选择实例，确保了全局解释不是简单的平均，而是对关键决策模式的高效覆盖。
*   **高度压缩**：能够将成千上万的时间步简化为少数几个关键的时间事件聚类。

### 8. 不足与局限
*   **计算复杂度**：PEP 事件的聚类过程计算开销较大，尤其是当时间序列较长或邻域采样数较多时。
*   **维度限制**：目前仅针对**单变量**时间序列进行了验证，尚未扩展到多变量（Multivariate）场景，后者涉及跨通道的复杂交互。
*   **评估主观性**：虽然量化了忠实度，但缺乏针对领域专家的真实人类评估（Human-in-the-loop），难以衡量解释在实际决策中的“有用性”。

（完）
