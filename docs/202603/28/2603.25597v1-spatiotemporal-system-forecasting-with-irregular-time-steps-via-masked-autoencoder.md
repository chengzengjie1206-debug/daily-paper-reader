---
title: Spatiotemporal System Forecasting with Irregular Time Steps via Masked Autoencoder
title_zh: 基于掩码自编码器的非规则时间步长时空系统预测
authors: "Kewei Zhu, Yanze Xin, Jinwei Hu, Xiaoyuan Cheng, Yiming Yang, Sibo Cheng"
date: 2026-03-26
pdf: "https://arxiv.org/pdf/2603.25597v1"
tags: ["query:ts"]
score: 9.0
evidence: 用于不规则时间序列预测的掩码自编码器
tldr: 针对高维动力系统中因数据缺失或稀疏观测导致的非均匀时间步长预测难题，本文提出一种物理时空掩码自编码器。该方法结合卷积自编码器提取空间特征，并利用针对不规则时间序列优化的掩码自编码器及注意力机制，在无需数据插值的情况下实现物理序列的一次性重构。实验证明该模型在模拟和真实海洋数据上均表现出优异的预测精度、鲁棒性和计算效率，为复杂时空系统建模提供了新方案。
selection_source: fresh_fetch
motivation: 传统数据驱动算法在处理具有不规则时间步长的高维动力系统时，往往因数据缺失或采样不均而导致预测精度下降。
method: 提出一种物理时空掩码自编码器，通过卷积提取空间特征并结合注意力机制，在不进行数据插补的情况下重构整个物理序列。
result: 在模拟数据集和真实海洋温度数据上的实验表明，该方法在预测精度、对非线性的鲁棒性以及计算效率方面均显著优于传统卷积和循环神经网络。
conclusion: 该模型能够有效捕捉复杂的时空模式且无需领域特定知识，在气候建模、流体力学及环境监测等科学计算领域具有广泛的应用前景。
---

## 摘要
对具有非规则时间步长的高维动力系统进行预测，对当前的数据驱动算法提出了重大挑战。这些非规则性源于数据缺失、稀疏观测或自适应计算技术，从而降低了预测精度。为了解决这些局限性，我们提出了一种新方法：物理-时空掩码自编码器（Physics-Spatiotemporal Masked Autoencoder）。该方法将用于空间特征提取的卷积自编码器与针对非规则时间序列优化的掩码自编码器相结合，利用注意力机制在单次预测过程中重建整个物理序列。该模型在保持系统物理完整性的同时，避免了对数据插补的需求。此处的“物理”是指由底层动力系统生成的高维场，而非强制执行显式的物理约束或偏微分方程（PDE）残差。我们在多个模拟数据集和真实海洋温度数据上评估了该方法。结果表明，与传统的卷积和循环网络方法相比，我们的方法在预测精度、对非线性的鲁棒性以及计算效率方面取得了显著提升。该模型展示了在无需领域特定知识的情况下捕捉复杂时空模式的潜力，在气候建模、流体动力学、海洋预报、环境监测和科学计算等领域具有应用前景。

## Abstract
Predicting high-dimensional dynamical systems with irregular time steps presents significant challenges for current data-driven algorithms. These irregularities arise from missing data, sparse observations, or adaptive computational techniques, reducing prediction accuracy. To address these limitations, we propose a novel method: a Physics-Spatiotemporal Masked Autoencoder. This method integrates convolutional autoencoders for spatial feature extraction with masked autoencoders optimised for irregular time series, leveraging attention mechanisms to reconstruct the entire physical sequence in a single prediction pass. The model avoids the need for data imputation while preserving physical integrity of the system. Here, 'physics' refers to high-dimensional fields generated by underlying dynamical systems, rather than the enforcement of explicit physical constraints or PDE residuals. We evaluate this approach on multiple simulated datasets and real-world ocean temperature data. The results demonstrate that our method achieves significant improvements in prediction accuracy, robustness to nonlinearities, and computational efficiency over traditional convolutional and recurrent network methods. The model shows potential for capturing complex spatiotemporal patterns without requiring domain-specific knowledge, with applications in climate modelling, fluid dynamics, ocean forecasting, environmental monitoring, and scientific computing.

---

## 论文详细总结（自动生成）

这是一份关于论文《Spatiotemporal System Forecasting with Irregular Time Steps via Masked Autoencoder》的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
该论文关注的是**高维动力系统（如流体、气候）在非规则时间步长下的预测难题**。
*   **研究动机**：在实际科学模拟或观测中，由于传感器故障、稀疏采样或数值求解器的自适应步长，数据往往呈现非均匀的时间分布。
*   **传统局限**：现有的循环神经网络（RNN）或卷积长短期记忆网络（ConvLSTM）通常假设时间步长是固定的。处理非规则数据时，往往需要预处理（如插值或数据同化），这会引入人为偏差、增加计算开销并掩盖真实的动力学特征。
*   **核心目标**：开发一种能够直接从非规则采样的高维数据中学习，且无需显式插值预处理的端到端预测模型。

### 2. 论文提出的方法论：P-STMAE
论文提出了一种**物理时空掩码自编码器（Physics-Spatiotemporal Masked Autoencoder, P-STMAE）**，其核心架构分为两个阶段：

*   **空间压缩（CAE阶段）**：使用卷积自编码器（Convolutional Autoencoder）将高维物理场 $x_t$ 压缩到低维潜空间（Latent Space） $z_t$。这极大地降低了后续 Transformer 处理高维数据的计算复杂度。
*   **时间建模（Masked Transformer阶段）**：
    *   **占位符策略**：将缺失的时间步和未来预测步视为“掩码（Mask）”，使用占位符变量填充。
    *   **自注意力机制**：利用 Transformer 的自注意力机制，模型可以同时关注序列中所有已观测到的状态，捕捉长程依赖关系。
    *   **非自回归预测**：与 RNN 的逐步迭代不同，该模型通过单次前向传播即可重构/预测整个序列，避免了误差累积。
*   **损失函数**：采用联合损失函数 $L = \text{MSE}_{\text{physical}} + \lambda \cdot \text{MSE}_{\text{latent}}$，同时优化物理空间和潜空间的重建精度。

### 3. 实验设计
论文在三个具有代表性的数据集上进行了验证：
*   **数据集**：
    1.  **浅水方程（Shallow Water）**：模拟非线性流体动力学，具有混沌特性。
    2.  **扩散反应方程（Diffusion-Reaction）**：来自 PDEBench 评估基准，测试耦合变量的模式形成。
    3.  **NOAA 海面温度（SST）**：真实的全球气候观测数据，包含噪声和复杂的长程依赖。
*   **基准模型（Benchmarks）**：
    *   **ConvRAE**：结合卷积和 LSTM 的潜空间模型（代表潜空间 RNN 方法）。
    *   **ConvLSTM**：在全物理空间进行时空建模（代表全空间 RNN 方法）。
*   **评估指标**：均方误差（MSE）、结构相似性指数（SSIM）、峰值信噪比（PSNR）。

### 4. 资源与算力
*   **算力说明**：论文**未明确列出**具体的 GPU 型号（如 A100 或 V100）及具体的训练总时长。
*   **实现细节**：提到了使用 RAdam（用于 P-STMAE）和 Adam（用于基准模型）优化器，Batch Size 为 32。模型推理基于 GPU 前向传播，强调其比传统 PDE 求解器更具计算效率。

### 5. 实验数量与充分性
*   **实验规模**：
    *   针对三个不同性质的数据集进行了全面对比。
    *   **消融实验**：对损失函数权重 $\lambda$ 进行了敏感性分析。
    *   **鲁棒性测试**：测试了不同的**缺失率（Missing Ratio）**（从 1 到 6 个步长缺失）和**采样扩张率（Sampling Dilation）**。
*   **充分性评价**：实验设计较为充分，涵盖了从理想模拟到复杂现实场景的跨度。通过引入不同程度的非规则性（扩张和缺失），客观地展示了 Transformer 架构在处理非均匀序列时优于 RNN 的特性。

### 6. 主要结论与发现
*   **精度领先**：P-STMAE 在所有数据集上的 MSE 均优于 ConvRAE 和 ConvLSTM，尤其在处理真实 SST 数据时表现出极强的泛化能力。
*   **非线性鲁棒性**：在采样间隔增大（非线性增强）的情况下，P-STMAE 的性能保持稳定，而 ConvLSTM 的误差迅速飙升。
*   **处理非规则性的优势**：掩码重建机制使得模型在数据缺失率高达 50% 或以上时，仍能保持较高的物理场重构精度。
*   **效率提升**：相比于需要逐步迭代的 RNN，P-STMAE 的单次并行推理显著降低了预测延迟。

### 7. 优点与亮点
*   **无需插值**：直接处理非规则时间步，解决了数据预处理带来的偏差问题。
*   **潜空间建模**：巧妙结合 CAE 和 Transformer，既保留了空间局部特征，又解决了 Transformer 处理超高维图像时的内存瓶颈。
*   **物理一致性**：虽然没有显式加入 PDE 约束，但通过潜空间动力学学习，模型在视觉和数值上均能很好地保持物理场的结构特征。

### 8. 不足与局限
*   **计算复杂度**：Transformer 的自注意力机制具有平方级复杂度，虽然在潜空间操作，但在处理极长序列时仍可能面临扩展性挑战。
*   **CAE 瓶颈**：模型的最终精度受限于卷积自编码器的重构能力，如果 CAE 压缩损失过大，后续 Transformer 无法补偿。
*   **超参数依赖**：虽然消融实验显示 $\lambda$ 在一定范围内稳定，但在不同物理系统下，空间压缩率和 Transformer 深度的配置仍需手动调优。
*   **缺乏显式物理约束**：模型本质上是纯数据驱动的，在极度稀疏的数据下，可能无法保证严格遵守质量守恒或能量守恒等物理定律。

（完）
