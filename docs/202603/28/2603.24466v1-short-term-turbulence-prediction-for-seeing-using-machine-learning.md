---
title: Short-Term Turbulence Prediction for Seeing Using Machine Learning
title_zh: 基于机器学习的视宁度短期湍流预测
authors: "Mary Joe Medlej, Rahul Srinivasan, Simon Prunet, Aziz Ziad, Christophe Giordano"
date: 2026-03-25
pdf: "https://arxiv.org/pdf/2603.24466v1"
tags: ["query:ts"]
score: 8.0
evidence: 使用统计和深度学习进行短期湍流预测
tldr: 本研究针对大气湍流对地面光学系统造成的干扰，利用机器学习模型预测未来两小时内的视宁度参数。研究对比了高斯过程、RNN、LSTM等模型，并提出了一种新型的概率深度学习方法FloTS。实验表明，FloTS在仅依赖历史视宁度数据的情况下，不仅实现了高精度的短期预测，还能有效量化预测的不确定性，为自适应光学系统的动态决策提供了重要支持。
selection_source: carryover_cache
motivation: 针对自适应光学系统在应对快速变化的大气湍流时存在的滞后性，探索能够提前预测视宁度的方案。
method: 比较了统计学模型（高斯过程）与深度学习模型（RNN、LSTM），并引入了基于正规化流的FloTS模型进行概率预测。
result: FloTS模型在预测精度和不确定性校准方面表现最优，能够提供比确定性模型更稳健的预测结果。
conclusion: 概率深度学习模型在短期大气湍流预测中具有显著优势，有助于提升地面天文观测和光通信系统的性能。
---

## 摘要
由大气折射率波动驱动的光学湍流对地面光学系统构成了重大挑战，因为它会扭曲光的传播。这种性能退化同时影响了天文观测和自由空间光通信。虽然自适应光学系统可以实时校正湍流效应，但其反应性本质限制了它们在快速变化条件下的有效性，这凸显了对预测性解决方案的需求。在本研究中，我们利用机器学习模型预测提前两小时的大气视宁度参数，从而解决短期湍流预报问题。我们比较了统计学和深度学习方法，特别关注概率模型，这些模型不仅能产生准确的预测，还能量化预测的不确定性，这对于动态环境中的稳健决策至关重要。我们的评估包括用于统计建模的高斯过程 (GP)、作为确定性基准的循环神经网络 (RNN) 和长短期记忆网络 (LSTM)，以及我们作为灵活概率深度学习方法新实现的用于时间序列的归一化流 (FloTS)。所有模型均仅在历史视宁度数据上进行训练，以便进行公平的性能比较。我们表明，FloTS 在预测准确性和良好校准的不确定性之间实现了最佳的整体平衡。

## Abstract
Optical turbulence, driven by fluctuations of the atmospheric refractive index, poses a significant challenge to ground-based optical systems, as it distorts the propagation of light. This degradation affects both astronomical observations and free-space optical communications. While adaptive optics systems correct turbulence effects in real-time, their reactive nature limits their effectiveness under rapidly changing conditions, underscoring the need for predictive solutions. In this study, we address the problem of short-term turbulence forecasting by leveraging machine learning models to predict the atmospheric seeing parameter up to two hours in advance. We compare statistical and deep learning approaches, with a particular focus on probabilistic models that not only produce accurate forecasts but also quantify predictive uncertainty, crucial for robust decision-making in dynamic environments. Our evaluation includes Gaussian processes (GP) for statistical modeling, recurrent neural networks (RNNs) and long short-term memory networks (LSTMs) as deterministic baselines, and our novel implementation of a normalizing flow for time series (FloTS) as a flexible probabilistic deep learning method. All models are trained exclusively on historical seeing data, allowing for a fair performance comparison. We show that FloTS achieves the best overall balance between predictive accuracy and well-calibrated uncertainty.

---

## 论文详细总结（自动生成）

### 论文总结：基于机器学习的视宁度短期湍流预测

#### 1. 核心问题与整体含义（研究动机和背景）
地面光学系统（如天文望远镜和自由空间光通信 FSO）受大气折射率波动产生的大气湍流（即“视宁度”参数 $\epsilon$）影响严重。虽然自适应光学（AO）系统可以实时校正，但其本质是“反应式”的，存在滞后性。
*   **研究动机**：为了实现从“反应式”到“预测式”的转变，需要对未来短时间内（如 2 小时内）的视宁度进行准确预报，以优化望远镜调度、资源分配及光通信系统的参数调整。
*   **核心问题**：如何在保证预测精度的同时，量化预测的不确定性，从而为动态环境下的决策提供稳健支持。

#### 2. 方法论：核心思想与技术细节
论文对比了统计学模型和深度学习模型，并重点引入了概率预测框架：
*   **确定性深度学习模型**：
    *   **RNN & LSTM**：采用编码器-解码器（Encoder-Decoder）架构。LSTM 通过门控机制解决 RNN 的梯度消失问题，用于捕捉时间序列中的长程依赖。
*   **概率统计模型**：
    *   **高斯过程 (GP)**：利用指数核函数建模时间相关性，能够自然地提供预测均值和置信区间，但受限于高斯分布假设。
*   **概率深度学习模型 (FloTS)**：
    *   **核心思想**：基于**归一化流（Normalizing Flow）**技术，通过一系列可逆变换将简单的基础分布（如高斯分布）映射为复杂的非高斯目标分布。
    *   **技术细节**：使用掩码自回归流（MAF）实现，并采用 LSTM 作为上下文编码器（Context Encoder）来处理历史观测数据 $\epsilon_-$，从而生成条件概率分布 $p(\epsilon_+ | \epsilon_-)$。
*   **概率校准**：引入了基于温度缩放（Temperature Scaling）的校准技术，通过调整预测分布的“温度”来匹配经验覆盖率，确保不确定性估计的可靠性。

#### 3. 实验设计
*   **数据集**：使用夏威夷莫纳克亚山（Maunakea）气象中心提供的 DIMM 视宁度数据，跨度从 2009 年 9 月至 2024 年 11 月。
*   **预处理**：将原始 1 分钟间隔的数据重采样并插值为 10 分钟间隔的均匀序列。
*   **实验设置**：
    *   **输入/输出**：使用过去 2 小时的历史数据预测未来 2 小时的视宁度。
    *   **Benchmark**：将 RNN、LSTM、GP 作为基准模型。
    *   **评估指标**：均方根误差 (RMSE)、皮尔逊相关系数 ($r$) 以及概率校准的 PP 图（Probability-Probability Plot）。

#### 4. 资源与算力
*   **算力支持**：文中提到计算工作部分在 **Leonardo 超级计算机**（CINECA-INFN 协议）上完成。
*   **具体细节**：论文**未明确说明**具体的 GPU 型号、数量以及每个模型的具体训练时长。但考虑到使用了 7 万余组序列对进行深度学习训练，且涉及归一化流，通常需要主流的数据中心级 GPU（如 A100 或 H100）支持。

#### 5. 实验数量与充分性
*   **数据规模**：共生成 71,906 组输入-输出对，其中 81.5% 用于训练，8.5% 用于验证，10% 用于测试。
*   **消融/对比实验**：
    *   测试了不同长度的历史窗口（2、3、4、5、6 小时）对预测效果的影响，发现 2 小时窗口表现最佳。
    *   对比了四种不同类型的模型，并对概率模型进行了专门的校准实验。
*   **充分性评价**：实验设计较为客观、公平。所有模型仅使用历史视宁度数据作为输入，排除了其他气象变量的干扰，从而纯粹地比较了模型对时间序列动态特性的建模能力。

#### 6. 主要结论与发现
*   **预测精度**：LSTM 在点预测（RMSE）方面表现最优，FloTS 紧随其后。
*   **不确定性量化**：FloTS 在捕捉非高斯分布特征和不确定性校准方面显著优于 GP。GP 的高斯假设在处理视宁度数据时存在偏差。
*   **时间依赖性**：研究发现 2 小时的历史数据已足以捕捉短期趋势，更长的历史窗口并未显著提升预测性能。
*   **模型趋势**：随着预测时间的延长（从 10 分钟到 120 分钟），所有模型的 RMSE 均上升，相关性下降，符合大气湍流的随机演化规律。

#### 7. 优点
*   **概率预测视角**：不仅给出预测值，还给出了置信度，这对天文观测决策（如是否切换观测目标）具有极高的实用价值。
*   **FloTS 模型创新**：将归一化流应用于视宁度预测，能够处理复杂的非高斯概率密度。
*   **长期真实数据验证**：使用了长达 15 年的真实观测数据，保证了结论的统计显著性。

#### 8. 不足与局限
*   **特征单一**：目前仅使用了视宁度单一变量。未来若能加入风速、风向、温度、湿度等气象特征，预测精度可能进一步提升。
*   **GP 的局限性**：所选用的 GP 核函数较为简单，未能充分利用 GP 处理多尺度时间结构的能力。
*   **应用限制**：模型目前针对特定站点（Maunakea）训练，其泛化到其他天文台站的能力尚需验证。
*   **算力细节缺失**：缺乏具体的训练效率和推理延迟数据，这对于实时 AO 系统的集成至关重要。

（完）
