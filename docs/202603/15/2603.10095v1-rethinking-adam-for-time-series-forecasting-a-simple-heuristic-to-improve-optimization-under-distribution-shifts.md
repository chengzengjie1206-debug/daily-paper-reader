---
title: "Rethinking Adam for Time Series Forecasting: A Simple Heuristic to Improve Optimization under Distribution Shifts"
title_zh: 重新审视用于时间序列预测的 Adam：一种改进分布偏移下优化的简单启发式方法
authors: "Yuze Dong, Jinsong Wu"
date: 2026-03-10
pdf: "https://arxiv.org/pdf/2603.10095v1"
tags: ["query:ts"]
score: 9.0
evidence: 改进分布偏移下的时序预测优化方法
tldr: 针对时间序列预测中的非平稳性和分布偏移问题，本文重新审视了 Adam 优化器，发现其二阶偏差修正限制了对动态损失景观的响应能力。为此，作者提出了 TS_Adam，通过移除二阶偏差修正来增强优化器的适应性。该方法简单高效，无需额外超参数，在多个预测任务中显著提升了模型性能，尤其在 ETT 数据集上表现优异，为非平稳数据优化提供了新思路。
selection_source: fresh_fetch
motivation: Adam 优化器的二阶偏差修正机制在处理非平稳时间序列的分布偏移时，会限制模型对动态损失景观变化的响应速度。
method: 提出 TS_Adam 优化器，通过在学习率计算中移除二阶偏差修正项，以轻量级的方式提升模型对分布漂移的适应能力。
result: "实验表明 TS_Adam 在长短期预测任务中均有提升，在 ETT 数据集上使 MICN 模型的 MSE 和 MAE 分别平均降低了 12.8% 和 5.7%。"
conclusion: TS_Adam 是一种简单且通用的优化策略，能有效应对现实场景中非平稳数据的挑战，且易于集成到现有模型中。
---

## 摘要
时间序列预测经常面临非平稳性的挑战，特别是分布漂移，即数据分布随时间演变。这种动态行为会削弱自适应优化器（如 Adam）的有效性，因为这些优化器通常是为平稳目标设计的。在本文中，我们在非平稳预测的背景下重新审视了 Adam，并发现其二阶偏差修正限制了对不断变化的损失景观（loss landscapes）的响应能力。为了解决这个问题，我们提出了 TS_Adam，这是一种轻量级变体，它从学习率计算中移除了二阶修正。这种简单的修改提高了对分布漂移的适应性，同时保留了优化器的核心结构，且不需要额外的超参数。TS_Adam 可以轻松集成到现有模型中，并在长期和短期预测任务中持续提高性能。在使用 MICN 模型的 ETT 数据集上，与 Adam 相比，它在 MSE 上平均降低了 12.8%，在 MAE 上平均降低了 5.7%。这些结果强调了 TS_Adam 作为一种有效的优化策略，在涉及非平稳数据的现实预测场景中的实用性和通用性。代码可在以下网址获取：https://github.com/DD-459-1/TS_Adam。

## Abstract
Time-series forecasting often faces challenges from non-stationarity, particularly distributional drift, where the data distribution evolves over time. This dynamic behavior can undermine the effectiveness of adaptive optimizers, such as Adam, which are typically designed for stationary objectives. In this paper, we revisit Adam in the context of non-stationary forecasting and identify that its second-order bias correction limits responsiveness to shifting loss landscapes. To address this, we propose TS_Adam, a lightweight variant that removes the second-order correction from the learning rate computation. This simple modification improves adaptability to distributional drift while preserving the optimizer core structure and requiring no additional hyperparameters. TS_Adam integrates easily into existing models and consistently improves performance across long- and short-term forecasting tasks. On the ETT datasets with the MICN model, it achieves an average reduction of 12.8% in MSE and 5.7% in MAE compared to Adam. These results underscore the practicality and versatility of TS_Adam as an effective optimization strategy for real-world forecasting scenarios involving non-stationary data. Code is available at: https://github.com/DD-459-1/TS_Adam.

---

## 论文详细总结（自动生成）

这篇论文对深度学习中最常用的 Adam 优化器在时间序列预测任务中的表现进行了重新审视，并提出了一种极其简单但有效的改进方案。以下是详细的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究背景**：时间序列预测（TSF）面临的核心挑战是非平稳性（Non-stationarity），即数据的统计属性（如均值、方差）随时间发生漂移。
*   **核心问题**：现有的研究大多集中在模型架构（如 Transformer、CNN 变体）或损失函数的设计上，而忽略了优化器的作用。
*   **研究动机**：作者发现 Adam 优化器是为平稳目标设计的。其“二阶偏差修正”机制（用于抵消训练初期二阶动量的偏差）在非平稳环境下会限制学习率的响应速度，导致模型难以快速追踪不断变化的损失景观（Loss Landscape），从而影响预测精度。

### 2. 论文提出的方法论：TS_Adam
*   **核心思想**：通过移除 Adam 优化器中的**二阶偏差修正项**，提高优化器对分布漂移（Distributional Drift）的适应能力。
*   **技术细节**：
    *   **标准 Adam**：计算二阶动量 $\hat{v}_t = v_t / (1 - \beta_2^t)$。由于 $\beta_2$ 通常很大（如 0.999），在训练的前几百甚至上千步中，这个修正项会显著改变有效步长。
    *   **TS_Adam**：直接令 $\hat{v}_t = v_t$。
*   **理论依据**：论文利用**动态遗憾边界（Dynamic Regret Bound）**证明，在非平稳环境下，优化器需要在“抑制噪声”和“追踪漂移”之间取得平衡。移除二阶修正可以使有效学习率在训练中早期保持在较高水平，从而更有效地减少由分布漂移引起的遗憾值。
*   **算法流程**：除了在更新步中跳过二阶偏差修正的除法运算外，其余步骤（一阶动量计算、偏差修正等）与标准 Adam 完全一致。

### 3. 实验设计
*   **数据集**：
    *   **长期预测**：ETT 系列（ETTh1, ETTh2, ETTm1, ETTm2）、ECL（电力消耗）、Weather（天气）。
    *   **短期预测**：M4 竞赛数据集（包含 10 万个不同频率的序列）。
*   **Benchmark 模型**：选择了三种代表性架构：MICN、PatchTST 和 SegRNN。
*   **对比方法**：对比了 Adam、AdamW、SGD、Yogi 和 Lookahead 等主流优化器。
*   **评估指标**：MSE、MAE、SMAPE、MASE 和 OWA。

### 4. 资源与算力
*   **硬件环境**：实验在一台单卡 **NVIDIA RTX 3090 GPU** 上完成。
*   **计算效率**：TS_Adam 相比 Adam 减少了每一步中的 $n$ 次除法运算（$n$ 为参数量）。理论计算表明，TS_Adam 在每步迭代中可减少约 **8.3% (1/12)** 的浮点运算量（FLOPs），且内存占用与 Adam 完全相同。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 6 个长期预测数据集（每个数据集 4 种预测长度）和完整的 M4 短期预测任务，每组实验均使用 3 个不同的随机种子运行并取均值。
*   **消融与鲁棒性实验**：
    *   测试了对超参数（学习率 $\alpha$、$\beta_1$）的敏感性。
    *   测试了不同 Batch Size 的影响。
    *   引入了高斯噪声和极端离群点，验证了在数据污染下的鲁棒性。
    *   测试了不同学习率调度策略（Cosine, Exponential）和权重衰减（Weight Decay）的影响。
*   **充分性评价**：实验设计非常全面，不仅涵盖了多种模型和任务，还通过统计显著性检验（p-value 和 Bonferroni 修正）证明了改进的可靠性，实验结果具有高度的说服力。

### 6. 主要结论与发现
*   **性能提升显著**：在 ETT 数据集上配合 MICN 模型，TS_Adam 相比 Adam 平均降低了 **12.8% 的 MSE** 和 **5.7% 的 MAE**。
*   **短期预测同样有效**：在 M4 数据集上，TS_Adam 在 SMAPE、MASE 和 OWA 指标上均优于所有基准方法。
*   **季节性相关性**：TS_Adam 的提升程度与数据的季节性强度正相关。季节性越强（分布漂移越快），TS_Adam 的优势越明显。
*   **收敛速度**：实验观察到 TS_Adam 通常比 Adam 收敛更快，且最终达到的测试损失更低。

### 7. 优点
*   **极简且高效**：无需引入任何新的超参数，仅通过删除一行代码逻辑即可实现性能提升。
*   **通用性强**：作为一种“即插即用”的优化策略，它可以轻松集成到任何现有的深度学习预测模型中。
*   **理论与实践结合**：通过动态遗憾理论解释了 Adam 在非平稳任务中的局限性，并用实验验证了理论推导。
*   **计算友好**：不仅不增加负担，反而略微降低了计算开销。

### 8. 不足与局限
*   **趋势主导数据的局限**：在趋势（Trend）占绝对主导、季节性极弱的数据集上，TS_Adam 的提升效果相对有限。
*   **初期波动风险**：移除偏差修正可能导致训练最开始的几步出现轻微震荡（尽管实验表明这不影响最终收敛）。
*   **应用范围限制**：论文主要针对时间序列预测任务，该启发式方法是否适用于其他非平稳任务（如在线学习或某些强化学习场景）仍需进一步验证。

（完）
