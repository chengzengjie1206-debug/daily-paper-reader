---
title: Hybrid Quantum Neural Network for Multivariate Clinical Time Series Forecasting
title_zh: 用于多变量临床时间序列预测的混合量子神经网络
authors: "Irene Iele, Floriano Caprio, Paolo Soda, Matteo Tortora"
date: 2026-03-09
pdf: "https://arxiv.org/pdf/2603.08072v1"
tags: ["query:ts"]
score: 9.0
evidence: 生理信号的多变量多步预测
tldr: 本研究针对临床生理信号（如心率、血氧等）的多变量多步预测，提出了一种混合量子-经典神经网络架构。该模型结合了GRU编码器与变分量子电路（VQC），利用量子层作为可学习的非线性特征混合器来建模变量间的复杂交互。在BIDMC数据集上的实验表明，该方法在保持高预测精度的同时，对噪声和缺失数据表现出更强的鲁棒性，为小样本临床环境下的生理时间序列预测提供了新的归纳偏置。
selection_source: fresh_fetch
motivation: 旨在通过预测生理信号的关键变化，支持临床中的主动监测和及时干预。
method: 提出一种将GRU编码器与变分量子电路（VQC）相结合的混合架构，利用量子层进行非线性特征建模。
result: 在BIDMC数据集上实现了与深度学习基准相当的精度，并在抗噪性和处理缺失输入方面表现更优。
conclusion: 混合量子层能为小规模临床数据集的生理时间序列预测提供有效的归纳偏置和鲁棒性增强。
---

## 摘要
生理信号预测可以通过预判患者状态的关键变化，支持主动监测和及时的临床干预。在这项工作中，我们通过联合预测 15、30 和 60 秒预测范围内的心率、血氧饱和度、脉搏率和呼吸率，解决了生理时间序列的多变量多步预测问题。我们提出了一种混合量子-经典架构，将变分量子电路 (VQC) 集成在循环神经网络骨干中。GRU 编码器将历史观测窗口总结为潜在表示，随后将其投影为用于参数化 VQC 的量子角度。量子层充当可学习的非线性特征混合器，在最终预测阶段之前对跨变量交互进行建模。我们在 BIDMC PPG 和呼吸数据集上，采用留一患者法 (Leave-One-Patient-Out) 协议对所提方法进行了评估。结果显示，与经典和深度学习基准模型相比，该方法具有竞争力的准确性，且对噪声和缺失输入具有更强的鲁棒性。这些发现表明，混合量子层可以为小样本临床环境下的生理时间序列预测提供有用的归纳偏置。

## Abstract
Forecasting physiological signals can support proactive monitoring and timely clinical intervention by anticipating critical changes in patient status. In this work, we address multivariate multi-horizon forecasting of physiological time series by jointly predicting heart rate, oxygen saturation, pulse rate, and respiratory rate at forecasting horizons of 15, 30, and 60 seconds. We propose a hybrid quantum-classical architecture that integrates a Variational Quantum Circuit (VQC) within a recurrent neural backbone. A GRU encoder summarizes the historical observation window into a latent representation, which is then projected into quantum angles used to parameterize the VQC. The quantum layer acts as a learnable non-linear feature mixer, modeling cross-variable interactions before the final prediction stage. We evaluate the proposed approach on the BIDMC PPG and Respiration dataset under a Leave-One-Patient-Out protocol. The results show competitive accuracy compared with classical and deep learning baselines, together with greater robustness to noise and missing inputs. These findings suggest that hybrid quantum layers can provide useful inductive biases for physiological time series forecasting in small-cohort clinical settings.

---

## 论文详细总结（自动生成）

这篇论文提出了一种结合经典循环神经网络与变分量子电路（VQC）的混合架构，旨在解决临床生理信号预测中的多变量、多步长预测难题。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究背景
*   **研究动机**：重症监护室（ICU）中的生理信号（如心率、血氧等）具有高度动态性和个体差异性。准确预测这些信号的未来走势（如提前15-60秒）对于临床预警和主动干预至关重要。
*   **核心挑战**：生理数据常伴有传感器伪影、数据缺失、非平稳动力学特征，且临床数据集通常规模较小（小样本学习），导致传统深度学习模型难以泛化。
*   **研究目标**：探索量子机器学习（QML）组件作为一种“结构化归纳偏置”，是否能增强模型在处理多变量交互时的表达能力和鲁棒性。

### 2. 方法论：混合量子-经典架构
该模型（GRU+VQC）由三个主要阶段组成：
*   **经典编码阶段**：使用门控循环单元（GRU）作为骨干网络，将历史观测窗口（4分钟，240个时间点）映射为一个潜在表示向量 $z$。
*   **量子特征混合阶段（核心）**：
    *   **角度投影**：通过可学习的线性变换将 $z$ 转换为量子旋转角 $\theta$。
    *   **变分量子电路 (VQC)**：使用 6 个量子比特。首先通过 $R_y$ 门进行角度编码，随后进入深度为 3 的变分层。每一层包含单比特旋转门（$R_z, R_y, R_z$）和基于环形拓扑的 CNOT 受控非门（用于引入量子纠缠）。
    *   **量子读取**：通过测量 Pauli-Z 算符的期望值，输出量子特征向量 $q$。
*   **混合预测阶段**：将经典特征 $z$ 与量子特征 $q$ 拼接，通过线性预测头同时输出四个生理指标（HR, SpO2, Pulse, RR）在三个时间跨度（15s, 30s, 60s）的预测值。

### 3. 实验设计
*   **数据集**：使用 **BIDMC PPG and Respiration Dataset**，包含 53 名 ICU 患者的生理记录。
*   **评估协议**：采用严格的**留一患者法 (Leave-One-Patient-Out, LOPO)**，共进行 53 折交叉验证，确保模型评估的独立性和临床实用性。
*   **对比基准 (Benchmarks)**：
    *   传统机器学习：ExtraTrees（极度随机树）。
    *   卷积架构：1D CNN、TCN（时间卷积网络）。
    *   循环架构：LSTM。
    *   Transformer 变体：PatchTST。
*   **评价指标**：平均绝对误差 (MAE)、均方根误差 (RMSE) 以及患者排名得分。

### 4. 资源与算力
*   **软件框架**：使用 **PennyLane** 库进行量子电路模拟。
*   **算力细节**：论文**未明确说明**具体的 GPU 型号、数量或训练总时长。由于 VQC 是在“解析模式”（无噪声模拟）下运行的，其计算开销主要集中在经典计算机对量子态演化的模拟上。

### 5. 实验数量与充分性
*   **实验规模**：进行了 53 折 LOPO 验证，这在小样本临床研究中是非常充分且严谨的。
*   **消融与鲁棒性实验**：
    *   **抗噪性测试**：在测试集加入不同强度的加斯噪声（$\sigma \in \{0, 0.01, 0.05\}$）。
    *   **缺失值测试**：模拟不同比例的数据缺失（0% 到 30%）。
*   **客观性**：通过“AvgWins”（获胜率）和患者维度的排名分析，多角度证明了模型在不同指标和不同患者身上的稳定性，实验设计较为公平、客观。

### 6. 主要结论与发现
*   **性能领先**：GRU+VQC 在 91.6% 的任务-步长组合中获得了最佳表现，整体 MAE 和 RMSE 均低于所有经典基准。
*   **鲁棒性优势**：在输入数据存在噪声或缺失时，混合量子模型的性能下降比纯经典模型（如 GRU）更缓慢，表现出更强的正则化效应。
*   **特征混合能力**：量子层作为非线性特征混合器，能有效捕捉生理信号间的跨变量耦合关系。

### 7. 优点
*   **归纳偏置创新**：不盲目追求“量子优越性”，而是将量子电路视为一种特殊的非线性变换层，为小样本学习提供了有效的归纳偏置。
*   **评估严谨**：采用 LOPO 协议避免了受试者内的数据泄露，结果更具临床参考价值。
*   **多任务处理**：一个模型同时处理多变量、多步长的预测，体现了良好的系统集成性。

### 8. 不足与局限
*   **模拟环境限制**：实验是在**无噪声的量子模拟器**上完成的，未考虑真实量子硬件（NISQ 时代）的门误差、退相干和读出噪声。
*   **样本量较小**：仅在 53 名患者的单一数据集上验证，其在更大规模、更多样化临床环境下的泛化能力有待观察。
*   **可解释性缺失**：虽然量子层提升了性能，但量子特征 $q$ 具体代表了何种生理学意义，文中缺乏深入的解释。

（完）
