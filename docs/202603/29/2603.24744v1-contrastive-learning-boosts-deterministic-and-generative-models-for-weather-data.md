---
title: Contrastive Learning Boosts Deterministic and Generative Models for Weather Data
title_zh: 对比学习提升气象数据的确定性与生成式模型
authors: Nathan Bailey
date: 2026-03-25
pdf: "https://arxiv.org/pdf/2603.24744v1"
tags: ["query:ts"]
score: 8.0
evidence: 用于天气预测和极端天气检测的对比学习
tldr: 针对气象数据高维、多模态及稀疏性挑战，本文提出SPARTA框架，利用对比学习将稀疏样本与完整样本对齐，生成低维时空嵌入。通过引入时间感知采样、循环一致性损失及图神经网络融合物理知识，该方法在处理真实世界稀疏数据时表现优于传统自编码器，显著提升了下游气象预测任务的性能。
selection_source: fresh_fetch
motivation: 旨在解决现有气象数据压缩方法在处理真实世界稀疏数据时鲁棒性不足，以及对比学习在地球科学领域应用研究不充分的问题。
method: 提出SPARTA框架，通过对比损失对齐稀疏与完整样本，并结合时间感知采样、循环一致性损失及图神经网络融合物理先验知识。
result: 实验结果表明，对比学习生成的低维嵌入在处理稀疏数据时比自编码器更具优势，有效增强了下游预测任务的准确性。
conclusion: 对比学习是处理稀疏地球科学数据的一种高效压缩手段，能够显著提升确定性和生成式气象模型的性能。
---

## 摘要
气象数据包含多个变量，由于其高维性和多模态特性，带来了巨大的挑战。创建低维嵌入需要将这些数据压缩到一个紧凑的共享潜空间中。这种压缩对于提高下游任务（如预报或极端天气检测）的效率和性能至关重要。自监督学习，特别是对比学习，提供了一种从无标签数据中生成低维、鲁棒嵌入的方法，从而在标签数据稀缺时支持下游任务。尽管对比学习在气象数据（特别是 ERA5 数据集）中已有初步探索，但现有文献尚未广泛研究其相对于其他压缩方法（尤其是自动编码器）的优势。此外，目前关于对比学习的工作并未研究这些模型如何整合稀疏数据，而稀疏数据在现实世界的数据收集过程中更为常见。探索并理解对比学习如何有助于为稀疏气象数据创建更鲁棒的嵌入，从而提高下游任务的性能，这一点至关重要。我们的工作在 ERA5 数据集上广泛探索了对比学习，通过对比损失项将稀疏样本与完整样本对齐，从而创建了稀疏数据增强的对比时空嵌入（SPARTA）。我们引入了一种时间感知批采样策略和循环一致性损失，以改善潜空间的结构。此外，我们提出了一种新型图神经网络融合技术，以注入特定领域的物理知识。最终，我们的结果表明，对比学习是稀疏地球科学数据的一种可行且有利的压缩方法，从而增强了下游任务的性能。

## Abstract
Weather data, comprising multiple variables, poses significant challenges due to its high dimensionality and multimodal nature. Creating low-dimensional embeddings requires compressing this data into a compact, shared latent space. This compression is required to improve the efficiency and performance of downstream tasks, such as forecasting or extreme-weather detection.   Self-supervised learning, particularly contrastive learning, offers a way to generate low-dimensional, robust embeddings from unlabelled data, enabling downstream tasks when labelled data is scarce. Despite initial exploration of contrastive learning in weather data, particularly with the ERA5 dataset, the current literature does not extensively examine its benefits relative to alternative compression methods, notably autoencoders. Moreover, current work on contrastive learning does not investigate how these models can incorporate sparse data, which is more common in real-world data collection. It is critical to explore and understand how contrastive learning contributes to creating more robust embeddings for sparse weather data, thereby improving performance on downstream tasks.   Our work extensively explores contrastive learning on the ERA5 dataset, aligning sparse samples with complete ones via a contrastive loss term to create SPARse-data augmented conTRAstive spatiotemporal embeddings (SPARTA). We introduce a temporally aware batch sampling strategy and a cycle-consistency loss to improve the structure of the latent space. Furthermore, we propose a novel graph neural network fusion technique to inject domain-specific physical knowledge. Ultimately, our results demonstrate that contrastive learning is a feasible and advantageous compression method for sparse geoscience data, thereby enhancing performance in downstream tasks.

---

## 论文详细总结（自动生成）

### 论文总结：对比学习提升气象数据的确定性与生成式模型

#### 1. 核心问题与整体含义
气象数据（如温度、风速、位势高度）具有**高维性、多模态性以及现实观测中的稀疏性**。传统方法（如自编码器 AE）在压缩数据时往往难以捕捉复杂的时空结构，且在处理稀疏观测时鲁棒性不足。本文的研究动机在于利用**自监督对比学习（Self-Supervised Contrastive Learning）**构建一个鲁棒的低维潜空间嵌入（Embedding），旨在解决稀疏数据下的特征提取问题，并提升下游任务（如天气预报、极端天气检测、生成式模拟）的效率和准确性。

#### 2. 论文提出的方法论：SPARTA 框架
论文提出了 **SPARTA**（SPArse-data augmented conTRAstive spatiotemporal embeddings），其核心思想是将对比学习与自编码器结构相结合：
*   **核心架构**：基于 SimCLR 框架，包含编码器 $f(\cdot)$、投影网络 $g(\cdot)$ 和一个用于重建的解码器 $D_x$。
*   **稀疏数据对齐**：通过将“稀疏掩码样本”与“完整样本”组成正对，利用对比损失（NT-Xent）强制模型在潜空间中将两者对齐，从而学习对缺失数据不敏感的鲁棒表示。
*   **时间感知硬负采样（Hard Negative Sampling）**：针对时序特性，显式选取时间邻近（30步内）的样本作为“硬负样本”，迫使模型学习更细微的时间判别特征。
*   **循环一致性损失（Cycle Consistency Loss）**：引入二阶有限差分惩罚项 $\ell_{cycle}$，约束潜空间中连续时间点嵌入的平滑度，从而提升预报任务的稳定性。
*   **GNN 模态融合**：提出一种新型图神经网络融合技术，根据物理相关性（如风速分量间、温度与位势高度间）构建图结构，通过图卷积（GCN）注入领域物理知识，优于传统的自注意力融合。

#### 3. 实验设计
*   **数据集**：使用 **ERA5** 全球大气再分析数据集（1959-2023年），分辨率为 5.625° (64x32)，包含 5 个关键气象变量。
*   **Benchmark（基准）**：主要对比对象是**确定性卷积自编码器（CAE）**。
*   **下游任务（评估场景）**：
    1.  **自回归预报**：使用 LSTM 在潜空间进行 100 步长线预测。
    2.  **条件潜扩散（Conditional Latent Diffusion）**：在潜空间进行生成式去噪，评估生成质量与鲁棒性。
    3.  **潜分类**：根据嵌入向量识别季节。
*   **对比维度**：对比了 Early-fusion（早期融合）、Late-fusion（自注意力后期融合）以及提出的 GNN-fusion。

#### 4. 资源与算力
*   **算力说明**：论文**未明确指出**具体的 GPU 型号、数量或总训练耗时。
*   **训练细节**：提到 Batch Size 为 128（对比学习）和 64（下游任务）；预训练阶段 100 Epoch，联合训练阶段 250 Epoch；使用了学习率调度器。

#### 5. 实验数量与充分性
*   **实验规模**：使用了超过 60 年的长时序数据，划分为训练（1959-1997）、验证（1998-2019）和测试（2010-2023）集。
*   **消融实验**：针对“硬负采样”和“循环一致性损失”分别做了消融测试，验证了各组件对潜空间平滑度和预报准确性的贡献。
*   **充分性评价**：实验设计较为**充分且客观**。通过 RRMSE、SSIM、PSNR、潜密度得分（Latent Density）和真实性得分（Realism Score）等多种指标多维度评估了潜空间质量。

#### 6. 主要结论与发现
*   **对比学习优势**：SPARTA 生成的潜空间比传统 AE 更具结构性和时间平滑性，预报性能提升高达 **32%**。
*   **稀疏处理能力**：在 50%-90% 的高掩码率下，对比学习能比 AE 更有效地恢复和利用特征。
*   **生成模型增益**：在潜扩散任务中，对比学习显著降低了生成样本的标准差，提高了生成的鲁棒性和对齐度。
*   **融合策略**：GNN 融合通过引入物理先验，在预报任务中表现优于 Self-attention，证明了物理知识在气象 AI 模型中的重要性。

#### 7. 优点
*   **端到端整合**：将对比学习与解码重建结合，解决了对比学习结果难以直接映射回原始物理空间的问题。
*   **时间平滑约束**：创新的循环一致性损失有效解决了时序数据在潜空间中的“跳变”问题，极大增强了预报能力。
*   **物理启发式设计**：GNN 融合策略为多模态气象数据处理提供了一种比纯数据驱动（如 Transformer）更高效的路径。

#### 8. 不足与局限
*   **分辨率限制**：实验仅在低分辨率（64x32）上进行，未验证其在高分辨率（如 0.25°）下的计算开销和表现。
*   **变量覆盖**：仅选取了 5 个变量，对于更复杂的气象系统（如包含降水、云量等非线性极强的变量）的泛化能力有待验证。
*   **算力信息缺失**：缺乏训练成本的量化数据，难以评估该方法在实际业务部署中的性价比。

（完）
