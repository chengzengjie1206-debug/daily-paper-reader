---
title: "TimeSqueeze: Dynamic Patching for Efficient Time Series Forecasting"
title_zh: TimeSqueeze：用于高效时间序列预测的动态分块
authors: "Sravan Kumar Ankireddy, Nikita Seleznev, Nam H. Nguyen, Yulun Wu, Senthil Kumar, Furong Huang, C. Bayan Bruss"
date: 2026-03-11
pdf: "https://arxiv.org/pdf/2603.11352v1"
tags: ["query:ts"]
score: 10.0
evidence: 用于高效时序预测的动态分块机制
tldr: TimeSqueeze 针对 Transformer 时间序列模型在点对点嵌入（计算量大）与固定长度分块（破坏局部特征）之间的权衡问题，提出了一种动态分块机制。该方法通过轻量级状态空间编码器提取特征，并根据信号复杂度自适应调整分块边界，在保留关键时间结构的同时大幅提升了计算效率和预测精度，为高效时间序列预测提供了新思路。
selection_source: fresh_fetch
motivation: 传统固定长度分块会破坏时间序列的自然过渡和局部动态，而点对点嵌入在处理长序列时面临严重的计算扩展性问题。
method: 提出一种动态分块机制，利用轻量级状态空间编码器提取特征，并根据局部信号复杂度自适应地为信息密集区分配短块、为平滑区分配长块。
result: 该方法在预训练中实现了 20 倍的收敛加速和 8 倍的数据效率提升，并在多个长程预测基准测试中显著优于点对点和固定分块架构。
conclusion: TimeSqueeze 通过内容感知的变分辨率压缩，在保持高保真度的同时极大地提高了 Transformer 模型处理时间序列的效率和性能。
---

## 摘要
基于 Transformer 的时间序列基础模型在分词（tokenization）选择上面临根本性的权衡：逐点嵌入（point-wise embeddings）保留了时间保真度，但随序列长度的扩展性较差；而固定长度的分块（fixed-length patching）通过施加统一边界提高了效率，但这可能会破坏自然过渡并模糊具有信息量的局部动态。为了解决这些局限性，我们引入了 TimeSqueeze，这是一种动态分块机制，可根据局部信号复杂度自适应地选择每个序列内的分块边界。TimeSqueeze 首先应用轻量级状态空间编码器（state-space encoder）来提取全分辨率的逐点特征，然后通过将短分块分配给信息密集区域、将长分块分配给平滑或冗余片段，来进行内容感知分割。这种可变分辨率压缩在保留关键时间结构的同时，大幅减少了提交给 Transformer 骨干网络的 token 序列。特别是在大规模预训练中，与等效的逐点 token 基线相比，TimeSqueeze 实现了高达 20 倍的收敛速度提升和 8 倍的数据效率提升。在长程预测基准测试中的实验表明，TimeSqueeze 一致优于使用逐点分词或固定大小分块的同类架构。

## Abstract
Transformer-based time series foundation models face a fundamental trade-off in choice of tokenization: point-wise embeddings preserve temporal fidelity but scale poorly with sequence length, whereas fixed-length patching improves efficiency by imposing uniform boundaries that may disrupt natural transitions and blur informative local dynamics. In order to address these limitations, we introduce TimeSqueeze, a dynamic patching mechanism that adaptively selects patch boundaries within each sequence based on local signal complexity. TimeSqueeze first applies a lightweight state-space encoder to extract full-resolution point-wise features, then performs content-aware segmentation by allocating short patches to information-dense regions and long patches to smooth or redundant segments. This variable-resolution compression preserves critical temporal structure while substantially reducing the token sequence presented to the Transformer backbone. Specifically for large-scale pretraining, TimeSqueeze attains up to 20x faster convergence and 8x higher data efficiency compared to equivalent point-token baselines. Experiments across long-horizon forecasting benchmarks show that TimeSqueeze consistently outperforms comparable architectures that use either point-wise tokenization or fixed-size patching.

---

## 论文详细总结（自动生成）

这是一份关于论文《TimeSqueeze: Dynamic Patching for Efficient Time Series Forecasting》的结构化深入总结：

### 1. 核心问题与研究动机
*   **研究背景**：Transformer 架构在时间序列预测中面临“分词（Tokenization）”的权衡。
    *   **逐点嵌入（Point-wise）**：保留了高频细节，但计算复杂度随序列长度呈平方级增长，难以处理长上下文。
    *   **固定分块（Fixed-length Patching）**：提高了效率，但统一的边界会破坏信号的自然过渡，且无法适应信号内部信息密度的变化（平滑区与剧变区被同等对待）。
*   **核心问题**：如何设计一种既能保持高保真度、又能实现极高计算效率的动态分词机制，以支持大规模时间序列基础模型的预训练？

### 2. 方法论：TimeSqueeze 核心思想与技术细节
TimeSqueeze 提出了一种**混合多分辨率架构**，包含以下四个关键步骤：
*   **轻量级状态空间（SSM）编码器**：使用 Mamba 层在原始分辨率下提取全量特征。SSM 具有线性复杂度，适合处理长序列并捕捉细粒度局部模式。
*   **基于相对偏差的动态分块**：
    *   **算法逻辑**：通过计算当前样本与前一样本的绝对差值，并将其与局部平均功率（滑动窗口内的均方根）进行对比。
    *   **公式原理**：若 $|x_i - x_{i-1}| > \tau \sqrt{P_i}$（其中 $\tau$ 是阈值，$P_i$ 是局部功率），则判定为分块边界。
    *   **变分辨率压缩**：在信息密集（波动大）区域分配短分块，在冗余（平滑）区域分配长分块。压缩时仅保留边界处的嵌入，以维持因果性。
*   **MoE Transformer 骨干网络**：将压缩后的 token 输入到带有混合专家（Mixture-of-Experts）机制的解码器架构中，进行大规模上下文建模。
*   **SSM 解码器与多预测头**：解码器将骨干网络的输出与编码器的残差特征结合，通过上采样恢复分辨率，最后由多个线性头同时预测不同长度的未来视野。

### 3. 实验设计
*   **数据集**：
    *   **预训练**：使用 Time-300B 数据集（包含能源、金融、交通、天气等 3000 亿个数据点）。
    *   **评估基准**：长程预测基准数据集 ETTh1/2、ETTm1/2 和 Weather。
*   **对比方法**：
    *   **基础模型**：Time-MoE（主要基线）、Moirai、TimesFM、Moment、Chronos。
    *   **传统模型**：iTransformer、PatchTST、DLinear、TimesNet 等。
*   **场景**：涵盖了 **Zero-shot（零样本预测）** 和 **Full-shot（全样本微调预测）**，以及单变量和多变量预测任务。

### 4. 资源与算力
*   **硬件环境**：使用了 **2 张 NVIDIA A100 80GB GPU**。
*   **训练细节**：
    *   TimeSqueeze 在 100,000 步迭代中完成预训练。
    *   相比逐点嵌入基线（Time-MoE），TimeSqueeze 减少了 **3.4 倍的显存占用**。
    *   在相同性能下，训练时间缩短了约 **20 倍**，数据效率提升了 **8 倍**。
    *   推理吞吐量在长预测视野下提升了高达 **10.5 倍**。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了大规模预训练（50B 到 300B token 级别），并在 5 个主流公开基准数据集上进行了详尽测试。
*   **消融实验**：非常充分。作者对比了：
    1.  动态分块 vs. 固定分块。
    2.  SSM 编码器 vs. 线性编码器。
    3.  有无残差连接（细粒度特征）。
    4.  有无原始位置 ID。
    5.  不同预训练上下文长度的影响。
*   **客观性**：通过在不同的骨干网络（如非 MoE 的 Encoder-only 架构）和不同的预训练数据集（如 GiftEvalPretrain）上重复实验，证明了该分词器的通用性。

### 6. 主要结论与发现
*   **效率与性能并存**：TimeSqueeze 在大幅降低计算成本的同时，保持了与最强逐点嵌入模型相当的预测精度，并显著优于固定分块模型。
*   **长上下文优势**：增加预训练时的上下文长度能显著提升下游任务性能，即使推理时使用的上下文较短。
*   **动态性价值**：实验证明，根据信号复杂度自适应分配计算资源（token）是提升时序模型扩展性的关键。

### 7. 优点（亮点）
*   **真正的动态性**：不同于以往在序列间调整分块大小的方法，TimeSqueeze 实现了**序列内部（within-sequence）**的动态边界调整。
*   **极高的工程价值**：20 倍的加速和 8 倍的数据效率提升，使得在有限算力下训练超大规模时序基础模型成为可能。
*   **即插即用**：该动态分词器可以集成到任何现有的 Transformer 骨干网络中。

### 8. 不足与局限
*   **超参数敏感性**：虽然 $\tau$（阈值）具有一定的鲁棒性，但要达到特定的平均压缩率，仍需要根据数据分布进行一定的超参数调优。
*   **极端压缩下的损失**：当压缩率超过 8 倍时，模型性能会出现较明显的下降，对于极度冗余数据的处理仍有优化空间。
*   **多变量建模**：虽然论文展示了多变量实验，但其核心预训练逻辑仍主要基于通道独立（Channel Independence）假设，对变量间复杂相关性的原生建模讨论较少。

（完）
