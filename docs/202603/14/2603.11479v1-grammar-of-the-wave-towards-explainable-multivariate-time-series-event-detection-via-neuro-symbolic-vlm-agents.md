---
title: "Grammar of the Wave: Towards Explainable Multivariate Time Series Event Detection via Neuro-Symbolic VLM Agents"
title_zh: 波形语法：通过神经符号 VLM 智能体实现可解释的多变量时间序列事件检测
authors: "Sky Chenwei Wan, Tianjun Hou, Yifei Wang, Xiqing Chang, Aymeric Jan"
date: 2026-03-12
pdf: "https://arxiv.org/pdf/2603.11479v1"
tags: ["query:ts"]
score: 9.0
evidence: 多变量时间序列事件检测
tldr: 针对多变量时间序列事件检测（TSED）中数据稀缺和语义复杂的挑战，本文提出了知识引导的 TSED 新范式。通过引入“事件逻辑树”（ELT）知识表示框架，结合神经符号 VLM 智能体，将自然语言描述转化为可解释的检测逻辑。该方法在减少对标注数据依赖的同时，显著提升了检测精度和解释性，并有效缓解了视觉语言模型的幻觉问题。
selection_source: fresh_fetch
motivation: 传统 TSED 方法难以从稀缺的标注数据中学习具有复杂语义结构的事件，且缺乏可解释性。
method: 提出事件逻辑树（ELT）框架，利用神经符号 VLM 智能体从信号可视化中提取基元，并按逻辑约束组合以实现事件定位。
result: 在真实世界数据集上的实验表明，该方法在零样本场景下优于监督微调基线和现有的 LLM/VLM 推理框架。
conclusion: ELT 框架成功桥接了语言语义与物理信号，为实现可解释且知识驱动的时间序列分析提供了有效路径。
---

## 摘要
时间序列事件检测（TSED）长期以来是一项重要任务，在许多高风险领域具有关键应用。与统计异常不同，事件由具有复杂内部结构的语义定义，在现实场景中很难从稀缺的标注数据中归纳学习。鉴于此，我们引入了知识引导的 TSED，这是一种新的设定，即在很少或没有训练数据的情况下，模型给定自然语言事件描述，并必须将其定位到多变量信号中的区间。为了应对这一挑战，我们引入了事件逻辑树（ELT），这是一种新颖的知识表示框架，通过建模事件内在的时序逻辑结构，架起了语言描述与物理时间序列数据之间的桥梁。基于 ELT，我们提出了一个神经符号 VLM 智能体框架，该框架从信号可视化中迭代地实例化原语，并在 ELT 约束下对其进行组合，从而生成检测到的区间以及以实例化树形式呈现的忠实解释。为了验证我们方法的有效性，我们发布了一个基于真实世界时间序列数据、包含专家知识和标注的基准测试。实验和人工评估表明，与监督微调基准以及现有的基于 LLM/VLM 的零样本时间序列推理框架相比，我们的方法具有优越性。我们还表明，ELT 在减轻 VLM 在匹配信号形态与事件语义时固有的幻觉方面至关重要。

## Abstract
Time Series Event Detection (TSED) has long been an important task with critical applications across many high-stakes domains. Unlike statistical anomalies, events are defined by semantics with complex internal structures, which are difficult to learn inductively from scarce labeled data in real-world settings. In light of this, we introduce Knowledge-Guided TSED, a new setting where a model is given a natural-language event description and must ground it to intervals in multivariate signals with little or no training data. To tackle this challenge, we introduce Event Logic Tree (ELT), a novel knowledge representation framework to bridge linguistic descriptions and physical time series data via modeling the intrinsic temporal-logic structures of events. Based on ELT, we present a neuro-symbolic VLM agent framework that iteratively instantiates primitives from signal visualizations and composes them under ELT constraints, producing both detected intervals and faithful explanations in the form of instantiated trees. To validate the effectiveness of our approach, we release a benchmark based on real-world time series data with expert knowledge and annotations. Experiments and human evaluation demonstrate the superiority of our method compared to supervised fine-tuning baselines and existing zero-shot time series reasoning frameworks based on LLMs/VLMs. We also show that ELT is critical in mitigating VLMs' inherent hallucination in matching signal morphology with event semantics.

---

## 论文详细总结（自动生成）

这是一份关于论文《Grammar of the Wave: Towards Explainable Multivariate Time Series Event Detection via Neuro-Symbolic VLM Agents》的结构化深入总结：

### 1. 论文的核心问题与整体含义（研究动机和背景）
*   **核心问题**：传统的时间序列事件检测（TSED）主要依赖**归纳学习（Inductive Learning）**，即从大量标注数据中学习模式。然而，在能源、医疗等高风险领域，标注数据稀缺且昂贵。此外，事件往往具有复杂的语义结构（如“A上升的同时B保持稳定，随后C下降”），传统模型难以理解这些逻辑，且缺乏人类可理解的解释性。
*   **研究动机**：人类专家通常通过阅读描述并观察波形来识别事件。论文试图模拟这一认知过程，提出**知识引导的 TSED（K-TSED）**设定：在无训练数据的情况下，仅根据自然语言描述，将事件定位到多变量信号的特定区间。

### 2. 论文提出的方法论
论文提出了一个名为 **SELA** 的神经符号 VLM 智能体框架，其核心是 **事件逻辑树（Event Logic Tree, ELT）**。

*   **核心思想**：将非结构化的文本描述转化为结构化的逻辑树，通过视觉语言模型（VLM）智能体在信号中寻找证据并进行逻辑组合。
*   **关键技术细节**：
    *   **ELT Schema（逻辑架构）**：
        *   **基元（Primitives）**：叶子节点，定义单个通道上的原子模式（如“压力骤增”）。
        *   **复合节点（Composites）**：内部节点，定义时序逻辑算子，包括 `SEQ`（顺序）、`SYNC`（同步）、`GUARD`（包含）和 `OR`（或）。
    *   **SELA 智能体协作**：
        *   **Logic Analyst（逻辑分析师）**：负责将文本描述翻译成 ELT Schema。
        *   **Signal Inspector（信号巡检员）**：利用主动可视化工具（缩放、标记、归一化）观察信号，为基元节点寻找匹配的时间区间。
    *   **逻辑计算**：采用模糊逻辑（Fuzzy Logic）和 Allen 区间代数，通过递归方式从底向上计算整个事件的置信度分数，同时强制执行“物理排他性”等公理以减少冲突。

### 3. 实验设计
*   **数据集**：发布了 **KITE (Knowledge-Infused Time Series Events)** 基准测试。
    *   **来源**：北海油气钻井的真实压力测试数据。
    *   **规模**：包含 KITE-easy（简单顺序事件）和 KITE-hard（具有复杂层级和互斥逻辑的事件）。
*   **对比方法（Baselines）**：
    *   **监督学习模型**：CNN、Transformer，以及时间序列大模型 Timer、Moment、Chronos（在低资源下微调）。
    *   **LLM/VLM 推理方法**：Numeric（直接输入数字）、VL-Time（将信号转为图片输入 VLM）。
    *   **人类专家**：具有多年行业经验的数据科学家。
*   **评估指标**：基于 IoU 的 F1 分数（F1@0.5 和 F1@0.9），用于衡量检测精度和定位准确性。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量或训练时长。
*   **原因分析**：由于 SELA 主要是基于现有的闭源大模型（如 GPT-4o/GPT-5 级别的推理模型）进行的零样本（Zero-shot）推理，不涉及大规模的参数训练，因此算力消耗主要体现在 API 调用和推理延迟上，而非传统的 GPU 训练集群。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在两个难度的子数据集上进行了全面对比。
    *   进行了**消融实验**，验证了 ELT 结构在抑制 VLM 幻觉方面的作用。
    *   引入了 **Oracle 实验**（使用人工修正的逻辑树），以区分逻辑解析错误和信号识别错误。
*   **充分性评价**：实验设计较为充分且客观。通过与人类专家的对比，确立了该任务的性能上限；通过与多种监督模型的对比，证明了在小样本/零样本场景下演绎推理的优势。

### 6. 论文的主要结论与发现
*   **性能优越性**：SELA 在零样本设定下显著优于现有的 LLM/VLM 推理框架，且在 F1@0.5 指标上接近人类专家水平。
*   **ELT 的关键作用**：消融实验证明，如果没有 ELT 的逻辑约束，VLM 容易陷入局部特征或产生过度自信的幻觉，尤其是在处理复杂、互斥的事件时。
*   **模型能力差异**：推理能力更强的模型（如 GPT-5 级别）在构建逻辑树和精确对齐边界方面表现出明显优势。

### 7. 优点（亮点）
*   **神经符号结合**：将 VLM 的感知能力与符号逻辑的严谨性结合，解决了深度学习模型“黑盒”检测的问题。
*   **高解释性**：输出不仅是时间区间，还包括一个实例化的逻辑树，人类专家可以清晰地看到每个子模式是如何支撑最终结论的。
*   **零样本能力**：无需昂贵的标注数据，仅靠行业知识（文本描述）即可部署，具有极强的实际应用潜力。

### 8. 不足与局限
*   **高精度定位瓶颈**：虽然 F1@0.5 表现优异，但在 F1@0.9（极高精度定位）上仍落后于人类专家，说明 VLM 在视觉对齐细微边界时仍有局限。
*   **依赖强大底座**：该方法高度依赖于顶级 VLM 的指令遵循和推理能力，在较弱的开源模型上可能难以复现同等效果。
*   **推理成本**：多智能体迭代对话和主动可视化工具的使用会产生较高的 Token 消耗和时间延迟。
*   **领域局限**：目前仅在能源领域验证，跨领域的泛化性（如医疗心电图、金融波动）仍需进一步实验支撑。

（完）
