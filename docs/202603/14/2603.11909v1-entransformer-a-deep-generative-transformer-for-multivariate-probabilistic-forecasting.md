---
title: "EnTransformer: A Deep Generative Transformer for Multivariate Probabilistic Forecasting"
title_zh: EnTransformer：一种用于多元概率预测的深度生成式 Transformer
authors: "Rajdeep Pathak, Rahul Goswami, Madhurima Panja, Palash Ghosh, Tanujit Chakraborty"
date: 2026-03-12
pdf: "https://arxiv.org/pdf/2603.11909v1"
tags: ["query:ts"]
score: 10.0
evidence: 多变量概率预测框架
tldr: 针对多元时间序列预测中不确定性量化难的问题，本文提出EnTransformer。该框架结合了Transformer的长程建模能力与engression随机学习范式，通过注入随机噪声和优化能量评分函数，在无需参数化假设的情况下直接学习条件预测分布。实验证明其在电力、交通等多个数据集上均能生成高质量的概率预测，性能优于现有基准模型。
selection_source: fresh_fetch
motivation: 现有的概率预测方法多依赖受限的参数化似然或分位数目标，难以捕捉多个相关时间序列间复杂的联合预测分布。
method: 将engression随机学习范式集成到Transformer架构中，通过注入随机噪声并优化基于能量的评分目标来直接建模条件分布。
result: 在Electricity、Traffic、Solar等多个主流基准数据集上的实验结果显示，EnTransformer生成的概率预测校准良好且性能一致领先。
conclusion: EnTransformer通过结合生成式随机学习与Transformer，为多元概率时间序列预测提供了一种无需参数假设且能有效处理复杂依赖关系的新方案。
---

## 摘要
可靠的不确定性量化在能源系统和交通网络等领域的多元时间序列预测问题中至关重要。尽管基于 Transformer 的架构近期在序列建模方面取得了强劲性能，但大多数概率预测方法仍依赖于具有限制性的参数化似然或基于分位数的优化目标。它们在捕捉多个相关时间序列之间的复杂联合预测分布时往往面临困难。本研究提出了 EnTransformer，这是一种深度生成式预测框架，它将用于建模条件分布的随机学习范式 engression 与 Transformer 强大的序列建模能力相结合。该方法将随机噪声注入模型表示中，并优化基于能量的评分目标，从而在不施加参数化假设的情况下直接学习条件预测分布。这种设计使 EnTransformer 能够生成连贯的多元预测轨迹，同时保留了 Transformer 有效建模长期时间依赖关系和序列间交互的能力。我们在 Electricity、Traffic、Solar、Taxi、KDD-cup 和 Wikipedia 等多个广泛使用的多元概率预测基准数据集上评估了 EnTransformer。实验结果表明，EnTransformer 能够产生校准良好的概率预测，并持续优于基准模型。

## Abstract
Reliable uncertainty quantification is critical in multivariate time series forecasting problems arising in domains such as energy systems and transportation networks, among many others. Although Transformer-based architectures have recently achieved strong performance for sequence modeling, most probabilistic forecasting approaches rely on restrictive parametric likelihoods or quantile-based objectives. They can struggle to capture complex joint predictive distributions across multiple correlated time series. This work proposes EnTransformer, a deep generative forecasting framework that integrates engression, a stochastic learning paradigm for modeling conditional distributions, with the expressive sequence modeling capabilities of Transformers. The proposed approach injects stochastic noise into the model representation and optimizes an energy-based scoring objective to directly learn the conditional predictive distribution without imposing parametric assumptions. This design enables EnTransformer to generate coherent multivariate forecast trajectories while preserving Transformers' capacity to effectively model long-range temporal dependencies and cross-series interactions. We evaluate our proposed EnTransformer on several widely used benchmarks for multivariate probabilistic forecasting, including Electricity, Traffic, Solar, Taxi, KDD-cup, and Wikipedia datasets. Experimental results demonstrate that EnTransformer produces well-calibrated probabilistic forecasts and consistently outperforms the benchmark models.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **EnTransformer** 的新型深度生成式框架，旨在解决多元时间序列预测中的不确定性量化问题。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
在能源、交通和金融等领域，准确预测多个相互关联的时间序列及其不确定性（概率预测）至关重要。
*   **现有挑战**：目前的概率预测方法大多依赖于**参数化似然函数**（假设数据服从某种特定分布，如高斯分布）或**分位数回归**。这些方法在处理高维、非线性且具有复杂联合分布的多元数据时显得过于僵化，难以捕捉序列间的复杂相关性。
*   **研究动机**：作者希望结合 Transformer 强大的长程依赖建模能力与 **Engression**（一种基于能量的随机学习范式）的灵活性，构建一个无需预设分布假设、能够直接生成多元预测轨迹的生成式模型。

### 2. 方法论：核心思想与技术细节
EnTransformer 的核心是将 Transformer 架构重新表述为一个**预添加噪声模型（Pre-additive Noise Model）**。
*   **核心思想**：通过在输入端注入随机噪声，并利用 Transformer 处理这些受扰动的序列，使模型能够从条件分布中采样，从而生成多个“可能的”未来轨迹。
*   **关键流程**：
    1.  **特征整合**：将历史目标序列与协变量（如时间特征）拼接。
    2.  **样本扩张**：将输入批次在 batch 维度上复制 $M$ 次。
    3.  **噪声注入**：在进入 Transformer 编码器前，为每个副本添加独立的随机噪声 $\epsilon$（高斯或均匀分布）。
    4.  **Transformer 处理**：利用自注意力机制捕捉时间依赖和跨序列交互。
    5.  **能量评分优化（Energy Score Loss）**：这是关键的损失函数。它包含两部分：
        *   **准确性项**：最小化生成样本与真实值之间的欧几里得距离。
        *   **分散性项**：最大化生成样本之间的两两距离，防止模型退化为确定性预测，确保预测区间覆盖真实的不确定性。

### 3. 实验设计
*   **数据集**：涵盖了 6 个主流多元时间序列基准：**Solar**（太阳能）、**Electricity**（电力）、**Traffic**（交通）、**KDD-cup**（空气质量）、**Taxi**（出租车需求）和 **Wikipedia**（网页浏览量）。
*   **基准模型（Baselines）**：对比了 9 种最先进的模型，包括：
    *   基于 RNN 的模型：Vec-LSTM, GP-Scaling, GP-Copula。
    *   基于正规化流（Normalizing Flow）的模型：LSTM-MAF, Transformer-MAF。
    *   基于扩散模型（Diffusion）的模型：TimeGrad, MG-Input。
    *   其他：TACTiS（基于 Copula 的 Transformer）。
*   **评估指标**：主要使用多元连续分级概率评分（$CRPS_{sum}$）和归一化均方根误差（$NRMSE_{sum}$）。

### 4. 资源与算力
*   **硬件**：实验在一台配备 **16 GB 显存的 NVIDIA Tesla P100 GPU** 上运行。
*   **训练效率**：论文明确提到了计算优势。例如，在 Traffic 数据集上，EnTransformer 每轮训练仅需 **5.38 秒**，比 Transformer-MAF 快 36.9%，比资源密集型的 TimeGrad 快 **82.1%**。
*   **训练配置**：训练 30 个 epoch，使用 Adam 优化器。

### 5. 实验数量与充分性
*   **实验规模**：在 6 个不同领域的数据集上进行了测试，每个实验均运行 **10 次独立重复实验**并报告均值和标准差，确保了结果的统计可靠性。
*   **消融实验**：作者专门研究了**训练样本数量 $M$** 对计算成本和预测性能的影响，发现 $M$ 在 2 到 5 之间通常能达到性能与效率的最佳平衡。
*   **统计检验**：使用了 **MCB（Multiple Comparison with the Best）测试**来证明 EnTransformer 在所有数据集上的综合排名显著优于其他模型。
*   **充分性评价**：实验设计非常充分，涵盖了准确性、校准度（PIT Q-Q 图）和计算效率等多个维度，对比方法也包含了最新的扩散模型，具有很强的说服力。

### 6. 主要结论与发现
*   **性能领先**：EnTransformer 在 Solar、Electricity、KDD-cup 和 Taxi 四个数据集上取得了最优的 $CRPS_{sum}$ 评分，在其他数据集上也保持了极强的竞争力。
*   **校准良好**：通过 PIT Q-Q 图验证，模型生成的概率区间与真实分布高度吻合，具有良好的统计校准性。
*   **高效性**：相比于复杂的扩散模型或流模型，EnTransformer 架构更简单，训练和推理速度显著更快。

### 7. 优点
*   **非参数化**：不需要对数据分布做任何先验假设，能够灵活捕捉复杂的联合分布。
*   **架构简洁**：仅需对标准 Transformer 做极小的修改（注入噪声和更换损失函数）即可实现高质量的概率预测。
*   **计算友好**：单次前向传播即可生成随机系综，适合大规模多元序列预测。

### 8. 不足与局限
*   **噪声敏感性**：噪声分布的选择（高斯 vs 均匀）和标准差 $\sigma$ 是超参数，可能需要针对不同数据集进行精细调优。
*   **局部偏差**：在某些数据集（如 Solar 和 Traffic）上，PIT Q-Q 图显示出轻微的校准偏差，可能需要后处理重校准技术。
*   **空间依赖建模**：虽然 Transformer 能捕捉跨序列交互，但对于具有明确空间拓扑结构（如路网图）的数据，未显式引入图卷积等空间先验。

（完）
