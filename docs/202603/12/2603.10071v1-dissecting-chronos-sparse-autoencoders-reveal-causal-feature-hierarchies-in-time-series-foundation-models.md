---
title: "Dissecting Chronos: Sparse Autoencoders Reveal Causal Feature Hierarchies in Time Series Foundation Models"
title_zh: 剖析 Chronos：稀疏自编码器揭示时间序列基础模型中的因果特征层级
authors: Anurag Mishra
date: 2026-03-10
pdf: "https://arxiv.org/pdf/2603.10071v1"
tags: ["query:ts"]
score: 10.0
evidence: 使用稀疏自编码器解释时间序列基础模型的内部表示
tldr: 本研究首次将稀疏自编码器（SAE）应用于时间序列基础模型 Chronos-T5，旨在揭示其内部黑盒机制。通过对 710M 参数模型的六个层级进行特征提取和消融实验，研究发现模型内部存在清晰的特征层级：从底层的频率特征到中层的突变检测，再到顶层的复杂时间概念。实验证明中层特征对预测精度最具因果影响力，而 Chronos-T5 的预测核心在于捕捉突变动力学而非简单的周期性规律。
selection_source: fresh_fetch
motivation: 时间序列基础模型在关键领域应用广泛，但其内部决策过程不透明，缺乏机械可解释性研究。
method: 在 Chronos-T5-Large 模型上训练 TopK SAE，并通过 392 次单特征消融实验验证特征的因果相关性。
result: 揭示了从低级频率到中级突变检测再到高级时间概念的特征层级，发现中层特征对预测精度的贡献最大。
conclusion: 机械可解释性方法可有效迁移至时序模型，证实 Chronos-T5 主要依赖突变动力学检测而非周期性模式识别。
---

## 摘要
时间序列基础模型 (TSFMs) 正越来越多地部署于高风险领域，然而其内部表示仍然是不透明的。我们展示了稀疏自编码器 (SAEs) 在 TSFM 上的首次应用，在 Chronos-T5-Large（7.1 亿参数）六个层级的激活值上训练了 TopK SAEs。通过 392 次单特征消融实验，我们证实了每个被消融的特征都会导致正向的 CRPS 退化，从而确认了其因果相关性。我们的分析揭示了一个随深度变化的层级结构：编码器早期层编码低级频率特征，中间层集中了因果关键的变化检测特征，而最终层则压缩了丰富但因果重要性较低的时间概念分类。最关键的特征存在于中间层（最大单特征 Delta CRPS = 38.61），而非语义最丰富的最终层，在最终层中，渐进式消融反而矛盾地提高了预测质量。这些发现表明，机械可解释性可以有效地迁移到 TSFMs，并且 Chronos-T5 依赖于突发动态检测而非周期性模式识别。

## Abstract
Time series foundation models (TSFMs) are increasingly deployed in high-stakes domains, yet their internal representations remain opaque. We present the first application of sparse autoencoders (SAEs) to a TSFM, training TopK SAEs on activations of Chronos-T5-Large (710M parameters) across six layers. Through 392 single-feature ablation experiments, we establish that every ablated feature produces a positive CRPS degradation, confirming causal relevance. Our analysis reveals a depth-dependent hierarchy: early encoder layers encode low-level frequency features, the mid-encoder concentrates causally critical change-detection features, and the final encoder compresses a rich but less causally important taxonomy of temporal concepts. The most critical features reside in the mid-encoder (max single-feature Delta CRPS = 38.61), not in the semantically richest final encoder layer, where progressive ablation paradoxically improves forecast quality. These findings demonstrate that mechanistic interpretability transfers effectively to TSFMs and that Chronos-T5 relies on abrupt-dynamics detection rather than periodic pattern recognition.

---

## 论文详细总结（自动生成）

以下是对论文《Dissecting Chronos: Sparse Autoencoders Reveal Causal Feature Hierarchies in Time Series Foundation Models》的结构化总结：

### 1. 论文的核心问题与整体含义
*   **研究动机**：时间序列基础模型（TSFMs）如 Chronos-T5 在预测任务中表现卓越，但其内部决策机制如同“黑盒”，缺乏透明度。
*   **核心问题**：能否将自然语言处理（NLP）中的“机械可解释性”（Mechanistic Interpretability）方法迁移到时序模型中，通过分解神经激活值来识别具有物理意义的特征，并验证这些特征对预测结果的因果影响。
*   **整体含义**：这是首次将稀疏自编码器（SAEs）应用于大规模时间序列基础模型的研究，揭示了模型内部处理时间信息的层级逻辑。

### 2. 论文提出的方法论
*   **核心思想**：利用稀疏自编码器（SAE）将 Chronos-T5 模型中稠密、叠加的神经激活值分解为稀疏且可解释的特征向量。
*   **关键技术细节**：
    *   **模型对象**：Chronos-T5-Large（7.1 亿参数），基于 T5 架构，将时序数值离散化为 4096个 Token。
    *   **TopK SAE**：在模型的 6 个关键位置（编码器第 5、11、23 层；解码器第 11、23 层及交叉注意力输出）训练 SAE。
    *   **特征提取公式**：通过 $z = \text{TopK}(W_{enc}(x - b_{dec}) + b_{enc}, k)$ 提取最强的 $k$ 个特征（$k=64$），特征维度扩展至 8192。
    *   **特征分类（Taxonomy）**：定义了 11 种时间概念（如趋势、季节性、水平位移、频率、波动率、噪声等），通过计算特征激活模式与合成诊断数据之间的皮尔逊相关性进行自动标注。
    *   **因果验证**：采用**单特征消融**（将特定特征置零）和**渐进式消融**（按贡献度排序依次移除特征），观察预测指标 CRPS（连续排名概率分数）的变化。

### 3. 实验设计
*   **数据集**：
    *   **合成诊断套件**：用于验证特征分类体系，包含具有已知属性（如趋势、季节性）的人造序列。
    *   **ETT 标杆数据集**：用于因果消融实验，评估特征对真实电力变压器温度预测的影响。
*   **对比基准**：以原始 Chronos-T5-Large 模型的预测性能为基准，对比消融不同层级、不同数量特征后的性能退化程度。

### 4. 资源与算力
*   **训练细节**：SAE 在约 10 万个时序窗口的激活值上训练，训练步数为 50,000 步，使用 Adam 优化器，学习率为 $3 \times 10^{-4}$。
*   **算力说明**：文中**未明确指出**具体的 GPU 型号、数量或总训练时长。

### 5. 实验数量与充分性
*   **实验规模**：进行了 392 次单特征消融实验，覆盖了编码器的三个关键层级。
*   **充分性评估**：
    *   **因果性验证充分**：100% 的消融实验均导致了 CRPS 的正向退化，有力证明了所提取特征的因果相关性。
    *   **局限性**：消融实验主要集中在 ETT 数据集上，且样本量（256 到 1024 个窗口）相对较小；此外，仅针对 Chronos-T5 这一种架构进行了分析。

### 6. 主要结论与发现
*   **特征层级性**：模型存在明显的深度依赖层级。早期层处理低级频率；中间层（第 11 层）是**因果核心**，集中处理突变检测（水平位移）；最终层（第 23 层）语义最丰富，涵盖了复杂的季节性和趋势概念。
*   **因果重要性悖论**：中间层特征对预测精度的影响最大（最大 $\Delta CRPS = 38.61$）。令人惊讶的是，消融最终层的特征反而可能**提高**预测质量，这表明最终层可能包含了一些过拟合或对特定领域（如 ETT）冗余的信息。
*   **预测逻辑**：Chronos-T5 更多地依赖于对**突发动力学（Abrupt Dynamics）**的检测，而非简单的周期性模式识别。

### 7. 优点
*   **方法创新**：成功将 SAE 引入时序领域，填补了 TSFM 机械可解释性的空白。
*   **因果导向**：不仅停留在特征可视化，还通过严谨的消融实验建立了特征与模型输出之间的因果联系。
*   **发现深刻**：揭示了中间层作为“计算瓶颈”的重要性，挑战了“越深层特征越重要”的直觉。

### 8. 不足与局限
*   **特征覆盖率低**：全局仅有 17.2% 的特征被成功标注，解码器层的标注率甚至低于 6%，大量特征仍处于“未知”状态。
*   **分类启发式**：特征分类依赖于人工定义的合成数据相关性，可能无法捕捉到模型内部更复杂的复合概念。
*   **泛化验证不足**：实验仅限于单一模型和有限的数据集，尚未在其他 TSFM 架构（如 TimesFM 或 Moirai）上验证结论的普适性。

（完）
