---
title: "Conformal prediction for high-dimensional functional time series: Applications to subnational mortality"
title_zh: 高维函数型时间序列的符合预测：在地方性死亡率中的应用
authors: Han Lin Shang
date: 2026-03-11
pdf: "https://arxiv.org/pdf/2603.10674v1"
tags: ["query:ts"]
score: 8.0
evidence: 高维函数型时间序列的符合预测
tldr: 本研究针对高维函数型时间序列预测中的不确定性量化问题，提出了一种模型无关且无分布假设的符合预测（Conformal Prediction）方法。为解决传统统计模型易受模型误设影响及自助法计算量大的问题，研究对比了分割符合预测和序列符合预测两种策略。通过对日本和加拿大的分地区分年龄死亡率数据进行实证分析，验证了该方法在构建预测区间方面的有效性与准确性，为高维复杂数据的预测提供了稳健的统计保障。
selection_source: fresh_fetch
motivation: 传统的预测不确定性量化方法易受模型误设影响且计算成本高，亟需一种模型无关且稳健的预测区间构建方法。
method: 研究采用了分割符合预测（通过验证集校准参数）和序列符合预测（通过自回归过程动态更新分位数）两种方法。
result: 通过对日本和加拿大的分地区死亡率数据进行测试，利用经验覆盖概率和平均区间评分评估了两种符合预测方法的有限样本预测性能。
conclusion: 符合预测方法能有效为高维函数型时间序列提供可靠的预测区间，且在处理实际人口统计数据时表现出良好的稳健性。
---

## 摘要
在统计学中，预测不确定性通常使用特定的统计模型来量化，尽管此类方法可能容易受到模型误设定、选择偏差和有限样本有效性受限的影响。虽然自助法（bootstrapping）可能缓解其中一些问题，但其计算需求通常很高。相反，我们采用一种模型无关且无分布的方法，即符合预测（conformal prediction），来构建高维函数型时间序列的预测区间。在丰富的符合预测方法族中，我们研究了拆分符合预测（split conformal prediction）和序列符合预测（sequential conformal prediction）。在拆分符合预测中，数据被分为训练集、验证集和测试集，其中验证集用于通过校准经验覆盖概率以匹配名义水平来选择最佳调节参数；此后，为测试集构建预测区间并评估其准确性。相比之下，序列符合预测通过自回归过程按顺序更新预测分位数，从而消除了对验证集的需求。利用来自日本和加拿大的地方性分年龄对数死亡率数据，我们使用经验覆盖概率和平均区间评分比较了这两种符合预测方法的有限样本预测性能。

## Abstract
In statistics, forecast uncertainty is often quantified using a specified statistical model, though such approaches may be vulnerable to model misspecification, selection bias, and limited finite-sample validity. While bootstrapping can potentially mitigate some of these concerns, it is often computationally demanding. Instead, we take a model-agnostic and distribution-free approach, namely conformal prediction, to construct prediction intervals in high-dimensional functional time series. Among a rich family of conformal prediction methods, we study split and sequential conformal prediction. In split conformal prediction, the data are divided into training, validation, and test sets, where the validation set is used to select optimal tuning parameters by calibrating empirical coverage probabilities to match nominal levels; after this, prediction intervals are constructed for the test set, and their accuracy is evaluated. In contrast, sequential conformal prediction removes the need for a validation set by updating predictive quantiles sequentially via an autoregressive process. Using subnational age-specific log-mortality data from Japan and Canada, we compare the finite-sample forecast performance of these two conformal methods using empirical coverage probability and the mean interval score.

---

## 论文详细总结（自动生成）

这篇论文探讨了高维函数型时间序列（HDFTS）的预测不确定性量化问题。以下是对该论文的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：如何在处理高维（截面数量 $N$ 可能大于时间点数量 $T$）且具有复杂时空依赖性的函数型数据时，构建稳健的预测区间。
*   **研究动机**：
    *   **传统模型局限**：指定的统计模型易受模型误设（misspecification）和选择偏差影响。
    *   **计算成本**：自助法（Bootstrapping）虽然能缓解部分问题，但在高维数据下计算压力巨大。
    *   **HDFTS 挑战**：在人口统计学（如地方性死亡率）等领域，数据呈现出跨地区、跨年龄的函数特性，且维度极高，现有的不确定性量化研究尚处于起步阶段。
*   **整体含义**：论文首次将**符合预测（Conformal Prediction）**引入 HDFTS 领域，提供了一种模型无关（model-agnostic）且无分布假设（distribution-free）的框架。

### 2. 方法论：核心思想与技术细节
论文采用了“分解 + 预测 + 符合校准”的流程：
*   **数据分解**：
    1.  **单向函数方差分析（One-way Functional ANOVA）**：利用函数中值磨光法（Functional Median Polish）将对数死亡率分解为确定的全局效应、地区效应和时变的残差过程。
    2.  **函数因子模型（FFM）**：对残差过程进行建模，提取低维的函数因子和载荷，以捕捉跨截面的相关性。
*   **符合预测策略**：
    1.  **分割符合预测（Split CP）**：将数据分为训练、验证和测试集。利用验证集计算残差的经验分位数，通过调节参数 $\xi_\alpha$ 校准覆盖概率，使其匹配名义水平（如 95%）。
    2.  **序列符合预测（Sequential CP）**：无需验证集。核心思想是利用**分位数回归**对绝对残差的滞后值进行建模（自回归过程），随着新观测值的到达，动态更新预测分位数。
*   **算法流程**：先通过 FFM 获得点预测，再利用上述 CP 方法根据历史残差分布构建围绕点预测的区间。

### 3. 实验设计
*   **数据集**：
    *   **日本数据**：47 个都道府县，1975-2023 年，分性别、分年龄（0-100+ 岁）的对数死亡率。
    *   **加拿大数据**（稳健性分析）：12 个省份和地区，1950-2016 年。
*   **评估设置**：采用**扩展窗口（Expanding-window）**预测方案，评估 1 到 10 步领先（h-step-ahead）的预测效果。
*   **对比基准（Benchmark）**：
    *   对比了 Split CP（基于标准差 sd 和基于分位数 quantile 两种统计量）。
    *   对比了不同的单变量预测方法（ARIMA 和 ETS）处理因子得分。
*   **评价指标**：经验覆盖概率（ECP）、覆盖概率偏差（CPD）和平均区间评分（MIS，综合衡量覆盖率和区间宽度）。

### 4. 资源与算力
*   论文**未明确说明**具体的硬件配置（如 GPU 型号或数量）。
*   由于该方法基于 R 语言开发（提到了 `hdftsa` 和 `demography` 等 R 包），且符合预测本身比自助法更高效，推测实验是在常规 CPU 服务器或工作站上完成的。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了两个国家（日本和加拿大）、两种性别、47+12 个地区、100 个年龄组以及 10 个预测步长。
*   **充分性**：实验设计较为充分。通过跨国数据的对比验证了方法的普适性；通过 10 年的测试期和扩展窗口法，确保了结果的统计显著性。
*   **客观性**：使用了标准的区间评分（MIS）和覆盖率指标，对比了多种 CP 变体，评价较为客观。

### 6. 主要结论与发现
*   **序列符合预测优于分割法**：序列符合预测（Sequential CP）在大多数情况下获得了更低的平均区间评分（MIS），且不需要预留验证集。
*   **覆盖率表现**：分割符合预测倾向于低估覆盖概率（尤其在长步长预测时）；而序列符合预测表现较为保守，倾向于略微高估覆盖概率，但在区间宽度（锐度）和覆盖率之间取得了更好的平衡。
*   **模型无关性**：无论底层预测模型是 ARIMA 还是 ETS，符合预测都能提供有效的区间估计。

### 7. 优点
*   **稳健性**：不依赖于误差项的正态性假设，对模型误设具有很强的容忍度。
*   **计算高效**：相比于需要反复重采样的自助法，符合预测（尤其是序列式）计算速度快，适合高维实时数据。
*   **实用性**：直接解决了人口统计学中 subnational（地方性）预测中样本量有限且维度高的痛点。

### 8. 不足与局限
*   **性别独立建模**：目前将男女数据分开建模，忽略了性别间的潜在相关性（作者在结论中提到这是未来的扩展方向）。
*   **自回归阶数选择**：在序列符合预测中，残差的自回归阶数 $p$ 依赖于信息准则，对于极度不平稳的残差序列，其捕捉能力可能受限。
*   **保守性**：序列符合预测虽然稳健，但有时过于保守（覆盖率远超 95%），这可能导致在某些应用场景下预测区间过宽。

（完）
