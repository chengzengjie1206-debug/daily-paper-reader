---
title: Interpretable models for forecasting high-dimensional functional time series
title_zh: 高维函数型时间序列预测的可解释模型
authors: "Han Lin Shang, Cristian F. Jiménez-Varón"
date: 2026-03-30
pdf: "https://arxiv.org/pdf/2603.28344v1"
tags: ["query:ts"]
score: 9.0
evidence: 高维函数型时间序列预测的可解释框架
tldr: 本研究针对具有时空相关性的高维函数型时间序列，提出了一种结合函数方差分析（FANOVA）与函数因子模型的可解释预测框架。通过FANOVA将数据分解为反映区域、性别等特定因素的确定性结构和随时间变化的残差过程，并利用因子模型捕捉残差中的随机趋势。以1975-2023年日本分地区死亡率数据为例，该方法在提升预测准确性的同时，增强了模型对数据驱动因素的解释力，为政策制定提供了透明的科学依据。
selection_source: fresh_fetch
motivation: 旨在解决高维函数型时间序列在建模时面临的复杂相关性以及统计降维模型缺乏可解释性的挑战。
method: 采用函数方差分析分解出确定性均值结构，并结合函数因子模型对残差过程中的随机趋势进行建模与预测。
result: 通过对日本分地区、分性别的死亡率数据进行实证分析，证明了该模型在点预测和区间预测精度上均优于传统方法。
conclusion: 该研究证明了利用可解释组件进行建模不仅能清晰揭示数据背后的驱动因素，还能显著提升高维时间序列的预测性能。
---

## 摘要
我们研究了高维函数型时间序列的建模与预测，这些序列可能具有时间依赖性和横截面相关性。我们采用函数型方差分析（FANOVA）将高维函数型时间序列（例如多年观察到的分地区、分年龄和分性别的死亡率）分解为两个不同的部分：确定性均值结构和随时间变化的残差过程。与纯统计降维技术不同，FANOVA 分解通过将序列划分为可归因于特定数据因素（如地区和性别差异）的效应以及总函数均值，提供了一个直接且可解释的框架。针对残差过程，我们应用函数型因子模型来捕捉剩余的随机趋势。通过将残差部分的预测与估计的确定性结构相结合，我们得到了高维函数型时间序列的预测曲线。以 1975 年至 2023 年日本分地区、分年龄的死亡率为例，我们评估并比较了不同预测期限下的点预测和区间预测的准确性。结果表明，利用这些可解释的组件不仅阐明了数据的潜在驱动因素，还提高了预测准确性，为循证决策提供了更透明的见解。

## Abstract
We study the modeling and forecasting of high-dimensional functional time series, which can be temporally dependent and cross-sectionally correlated. We implement a functional analysis of variance (FANOVA) to decompose high-dimensional functional time series, such as subnational age- and sex-specific mortality observed over years, into two distinct components: a deterministic mean structure and a residual process varying over time. Unlike purely statistical dimensionality-reduction techniques, the FANOVA decomposition provides a direct and interpretable framework by partitioning the series into effects attributable to data-specific factors, such as regional and sex-level variations, and a grand functional mean. From the residual process, we implement a functional factor model to capture the remaining stochastic trends. By combining the forecasts of the residual component with the estimated deterministic structure, we obtain the forecasted curves for high-dimensional functional time series. Illustrated by the age-specific Japanese subnational mortality rates from 1975 to 2023, we evaluate and compare the accuracy of the point and interval forecasts across various forecast horizons. The results demonstrate that leveraging these interpretable components not only clarifies the underlying drivers of the data but also improves forecast accuracy, providing more transparent insights for evidence-based policy decisions.

---

## 论文详细总结（自动生成）

以下是对论文《Interpretable models for forecasting high-dimensional functional time series》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：如何对具有高度复杂性、时间依赖性和横截面相关性的**高维函数型时间序列（HDFTS）**进行既准确又具有可解释性的建模与预测。
*   **研究背景**：在人口统计学（如分地区死亡率）等领域，数据通常以曲线形式随时间变化。现有的降维技术（如函数型主成分分析或因子模型）虽然计算高效，但生成的潜在因子往往是纯数学构造，缺乏实际物理意义（“黑盒”性质），导致决策者难以理解驱动变化的具体因素（如特定地区或性别的差异）。
*   **研究动机**：提出一种结构化框架，将统计降维的灵活性与方差分析的可解释性相结合，以识别数据中的确定性驱动因素。

### 2. 方法论
论文提出了一个三阶段的建模框架（TWA + OWA + FFM）：
1.  **两路函数方差分析 (Two-way FANOVA)**：
    *   将原始函数数据分解为：**全局均值函数** + **行效应（地区）** + **列效应（性别）** + **交互项与残差**。
    *   这一步提取了数据中由已知分类因子驱动的确定性结构。
2.  **一路函数方差分析 (One-way FANOVA)**：
    *   针对上一步的交互项，按性别分别进行一路 FANOVA，进一步提取不同性别下各地区的特定均值效应，以捕捉跨组的异质性。
3.  **函数因子模型 (Functional Factor Model, FFM)**：
    *   对经过前两步处理后的随机残差过程进行建模。利用特征分析提取随时间变化的公共因子和函数型因子载荷，捕捉剩余的随机趋势。
4.  **预测与不确定性量化**：
    *   **点预测**：结合 FANOVA 的确定性部分和 FFM 对残差的预测值。
    *   **区间预测**：采用**符合性预测 (Conformal Prediction)** 方法（包括 Split 和 Sequential 两种模式），这是一种模型无关、无分布假设的方法，用于构建具有理论覆盖保证的预测区间。

### 3. 实验设计
*   **数据集**：1975-2023 年日本分地区（47 个都道府县）、分性别、分年龄（0-100+ 岁）的死亡率数据。
*   **场景**：对未来 1 到 10 年的死亡率曲线进行点预测和区间预测。
*   **Benchmark（基准方法）**：
    *   **TWA + MFTS**：Jiménez-Varón 等人 (2024) 提出的结合两路 FANOVA 与多元函数时间序列的方法。
    *   **TWA + FFM**：不包含第二步交互项分解的简化版模型。
*   **对比维度**：对比了不同的基础预测器（ETS 指数平滑和 ARIMA）以及不同的区间预测校准策略。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的硬件配置（如 GPU 型号、数量）或具体的训练时长。
*   **软件实现**：作者提到使用了 R 语言及其相关包（如 `demography`），并提供了 GitHub 仓库地址以供复现。由于函数型数据分析主要涉及矩阵运算和特征分解，通常在常规 CPU 服务器或高性能工作站上即可完成。

### 5. 实验数量与充分性
*   **实验规模**：实验涵盖了日本全部 47 个地区和 2 个性别的完整数据集，时间跨度近 50 年。
*   **充分性**：
    *   采用了**扩展窗口（Expanding-window）**方案进行回测，生成了多组不同预测期限（h=1 到 10）的样本，确保了评估的稳健性。
    *   进行了**消融对比**（如 TWA+OWA+FFM vs TWA+FFM），验证了引入交互项分解的必要性。
    *   同时评估了点预测精度（RMSFE, MAFE）和区间预测质量（覆盖率 ECP、区间评分 IS），实验设计较为全面、客观。

### 6. 主要结论与发现
*   **预测精度提升**：提出的 TWA+OWA+FFM 框架在绝大多数预测期限下，其点预测误差（RMSFE 和 MAFE）均低于现有的基准方法。
*   **交互项的重要性**：通过一路 FANOVA 捕捉地区与性别的交互效应，能显著改善预测性能，尤其是在处理具有显著异质性的男性数据时。
*   **区间预测的有效性**：**序列符合性预测 (Sequential Conformal Prediction)** 在处理有限样本时表现更稳健，能够自动随新数据到达更新预测分位数，避免了划分验证集导致的样本浪费。
*   **可解释性价值**：模型成功将死亡率的变化分解为可观察的地理和人口因素，为政策制定提供了透明的科学依据。

### 7. 优点
*   **高度可解释**：不同于纯粹的“黑盒”因子模型，该模型允许研究者量化地区差异和性别差异对整体趋势的具体贡献。
*   **分布自由**：引入符合性预测技术，使得预测区间的构建不依赖于误差项的正态性假设，增强了模型在现实复杂数据中的鲁棒性。
*   **结构灵活**：框架可以根据数据的维度（如增加更多分类因子）进行扩展。

### 8. 不足与局限
*   **分类因子依赖**：模型的可解释性高度依赖于预先定义的分类标签（如地区、性别）。如果数据缺乏明确的类别属性，该方法的优势将难以发挥。
*   **计算复杂度**：随着横截面维度（N）和分类因子数量的增加，FANOVA 的分解和约束条件的处理会变得更加复杂。
*   **长时预测偏差**：在极长期的预测中，符合性预测的 Split 模式可能会因为验证集与测试集的时间跨度过大而导致覆盖率下降（虽然 Sequential 模式缓解了这一问题）。

（完）
