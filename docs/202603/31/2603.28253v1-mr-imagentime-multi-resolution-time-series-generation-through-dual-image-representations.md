---
title: "MR-ImagenTime: Multi-Resolution Time Series Generation through Dual Image Representations"
title_zh: MR-ImagenTime：通过双重图像表示的多分辨率时间序列生成
authors: "Xianyong Xu, Yuanjun Zuo, Zhihong Huang, Yihan Qin, Haoxian Xu, Leilei Du, Haotian Wang"
date: 2026-03-30
pdf: "https://arxiv.org/pdf/2603.28253v1"
tags: ["query:ts"]
score: 9.0
evidence: 用于预测的分层多分辨率趋势分解
tldr: "针对现有时间序列预测模型在处理变长输入和多尺度建模方面的局限性，本文提出了MR-CDM框架。该框架通过分层多分辨率趋势分解、自适应嵌入机制以及多尺度条件扩散过程，实现了对复杂时间序列的高效建模。在四个真实数据集上的评估结果显示，MR-CDM在MAE和RMSE指标上显著优于CSDI和Informer等主流模型，性能提升约6-10%，为变长多尺度时间序列生成提供了有效的解决方案。"
selection_source: fresh_fetch
motivation: 现有时间序列模型在处理变长输入和捕捉多尺度特征方面存在不足，限制了预测的准确性。
method: 提出MR-CDM框架，结合了分层多分辨率趋势分解、自适应嵌入机制和多尺度条件扩散过程。
result: "在四个真实数据集上，该方法相比CSDI和Informer等基准模型，将MAE和RMSE降低了约6-10%。"
conclusion: MR-CDM通过创新的多分辨率表示和扩散模型，显著增强了时间序列生成的建模能力和预测精度。
---

## 摘要
时间序列预测在许多领域都至关重要，然而现有模型在处理固定长度输入和多尺度建模不足方面面临挑战。我们提出了 MR-CDM，这是一个结合了分层多分辨率趋势分解、针对变长输入的自适应嵌入机制以及多尺度条件扩散过程的框架。在四个真实世界数据集上的评估表明，MR-CDM 显著优于最先进的基准模型（例如 CSDI、Informer），在一定程度上将 MAE 和 RMSE 降低了约 6-10。

## Abstract
Time series forecasting is vital across many domains, yet existing models struggle with fixed-length inputs and inadequate multi-scale modeling. We propose MR-CDM, a framework combining hierarchical multi-resolution trend decomposition, an adaptive embedding mechanism for variable-length inputs, and a multi-scale conditional diffusion process. Evaluations on four real-world datasets demonstrate that MR-CDM significantly outperforms state-of-the-art baselines (e.g., CSDI, Informer), reducing MAE and RMSE by approximately 6-10 to a certain degree.

---

## 论文详细总结（自动生成）

以下是对论文《MR-ImagenTime: Multi-Resolution Time Series Generation through Dual Image Representations》的结构化总结：

### 1. 核心问题与研究背景
*   **核心问题**：传统时间序列预测模型（如RNN、Transformer及标准扩散模型）在处理**变长输入**和**多尺度模式**（如趋势、季节性、噪声的交织）时存在局限。
*   **研究动机**：现实世界的数据（如电力需求）往往具有异构采样率和复杂的多尺度动态。现有方法通常假设固定输入窗口，且在将时间序列映射为图像表示时，容易破坏时间连续性和长程依赖关系。

### 2. 方法论：MR-CDM 框架
该模型采用五阶段流水线处理多尺度特征：
*   **多尺度趋势分解**：使用三个不同窗口（5, 25, 51）的移动平均滤波器，将序列分解为短期波动（Trend 1）、中期循环（Trend 2）、长期趋势（Trend 3）和高频残差。
*   **双重图像表示映射**：
    *   **延迟嵌入（Delay Embedding）**：将短期、中期信号和残差转换为 $32 \times 32$ 的 2D 图像，以保留精细的时间动力学。
    *   **短时傅里叶变换（STFT）**：将长期趋势映射到时频域，突出周期性特征。
*   **图像融合与条件扩散**：将上述四路分支连接成一个 **35通道** 的融合图像，输入到条件扩散模型中。扩散过程以历史上下文为条件，确保生成序列的时间一致性。
*   **增强型分层重构器**：通过并行执行分层重构、跨尺度注意力和自适应加权，从扩散生成的图像中恢复高保真度的时间序列预测。

### 3. 实验设计
*   **数据集**：
    *   **ETTh1**：电力变压器温度数据集（小时采样）。
    *   **合成数据集**：模拟 ETTh1 的统计特性，包含日/周周期、趋势和高斯噪声。
    *   **跨领域验证**：Weather（天气预测）和 Traffic（交通流预测）数据集。
*   **基准模型（Benchmarks）**：
    *   传统模型：ARIMA(2,1,2)。
    *   深度学习模型：两层堆叠的 LSTM。
    *   先进扩散模型：CSDI（基于条件的评分扩散模型）。
*   **评估指标**：MSE、MAE、RMSE。

### 4. 资源与算力
*   **硬件配置**：工作站配备 Intel Xeon Silver 4110 CPU，256 GB DDR4 RAM。
*   **GPU 算力**：使用了 **两块 NVIDIA TITAN RTX GPU**（每块 24 GB 显存）。
*   **软件环境**：Ubuntu 20.04, Python 3.11, PyTorch 2.0.1, CUDA 11.8。
*   **训练细节**：例如 LSTM 训练了 200 个 epoch，消融实验训练了 50 个 epoch。

### 5. 实验数量与充分性
*   **实验规模**：
    *   **主实验**：在四个数据集上进行了性能对比。
    *   **多步预测实验**：测试了 24、48、96、192 步的不同预测长度。
    *   **消融实验**：进行了两轮。第一轮验证分解模块、条件机制和融合模块的必要性；第二轮验证不同趋势组件（Trend 1/3）的影响。
    *   **敏感性分析**：分析了不同输入长度（48, 96, 192）对结果的影响。
    *   **多次运行验证**：每个实验重复 3 次（不同随机种子），并报告均值和标准差。
*   **评价**：实验设计非常充分且客观，涵盖了从消融分析到跨领域泛化能力的全面评估，并通过多次运行确保了结果的可重现性。

### 6. 主要结论与发现
*   **性能卓越**：MR-CDM 在所有数据集上均显著优于基准模型。在 ETTh1 上，相比 ARIMA 降低了 96.5% 的 MSE，相比 LSTM 提升了 89%，也优于先进的 CSDI。
*   **多尺度建模的必要性**：消融实验显示，移除趋势分解模块会导致性能下降约 89.6%，证明了显式解耦不同频率特征的重要性。
*   **条件机制的关键作用**：无条件扩散模型的性能下降了 1280%，说明历史上下文引导对于稳定预测至关重要。
*   **长程稳定性**：随着预测步数增加，MR-CDM 的误差增长率明显低于其他模型，表现出更强的抑制误差累积的能力。

### 7. 优点与亮点
*   **创新的表示方法**：结合延迟嵌入和 STFT 的双重图像表示，既利用了计算机视觉的空域归纳偏置，又保留了频域的周期性。
*   **处理变长输入**：通过图像映射机制，模型能够灵活处理不同长度的输入序列，而不受固定窗口限制。
*   **分层重构**：多路径重构器有效地整合了跨尺度依赖，避免了单一路径解码的信息丢失。

### 8. 不足与局限
*   **计算复杂度**：虽然论文提到提高了效率，但引入多尺度分解、STFT 变换及 35 通道的图像处理，其推理延迟和内存开销可能高于简单的线性或 RNN 模型。
*   **超参数敏感性**：移动平均窗口（5, 25, 51）和延迟嵌入参数（$\tau=3, d=32$）是手动设定的，对于不同特性的数据集可能需要重新调优。
*   **应用限制**：目前主要验证了单变量和部分多变量任务，在极高维度的多元时间序列上的扩展性仍有待进一步探讨。

（完）
