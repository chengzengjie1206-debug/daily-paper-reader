---
title: Skillful Kilometer-Scale Regional Weather Forecasting via Global and Regional Coupling
title_zh: 通过全球与区域耦合实现高精度公里级区域天气预报
authors: "Weiqi Chen, Wenwei Wang, Qilong Yuan, Lefei Shen, Bingqing Peng, Jiawei Chen, Bo Wu, Liang Sun"
date: 2026-03-30
pdf: "https://arxiv.org/pdf/2603.28173v1"
tags: ["query:ts"]
score: 9.0
evidence: 基于Transformer的高分辨率天气预报全局模型
tldr: 本研究针对高分辨率区域天气预报中大尺度动力学与小尺度过程耦合难的问题，提出了一种全球-区域耦合框架。该框架通过创新的ScaleMixer双向耦合模块，将预训练的Transformer全球模型与高分辨率区域网络结合，实现了中国区域5公里分辨率、1小时频率的预报。实验证明，该模型在捕捉地形风场和焚风效应等精细现象上表现优异，显著超越了传统数值天气预报和现有AI基准。
selection_source: fresh_fetch
motivation: 现有的数据驱动模型在处理区域高分辨率预报时，难以有效协调大尺度环流与地形、海岸等引起的小尺度过程之间的多尺度相互作用。
method: 提出ScaleMixer模块，通过自适应关键位置采样和注意力机制，实现全球模型与区域高分辨率网络之间的双向特征交互。
result: 在中国区域实现了5公里分辨率的预报，其性能在再分析数据和地面观测站数据上均显著优于业务化数值预报系统和现有AI模型。
conclusion: 全球与区域模型的协同耦合是实现高精度、公里级区域天气预报的有效途径，能显著提升对复杂地形下精细气象现象的模拟能力。
---

## 摘要
数据驱动的天气模型推动了全球中期预报的发展，但由于大尺度动力学与小尺度过程（如地形诱导环流和海岸效应）之间尚未解决的多尺度相互作用，高分辨率区域预测仍具挑战性。本文提出了一种用于公里级区域天气预报的全球-区域耦合框架，通过一种新型双向耦合模块 ScaleMixer，将预训练的基于 Transformer 的全球模型与高分辨率区域网络协同耦合。ScaleMixer 通过自适应关键位置采样动态识别气象关键区域，并通过专门的注意力

## Abstract
Data-driven weather models have advanced global medium-range forecasting, yet high-resolution regional prediction remains challenging due to unresolved multiscale interactions between large-scale dynamics and small-scale processes such as terrain-induced circulations and coastal effects. This paper presents a global-regional coupling framework for kilometer-scale regional weather forecasting that synergistically couples a pretrained Transformer-based global model with a high-resolution regional network via a novel bidirectional coupling module, ScaleMixer. ScaleMixer dynamically identifies meteorologically critical regions through adaptive key-position sampling and enables cross-scale feature interaction through dedicated attention mechanisms. The framework produces forecasts at $0.05^\circ$ ($\sim 5 \mathrm{km}$ ) and 1-hour resolution over China, significantly outperforming operational NWP and AI baselines on both gridded reanalysis data and real-time weather station observations. It exhibits exceptional skill in capturing fine-grained phenomena such as orographic wind patterns and Foehn warming, demonstrating effective global-scale coherence with high-resolution fidelity. The code is available at https://anonymous.4open.science/r/ScaleMixer-6B66.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **ScaleMixer** 的全球-区域耦合框架，旨在解决公里级高分辨率区域天气预报中的多尺度交互难题。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的 AI 全球天气模型（如 Pangu-Weather, GraphCast）分辨率通常在 0.25°（约 25km），难以捕捉地形诱导环流、海岸风等公里级（5km 及以下）的小尺度气象过程。
*   **研究动机**：
    *   **多尺度交互缺失**：传统方法通常将全球预报作为静态输入进行降尺度，忽略了大尺度环流与局部过程之间的动态双向反馈。
    *   **时空对齐难题**：全球模型通常是 6 小时步长，而区域预报需要逐小时的精细化输出。
    *   **复杂地形挑战**：如中国横断山脉等地区，剧烈的地形梯度对预报模型的精细化程度要求极高。

### 2. 方法论：ScaleMixer 耦合框架
*   **核心思想**：将预训练的全球 Transformer 模型与高分辨率区域网络通过 **ScaleMixer** 模块进行双向耦合，既保留全球尺度的动力学一致性，又提升区域尺度的精细度。
*   **关键技术细节**：
    *   **自适应关键位置采样（Adaptive Key-position Sampling）**：利用卷积网络动态识别气象关键区域（如台风边缘、山脊），从全球 Token 中采样出 $m$ 个显著位置，避免全量交互带来的计算冗余。
    *   **双向特征融合**：
        1.  **全球到位置注意力**：将全球背景信息聚合到选定的关键位置。
        2.  **位置到区域注意力**：将富含全球背景的关键位置特征传播给高分辨率区域 Token。
        3.  **区域反馈**：将更新后的区域特征反馈给全球 Token，实现闭环交互。
    *   **时间对齐增强**：引入自适应层归一化（AdaLN），通过傅里叶嵌入处理预报提前量（Lead Time），放大高频信号以实现逐小时预报。
    *   **三阶段训练协议**：全球模型预训练 $\rightarrow$ 区域模型单步训练 $\rightarrow$ 48 小时自回归滚动微调。

### 3. 实验设计
*   **数据集**：
    *   **全球数据**：ERA5 再分析数据（0.25°）用于预训练。
    *   **区域数据**：中国气象局陆面数据同化系统（CLDAS，0.05°）用于区域训练和验证。
    *   **实测验证**：中国境内 2216 个地面气象站的实时观测数据。
*   **Benchmark 与对比方法**：
    *   **业务化基准**：欧洲中期天气预报中心（ECMWF）的 **IFS-HRES**（0.1° 业务系统）。
    *   **AI 基准**：Baguan（SOTA 全球模型）、OneForecast（嵌套网格法）、LAM（有限区域模型）。
*   **评估指标**：纬度加权均方根误差（RMSE）和异常相关系数（ACC）。

### 4. 资源与算力
*   **硬件设备**：使用了 NVIDIA **A800** GPU 集群。
*   **训练耗时**：
    *   **全球预训练**：32 张 A800，运行 150,000 步。
    *   **区域训练**：8 张 A800，运行约 20 天（包含单步训练和滚动微调）。
*   **推理效率**：在单张 GPU 上，48 小时预报仅需 **不到 3 分钟**，而传统的 IFS-HRES 在大规模 CPU 集群上通常需要约 1 小时。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 2024 年 10-12 月的后验测试（Hindcast）和 2025 年 1-4 月的实时业务化预报测试。
*   **消融实验**：对比了随机采样、固定网格采样、单向耦合等变体，验证了自适应采样和双向耦合的必要性。
*   **公平性**：针对分辨率差异，对 IFS-HRES 和全球 AI 模型进行了专门的降尺度和偏差修正处理，确保在 0.05° 尺度下进行公平对比。
*   **充分性**：通过地面站点的直接对比（非格点数据对比），进一步排除了再分析数据可能带来的偏差，实验设计较为客观、全面。

### 6. 主要结论与发现
*   **性能领先**：ScaleMixer 在所有 7 个地面变量（温度、风速、气压等）上均显著优于 IFS-HRES 和其他 AI 模型。在地面站验证中，平均 RMSE 提升了 **27.63%**。
*   **精细现象捕捉**：模型成功模拟了横断山脉地区的**地形风场异质性**和**焚风效应**（背风坡增温 4-8°C），展现了极强的公里级解析能力。
*   **全球一致性**：双向耦合机制确保了区域预报不会偏离全球大尺度环流背景。

### 7. 优点与亮点
*   **创新的采样机制**：ScaleMixer 的自适应采样解决了高分辨率下计算量爆炸的问题，使模型能“好钢用在刀刃上”。
*   **端到端耦合**：不同于传统的“全球驱动区域”单向模式，该模型实现了特征级的双向反馈。
*   **极高的实用价值**：推理速度极快，且在复杂地形下的表现优于昂贵的数值天气预报（NWP）系统。

### 8. 不足与局限
*   **物理约束缺失**：作为纯数据驱动模型，缺乏显式的物理方程（如纳维-斯托克斯方程）约束，在极端天气或长期滚动预报中可能产生不符合物理规律的伪影。
*   **数据依赖性**：高度依赖高质量的区域再分析数据（如 CLDAS），在缺乏高精度同化数据的地区难以部署。
*   **变量覆盖**：目前主要集中在地面变量，对于高空大气层结的精细化预报讨论相对较少。

（完）
