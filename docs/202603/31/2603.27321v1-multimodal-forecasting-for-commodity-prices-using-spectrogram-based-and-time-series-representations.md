---
title: Multimodal Forecasting for Commodity Prices Using Spectrogram-Based and Time Series Representations
title_zh: 基于频谱图和时间序列表示的大宗商品价格多模态预测
authors: "Soyeon Park, Doohee Chung, Charmgil Hong"
date: 2026-03-28
pdf: "https://arxiv.org/pdf/2603.27321v1"
tags: ["query:ts"]
score: 9.0
evidence: 利用多模态融合进行多元时间序列预测
tldr: 本研究针对大宗商品价格预测中复杂的多变量依赖和异构外部影响，提出了频谱增强多模态融合模型（SEMF）。该方法通过Morlet小波变换将目标时间序列转化为频谱图，利用Vision Transformer提取频率特征，并结合Transformer处理外部宏观经济信号。通过双向交叉注意力机制整合多模态信息，SEMF在多个预测任务中显著优于现有基准，为捕捉金融序列的多尺度模式提供了有效方案。
selection_source: fresh_fetch
motivation: 旨在解决大宗商品价格预测中跨变量依赖复杂及外部异构因素干扰导致的预测难题。
method: 结合Morlet小波频谱图的视觉特征与外部变量的时序特征，通过双向交叉注意力机制实现多模态融合。
result: SEMF在多项大宗商品预测任务中表现出色，在多个预测维度上均超越了七种主流基准模型。
conclusion: 研究证明了将频谱表征与时序数据融合能更有效地捕捉金融时间序列中的多尺度特征。
---

## 摘要
由于复杂的变量间依赖关系以及异构外部影响的存在，多元时间序列预测仍然具有挑战性。本文提出了频谱图增强多模态融合（SEMF），它结合了频谱和时间表示，以实现更准确、更稳健的预测。目标时间序列被转换为 Morlet 小波频谱图，Vision Transformer 编码器从中提取局部化的频率感知特征。与此同时，金融指标和宏观经济信号等外生变量通过 Transformer 进行编码，以捕捉时间依赖性和多元动态。双向交叉注意力模块将这些模态整合到一个统一的表示中，在建模跨模态相关性的同时保留了独特的信号特征。应用于多个大宗商品价格预测任务，SEMF 在多个预测周期和评估指标上，相比七个具有竞争力的基准模型实现了持续的改进。这些结果证明了多模态融合和基于频谱图的编码在捕捉复杂金融时间序列中多尺度模式方面的有效性。

## Abstract
Forecasting multivariate time series remains challenging due to complex cross-variable dependencies and the presence of heterogeneous external influences. This paper presents Spectrogram-Enhanced Multimodal Fusion (SEMF), which combines spectral and temporal representations for more accurate and robust forecasting. The target time series is transformed into Morlet wavelet spectrograms, from which a Vision Transformer encoder extracts localized, frequency-aware features. In parallel, exogenous variables, such as financial indicators and macroeconomic signals, are encoded via a Transformer to capture temporal dependencies and multivariate dynamics. A bidirectional cross-attention module integrates these modalities into a unified representation that preserves distinct signal characteristics while modeling cross-modal correlations. Applied to multiple commodity price forecasting tasks, SEMF achieves consistent improvements over seven competitive baselines across multiple forecasting horizons and evaluation metrics. These results demonstrate the effectiveness of multimodal fusion and spectrogram-based encoding in capturing multi-scale patterns within complex financial time series.

---

## 论文详细总结（自动生成）

以下是对论文《Multimodal Forecasting for Commodity Prices Using Spectrogram-Based and Time Series Representations》的结构化深入总结：

### 1. 核心问题与研究动机
*   **核心问题**：大宗商品（如黄金、原油、煤炭等）价格预测具有高度的非线性、非平稳性，且受宏观经济、地缘政治等复杂外部因素影响。传统时序模型在处理多尺度模式和长程依赖时容易出现性能退化。
*   **研究动机**：
    *   **表征局限性**：纯时间序列模型难以捕捉频率域的瞬态变化；而现有的图像化预测方法（如线图）丢失了非平稳信号的频谱特征。
    *   **多模态需求**：大宗商品价格不仅取决于历史价格，还深受外生变量（如美债收益率、美元指数）的影响，需要一种能有效融合频谱特征与外部上下文的架构。
    *   **决策一致性**：企业决策需要跨多个预测周期（短、中、长期）的稳定预测，而现有模型在长周期下往往表现不佳。

### 2. 提出的方法论：SEMF 架构
论文提出了**频谱增强多模态融合（SEMF）**框架，核心思想是“双路径编码+双向交叉注意力融合”：
*   **目标序列路径（频谱编码）**：
    *   使用 **Morlet 小波变换**将目标价格序列转化为时频频谱图。相比 STFT，Morlet 小波能提供自适应的分辨率（高频处时间定位准，低频处频率分辨率高）。
    *   使用 **Vision Transformer (ViT)** 作为编码器，将频谱图切分为 Patch，提取局部和全局的频率感知特征。
*   **外生变量路径（时序编码）**：
    *   引入宏观经济指标（美债收益率、标普500、VIX指数等）。
    *   采用 **可逆实例归一化 (RevIN)** 处理分布偏移。
    *   使用 **Transformer 编码器** 捕捉外生变量间的多元动态和长程时间依赖。
*   **融合与预测模块**：
    *   **双向交叉注意力 (Bi-CA)**：频谱特征和时序特征互为 Query/Key-Value 进行交互，实现模态间的对齐与信息交换。
    *   **多周期预测头**：通过一个共享的 MLP 同时输出 1, 3, 7, 14, 21, 35 天的预测值，避免递归预测带来的误差累积。

### 3. 实验设计
*   **数据集**：2013年4月至2026年1月的多元日频数据，涵盖煤炭（Coal）、黄金（Gold）、钢铁（Steel）三种大宗商品。
*   **外生变量**：包括美债收益率（10年、2年、3个月）、美元指数（DXY）、汇率（USD/CNY, JPY, KRW）、标普500指数及VIX波动率指数、LME商品指数。
*   **基准模型 (Benchmarks)**：
    *   **时序模型**：LSTM, iTransformer, TimesNet, PatchTST。
    *   **图像/视觉模型**：VisionTS, ViT-num-spec（基于频谱的单模态 ViT）。
*   **评估指标**：RMSE, RMAE, MAPE, $R^2$。

### 4. 资源与算力
*   **算力说明**：论文中**未明确提及**具体的 GPU 型号、数量、训练时长或内存消耗等硬件资源细节。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在 3 种不同特性的商品上进行了测试。
    *   对比了 7 种主流基准模型。
    *   涵盖了从 1 天到 35 天的 6 个预测周期。
*   **消融实验**：非常充分，分别验证了：
    1.  **图像变换方式**：对比了线图、STFT、CMOR 和 Morlet 小波。
    2.  **外生编码器**：对比了 MLP 和 Transformer。
    3.  **融合策略**：对比了单向和双向交叉注意力。
    4.  **超参数敏感性**：分析了 Patch 大小、小波尺度、历史窗口长度的影响。
*   **客观性**：实验采用了统一的数据分割（0.65:0.15:0.20）和预处理流程，对比具有公平性。

### 6. 主要结论与发现
*   **性能领先**：SEMF 在所有商品和大多数指标上均优于基准模型，尤其在黄金和钢铁预测中表现出极强的稳健性。
*   **长周期稳定性**：与传统模型随预测周期增加而误差剧增不同，SEMF 在长周期（如21、35天）下依然保持相对稳定的误差水平。
*   **频谱价值**：实验证明 Morlet 小波频谱图比原始时序或简单的线图图像更能捕捉金融市场的波动模式。
*   **融合有效性**：双向交叉注意力机制显著提升了模型整合宏观经济信号的能力。

### 7. 优点与亮点
*   **多模态创新**：成功将计算机视觉（ViT）处理频谱图的能力与 Transformer 处理时序数据的能力结合，解决了金融信号非平稳性的难题。
*   **时频域结合**：利用 Morlet 小波的数学特性，为深度学习模型提供了比原始数值更丰富的物理特征。
*   **决策导向**：模型设计考虑了实际业务中对多周期一致性的需求，具有较强的工业应用潜力。

### 8. 不足与局限
*   **数据频率限制**：实验仅基于日频数据，未验证其在分钟级或秒级高频交易数据中的表现。
*   **计算复杂度**：虽然未提及算力，但双路径编码（ViT + Transformer）加上双向交叉注意力，其计算开销和推理延迟显著高于单一时序模型。
*   **资产覆盖面**：仅测试了三种大宗商品，虽然具有代表性，但对于农产品、能源或加密货币等其他波动特征迥异的资产，其泛化能力尚待验证。
*   **黑盒性质**：尽管使用了注意力机制，但对于“哪些宏观因素在何时影响了频谱特征”的解释性分析仍显不足。

（完）
