---
title: "VAN-AD: Visual Masked Autoencoder with Normalizing Flow For Time Series Anomaly Detection"
title_zh: VAN-AD：结合归一化流的视觉掩码自编码器用于时间序列异常检测
authors: "PengYu Chen, Shang Wan, Xiaohou Shi, Yuan Chang, Yan Sun, Sajal K. Das"
date: 2026-03-27
pdf: "https://arxiv.org/pdf/2603.26842v1"
tags: ["query:ts"]
score: 10.0
evidence: 用于时间序列异常检测的视觉掩码自编码器
tldr: 针对时间序列异常检测（TSAD）中模型泛化能力不足的问题，本文提出VAN-AD框架。该方法将预训练的视觉掩码自编码器（MAE）引入TSAD，通过自适应分布映射模块（ADMM）解决过度泛化问题，并结合归一化流模块（NFM）增强全局分布感知。实验证明，VAN-AD在九个真实数据集上均优于现有最先进方法，展现了强大的跨领域检测能力。
selection_source: fresh_fetch
motivation: 现有TSAD模型泛化性差且难以处理训练数据稀缺的场景，而现有大模型方法存在跨模态鸿沟或域内异质性挑战。
method: 提出VAN-AD框架，利用预训练视觉MAE，并结合ADMM模块放大异常差异及NFM模块估计全局概率密度。
result: 在九个真实世界数据集上的广泛实验表明，VAN-AD在多项评估指标上均一致优于现有的最先进方法。
conclusion: 本研究证明了将大规模视觉预训练模型应用于时间序列异常检测的有效性，并为跨模态迁移学习提供了新思路。
---

## 摘要
时间序列异常检测 (TSAD) 对于维护物联网服务系统的可靠性和安全性至关重要。现有方法需要为每个数据集训练一个特定模型，这在不同目标数据集之间表现出有限的泛化能力，阻碍了在训练数据稀缺的各种场景下的异常检测性能。为了解决这一局限性，基础模型已成为一个极具前景的方向。然而，现有方法要么重新利用大语言模型 (LLMs)，要么构建大规模时间序列数据集来开发通用的异常检测基础模型，但仍面临严重的跨模态差距或域内异质性带来的挑战。在本文中，我们研究了大规模视觉模型在 TSAD 中的适用性。具体而言，我们将一个在 ImageNet 上预训练的视觉掩码自编码器 (MAE) 适配到 TSAD 任务中。然而，将 MAE 直接迁移到 TSAD 会引入两个关键挑战：过度泛化和局部感知受限。为了应对这些挑战，我们提出了 VAN-AD，一种基于 MAE 的新型 TSAD 框架。为了缓解过度泛化问题，我们设计了一个自适应分布映射模块 (ADMM)，它将 MAE 重构前后的结果映射到一个统一的统计空间中，以放大由异常模式引起的差异。为了克服局部感知的局限性，我们进一步开发了一个归一化流模块 (NFM)，它将 MAE 与归一化流相结合，以估计当前窗口在全局分布下的概率密度。在九个真实世界数据集上的广泛实验表明，VAN-AD 在多个评估指标上始终优于现有的最先进方法。我们在 https://github.com/PenyChen/VAN-AD 上公开了代码和数据集。

## Abstract
Time series anomaly detection (TSAD) is essential for maintaining the reliability and security of IoT-enabled service systems. Existing methods require training one specific model for each dataset, which exhibits limited generalization capability across different target datasets, hindering anomaly detection performance in various scenarios with scarce training data. To address this limitation, foundation models have emerged as a promising direction. However, existing approaches either repurpose large language models (LLMs) or construct largescale time series datasets to develop general anomaly detection foundation models, and still face challenges caused by severe cross-modal gaps or in-domain heterogeneity. In this paper, we investigate the applicability of large-scale vision models to TSAD. Specifically, we adapt a visual Masked Autoencoder (MAE) pretrained on ImageNet to the TSAD task. However, directly transferring MAE to TSAD introduces two key challenges: overgeneralization and limited local perception. To address these challenges, we propose VAN-AD, a novel MAE-based framework for TSAD. To alleviate the over-generalization issue, we design an Adaptive Distribution Mapping Module (ADMM), which maps the reconstruction results before and after MAE into a unified statistical space to amplify discrepancies caused by abnormal patterns. To overcome the limitation of local perception, we further develop a Normalizing Flow Module (NFM), which combines MAE with normalizing flow to estimate the probability density of the current window under the global distribution. Extensive experiments on nine real-world datasets demonstrate that VAN-AD consistently outperforms existing state-of-the-art methods across multiple evaluation metrics.We make our code and datasets available at https://github.com/PenyChen/VAN-AD.

---

## 论文详细总结（自动生成）

这篇论文提出了 **VAN-AD**，一种将预训练视觉基础模型应用于时间序列异常检测（TSAD）的新型框架。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的时间序列异常检测方法通常针对特定数据集训练，泛化能力差。虽然近期出现了基于时间序列（TS-based）或大语言模型（Text-based）的基础模型，但前者面临高质量异常数据稀缺的问题，后者则存在严重的跨模态鸿沟。
*   **研究动机**：作者发现自然图像与时间序列在结构特征（如趋势、季节性、突变）上具有相似性。因此，研究如何利用在 ImageNet 上预训练的**视觉掩码自编码器（MAE）**来提取时间序列的通用先验知识。
*   **挑战**：直接迁移 MAE 存在两个痛点：
    1.  **过度泛化**：MAE 重构能力太强，导致异常点也能被很好地重构，缩小了正常与异常的差异。
    2.  **局部感知受限**：受限于 Transformer 的计算复杂度，滑动窗口通常较短，难以识别长程异常。

### 2. 方法论：VAN-AD 框架
VAN-AD 由四个核心模块组成：
*   **前向模块（Forward Module）**：将多变量时间序列编码为“变量-时间”热图矩阵，并调整为 224x224 的三通道灰度图，使其适配预训练 MAE 的输入格式。
*   **重构模块（Reconstruction Module）**：采用**棋盘格掩码（Checkerboard Masking）**策略生成互补的图像对，利用冻结的预训练 MAE 重构缺失区域。这种策略能同时捕捉变量间依赖和时间相关性。
*   **自适应分布映射模块（ADMM）**：为了解决过度泛化，ADMM 将重构后的序列映射回原始序列的统计空间（均值和标准差），从而放大异常模式引起的重构差异。
*   **归一化流模块（NFM）**：在重构序列之上构建**掩码自回归流（MAF）**。NFM 学习正常模式的全局概率分布，通过估计当前窗口的概率密度来补偿 MAE 局部感知的不足。
*   **异常评分**：最终得分为重构误差（$S_{MAE}$）与负对数似然得分（$S_{NF}$）的加权结合。

### 3. 实验设计
*   **数据集**：涵盖 9 个真实世界数据集（CalIt2, CICIDS, DLR, GECCO, MSL, NYC, PUMP, PSM, SWAN），涉及物联网、网络流量、航天、水处理等多个领域。
*   **Benchmark 与对比方法**：
    *   **TS-based 基础模型**：DADA, Timer。
    *   **Text-based 基础模型**：CALF, GPT4TS。
    *   **深度学习 SOTA**：CATCH, MtsCID, CAROTS, iTransformer 等。
    *   **经典方法**：LOF, PCA, KNN 等。
*   **评估指标**：采用对阈值不敏感的 AUC-ROC、AUC-PR 以及更先进的 VUS-ROC、VUS-PR。

### 4. 资源与算力
*   **硬件**：使用单台 **NVIDIA A800 (40GB)** GPU 进行实验。
*   **训练细节**：默认 Batch Size 为 128，训练 5 个 Epoch，学习率为 0.005。
*   **模型规模**：主要使用 MAE-base（112M 参数），但也测试了 Large（330M）和 Huge（657M）版本。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了大规模的对比实验（9 个数据集 vs 15 种基准方法）。
*   **消融实验**：详细验证了 ADMM 和 NFM 的必要性，并对比了“零样本（Zero-shot）”表现。
*   **深入分析**：
    *   **骨干网络分析**：探讨了不同规模 MAE 对性能的影响。
    *   **成像策略分析**：对比了 6 种时间序列转图像的方法（如 GAF, STFT, Heatmap 等）。
    *   **微调分析**：验证了冻结 MAE 权重比全量或部分微调效果更好（防止过拟合）。
    *   **参数敏感性**：测试了窗口大小和权重参数 $\lambda$ 的影响。
*   **评价**：实验设计非常全面、客观，通过可视化手段直观展示了各模块的功能，具有很强的说服力。

### 6. 主要结论与发现
*   **视觉先验有效性**：预训练的视觉 MAE 确实包含了可迁移至时间序列的结构化先验知识。
*   **性能领先**：VAN-AD 在所有 9 个数据集上均显著优于现有的 TS-based 和 Text-based 基础模型。
*   **模型规模悖论**：更大的视觉模型（Large/Huge）在 TSAD 任务上表现反而略有下降，可能是因为过拟合了图像特有特征。
*   **冻结胜过微调**：保持 MAE 权重冻结能更好地保留通用的视觉先验，微调反而会破坏这种泛化能力。

### 7. 优点
*   **跨模态创新**：避开了 LLM 的离散文本限制，利用视觉模型的连续性特征处理时间序列。
*   **模块设计精巧**：ADMM 有效解决了重构模型的“过度泛化”通病，NFM 弥补了 Transformer 窗口感知的局限。
*   **高效性**：采用热图（Heatmap）编码方式，相比其他通道独立的成像方法，推理速度大幅提升。

### 8. 不足与局限
*   **频率信息缺失**：热图编码虽然高效，但相比 STFT 或小波变换，对频域特征的显式建模能力较弱。
*   **计算开销**：尽管比 LLM 轻量，但引入 MAE 和归一化流在资源受限的嵌入式 IoT 设备上可能仍存在部署压力。
*   **窗口依赖**：虽然 NFM 增强了全局感知，但核心重构逻辑仍基于滑动窗口，对于极长周期的异常模式可能仍有提升空间。

（完）
