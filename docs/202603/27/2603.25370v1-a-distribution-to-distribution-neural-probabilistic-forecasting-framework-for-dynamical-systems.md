---
title: A Distribution-to-Distribution Neural Probabilistic Forecasting Framework for Dynamical Systems
title_zh: 一种用于动力系统的分布到分布神经概率预测框架
authors: "Tianlin Yang, Hailiang Du, Louis Aslett"
date: 2026-03-26
pdf: "https://arxiv.org/pdf/2603.25370v1"
tags: ["query:ts"]
score: 9.0
evidence: 神经概率预测框架
tldr: 该研究针对动力系统概率预测中传统方法依赖轨迹采样的问题，提出了一种分布到分布（D2D）的神经预测框架。该框架利用核均值嵌入表示输入分布，并通过混合密度网络参数化输出分布，实现了预测不确定性在统一端到端架构中的直接递归传播。在Lorenz63系统上的实验证明，该方法无需显式集合模拟即可捕捉非线性动力学下的复杂分布演化，性能优于或等同于传统基准。
selection_source: fresh_fetch
motivation: 现有的概率预测方法大多基于轨迹采样或集合模拟，缺乏直接演化预测分布作为动力学对象的能力。
method: 提出一种D2D框架，结合核均值嵌入进行分布编码和混合密度网络进行解码，实现分布的直接递归传播。
result: 在Lorenz63混沌系统上，该模型成功捕捉了非线性分布演化，且在预测技巧上达到或超过了简化完美模型基准。
conclusion: 该研究为概率预测提供了一种新范式，证明了直接学习和演化预测分布比间接重构集合更具优势。
---

## 摘要
概率预测通过将预测表示为概率分布而非确定性轨迹，为动力系统中的不确定性量化提供了一个原则性框架。然而，现有的预测方法，无论是基于物理的还是基于神经网络的，在根本上仍然是面向轨迹的：预测分布通常通过集成或采样获得，而不是作为动力学对象直接演化。本文开发了一种分布到分布（D2D）神经概率预测框架，直接对预测分布进行操作。该框架围绕一个可替换的神经预测模块引入了分布编码和解码结构，利用核均值嵌入（kernel mean embeddings）来表示输入分布，并使用混合密度网络（mixture density networks）来参数化输出预测分布。这种设计使得预测不确定性能够在统一的端到端神经架构中进行递归传播，且模型的训练和评估直接根据概率预测技巧进行。该框架在 Lorenz63 混沌动力系统上进行了演示。结果表明，D2D 模型捕捉到了非线性动力学下非平凡的分布演化，在无需显式集成模拟的情况下生成了有效的概率预测，并且与简化的完美模型基准相比具有竞争力，在某些情况下甚至表现更优。这些发现指向了概率预测的一种新范式，即直接学习和演化预测分布，而不是通过基于集成的不确定性传播进行间接重构。

## Abstract
Probabilistic forecasting provides a principled framework for uncertainty quantification in dynamical systems by representing predictions as probability distributions rather than deterministic trajectories. However, existing forecasting approaches, whether physics-based or neural-network-based, remain fundamentally trajectory-oriented: predictive distributions are usually accessed through ensembles or sampling, rather than evolved directly as dynamical objects. A distribution-to-distribution (D2D) neural probabilistic forecasting framework is developed to operate directly on predictive distributions. The framework introduces a distributional encoding and decoding structure around a replaceable neural forecasting module, using kernel mean embeddings to represent input distributions and mixture density networks to parameterise output predictive distributions. This design enables recursive propagation of predictive uncertainty within a unified end-to-end neural architecture, with model training and evaluation carried out directly in terms of probabilistic forecast skill. The framework is demonstrated on the Lorenz63 chaotic dynamical system. Results show that the D2D model captures nontrivial distributional evolution under nonlinear dynamics, produces skillful probabilistic forecasts without explicit ensemble simulation, and remains competitive with, and in some cases outperforms, a simplified perfect model benchmark. These findings point to a new paradigm for probabilistic forecasting, in which predictive distributions are learned and evolved directly rather than reconstructed indirectly through ensemble-based uncertainty propagation.

---

## 论文详细总结（自动生成）

### 论文总结：一种用于动力系统的分布到分布（D2D）神经概率预测框架

#### 1. 核心问题与整体含义
*   **研究动机**：传统的动力系统预测（无论是基于物理方程还是神经网络）大多是“面向轨迹”的，即演化单个状态点。不确定性量化通常通过集成（Ensemble）采样或事后统计校准来实现，这不仅计算成本高，且不确定性被视为辅助诊断而非模型演化的内在部分。
*   **核心问题**：如何构建一个能够直接演化“预测分布”本身，而非演化单个轨迹的端到端神经预测框架，使不确定性传播成为动力学演化的核心。

#### 2. 方法论
该论文提出了 **D2D（Distribution-to-Distribution）框架**，其核心思想是将预测分布视为一等公民，直接学习分布空间的变换算子 $F_\Theta: Q \to Q$。
*   **分布编码器（Encoder）**：采用**核均值嵌入（Kernel Mean Embedding, KME）**。利用高斯径向基函数（RBF）核将输入的概率分布映射到再生核希尔伯特空间（RKHS）中的有限维向量表示。对于高斯分布输入，该嵌入有解析解，计算高效。
*   **神经预测模块（Backbone）**：采用可替换的架构（实验中使用 **LSTM**），负责在嵌入空间中处理时间序列动力学。
*   **分布解码器（Decoder）**：采用**混合密度网络（MDN）**。将神经模块的输出解码为高斯混合模型（GMM）的参数（权重、均值、方差），从而直接生成连续的预测概率密度函数（PDF）。
*   **训练目标**：使用**对数评分（Logarithmic Score）**，即最小化负对数似然（NLL）。论文强调这是唯一原则性的局部适当评分规则，能确保不确定性量化的真实性。
*   **训练策略**：
    *   **直接策略**：针对特定提前期训练。
    *   **迭代策略**：通过**课程学习（Curriculum Learning）**逐步增加递归步数，学习动力学一致的分布演化算子。

#### 3. 实验设计
*   **数据集/场景**：经典的 **Lorenz63 混沌动力系统**。该系统具有强烈的初值敏感性和非线性，是测试不确定性传播的标杆。
*   **Benchmark（基准）**：
    *   **完美模型（Perfect Model）**：使用真实的 Lorenz63 方程进行 128 个成员的集成预测，并结合高斯核平滑（Kernel Dressing）。
    *   **气候态预测（Climatology）**：作为零技巧基准。
*   **对比方法**：
    *   不同课程学习长度的迭代 D2D 模型（16$\Delta t$ 到 128$\Delta t$）。
    *   直接预测策略（Direct D2D）结合分层时间聚合。
    *   不同观测噪声水平（0.01 到 0.08）下的表现对比。

#### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量或训练时长。
*   **数据规模**：实验使用了 30,000 个训练样本、30,000 个验证样本和 30,000 个测试样本。

#### 5. 实验数量与充分性
*   **实验规模**：涵盖了 4 种不同的噪声水平、5 种不同的训练提前期设置，以及迭代与直接策略的对比。
*   **充分性**：实验设计较为充分，使用了 **Bootstrap 重采样**（95% 置信区间）来评估测试性能的抽样不确定性，确保了结果的客观性。通过可视化 PDF 演化过程，直观证明了模型捕捉非高斯、多峰分布的能力。

#### 6 主要结论与发现
*   **分布演化能力**：D2D 模型能够成功捕捉非线性动力学导致的分布拉伸、变形和非高斯结构，无需显式集成模拟。
*   **迭代优于直接**：迭代训练的 D2D 模型在长程预测上远优于直接预测模型，因为前者学习到了动力学一致的演化算子。
*   **噪声鲁棒性**：在高噪声环境下，D2D 模型的表现甚至超过了简化的“完美模型”基准，因为它在学习动力学的同时，隐式地捕获了类似数据同化（Data Assimilation）的修正效果。
*   **课程学习的重要性**：较长提前期的课程训练不仅提升了长程预测，甚至能通过引入更丰富的动力学约束来改善短程预测技巧。

#### 7. 优点
*   **范式创新**：将概率预测从“轨迹采样”转向“分布演化”，为 AI 动力学建模提供了新视角。
*   **端到端优化**：将编码、预测、解码集成在统一架构中，直接针对概率技巧（对数评分）进行优化。
*   **计算效率**：推理时只需单次前向传播即可获得完整分布参数，避免了传统集成预测巨大的计算开销。
*   **理论严谨**：坚持使用对数评分作为训练和评估的统一标准。

#### 8. 不足与局限
*   **边缘分布限制**：目前模型主要针对状态变量的**边缘分布（Marginal Distributions）**进行建模，尚未实现高维全联合分布（Full Joint Distribution）的直接演化，忽略了变量间的瞬时相关性。
*   **系统复杂度**：实验仅限于低维的 Lorenz63 系统，在大规模高维系统（如全球天气预报）上的可扩展性和计算可行性尚待验证。
*   **核函数依赖**：KME 的性能受限于核函数（如 RBF）的选择和带宽参数，对于极度复杂的分布形态可能存在表达瓶颈。

（完）
