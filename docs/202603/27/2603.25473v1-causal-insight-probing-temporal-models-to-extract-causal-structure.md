---
title: "Causal-INSIGHT: Probing Temporal Models to Extract Causal Structure"
title_zh: Causal-INSIGHT：探测时间模型以提取因果结构
authors: "Benjamin Redden, Hui Wang, Shuyan Li"
date: 2026-03-26
pdf: "https://arxiv.org/pdf/2603.25473v1"
tags: ["query:ts"]
score: 9.0
evidence: 多变量时间序列预测器的后置解释框架
tldr: 本研究提出Causal-INSIGHT，一个模型无关的事后解释框架，旨在从预训练的时间预测模型中提取其依赖的有向时间因果结构。该方法通过在推理阶段对输入进行系统性的干预式钳位，分析模型响应以构建影响信号，并引入Qbic准则平衡预测保真度与结构复杂性。实验证明该方法在多种架构上具有通用性，能显著提升时间延迟定位的准确性，为理解复杂动态系统的预测逻辑提供了新视角。
selection_source: fresh_fetch
motivation: 现有的时间序列模型往往缺乏透明度，难以揭示预测过程中所依赖的内部因果交互和时间滞后结构。
method: 通过在推理时对输入进行干预式钳位并观察模型响应，结合Qbic稀疏感知准则来提取模型隐含的因果图。
result: 在合成与现实数据集的实验中，该方法成功应用于多种模型架构，并在因果结构准确性和时间延迟定位方面表现出色。
conclusion: Causal-INSIGHT提供了一种通用的事后分析手段，能够有效提取预测模型内部的因果逻辑，增强了时间序列模型的可解释性。
---

## 摘要
理解多元时间序列中的有向时间交互对于解释复杂的动力系统以及在其上训练的预测模型至关重要。我们提出了 Causal-INSIGHT，这是一个与模型无关的事后解释框架，用于从训练好的时间预测器中提取模型隐含的（依赖于预测器的）、有向的、时滞的影响结构。Causal-INSIGHT 并非在数据生成过程层面推断因果结构，而是分析固定的预训练预测器如何响应在推理阶段应用的、受干预启发的系统性输入钳制（input clamping）。基于这些响应，我们构建了反映预测器预测所依赖的依赖关系的有向时间影响信号，并引入了 Qbic，这是一种感知稀疏性的图选择准则，在不需要地面真值图标签的情况下平衡了预测保真度和结构复杂度。在合成、模拟和真实基准测试中的实验表明，Causal-INSIGHT 能够推广到多种骨干架构，保持了具有竞争力的结构准确性，并在应用于现有预测器时显著提升了时间延迟定位的效果。

## Abstract
Understanding directed temporal interactions in multivariate time series is essential for interpreting complex dynamical systems and the predictive models trained on them. We present Causal-INSIGHT, a model-agnostic, post-hoc interpretation framework for extracting model-implied (predictor-dependent), directed, time-lagged influence structure from trained temporal predictors. Rather than inferring causal structure at the level of the data-generating process, Causal-INSIGHT analyzes how a fixed, pre-trained predictor responds to systematic, intervention-inspired input clamping applied at inference time.   From these responses, we construct directed temporal influence signals that reflect the dependencies the predictor relies on for prediction, and introduce Qbic, a sparsity-aware graph selection criterion that balances predictive fidelity and structural complexity without requiring ground-truth graph labels. Experiments across synthetic, simulated, and realistic benchmarks show that Causal-INSIGHT generalizes across diverse backbone architectures, maintains competitive structural accuracy, and yields significant improvements in temporal delay localization when applied to existing predictors.

---

## 论文详细总结（自动生成）

这是一份关于论文《Causal-INSIGHT: Probing Temporal Models to Extract Causal Structure》的结构化深度总结：

### 1. 核心问题与研究动机
*   **核心问题**：如何从一个已经训练好的、黑盒化的多元时间序列预测模型中，提取出该模型所依赖的有向、带时间滞后（Time-lagged）的因果影响结构？
*   **研究动机**：
    *   深度学习模型在时间序列预测中表现优异，但其内部逻辑不透明，难以解释变量间的动态交互。
    *   现有的时间因果发现方法通常将“预测”与“因果推断”耦合（如在训练中加入稀疏约束），缺乏一种通用的、事后（Post-hoc）的解释框架来分析任意已训练好的模型。
    *   旨在区分“数据生成过程的因果”与“模型隐含的因果”，重点在于理解模型预测时到底利用了哪些历史信息。

### 2. 方法论
Causal-INSIGHT 是一个模型无关的框架，主要包含以下关键步骤：
*   **核心思想**：通过在推理阶段对输入进行“干预式钳位（Input Clamping）”，观察模型输出的变化，从而量化变量间的敏感度。
*   **关键技术流程**：
    1.  **模型探测（Probing）**：保持预训练模型 $f_\theta$ 固定。在推理时，将输入序列中的某个变量 $i$ 在特定时间点 $t_0$ 强制固定为一个特定值 $x^*$（钳位），而保持其他输入不变。
    2.  **影响信号构建**：计算钳位后的预测输出与原始预测输出之间的绝对偏差，生成影响信号张量 $S_{i,j,t}$。该信号反映了变量 $i$ 的变化如何随时间传播并影响变量 $j$ 的预测。
    3.  **时间信号归约**：提取每个变量对之间的峰值影响强度及其对应的延迟（Lag）。
    4.  **Qbic 评分准则**：提出一种新的稀疏感知图选择指标 **Qbic (Quality-BIC)**。它在不需要真实标签的情况下，通过衡量“仅保留推断出的父节点作为输入时模型的预测误差”与“图复杂度（边数）”之间的平衡，自动选择最佳的图稀疏度。
    5.  **图构建**：根据 Qbic 最小值选择最终的有向时间因果图。

### 3. 实验设计
*   **数据集/场景**：
    *   **合成数据（Synthetic）**：包含 Fork、V-structure、Mediator、Diamond 四种经典因果结构，共 40 个数据集。
    *   **物理模拟（Lorenz-96）**：具有混沌特性的动力系统，包含 10 个模拟序列。
    *   **现实模拟（fMRI）**：模拟脑部功能磁共振成像数据，具有高维、噪声大、间接测量等特点，共 28 个数据集。
*   **对比方法（Baselines）**：
    *   深度学习因果发现方法：cMLP、cLSTM、TCDF、CUTS、CausalFormer。
    *   传统统计方法：PCMCI+（用于 fMRI 基准）。
*   **评估指标**：结构准确性（F1、Precision、Recall、SHD）、时间延迟定位准确性（PoD）。

### 4. 资源与算力
*   **硬件环境**：实验运行在 Ubuntu 22.04 系统上，配备了 **NVIDIA H100 SXM GPU (80 GB VRAM)**。
*   **软件框架**：使用 PyTorch 2.1.0 和 CUDA 11.8。
*   **训练细节**：模型训练采用 MSE 损失函数，设置了早停机制（Synthetic/Lorenz 为 20 epochs，fMRI 为 10 epochs）。由于是事后分析，Causal-INSIGHT 本身在推理阶段运行，计算开销主要取决于模型的多次前向传播。

### 5. 实验数量与充分性
*   **实验规模**：总共在 **78 个** 不同的数据集上进行了测试，涵盖了从简单合成结构到复杂高维现实模拟的多种场景。
*   **消融实验**：
    *   验证了 **Qbic 准则** 的有效性（展示了其与真实 F1 分数的高度相关性）。
    *   验证了 **因果信号质量** 的重要性（通过随机置换信号进行对比）。
    *   探讨了 **钳位量级（Clamping Magnitude）** 对结果的影响。
*   **充分性评价**：实验设计较为全面，不仅对比了多种 SOTA 方法，还验证了框架在 MLP、CNN、LSTM 和 Transformer 等不同骨干架构上的通用性，结果具有较强的说服力和客观性。

### 6. 主要结论与发现
*   **通用性**：Causal-INSIGHT 可以成功应用于各种架构，证明了预测模型内部确实隐含编码了稳定的有向依赖关系。
*   **延迟定位优势**：相比于原始的 CausalFormer 解释机制，Causal-INSIGHT 在时间延迟定位（PoD）上取得了显著提升（中位数提升达 0.30），解决了现有模型在“何时发生影响”判定上的不足。
*   **结构恢复**：在 fMRI 等复杂数据集上，该方法在保持高 F1 分数的同时，具有更低的结构汉明距离（SHD）和误发现率（FDR）。

### 7. 优点
*   **模型无关（Model-agnostic）**：不需要修改模型架构或训练过程，可直接用于任何已有的时间序列预测器。
*   **无需真值标签**：通过 Qbic 准则实现了无监督的图稀疏性选择，具有很强的实用性。
*   **提升解释精度**：显著增强了对时间延迟（Lag）的识别能力，使因果解释在时间维度上更精确。

### 8. 不足与局限
*   **因果假设限制**：依赖于 Granger 因果假设、因果充分性（无未观测干扰项）和弱平稳性，在违反这些假设的复杂现实场景中可能失效。
*   **边际影响局限**：目前的钳位操作是一次针对一个变量进行的，可能难以捕捉多个变量共同作用产生的更高阶交互影响。
*   **计算开销**：虽然是线性的，但提取完整结构需要对每个变量进行探测，对于变量极多的超大规模系统，推理次数会随变量数 $N$ 增加。

（完）
