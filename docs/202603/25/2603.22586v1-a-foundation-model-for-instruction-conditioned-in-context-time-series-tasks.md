---
title: A Foundation Model for Instruction-Conditioned In-Context Time Series Tasks
title_zh: 一种用于指令调节的上下文内时间序列任务的基础模型
authors: "Anish Saha, Konstantin Shmakov"
date: 2026-03-23
pdf: "https://arxiv.org/pdf/2603.22586v1"
tags: ["query:ts"]
score: 9.0
evidence: 用于上下文学习时间序列任务的基础模型
tldr: 本研究提出了一种基于指令调节的时间序列基础模型，旨在通过上下文学习实现推理时的快速适配。该模型采用分层Transformer架构和分位数回归T5编码器-解码器，通过结构化分词处理历史示例和查询。在包含预测、填补、分类等多种任务的大规模数据集上进行预训练，使其无需微调即可在预测、分类和异常检测等任务中表现出色，显著提升了模型对局部结构的适应能力。
selection_source: fresh_fetch
motivation: 现有时间序列模型缺乏对显式指令和示例演示的利用，限制了其在推理阶段通过上下文学习进行灵活适配的能力。
method: 提出一种基于T5的分层Transformer架构，结合结构化分词方案和多任务预训练，实现对示例对的条件解码。
result: 在fev-bench和GIFT-Eval等多个预测基准上超越了强基线模型，并在分类和异常检测任务中保持了竞争力。
conclusion: 该模型证明了显式指令调节和多任务上下文学习能有效提升时间序列基础模型的泛化性能和局部适配能力。
---

## 摘要
上下文学习（ICL）允许模型在推理时通过以示例为条件而非更新参数来进行自适应。现有的时间序列基础模型使用隐式位置上下文、检索或特定任务目标，但很少使用显式的指令调节演示。我们提出了一种基于分位数回归 T5 编码器-解码器的基础模型，用于指令调节的上下文内时间序列任务。历史示例和查询通过结构化分词方案进行编码，该方案标记了目标序列、协变量、上下文和特定任务的未来信息。具有逐示例编码、示例级融合和跨示例注意力的分层 Transformer 以演示对为条件进行解码，从而在无需特定任务微调的情况下实现预测及相关任务。我们在大规模真实和合成时间序列上进行训练，采用监督预测加自监督任务，包括插补、重建、分类、异常检测和源分离。这种多任务训练学习了任务映射的分布，并提高了推理时对局部结构的适应能力。在不同的数据集、频率和预测范围下，我们的方法在点预测和概率预测基准（包括 fev-bench 和 GIFT-Eval）上优于强大的基础基准模型，同时在分类和异常检测方面保持竞争力。

## Abstract
In-context learning (ICL) allows a model to adapt at inference time by conditioning on examples rather than updating parameters. Existing time-series foundation models use implicit positional context, retrieval, or task-specific objectives, but rarely explicit instruction-conditioned demonstrations. We present a foundation model for instruction-conditioned in-context time-series tasks based on a quantile-regression T5 encoder-decoder. Historical examples and queries are encoded with a structured tokenization scheme that marks target series, covariates, context, and task-specific future information. A hierarchical Transformer with per-example encoding, example-level fusion, and cross-example attention conditions decoding on demonstration pairs, enabling forecasting and related tasks without task-specific fine-tuning. We train on large-scale real and synthetic time series using supervised forecasting plus self-supervised tasks, including imputation, reconstruction, classification, anomaly detection, and source demixing. This multi-task training learns a distribution over task mappings and improves adaptation to local structure at inference time. Across diverse datasets, frequencies, and horizons, our method outperforms strong foundation baselines on point and probabilistic forecasting benchmarks, including fev-bench and GIFT-Eval, while remaining competitive on classification and anomaly detection.

---

## 论文详细总结（自动生成）

### 论文结构化总结：iAmTime 基础模型

#### 1. 核心问题与整体含义（研究动机和背景）
尽管大型语言模型（LLM）通过上下文学习（In-Context Learning, ICL）实现了强大的任务适配能力，但现有的时间序列基础模型大多仍依赖于隐式的历史窗口或特定的任务头，缺乏像 NLP 那样通过“指令+示例演示”来定义任务的能力。
**核心问题**：如何构建一个能够通过显式示例演示（Input-Output Demonstrations）在推理时自动推断并执行多种时间序列任务（如预测、分类、异常检测等）的统一基础模型，而无需任何参数微调。

#### 2. 方法论：核心思想与技术细节
论文提出了 **iAmTime** 模型，其核心思想是将时间序列任务建模为指令调节的上下文推理过程。
*   **架构设计**：采用基于 T5 编码器-解码器的分层 Transformer 架构。
    *   **结构化分词（Prompt-like Tokenization）**：引入专门的语义 Token（如 `[TARGET SERIES]`, `[EXOG]`, `[MID]`, `[END]`），将目标序列、协变量、历史背景和未来信息显式区分，防止表示泄露。
    *   **三阶段分层注意力**：
        1.  **个体编码**：独立处理每个一维序列。
        2.  **示例内融合**：在单个示例内部实现目标与协变量的交互。
        3.  **跨示例融合（ICL 规则提取）**：通过均值池化聚合 N 个示例的信息，生成全局“规则”向量来调节查询（Query）的解码。
*   **摊销元学习（Amortized Meta-Learning）**：模型在预训练阶段不仅学习预测，还学习如何根据示例“学习任务”。
*   **概率解码**：使用混合专家（MoE）交叉注意力和分位数回归（Quantile Regression），直接输出多步预测的分布。

#### 3. 实验设计
*   **数据集**：
    *   **预训练**：整合了 Chronos 和 GIFT-Eval 的语料库（约 6600 万条序列），并使用 TSMixup、KernelSynth 和自定义的多变量合成逻辑进行增强。
    *   **评估基准**：**fev-bench**（包含 100 个涵盖多领域、多变量的任务）和 **GIFT-Eval**（24 个数据集，97 个评估设置）。
*   **对比方法**：
    *   **基础模型**：Chronos-2, TimesFM-2.5, TiRex, Moirai-2.0, TabPFN-TS, Toto-1.0 等。
    *   **传统/深度学习基线**：AutoARIMA, ETS, PatchTST, TFT, N-BEATS, DLinear 等。
*   **评估指标**：MASE（点预测精度）和 CRPS（概率校准质量）。

#### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的训练算力细节（如 GPU 型号、数量、总训练时长或能耗）。
*   **推理效率**：文中提供了推理时间的对比（表 5），显示 iAmTime 的中位推理时间（4.8s）优于 TimesFM-2.5（16.9s）和 TabPFN-TS（305.5s），但慢于 Chronos-2（2.7s）和 TiRex（1.4s）。

#### 5. 实验数量与充分性
*   **实验规模**：实验非常充分。在两个大型第三方 Benchmark（fev-bench 和 GIFT-Eval）上进行了全面测试，涵盖了单变量、多变量、长短期预测等多种场景。
*   **消融实验**：针对“无示例训练”、“无语义 Token”和“无元学习任务”进行了详细消融，证明了每个组件对 ICL 能力的贡献。
*   **客观性**：通过严格排除预训练集中的测试数据，并使用第三方评估协议，确保了实验的公平性和零样本（Zero-shot）性能的真实性。

#### 6. 主要结论与发现
*   **性能领先**：iAmTime 在 fev-bench 和 GIFT-Eval 上的综合排名均位居第一，尤其在概率预测（CRPS）方面表现卓越。
*   **多任务适配**：同一个模型在不改变参数的情况下，仅通过更换上下文示例，即可在分类和异常检测任务中达到竞争水平。
*   **结构化 Token 的必要性**：消融实验显示，移除语义 Token 会导致性能大幅下降，证明了显式角色标注对时间序列 ICL 至关重要。
*   **元学习红利**：多任务预训练（如填补、去噪、分离）显著增强了模型在纯预测任务上的泛化能力。

#### 7. 优点（亮点）
*   **统一框架**：首次在时间序列领域实现了真正意义上的“指令+示例”驱动的通用任务处理。
*   **分层架构**：巧妙解决了多变量、多示例带来的计算复杂度和表示混淆问题。
*   **数据合成创新**：提出的多变量关系合成逻辑（如滞后影响、共振、冲击注入）为训练强大的 TS 基础模型提供了高质量数据源。

#### 8. 不足与局限
*   **算力透明度**：缺乏训练资源消耗的描述，难以评估其训练成本。
*   **示例依赖性**：ICL 性能高度依赖于所选示例的质量和相关性，如何自动选择最佳示例（Example Selection）在文中未深入探讨。
*   **长序列瓶颈**：尽管采用了分层架构，但处理极大量示例或极长历史窗口时，显存占用和计算延迟仍可能成为实际部署的障碍。

（完）
