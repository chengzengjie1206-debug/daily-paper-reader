---
title: "Rethinking Multimodal Fusion for Time Series: Auxiliary Modalities Need Constrained Fusion"
title_zh: 重新思考时间序列的多模态融合：辅助模态需要受限融合
authors: "Seunghan Lee, Jun Seo, Jaehoon Lee, Sungdong Yoo, Minjae Kim, Tae Yoon Lim, Dongwan Kang, Hwanil Choi, SoonYoung Lee, Wonbin Ahn"
date: 2026-03-23
pdf: "https://arxiv.org/pdf/2603.22372v1"
tags: ["query:ts"]
score: 9.0
evidence: 具有约束集成的时序预测多模态融合
tldr: 本研究针对多模态时间序列预测中辅助模态融合效果不佳的问题，指出简单的融合策略会引入无关噪声。作者提出了受限融合的理念，并设计了受控融合适配器（CFA）。CFA作为一种即插即用的低秩适配器，能在不改变原有模型架构的前提下过滤无关文本信息，实现更精准的跨模态对齐，显著提升了预测性能。
selection_source: fresh_fetch
motivation: 发现简单的多模态融合策略往往因引入辅助模态中的无关信息，导致模型性能甚至低于单模态时间序列模型。
method: 提出受控融合适配器（CFA），通过低秩适配器在融合前过滤无关文本特征，实现与时间序列动态对齐的受限交互。
result: 在多种数据集和模型架构上的2万多次实验证明，CFA及受限融合方法一致优于传统的简单融合策略。
conclusion: 对辅助模态进行受限融合是提升多模态时间序列预测性能的关键，CFA为该领域提供了一个通用且高效的插件式方案。
---

## 摘要
近期多模态学习的进展推动了将文本或视觉等辅助模态集成到时间序列（TS）预测中。然而，大多数现有方法带来的收益有限，通常仅在特定数据集上提高性能，或者依赖于限制泛化能力的特定架构设计。在本文中，我们展示了采用朴素融合策略（如简单的加法或拼接）的多模态模型往往表现不如单模态 TS 模型，我们将此归因于辅助模态的无控集成，这可能会引入无关信息。受此观察启发，我们探索了旨在控制此类集成的各种受限融合方法，并发现它们始终优于朴素融合方法。此外，我们提出了受控融合适配器（CFA），这是一种简单的插件式方法，能够在不修改 TS 骨干网络的情况下实现受控的跨模态交互，仅集成与 TS 动态对齐的相关文本信息。CFA 采用低秩适配器在将文本信息融合进时间表示之前过滤掉无关信息。我们在各种数据集和 TS/文本模型上进行了超过 2 万次实验，证明了包括 CFA 在内的受限融合方法的有效性。代码已公开：https://github.com/seunghan96/cfa/。

## Abstract
Recent advances in multimodal learning have motivated the integration of auxiliary modalities such as text or vision into time series (TS) forecasting. However, most existing methods provide limited gains, often improving performance only in specific datasets or relying on architecture-specific designs that limit generalization. In this paper, we show that multimodal models with naive fusion strategies (e.g., simple addition or concatenation) often underperform unimodal TS models, which we attribute to the uncontrolled integration of auxiliary modalities which may introduce irrelevant information. Motivated by this observation, we explore various constrained fusion methods designed to control such integration and find that they consistently outperform naive fusion methods. Furthermore, we propose Controlled Fusion Adapter (CFA), a simple plug-in method that enables controlled cross-modal interactions without modifying the TS backbone, integrating only relevant textual information aligned with TS dynamics. CFA employs low-rank adapters to filter irrelevant textual information before fusing it into temporal representations. We conduct over 20K experiments across various datasets and TS/text models, demonstrating the effectiveness of the constrained fusion methods including CFA. Code is publicly available at: https://github.com/seunghan96/cfa/.

---

## 论文详细总结（自动生成）

以下是对论文《Rethinking Multimodal Fusion for Time Series: Auxiliary Modalities Need Constrained Fusion》的结构化总结：

### 1. 论文的核心问题与整体含义
*   **研究动机**：随着大语言模型（LLM）的发展，将文本等辅助模态引入时间序列（TS）预测成为趋势。然而，作者发现采用简单的加法（Additive）或拼接（Concatenation）等“朴素融合”策略，往往导致多模态模型的表现甚至不如仅使用时间序列的单模态模型。
*   **核心问题**：朴素融合忽略了模态间的对齐难度，辅助模态（如文本）中包含的无关或冲突信息会干扰核心的时间表示学习。因此，论文探讨了如何通过“受限融合”（Constrained Fusion）来有效集成辅助信息。

### 2. 论文提出的方法论
*   **核心思想**：提出**受控融合适配器（Controlled Fusion Adapter, CFA）**。这是一种即插即用的插件式模块，其核心逻辑是：时间序列是主模态，文本是辅助模态，融合过程必须受到约束，以过滤无关信号并保护 TS 骨干网络的表示。
*   **关键技术细节**：
    *   **低秩瓶颈（Low-rank Bottleneck）**：借鉴 LoRA 的思想，将高维文本嵌入先通过下投影矩阵 $W_{down}$ 压缩到低维空间（瓶颈层），再通过上投影矩阵 $W_{up}$ 还原。
    *   **受限交互**：通过低秩约束限制文本信号的表达能力，迫使模型仅保留与 TS 动态相关的关键上下文。
    *   **残差连接**：将处理后的文本信号作为微小的残差项加到 TS 嵌入中，且 $W_{up}$ 初始化接近于零，确保训练初期不破坏 TS 骨干的稳定性。
*   **算法流程**：文本经编码器生成嵌入 -> 进入 CFA 适配器（下投影 -> ReLU/LayerNorm -> 上投影）-> 与 TS 编码器各层的隐藏表示进行残差相加 -> 输出预测。

### 3. 实验设计
*   **数据集/场景**：使用了 **Time-MMD** 基准中的 9 个多领域多模态数据集，涵盖农业、气候、经济、能源、环境、公共卫生、安全、社会公益和交通。
*   **Benchmark 与对比方法**：
    *   **单模态基准**：14 种主流 TS 模型（如 PatchTST, iTransformer, DLinear, Koopa 等）。
    *   **融合策略对比**：对比了 6 种朴素融合（加法/拼接分别在 First/Middle/Last 层）和 4 种受限融合（Gating 门控、FiLM 线性调制、Orthogonal 正交融合以及本文的 CFA）。
    *   **文本模型**：使用了 BERT、GPT2、Llama3 和 Doc2Vec 四种编码器。

### 4. 资源与算力
*   **算力说明**：论文中**未明确提及**具体的 GPU 型号、数量或总训练时长。
*   **规模暗示**：作者提到进行了超过 **20,000 次**实验配置（14 TS 模型 × 4 文本模型 × 9 数据集 × 4 预测长度 × 10 种融合策略），并对每种设置进行了 10 个学习率的超参数搜索，这暗示了背后有庞大的计算资源支持。

### 5. 实验数量与充分性
*   **实验充分性**：实验规模极其宏大，覆盖了从 Transformer 到线性模型的各类架构，以及从轻量级到大语言模型的各类文本编码器。
*   **客观性与公平性**：
    *   通过“无关文本实验”（用错误数据集的文本进行测试）验证了方法的鲁棒性。
    *   所有对比方法均经过了相同的学习率调优，确保了比较的公平性。
    *   使用了 MSE 和 MAE 双重指标，并在多个预测步长（Horizons）下取平均值。

### 6. 论文的主要结论与发现
*   **朴素融合的负面影响**：在超过 2000 种设置中，朴素融合（尤其是拼接）经常导致性能下降甚至模型不收敛（Divergence）。
*   **受限融合的必要性**：受限融合策略（如 CFA、Gating）一致性地优于朴素融合。
*   **CFA 的优越性**：CFA 在 9 个数据集中的 7 个排名第一，在 14 个 TS 模型中的 13 个上实现了性能提升，且对无关文本的干扰表现出极强的鲁棒性。
*   **表示分布**：发现预测准确性与隐藏表示的“有效秩”（Effective Rank）呈正相关，即更分散、信息量更大的表示有助于预测。

### 7. 优点
*   **即插即用（Plug-in）**：无需修改 TS 骨干网络架构，适用于任何现有的单模态 TS 模型。
*   **参数高效**：CFA 仅增加约 0.61% 的参数量和 0.04% 的 FLOPs，计算开销极低。
*   **鲁棒性强**：通过低秩瓶颈有效解决了多模态学习中常见的“噪声引入”问题。
*   **验证详尽**：2 万次实验提供了极具说服力的实证证据。

### 8. 不足与局限
*   **模态局限性**：目前研究主要集中在“文本+时间序列”，尚未深入探讨视觉或表格数据作为辅助模态时的表现。
*   **理论深度**：虽然提供了低秩子空间的理论视角，但对于“为什么低秩能自动过滤噪声”的深层数学解释仍以经验性分析为主。
*   **任务范围**：实验仅限于长短期时间序列预测，未涉及分类、异常检测等其他 TS 任务。

（完）
