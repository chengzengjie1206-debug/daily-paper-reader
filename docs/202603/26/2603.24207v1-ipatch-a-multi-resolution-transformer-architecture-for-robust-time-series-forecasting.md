---
title: "IPatch: A Multi-Resolution Transformer Architecture for Robust Time-Series Forecasting"
title_zh: IPatch：一种用于稳健时间序列预测的多分辨率 Transformer 架构
authors: "Aymane Harkati, Moncef Garouani, Olivier Teste, Julien Aligon, Mohamed Hamlich"
date: 2026-03-25
pdf: "https://arxiv.org/pdf/2603.24207v1"
tags: ["query:ts"]
score: 10.0
evidence: 用于多变量时间序列预测的多分辨率Transformer架构
tldr: 针对多元时间序列预测中捕捉短期波动与长期依赖的挑战，本文提出IPatch架构。该模型结合了点级（point-wise）和块级（patch-wise）表示，通过多分辨率Transformer同时建模细粒度细节与宏观上下文。实验证明，IPatch在7个基准数据集上显著提升了预测精度、抗噪能力和泛化性能，有效解决了单一表示在效率与精度间的权衡问题。
selection_source: fresh_fetch
motivation: 传统Transformer在时间序列预测中面临点级表示计算开销大且难以捕捉长程依赖，而块级表示虽高效却易丢失关键细粒度信息的矛盾。
method: 提出IPatch架构，通过集成点级和块级Token，在多分辨率下同时建模时间序列的局部动态与全局上下文。
result: 在7个基准数据集上的实验结果显示，IPatch在预测准确性、鲁棒性和跨预测周期的泛化能力上均优于单一表示的基准模型。
conclusion: IPatch证明了多分辨率表示在提升时间序列预测模型性能方面的有效性，为平衡细粒度特征与长程依赖提供了新思路。
---

## 摘要
由于需要同时捕捉短期波动和长期时间依赖关系，多变量时间序列的准确预测仍然具有挑战性。基于 Transformer 的模型已成为一种强大的方法，但其性能关键取决于时间数据的表示方式。传统的逐点（point-wise）表示保留了单个时间步的信息，从而实现细粒度建模，但其计算成本往往较高，且在建模更广泛的上下文依赖关系方面效果较差，限制了其在长序列上的可扩展性。逐块（patch-wise）表示将连续的时间步聚合为紧凑的标记（tokens），以提高效率并建模局部时间动态，但它们往往会丢弃细粒度的时间细节，而这些细节对于波动较大或复杂的时间序列的准确预测至关重要。我们提出了 IPatch，这是一种多分辨率 Transformer 架构，它集成了逐点和逐块标记，在多个分辨率下对时间信息进行建模。在 7 个基准数据集上的实验表明，与单一表示的基准模型相比，IPatch 在各种预测周期内一致地提高了预测准确性、对噪声的稳健性以及泛化能力。

## Abstract
Accurate forecasting of multivariate time series remains challenging due to the need to capture both short-term fluctuations and long-range temporal dependencies. Transformer-based models have emerged as a powerful approach, but their performance depends critically on the representation of temporal data. Traditional point-wise representations preserve individual time-step information, enabling fine-grained modeling, yet they tend to be computationally expensive and less effective at modeling broader contextual dependencies, limiting their scalability to long sequences. Patch-wise representations aggregate consecutive steps into compact tokens to improve efficiency and model local temporal dynamics, but they often discard fine-grained temporal details that are critical for accurate predictions in volatile or complex time series. We propose IPatch, a multi-resolution Transformer architecture that integrates both point-wise and patch-wise tokens, modeling temporal information at multiple resolutions. Experiments on 7 benchmark datasets demonstrate that IPatch consistently improves forecasting accuracy, robustness to noise, and generalization across various prediction horizons compared to single-representation baselines.

---

## 论文详细总结（自动生成）

这是一份关于论文《IPatch: A Multi-Resolution Transformer Architecture for Robust Time-Series Forecasting》的结构化总结：

### 1. 论文的核心问题与整体含义
*   **研究动机**：在多变量时间序列预测中，Transformer 模型面临“表示粒度”的权衡难题。
    *   **逐点表示（Point-wise）**：保留了细粒度细节，但计算开销大，且难以捕捉宏观上下文。
    *   **逐块表示（Patch-wise）**：通过聚合时间步提高了效率并捕捉了局部语义，但不可避免地丢失了对波动剧烈或复杂序列至关重要的细粒度细节。
*   **核心目标**：提出一种名为 **IPatch** 的混合架构，旨在统一这两种表示方式，在不显著增加计算负担的前提下，同时建模局部精细动态和全局长程依赖。

### 2. 论文提出的方法论
*   **核心思想**：采用**双流（Dual-Stream）架构**，并行处理块级全局信息和点级局部细节。
*   **关键技术细节**：
    *   **多分辨率 Tokenization**：将输入序列划分为均匀的 Patch，并进行位置编码。
    *   **块间注意力流（Inter-patch Attention）**：使用标准 Transformer 编码器处理 Patch Token，捕捉不同时间段之间的宏观趋势和长程依赖。
    *   **块内自相关流（Intra-patch Autocorrelation）**：在每个 Patch 内部进行点级建模。利用**快速傅里叶变换（FFT）**在频率域检测周期性模式，并通过 **Roll（滚动）操作**对齐重复模式。
    *   **Fourier-KAN (FKAN)**：在自相关模块中，使用基于傅里叶基函数的 Kolmogorov-Arnold Network (KAN) 代替传统的 Softmax。FKAN 允许学习正向和负向的贡献，对周期性有更强的自适应控制力。
    *   **特征融合**：将两个流的输出拼接（Concatenate）后展平，通过线性层映射到最终的预测长度。

### 3. 实验设计
*   **数据集**：涵盖 7 个主流基准数据集：ETT（ETTh1, ETTh2, ETTm1, ETTm2）、Electricity（电力）、Weather（天气）和 ILI（流感病例）。
*   **预测任务**：长程时间序列预测。预测窗口 $H$ 在 ETT/电力/天气上为 $\{96, 192, 336, 720\}$，在 ILI 上为 $\{24, 36, 48, 60\}$。
*   **对比方法（Baselines）**：
    *   **Transformer 类**：PatchTST, FEDformer, Autoformer, Informer, Crossformer。
    *   **线性/混合类**：DLinear, TimeMixer, TimesNet, MICN。
    *   **分解类**：FiLM, Stationary。

### 4. 资源与算力
*   **算力说明**：文中**未明确提及**具体的 GPU 型号、数量或总训练时长。
*   **效率评估**：论文通过图表展示了 IPatch 在 ILI 数据集上的训练时间（对数刻度）与 MSE 的关系。结果显示其训练速度远快于 FEDformer 和 Crossformer 等复杂 Transformer，且在精度上优于轻量级的 DLinear。

### 5. 实验数量与充分性
*   **实验规模**：
    *   **主实验**：在 7 个数据集、4 个预测周期下进行了 28 组对比实验。
    *   **消融实验**：验证了“仅 Patch”、“仅自相关”以及“不同激活函数（FKAN vs Softmax）”的影响。
    *   **可视化实验**：提供了预测曲线的定性对比。
*   **客观性与公平性**：使用了统一的 **TFB 评测框架**，确保了预处理、数据分割的一致性。所有基准模型均经过了独立的超参数优化，以确保对比的公平性。

### 6. 论文的主要结论与发现
*   **性能领先**：IPatch 在 28 种配置中，MAE 有 26 种达到最优，MSE 有 21 种达到最优。
*   **鲁棒性强**：在噪声较大且数据稀疏的 ILI 数据集上，IPatch 的提升最为显著（MSE 提升高达 24%），证明了结合点级细节对处理波动数据的有效性。
*   **组件互补**：消融实验证明，单纯的 Patch 或自相关模块性能均不如混合架构，FKAN 激活函数在长程预测中比 Softmax 更有优势（如 ETTh1 提升 21%）。

### 7. 优点（亮点）
*   **多分辨率融合**：巧妙地解决了 Patch 机制带来的信息丢失问题，实现了精度与效率的平衡。
*   **引入 FKAN**：将新兴的 KAN 网络与傅里叶变换结合，增强了模型对频域特征的表达能力。
*   **泛化性好**：在不同领域（能源、气象、医疗）和不同时间频率（15分钟到1周）的数据上均表现稳健。

### 8. 不足与局限
*   **超参数敏感性**：虽然论文提到了 Patch 大小的影响，但未深入探讨在不同数据集下如何自动选择最优的 Patch 长度。
*   **计算复杂度**：虽然比全注意力模型快，但双流架构相比单流模型（如 PatchTST）或线性模型（如 DLinear）仍增加了模型参数量和实现复杂度。
*   **应用限制**：实验主要集中在长程预测，对于极短期的实时流数据预测表现尚未验证。

（完）
