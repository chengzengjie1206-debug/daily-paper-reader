---
title: Exploring Multimodal Prompts For Unsupervised Continuous Anomaly Detection
title_zh: 探索用于无监督连续异常检测的多模态提示
authors: "Mingle Zhou, Jiahui Liu, Jin Wan, Gang Li, Min Li"
date: 2026-03-23
pdf: "https://arxiv.org/pdf/2603.21562v1"
tags: ["query:ts"]
score: 9.0
evidence: 无监督连续异常检测框架
tldr: 针对无监督连续异常检测中仅依赖视觉信息导致建模不足的问题，本文提出多模态提示框架。通过持续多模态提示记忆库提取视觉与文本原型，并利用缺陷语义引导的自适应融合机制提升精度与鲁棒性。实验证明该方法在MVTec AD和VisA数据集上达到了SOTA性能。
selection_source: fresh_fetch
motivation: 现有的连续异常检测方法仅利用视觉信息，难以在复杂场景下准确捕捉正常模式的流形分布。
method: 提出包含持续多模态提示记忆库（CMPMB）和缺陷语义引导自适应融合机制（DSG-AFM）的框架，整合视觉与文本特征。
result: 在MVTec AD和VisA基准测试中，该方法在图像级AUROC和像素级AUPR指标上均取得了最先进的性能。
conclusion: 引入多模态提示能显著增强连续异常检测的表征能力和鲁棒性，为解决UCAD中的遗忘与精度平衡问题提供了新思路。
---

## 摘要
无监督连续异常检测 (UCAD) 因能有效解决传统无监督异常检测 (UAD) 中的灾难性遗忘和沉重计算负担问题而备受关注。然而，现有的仅依赖视觉信息的 UCAD 方法不足以捕捉复杂场景中的正常流形，从而阻碍了异常检测准确率的进一步提升。为了克服这一局限性，我们提出了一种基于多模态提示的无监督连续异常检测框架。具体而言，我们引入了一个连续多模态提示记忆库 (CMPMB)，它在连续任务中逐步提取并保留来自视觉和文本领域的原型正常模式，从而产生更丰富的正常表示。此外，我们设计了一种缺陷语义引导的自适应融合机制 (DSG-AFM)，该机制集成了自适应归一化模块 (ANM) 和动态融合策略 (DFS)，以共同增强检测准确率和对抗鲁棒性。在 MVTec AD 和 VisA 数据集上的基准实验表明，我们的方法在图像级 AUROC 和像素级 AUPR 指标上达到了最先进 (SOTA) 的性能。

## Abstract
Unsupervised Continuous Anomaly Detection (UCAD) is gaining attention for effectively addressing the catastrophic forgetting and heavy computational burden issues in traditional Unsupervised Anomaly Detection (UAD). However, existing UCAD approaches that rely solely on visual information are insufficient to capture the manifold of normality in complex scenes, thereby impeding further gains in anomaly detection accuracy. To overcome this limitation, we propose an unsupervised continual anomaly detection framework grounded in multimodal prompting. Specifically, we introduce a Continual Multimodal Prompt Memory Bank (CMPMB) that progressively distills and retains prototypical normal patterns from both visual and textual domains across consecutive tasks, yielding a richer representation of normality. Furthermore, we devise a Defect-Semantic-Guided Adaptive Fusion Mechanism (DSG-AFM) that integrates an Adaptive Normalization Module (ANM) with a Dynamic Fusion Strategy (DFS) to jointly enhance detection accuracy and adversarial robustness. Benchmark experiments on MVTec AD and VisA datasets show that our approach achieves state-of-the-art (SOTA) performance on image-level AUROC and pixel-level AUPR metrics.

---

## 论文详细总结（自动生成）

这篇论文针对工业视觉检测中的**无监督连续异常检测（UCAD）**问题，提出了一种结合多模态提示的新型框架。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：传统的无监督异常检测（UAD）在面对连续任务时，存在**灾难性遗忘**和**计算开销巨大**的问题。
*   **研究动机**：现有的连续异常检测方法（如 UCAD）主要依赖单一的视觉信息，难以在复杂工业场景中准确捕捉“正常模式”的流形分布。作者认为，引入文本语义等多模态信息可以提供更丰富的先验指导，从而突破性能瓶颈。

### 2. 方法论：核心思想与关键技术
论文提出了一个基于多模态提示的连续学习框架，包含两个核心组件：
*   **持续多模态提示记忆库 (CMPMB)**：
    *   **核心思想**：通过一个四元组 $M = (K, P_T, P_V, F)$ 来存储和更新知识。
    *   **关键技术**：
        *   **任务标识符 ($K$)**：利用最远点采样（FPS）提取特征，用于在推理时快速识别当前任务。
        *   **可学习文本提示 ($P_T$)**：基于 CLIP 架构，通过“a photo of a [class] with [learnable prompt]”模板，利用噪声增强样本进行对比学习，建模正常模式的语义分布。
        *   **精细化视觉提示 ($P_V$)**：采用前缀微调（Prefix Tuning）技术，将可学习向量注入预训练视觉骨干网络的每一层。
        *   **正常特征库 ($F$)**：使用核心集采样（Coreset Sampling）压缩并存储正常样本特征。
*   **缺陷语义引导的自适应融合机制 (DSG-AFM)**：
    *   **自适应归一化模块 (ANM)**：通过贪婪搜索策略动态调整 Sigmoid 函数的中心位置（参数 $b$），以拉开正常与异常区域的分数差距。
    *   **动态融合策略 (DFS)**：将视觉分支（基于特征库对比）和文本分支（基于跨模态相似度）生成的异常图进行加权融合（权重参数 $\alpha$）。

### 3. 实验设计
*   **数据集**：使用了两个主流工业异常检测基准：**MVTec AD**（15类工业品）和 **VisA**（12类复杂工业场景，目前最大的像素级标注数据集）。
*   **对比方法**：对比了 14 种方法，涵盖了不同范式：
    *   传统 UAD：PatchCore, SPADE, PaDiM, DRAEM 等。
    *   统一模型（Unified）：UniAD, SimpleNet。
    *   连续学习基准：UCAD（之前的 SOTA）、DNE，以及经过改造支持连续学习的 PatchCore* 和 UniAD*。
*   **评价指标**：图像级 AUROC（分类准确度）、像素级 AUPR（分割精度）以及遗忘测量值（FM，评估抗遗忘能力）。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号和数量。
*   **训练细节**：提到了使用 Adam 优化器，学习率为 0.00005，动量 0.9，Batch Size 为 8，训练轮数为 50 Epochs。骨干网络采用了预训练的 CLIP (ViT-B/16)。

### 5. 实验数量与充分性
*   **实验规模**：在两个数据集共 27 个类别上进行了详尽测试。
*   **消融实验**：针对 CMPMB、DSG-AFM、文本提示、视觉提示、融合系数 $\alpha$、归一化参数 $k$ 以及 ViT 特征层选择（发现第 5 层效果最好）均做了独立验证。
*   **充分性评价**：实验设计较为全面，不仅对比了最终性能，还通过 FM 指标客观评估了连续学习中的稳定性。对比基准包含了最新的 SOTA 方法，实验结果具有较高的说服力。

### 6. 主要结论与发现
*   **性能突破**：在 MVTec AD 上，该方法比之前的 SOTA（UCAD）在检测准确率上提升了 **4.4%**，在分割准确率（AUPR）上大幅提升了 **14.8%**。
*   **多模态优势**：实验证明，文本提示能显著增强模型对正常模式语义的理解，尤其在像素级定位上表现优异。
*   **平衡性**：虽然引入文本信息使遗忘率（FM）略有波动，但整体性能增益远超其副作用，实现了塑性（学习新知识）与稳定性（保留旧知识）的良好平衡。

### 7. 优点与亮点
*   **创新性**：首次将多模态提示学习引入无监督连续异常检测领域。
*   **自适应性**：DSG-AFM 机制能够根据不同任务的分布差异动态调整归一化参数，增强了模型的鲁棒性。
*   **实用性**：无需存储大量原始样本（仅存储提示向量和压缩特征），符合工业场景对存储效率的要求。

### 8. 不足与局限
*   **遗忘率略增**：引入文本提示后，FM 指标在某些任务上略高于纯视觉方法，表明多模态信息的引入可能对旧任务的稳定性产生轻微干扰。
*   **依赖预训练模型**：高度依赖 CLIP 等大规模预训练模型的特征表达能力，若预训练模型未覆盖某些特殊工业领域，效果可能受限。
*   **模态扩展有限**：目前仅整合了视觉和文本，尚未探索声音、传感器等其他工业模态。

（完）
