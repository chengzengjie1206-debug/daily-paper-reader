---
title: Embracing Heteroscedasticity for Probabilistic Time Series Forecasting
title_zh: 拥抱异方差性以实现概率时间序列预测
authors: "Yijun Wang, Qiyuan Zhuang, Xiu-Shen Wei"
date: 2026-03-25
pdf: "https://arxiv.org/pdf/2603.24254v1"
tags: ["query:ts"]
score: 10.0
evidence: 考虑异方差性的概率时间序列预测
tldr: 针对概率时间序列预测中现实数据普遍存在的异方差性（随时间变化的方差），本文指出传统非自回归生成模型因采用MSE损失而隐含同方差假设的局限。为此，提出LSG-VAE框架，通过位置-尺度似然显式建模预测均值和时变方差。该方法不仅能准确捕捉偶然不确定性，还通过自适应衰减机制提升了趋势预测的鲁棒性。实验证明其在9个基准数据集上优于15种强基准模型，且具备高效的实时部署能力。
selection_source: fresh_fetch
motivation: 现有非自回归生成模型多采用基于MSE的训练目标，隐含了同方差假设，无法有效处理现实时间序列中随时间变化的异方差性。
method: 提出LSG-VAE框架，利用位置-尺度高斯似然显式参数化预测均值和时变方差，并引入自适应衰减机制以降低高波动观测值对训练的影响。
result: 在9个基准数据集上的广泛实验表明，LSG-VAE在预测精度和不确定性量化方面一致优于15种先进的生成式基准模型。
conclusion: LSG-VAE通过显式建模异方差性，在保持高计算效率的同时，显著提升了概率时间序列预测的准确性和鲁棒性。
---

## 摘要
概率时间序列预测 (PTSF) 旨在对未来观测值的完整预测分布进行建模，从而实现准确的预测和有原则的不确定性量化。PTSF 的一个核心要求是拥抱异方差性，因为现实世界的时间序列由于非平稳动力学、状态转变和不断变化的外部条件而表现出随时间变化的条件方差。然而，大多数现有的非自回归生成式 PTSF 方法（如 TimeVAE 和 $K^2$VAE）依赖于基于均方误差 (MSE) 的训练目标，这些目标隐含地施加了同方差假设，从而从根本上限制了它们建模时间异方差性的能力。为了解决这一局限性，我们提出了位置-尺度高斯变分自编码器 (LSG-VAE)，这是一个简单但有效的框架，通过位置-尺度似然公式显式地对预测均值和随时间变化的方差进行参数化。这种设计使 LSG-VAE 能够忠实地捕捉异方差偶然不确定性，并引入了一种自适应衰减机制，在训练过程中自动降低高波动观测值的权重，从而提高了趋势预测的鲁棒性。在九个基准数据集上的广泛实验表明，LSG-VAE 在保持适用于实时部署的高计算效率的同时，始终优于十五个强劲的生成式基准模型。

## Abstract
Probabilistic time series forecasting (PTSF) aims to model the full predictive distribution of future observations, enabling both accurate forecasting and principled uncertainty quantification. A central requirement of PTSF is to embrace heteroscedasticity, as real-world time series exhibit time-varying conditional variances induced by nonstationary dynamics, regime changes, and evolving external conditions. However, most existing non-autoregressive generative approaches to PTSF, such as TimeVAE and $K^2$VAE, rely on MSE-based training objectives that implicitly impose a homoscedastic assumption, thereby fundamentally limiting their ability to model temporal heteroscedasticity. To address this limitation, we propose the Location-Scale Gaussian VAE (LSG-VAE), a simple but effective framework that explicitly parameterizes both the predictive mean and time-dependent variance through a location-scale likelihood formulation. This design enables LSG-VAE to faithfully capture heteroscedastic aleatoric uncertainty and introduces an adaptive attenuation mechanism that automatically down-weights highly volatile observations during training, leading to improved robustness in trend prediction. Extensive experiments on nine benchmark datasets demonstrate that LSG-VAE consistently outperforms fifteen strong generative baselines while maintaining high computational efficiency suitable for real-time deployment.

---

## 论文详细总结（自动生成）

### 论文结构化总结：拥抱异方差性以实现概率时间序列预测

#### 1. 核心问题与整体含义（研究动机和背景）
概率时间序列预测（PTSF）的核心挑战在于如何准确建模未来观测值的完整分布，并量化不确定性。现实世界的数据通常具有**异方差性（Heteroscedasticity）**，即条件方差随时间动态变化（如金融市场的波动或极端天气）。
*   **研究动机**：现有的非自回归生成模型（如 TimeVAE, $K^2$VAE）大多采用基于均方误差（MSE）的训练目标。从数学上讲，最小化 MSE 隐含了**同方差假设**（假设方差恒定）。
*   **核心局限**：这种假设导致模型在面对高波动数据时，均值预测器会过度拟合随机噪声，且无法提供可靠的置信区间。

#### 2. 论文提出的方法论：LSG-VAE
论文提出了 **位置-尺度高斯变分自编码器（LSG-VAE）**，旨在显式建模时变方差。
*   **核心思想**：将传统的 MSE 损失替换为基于**位置-尺度（Location-Scale）**的高斯似然函数，同时预测均值（位置 $\mu$）和标准差（尺度 $\sigma$）。
*   **关键技术细节**：
    1.  **补丁化编码（Patching）**：将输入序列分段以捕捉局部语义。
    2.  **非自回归潜动力学**：通过一个轻量级的全局投影（MLP）将过去隐藏状态直接映射到未来，避免了自回归模型的误差累积和高延迟。
    3.  **双头解码器**：解码器分为“位置头”和“尺度头”。位置头预测趋势，尺度头通过 Softplus 激活函数预测瞬时波动率。
    4.  **自适应衰减机制（Adaptive Attenuation）**：在优化高斯负对数似然（NLL）时，梯度的权重与 $1/\sigma^2$ 成正比。这意味着当模型遇到高波动或异常值时，会自动增大 $\sigma$，从而降低该点对均值学习的梯度贡献，使模型对噪声更具鲁棒性。

#### 3. 实验设计
*   **数据集**：涵盖 9 个主流真实世界基准数据集，包括能源（ETT 系列、Electricity）、交通（Traffic）、天气（Weather）、经济（Exchange）和医疗（ILI）。
*   **对比方法（15 种）**：
    *   **点预测增强模型**：PatchTST, iTransformer, Koopa, FITS（均外接高斯输出头）。
    *   **生成式模型**：扩散模型（TimeGrad, CSDI, NsDiff, DiffusionTS, TMDM）、流模型（GRU-NVP/MAF）以及变分自编码器（$K^2$VAE）。
*   **评估指标**：CRPS（衡量分布拟合质量）、NMAE（衡量点预测精度）、QICE（衡量置信区间校准度）。

#### 4. 资源与算力
*   **硬件环境**：实验在一台配备 **NVIDIA GeForce RTX 4090 (24GB)** 的服务器上运行。
*   **软件框架**：基于 PyTorch 实现。
*   **训练细节**：默认 Batch Size 为 32，使用 Adam 优化器，学习率在 $10^{-4}$ 到 $10^{-3}$ 之间，采用早停策略（Patience=10）。

#### 5. 实验数量与充分性
*   **实验规模**：论文进行了大规模的实验验证，包括：
    *   **长程预测对比**：在 9 个数据集上测试了 4 种不同的预测长度（96, 192, 336, 720）。
    *   **生成质量评估**：遵循 NsDiff 协议验证分布校准度。
    *   **消融实验**：验证了预测损失、重构损失、RevIN 模块以及超参数（补丁长度、隐藏层维度）的影响。
    *   **泛化性分析**：将 LSG 机制作为插件引入 GAN 和 $K^2$VAE，证明了该思想的通用性。
*   **充分性评价**：实验设计非常充分且客观。通过合成数据验证了模型恢复真实波动率的能力，并使用了 Wilcoxon 符号秩检验来确保结果的统计显著性。

#### 6. 主要结论与发现
*   **性能领先**：LSG-VAE 在所有数据集的 CRPS 指标上均达到最优，显著优于复杂的扩散模型。
*   **鲁棒性增强**：即使在点预测指标（NMAE）上，LSG-VAE 也优于专门的点预测模型，证明了异方差建模能反哺均值预测的准确性。
*   **高效推理**：由于采用非自回归架构，其推理速度比扩散模型快几个数量级，且内存占用极低，适合实时部署。
*   **校准度高**：生成的置信区间能随数据波动动态“呼吸”（扩张或收缩），而非像基准模型那样产生僵硬的固定宽度区间。

#### 7. 优点
*   **理论简洁且扎实**：从变分下界（ELBO）出发，通过简单的似然函数重构解决了复杂的异方差建模问题。
*   **自适应性强**：自适应衰减机制为处理时间序列中的异常值和非平稳性提供了一种优雅的数学解决方案。
*   **通用插件属性**：实验证明 LSG 机制可以轻松集成到其他生成框架（如 GAN）中并提升性能。

#### 8. 不足与局限
*   **分布假设限制**：模型假设残差服从高斯分布。虽然在大多数场景有效，但对于具有多峰分布（Multimodal）或极端重尾分布的复杂数据，单一高斯分布可能拟合不足。
*   **应用场景**：主要侧重于长程预测，对于极短程或极高频的金融秒级交易数据的适用性尚未深入探讨。
*   **依赖标准化**：实验显示模型高度依赖 RevIN 等标准化技术来处理全局分布偏移。

（完）
