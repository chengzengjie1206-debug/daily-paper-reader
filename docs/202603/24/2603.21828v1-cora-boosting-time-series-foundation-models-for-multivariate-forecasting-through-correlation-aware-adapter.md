---
title: "CoRA: Boosting Time Series Foundation Models for Multivariate Forecasting through Correlation-aware Adapter"
title_zh: CoRA：通过相关性感知适配器提升时间序列基础模型的多变量预测性能
authors: "Hanyin Cheng, Xingjian Wu, Yang Shu, Zhongwen Rao, Lujia Pan, Bin Yang, Chenjuan Guo"
date: 2026-03-23
pdf: "https://arxiv.org/pdf/2603.21828v1"
tags: ["query:ts"]
score: 10.0
evidence: 多变量时间序列预测和通道相关性
tldr: 针对现有时间序列大模型（TSFM）在多变量预测中忽视通道间相关性的问题，本文提出了一种轻量级即插即用适配器CoRA。CoRA通过将相关性矩阵分解为低秩的时变和时不变分量，并利用可学习多项式捕捉动态模式，同时引入双重对比学习机制来识别通道间的正负相关性。在10个真实数据集上的实验证明，CoRA能显著提升TSFM在多变量预测任务中的性能。
selection_source: fresh_fetch
motivation: 现有的时间序列大模型多采用通道独立建模，忽略了对多变量预测至关重要的通道间相关性。
method: 提出CoRA适配器，通过低秩分解捕捉时变与时不变相关性，并结合双重对比学习优化通道间的关联建模。
result: 在10个真实世界数据集上的广泛实验表明，CoRA能有效增强多种TSFM在多变量预测任务中的表现。
conclusion: CoRA作为一种高效的轻量级方案，成功弥补了现有大模型在处理复杂多变量相关性方面的短板。
---

## 摘要
大多数现有的时间序列基础模型（TSFMs）采用通道独立建模，专注于捕捉和泛化时间依赖关系，而忽略了通道间的相关性或忽视了相关性的不同方面。然而，这些相关性在多变量时间序列预测中起着至关重要的作用。为此，我们提出了一种相关性感知适配器（CoRA），这是一种轻量级的即插即用方法，仅需对 TSFMs 进行微调，并能够捕捉不同类型的相关性，从而提高预测性能。具体而言，为了降低复杂度，我们创新性地将相关矩阵分解为低秩的时变（Time-Varying）和时不变（Time-Invariant）组件。对于时变组件，我们进一步设计了可学习的多项式，通过捕捉趋势或周期性模式来学习动态相关性。为了学习仅出现在部分通道之间的正负相关性，我们引入了一种新颖的双重对比学习方法，通过投影层识别相关性，并在训练过程中由异构局部（Heterogeneous-Partial）对比损失进行调节，且不会在推理阶段引入额外的复杂度。在 10 个真实世界数据集上的广泛实验表明，CoRA 可以提升 TSFMs 在多变量预测中的性能。

## Abstract
Most existing Time Series Foundation Models (TSFMs) use channel independent modeling and focus on capturing and generalizing temporal dependencies, while neglecting the correlations among channels or overlooking the different aspects of correlations. However, these correlations play a vital role in Multivariate time series forecasting. To address this, we propose a CoRrelation-aware Adapter (CoRA), a lightweight plug-and-play method that requires only fine-tuning with TSFMs and is able to capture different types of correlations, so as to improve forecast performance. Specifically, to reduce complexity, we innovatively decompose the correlation matrix into low-rank Time-Varying and Time-Invariant components. For the Time-Varying component, we further design learnable polynomials to learn dynamic correlations by capturing trends or periodic patterns. To learn positive and negative correlations that appear only among some channels, we introduce a novel dual contrastive learning method that identifies correlations through projection layers, regulated by a Heterogeneous-Partial contrastive loss during training, without introducing additional complexity in the inference stage. Extensive experiments on 10 real-world datasets demonstrate that CoRA can improve TSFMs in multivariate forecasting performance.

---

## 论文详细总结（自动生成）

以下是对论文《CoRA: Boosting Time Series Foundation Models for Multivariate Forecasting through Correlation-aware Adapter》的结构化总结：

### 1. 核心问题与整体含义
*   **研究背景**：当前的时间序列基础模型（TSFMs）在预训练时多采用“通道独立（Channel Independent）”策略，以增强跨域泛化能力。
*   **核心问题**：在多变量时间序列预测中，通道间的相关性（Correlation）至关重要。现有的 TSFMs 往往忽略了相关性的三个关键维度：**动态相关性**（随时间变化）、**异构相关性**（正负相关并存）和**局部相关性**（仅部分通道相关）。
*   **研究意义**：本文旨在设计一种轻量级、即插即用的适配器（Adapter），在不改变预训练模型参数的情况下，通过微调使 TSFMs 具备捕捉复杂多变量相关性的能力。

### 2. 方法论
CoRA（Correlation-aware Adapter）的核心思想是通过解耦和对比学习来高效建模相关性：
*   **动态相关性估计（DCE）**：
    *   **低秩分解**：将相关矩阵分解为“时变（Time-Varying）”和“时不变（Time-Invariant）”两个低秩组件，以降低计算复杂度。
    *   **可学习时间感知多项式**：利用高阶多项式基函数来拟合相关性的趋势和周期性变化，证明了其在局部平稳序列下的逼近能力。
*   **异构相关性划分（HD）**：
    *   利用通道感知投影层（基于 SE 机制），将时间序列表示投影到独立的**正相关空间**和**负相关空间**，从而分别处理不同性质的相互作用。
*   **异构局部对比学习（HPCL）**：
    *   **双重对比损失**：在训练阶段，利用 DCE 生成的相关性矩阵作为伪标签，引导模型在投影空间中拉近强相关通道、推开弱相关通道。
    *   **推理效率**：对比学习仅在训练时使用，推理阶段仅保留线性复杂度的投影层，使推理复杂度维持在 $O(N)$（$N$ 为通道数）。

### 3. 实验设计
*   **数据集**：涵盖 10 个真实世界基准数据集，包括 ETT（4个子集）、Electricity、Traffic、Solar、Weather、AQShunyi 和 ZafNoo，涉及电力、交通、能源、环境等多个领域。
*   **Benchmark 设置**：主要在 **5% Few-shot**（少量样本微调）设置下进行评估，预测长度 $H \in \{96, 192, 336, 720\}$。
*   **对比方法（Baselines）**：
    *   **基于 LLM 的模型**：GPT4TS, CALF, UniTime。
    *   **预训练时间序列模型**：Moment, Timer, TTM。
    *   **相关性插件对比**：LIFT, C-LoRA。

### 4. 资源与算力
*   **硬件环境**：实验在 **NVIDIA Tesla-A800 GPU** 上运行。
*   **算力开销**：论文强调了 CoRA 的轻量化。在 Electricity 数据集（321个通道）上，加入 CoRA 后的推理时间与原始模型几乎持平，参数量增加极小（例如在 GPT4TS 上仅增加约 1-2M 参数）。

### 5. 实验数量与充分性
*   **实验规模**：进行了跨 10 个数据集、6 种基础模型、4 种预测长度的全面测试。
*   **消融实验**：验证了 DCE、HD 和 HPCL 三个模块的各自贡献，证明了组合模块的优越性。
*   **敏感性分析**：对多项式阶数 $K$、分解秩 $M$、投影层数等超参数进行了详细讨论。
*   **客观性**：采用了统一的 TSFM-Bench 框架进行评估，不使用“Drop Last”等技巧，确保了对比的公平性。实验覆盖了从 3% 到 20% 的不同数据比例，验证了模型在极低数据量下的鲁棒性。

### 6. 主要结论与发现
*   **性能提升**：CoRA 在所有测试的基础模型上均实现了性能提升，平均降低了 MSE 和 MAE。
*   **通用性**：证明了 CoRA 是一个通用的插件，无论是对基于 LLM 的模型还是纯时间序列预训练模型都有效。
*   **效率优势**：相比于其他相关性插件（如 LIFT），CoRA 在 Few-shot 场景下表现更稳定，且推理速度更快（$O(N)$ 复杂度）。
*   **相关性建模**：可视化结果证实，CoRA 能够准确识别不同时间步下通道间的正负相关关系。

### 7. 优点
*   **轻量且高效**：通过低秩分解和多项式拟合，解决了传统通道交互方法 $O(N^2)$ 复杂度的问题。
*   **理论支撑**：为时空分解和多项式逼近提供了理论证明。
*   **即插即用**：无需重新训练庞大的基础模型，仅需微调适配器即可显著增强多变量预测能力。

### 8. 不足与局限
*   **微调依赖**：虽然是轻量级适配器，但仍需要在下游数据上进行微调，无法直接实现完全的 Zero-shot 相关性迁移。
*   **超参数敏感度**：虽然论文给出了推荐值，但在不同特征维度的序列上，多项式阶数和低秩维度的选择可能仍需手动调优。
*   **局部平稳假设**：理论证明基于序列局部平稳的假设，对于极端非平稳或突发性极强的序列，其多项式拟合效果可能受限。

（完）
