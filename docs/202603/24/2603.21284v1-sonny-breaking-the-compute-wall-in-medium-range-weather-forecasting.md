---
title: "Sonny: Breaking the Compute Wall in Medium-Range Weather Forecasting"
title_zh: Sonny：打破中程天气预报中的算力壁垒
authors: Minjong Cheon
date: 2026-03-22
pdf: "https://arxiv.org/pdf/2603.21284v1"
tags: ["query:ts"]
score: 9.0
evidence: 用于中程天气预测的高效分层 Transformer
tldr: 针对深度学习天气预报模型对计算资源需求过高的问题，本文提出了一种高效的分层Transformer模型Sonny。该模型采用两阶段StepsNet设计，通过窄慢路径模拟大气动力学，快路径整合热力学相互作用，并利用EMA技术稳定中程预测。Sonny在WeatherBench2上表现优异，且仅需单张A40显卡即可在5.5天内完成训练，显著降低了学术研究的算力门槛。
selection_source: fresh_fetch
motivation: 旨在解决现有深度学习天气预报模型因计算资源消耗巨大而导致学术研究门槛过高的问题。
method: 提出了一种名为Sonny的高效分层Transformer，核心是结合动力学与热力学建模的两阶段StepsNet架构，并辅以EMA训练策略。
result: 在WeatherBench2基准测试中，Sonny展现出与业务系统相当的竞争力，且在热带地区的长时效预测上优于FastNet。
conclusion: Sonny证明了在有限的计算预算下也能实现高性能的中程天气预报，为算力受限的团队提供了可行的技术方案。
---

## 摘要
天气预报是保护生命和基础设施免受高影响大气事件侵害的基础性问题。近年来，基于深度学习的数据驱动天气预报方法表现出强劲的性能，其准确率通常可与业务化数值系统相媲美。然而，许多现有模型依赖于大规模训练方案和计算密集型架构，这为计算资源有限的学术团队增加了实际门槛。在此，我们介绍了 Sonny，这是一种高效的分层 Transformer，它在保持具有竞争力的中程预报性能的同时，在合理的计算预算内具有可行性。Sonny 的核心是两阶段 StepsNet 设计：窄的慢速路径首先对大规模大气动力学建模，随后的全宽快速路径则整合了热力学相互作用。为了在不增加额外微调阶段的情况下稳定中程外推，我们在训练过程中应用了指数移动平均（EMA）。在 WeatherBench2 上，Sonny 展现了稳健的中程预报能力，与业务基准模型相比具有竞争力，并表现出优于 FastNet 的明显优势，特别是在延长的热带预报时效方面。在实践中，Sonny 可以在单块 NVIDIA A40 GPU 上在大约 5.5 天内训练至收敛。

## Abstract
Weather forecasting is a fundamental problem for protecting lives and infrastructure from high-impact atmospheric events. Recently, data-driven weather forecasting methods based on deep learning have demonstrated strong performance, often reaching accuracy levels competitive with operational numerical systems. However, many existing models rely on large-scale training regimes and compute-intensive architectures, which raises the practical barrier for academic groups with limited compute resources. Here we introduce Sonny, an efficient hierarchical transformer that achieves competitive medium-range forecasting performance while remaining feasible within reasonable compute budgets. At the core of Sonny is a two-stage StepsNet design: a narrow slow path first models large-scale atmospheric dynamics, and a subsequent full-width fast path integrates thermodynamic interactions. To stabilize medium-range rollout without an additional fine-tuning stage, we apply exponential moving average (EMA) during training. On WeatherBench2, Sonny yields robust medium-range forecast skill, remains competitive with operational baselines, and demonstrates clear advantages over FastNet, particularly at extended tropical lead times. In practice, Sonny can be trained to convergence on a single NVIDIA A40 GPU in approximately 5.5 days.

---

## 论文详细总结（自动生成）

以下是对论文《Sonny: Breaking the Compute Wall in Medium-Range Weather Forecasting》的结构化总结：

### 1. 核心问题与研究背景
*   **核心问题**：当前领先的数据驱动天气预报模型（如 GraphCast、Pangu-Weather）虽然准确率极高，但通常依赖大规模的计算集群（如数百块 GPU/TPU）进行训练。这种“算力壁垒”限制了资源有限的学术团队参与下一代全球预报模型的开发。
*   **研究动机**：旨在开发一种名为 **Sonny** 的小型天气模型（Small Weather Model, SWM），在大幅降低计算成本（单卡可练）的同时，保持与业务化数值天气预报（NWP）系统及主流 AI 模型相当的中程预报性能。

### 2. 方法论
Sonny 基于 **StepsNet** 架构，采用了物理启发式的分层设计：
*   **两阶段 StepsNet 设计**：
    *   **第一阶段（慢速路径/Slow Path）**：仅处理“动力学”变量（风速 U/V、位势高度 Z、气压 P）。使用较窄的隐藏维度建模大规模大气动力学骨架，降低了处理长程空间依赖时的内存开销。
    *   **第二阶段（快速路径/Fast Path）**：将第一阶段的特征与“热力学”变量（温度 T、比湿 Q）融合。在全宽度维度下捕捉复杂的非线性相互作用（如水分输送、潜热释放）。
*   **随机动力学预报（Randomized Dynamics Forecasting）**：训练时不固定时间步长，而是从 $\{6, 12, 24\}$ 小时中随机采样 $\delta t$，增强了模型对不同预报时效的适应性。
*   **EMA 训练策略**：在训练过程中对模型权重应用**指数移动平均（EMA）**（衰减因子 0.9）。这一技术有效地平滑了优化空间，无需昂贵的递归微调（Fine-tuning）即可稳定中程外推过程。
*   **损失函数**：采用物理加权损失，结合了气压权重（靠近地表权重更高）和纬度权重（处理球面网格非均匀性）。

### 3. 实验设计
*   **数据集**：使用 WeatherBench2 (WB2) 提供的 ERA5 再分析数据。
    *   **分辨率**：1.5° 空间分辨率（121 × 240 网格），6 小时时间分辨率。
    *   **变量**：包含 13 个压力层级的 5 个大气变量和 4 个地表变量。
*   **基准模型（Benchmarks）**：
    *   **业务系统**：ECMWF 的 HRES（高分辨率预报）和 Met Office GM。
    *   **AI 模型**：FastNet O96、Fengwu、Pangu-Weather、GraphCast 等（部分用于效率对比）。
*   **评估指标**：均方根误差 (RMSE) 和异常相关系数 (ACC)。
*   **案例研究**：针对 2022 年南玛都台风（热带气旋）和 2022 年埃利奥特冬暴（温带气旋）进行了个案分析。

### 4. 资源与算力
*   **硬件**：仅使用 **1 张 NVIDIA A40 GPU**（显存占用约 32 GB）。
*   **训练时长**：约 **5.5 天** 即可收敛。
*   **模型规模**：参数量仅为 **20.5M**，远小于 Pangu-Weather (256M) 或 GraphCast (36.7M)。
*   **推理效率**：在 1.5° 分辨率下，单次推理延迟约为 125.48 毫秒。

### 5. 实验数量与充分性
*   **实验覆盖**：
    *   **消融实验**：对比了使用 EMA 与不使用 EMA 的性能差异，证明 EMA 能平均降低 3.34% 的误差。
    *   **全球评估**：在 10 天预报时效内，对 9 个核心气象变量进行了全面的 RMSE 和 ACC 评估。
    *   **分区对比**：分别在北半球中纬度（NHET）、南半球中纬度（SHET）和热带地区（Tropics）进行了对比。
*   **充分性评价**：实验设计较为客观，遵循了 WeatherBench2 的标准评估协议。虽然模型规模较小，但通过与 HRES 和 FastNet 的直接对比，充分证明了其在受限算力下的优越性。

### 6. 主要结论与发现
*   **性能竞争力**：Sonny 在中程预报（10天）中表现稳健，ACC 指标与 HRES 相当。
*   **热带地区优势**：在热带地区的长时效预报中，Sonny 显著优于 FastNet O96，且在 T850 等变量上表现出更慢的性能衰减。
*   **稳定性**：EMA 技术成功替代了复杂的递归微调，解决了深度学习模型在多步迭代预报中容易出现的误差累积和崩溃问题。
*   **极端事件捕捉**：尽管分辨率较低（1.5°），模型仍能准确捕捉台风路径和冬暴的快速增强过程，缓解了深度学习模型常见的“过度平滑”现象。

### 7. 优点
*   **极高的算力效率**：打破了学术界开发全球天气模型的门槛，证明了“小模型”也能有“大作为”。
*   **物理启发式架构**：通过动力学与热力学的解耦处理，优化了 Transformer 的计算效率。
*   **训练稳定性**：引入 EMA 是一种低成本且高效的稳定手段，具有很强的实用参考价值。

### 8. 不足与局限
*   **空间分辨率较低**：1.5° 的分辨率（约 150km）难以捕捉中小尺度天气系统，相比业务模型（0.1° - 0.25°）仍有差距。
*   **变量覆盖有限**：目前尚未整合陆面过程和海洋变量，这可能限制了其在更长时效（如次季节）预报中的表现。
*   **偏差风险**：在极端事件（如冬暴）的强度预测上仍存在一定偏差（如 4.9 hPa 的气压误差），低分辨率可能导致对极值捕捉不够精确。

（完）
