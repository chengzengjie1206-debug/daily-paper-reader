---
title: "TinyGLASS: Real-Time Self-Supervised In-Sensor Anomaly Detection"
title_zh: TinyGLASS：实时自监督传感器内异常检测
authors: "Pietro Bonazzi, Rafael Sutter, Luigi Capogrosso, Mischa Buob, Michele Magno"
date: 2026-03-17
pdf: "https://arxiv.org/pdf/2603.16451v2"
tags: ["query:ts"]
score: 9.0
evidence: 针对工业数据的实时自监督异常检测
tldr: 针对工业质检中异常检测模型计算量大、难以在边缘及传感器内部署的问题，本文提出TinyGLASS。它通过将GLASS框架的骨干网络替换为轻量化ResNet-18，并结合INT8量化与静态图追踪技术，实现了在资源受限的Sony IMX500传感器上的实时运行。该方法在保持高检测精度的同时，显著降低了参数量和功耗，为工业实时异常检测提供了高效的端侧解决方案。
selection_source: fresh_fetch
motivation: 现有的自监督异常检测模型计算需求过高，难以部署在资源受限的边缘平台或传感器内处理架构中。
method: 将GLASS框架的骨干网络替换为ResNet-18，并进行INT8量化和静态图追踪等部署优化，以适配Sony IMX500智能视觉传感器。
result: "模型参数压缩了8.6倍，在MVTec-AD上达到94.2%的AUROC，并在8MB内存限制下实现20 FPS的实时检测与极低功耗。"
conclusion: TinyGLASS证明了通过模型轻量化与硬件适配，可以在极低资源消耗下实现高性能、实时的传感器内工业异常检测。
---

## 摘要
异常检测在工业质量控制中起着关键作用，尽管缺乏带标签的故障样本，也必须识别出缺陷。最近的自监督方法（如 GLASS）仅使用无缺陷数据学习正常的视觉模式，并在工业基准测试中表现出强大的性能。然而，它们的计算需求限制了在资源受限的边缘平台上的部署，在传感器内处理架构中更是如此。本研究介绍了 TinyGLASS，这是 GLASS 框架的一种轻量化适配版本，专为实时边缘和传感器内异常检测而设计。所提出的架构将原始的 WideResNet-50 主干网络替换为紧凑的 ResNet-18，并引入了基于部署的修改，以实现静态图追踪和 INT8 量化。我们在索尼 IMX500 智能视觉传感器上评估了所提出的方法，利用索尼模型压缩工具包（Sony Model Compression Toolkit）发挥了传感器内处理器的性能。除了在 MVTec-AD 基准测试上评估性能外，我们还研究了对受污染训练数据的鲁棒性，并引入了一个名为 MMS 数据集的自定义工业数据集，用于跨设备评估。实验结果表明，TinyGLASS 在保持竞争力的检测性能的同时，实现了 8.6 倍的参数压缩，在 MVTec-AD 上达到了 94.2% 的图像级 AUROC，并在 IMX500 平台 8 MB 的内存限制下以 20 FPS 的速度运行。系统分析显示了低功耗（每次推理 4.0 mJ）、实时端到端延迟（20 FPS）和高能效（470 GMAC/J）。此外，该模型在适度水平的训练数据污染下表现出稳定的性能。

## Abstract
Anomaly detection plays a key role in industrial quality control, where defects must be identified despite the scarcity of labeled faulty samples. Recent self-supervised approaches, such as GLASS, learn normal visual patterns using only defect-free data and have shown strong performance on industrial benchmarks. However, their computational requirements limit the deployment on resource-constrained edge platforms, and even more so within in-sensor processing architectures. This work introduces TinyGLASS, a lightweight adaptation of the GLASS framework designed for real-time edge and in-sensor anomaly detection. The proposed architecture replaces the original WideResNet-50 backbone with a compact ResNet-18 and introduces deployment-based modifications that enable static graph tracing and INT8 quantization. We evaluated the proposed approach on the Sony IMX500 intelligent vision sensor, exploiting the in-sensor processor using the Sony Model Compression Toolkit. In addition to evaluating performance on the MVTec-AD benchmark, we investigate robustness to contaminated training data and introduce a custom industrial dataset, named MMS Dataset, for cross-device evaluation. Experimental results show that TinyGLASS achieves 8.6x parameter compression while maintaining competitive detection performance, reaching 94.2% image-level AUROC on MVTec-AD and operating at 20 FPS within the 8 MB memory constraints of the IMX500 platform. System profiling showcases low power consumption (4.0 mJ per inference), real-time end-to-end latency (20 FPS), and high energy efficiency (470 GMAC/J). Furthermore, the model demonstrates stable performance under moderate levels of training data contamination.

---

## 论文详细总结（自动生成）

以下是对论文《TinyGLASS: Real-Time Self-Supervised In-Sensor Anomaly Detection》的结构化总结：

### 1. 核心问题与整体含义
*   **研究背景**：在工业质量控制中，缺陷样本稀缺，因此仅利用正常数据学习的自监督异常检测（AD）方法（如 GLASS）成为主流。
*   **核心问题**：现有的高性能异常检测模型（如基于 WideResNet-50 的架构）计算开销巨大，难以部署在资源极度受限的边缘平台，尤其是集成在图像传感器内部的处理器（In-Sensor Processing）上。
*   **研究意义**：本文旨在填补先进异常检测算法与超低功耗边缘硬件之间的鸿沟，实现工业场景下的实时、端到端传感器内异常检测。

### 2. 方法论
*   **核心思想**：对 GLASS 框架进行轻量化改造和“部署感知”优化，使其适配 Sony IMX500 智能视觉传感器的硬件约束。
*   **关键技术细节**：
    *   **骨干网络替换**：将原始的 WideResNet-50 替换为 ResNet-18。为了进一步节省资源，仅执行到 `layer3`，将特征维度从 1536 降至 384。
    *   **部署感知修改**：
        *   **静态图追踪**：移除 PyTorch hooks，修改前向传播逻辑直接返回中间层特征，消除动态张量形状，以支持硬件要求的静态图追踪。
        *   **算子适配**：将判别器输入端的展平（Flatten）和全连接层改为全卷积（Conv2D）操作，保持 4D 张量流，确保兼容硬件加速器。
    *   **量化技术**：利用 Sony Model Compression Toolkit 将权重和激活值统一量化为 **INT8**。
    *   **损失函数**：结合二元交叉熵损失（LBCE）和 Focal Loss（Lfocal），以应对正常区域与缺陷区域之间的类别不平衡。

### 3. 实验设计
*   **数据集**：
    *   **MVTec-AD**：标准的工业异常检测基准数据集（15个类别）。
    *   **MMS Dataset（自定义）**：作者引入的新数据集，包含微型工业组件的裂纹、划痕、缺失等缺陷，并分别使用高分辨率显微镜和 IMX500 传感器拍摄，用于跨设备评估。
*   **Benchmark 与对比对象**：
    *   以原始 **GLASS (WideResNet-50)** 作为性能基准。
    *   对比了浮点模型（FP32）与量化模型（INT8）的性能差异。
*   **评估指标**：图像级 AUROC（I-AUROC）、像素级 AUROC（P-AUROC）、推理速度（FPS）、功耗（mJ/inference）及能效（GMAC/J）。

### 4. 资源与算力
*   **训练资源**：使用了 **3 台 NVIDIA RTX A6000 GPU** 进行训练。
*   **训练配置**：Batch size 为 8，最大训练 200 个 epoch，使用 AdamW 优化器。
*   **部署硬件**：**Sony IMX500** 智能视觉传感器（集成在 Raspberry Pi AI Camera 上），其内存限制为 **8 MB**。
*   **推理开销**：单次推理仅需 **1.88 G MACs**，功耗为 **4.0 mJ**。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在 MVTec-AD 的 15 个类别上进行了全面测试。
    *   针对“数据污染”场景，设置了 0%、5%、10%、20%、30% 五种不同的训练集污染率实验。
    *   在自定义 MMS 数据集上进行了跨设备验证。
*   **充分性评价**：实验设计较为全面，不仅关注算法精度，还深入探讨了硬件部署的实际指标（功耗、延迟、内存占用）以及工业现场常见的“训练集不纯”问题，具有较强的客观性和实用参考价值。

### 6. 主要结论与发现
*   **高效压缩**：TinyGLASS 实现了 **8.6 倍** 的参数压缩，模型最终仅占用约 **5.41 MB** 内存。
*   **性能平衡**：在 MVTec-AD 上达到 **94.2%** 的图像级 AUROC，相比原版 GLASS 仅下降了 4.9%，但在边缘端实现了质的飞跃。
*   **实时性与能效**：在 IMX500 上实现了 **20 FPS** 的实时推理，能效比高达 **470 GMAC/J**。
*   **鲁棒性**：模型对训练数据污染具有一定的容忍度，在 30% 污染率下，MVTec-AD (carpet) 的 I-AUROC 仍保持在 88.3%。

### 7. 优点
*   **首创性**：这是首个在传感器内（In-sensor）实现实时视觉异常检测的演示。
*   **部署导向**：并非简单的模型剪枝，而是针对硬件底层约束（如静态图、算子限制）进行了深度重构。
*   **实用性强**：引入了 MMS 数据集并考虑了数据污染问题，更贴近真实的工业生产环境。

### 8. 不足与局限
*   **精度损失**：为了极致的轻量化，像素级定位精度（P-AUROC）下降较明显（约 7.4%），对于极微小缺陷的定位可能不够精确。
*   **硬件绑定**：部分优化手段（如特定的量化工具包）高度依赖 Sony IMX500 平台，迁移到其他 AI 芯片可能需要重新适配。
*   **数据集规模**：自定义的 MMS 数据集类别相对较少，未来需在更多样化的工业零件上验证泛化性。

（完）
