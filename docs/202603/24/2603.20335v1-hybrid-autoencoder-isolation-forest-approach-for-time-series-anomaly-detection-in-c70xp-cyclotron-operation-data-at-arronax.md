---
title: Hybrid Autoencoder-Isolation Forest approach for time series anomaly detection in C70XP cyclotron operation data at ARRONAX
title_zh: ARRONAX C70XP 回旋加速器运行数据中时间序列异常检测的混合自编码器-孤立森林方法
authors: "F Basbous, F Poirier, F Haddad, D Mateus"
date: 2026-03-20
pdf: "https://arxiv.org/pdf/2603.20335v1"
tags: ["query:ts"]
score: 10.0
evidence: 用于时间序列异常检测的混合自编码器-孤立森林方法
tldr: 本研究针对ARRONAX的C70XP回旋加速器在放射性同位素生产中易发生故障的问题，提出了一种结合全连接自动编码器（AE）与孤立森林（IF）的混合异常检测方法。该方法利用AE重构传感器数据的平均立方误差（MCE）作为IF的输入，有效克服了传统IF在检测均值附近细微异常时的局限性。通过对质子束流强度时间序列数据的验证，证明了该方法能显著提升早期异常检测性能，保障系统稳定运行。
selection_source: fresh_fetch
motivation: 旨在解决C70XP回旋加速器运行中复杂系统易故障导致的停机问题，并克服传统孤立森林算法难以捕捉细微异常的局限。
method: 提出一种混合模型，将全连接自动编码器重构数据的平均立方误差（MCE）作为孤立森林算法的输入特征。
result: 在质子束流强度时间序列数据上的实验结果表明，该混合方法在检测性能上较传统方法有显著提升。
conclusion: 结合AE特征提取与IF异常识别的混合架构是提升回旋加速器等复杂系统异常检测准确性的有效途径。
---

## 摘要
ARRONAX 公益团体的 C70XP 回旋加速器用于医疗和研究用途的放射性同位素生产，其运行依赖于复杂且昂贵的系统，这些系统容易发生故障并导致运行中断。在此背景下，本研究旨在开发一种基于机器学习的方法，利用特定时间窗口内的传感器测量数据进行早期异常检测，以提升系统性能。孤立森林（IF）是应用最广泛的异常检测方法之一，以其高效性和可扩展性著称。然而，由于其依赖于轴平行分割，限制了其检测细微异常的能力，尤其是那些发生在正常数据均值附近的异常。本研究提出了一种结合全连接自编码器（AE）与孤立森林（IF）的混合方法，以增强对细微异常的检测能力。具体而言，该方法将 AE 重构传感器数据产生的平均立方误差（MCE）作为 IF 模型的输入。通过在质子束强度时间序列数据上的验证，实验结果证实该方法显著提升了检测性能。

## Abstract
The Interest Public Group ARRONAX's C70XP cyclotron, used for radioisotope production for medical and research applications, relies on complex and costly systems that are prone to failures, leading to operational disruptions. In this context, this study aims to develop a machine learning-based method for early anomaly detection, from sensor measurements over a temporal window, to enhance system performance. One of the most widely recognized methods for anomaly detection is Isolation Forest (IF), known for its effectiveness and scalability. However, its reliance on axis-parallel splits limits its ability to detect subtle anomalies, especially those occurring near the mean of normal data. This study proposes a hybrid approach that combines a fully connected Autoencoder (AE) with IF to enhance the detection of subtle anomalies. In particular, the Mean Cubic Error (MCE) of the sensor data reconstructed by the AE is used as input to the IF model. Validated on proton beam intensity time series data, the proposed method demonstrates a clear improvement in detection performance, as confirmed by the experimental results.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种针对回旋加速器运行数据进行异常检测的混合机器学习模型。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义
*   **研究背景**：ARRONAX 的 C70XP 回旋加速器是用于医疗和科研放射性同位素生产的关键设备。由于系统极其复杂且昂贵，任何运行中断都会导致巨大的经济损失和研究延误。
*   **核心问题**：传统的异常检测算法（如孤立森林 IF）虽然高效，但难以检测到“细微异常”（Subtle Anomalies）。这些异常往往发生在正常数值范围内，但其内部波动特征已偏离正常模式。
*   **研究动机**：通过改进特征空间表示，提升对早期故障迹象（组件退化等）的识别能力，从而实现更早的预警。

### 2. 论文提出的方法论
该论文提出了一种 **AE-IF（自编码器-孤立森林）** 混合架构，核心思想是将原始数据转换到“重构误差空间”中，以放大异常特征。
*   **核心思想**：利用自编码器（AE）仅在正常数据上训练的特性，使其对正常模式具有极高的重构精度，而对异常模式产生较大的重构误差。
*   **关键流程**：
    1.  **数据分段**：将时间序列信号（质子束强度）划分为固定长度（k=6）的时间窗口。
    2.  **AE 训练**：使用全连接自编码器，在标准化后的**正常样本**上进行训练，最小化均方误差（MSE）。
    3.  **特征转换（MCE）**：将所有数据（正常+异常）输入训练好的 AE，计算输入与输出之间的**平均立方误差（Mean Cubic Error, MCE）**。公式定义为：$MCE = \frac{1}{a} \sum (x_{ij} - \hat{x}_{ij})^3$。
    4.  **IF 检测**：将计算出的 MCE 值作为新特征输入到孤立森林（IF）中。由于异常点的 MCE 值显著高于正常点，IF 可以更轻松地将其隔离。

### 3. 实验设计
*   **数据集**：采集了 25 组质子束强度的单变量时间序列，每组持续 7-10 天，采样频率为 1Hz。
*   **实验场景**：针对回旋加速器运行中的“全局异常”（超出阈值的剧烈波动）和“细微局部异常”（阈值内的异常波动）进行检测。
*   **Benchmark（基准方法）**：
    *   **标准 IF**：直接应用于原始强度数据。
    *   **PCA-IF**：先使用主成分分析（PCA）降维，再应用 IF。
*   **评价指标**：召回率（Recall）、精确率（Precision）、F1 分数、PR 曲线下面积（AUC-PR）。

### 4. 资源与算力
*   **算力说明**：论文中**未明确提及**具体的硬件配置（如 GPU 型号、数量）或训练时长。考虑到模型使用的是简单的全连接自编码器和孤立森林，且窗口长度较短（k=6），预计该方法对算力的需求较低，可在普通工作站甚至工业控制计算机上运行。

### 5. 实验数量与充分性
*   **实验规模**：使用了 25 个长周期运行序列，其中 60%（15 个序列）用于开发（含 10 折交叉验证），40%（10 个序列）用于最终测试。
*   **充分性评价**：实验设计较为合理，通过 10 折交叉验证确保了模型的稳健性。论文还通过可视化（图 3）展示了不同特征空间中异常点与正常点均值的距离，从定性角度解释了 AE-IF 优于 PCA-IF 和标准 IF 的原因。实验对比了基准方法，证明了改进的有效性。

### 6 主要结论与发现
*   **性能提升**：AE-IF 模型显著优于其他方法。其 F1 分数达到 **0.87**，远高于标准 IF（0.41）和 PCA-IF（0.61）。
*   **特征空间的重要性**：研究发现，在原始空间中，细微异常与正常数据的中位距离小于 $10^{-2}$；而在 AE 产生的 MCE 空间中，该距离扩大到了约 13。这表明**特征空间转换**比单纯修改算法逻辑更能提升检测效果。
*   **高精度与高召回**：AE-IF 在保持高召回率（0.81）的同时，拥有极高的精确率（0.94），这意味着它在检测出更多异常的同时，误报率极低。

### 7. 优点
*   **针对性强**：专门解决了工业场景中“阈值内异常”难以捕捉的痛点。
*   **架构简洁**：结合了深度学习的特征提取能力和传统统计算法（IF）的无监督优势，易于部署。
*   **可解释性**：通过重构误差的物理意义解释了异常的来源。

### 8. 不足与局限
*   **变量单一**：目前仅验证了单变量（束流强度）时间序列，未探讨多传感器融合的情况。
*   **超参数敏感性**：论文未详细讨论窗口长度（k=6）或 AE 隐藏层深度对结果的影响。
*   **误差定义**：使用了立方误差（MCE），虽然能放大差异，但在某些情况下可能导致正负误差抵消（除非取绝对值或平方，论文公式中未体现绝对值符号），其数学鲁棒性有待进一步探讨。

（完）
