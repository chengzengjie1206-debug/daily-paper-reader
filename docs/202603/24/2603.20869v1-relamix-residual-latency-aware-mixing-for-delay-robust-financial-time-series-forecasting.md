---
title: "ReLaMix: Residual Latency-Aware Mixing for Delay-Robust Financial Time-Series Forecasting"
title_zh: ReLaMix：用于延迟鲁棒金融时间序列预测的残差延迟感知混合网络
authors: "Tianyou Lai, Wentao Yue, Jiayi Zhou, Chaoyuan Hao, Lingke Chang, Qingyu Mao, Zhibo Niu, Qilei Li"
date: 2026-03-21
pdf: "https://arxiv.org/pdf/2603.20869v1"
tags: ["query:ts"]
score: 9.0
evidence: 具有延迟鲁棒性的金融时序预测方法
tldr: 针对高频金融市场中因延迟导致的观测值陈旧问题，本文提出ReLaMix模型。该模型在TimeMixer基础上引入可学习的瓶颈压缩与残差细化机制，旨在从受零阶保持（ZOH）干扰的信号中恢复真实动态。通过显式抑制冗余陈旧值并增强残差混合，ReLaMix在多个高频交易数据集上实现了领先的预测精度，且参数量更少，具有极强的鲁棒性和泛化能力。
selection_source: fresh_fetch
motivation: 解决高频金融预测中因异步数据采集和传输延迟导致的观测值陈旧及信号停滞问题。
method: 提出一种轻量级的残差延迟感知混合网络，结合瓶颈压缩与残差细化来抑制冗余并提取有效市场动态。
result: 在PAXGUSDT和BTCUSDT等高频数据集上，该方法在多种延迟比例下均优于主流的Mixer和Transformer模型。
conclusion: 实验证明残差瓶颈混合机制能有效提升存在延迟干扰下的金融时间序列预测性能，且具备良好的跨资产泛化性。
---

## 摘要
现实高频市场中的金融时间序列预测常受到异步数据采集和传输延迟导致的观测值延迟或部分陈旧的阻碍。为了更好地反映此类实际情况，我们研究了一种模拟延迟设置，其中部分历史信号受到零阶保持（ZOH）机制的破坏，通过阶梯状停滞伪影显著增加了预测难度。在本文中，我们提出了 ReLaMix（残差延迟感知混合网络），这是 TimeMixer 的一种轻量级扩展，它集成了可学习的瓶颈压缩与残差细化，用于在延迟观测下实现鲁棒的信号恢复。ReLaMix 显式地抑制了来自重复陈旧值的冗余，同时通过残差混合增强保留了具有信息量的市场动态。在大型秒级分辨率 PAXGUSDT 基准测试上的实验表明，ReLaMix 在多种延迟比例和预测时界下始终保持最先进的准确度，以显著更少的参数量优于强大的 Mixer 和 Transformer 基准模型。此外，在 BTCUSDT 上的额外评估证实了所提框架的跨资产泛化能力。这些结果突显了残差瓶颈混合在现实延迟导致的陈旧情况下进行高频金融预测的有效性。

## Abstract
Financial time-series forecasting in real-world high-frequency markets is often hindered by delayed or partially stale observations caused by asynchronous data acquisition and transmission latency. To better reflect such practical conditions, we investigate a simulated delay setting where a portion of historical signals is corrupted by a Zero-Order Hold (ZOH) mechanism, significantly increasing forecasting difficulty through stepwise stagnation artifacts. In this paper, we propose ReLaMix (Residual Latency-Aware Mixing Network), a lightweight extension of TimeMixer that integrates learnable bottleneck compression with residual refinement for robust signal recovery under delayed observations. ReLaMix explicitly suppresses redundancy from repeated stale values while preserving informative market dynamics via residual mixing enhancement. Experiments on a large-scale second-resolution PAXGUSDT benchmark demonstrate that ReLaMix consistently achieves state-of-the-art accuracy across multiple delay ratios and prediction horizons, outperforming strong mixer and Transformer baselines with substantially fewer parameters. Moreover, additional evaluations on BTCUSDT confirm the cross-asset generalization ability of the proposed framework. These results highlight the effectiveness of residual bottleneck mixing for high-frequency financial forecasting under realistic latency-induced staleness.

---

## 论文详细总结（自动生成）

这是一份关于论文《ReLaMix: Residual Latency-Aware Mixing for Delay-Robust Financial Time-Series Forecasting》的结构化深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
在高频交易（HFT）等现实金融场景中，由于网络拥塞、硬件性能限制或交易所接口瓶颈，数据采集往往存在物理延迟。这导致观测到的信号并非实时更新，而是呈现出**零阶保持（Zero-Order Hold, ZOH）**特征——即在收到新数据前，观测值保持上一时刻的数值不变，形成“阶梯状”停滞伪影。
*   **痛点**：现有模型（如 LSTM、Transformer 或常规 Mixer）在面对这种陈旧数据时，容易将停滞伪影误判为真实的低波动或价格跳跃，导致过拟合噪声或预测失效。
*   **目标**：开发一种轻量级、端到端的模型，既能过滤延迟产生的冗余信息，又能高效恢复真实的金融动态，满足高频交易对低推理延迟的要求。

### 2. 方法论：核心思想与关键技术
论文提出了 **ReLaMix（残差延迟感知混合网络）**，其核心思想是利用**信息瓶颈（Information Bottleneck）**原则进行信号去噪与特征提取。

*   **瓶颈压缩（Bottleneck Compression）**：模型首先将高维的受损输入投影到一个极低维的潜在空间（例如将维度压缩至 32）。这一步旨在显式过滤掉 ZOH 机制产生的重复冗余值，仅保留最具预测性的信号特征。
*   **残差延迟感知混合块（Mixing Block）**：
    *   **时间混合（Time Mixing）**：在时间维度上进行线性混合，通过全局感受野平滑 ZOH 引入的局部不连续性。
    *   **特征混合（Feature Mixing）**：采用“扩张-压缩”机制（如 $32 \to 64 \to 32$），类似于轻量级网络中的倒置残差结构，增强模型捕捉非线性市场动态（如价量耦合）的能力。
*   **多级跳跃连接（Multi-Level Skip Connections）**：通过累积残差连接将各层特征聚合，形成“信息高速公路”，防止深层网络中的梯度消失，并确保低层预测线索能直达输出端。
*   **端到端训练**：模型直接学习从“受损历史序列”到“未来清洁序列”的映射，避免了传统“先填补、后预测”两阶段方案带来的误差累积。

### 3. 实验设计
*   **数据集**：
    *   **PAXGUSDT**：来自币安（Binance）的秒级高频交易数据（OHLCV特征）。
    *   **BTCUSDT**：用于验证跨资产泛化能力的测试集。
*   **实验场景**：模拟了 15%、25%、35% 三种不同的随机延迟比例，并设置了 $k=1, 5, 7, 10$ 等多个预测时界（Horizons）。
*   **Benchmark（对比方法）**：
    *   循环神经网络：LSTM, GRU。
    *   卷积/混合架构：TimesNet, TimeMixer。
    *   Transformer 架构：PatchTST, LiT。

### 4. 资源与算力
*   **硬件**：实验在一台拥有单张 **NVIDIA GeForce RTX 4090 GPU** 的服务器上完成。
*   **软件/参数**：使用 PyTorch 框架，Adam 优化器，学习率为 $1 \times 10^{-3}$，Batch Size 为 32。
*   **训练时长**：文中未明确给出具体的训练总时长，但提到应用了早停（Early Stopping）机制来防止过拟合。

### 5. 实验数量与充分性
*   **实验规模**：论文在两个主流加密货币数据集上进行了测试，涵盖了 3 种延迟率和 4 种预测长度的组合，总计进行了数十组对比实验。
*   **消融实验**：专门设计了“无压缩模块”和“无残差细化”的变体实验，验证了各组件的必要性。
*   **客观性**：采用了严格的非重叠分段评估协议以防止时间泄漏（Temporal Leakage），实验设计较为客观、公平。

### 6. 主要结论与发现
*   **精度领先**：ReLaMix 在所有延迟比例和预测时界下均达到了 SOTA 性能，MSE 和 MAE 显著低于 Transformer 和其他 Mixer 模型。
*   **极度轻量化**：ReLaMix 的参数量仅为 **13K~15K** 左右，远小于 TimeMixer（~41K）、PatchTST（~102K）和 TimesNet（~229K）。
*   **鲁棒性与泛化性**：在延迟比例增加（如 35%）时，ReLaMix 的性能下降幅度最小；在 BTCUSDT 上的测试证明了其具备良好的跨资产迁移能力。
*   **消融发现**：残差细化对性能的影响最为剧烈，移除后会导致模型崩溃；而瓶颈压缩则在维持轻量化的同时有效提升了去噪效果。

### 7. 优点（亮点）
*   **针对性强**：专门针对金融高频交易中的 ZOH 延迟问题建模，而非通用的缺失值处理。
*   **效率极高**：参数量极小，非常适合对推理速度要求极高的实盘量化交易环境。
*   **设计优雅**：结合了信息瓶颈理论与现代 Mixer 架构，通过简单的线性层组合实现了强大的去噪恢复能力。

### 8. 不足与局限
*   **延迟模式单一**：实验主要基于随机分布的 ZOH 模拟延迟，现实中的延迟可能具有更复杂的自相关性或突发性（如交易所宕机导致的长时间停滞），模型在极端异常情况下的表现尚待验证。
*   **资产覆盖面**：虽然测试了加密货币，但未在股票、期货等传统高频市场（其微观结构不同）进行验证。
*   **硬件实测缺失**：虽然强调了轻量化，但未给出在特定嵌入式或 FPGA 交易设备上的实际推理延迟（Latency）数值。

（完）
