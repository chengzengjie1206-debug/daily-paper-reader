---
title: "FISformer: Replacing Self-Attention with a Fuzzy Inference System in Transformer Models for Time Series Forecasting"
title_zh: FISformer：在用于时间序列预测的 Transformer 模型中用模糊推理系统替换自注意力机制
authors: "Bulent Haznedar, Levent Karacan"
date: 2026-03-23
pdf: "https://arxiv.org/pdf/2603.21724v1"
tags: ["query:ts"]
score: 10.0
evidence: 用于多变量预测的可解释模糊推理系统
tldr: 针对传统Transformer在时间序列预测中难以建模不确定性和非线性依赖的问题，本文提出FISFormer。该模型创新性地利用模糊推理系统（FIS）取代了传统的点积自注意力机制，通过可学习的隶属度函数和规则推理来估算Token间的关联强度。实验证明，FISFormer在多个基准数据集上不仅提升了预测精度和鲁棒性，还增强了模型的可解释性，为时序建模提供了新思路。
selection_source: fresh_fetch
motivation: 传统Transformer依赖的点积注意力机制在处理多变量时间序列时，难以有效捕捉不确定性和复杂的非线性依赖关系。
method: 提出FIS交互机制，利用模糊推理过程替代自注意力，通过可学习的隶属度函数和规则库计算Token间的交互权重并进行特征融合。
result: 在多个公开基准数据集上的实验结果显示，FISFormer在预测精度、抗噪能力和可解释性方面均显著优于现有的SOTA模型。
conclusion: 模糊推理系统是传统注意力机制的有效替代方案，能够显著提升Transformer模型在时间序列预测中的不确定性建模能力。
---

## 摘要
Transformer 在时间序列预测方面取得了显著进展，但其对确定性点积注意力的依赖限制了其在多变量时间维度上建模不确定性和非线性依赖关系的能力。为了解决这一局限性，我们提出了 FISFormer，这是一种由模糊推理系统（FIS）驱动的 Transformer，它用 FIS 交互机制取代了传统的注意力机制。在该框架中，每个查询-键（query-key）对在每个特征维度上都会经历一个模糊推理过程，其中可学习的隶属度函数和基于规则的推理用于估计 token 间的关系强度。这些由 FIS 衍生的交互权重捕捉了不确定性，并提供了 token 之间可解释的连续映射。在 token 轴上应用 softmax 操作以归一化这些权重，然后通过逐元素乘法将其与相应的数值（value）特征结合，从而产生最终的上下文增强 token 表示。这一设计将模糊逻辑的可解释性和不确定性建模能力与 Transformer 的表征能力融合在一起。在多个基准数据集上的广泛实验表明，与最先进的 Transformer 变体相比，FISFormer 实现了卓越的预测精度、噪声鲁棒性和可解释性，证明了模糊推理是传统注意力机制的有效替代方案。

## Abstract
Transformers have achieved remarkable progress in time series forecasting, yet their reliance on deterministic dot-product attention limits their capacity to model uncertainty and nonlinear dependencies across multivariate temporal dimensions. To address this limitation, we propose FISFormer, a Fuzzy Inference System-driven Transformer that replaces conventional attention with a FIS Interaction mechanism. In this framework, each query-key pair undergoes a fuzzy inference process for every feature dimension, where learnable membership functions and rule-based reasoning estimate token-wise relational strengths. These FIS-derived interaction weights capture uncertainty and provide interpretable, continuous mappings between tokens. A softmax operation is applied along the token axis to normalize these weights, which are then combined with the corresponding value features through element-wise multiplication to yield the final context-enhanced token representations. This design fuses the interpretability and uncertainty modeling of fuzzy logic with the representational power of Transformers. Extensive experiments on multiple benchmark datasets demonstrate that FISFormer achieves superior forecasting accuracy, noise robustness, and interpretability compared to state-of-the-art Transformer variants, establishing fuzzy inference as an effective alternative to conventional attention mechanisms.

---

## 论文详细总结（自动生成）

以下是对论文《FISformer: Replacing Self-Attention with a Fuzzy Inference System in Transformer Models for Time Series Forecasting》的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：传统 Transformer 依赖的**确定性点积自注意力机制（Dot-product Attention）**在处理多变量时间序列时存在局限。它难以有效建模现实数据中普遍存在的**不确定性**和**复杂的非线性依赖关系**，且缺乏可解释性。
*   **研究背景**：虽然已有研究通过稀疏注意力或架构调整（如 iTransformer）提升了效率，但底层交互逻辑仍基于“硬性”的相似度计算。本文旨在引入模糊逻辑，利用其处理模糊性和不确定性的天然优势，重构 Transformer 的核心交互层。

### 2. 方法论：FIS 交互机制
FISformer 的核心是将标准注意力替换为基于 **Sugeno 型模糊推理系统（FIS）** 的交互模块，包含四个关键阶段：
*   **模糊化（Fuzzification）**：使用可学习的**高斯隶属度函数**将查询（Query）和键（Key）映射为模糊隶属度。每个特征维度拥有独立的隶属度参数（均值和标准差），允许模型捕捉数据的软边界。
*   **规则生成（Rule Generation）**：采用乘积型 T-范数（Product-based t-norm）计算模糊规则的激活强度（Firing Strength）。每一条规则代表了 Token 之间一种可解释的交互逻辑。
*   **解模糊与聚合（Defuzzification & Aggregation）**：遵循一阶 Sugeno 推理模型，规则的输出（Consequent）是输入 Query 和 Key 的线性组合。通过对所有规则的激活强度进行归一化加权求和，得到最终的“模糊交互图”。
*   **FIS 交互输出**：在 Token 轴上应用 Softmax 归一化，生成交互权重，最后与数值（Value）向量进行逐元素相乘，实现上下文增强。
*   **架构集成**：该模块被嵌入到类似 iTransformer 的倒置架构中，将每个变量视为独立 Token，以更好地捕捉多变量相关性。

### 3. 实验设计
*   **数据集**：涵盖了 7 个领域的 11 个子数据集，包括 ETT (h1, h2, m1, m2)、Exchange、Weather、ECL (电力)、Traffic、Solar-Energy 和 PEMS (03, 04, 07, 08)。
*   **预测设置**：固定回看窗口（通常为 96），预测长度设为多个梯度（如 96, 192, 336, 720）。
*   **对比基准（Benchmarks）**：
    *   经典 Transformer 变体：Informer, Autoformer, FEDformer, Crossformer。
    *   强力基准：iTransformer（目前 SOTA 之一）。
    *   模糊增强模型：FANTF (Fuzzy Attention-Integrated Transformer)。
*   **评价指标**：均方误差 (MSE) 和 平均绝对误差 (MAE)。

### 4. 资源与算力
*   **硬件设备**：单张 **NVIDIA GeForce RTX 2080 Ti (12GB)** GPU。
*   **软件框架**：PyTorch。
*   **训练细节**：使用 Adam 优化器，初始学习率在 $\{10^{-4}, 10^{-3}\}$ 之间微调，Batch Size 为 32，训练 10 个 Epoch。
*   **模型配置**：Transformer 层数 $L \in \{2, 3, 4\}$，隐藏层维度 $D \in \{256, 512\}$。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了大规模的对比实验，涵盖了数十种预测长度与数据集的组合。
*   **消融实验**：
    *   对比了 FIS 交互与标准自注意力的直接效果。
    *   验证了将 FIS 模块集成到不同架构（如 Informer）中的通用性。
    *   测试了不同隶属度函数（高斯、三角形、梯形）的影响。
    *   分析了学习率敏感性。
*   **充分性评价**：实验设计非常充分且客观。通过在不同维度（高维 vs 低维）、不同频率（小时 vs 分钟）的数据集上验证，证明了结果的普适性。

### 6. 主要结论与发现
*   **性能卓越**：FISformer 在 7 个数据集中的 6 个上达到了最优性能（SOTA），显著优于 iTransformer 和 FANTF。
*   **鲁棒性强**：在噪声较大的 Exchange 和 Weather 数据集上，FIS 机制表现出比点积注意力更强的稳定性。
*   **计算效率**：复杂度分析显示，FIS 交互在 Token 轴上独立运行，其时间复杂度与序列长度呈**线性关系** $O(T \cdot d \cdot R)$（$R$ 为规则数），在长序列预测中比标准注意力的平方复杂度更具优势。
*   **可解释性**：通过模糊规则的激活状态，模型能够提供比黑盒注意力更透明的 Token 交互逻辑。

### 7. 亮点与优点
*   **理论融合**：成功将经典的模糊推理系统（FIS）深度嵌入深度学习核心层，而非简单的后处理或权重修正。
*   **不确定性建模**：通过可学习的隶属度函数，模型能够自适应地处理时间序列中的波动和测量误差。
*   **架构灵活性**：FIS 模块具有即插即用的特性，实验证明其能显著提升旧模型（如 Informer）的性能。

### 8. 不足与局限
*   **超参数增加**：引入了模糊规则数、隶属度函数类型等额外超参数，增加了调参成本。
*   **规则数限制**：虽然复杂度是线性的，但如果为了捕捉极复杂关系而大幅增加模糊规则数 $R$，计算开销仍会上升。
*   **应用范围**：目前仅在时间序列预测任务上验证，尚未在自然语言处理（NLP）或计算机视觉（CV）等更广泛的 Transformer 应用领域进行测试。

（完）
