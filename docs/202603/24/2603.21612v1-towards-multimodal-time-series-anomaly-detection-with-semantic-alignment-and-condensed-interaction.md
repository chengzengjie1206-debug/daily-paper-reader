---
title: Towards Multimodal Time Series Anomaly Detection with Semantic Alignment and Condensed Interaction
title_zh: 迈向具有语义对齐和压缩交互的多模态时间序列异常检测
authors: "Shiyan Hu, Jianxin Jin, Yang Shu, Peng Chen, Bin Yang, Chenjuan Guo"
date: 2026-03-23
pdf: "https://arxiv.org/pdf/2603.21612v1"
tags: ["query:ts"]
score: 10.0
evidence: 具有语义对齐的多模态时间序列异常检测
tldr: 本研究针对时间序列异常检测中单一模态数据局限性，提出多模态模型MindTS。该模型通过细粒度时空语义对齐解决异构数据一致性问题，并利用内容压缩重构过滤冗余信息，增强跨模态交互。在六个真实数据集上的实验证明，MindTS在处理多模态时间序列异常检测任务上具有显著优势，为复杂动态系统的异常监控提供了新思路。
selection_source: fresh_fetch
motivation: 传统时间序列异常检测主要依赖单一数值模态，忽视了文本等其他模态提供的互补信息，且面临异构数据对齐和冗余信息过滤的挑战。
method: 提出MindTS模型，通过细粒度时空语义对齐机制整合内外文本信息，并利用内容压缩重构技术过滤冗余信息以实现高效的跨模态交互。
result: 在六个真实世界多模态数据集上的广泛实验表明，MindTS的性能达到或超过了现有的先进基准方法。
conclusion: 通过语义对齐和压缩交互，MindTS证明了整合多模态信息能显著提升时间序列异常检测的准确性与鲁棒性。
---

## 摘要
时间序列异常检测在许多动态系统中起着至关重要的作用。尽管其具有重要性，以往的方法主要依赖于单模态数值数据，忽视了来自其他模态的补充信息的重要性。在本文中，我们提出了一种新型的多模态时间序列异常检测模型 (MindTS)，重点解决两个关键挑战：(1) 如何在异构多模态数据之间实现语义一致的对齐，以及 (2) 如何过滤冗余的模态信息以有效地增强跨模态交互。为了解决第一个挑战，我们提出了细粒度的时间-文本语义对齐。它通过跨视图文本融合和多模态对齐机制整合了外源性和内源性文本信息，实现了时间与文本模态之间语义一致的对齐。针对第二个挑战，我们引入了内容压缩器重构，它过滤了对齐文本模态中的冗余信息，并进行跨模态重构以实现交互。在六个真实世界多模态数据集上的广泛实验表明，所提出的 MindTS 与现有方法相比取得了具有竞争力的或更优的结果。代码可在以下网址获取：https://github.com/decisionintelligence/MindTS。

## Abstract
Time series anomaly detection plays a critical role in many dynamic systems. Despite its importance, previous approaches have primarily relied on unimodal numerical data, overlooking the importance of complementary information from other modalities. In this paper, we propose a novel multimodal time series anomaly detection model (MindTS) that focuses on addressing two key challenges: (1) how to achieve semantically consistent alignment across heterogeneous multimodal data, and (2) how to filter out redundant modality information to enhance cross-modal interaction effectively. To address the first challenge, we propose Fine-grained Time-text Semantic Alignment. It integrates exogenous and endogenous text information through cross-view text fusion and a multimodal alignment mechanism, achieving semantically consistent alignment between time and text modalities. For the second challenge, we introduce Content Condenser Reconstruction, which filters redundant information within the aligned text modality and performs cross-modal reconstruction to enable interaction. Extensive experiments on six real-world multimodal datasets demonstrate that the proposed MindTS achieves competitive or superior results compared to existing methods. The code is available at: https://github.com/decisionintelligence/MindTS.

---

## 论文详细总结（自动生成）

这是一份关于论文《Towards Multimodal Time Series Anomaly Detection with Semantic Alignment and Condensed Interaction》（MindTS）的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
传统的时间序列异常检测（TSAD）主要依赖单一的数值模态，忽略了如新闻报告、政策文档等文本模态提供的丰富背景信息。然而，构建多模态 TSAD 模型面临两大挑战：
*   **语义对齐难题**：时间序列（连续信号）与文本（离散符号）存在巨大的模态鸿沟，且外源文本往往与具体的时间片段关联较弱。
*   **信息冗余干扰**：文本中包含大量与异常检测无关的冗余描述，直接融合会稀释关键特征，干扰跨模态交互的有效性。

### 2. 方法论：核心思想与技术细节
论文提出了 **MindTS** 框架，其核心流程如下：
*   **细粒度时间-文本语义对齐**：
    *   **内源文本生成**：利用大语言模型（LLM）根据每个时间补丁（Patch）的统计特征（均值、极值、趋势等）生成描述性文本，确保文本与时间序列在局部特征上高度一致。
    *   **跨视图文本融合**：将内源文本作为 Query，外源文本（外部报告）作为 Key 和 Value，通过交叉注意力机制提取与当前时间片段最相关的背景信息。
    *   **多模态对齐策略**：采用对比学习损失（Contrastive Learning），在统一的嵌入空间中拉近匹配的时间-文本对，推开不匹配的对。
*   **内容压缩重构（Content Condenser Reconstruction）**：
    *   **内容压缩器**：基于信息瓶颈（Information Bottleneck）原则，通过一个 MLP 预测掩码概率，并利用伯努利采样生成二进制掩码，过滤掉冗余的文本表示。
    *   **互信息最小化**：通过 KL 散度约束压缩过程，确保在保留关键信息的同时实现表示的紧凑性。
    *   **跨模态重构**：使用压缩后的文本表示来重构被随机掩码的时间序列补丁。重构误差（MSE）被用作最终的异常得分。

### 3. 实验设计
*   **数据集**：使用了 6 个真实世界多模态数据集，涵盖天气（Weather）、能源（Energy）、环境（Environment）以及金融领域（KR, EWJ, MDT）。这些数据集均包含数值序列和对应的时间戳对齐文本。
*   **Benchmark 与对比方法**：
    *   **对比方法**：共 17-19 种基准方法，包括基于 LLM 的方法（LLMMixer, UniTime, GPT4TS）、预训练模型（DADA, Timer, UniTS）、深度学习模型（TimesNet, DCdetector, PatchTST）以及传统非学习方法（IForest, PCA）。
    *   **多模态扩展**：为了公平竞争，还将 11 种单模态基准通过 MM-TSFLib 框架扩展为多模态版本进行对比。
*   **评价指标**：采用了 Affiliated-F1、VUS-PR、VUS-ROC 等 16 种标签和得分评价指标。

### 4. 资源与算力
*   **硬件**：实验运行在 **NVIDIA Tesla-A800-80GB GPU** 上。
*   **软件框架**：基于 PyTorch 实现，使用 Adam 优化器。
*   **算力细节**：文中提供了推理时间（Inference Time）和内存占用（Memory Cost）的对比。例如，在 MDT 数据集上推理时间约为 0.23s，内存占用约 14.69GB。虽然未明确给出总训练时长，但通过推理效率分析，该模型在现代硬件上具有实用性。

### 5. 实验数量与充分性
实验设计非常充分且客观：
*   **广泛对比**：在 6 个数据集上与近 20 种方法进行了全面对比。
*   **消融实验**：设计了 6 种变体（如移除外源/内源文本、移除对齐模块、移除压缩器等），验证了每个组件的必要性。
*   **参数敏感性**：测试了补丁大小（Patch Size）、掩码比例（Mask Ratio）和压缩强度（$\mu$）对性能的影响。
*   **LLM 鲁棒性**：测试了不同后端 LLM（GPT2, BERT, Llama, DeepSeek）对结果的影响，证明性能提升主要源于架构设计而非特定 LLM。
*   **文本质量分析**：模拟了噪声、无关和不完整文本的情况，验证了模型的鲁棒性。

### 6. 主要结论与发现
*   **MindTS 达到 SOTA**：在所有 6 个数据集的各项核心指标上均优于现有单模态和多模态方法。
*   **多模态优于单模态**：实验证明引入文本信息显著提升了异常检测的准确性，尤其是在复杂场景下。
*   **冗余过滤至关重要**：消融实验显示，如果不进行内容压缩，直接融合文本反而可能导致性能下降，证明了过滤冗余信息的必要性。

### 7. 优点与亮点
*   **双视图文本建模**：结合了内源文本的“局部准确性”和外源文本的“全局背景”，解决了异构数据对齐的痛点。
*   **信息瓶颈的应用**：创新性地引入内容压缩器，利用互信息最小化来处理文本冗余，这在多模态时间序列领域较为新颖。
*   **任务通用性**：虽然主攻异常检测，但附录实验显示该架构在时间序列预测任务中也具有竞争力，展现了良好的扩展性。

### 8. 不足与局限
*   **模态局限性**：目前仅聚焦于“时间序列+文本”，尚未涵盖图像或视频模态，对于包含视觉信息的复杂监控场景可能受限。
*   **预处理开销**：生成内源文本需要调用 LLM 进行统计描述，这在超大规模实时流数据处理中可能会带来额外的预处理延迟。
*   **外部文本依赖**：模型性能仍受限于外源文本的获取质量和时间戳对齐的精度，在缺乏外部报道的封闭系统中难以发挥优势。

（完）
