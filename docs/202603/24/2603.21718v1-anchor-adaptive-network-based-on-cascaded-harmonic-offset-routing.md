---
title: "ANCHOR: Adaptive Network based on Cascaded Harmonic Offset Routing"
title_zh: ANCHOR：基于级联谐波偏移路由的自适应网络
authors: "Wangye Jiang, Haoming Yang, Jingya Zhang"
date: 2026-03-23
pdf: "https://arxiv.org/pdf/2603.21718v1"
tags: ["query:ts"]
score: 9.0
evidence: 针对非平稳时间序列的高级频率引导建模
tldr: 针对非平稳时间序列建模中存在的动态相位漂移和量化误差挑战，本文提出了一种名为 ANCHOR 的通用骨干网络。该方法通过将时间演化解耦为宏观周期和微观相位扰动，并引入频率引导的可变形模块（FGDM）来补偿相位偏差。结合正交通道划分和渐进式残差架构，ANCHOR 实现了多尺度谐波特征的高效提取，在预测、异常检测和分类等多个任务中表现优异。
selection_source: fresh_fetch
motivation: 传统方法直接从一维序列提取特征，难以处理非平稳信号中的动态相位漂移和离散量化误差。
method: 提出频率引导的可变形模块（FGDM）补偿相位偏差，并构建基于级联谐波偏移路由的自适应网络（ANCHOR）。
result: 在短期预测任务中达到最优性能，并在异常检测和时间序列分类任务中展现出极强的竞争力。
conclusion: ANCHOR 通过解耦宏观周期与微观扰动，证明了其作为通用时间序列建模骨干网络的有效性和高效性。
---

## 摘要
时间序列分析在广泛的现实应用中发挥着基础性作用，然而准确建模复杂的非平稳信号仍然是各项下游任务面临的共同挑战。现有方法试图直接从一维序列中提取特征，难以处理普遍存在的动态相位漂移和离散量化误差。为了解决这一问题，我们将时间演化解耦为宏观物理周期和微观相位扰动，并将源自实数快速傅里叶变换（RFFT）的频域先验注入到底层空间采样过程中。基于此思路，我们提出了一种频率引导的可变形模块（FGDM），用于自适应地补偿微观相位偏差。在 FGDM 的基础上，我们进一步开发了基于级联谐波偏移路由的自适应网络（ANCHOR），作为时间序列建模的通用骨干网络。通过正交通道划分和渐进式残差架构，ANCHOR 有效地解耦了多尺度谐波特征，同时显著抑制了多分支网络的计算冗余。大量实验表明，ANCHOR 在大多数短期预测子任务中实现了最佳性能，并在异常检测和时间序列分类的多个特定子任务上表现出强劲的竞争力，验证了其作为通用时间序列基础骨干网络的有效性。

## Abstract
Time series analysis plays a foundational role in a wide range of real-world applications, yet accurately modeling complex non-stationary signals remains a shared challenge across downstream tasks. Existing methods attempt to extract features directly from one-dimensional sequences, making it difficult to handle the widely observed dynamic phase drift and discrete quantization error. To address this issue, we decouple temporal evolution into macroscopic physical periods and microscopic phase perturbations, and inject frequency-domain priors derived from the Real Fast Fourier Transform (RFFT) into the underlying spatial sampling process. Based on this idea, we propose a Frequency-Guided Deformable Module (FGDM) to adaptively compensate for microscopic phase deviations. Built upon FGDM, we further develop an Adaptive Network based on Cascaded Harmonic Offset Routing (ANCHOR) as a general-purpose backbone for time-series modeling. Through orthogonal channel partitioning and a progressive residual architecture, ANCHOR efficiently decouples multi-scale harmonic features while substantially suppressing the computational redundancy of multi-branch networks. Extensive experiments demonstrate that ANCHOR achieves the best performance in most short-term forecasting sub-tasks and exhibits strong competitiveness on several specific sub-tasks in anomaly detection and time-series classification, validating its effectiveness as a universal time-series foundation backbone.

---

## 论文详细总结（自动生成）

以下是对论文《ANCHOR: Adaptive Network based on Cascaded Harmonic Offset Routing》的结构化深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
时间序列分析在气候建模、电力调度、工业物联网等领域至关重要。然而，现实中的时间序列通常是非平稳的，包含多尺度周期性。
*   **现有挑战：** 传统深度学习模型（如 Transformer 或纯 CNN）直接在时域提取特征，难以处理**动态相位漂移**和**离散量化误差**（即 FFT 中的“栅栏效应”）。
*   **研究动机：** 论文试图将时间演化解耦为“宏观物理周期”和“微观相位扰动”，通过引入频域先验来引导时域的空间采样，从而构建一个通用的时间序列骨干网络。

### 2. 核心方法论
论文提出了 **ANCHOR** 架构，其核心组件和逻辑如下：
*   **显式物理先验提取：** 利用实数快速傅里叶变换（RFFT）提取信号中能量最高的 $K$ 个频率索引，并将其映射为物理周期 $T_k$。
*   **频率引导可变形模块 (FGDM)：** 
    *   **核心思想：** 将离散的 $T_k$ 作为粗粒度的“空间锚点”，引入一维可变形卷积（1D Deformable Convolution）。
    *   **相位补偿：** 通过学习连续的子像素偏移量 $\Delta p_n$，利用双线性插值在时域内自主对齐连续相位，从而消除离散傅里叶变换带来的量化偏差。
*   **ANCHOR 整体架构：**
    *   **正交通道划分：** 将输入通道等分为 $N$ 个子空间，减少计算冗余。
    *   **非对称路由机制：** 针对不同频段采用“小卷积核处理高保真强信号，大卷积核处理积分弱特征”的策略。
    *   **渐进式残差级联：** 通过多层级联逐步融合多尺度频率-空间特征，实现宏观模式与微观扰动的深度解耦。

### 3. 实验设计
论文在三大主流时间序列任务上进行了验证：
*   **短期预测：** 使用 **M4** 竞赛数据集（包含 10 万条独立序列），评估指标为 sMAPE、MASE 和 OWA。
*   **异常检测：** 使用 **SMD**（服务器监控）、**SWaT**（水处理）和 **PSM**（航天）数据集，采用无监督重构框架，评估 Precision、Recall 和 F1-score。
*   **时间序列分类：** 使用 **UEA** 多变量分类存档中的 10 个子集（涵盖手势、语音、心电图等），评估 Accuracy。
*   **对比方法：** 包括最新的大语言模型微调方法（Time-LLM, SE-LLM）、Transformer 变体（iTransformer, PatchTST）、CNN 变体（ModernTCN, TimesNet）以及 Mamba 架构模型。

### 4. 资源与算力
*   **算力说明：** 论文中**未明确提及**具体的硬件配置（如 GPU 型号、数量）以及训练所需的总时长。
*   **效率分析：** 作者在理论复杂度分析中指出，通过通道划分策略，ANCHOR 的计算开销相比传统全通道多分支网络有显著的常数级降低。

### 5. 实验数量与充分性
*   **实验规模：** 实验覆盖了从短期预测到分类、异常检测的完整闭环。特别是在 M4 数据集上的 10 万条序列测试，证明了模型在处理高度异构数据时的鲁棒性。
*   **充分性：** 论文对比了 2024 年至 2026 年间（注：论文标注日期为 2026 年）的最前沿基准模型，实验设置较为客观，涵盖了不同采样频率和物理场景。
*   **消融与分析：** 提供了关于通道划分复杂度的数学推导，验证了 FGDM 模块作为“插件”的有效性。

### 6. 主要结论与发现
*   **预测性能：** ANCHOR 在 M4 短期预测任务中几乎在所有子项（年、季、月等）上均取得了最优（SOTA）结果。
*   **通用性：** 模型不仅在预测上表现出色，在异常检测和分类任务中也展现了极强的竞争力，证明其可以作为通用的时间序列骨干网络（Backbone）。
*   **相位对齐的重要性：** 实验证明，通过可变形卷积补偿频域量化误差，能显著提升模型捕捉非平稳信号特征的能力。

### 7. 优点与亮点
*   **跨域结合：** 创新性地将经典信号处理（RFFT）与现代深度学习算子（可变形卷积）结合，解决了频域离散性与时域连续性之间的矛盾。
*   **计算高效：** 采用通道划分和渐进式残差结构，在保持多尺度特征提取能力的同时，有效抑制了多分支网络的参数冗余。
*   **物理可解释性：** 引入物理周期作为采样锚点，使模型的空间搜索不再是“盲目搜索”，而是具有物理意义的导航。

### 8. 不足与局限
*   **长程预测覆盖：** 虽然论文提到 ANCHOR 是通用骨干，但在实验部分重点展示了短期预测（M4），对于超长序列（如 ETT 数据集的长程预测）的对比相对较少。
*   **超参数敏感性：** 提取前 $K$ 个频率索引中的 $K$ 值选取可能对不同数据集具有敏感性，文中未详细讨论 $K$ 的自动化选择机制。
*   **硬件细节缺失：** 缺乏实际运行时间的对比数据，难以直观评估其在嵌入式或实时工业场景中的部署优势。

（完）
