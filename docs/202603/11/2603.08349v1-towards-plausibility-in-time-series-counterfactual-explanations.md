---
title: Towards plausibility in time series counterfactual explanations
title_zh: 迈向时间序列反事实解释的合理性
authors: "Marcin Kostrzewa, Krzysztof Galus, Maciej Zięba"
date: 2026-03-09
pdf: "https://arxiv.org/pdf/2603.08349v1"
tags: ["query:ts"]
score: 8.0
evidence: 时间序列分类的可解释反事实解释
tldr: 针对时间序列分类中反事实解释缺乏合理性的问题，本文提出一种基于梯度优化的新方法。该方法通过结合 soft-DTW 对齐与目标类别的 k 近邻样本，在损失函数中引入合理性约束，确保生成的反事实样本具有真实的时间结构。实验证明，该方法在保持有效性的同时，显著提升了时间序列模式的真实感和分布一致性，为可解释人工智能提供了更可靠的工具。
selection_source: fresh_fetch
motivation: 现有的时间序列反事实解释方法生成的样本往往缺乏现实的时间结构，难以在实际应用中被视为合理的解释。
method: 提出一种多目标优化方法，利用 soft-DTW 将生成的反事实样本与目标类别的 k 近邻进行对齐，以增强其时间合理性。
result: 实验表明，该方法在维持高有效性的同时，在分布对齐和时间真实感指标上显著优于现有的强基准方法。
conclusion: 通过结合 soft-DTW 和近邻对齐，可以生成既符合分类目标又具备高度时间模式一致性的高质量反事实解释。
---

## 摘要
我们提出了一种为时间序列分类问题生成合理反事实解释的新方法。该方法直接在输入空间中执行基于梯度的优化。为了增强合理性，我们将 soft-DTW（动态时间规整）对齐与目标类别的 $k$ 最近邻相结合，这有效地促使生成的反事实样本采用真实的时间结构。整体优化目标是一个平衡关键反事实属性的多方面损失函数。它包含了有效性、稀疏性和接近性的损失，以及基于 soft-DTW 的新型合理性组件。我们针对几种强大的参考方法对我们的方法进行了评估，从多个维度测量了生成的反事实样本的关键属性。结果表明，我们的方法在有效性方面取得了具有竞争力的性能，同时在与目标类别的分布对齐方面显著优于现有方法，表明其具有卓越的时间真实性。此外，定性分析突显了现有方法在保持真实时间结构方面的关键局限性。这项工作表明，所提出的方法能够为时间序列分类器一致地生成反事实解释，这些解释不仅有效，而且高度合理且符合时间模式。

## Abstract
We present a new method for generating plausible counterfactual explanations for time series classification problems. The approach performs gradient-based optimization directly in the input space. To enforce plausibility, we integrate soft-DTW (dynamic time warping) alignment with $k$-nearest neighbors from the target class, which effectively encourages the generated counterfactuals to adopt a realistic temporal structure. The overall optimization objective is a multi-faceted loss function that balances key counterfactual properties. It incorporates losses for validity, sparsity, and proximity, alongside the novel soft-DTW-based plausibility component. We conduct an evaluation of our method against several strong reference approaches, measuring the key properties of the generated counterfactuals across multiple dimensions. The results demonstrate that our method achieves competitive performance in validity while significantly outperforming existing approaches in distributional alignment with the target class, indicating superior temporal realism. Furthermore, a qualitative analysis highlights the critical limitations of existing methods in preserving realistic temporal structure. This work shows that the proposed method consistently generates counterfactual explanations for time series classifiers that are not only valid but also highly plausible and consistent with temporal patterns.

---

## 论文详细总结（自动生成）

以下是对论文《Towards plausibility in time series counterfactual explanations》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：如何在保持时间序列分类器反事实解释（Counterfactual Explanations, CFEs）有效性的同时，确保生成的解释具有**合理性（Plausibility）**。
*   **研究动机**：反事实解释通过回答“为了改变预测结果，输入需要做哪些最小改动”来提供可操作的见解。然而，现有的时间序列 CFE 方法往往生成类似“对抗攻击”的噪声，或者通过简单的片段替换导致时间结构不连续。在医疗（如 ECG）或金融等高风险领域，缺乏时间逻辑和现实感的解释难以获得用户信任。
*   **整体含义**：本文旨在通过引入时间对齐约束，使生成的反事实样本在统计分布和时间模式上更接近目标类别的真实数据。

### 2. 方法论：核心思想与关键技术
*   **核心思想**：在输入空间直接进行基于梯度的优化，通过一个多目标损失函数平衡有效性、接近性、稀疏性和合理性。
*   **关键技术细节**：
    *   **Soft-DTW 约束**：由于传统的动态时间规整（DTW）不可微，作者引入了 **soft-DTW**。它通过平滑最小化算子（soft-minimum）使对齐过程可微，从而允许梯度回传。
    *   **k-NN 对齐**：为了增强合理性，算法计算生成的反事实样本 $X'$ 与目标类别中 $k$ 个最近邻样本之间的平均 soft-DTW 距离。
*   **优化目标函数**：
    $$L_{CF} = L_{prox} + L_{sparse} + \lambda \cdot (L_{valid} + L_{DTW})$$
    *   $L_{prox}$：平方欧几里得距离，确保改动最小。
    *   $L_{sparse}$：$L_1$ 范数，鼓励局部化修改。
    *   $L_{valid}$：合页损失（Hinge Loss），确保分类器将 $X'$ 识别为目标类别。
    *   $L_{DTW}$：合理性损失，促使 $X'$ 学习目标类别的真实时间模式。

### 3. 实验设计
*   **数据集**：使用了来自 UCI 和 UEA 库的 8 个代表性数据集，包括单变量（如 CBF, TwoLeadECG, GunPoint, Earthquakes, Coffee, ItalyPowerDemand）和多变量（如 Cricket, Epilepsy）。
*   **Benchmark 与对比方法**：
    *   **Glacier**：基于自动编码器潜在空间扰动的方法。
    *   **M-CELS**：基于显著性图引导的稀疏扰动方法。
*   **评估指标**：
    *   **Validity (Val)**：预测标签翻转的成功率。
    *   **Proximity (L2) & Sparsity (L1)**：改动的幅度。
    *   **Plausibility (DTW)**：与目标类样本的平均 DTW 距离。
    *   **Isolation Forest Score**：使用孤立森林检测反事实样本是否被视为目标类分布内的“正常值”。

### 4. 资源与算力
*   **硬件环境**：实验在一台配备 **M4 芯片（14 核 CPU）和 48 GB 内存**的 MacBook Pro 上完成。
*   **训练时长**：文中未详细列出每个实验的具体分钟数，但通过“吞吐量（每秒处理样本数）”指标进行了效率分析。随着 $k$ 值增加，计算 soft-DTW 的开销显著增大，导致处理速度下降。
*   **开源资源**：作者提供了 GitHub 代码库以供复现。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 8 个不同维度、长度和类别数量的数据集，具有较好的泛化代表性。
*   **超参数分析**：针对 $\lambda$（平衡因子）和 $k$（近邻数）进行了详细的消融实验和敏感性分析。
*   **定性分析**：通过可视化 ECG 和 CBF 数据集的生成结果，直观展示了该方法在保留几何形状（如“钟形”或“漏斗形”）方面的优势。
*   **公平性**：对比方法均采用了原作者推荐的最佳配置，实验设计较为客观。

### 6. 主要结论与发现
*   **合理性显著提升**：该方法生成的反事实样本在 DTW 距离指标上通常比基准方法低一个数量级，且在孤立森林评分中表现优异，证明其更符合真实分布。
*   **有效性保障**：在几乎所有数据集上均达到了 100% 或接近 100% 的有效性（标签翻转成功率），远高于 Glacier 和 M-CELS。
*   **权衡关系（Trade-off）**：实验发现，为了获得更高的合理性，必须牺牲一定的接近性（即 $L_1$ 和 $L_2$ 距离会变大）。这意味着“合理的改动”往往比“最小的对抗扰动”需要更显著但更有意义的形态变化。

### 7. 优点：亮点与创新
*   **显式合理性建模**：不同于以往方法间接通过 AE 重构误差来约束合理性，本文直接利用 soft-DTW 进行时间模式对齐，思路更直接有效。
*   **鲁棒的时间对齐**：利用 DTW 对时间偏移的鲁棒性，生成的解释能够捕捉到目标类别的核心特征（如波峰位置的移动），而非简单的点对点修改。
*   **多变量支持**：方法自然扩展到多变量时间序列，表现稳健。

### 8. 不足与局限
*   **计算复杂度**：soft-DTW 的计算复杂度为 $O(T^2)$（$T$ 为序列长度），且需要对 $k$ 个邻居重复计算，这限制了其在超长序列上的实时应用。
*   **依赖 k-NN 质量**：合理性高度依赖于训练集中目标类样本的质量。如果目标类分布是多模态的（即存在多种截然不同的模式），简单的 $k$ 近邻对齐可能会导致生成的解释在不同模式间“迷失”。
*   **超参数敏感性**：$\lambda$ 的选择对结果影响较大，实际应用中可能需要针对不同领域进行精细调优。

（完）
