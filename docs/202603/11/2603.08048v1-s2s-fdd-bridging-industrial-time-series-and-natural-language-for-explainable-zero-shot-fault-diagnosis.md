---
title: "S2S-FDD: Bridging Industrial Time Series and Natural Language for Explainable Zero-shot Fault Diagnosis"
title_zh: S2S-FDD：连接工业时间序列与自然语言，实现可解释的零样本故障诊断
authors: "Baoxue Li, Chunhui Zhao"
date: 2026-03-09
pdf: "https://arxiv.org/pdf/2603.08048v1"
tags: ["query:ts"]
score: 8.0
evidence: 工业信号的可解释零样本故障诊断
tldr: 针对工业故障诊断中传统模型缺乏可解释性及大语言模型难以处理高维传感器信号的问题，本文提出S2S-FDD框架。该框架通过信号到语义算子将时间序列转化为自然语言描述，并结合多轮树状诊断方法，利用历史文档和动态查询实现可解释的零样本故障诊断，同时支持人机交互反馈以持续优化诊断性能。
selection_source: fresh_fetch
motivation: 传统诊断模型输出抽象且LLM难以直接理解高维工业信号，导致无法回答故障原因及修复建议等关键问题。
method: 设计信号到语义算子将时序信号转化为文本摘要，并采用多轮树状诊断结构结合历史文档进行推理。
result: 在多相流过程实验中证明了该框架在实现可解释的零样本故障诊断方面的有效性。
conclusion: 该研究成功桥接了工业信号与自然语言语义，为工业系统的智能化、可解释诊断提供了新路径。
---

## 摘要
故障诊断对于工业系统的安全运行至关重要。传统的诊断模型通常产生抽象的输出，如异常评分或故障类别，无法回答诸如“为什么”或“如何修复”等关键运维问题。虽然大语言模型（LLMs）具有强大的泛化和推理能力，但它们在离散文本语料库上的训练使得在处理高维、时序工业信号时存在语义鸿沟。为了应对这一挑战，我们提出了一个信号到语义（Signals-to-Semantics）故障诊断（S2S-FDD）框架，通过两项关键创新将高维传感器信号与自然语言语义相连接：我们首先设计了一个信号到语义算子，将抽象的时间序列信号转换为自然语言摘要，捕捉趋势、周期性和偏差。基于这些描述，我们设计了一种多轮树状结构诊断方法，通过参考历史维护文档并动态查询额外信号来进行故障诊断。该框架进一步支持人机回环反馈，以实现持续优化。在多相流过程上的实验证明了所提方法在可解释零样本故障诊断方面的可行性和有效性。

## Abstract
Fault diagnosis is critical for the safe operation of industrial systems. Conventional diagnosis models typically produce abstract outputs such as anomaly scores or fault categories, failing to answer critical operational questions like "Why" or "How to repair". While large language models (LLMs) offer strong generalization and reasoning abilities, their training on discrete textual corpora creates a semantic gap when processing high-dimensional, temporal industrial signals. To address this challenge, we propose a Signals-to-Semantics fault diagnosis (S2S-FDD) framework that bridges high-dimensional sensor signals with natural language semantics through two key innovations: We first design a Signal-to-Semantic operator to convert abstract time-series signals into natural language summaries, capturing trends, periodicity, and deviations. Based on the descriptions, we design a multi-turn tree-structured diagnosis method to perform fault diagnosis by referencing historical maintenance documents and dynamically querying additional signals. The framework further supports human-in-the-loop feedback for continuous refinement. Experiments on the multiphase flow process show the feasibility and effectiveness of the proposed method for explainable zero-shot fault diagnosis.

---

## 论文详细总结（自动生成）

这是一份关于论文《S2S-FDD: Bridging Industrial Time Series and Natural Language for Explainable Zero-shot Fault Diagnosis》的结构化深入总结：

### 1. 论文的核心问题与整体含义
*   **研究背景**：工业故障诊断对系统安全至关重要。虽然深度学习模型在检测异常方面表现出色，但通常只能输出抽象的评分或类别，无法解释“为什么发生故障”或“如何修复”。
*   **核心问题**：
    1.  **语义鸿沟**：大语言模型（LLM）擅长逻辑推理，但难以直接理解高维、连续、动态的工业传感器信号。
    2.  **数据稀缺**：工业现场故障样本极少，传统监督学习难以覆盖未见过的故障。
*   **研究目标**：提出一个名为 **S2S-FDD** 的框架，通过将时序信号转化为自然语言描述，利用 LLM 的推理能力实现**可解释的零样本（Zero-shot）故障诊断**。

### 2. 论文提出的方法论
该框架由两个核心组件构成：

*   **信号到语义（S2S）算子**：
    *   **重构逻辑**：利用正常工况下的数据通过 K-means 聚类提取代表性样本，构建状态矩阵 $D$。
    *   **残差计算**：将实时信号 $W_{in}$ 表示为正常模式的线性组合，计算重构残差。残差显著增大则预示故障。
    *   **文本生成**：根据残差分析识别异常变量，提取其趋势（增加/减少）、周期性、偏差程度等特征，并将其填入预设模板，由 LLM 生成简洁的自然语言摘要（如“压力传感器 PT312 在 150s 后持续下降，偏差达 20%”）。
*   **多轮树状诊断方法**：
    *   **知识检索（RAG）**：将生成的时序描述与历史维护记录、专家经验库进行向量匹配，检索相关知识。
    *   **树状推理与工具调用**：LLM 充当诊断专家。如果初始信息不足，LLM 可以通过“函数调用（Function Calling）”动态请求查询其他传感器的详细数据。
    *   **人机回环**：支持专家对诊断报告进行审核和反馈，并将验证后的结果存入知识库，实现闭环优化。

### 3. 实验设计
*   **实验场景**：使用英国克兰菲尔德大学（Cranfield University）的**多相流过程（Multiphase Flow Process）**设施数据集。该设施模拟水、油、气混合输送过程。
*   **故障类型**：选择了 5 种典型故障（如气路堵塞、水路堵塞、分离器堵塞等），共 13 个具体案例。
*   **对比方法（Benchmark）**：
    *   **非推理模型**：Qwen2.5 (7B/72B), DeepSeek-V3。
    *   **推理模型（Reasoning LLMs）**：DeepSeek-R1 (及其蒸馏版本 7B/32B), QwQ-32B。
*   **评估指标**：诊断准确率（ACC@5，即 5 次投票后的最终准确率）。

### 4. 资源与算力
*   **算力说明**：论文中**未明确提及**具体的硬件配置（如 GPU 型号、数量）或训练/推理时长。
*   **模型调用**：由于主要使用现成的 LLM（如 Qwen 和 DeepSeek 系列），实验重点在于 Prompt 工程和框架逻辑，而非模型本身的预训练。

### 5. 实验数量与充分性
*   **实验规模**：针对 5 种故障类型设计了 13 个测试案例。
*   **重复性验证**：每个案例独立运行 5 次推理，通过投票机制确定最终结果，以减少 LLM 输出的随机性影响。
*   **充分性评价**：实验覆盖了从 7B 到超大规模参数的模型，对比了“普通 LLM”与“强化推理 LLM”的差异。虽然样本案例数量（13个）不算庞大，但对于验证“零样本”和“可解释性”这一新颖框架而言，具有较强的代表性和说服力。

### 6. 主要结论与发现
*   **零样本能力显著**：在完全不使用故障样本训练的情况下，DeepSeek-R1 达到了 **76.92%** 的诊断准确率。
*   **推理模型占优**：具有强化推理能力的模型（如 DeepSeek-R1, QwQ）在准确率上远高于普通指令微调模型（如 Qwen2.5-7B 仅 23.08%）。
*   **可解释性强**：LLM 不仅给出了故障编号，还提供了详细的推理链（Reasoning Chain），能够解释为何排除某些故障，并指出关键的传感器异常证据。
*   **参数规模效应**：在推理模型中，参数量越大（如从 7B 到 32B 再到 Full R1），诊断的逻辑严密性和准确性越高。

### 7. 优点与亮点
*   **首创性**：首次正式定义了工业时序描述任务，并实现了时序信号与自然语言语义的深度对齐。
*   **无需故障数据**：解决了工业领域“故障样本获取难”的痛点，仅需少量正常数据即可部署。
*   **动态交互**：树状诊断结构允许模型像人类专家一样“按需索取”数据，提高了诊断效率并降低了上下文窗口压力。
*   **闭环进化**：通过人机回环机制，使系统具备了持续学习和知识积累的能力。

### 8. 不足与局限
*   **重构算子限制**：S2S 算子基于线性重构假设，对于极度复杂的非线性工业过程，可能无法精准捕捉所有细微异常。
*   **数据集单一**：目前仅在多相流过程一个数据集上进行了验证，其在不同行业（如电力、半导体制造）的泛化性仍待观察。
*   **实时性挑战**：多轮对话和 LLM 推理（尤其是 Reasoning 模型）存在较高的延迟，可能难以满足毫秒级的实时诊断需求。
*   **Prompt 依赖**：诊断效果高度依赖于 [PROCESS_INFO] 等背景知识的编写质量。

（完）
