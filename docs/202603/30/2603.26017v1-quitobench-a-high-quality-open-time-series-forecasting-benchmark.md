---
title: "QuitoBench: A High-Quality Open Time Series Forecasting Benchmark"
title_zh: QuitoBench：一个高质量的开放时间序列预测基准
authors: "Siqiao Xue, Zhaoyang Zhu, Wei Zhang, Rongyao Cai, Rui Wang, Yixiang Mu, Fan Zhou, Jianguo Li, Peng Di, Hang Yu"
date: 2026-03-27
pdf: "https://arxiv.org/pdf/2603.26017v1"
tags: ["query:ts"]
score: 10.0
evidence: 大规模时间序列预测基准测试
tldr: QuitoBench 是一个基于支付宝十亿级流量数据构建的高质量、状态平衡的时间序列预测基准，涵盖趋势、季节性和可预测性等8种状态。该研究评估了10种主流模型，发现深度学习模型在短上下文表现优异，而基础模型在长上下文占优，并揭示了数据规模对性能提升的作用远超模型规模。
selection_source: fresh_fetch
motivation: 现有的时间序列预测基准缺乏大规模、高质量的数据，且未能充分考虑趋势、季节性等影响预测难度的核心属性。
method: 提出 QuitoBench 基准，基于支付宝九大业务领域的十亿级语料库，通过8种状态组合对10种深度学习、基础模型及统计模型进行全面评估。
result: 实验发现深度学习模型在短上下文领先，而基础模型在长上下文占优，且可预测性是决定预测难度的核心因素。
conclusion: 增加训练数据量对提升预测性能的贡献显著大于增加模型参数量，这一结论在深度学习和基础模型中均成立。
---

## 摘要
时间序列预测在金融、医疗和云计算等领域至关重要，但其进展受到一个根本性瓶颈的制约：大规模、高质量基准测试集的匮乏。为了填补这一空白，我们推出了 QuitoBench，这是一个针对时间序列预测的状态平衡（regime-balanced）基准，涵盖了八种“趋势×季节性×可预测性”（TSF）状态，旨在捕捉与预测相关的属性，而非应用定义的领域标签。该基准基于 Quito 构建，后者是一个包含来自支付宝九个业务领域的十亿级应用流量时间序列语料库。通过在 232,200 个评估实例上对来自深度学习、基础模型和统计基准的 10 个模型进行测试，我们报告了四个关键发现：(i) 存在上下文长度的交叉现象，即深度学习模型在短上下文 ($L=96$) 下领先，而基础模型在长上下文 ($L \ge 576$) 下占据主导地位；(ii) 可预测性是影响难度的主要因素，导致不同状态间的 MAE 差距达 3.64 倍；(iii) 深度学习模型在参数量少 59 倍的情况下，性能可与基础模型持平或更优；(iv) 对于这两类模型，扩大训练数据量带来的收益都显著大于扩大模型规模。这些发现得到了跨基准和跨指标的一致性验证。我们的开源发布为时间序列预测研究提供了可复现的、感知状态的评估。

## Abstract
Time series forecasting is critical across finance, healthcare, and cloud computing, yet progress is constrained by a fundamental bottleneck: the scarcity of large-scale, high-quality benchmarks. To address this gap, we introduce \textsc{QuitoBench}, a regime-balanced benchmark for time series forecasting with coverage across eight trend$\times$seasonality$\times$forecastability (TSF) regimes, designed to capture forecasting-relevant properties rather than application-defined domain labels. The benchmark is built upon \textsc{Quito}, a billion-scale time series corpus of application traffic from Alipay spanning nine business domains. Benchmarking 10 models from deep learning, foundation models, and statistical baselines across 232,200 evaluation instances, we report four key findings: (i) a context-length crossover where deep learning models lead at short context ($L=96$) but foundation models dominate at long context ($L \ge 576$); (ii) forecastability is the dominant difficulty driver, producing a $3.64 \times$ MAE gap across regimes; (iii) deep learning models match or surpass foundation models at $59 \times$ fewer parameters; and (iv) scaling the amount of training data provides substantially greater benefit than scaling model size for both model families. These findings are validated by strong cross-benchmark and cross-metric consistency. Our open-source release enables reproducible, regime-aware evaluation for time series forecasting research.

---

## 论文详细总结（自动生成）

### QuitoBench：高质量开放时间序列预测基准论文总结

#### 1. 核心问题与研究动机
*   **核心问题**：时间序列预测领域缺乏类似于 CV（ImageNet）或 NLP（GLUE）的大规模、高质量标准基准。
*   **研究动机**：
    *   **基准生态缺失**：时间序列论文中基准类论文占比仅 4.2%，远低于 NLP 和 CV。
    *   **现有基准缺陷**：如 GIFT-Eval 和 Timer 存在分类粗糙（按应用领域而非统计特性分类）、分布严重倾斜（某些简单状态占比过高）、信息泄露（使用公开数据集导致预训练模型“见过”测试集）以及序列过短（无法评估长上下文）等问题。
    *   **模型选择困惑**：在参数量巨大的基础模型（FM）与轻量级深度学习（DL）模型之间，缺乏明确的部署指导。

#### 2. 方法论：核心思想与技术细节
*   **核心思想**：提出基于**状态平衡（Regime-balanced）**的评估框架，通过内在统计属性而非领域标签来定义预测难度。
*   **关键技术细节**：
    *   **TSF 诊断体系**：将序列按三个维度分类：
        1.  **趋势强度 (T)**：通过 STL 分解计算趋势分量解释的方差比例。
        2.  **季节性强度 (S)**：通过 STL 分解计算季节分量解释的方差比例。
        3.  **可预测性 (F)**：定义为 $1 - \text{归一化谱熵}$，衡量信号的规律性。
    *   **状态划分**：设定阈值（默认 0.4）将 T、S、F 简化为高/低二元标签，组合成 8 种 TSF 状态（如“高趋势-高季节性-低可预测性”）。
    *   **Quito 数据集**：源自支付宝生产环境，包含 9 个业务领域（金融、电商、基础设施等），共 16 亿 Token，涵盖分钟级和小时级粒度。
    *   **QuitoBench 构建**：从 Quito 中进行分层抽样，确保 8 种 TSF 状态在测试集中近乎均匀分布（每种约 12.5%），从而消除分布偏差。

#### 3. 实验设计
*   **数据集/场景**：使用 Quito-Min（10 分钟采样）和 Quito-Hour（1 小时采样），序列长度达 5,900 至 15,300 个时间步。
*   **对比方法（10 种模型）**：
    *   **深度学习 (DL)**：CrossFormer, DLinear, iTransformer, PatchTST, TSMixer。
    *   **基础模型 (FM)**：Chronos-2, TimesFM-2.5, TiRex。
    *   **统计基准**：指数平滑 (ES), 季节性朴素预测 (SNaive)。
*   **评估配置**：3 种上下文长度 ($L \in \{96, 576, 1024\}$) $\times$ 3 种预测长度 ($H \in \{48, 288, 512\}$) $\times$ 2 种模式（单变量/多变量），共 18 种配置。
*   **评估方式**：采用单位步长的**密集滚动窗口评估**，总计生成约 1.6 亿个预测点，确保结果的统计稳定性。

#### 4. 资源与算力
*   **算力说明**：论文明确提到在进行缩放定律（Scaling Laws）实验时，使用了 **8 台 NVIDIA A100 80GB GPU**。
*   **训练细节**：DL 模型经历了超参调优、从头训练和测试三个阶段；FM 模型主要采用零样本（Zero-shot）推理，但在缩放实验中也进行了从头训练。

#### 5. 实验数量与充分性
*   **实验规模**：涵盖 232,200 个评估实例，生成 1.6 亿个预测值，规模远超传统基准。
*   **充分性与公平性**：
    *   **客观性**：通过状态平衡设计，避免了模型因在某种常见状态（如高季节性）表现好而掩盖在其他状态下的失败。
    *   **公平性**：所有模型在相同的无泄露时间切片上评估；DL 模型经过严格调优，FM 模型采用零样本以体现其通用性。
    *   **鲁棒性验证**：进行了跨指标（MAE vs MSE）和跨基准（Quito vs Timer）的一致性测试，证明结论具有普适性。

#### 6. 主要结论与发现
1.  **上下文长度交叉**：DL 模型在短上下文 ($L=96$) 表现更好；FM 模型在长上下文 ($L \ge 576$) 展现出显著优势。
2.  **难度驱动因素**：**可预测性 (F)** 是决定难度的核心，不同状态间的 MAE 差距可达 3.64 倍。
3.  **参数效率**：DL 模型在参数量少 59 倍的情况下，整体性能可匹配甚至超越 FM 模型。
4.  **缩放定律**：对于 DL 和 FM，**增加训练数据量**带来的性能提升远大于增加模型参数量。
5.  **模型选择建议**：资源受限或短历史用 CrossFormer；长历史且强季节性序列用 Chronos-2。

#### 7. 优点：亮点设计
*   **状态平衡设计**：解决了传统基准中“简单数据主导总分”的问题，提供了更细致的诊断视角。
*   **彻底消除泄露**：由于数据源自封闭的工业环境，完全排除了预训练模型在公开数据集上可能产生的直接或间接泄露。
*   **长序列支持**：支持长达 1024 的上下文评估，填补了现有基准多为短序列的空白。

#### 8. 不足与局限
*   **数据来源单一**：虽然涵盖 9 个业务领域，但所有数据均来自同一家公司（蚂蚁集团/支付宝），可能存在特定的工业负载偏置。
*   **任务覆盖限制**：目前主要关注点预测（Point Forecasting），未深入探讨概率预测、异常检测等其他时间序列任务。
*   **应用场景局限**：数据主要为应用流量（Application Traffic），对于物理传感器、气象或金融高频交易等其他类型的序列特征覆盖可能有限。

（完）
