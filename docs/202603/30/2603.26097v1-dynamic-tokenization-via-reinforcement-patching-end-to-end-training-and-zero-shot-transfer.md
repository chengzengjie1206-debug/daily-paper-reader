---
title: "Dynamic Tokenization via Reinforcement Patching: End-to-end Training and Zero-shot Transfer"
title_zh: 通过强化分块实现动态分词：端到端训练与零样本迁移
authors: "Yulun Wu, Sravan Kumar Ankireddy, Samuel Sharpe, Nikita Seleznev, Dehao Yuan, Hyeji Kim, Nam H. Nguyen"
date: 2026-03-27
pdf: "https://arxiv.org/pdf/2603.26097v1"
tags: ["query:ts"]
score: 8.0
evidence: 长时程时间序列序列的动态分词
tldr: 针对长序列数据中固定尺寸分块缺乏灵活性且难以端到端优化的问题，本文提出了ReinPatch框架。该框架利用强化学习（GRPG）将分块边界确定建模为离散决策过程，实现了分块策略与下游模型的联合优化。ReinPatch支持严格的压缩率控制和多级分层建模，在时间序列预测任务中表现优异，并具备作为独立分块器的潜力。
selection_source: fresh_fetch
motivation: 现有的固定尺寸分块方法难以捕捉数据的动态特性，而现有的自适应分块方法通常依赖启发式规则或复杂的连续松弛技术。
method: 提出ReinPatch框架，利用组相对策略梯度（GRPG）将分块边界定位建模为离散决策，实现分块策略与下游骨干网络的端到端联合训练。
result: 在时间序列预测实验中，ReinPatch在性能上优于现有的数据驱动分块策略，并能有效控制压缩率和支持多级分层建模。
conclusion: ReinPatch为长序列数据的动态分块提供了一种高效的强化学习方案，其解耦设计使其能够作为通用的基础分块器应用于多种场景。
---

## 摘要
高效地聚合空间或时间范围以获取紧凑表示已成为现代深度学习模型的一个统一原则，然而，为长序列数据（特别是像时间序列这样的连续序列）学习数据自适应表示仍然是一个开放的挑战。虽然固定大小的分块（patching）提高了可扩展性和性能，但端到端地发现变长、数据驱动的分块通常迫使模型依赖于软离散化、特定的骨干网络或启发式规则。在这项工作中，我们提出了强化分块（Reinforcement Patching，简称 ReinPatch），这是第一个利用强化学习联合优化序列分块策略及其下游序列骨干模型的框架。通过将分块边界放置建模为通过群体相对策略梯度（GRPG）优化的离散决策过程，ReinPatch 绕过了对连续松弛的需求，并以一种自然的方式执行动态分块策略优化。此外，我们的方法允许严格执行所需的压缩率，使下游骨干网络能够高效扩展，并自然地支持多级分层建模。我们在时间序列预测数据集上评估了 ReinPatch，结果表明，与最先进的数据驱动分块策略相比，它展现出了极具竞争力的性能。此外，我们的分离式设计允许将分块模块提取为独立的基础分块器，为社区提供了关于纯性能驱动的神经分块策略所偏好的分割行为的视觉和经验见解。

## Abstract
Efficiently aggregating spatial or temporal horizons to acquire compact representations has become a unifying principle in modern deep learning models, yet learning data-adaptive representations for long-horizon sequence data, especially continuous sequences like time series, remains an open challenge. While fixed-size patching has improved scalability and performance, discovering variable-sized, data-driven patches end-to-end often forces models to rely on soft discretization, specific backbones, or heuristic rules. In this work, we propose Reinforcement Patching (ReinPatch), the first framework to jointly optimize a sequence patching policy and its downstream sequence backbone model using reinforcement learning. By formulating patch boundary placement as a discrete decision process optimized via Group Relative Policy Gradient (GRPG), ReinPatch bypasses the need for continuous relaxations and performs dynamic patching policy optimization in a natural manner. Moreover, our method allows strict enforcement of a desired compression rate, freeing the downstream backbone to scale efficiently, and naturally supports multi-level hierarchical modeling. We evaluate ReinPatch on time-series forecasting datasets, where it demonstrates compelling performance compared to state-of-the-art data-driven patching strategies. Furthermore, our detached design allows the patching module to be extracted as a standalone foundation patcher, providing the community with visual and empirical insights into the segmentation behaviors preferred by a purely performance-driven neural patching strategy.

---

## 论文详细总结（自动生成）

以下是对论文《Dynamic Tokenization via Reinforcement Patching: End-to-end Training and Zero-shot Transfer》的结构化深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
论文聚焦于长序列数据（尤其是连续的时间序列）的高效表示问题。在深度学习中，将原始信号压缩为紧凑的分块（Patching）已成为提升模型扩展性的关键。然而，现有的分块策略存在明显局限：
*   **固定尺寸分块**（如 PatchTST）无法适应数据的局部动态变化。
*   **现有自适应方法**通常依赖于手工设计的启发式规则（如基于熵或方差）或复杂的连续松弛技术（为了使边界决策可微），这导致分块策略与特定骨干网络耦合过紧，难以通用化。
*   **核心动机**：探索一种能够与下游任务联合优化、且能自然处理离散边界决策的动态分块框架。

### 2. 方法论：ReinPatch 框架
ReinPatch 的核心思想是将分块边界的放置建模为一个**离散决策过程**，并利用强化学习（RL）进行优化。

*   **核心思想**：将分块策略网络（Policy Net）与下游序列模型（Backbone）解耦。策略网络决定在哪里划定边界，骨干网络则在这些边界基础上进行预测。
*   **关键技术细节**：
    *   **MDP 建模**：将分块过程建模为马尔可夫决策过程（或上下文老虎机）。状态 $s_t$ 是输入序列，动作 $a_t \in \{0, 1\}$ 表示是否在当前位置设置边界。
    *   **优化算法（GRPG）**：采用**组相对策略梯度（Group Relative Policy Gradient）**。对于每个序列，采样 $G$ 组不同的分块方案，根据下游任务的负损失作为奖励（Reward），通过计算组内相对优势来更新策略网络。这种方式无需训练额外的 Critic 网络，且避开了复杂的连续松弛。
    *   **硬性压缩率限制**：不同于以往使用辅助损失函数的软约束，ReinPatch 在环境中强制执行最小压缩率。如果策略网络生成的块太细碎，系统会自动合并右侧的块。这允许在训练前就预规划模型规模。
    *   **解耦设计**：分块器是一个轻量级的 Transformer，可以独立于骨干网络进行预训练，从而成为“基础分块器（Foundation Patcher）”。

### 3. 实验设计
*   **数据集**：使用了 6 个主流的多变量时间序列预测基准数据集：ETTh1, ETTh2, ETTm1, ETTm2, Weather, Electricity。
*   **预训练数据集**：使用统一时间序列数据集（UTSD）进行大规模预训练，以验证零样本（Zero-shot）迁移能力。
*   **Benchmark 与对比方法**：
    *   **固定尺寸类**：Static Patching, Selective Patching, Periodic Patching。
    *   **变量尺寸/规则类**：Entropy Patching（基于熵）, TimeSqueeze（基于局部方差）。
    *   **端到端学习类**：H-Net（基于余弦相似度的连续松弛方法）。
*   **评估指标**：均方误差（MSE）和平均绝对误差（MAE），涵盖四个预测窗口（96, 192, 336, 720）。

### 4. 资源与算力
*   **文中说明**：论文**未明确提及**具体的 GPU 型号、数量或具体的训练总时长。
*   **隐含信息**：由于涉及大规模数据集（UTSD）的预训练以及强化学习中的多组采样（Group size $G$），可以推断其算力需求高于传统的监督学习模型，但其分块器本身设计为轻量级 Transformer，旨在平衡效率。

### 5. 实验数量与充分性
*   **实验规模**：在 6 个数据集、24 种预测配置下进行了测试，并提供了 5 个随机种子的平均值和标准差。
*   **消融实验**：
    *   对比了不同回看窗口（Look-back window）对性能的影响，证明了 ReinPatch 在长窗口下的优势。
    *   对比了“端到端训练”与“零样本迁移”两种模式。
*   **客观性与公平性**：所有对比方法均使用了统一的骨干网络架构，仅改变分块策略，这保证了对比的公平性，排除了架构红利。

### 6. 主要结论与发现
*   **性能领先**：ReinPatch 在绝大多数配置下优于现有的静态和动态分块方法。
*   **零样本迁移能力强**：在 UTSD 上预训练的“基础分块器”在未见过的任务上表现甚至优于针对该任务专门训练的端到端模型，显示出强大的泛化性。
*   **长序列优势**：随着输入窗口增加，ReinPatch 与基于启发式规则（如熵）的方法之间的差距拉大，证明了学习到的策略比人工规则更鲁棒。
*   **分块行为洞察**：可视化显示，ReinPatch 倾向于将主要的转折点包裹在块内，而不是简单地在变化剧烈处切分，这保留了局部的时间结构。

### 7. 优点（亮点）
*   **首创性**：首次将强化学习引入序列分块的端到端联合优化。
*   **架构无关性**：分块器与骨干网络解耦，使其可以作为通用的预处理模块或插件使用。
*   **硬约束机制**：通过环境强制执行压缩率，解决了“奖励黑客（Reward Hacking）”问题，并有利于模型推理效率的预规划。
*   **多级支持**：自然支持多层级的分层建模（Hierarchical Modeling）。

### 8. 不足与局限
*   **单变量限制**：目前的基础分块器主要在单变量数据上预训练，尚未充分探索多变量间的跨通道分块依赖。
*   **因果任务挑战**：在生成式（因果）任务中，由于无法预见未来分布，在线阈值处理仍是一个挑战，目前主要在非因果任务（如预测）中表现最稳健。
*   **训练开销**：强化学习的采样过程（Group sampling）在训练阶段会增加计算负担。

（完）
