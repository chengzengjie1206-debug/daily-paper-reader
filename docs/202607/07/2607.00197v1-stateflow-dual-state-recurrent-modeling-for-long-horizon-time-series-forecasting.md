---
title: "StateFlow: Dual-State Recurrent Modeling for Long-Horizon Time Series Forecasting"
title_zh: StateFlow：用于长程时间序列预测的双状态循环建模
authors: "Haroon Gharwi, Yue Dai, Kai Shu"
date: 2026-06-30
pdf: "https://arxiv.org/pdf/2607.00197v1"
tags: ["query:ts"]
score: 10.0
evidence: 长程多元时间序列预测
tldr: 针对长程多变量时间序列预测中非平稳性和误差累积的挑战，本文提出StateFlow框架。该方法扩展了VARNN模型，通过双状态循环骨干网络同时捕捉主要时间动力学（隐藏状态）和局部预测偏差（残差记忆状态）。结合分块解码器和两阶段优化策略，StateFlow在保持线性循环编码和紧凑设计的同时，在多个基准数据集上达到了与Transformer等强基准模型相当的性能。
selection_source: fresh_fetch
motivation: 现有的VARNN模型虽能通过残差记忆追踪序列变动，但仅限于单步预测，难以直接应用于复杂的长程多变量时间序列预测任务。
method: 提出StateFlow框架，利用双状态循环网络提取隐藏状态和残差记忆轨迹，并通过分块解码器和两阶段优化策略实现直接多步预测。
result: 在标准长程预测基准测试中，StateFlow展现出与线性、循环、卷积及Transformer类模型相比极具竞争力的预测性能。
conclusion: 该研究证明了结合残差记忆的循环模型在处理长程预测任务时具有高效性，为非平稳时间序列建模提供了一种紧凑且强大的新方案。
---

## 摘要
长程多变量时间序列预测（LTSF）由于非平稳性、状态转移和误差累积，仍然具有挑战性。变分感知递归神经网络（VARNN）旨在通过维护由单步预测误差驱动的残差记忆状态来跟踪这种变异性。然而，其原始公式仅限于单步序列回归，并不直接支持多步预测。在这项工作中，我们将 VARNN 扩展到长程预测，并引入了 StateFlow，这是一个循环预测框架，它使用 VARNN 作为双状态循环骨干，从回看序列中捕获两个互补信号：代表主要时间动态（包括趋势、季节性、水平变化和循环模式）的隐藏状态轨迹，以及代表结构化局部预测偏差的残差记忆轨迹，后者由单步基础预测与观测值之间误差的非线性循环变换驱动。一个基于块的解码器分别汇总这些轨迹，并将它们映射到未来时间范围以进行直接多步预测。我们进一步采用了一种两阶段优化策略，首先通过单步基础预测目标训练 VARNN 编码器，以优化回看序列上的内部表示，然后训练一个针对特定预测范围的解码器进行直接多步预测。在标准 LTSF 基准测试上的实验表明，StateFlow 在保持线性循环编码和紧凑模型设计的同时，与强大的线性、循环、卷积和基于 Transformer 的基线模型相比，取得了具有竞争力的性能。

## Abstract
Long-horizon multivariate time series forecasting (LTSF) remains challenging due to non-stationarity, regime shifts, and error accumulation. The Variability-Aware Recursive Neural Network (VARNN) is designed to track such variability by maintaining a residual-memory state driven by one-step prediction errors. However, its original formulation is limited to one-step sequence regression and does not directly support multi-step forecasting. In this work, we extend VARNN to long-horizon forecasting and introduce StateFlow, a recurrent forecasting framework that uses VARNN as a dual-state recurrent backbone to capture two complementary signals from the lookback sequence: a hidden-state trajectory representing primary temporal dynamics, including trend, seasonality, level changes, and recurring patterns, and a residual-memory trajectory representing structured local prediction deviations, driven from a nonlinear recurrent transformation of errors between one-step base predictions and observed values. A chunk-based decoder separately summarizes these trajectories and maps them to the future horizon for direct multi-step forecasting. We further employ a two-stage optimization strategy that first trains the VARNN encoder through a one-step base prediction objective to optimize the internal representations over the lookback sequence, and then trains a horizon-specific decoder for direct multi-step forecasting. Experiments on standard LTSF benchmarks show that StateFlow achieves competitive performance against strong linear, recurrent, convolutional, and Transformer-based baselines while preserving linear recurrent encoding and a compact model design.

---

## 论文详细总结（自动生成）

这是一份关于论文《StateFlow: Dual-State Recurrent Modeling for Long-Horizon Time Series Forecasting》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：长程多变量时间序列预测（LTSF）面临非平稳性、状态转移（Regime Shifts）和误差累积的挑战。
*   **研究动机**：
    *   **Transformer 的局限**：虽然目前主流，但具有序列长度的平方复杂度，且全局注意力机制在处理长回看窗口时计算开销大。
    *   **传统 RNN 的不足**：虽然具有线性复杂度，但通常将所有信息压缩进单一隐藏状态，难以显式捕捉非平稳环境下的系统性预测偏差。
    *   **VARNN 的局限**：现有的变分感知递归神经网络（VARNN）虽能通过残差记忆追踪变异性，但仅限于单步预测，无法直接用于长程多步预测。
*   **整体含义**：本文提出 **StateFlow** 框架，旨在将 VARNN 扩展至长程预测领域，通过双状态循环建模同时捕捉时间动力学和局部偏差。

### 2. 核心方法论
StateFlow 包含三个主要组件：
*   **双状态循环编码器（VARNN Encoder）**：
    *   **隐藏状态 ($h_t$)**：捕捉主要的趋势、季节性和循环模式。
    *   **残差记忆状态 ($e_t$)**：通过非线性变换捕捉“单步基础预测”与“实际观测值”之间的误差（残差）。这种设计允许模型显式地跟踪序列的漂移和偏差。
*   **分块解码器（Chunk-based Decoder）**：
    *   将编码器生成的长轨迹序列划分为多个小块（Chunks），分别对隐藏状态和残差记忆轨迹进行局部汇总。
    *   这种设计在保留局部模式的同时，大幅减少了直接展平（Flattening）带来的参数爆炸问题。
*   **两阶段优化策略**：
    *   **第一阶段**：通过单步预测目标预训练编码器，使残差记忆状态获得明确的学习信号。
    *   **第二阶段**：冻结编码器，仅针对特定的预测长度（Horizon）训练解码器，实现直接多步预测。

### 3. 实验设计
*   **数据集**：使用了 7 个标准 LTSF 基准数据集：ETTh1, ETTh2, ETTm1, ETTm2, Weather, ECL (电力), Traffic (交通)。
*   **实验设置**：回看窗口 $L=96$，预测长度 $H \in \{96, 192, 336, 720\}$。
*   **对比方法（Baselines）**：
    *   **Transformer 类**：iTransformer, PatchTST, Crossformer, Autoformer。
    *   **线性类**：DLinear, RLinear, TiDE。
    *   **卷积/其他类**：TimesNet, SCINet, Stationary。
*   **评估指标**：均方误差（MSE）和平均绝对误差（MAE）。

### 4. 资源与算力
*   **算力说明**：论文中**未明确提及**具体的 GPU 型号、数量或总训练时长。
*   **实现细节**：提到使用 PyTorch 实现，随机种子为 2026。
*   **复杂度分析**：论文强调了该模型具有**线性计算复杂度** $O(L)$，相比 Transformer 的 $O(L^2)$ 在长序列处理上更具优势。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在 7 个数据集、4 种预测长度下进行了全面测试，共计 28 组核心对比实验。
    *   **消融实验**：涵盖了编码器表示（h vs e vs h+e）、双状态融合策略（独立分块 vs 联合分块）、解码器结构（分块 vs 直接展平）、编码器类型（VARNN vs LSTM/GRU/RNN）以及优化策略（一阶段 vs 两阶段）。
*   **充分性评价**：实验设计非常充分且客观。通过与多种不同架构（线性、注意力、卷积）的最新 SOTA 模型对比，证明了其竞争力和参数效率。

### 6. 主要结论与发现
*   **性能领先**：StateFlow 在 28 项设置中获得了 15 项 MSE 第一和 14 项 MAE 第一，表现优于大多数 Transformer 和线性模型。
*   **残差记忆的有效性**：显式建模预测残差能显著提升非平稳序列的预测精度。
*   **参数效率**：相比于 LSTM，VARNN 编码器的参数量减少了约 80%，且预测精度更高。
*   **两阶段训练优势**：先学习残差感知表示再进行长程解码，比端到端联合训练效果更好。

### 7. 优点（亮点）
*   **高效性**：保持了循环神经网络的线性扩展性，适合处理长回看窗口。
*   **创新的双状态设计**：将“规律性动态”与“偏差性动态”解耦，增强了对非平稳数据的鲁棒性。
*   **紧凑的模型设计**：通过分块解码器平衡了性能与参数量，避免了传统 RNN 在长程预测中的表示坍缩。
*   **模块化**：预训练的编码器可以复用于不同的预测长度，节省了重复训练的开销。

### 8. 不足与局限
*   **变量间依赖建模**：目前采用通道独立（Channel-independent）策略，虽然在很多数据集上有效，但在变量间相关性极强的场景（如高维 ECL 或 Traffic）下，可能不如 iTransformer 等显式建模跨变量相关性的模型。
*   **训练流程复杂**：两阶段优化虽然提升了性能，但相比一阶段端到端训练，增加了训练流程的复杂度和管理成本。
*   **超参数敏感性**：分块大小（Chunk size）和步长（Stride）等超参数可能需要针对不同数据集进行精细调优。

（完）
