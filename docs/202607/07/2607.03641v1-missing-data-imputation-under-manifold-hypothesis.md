---
title: Missing Data Imputation under Manifold Hypothesis
title_zh: 流形假设下的缺失数据填补
authors: "Zelong Bi, Amuchechukwu Ibenegbu, Sarat Moka"
date: 2026-07-03
pdf: "https://arxiv.org/pdf/2607.03641v1"
tags: ["query:ts"]
score: 9.0
evidence: 通过流形假设填补缺失数据
tldr: 本研究基于流形假设，利用混合变分自编码器（VAEs）提取高维数据的低维几何结构，并提出一种基于模型的缺失数据填补方法。该方法通过采样-重要性-重采样（SIR）程序从条件概率分布中采样，并结合潜空间扩散模型，在保持数据几何特征的同时实现高效、可量化不确定性的实时填补，显著提升了填补的准确性与灵活性。
selection_source: fresh_fetch
motivation: 旨在利用高维数据背后的低维流形结构，解决现有缺失数据填补方法难以捕捉复杂局部与全局变量关系的问题。
method: 提出一种基于混合VAE和SIR程序的填补框架，并引入潜空间联合扩散模型来增强对缺失值的采样与建模能力。
result: 该方法在多项基准测试中达到了最先进的填补性能，能够有效量化不确定性，并支持无需重新训练的即时填补。
conclusion: 结合流形几何与生成模型为缺失数据填补提供了系统且高效的方案，证明了尊重底层几何结构对提升填补质量的关键作用。
---

## 摘要
流形假设认为高维数据集中在低维嵌入流形附近。混合变分自编码器（VAEs）的最新进展为准确提取这种底层结构提供了强大的工具。由此产生的几何结构自然地引入了变量之间的局部和全局关系，从而为缺失数据填补提供了一种系统化的方法。我们提出了一种基于模型的填补方法，通过采样-重要性-重采样（SIR）过程实现从 \( p(\bm{x}_{\mathrm{mis}} \mid \bm{x}_{\mathrm{obs}}) \) 中采样，并可进一步通过潜空间中的联合扩散模型进行增强。我们的方法在遵循底层几何结构的同时填补缺失数据，与现有最先进的方法相比具有竞争力的性能，能够量化填补的不确定性，且由于是基于模型的，因此支持即时填补而无需重新运行整个流程。

## Abstract
The manifold hypothesis posits that high-dimensional data are concentrated near a low-dimensional embedded manifold. Recent advances in mixture variational autoencoders (VAEs) provide a powerful tool for extracting such underlying structure in a faithful manner. The resulting geometric structure naturally introduces local and global relationships among variables, thereby providing a systematic way of imputing missing data. We propose a model-based imputation method that enables sampling from \( p(\bm{x}_{\mathrm{mis}} \mid \bm{x}_{\mathrm{obs}}) \) via a sampling-importance-resampling (SIR) procedure, which can be further augmented with a joint diffusion model in the latent space. Our method imputes missing data while respecting the underlying geometry, achieves competitive performance compared to state-of-the-art procedures, quantifies uncertainty in the imputations, and is model-based, thereby enabling on-the-fly imputation without rerunning the entire procedure.

---

## 论文详细总结（自动生成）

这篇论文《Missing Data Imputation under Manifold Hypothesis》（流形假设下的缺失数据填补）由新南威尔士大学的研究团队撰写。以下是对该论文的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：如何在数据满足“流形假设”（即高维数据实际上分布在低维流形附近）的情况下，更有效地填补缺失值。
*   **研究动机**：现有的主流填补方法（如 MissForest）虽然强大，但往往忽略了数据背后的几何结构。如果数据存在流形结构，变量之间就存在确定性的几何约束（例如，若数据在圆周上，已知 $x$ 坐标，则 $y$ 坐标被严格限制）。
*   **整体含义**：论文旨在结合**流形学习**与**生成式建模**，利用混合变分自编码器（Mixture VAEs）捕获数据的局部和全局几何关系，从而实现既符合数据分布又尊重几何结构的填补。

### 2. 方法论
核心思想是将填补问题转化为从条件概率分布 $p(x_{mis} | x_{obs})$ 中采样的过程。
*   **流形学习基础**：采用混合 VAE 模型，将流形划分为多个“图表”（Charts）。每个图表由一个局部坐标映射（编码器）及其逆映射（解码器）组成。
*   **SIR 采样程序**：由于直接从 $p(x_{mis} | x_{obs})$ 采样较难，论文引入了**采样-重要性-重采样（SIR）**算法。
    1.  从先验分布中抽取潜在变量（坐标 $z$ 和图表索引 $c$）。
    2.  根据观测值 $x_{obs}$ 计算每个样本的权重（似然值）。
    3.  根据权重进行重采样，获得符合观测约束的潜在变量。
    4.  通过解码器生成缺失部分的填补值。
*   **联合潜空间扩散模型**：为了生成更高质量的潜在样本，论文设计了一个联合扩散过程，同时处理连续变量（坐标 $z$）和离散变量（图表索引 $c$），在潜空间中细化数据分布。
*   **模型特性**：该方法是**归纳式（Inductive）**的，一旦训练完成，即可对新样本进行实时填补，无需重新运行整个流程。

### 3. 实验设计
*   **数据集**：
    *   **合成数据集**：双圆环（Two Circles）、球面（Sphere）、环面（Torus），用于验证几何保持能力。
    *   **真实数据集**：超导材料（Superconductivity）、能源（Energy）、金融（Finance）、发电厂（Powerplant）、葡萄酒（Wine）、面部表情（Facial Expression）。
*   **缺失机制**：测试了完全随机缺失（MCAR）、随机缺失（MAR）和非随机缺失（MNAR）三种场景。
*   **基准方法（Benchmarks）**：对比了均值填补、PCA、KNN、MICE、MissForest、GAIN（生成对抗网络）和标准 VAE。
*   **评估指标**：RMSE（均方根误差）、Wasserstein 距离（衡量分布一致性）以及下游回归任务的预测性能。

### 4. 资源与算力
*   论文提到计算工作使用了新南威尔士大学（UNSW Sydney）的 **Katana 计算集群**。
*   **具体细节**：文中**未明确说明**具体的 GPU 型号、数量或具体的训练时长。但提到扩散模型使用了 500 个步长，且由于是在低维潜空间操作，架构相对轻量。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 9 个不同性质的数据集，缺失率从 5% 跨越到 45%，并覆盖了三种缺失机制。
*   **充分性**：实验设计非常详尽。除了常规的误差分析，还引入了 Wasserstein 距离来评估几何保真度，并进行了下游任务性能测试。
*   **客观性**：对比了多种经典与前沿算法，并在附录中提供了大量的实验数据表，结果展示较为透明。

### 6. 主要结论与发现
*   **几何保持**：在合成数据集上，该方法在保持流形形状方面显著优于 MissForest 等方法。
*   **性能优势**：在真实数据集中，该方法与最强基准 MissForest 表现相当，但在**高缺失率**和**复杂缺失机制（MAR/MNAR）**下表现出更强的鲁棒性和领先优势。
*   **指标反思**：论文发现 RMSE 在流形数据填补中可能具有误导性，而 Wasserstein 距离能更好地反映填补值是否落在流形结构上。

### 7. 优点
*   **尊重几何结构**：是少数显式利用流形假设进行填补的方法，填补结果更符合物理或逻辑约束。
*   **不确定性量化**：作为概率模型，它不仅提供点估计，还能通过多次采样量化填补的不确定性。
*   **灵活性**：支持在线/即时填补，适用于流式数据场景。

### 8. 不足与局限
*   **流形假设依赖**：如果数据本身不具备明显的流形结构（如纯类别数据或极高维稀疏数据），该方法的优势可能无法体现。
*   **连续性限制**：目前主要针对连续变量设计，对离散/分类变量的直接填补支持有限。
*   **训练样本要求**：学习可靠的流形结构需要一定数量的完整观测样本作为训练集，在极端缺乏完整数据的场景下可能难以初始化。

（完）
