---
title: Advances in Neural Controlled Differential Equations
title_zh: 神经控制微分方程的进展
authors: Benjamin Walker
date: 2026-07-06
pdf: "https://arxiv.org/pdf/2607.05280v1"
tags: ["query:ts"]
score: 9.0
evidence: 针对不规则采样数据的连续时间模型
tldr: 针对神经控制微分方程（NCDE）在处理连续时间序列时计算成本高且难以并行的问题，本文提出了三种改进方法：Log-NCDEs利用Log-ODE方法加速训练，Linear NCDEs通过线性向量场实现并行计算，Structured Linear NCDEs进一步优化效率。这些方法在保持高表达能力的同时，将训练速度提升了三个数量级，并在多项基准测试中达到SOTA水平。
selection_source: fresh_fetch
motivation: 传统的NCDE模型由于采用非线性向量场，导致计算开销巨大且无法并行化，限制了其在大规模数据上的应用。
method: 本文提出了Log-NCDEs、线性NCDEs以及结构化线性NCDEs，通过近似求解、线性化向量场和结构化矩阵来优化计算。
result: 实验表明，所提方法在多个时间序列基准任务上实现了最先进的性能，同时将单步训练时间缩短了多达三个数量级。
conclusion: 通过引入线性化和结构化设计，NCDE可以在不损失表达能力的前提下显著提升扩展性和计算效率。
---

## 摘要
许多现实世界的系统是连续演化的，然而大多数机器学习模型将时间序列解释为离散序列。相比之下，连续时间方法将时间序列视为底层输入路径的样本，这种表述方式能够自然地处理不规则采样或过采样的数据。在这些方法中，神经控制微分方程（NCDEs）是一类具有极强表达能力的模型，它们利用神经网络对向量场进行参数化，并通过求解由输入路径驱动的动力系统来演化其隐藏状态。NCDEs 通常使用非线性向量场，因此其表达能力和连续时间灵活性是以计算成本高昂且本质上具有顺序性的前向传播为代价的，这限制了它们的可扩展性和实际应用。本论文通过三项互补的贡献推进了 NCDEs 的训练和可扩展性。首先，基于神经粗糙微分方程，Log-NCDEs 应用 Log-ODE 方法在训练期间高效地逼近 NCDE 的解，从而提高了计算速度和经验性能。其次，线性 NCDEs（Linear NCDEs）用线性向量场取代了非线性向量场，在不牺牲理论表达能力的前提下，实现了闭式解和时间并行计算。第三，结构化线性 NCDEs（Structured Linear NCDEs）利用结构化线性向量场进一步增强了效率，同时保持了理论表达能力和经验性能。总之，这些方法将 NCDE 每个训练步骤的时间缩短了多达三个数量级，同时在各种时间序列基准测试中实现了最先进的性能。

## Abstract
Many real-world systems evolve continuously, yet most machine learning models interpret time series as discrete sequences. Continuous-time approaches instead treat time series as samples from an underlying input path, a formulation that naturally accommodates irregularly sampled or oversampled data. Among these, Neural Controlled Differential Equations (NCDEs) are a maximally expressive class of models that parametrise a vector field using a neural network and evolve their hidden state by solving a dynamical system driven by the input path. NCDEs typically use a non-linear vector field, so their expressive power and continuous-time flexibility come at the cost of a forward pass that is both computationally expensive and inherently sequential, limiting their scalability and practical applicability.   This thesis advances the training and scalability of NCDEs through three complementary contributions. First, building on neural rough differential equations, Log-NCDEs apply the Log-ODE method to efficiently approximate an NCDE's solution during training, improving both computational speed and empirical performance. Second, Linear NCDEs replace the non-linear vector field with a linear one, enabling closed-form solutions and parallel-in-time computation without sacrificing theoretical expressivity. Third, Structured Linear NCDEs use structured linear vector fields to further enhance efficiency while maintaining theoretical expressiveness and empirical performance.   Collectively, these methods reduce the time per training step for an NCDE by up to three orders of magnitude while achieving state-of-the-art performance across diverse time series benchmarks.

---

## 论文详细总结（自动生成）

这篇论文是 Benjamin Walker 在牛津大学攻读博士学位期间的论文，系统性地探讨了神经控制微分方程（NCDEs）在处理时间序列数据时的效率与扩展性问题。以下是对该论文的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：传统的神经控制微分方程（NCDE）虽然在处理不规则采样和连续演化数据方面具有极强的理论表达能力，但其前向传播依赖于数值 ODE 求解器，导致计算成本高昂且具有本质上的顺序性（无法在时间维度上并行），难以扩展到长序列或大规模数据集。
*   **研究动机**：在医疗（如心电图）和金融等领域，数据往往以高频或不规则采样路径的形式存在。作者旨在保留 NCDE 处理连续路径的优势，同时通过数学优化和架构创新，使其计算效率达到甚至超过现代离散序列模型（如 Transformer 或 SSM）。

### 2. 论文提出的方法论
论文提出了三项互补的技术改进：
*   **Log-NCDEs**：
    *   **核心思想**：利用 **Log-ODE 方法**（基于粗糙路径理论）在训练期间逼近 NCDE 的解。
    *   **技术细节**：通过计算输入路径在小区间上的**对数签名（Log-signature）**，将原有的非线性 CDE 转化为一系列自治 ODE。利用迭代李括号（Lie brackets）构造向量场，减少了求解器的步数，同时提升了对路径几何特征的捕捉能力。
*   **Linear NCDEs（线性 NCDE）**：
    *   **核心思想**：将向量场限制为关于隐藏状态的线性函数。
    *   **技术细节**：形式为 $dh_t = A_\theta h_t d\omega X_t$。由于其线性结构，该方程在每个区间上具有闭式解（矩阵指数形式）。这使得模型可以利用**并行关联扫描（Parallel Associative Scan）**算法，将计算复杂度从时间上的 $O(n)$ 降低到 $O(\log n)$。
*   **Structured Linear NCDEs (SLiCEs)**：
    *   **核心思想**：为了解决线性 NCDE 中稠密矩阵 $A_\theta$ 参数量过大（$O(d_h^3)$）的问题，引入结构化矩阵。
    *   **关键技术**：探索了**分块对角（Block-diagonal）**、**对角加低秩（DPLR）**、**稀疏矩阵**和**Walsh-Hadamard 变换**等结构。证明了这些结构在保持“最大概率表达能力”的同时，显著降低了内存和计算开销。

### 3. 实验设计
*   **数据集**：
    *   **UEA 多变量时间序列分类库**：选取了 EigenWorms（长序列典型）、Heartbeat 等 6 个代表性数据集。
    *   **PPG-DaLiA**：心率回归任务，涉及极长序列（约 50,000 步）。
    *   **Toy Datasets**：用于验证签名项预测能力和 A5 置换群状态跟踪能力。
*   **Benchmark 与对比方法**：
    *   **离散模型**：LRU、S5、S6（Mamba 的核心）、Mamba、LSTM、Transformer。
    *   **连续模型**：原始 NCDE、神经粗糙微分方程（NRDE）。
    *   **对比维度**：分类准确率、均方误差（MSE）、单步训练时间、GPU 显存占用。

### 4. 资源与算力
*   **硬件设备**：
    *   主要实验在 **NVIDIA RTX 4090** 上运行。
    *   SLiCE 相关的效率测试在 **NVIDIA H100** 上完成。
*   **训练细节**：
    *   使用了 Adam 优化器，配合学习率预热和余弦退火。
    *   针对长序列（如 EigenWorms），对比了顺序执行与并行扫描的耗时差异。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从基础理论验证（Toy tasks）到复杂现实场景（UEA, PPG）的多层次实验。
*   **充分性与客观性**：
    *   每个实验均采用 **5 个随机种子/数据划分**取平均值，并标注标准差。
    *   进行了详尽的超参数网格搜索。
    *   **消融实验**：对比了不同矩阵结构（稠密 vs 结构化）对表达能力的影响，实验设计严谨且公平。

### 6. 主要结论与发现
*   **效率飞跃**：在线性化并结合 Log-ODE 方法后，Linear NCDE 在长序列（EigenWorms）上的训练速度比原始 NCDE 快了 **1500 倍**（从 3 天缩短至 3 分钟）。
*   **表达能力**：证明了线性 NCDE 依然是“最大表达”的，能够逼近任何连续路径泛函。
*   **结构化优势**：SLiCE（尤其是分块对角结构）在状态跟踪任务上优于 Mamba 等对角结构模型，解决了对角 SSM 在处理某些逻辑任务（如奇偶校验）时的局限性。
*   **SOTA 性能**：Log-NCDEs 和 SLiCEs 在多个 UEA 任务上达到了最先进的准确率，同时保持了极高的计算效率。

### 7. 优点
*   **理论与实践结合**：不仅有深厚的数学证明（如 Lip($\gamma$) 正则性证明），还提供了高效的工程实现方案。
*   **解决长序列痛点**：通过并行关联扫描，彻底解决了连续时间模型无法在大规模并行硬件（GPU）上充分发挥性能的问题。
*   **通用性**：提出的 SLiCE 框架可以兼容现有的状态空间模型（SSM），为其提供了更强的数学解释。

### 8. 不足与局限
*   **Log-ODE 深度限制**：实验主要集中在深度 $N=1$ 和 $N=2$，更高阶的对数签名计算极其复杂，且计算成本随阶数指数增长。
*   **算子支持**：稀疏矩阵结构在现有深度学习框架（如 PyTorch/JAX）中缺乏高效的底层内核支持，导致理论上的加速在实际中受限。
*   **应用范围**：虽然在时间序列上表现优异，但在自然语言处理（NLP）等超大规模预训练任务上的表现仍需进一步验证。

（完）
