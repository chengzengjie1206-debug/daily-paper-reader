---
title: "Emputation: Identification-Guided Neural Imputation Framework"
title_zh: Emputation：识别引导的神经网络插补框架
authors: "Yanjiao Yang, Yikun Zhang, Xinwei Shen, Yen-Chi Chen"
date: 2026-07-06
pdf: "https://arxiv.org/pdf/2607.05279v1"
tags: ["query:ts"]
score: 10.0
evidence: 用于学习插补模型的深度生成框架
tldr: 本研究提出了Emputation，一个基于深度生成模型的缺失值填补框架。针对给定观测变量下缺失变量的外推分布，该框架利用特定的缺失假设来指导模型训练，确保目标分布的可识别性。通过引入基于能量评分的“填补风险”目标函数，Emputation能够根据识别假设动态调整掩码策略，实现直接的条件采样。实验证明，该方法在包括非随机缺失（MNAR）在内的多种假设下均能准确恢复目标分布，并在模拟和阿尔茨海默病真实数据集中表现优异。
selection_source: fresh_fetch
motivation: 旨在解决在不同缺失机制（尤其是非随机缺失）下，如何准确识别并恢复缺失变量条件分布的问题。
method: 提出一种基于能量评分的填补风险目标函数，通过识别假设指导观测值的掩码和训练样本选择。
result: 理论证明该方法可恢复多种缺失假设下的目标分布，并在数值模拟和临床数据集上展现出卓越的填补性能。
conclusion: Emputation为复杂缺失机制下的多重填补提供了一个具有理论保证且灵活高效的深度学习解决方案。
---

## 摘要
我们提出了 Emputation，这是一个用于学习插补模型的深度生成框架。Emputation 针对给定观测变量下缺失变量的外推分布，其训练由特定的缺失假设引导，这些假设保证了目标分布的可识别性。训练目标被称为 emputation 风险，是一种基于能量评分（energy-score）的风险，其中识别假设决定了观测条目如何被掩码以及哪些观测值参与训练。由此产生的框架能够为多重插补提供直接的条件采样。我们证明，在包括几种非随机缺失（MNAR）假设在内的广泛识别假设下，emputation 风险的总体极小值点可以恢复目标外推分布。仿真实验表明，该方法在逐点和分布评估指标下均表现出优异的性能，在阿尔茨海默病数据集上的应用也证明了其其实际价值。

## Abstract
We propose Emputation, a deep generative framework for learning imputation models. Emputation targets the extrapolation distribution of missing variables given observed variables, and training is guided by specific missingness assumptions that guarantee identification of the target distribution. The training objective, called the emputation risk, is an energy-score-based risk in which the identification assumption determines how observed entries are masked and which observations contribute to training. The resulting framework enables direct conditional sampling for multiple imputation. We show that the population minimizer of the emputation risk recovers the target extrapolation distribution under a broad class of identification assumptions, including several missing-not-at-random assumptions. Simulations show strong performance under both pointwise and distributional evaluation metrics, and an application to an Alzheimer's disease dataset demonstrates its practical value.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **Emputation** 的深度生成式缺失值插补框架。它将统计学中的“识别假设”（Identification Assumptions）与机器学习中的“深度生成模型”相结合，旨在解决复杂缺失机制下的数据恢复问题。

以下是对该论文的详细总结：

### 1. 核心问题与研究动机
*   **研究背景**：在现实数据集中，缺失值（Missing Data）普遍存在。处理不当会导致分析偏差。
*   **核心冲突**：
    *   **统计学界**：侧重于简单的、可解释的模型（如模式混合模型），虽然有坚实的识别理论，但难以捕捉复杂的高维分布。
    *   **机器学习界**：侧重于深度生成模型（如 GAIN, VAE），虽然拟合能力强，但往往忽略了缺失机制（如非随机缺失 MNAR）的统计识别假设，导致填补结果在理论上缺乏保证。
*   **研究动机**：开发一个既能利用神经网络的强大拟合能力，又能显式嵌入统计识别假设的框架，以准确恢复缺失变量的条件分布。

### 2. 方法论
*   **核心思想**：将插补问题视为一个“分布回归”任务。通过最小化**填补风险（Emputation Risk）**，使模型学习到在给定观测值和缺失模式下的外推分布（Extrapolation Distribution）。
*   **关键技术细节**：
    *   **Engression 基础**：基于能量评分（Energy Score）的生成模型，能够学习完整的条件分布而非仅仅是均值。
    *   **掩码策略（Masking, $M_r$）**：在训练期间，根据预设的缺失假设，战略性地掩盖部分已观测到的数据，作为训练目标。
    *   **选择准则（Selection, $S_r$）**：根据响应模式选择特定的样本子集参与特定模式的训练（例如，在 CCMV 假设下只选择完整案例进行训练）。
    *   **模型结构**：输入为“观测值 + 随机噪声 + 缺失模式向量”，通过多层感知机（MLP）输出填补后的完整向量。
*   **算法流程**：
    1.  确定缺失假设（如 MCAR, CCMV, 或树状图模型）。
    2.  根据假设定义掩码模式 $M_r$ 和选择函数 $S_r$。
    3.  利用能量评分构建经验风险函数。
    4.  通过随机梯度下降训练神经网络。
    5.  训练完成后，通过向模型输入噪声实现直接的条件采样（支持多重插补）。

### 3. 实验设计
*   **数据集**：
    *   **UCI 基准数据集**：Concrete（混凝土）、Wine（葡萄酒）、CCPP（联合循环发电厂）。
    *   **真实应用数据集**：美国国家阿尔茨海默病协调中心（NACC）的临床数据集。
*   **缺失场景**：模拟了完全随机缺失（MCAR）、随机缺失（MAR）和非随机缺失（MNAR，特别是完整案例缺失值 CCMV）。
*   **Benchmark（对比方法）**：
    *   均值插补（Mean Imputation）。
    *   EM 算法（基于多元高斯模型）。
    *   MICE（链式方程多重插补）。
    *   missForest（基于随机森林的非参数插补）。
    *   GAIN（基于生成对抗网络的插补）。

### 4. 资源与算力
*   **模型配置**：使用了 3 层隐藏层、每层 500 个单元的反馈神经网络，采用 ReLU 激活函数。
*   **训练细节**：训练 500 到 1000 个 epoch，学习率为 $10^{-4}$，Monte Carlo 采样数 $B=2$。
*   **算力说明**：论文**未明确说明**具体的 GPU 型号或训练总时长。但从模型规模（中等规模 MLP）和数据集大小（数千至一万样本）来看，该方法在常规工作站级 GPU 上即可高效运行。

### 5. 实验数量与充分性
*   **实验规模**：
    *   对 3 个基准数据集进行了 100 次独立重复实验。
    *   测试了 20% 和 40% 两种缺失率。
    *   进行了敏感性分析，探讨了完整案例比例对模型性能的影响。
*   **充分性评价**：实验设计较为充分。不仅评估了逐点准确性（RMSE, MAE），还重点评估了分布一致性（MADC 相关性差异、能量距离、MMD^2），这在插补研究中是非常客观且必要的。

### 6. 主要结论与发现
*   **理论一致性**：理论证明了在多种识别假设下，Emputation 的风险极小值点能够准确恢复目标外推分布。
*   **性能优越性**：在分布评估指标（能量距离和 MMD）上，Emputation 显著优于 MICE、missForest 和 GAIN，尤其是在 MNAR 场景下。
*   **鲁棒性**：即使在缺失假设轻微误设的情况下，该方法依然表现出较强的竞争力。
*   **实际价值**：在阿尔茨海默病数据分析中，Emputation 能够纠正完整案例分析（Complete-Case Analysis）带来的选择偏差，提供更可靠的临床风险因素估计。

### 7. 优点
*   **理论与实践结合**：首次将模式混合模型的识别理论系统性地整合进深度生成框架。
*   **分布学习**：利用能量评分避免了传统方法容易导致的“均值回归”问题，能够生成具有统计特性的多样化填补值。
*   **灵活性**：一个框架即可处理单调缺失、非单调缺失以及多种 MNAR 假设。

### 8. 不足与局限
*   **假设依赖**：模型性能高度依赖于用户选择的缺失假设（如 CCMV 或 MCAR），而这些假设在实际中往往是不可检验的（Non-testable）。
*   **计算开销**：相比于 MICE 等传统方法，深度学习模型的训练成本更高，且需要调节超参数。
*   **MAR 扩展性**：虽然涵盖了单调缺失下的 MAR，但对于一般的非单调 MAR 机制，其识别与训练目标的关联仍有待进一步探索。

（完）
