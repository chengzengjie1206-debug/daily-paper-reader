---
title: Simultaneous Inference for Partially Observed Functional Time Series
title_zh: 部分观测函数型时间序列的同步推断
authors: "Patrick Bastian, Tim Kutta"
date: 2026-06-30
pdf: "https://arxiv.org/pdf/2606.31269v1"
tags: ["query:ts"]
score: 9.0
evidence: 针对存在缺失数据的部分观测函数型时间序列的推理
tldr: 针对传感器中断导致的函数型时间序列部分缺失问题，本文提出了首个针对相关性部分观测数据的统计推断方法。研究采用有界函数空间上的上确界范数，实现了全域同时推断（如同时置信带），并扩展了多尺度推断方法以检测非平稳趋势。该方法结合高斯逼近与随机过程理论，在处理空气污染等实际数据时表现优于传统独立性假设方法。
selection_source: fresh_fetch
motivation: 现有的函数型数据分析方法大多假设数据相互独立，无法处理具有时间相关性且存在观测缺失的复杂传感器数据。
method: 在配备上确界范数的有界函数空间上建模，结合高斯逼近理论和多尺度推断，构建了适用于部分观测函数型时间序列的推断框架。
result: 实现了跨整个函数域的同时置信带推断，并能有效检测时间序列中的非平稳趋势，且在完全观测情况下也优于传统方法。
conclusion: 该研究为依赖性部分观测函数型数据提供了严谨的统计推断工具，特别适用于城市污染水平监测等实际应用场景。
---

## 摘要
函数型数据分析 (FDA) 为分析连续时间随机过程样本提供了统计方法。测量值通常以关键科学变量的传感器数据形式出现。传感器不规则中断的实际问题激发了人们对分析部分观测随机函数的兴趣。具体而言，本文受一组间歇性缺失的污染数据时间序列启发，这些数据在污染路径和缺失模式上具有依赖性。为了进行统计分析，我们开发了首个针对依赖性、部分观测函数型时间序列的推断方法。现有方法不适用于此任务，因为它们严重依赖于数据函数的独立性。在数学上，我们在配备上确界范数的有界函数空间上对数据进行建模。这允许在整个函数域上进行同步推断，包括同步置信带——这是现有的基于希尔伯特空间的方法无法提供的。为了研究时间序列中的非平稳趋势，我们将最先进的多尺度推断方法（最初为标量数据开发）扩展到部分观测函数。后一种方法的主要应用是检测市中心污染水平是否超标。我们的方法将最先进的高斯近似与随机过程理论相结合。有趣的是，通过避免使用函数型中心极限定理 (CLT)，它还改进了完全观测函数型时间序列的现有结果。

## Abstract
Functional data analysis (FDA) provides statistical methods for analyzing samples of time-continuous stochastic processes. Measurements often arise in the form of sensor data for a key scientific variable. The practical problem of irregular sensor disruptions has fostered interest in analyzing partially observed random functions. Specifically, this paper is motivated by a time series of intermittently missing pollution data with dependence along pollution paths and missingness patterns. To allow statistical analysis, we develop the first inference methods for dependent, partially observed functional time series. Existing methods were not appropriate for this task, because they heavily rely on the independence of the data functions. Mathematically, we model data on the space of bounded functions equipped with the supremum norm. This allows simultaneous inference across the entire functional domain, including simultaneous confidence bands -- something existing Hilbert-space-based methods cannot provide. To study non-stationary trends along the time series, we extend state-of-the-art multiscale inference methods (originally developed for scalar data) to partially observed functions. The key application of the latter methods is testing for excessive pollution levels in inner cities. Our approach combines state-of-the-art Gaussian approximations with stochastic process theory. Interestingly, it also improves existing results for fully observed functional time series by avoiding a functional CLT.

---

## 论文详细总结（自动生成）

这是一份关于论文《Simultaneous Inference for Partially Observed Functional Time Series》（部分观测函数型时间序列的同步推断）的深度结构化总结：

### 1. 论文的核心问题与整体含义
*   **研究动机**：在函数型数据分析（FDA）中，理想情况是观察到完整的连续曲线。然而，在实际应用（如空气质量监测、医疗追踪）中，由于传感器故障或人为错误，数据往往是部分缺失的。
*   **核心挑战**：现有的处理部分观测函数的方法大多假设样本之间是相互独立的。但在时间序列背景下，观测值本身以及“缺失模式”通常都存在时间上的依赖性（例如，传感器故障可能持续数天）。
*   **研究目标**：本文旨在为具有序列相关性的部分观测函数型时间序列开发一套严谨的统计推断框架，重点解决同步置信带（SCB）的构建和非平稳趋势的检测问题。

### 2. 论文提出的方法论
*   **建模空间**：不同于传统基于希尔伯特空间（$L^2$）的方法，本文在配备上确界范数的有界函数空间（$l^\infty$）上建模。这种选择允许进行点对点的评估，从而能够构建全域同步的推断工具。
*   **同步置信带（SCB）**：
    *   核心思想是先在离散网格点上构建大量的同步置信区间。
    *   利用高维高斯逼近理论（避开了复杂的函数型中心极限定理）来处理网格点上的最大偏差。
    *   通过线性插值将网格点上的区间连接成连续的置信带，并证明了在均值函数足够平滑的情况下，这种插值不会导致覆盖率失效。
*   **多尺度推断（Multiscale Inference）**：
    *   针对非平稳时间序列，引入了多尺度统计量。
    *   该方法通过在不同时间跨度和不同尺度上滑动窗口，自动适应不同长度和强度的信号。
    *   关键应用包括平稳性检验、变点定位以及检测函数是否超过预设的科学阈值（如污染超标）。

### 3. 实验设计
*   **模拟实验场景**：
    *   **数据生成**：涵盖了独立同分布（i.i.d.）场景和函数自回归（FAR）模型，并区分了轻尾（正态）和重尾（t分布）噪声。
    *   **缺失机制**：设计了随机区间缺失和基于双指数等待时间模型的依赖性缺失。
*   **Benchmark（基准对比）**：
    *   在平稳性检验方面，对比了 Hudecova 和 Kirch (2025) 的方法（简称 HK 方法）。
*   **实际应用数据集**：
    *   使用了来自印度孟买 Bandra Kurla Complex 的 PM10 空气污染数据（OpenAQ 数据库），分析时间跨度约 6 个月，包含严重的传感器中断和缺失。

### 4. 资源与算力
*   **算力说明**：论文中未明确提及具体的 GPU 型号、数量或训练时长。由于该研究属于统计学理论与方法论范畴，主要涉及数值模拟和渐近性质的验证，通常在标准 CPU 集群或工作站上通过 R 或 Python 即可完成，不依赖大规模深度学习算力。

### 5. 实验数量与充分性
*   **实验规模**：
    *   模拟实验部分针对每种配置均进行了 1000 次重复运行。
    *   测试了不同的样本量（N=20, 50, 80, 100, 200）和不同的网格密度。
*   **充分性评价**：实验设计较为充分且客观。作者不仅验证了所提方法在依赖性数据下的有效性，还展示了在完全观测（无缺失）的情况下，该方法依然优于传统基于函数型 CLT 的方法。通过对比 HK 方法，证明了其在检测局部化变化（而非全局均匀变化）方面的优越性。

### 6. 论文的主要结论与发现
*   **推断有效性**：证明了即使在数据缺失和序列相关共存的情况下，所构建的同步置信带仍能保持渐近覆盖水平。
*   **多尺度优势**：多尺度统计量能够有效识别非平稳趋势。在污染数据分析中，该方法成功检测到了孟买 PM10 水平在特定季节和时段的显著超标现象。
*   **理论贡献**：提出了一种不需要函数型中心极限定理的推断路径，这为处理更复杂的非平稳函数型数据提供了新的数学工具。

### 7. 优点
*   **现实针对性强**：直接解决了传感器数据中常见的“缺失”与“相关”并存的问题。
*   **数学框架优雅**：采用 $l^\infty$ 空间和高维逼近理论，避开了 $L^2$ 空间无法定义点评估的局限。
*   **灵活性高**：多尺度方法不需要预先知道信号的结构（如变点个数或形状），具有自适应性。

### 8. 不足与局限
*   **平滑性依赖**：置信带的插值效果依赖于均值函数的 Hölder 连续性假设，对于极度不连续的函数可能表现不佳。
*   **小样本保守性**：在样本量非常小（如 N=20）且缺失率极高时，高斯逼近可能导致推断结果偏向保守。
*   **计算复杂度**：随着网格点数量 $p$ 的增加，处理高维协方差矩阵的计算开销会增大，尽管论文证明了 $p$ 可以随 $N$ 多项式增长。

（完）
