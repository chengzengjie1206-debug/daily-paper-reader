---
title: Multi-Scale Convolution with Optimal Transport Attention Effect on Multivariate Time Series
title_zh: 多尺度卷积与最优传输注意力对多元时间序列的影响
authors: "HaoChong Fu, Jian Xu"
date: 2026-07-12
pdf: "https://arxiv.org/pdf/2607.10740v1"
tags: ["query:aq-forecast"]
score: 8.0
evidence: 多尺度卷积与最优传输注意力用于多变量时间序列
tldr: 多变量时间序列分析面临多粒度模式捕获与噪声抑制的挑战。本文提出MSC-OT架构，通过多尺度卷积增强注意力矩阵并引入Sinkhorn最优传输正则化，基于倒置嵌入有效捕获跨变量关系。在ETT、Electricity等数据集上，短期与长期预测均取得优异性能。消融实验验证了各组件的协同增效作用。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.10740v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 744, \"height\": 766, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.10740v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 925, \"height\": 387, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.10740v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 927, \"height\": 387, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.10740v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 929, \"height\": 320, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.10740v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 911, \"height\": 890, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.10740v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 928, \"height\": 407, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.10740v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1859, \"height\": 228, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.10740v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 874, \"height\": 803, \"label\": \"Table\"}]"
motivation: 现有方法难以同时捕获多变量时间序列的多尺度结构模式并有效抑制噪声干扰。
method: 提出MSC-OT，包含多尺度卷积增强、Sinkhorn最优传输正则化及自适应融合策略。
result: 在ETT、Electricity等五个数据集上，MSC-OT在短期和长期预测任务中均达到领先性能。
conclusion: 多尺度卷积与最优传输正则化能有效提升多变量时间序列预测的准确性和鲁棒性。
---

## 摘要
多元时间序列分析在许多实际应用中扮演着重要角色，但在捕获多粒度结构模式和适当抑制噪声方面仍存在挑战。本文提出了多尺度卷积与最优传输注意力（MSC-OT）。MSC-OT是一种优化注意力机制的有效架构，它结合了多尺度卷积和基于倒置嵌入的Sinkhorn最优传输方法。倒置嵌入方法将每个变量嵌入为一个标记，使模型能够更好地捕获跨变量关系。MSC-OT由两部分组成：（1）多尺度卷积增强，它基于倒置嵌入对注意力分数矩阵应用多尺度卷积，捕获由压缩时间表示引起的变量交互空间中的局部结构模式；（2）Sinkhorn最优传输正则化，将注意力计算公式化为最优传输问题，并采用迭代矩阵缩放以确保变量间信息流的平衡。自适应融合策略利用softmax归一化的可学习权重动态组合基础注意力、卷积增强和最优传输正则化分数。在包括ETT、Electricity、Traffic、Solar-Energy和Exchange-Rate在内的广泛使用的数据集上的实验表明，MSC-OT在短期和长期预测任务中均取得了良好性能。消融实验进一步验证了每个提出组件的有效性及其对提高多元时间序列预测准确性的协同贡献。

## Abstract
The analysis of Multivariate Time Series (MTS) plays an important role in a lot of real-world practical applications, but it still remains some challenging problem about capturing multi-granularity structural patterns and suppressing noise appropriately. Multi-Scale Convolution with Optimal Transport Attention (MSC-OT) is proposed in this paper. MSC-OT is a useful architecture to optimize the attention mechanism. It combines multi-scale convolution with Sinkhorn optimal transport method based on inverted embedding. The inverted embedding approach embeds each variable as a token and allows the model to capture cross-variate relationships better. MSC-OT consists of two part: (1) Multi-Scale Convolution Enhancement, that applies multi-scale convolutions to attention score matrices based on inverted embedding, capturing local structural patterns in the variate-interaction space induced by compressed temporal representations; (2) Sinkhorn Optimal Transport Regularization, that formulates attention computation as an optimal transport problem and employs iterative matrix scaling to ensure balanced information flow across variates. Adaptive Fusion Strategy utilizes softmax-normalized learnable weights to dynamically combine base attention, convolution-enhanced, and OT-regularized scores. Experiments on widely-used datasets, including ETT, Electricity, Traffic, Solar-Energy, and Exchange-Rate, show that MSC-OT achieves well performance in both short-term and long-term forecasting tasks. Ablation experiments further validate the effectiveness of each proposed component and their synergistic contributions to improving prediction accuracy for multivariate time series forecasting.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）
- **核心问题**：多元时间序列（MTS）预测面临两大挑战：一是难以同时捕获多粒度（multi-granularity）的结构模式（如短期波动、周期性、层次化依赖）；二是实际数据中的异常噪声容易干扰注意力分布，导致预测偏差。传统Transformer模型通常将同一时间步的多个变量嵌入为一个token，侧重时间依赖而忽略跨变量关系。近期倒置嵌入（Inverted Embedding）范式（如iTransformer）将每个变量的整个时间序列视为一个token，虽能显式建模跨变量依赖，但会丢失细粒度的时间局部模式。
- **研究动机**：在保留倒置嵌入优势（建模跨变量关系）的同时，补偿其时间局部信息损失，并增强对噪声的鲁棒性。
- **整体含义**：提出一种新型注意力机制MSC-OT，通过多尺度卷积增强注意力矩阵以捕捉局部结构，并通过Sinkhorn最优传输正则化实现平衡分布与噪声抑制，最终通过自适应融合策略动态组合多个注意力分支。

### 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程（文字说明）
- **核心思想**：MSC-OT在倒置嵌入的iTransformer框架基础上，在注意力计算中引入两个互补模块：
  - **多尺度卷积增强（Multi-Scale Convolution Enhancement）**：对原始注意力分数矩阵 \(S = QK^T / \sqrt{d_k}\) 应用并行的2D卷积（核大小3×3和5×5），捕获不同粒度的局部结构模式。多尺度特征通过简单平均聚合得到 \(S_{\text{conv}}\)。
  - **Sinkhorn最优传输正则化（Sinkhorn Optimal Transport Regularization）**：将注意力计算建模为熵正则化的最优传输问题。成本矩阵 \(C = -S\)，求解传输计划 \(P^* = \arg\min_{P \in U(r,c)} \langle C, P \rangle - \lambda H(P)\)，其中 \(H(P)\) 为熵，\(\lambda = 5.0\)。通过Sinkhorn算法迭代进行行和列归一化得到双随机矩阵 \(P\)，此过程天然抑制异常值、平滑注意力分布。
  - **自适应分数融合（Adaptive Score Fusion）**：使用可学习的权重 \(\alpha, \beta, \gamma\)（经softmax归一化）组合基础注意力 \(S\)、卷积增强分数 \(S_{\text{conv}}\) 和OT正则化分数 \(P\)，初始化为 \([0.7, 0.2, 0.1]\)，模型可自动适应不同数据集的特征。
- **关键技术细节**：
  - 使用tanh激活将注意力分数裁剪至 \([-8, 8]\) 防止溢出；在Sinkhorn计算前对分数进行裁剪至 \([-50, 50]\) 并减去最大值以稳定指数运算。
  - 卷积权重以0.1的常数值初始化，防止早期训练破坏原始注意力模式。
  - Sinkhorn迭代次数设为2次（实验表明足够收敛）。
  - 模型架构采用2层编码器、1层解码器，模型维度512，8个注意力头，前馈维度2048，dropout 0.1。

### 3. 实验设计：使用了哪些数据集/场景，它的benchmark是什么，对比了哪些方法
- **数据集**：五个广泛使用的基准数据集：
  - ECL（Electricity）、ETT（包含4个子集：ETTh1、ETTh2、ETTm1、ETTm2）、Exchange-Rate、Traffic、Weather。
- **场景**：长期预测（预测长度S ∈ {96, 192, 336, 720}），输入序列长度固定为96。
- **对比方法**：包括iTransformer、RLinear、PatchTST、Crossformer、TimesNet、SCINet、FEDformer、Autoformer等近年代表性模型。
- **评价指标**：均方误差（MSE）和平均绝对误差（MAE），在多次随机种子下取平均。

### 4. 资源与算力：如果文中有提到，请总结使用了多少算力（GPU型号、数量、训练时长等）。若未明确说明，也请指出这一点。
- **文中未明确提及**具体的GPU型号、数量或训练时长。仅给出了训练超参数：优化器Adam，学习率1e-4，批次大小32，早停耐心值3个epoch。未提供硬件配置信息。

### 5. 实验数量与充分性：大概做了多少组实验（如不同数据集、消融实验等），这些实验是否充分、是否客观、公平。
- **实验数量**：
  - 主实验：在5个数据集（ETT视为4个子集算平均值）上，每个数据集4种预测长度，共约5×4=20组对比结果（表1）。
  - 消融实验：在ECL数据集上分析了各组件（多尺度卷积、OT正则化及其组合）的贡献（图5）；在Traffic数据集上分析了Sinkhorn迭代次数的影响（图6）。
  - 权重分析：给出了不同预测长度下自适应融合权重的具体数值（表2），并可视化了各数据集上的平均权重分布（图4）。
  - 超参数敏感性分析：评估了OT正则化强度λ（1.0~10.0）和Sinkhorn迭代次数（1~5）的影响。
- **充分性与公平性**：
  - 覆盖了多个领域、不同维度的数据集，比较了当前主流方法，实验设计较为全面。
  - 消融实验验证了各组件的单独和协同作用，结果客观。
  - 但未提供统计显著性检验（如置信区间或t检验），也未明确说明数据划分方式（如是否采用滑动窗口或固定划分），可能影响结果的可复现性。

### 6. 论文的主要结论与发现
- MSC-OT在ECL、Traffic、Weather数据集上取得了最佳MSE，在Exchange上为次优；在所有数据集上（除ETT外）取得了最佳MAE。
- 自适应融合权重显示，多尺度卷积（β）和OT正则化（γ）在某些数据集（如ECL、Exchange）中合计占比超过50%，表明这两个组件对注意力优化有显著贡献。
- 消融实验表明，多尺度卷积和OT正则化具有协同效应：卷积捕获局部模式，OT保证全局信息平衡，两者结合优于单独使用。
- Sinkhorn迭代2次即可达到近最优性能，继续增加收益甚微但计算成本线性增长。
- 自适应融合策略优于固定等权重分配，证明端到端学习的必要性。

### 7. 优点：方法或实验设计上有哪些亮点
- **方法创新性**：
  - 创造性地将多尺度卷积引入注意力分数矩阵，弥补倒置嵌入丢失的局部时间结构信息。
  - 将Sinkhorn最优传输作为正则化项而非完全替代softmax，保留了原始注意力的判别能力，同时利用双随机约束和熵正则化自然抑制噪声。
  - 自适应融合策略使模型能针对不同数据特性动态调整各分支贡献，增强了泛化能力。
- **实验设计亮点**：
  - 不仅报告了平均性能，还详细展示了各预测长度下的权重变化，揭示了组件的实际影响。
  - 对关键超参数（λ、迭代次数）进行了系统敏感性分析，提供了实用选择依据。

### 8. 不足与局限
- **实验覆盖局限**：
  - 仅使用了5个数据集，未包含更多样化的MTS应用（如金融、医疗、传感器数据），可能影响结论的普适性。
  - 未提供计算资源消耗对比（参数量、FLOPs、推理时间），难以评估实际部署成本。
- **方法局限性**：
  - MSC-OT专为倒置嵌入设计，论文指出当应用于传统时间步嵌入时优势会减弱，适用范围有限。
  - Sinkhorn迭代即使仅2步仍带来额外计算开销，未与iTransformer的原始复杂度进行定量比较。
  - 自适应融合权重初始值依赖经验，可能对某些数据集不如自动搜索最优。
- **偏差风险**：
  - 消融实验仅基于ECL和Traffic数据集，未在其他数据集上重复验证组件效果，可能存在数据偏差。
  - 未报告多次运行的标准差或置信区间，无法判断结果是否统计显著。

（完）
