---
title: "SPLICE: Latent Diffusion over JEPA Embeddings for Conformal Time-Series Inpainting"
title_zh: SPLICE：基于 JEPA 嵌入的潜在扩散模型，用于符合预测的时间序列补全
authors: Arnaud Zinflou
date: 2026-04-30
pdf: "https://arxiv.org/pdf/2605.00126v1"
tags: ["query:ts"]
score: 9.0
evidence: 用于时间序列修复和插补的潜扩散模型
tldr: 针对电力系统时间序列补全缺乏可靠性保证的问题，本文提出SPLICE框架。该框架结合了JEPA编码器、潜在扩散模型（或流匹配）和自适应共形推理（ACI）。通过在64维潜在空间进行生成式补全，并利用ACI提供具有统计覆盖保证的预测区间，SPLICE在多个数据集上实现了最低的均方误差和CRPS，同时在推理速度和跨领域迁移能力上表现优异。
selection_source: fresh_fetch
motivation: 现有的生成式时间序列补全模型缺乏有限样本的可靠性保证，难以满足电力系统调度和规划对数据准确性的严格要求。
method: 提出一种模块化框架，利用JEPA编码器映射潜在空间，通过条件潜在桥接生成候选轨迹，并结合自适应共形推理（ACI）生成具有覆盖保证的预测区间。
result: "在13个负荷数据集上，SPLICE在MSE和CRPS指标上均优于现有基准模型，且流匹配变体实现了5-10倍的加速，同时ACI确保了93-95%的经验覆盖率。"
conclusion: SPLICE通过结合潜在扩散与共形推理，为时间序列补全提供了一种高效、准确且具备统计可靠性保证的解决方案，并展现了强大的跨领域迁移潜力。
---

## 摘要
用于时间序列插补的生成模型虽然具有很强的重建精度，但无法提供有限样本的可靠性保证，这在插补值用于调度和规划的电力系统中是一个关键限制。我们提出了 SPLICE（基于符合包络的自监督预测潜在补全），这是一个将潜在生成插补与无分布、在线自适应预测区间相结合的模块化框架。JEPA 编码器将每日负荷段映射到 64 维潜在空间；具有四种采样模式的条件潜在桥接生成候选缺失轨迹；按小时调节的解码器映射回信号空间；自适应符合推理 (ACI) 为输出提供具有覆盖率保证的预测带。流匹配变体在 5-10 个 ODE 步内实现了与 DDIM 相当的质量（加速 5-10 倍）。在 13 个负荷数据集（9 个私有数据集、3 个 UCI 电力数据集、ETTh1）上，SPLICE 实现了最低的平均负荷 MSE (0.056)，在 91 天缺失间隔的 12 个非退化数据集中胜出 9 个，在所有缺失长度的 32 个实验中胜出 18 个（对比五个成熟基准），并获得了最佳的 CRPS (0.161，较最强竞争对手降低 18.3%)。ACI 提供了 93-95% 的经验覆盖率，纠正了静态符合预测中观察到的高达 7.5 个百分点的覆盖不足问题。在九个数据源上训练的池化 JEPA 编码器可迁移到四个未见领域，仅需快速的桥接微调即可达到或超过针对每个数据集训练的专家模型。

## Abstract
Generative models for time-series imputation achieve strong reconstruction accuracy, yet provide no finite-sample reliability guarantees, a critical limitation in power systems where imputed values inform dispatch and planning. We introduce SPLICE (Self-supervised Predictive Latent Inpainting with Conformal Envelopes), a modular framework coupling latent generative imputation with distribution-free, online-adaptive prediction intervals. A JEPA encoder maps daily load segments into a 64-dimensional latent space; a conditional latent bridge with four sampling modes generates candidate gap trajectories; an hourly-conditioned decoder maps back to signal space; and Adaptive Conformal Inference (ACI) wraps the output with coverage-guaranteed prediction bands. The flow-matching variant achieves comparable quality to DDIM in 5--10 ODE steps (5-10x speedup). On thirteen load datasets (nine proprietary, three UCI Electricity, ETTh1), SPLICE achieves the lowest mean Load-only MSE (0.056), winning 9/12 non-degenerate datasets at 91-day gaps and 18/32 across all gap lengths vs. five established baselines, and produces the best CRPS (0.161, -18.3% vs. the strongest competitor). ACI delivers 93--95% empirical coverage, correcting under-coverage failures of up to 7.5 pp observed with static conformal prediction. A pooled JEPA encoder trained on nine feeds transfers to four unseen domains, matching or exceeding per-dataset oracles with only a quick bridge fine-tuning.

---

## 论文详细总结（自动生成）

这是一份关于论文《SPLICE: Latent Diffusion over JEPA Embeddings for Conformal Time-Series Inpainting》的结构化深入分析报告：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：在电力系统等安全敏感领域，时间序列的缺失数据补全（Inpainting）不仅要求高精度的点预测，更需要具备**可靠的置信区间（可靠性保证）**。
*   **研究背景**：现有的深度生成模型（如扩散模型）虽然能生成逼真的重建结果，但往往缺乏有限样本下的统计可靠性保证。在电力调度中，如果补全值过于自信而偏离实际，可能导致严重的决策失误。
*   **整体含义**：论文提出了 **SPLICE** 框架，旨在通过结合自监督表征学习（JEPA）、潜在空间生成模型（扩散/流匹配）和自适应符合推理（ACI），构建一个既能捕捉复杂时空特征，又能提供严格覆盖率保证的补全系统。

### 2. 论文提出的方法论
SPLICE 是一个四阶段的模块化框架，核心思想是在学习到的潜在空间（Latent Space）中进行轨迹“想象”：
*   **JEPA 编码器（表征阶段）**：采用联合嵌入预测架构（JEPA），将每日 24 小时的负荷段映射到 64 维的紧凑潜在空间。其损失函数结合了余弦预测损失、方差正则化（防止崩溃）和协方差正则化（鼓励信息分散）。
*   **条件潜在桥接（生成阶段）**：在潜在空间中预测缺失段的轨迹。支持四种模式：确定性 Transformer、噪声扰动、DDIM 扩散模型以及**流匹配（Flow Matching）**。流匹配通过学习直线 ODE 路径，仅需 5-10 步即可完成高质量采样。
*   **小时调节解码器（重建阶段）**：将潜在轨迹映射回原始信号空间。该解码器引入了外部协变量（天气、日历特征）进行逐小时调节，并使用负荷加权 MSE 损失来强化目标变量的准确性。
*   **自适应符合推理 ACI（校准阶段）**：这是提供可靠性保证的关键。ACI 在线调整误覆盖水平 $\alpha_t$，根据历史覆盖表现动态放宽或收紧预测区间，从而在非平稳分布（如季节性漂移）下维持长期 95% 的覆盖率。

### 3. 实验设计
*   **数据集**：共 13 个数据集，包括 9 个来自北美公用事业公司的私有负荷数据集、3 个 UCI 电力数据集（MT_001/150/320）和 ETTh1 数据集。涵盖了居民、商业、工业等多种用电模式。
*   **场景设置**：模拟长达 91 天（2,184 小时）的连续数据缺失，同时也测试了 7 天和 30 天的短中期缺失。
*   **Benchmark 与对比方法**：
    *   **传统方法**：Seasonal Naïve（季节性朴素法）。
    *   **深度学习/生成方法**：BRITS（双向 RNN）、SAITS（自注意力机制）、CSDI（基于观测空间的扩散模型）、TimesNet。
    *   **内部消融**：VAE+Bridge（对比 JEPA 的优越性）、静态 CQR（对比 ACI 的自适应性）。

### 4. 资源与算力
*   **硬件环境**：所有模型均在单张 **NVIDIA RTX A4000 (16 GB)** GPU 上训练。
*   **训练时长**：每个数据集的完整流水线训练约需 **8-12 分钟**。
*   **推理效率**：对于 91 天的缺失窗口，推理耗时仅为 **6.5-9.0 毫秒**。流匹配变体相比 DDIM 实现了 5-10 倍的加速。

### 5. 实验数量与充分性
*   **实验规模**：论文在 13 个数据集上进行了详尽测试，每个实验运行 3 个随机种子并报告均值与标准差。
*   **充分性**：
    *   **多维度评估**：不仅对比了 MSE（点准确性），还对比了 CRPS（概率分布质量）、经验覆盖率（可靠性）和下游预测任务的性能。
    *   **消融实验**：针对 JEPA 嵌入、解码器增强、生成后端（DDIM vs. 流匹配）等进行了细致的消融分析。
    *   **迁移学习**：专门设计了跨领域迁移实验，验证了“池化编码器”在未见数据集上的泛化能力。
*   **客观性**：使用了公开的 PyPOTS 库运行基准模型，确保了对比的公平性。

### 6. 主要结论与发现
*   **精度领先**：SPLICE 在 13 个数据集上的平均负荷 MSE 为 0.056，在 32 组实验对比中胜出 18 组（56%），显著优于 SAITS 和 CSDI。
*   **可靠性保证**：ACI 成功纠正了静态符合预测在非平稳数据下的覆盖不足问题（提升高达 7.5 个百分点），实现了 93-95% 的稳定覆盖。
*   **分布质量**：通过潜在空间噪声扰动生成的系综（Ensemble）获得了最低的 CRPS（0.161），比最强竞争对手提升了 18.3%。
*   **迁移潜力**：预训练的池化 JEPA 编码器在仅微调“桥接层”的情况下，即可在未见领域达到甚至超过针对该领域专门训练的模型效果。

### 7. 优点（亮点）
*   **模块化解耦**：编码器、桥接器、解码器和校准器相互独立，可以根据需求灵活替换（如将 DDIM 换成流匹配）。
*   **潜在空间生成的优势**：在 64 维潜在空间而非高维观测空间进行扩散，极大地降低了计算开销并提高了轨迹的物理一致性。
*   **理论与实践结合**：将 LeCun 的 JEPA 世界模型理念与符合预测（Conformal Prediction）结合，解决了生成模型“不敢用”的痛点。

### 8. 不足与局限
*   **领域局限性**：实验主要集中在电力负荷数据，尚未在天然气、水务或可再生能源（如风电、光伏）等其他时间序列领域验证。
*   **极端长度测试不足**：虽然测试了 91 天，但对于极短（如几小时）或极长（如一年以上）的缺失表现尚不明确。
*   **特定模式丢失**：在处理具有双峰分布或大量零值（如路灯负荷）的数据时，64 维的潜在嵌入可能会丢失一些细微的开关特征，导致表现略逊于直接在观测空间操作的方法（如 SAITS）。
*   **超参数敏感性**：流匹配和扩散模型的分类器自由引导（CFG）比例需要针对不同数据集进行微调。

（完）
