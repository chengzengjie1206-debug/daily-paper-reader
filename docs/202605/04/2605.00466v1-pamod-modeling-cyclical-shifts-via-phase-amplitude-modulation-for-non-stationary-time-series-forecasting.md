---
title: "PAMod: Modeling Cyclical Shifts via Phase-Amplitude Modulation for Non-stationary Time Series Forecasting"
title_zh: PAMod：通过相位-振幅调制建模非平稳时间序列预测中的周期性偏移
authors: "Yingbo Zhou, Yutong Ye, Shuhao Li, Rui Qian, Qiang Huang, Lemao Liu, Li Sun, Dejing Dou"
date: 2026-05-01
pdf: "https://arxiv.org/pdf/2605.00466v1"
tags: ["query:ts"]
score: 8.0
evidence: 通过相幅调制进行非平稳时间序列预测
tldr: 针对非平稳时间序列预测中分布随周期变化的挑战，本文提出PAMod框架。传统方法如RevIN假设分布恒定，而PAMod通过相位-振幅调制技术，在归一化空间中利用周期性嵌入来建模均值和方差的循环偏移。数学证明显示该调制等同于动态反归一化。实验表明，PAMod在12个基准测试中取得SOTA性能，且具有轻量级和即插即用的特性，能显著提升现有模型的预测能力。
selection_source: fresh_fetch
motivation: 现有预测方法往往假设历史与未来分布一致，无法有效处理现实应用中随周期（如季节、节假日）波动的非平稳分布偏移。
method: 提出一种相位-振幅调制框架，通过相位调制捕捉均值偏移，通过振幅调制适应方差变化，实现归一化空间内的动态分布建模。
result: 在12个真实世界基准数据集上，PAMod以更少的计算资源实现了最先进的预测性能。
conclusion: PAMod在数学上统一了分布自适应与表示学习，作为一种新型即插即用技术，能有效增强现有时间序列预测模型的性能。
---

## 摘要
现实世界的时间序列预测面临着非平稳统计特性的根本挑战，包括均值和方差随时间的偏移。虽然可逆实例归一化（RevIN）通过对输入进行平稳化和对输出进行去归一化展现了潜力，但它依赖于历史分布与未来分布保持一致的强假设。我们观察到，在许多实际应用中，分布偏移遵循与周期性位置相关的循环模式（例如季节性和节假日的波动）。为此，我们提出了 PAMod，这是一个轻量级且强大的框架，通过在归一化特征空间中进行相位-振幅调制来建模周期性分布偏移。PAMod 学习周期性嵌入来调制表示：相位调制捕捉均值偏移，而振幅调制则适应方差变化。至关重要的是，我们在数学上证明了在归一化空间中进行调制等同于应用动态去归一化，从而实现了分布自适应与表示学习的优雅统一。在 12 个真实世界基准测试上的广泛实验表明，PAMod 以更少的计算资源实现了最先进的性能。此外，我们的调制机制作为一种新颖的即插即用技术，可以通过简单的集成来改进现有的时间序列预测方法。

## Abstract
Real-world time series forecasting faces the fundamental challenge of non-stationary statistical properties, including shifts in mean and variance over time. While reversible instance normalization (RevIN) has shown promise by stationarizing inputs and denormalizing outputs, it relies on the strong assumption that historical and future distributions remain identical. We observe that in many practical applications, distribution shifts follow cyclical patterns that correlate with periodic positions (e.g., seasonal and holiday volatility). To this end, we propose PAMod, a lightweight yet powerful framework that models cyclical distribution shifts via Phase-Amplitude Modulation in the normalized feature space. PAMod learns periodic embeddings to modulate representations: phase modulation captures mean shifts, while amplitude modulation adapts to variance changes. Crucially, we prove mathematically that modulating in normalized space is equivalent to applying dynamic denormalization, offering an elegant unification of distribution adaptation and representation learning. Extensive experiments on twelve real-world benchmarks demonstrate that PAMod achieves state-of-the-art performance with fewer computational resources. Furthermore, our modulation mechanism, as a novel plug-and-play technique, can improve existing time-series forecasting methods with simple integration.

---

## 论文详细总结（自动生成）

以下是对论文《PAMod: Modeling Cyclical Shifts via Phase-Amplitude Modulation for Non-stationary Time Series Forecasting》的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：现实世界的时间序列具有高度的**非平稳性**（均值和方差随时间变化），导致历史数据与未来数据的分布不一致（Distribution Shift）。
*   **研究动机**：
    *   现有的归一化方法（如 RevIN）假设历史与未来分布相同，仅做静态的反归一化，无法应对动态偏移。
    *   复杂的深度架构试图隐式吸收分布偏移，但效率低下且容易受到噪声干扰。
    *   **关键观察**：作者发现许多分布偏移并非随机，而是遵循与时间位置相关的**周期性/循环模式**（例如交通流量在每天同一时段的波动规律）。
*   **整体含义**：提出一种显式建模“周期性分布偏移”的轻量级框架，将非平稳性从“干扰”转变为“可建模的结构化信息”。

### 2. 方法论
*   **核心思想**：借鉴通信系统中的**相位-振幅调制（PAM）**理论，在归一化后的特征空间中，利用可学习的周期性嵌入来动态调整表示。
*   **关键技术细节**：
    *   **周期性嵌入学习**：根据当前时间在周期内的位置（$t \mod L$），从嵌入池中检索对应的相位嵌入（Phase Embedding）和振幅嵌入（Amplitude Embedding）。
    *   **相位调制（Phase Modulation）**：通过**加法变换**捕捉系统性的均值偏移（Mean Shifts）。
    *   **振幅调制（Amplitude Modulation）**：通过**乘法缩放**适应方差的变化（Variance Changes）。
    *   **数学证明**：论文在理论上证明了在归一化空间中进行这种调制，在数学上等价于应用一种**动态去归一化**过程，从而统一了分布自适应与特征学习。
*   **算法流程**：
    1.  **归一化**：对输入序列进行实例归一化，移除随机非平稳性。
    2.  **特征提取**：通过线性层和激活函数提取非线性特征（作为“载波”）。
    3.  **调制**：将特征与周期性嵌入进行元素级乘法（振幅）和加法（相位）融合。
    4.  **预测与反归一化**：通过 MLP 预测头输出结果，并结合初始均值/方差进行反归一化还原。

### 3. 实验设计
*   **数据集**：涵盖 12 个真实世界基准数据集，包括 ETT 系列（4个）、Electricity (ECL)、Traffic、Solar-Energy、Weather 以及 PEMS 系列（4个）。
*   **Benchmark 设置**：
    *   PEMS 数据集预测长度：{12, 24, 48, 96}。
    *   其他数据集预测长度：{96, 192, 336, 720}。
    *   统一回看窗口长度为 96。
*   **对比方法**：对比了 9 种 SOTA 模型，包括 TQNet (2025), TimeEmb (2025), FilterTS (2025), Amplifier (2025), TimeXer (2024), CycleNet (2024), TimeMixer (2024), iTransformer (2024), PatchTST (2023)。

### 4. 资源与算力
*   **硬件环境**：单张 **NVIDIA GeForce RTX 4090 GPU (24GB)**。
*   **算力表现**：
    *   **参数效率**：参数量比基于 Transformer 的方法少 5-10 倍。
    *   **训练速度**：在 Traffic 数据集（862个变量）上，PAMod 的训练速度极快（约 5.8s/epoch），而 PatchTST 需要 172.1s/epoch，效率提升近 30 倍。
    *   **内存占用**：在大规模变量场景下，内存消耗远低于 TimeXer 和 PatchTST。

### 5. 实验数量与充分性
*   **实验规模**：
    *   **主实验**：在 12 个数据集、48 种预测场景下进行了完整测试。
    *   **消融实验**：验证了相位/振幅组件的必要性、不同损失函数的影响以及周期长度 $\tau$ 的敏感性。
    *   **兼容性实验**：将 PAMod 作为插件集成到 DLinear、PatchTST、iTransformer 和 TQNet 中，均显著提升了原模型性能。
*   **充分性与公平性**：实验采用了 5 个随机种子取平均值并标注了误差棒（Error Bars），对比基线均为近两年的顶级会议论文，实验设计严谨、客观。

### 6. 主要结论与发现
*   **性能领先**：PAMod 在 19/24 的评估案例中排名第一，平均降低 MSE 约 4-16%。
*   **周期性偏移的价值**：显式建模周期性非平稳性比单纯消除它（归一化）或盲目保留它（复杂模型）更有效。
*   **即插即用**：PAMod 作为一个轻量级模块，可以显著增强现有各种架构（MLP、Transformer 等）处理非平稳数据的能力。
*   **可解释性**：t-SNE 可视化证明相位嵌入成功捕捉了时间维度的均值偏移，而振幅嵌入捕捉了通道维度的方差波动。

### 7. 优点
*   **理论创新**：巧妙地将通信领域的相幅调制理论引入时序预测，并提供了坚实的数学证明。
*   **极高效率**：在保持 SOTA 性能的同时，极大地降低了计算开销和内存占用，适合实际工业部署。
*   **通用性强**：不限制骨干网络，具有极佳的架构兼容性。
*   **鲁棒性**：实验证明其对长回看窗口具有良好的扩展性。

### 8. 不足与局限
*   **周期依赖性**：模型性能高度依赖于预设的周期长度 $L$。如果数据存在多周期重叠或周期不明显，可能需要复杂的预处理或调优。
*   **非周期偏移处理**：对于突发性的、完全随机的分布偏移（如突发政策变动或极端天气），该周期性建模机制可能失效。
*   **通道一致性假设**：目前对所有通道使用统一的周期长度，但在某些复杂系统中，不同变量可能具有不同的周期特性。

（完）
