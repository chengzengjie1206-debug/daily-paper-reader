---
title: "FRWKV+: Adaptive Periodic-Position Branch Interaction for Frequency-Space Linear Time Series Forecasting"
title_zh: "FRWKV+: 面向频域线性时间序列预测的自适应周期-位置分支交互"
authors: "Qingyuan Yang, Dongyue Chen, Da Teng, Junhua Xiao, Jiaji Pan, Shizhuo Deng"
date: 2026-05-15
pdf: "https://arxiv.org/pdf/2605.15690v1"
tags: ["query:ts"]
score: 9.0
evidence: 频域空间中的长期时间序列预测
tldr: 针对长期时间序列预测中频域建模存在的实部与虚部交互弱、周期位置信息不可靠等问题，本文提出FRWKV+模型。该模型通过引入跨分支门控机制实现实虚流的互调制，并利用Adaptive PhaseGate提取周期位置上下文。结合自适应信任机制，模型能在样本和通道层面动态调整修正强度，在保持FRWKV骨干网络高效性的同时，显著提升了周期性模式下的预测精度。
selection_source: fresh_fetch
motivation: 现有的频域预测模型在实虚部信息交换及周期位置线索的可靠利用方面存在不足。
method: 提出FRWKV+，通过跨分支门控增强实虚部交互，并利用带有自适应信任机制的PhaseGate进行周期位置修正。
result: 实验表明FRWKV+在同族模型中获得了最大的MSE胜率覆盖，并在特定周期性场景下表现出明显优势。
conclusion: 自适应的周期位置分支交互机制能有效提升频域线性预测模型的性能，且具备良好的灵活性。
---

## 摘要
长期时间序列预测对于能源、金融、交通和医疗系统的决策至关重要。近期的轻量级预测模型通过在变换空间或线性化空间中运行来提高效率，但频域预测仍面临两个挑战：复数谱的实部和虚部流包含互补信息，但这些信息往往交换较弱；此外，周期-位置线索仅在对当前数据集和预测范围可靠时，才能辅助循环模式。为了应对这些挑战，我们提出了 FRWKV+，这是一种增强的 FRWKV 预测模型，用于选择性周期-位置分支交互。FRWKV+ 首先引入了跨分支门控，在实频流和虚频流之间交换紧凑的上下文，允许每个流对另一个流进行调制。随后，它利用自适应相位门（Adaptive PhaseGate）机制提取周期-位置上下文，并对这些门控生成带符号的修正。一种自适应信任机制在样本、变量和通道级别控制修正强度，从而使周期-位置信息作为可靠的修正信号被采纳，同时保留 FRWKV 骨干网络的效率。外部基准测试表报告了一个单独标记的 FRWKV 系列选定系统用于手稿级比较，而机制级断言则基于严格的同种子 FRWKV 系列消融实验和代表性组件级消融实验。在这种匹配协议下，FRWKV+ 在系列变体中实现了最大的 MSE 胜出覆盖率，并在选定的周期性区间内提供了明显的增益。组件分析进一步支持了周期-位置上下文、带符号修正和自适应信任在这些区间内的有效性，同时也揭示了更简单的修正规则仍然更具优势的边界情况。

## Abstract
Long-term time series forecasting is essential for decision making in energy, finance, transportation, and healthcare systems. Recent lightweight forecasting models improve efficiency by operating in transformed or linearized spaces, but two challenges remain in frequency-space forecasting. The real and imaginary streams of complex spectra contain complementary information that is often weakly exchanged, and periodic-position cues can help recurring patterns only when they are reliable for the current dataset and prediction horizon. To address these challenges, we propose FRWKV+, an enhanced FRWKV forecasting model for selective periodic-position branch interaction. FRWKV+ first introduces cross-branch gates that exchange compact contexts between the real and imaginary frequency streams, allowing each stream to modulate the other. It then uses the Adaptive PhaseGate mechanism to extract periodic-position context and generate signed corrections to these gates. An adaptive trust mechanism controls the correction strength at the sample, variable, and channel levels, so periodic-position information is admitted as a reliable correction signal while preserving the efficiency of the FRWKV backbone. External benchmark tables report a separately labeled FRWKV-family selected system for manuscript-level comparison, while mechanism-level claims are based on strict matched-seed FRWKV-family ablations and representative component-level ablations. Under this matched protocol, FRWKV+ achieves the largest MSE winner coverage among the family variants and provides clear gains in selected periodic regimes. Component analysis further supports the usefulness of periodic-position context, signed correction, and adaptive trust in these regimes, while revealing boundary cases where simpler correction rules remain preferable.

---

## 论文详细总结（自动生成）

这是一份关于论文《FRWKV+: Adaptive Periodic-Position Branch Interaction for Frequency-Space Linear Time Series Forecasting》的结构化总结：

### 1. 论文的核心问题与整体含义
论文针对长期多变量时间序列预测（LSTF）中的**频域建模**效率与精度平衡问题展开研究。虽然现有的轻量级频域模型（如 FRWKV）具有较高的计算效率，但存在两个核心瓶颈：
*   **实虚部交互不足**：复数频谱的实部和虚部流通常被独立处理，缺乏互补信息的交换。
*   **周期位置信息的不可靠性**：周期性线索在不同数据集和预测步长下的可靠性差异巨大，盲目引入可能引入噪声。
**整体含义**：提出一种增强型模型 FRWKV+，通过自适应的跨分支交互和信任控制机制，在保持线性复杂度的同时，选择性地利用周期位置信息来修正频域预测。

### 2. 论文提出的方法论
FRWKV+ 的核心思想是“**选择性修正**”，主要包含以下关键技术：
*   **跨分支门控（CrossBranchGate）**：在实部和虚部频率流之间引入交互。通过对一个分支的频率响应进行全局平均池化提取上下文，生成门控信号来调制另一个分支。
*   **周期位置上下文编码器（PPCE）**：将时域嵌入序列按设定的周期长度进行折叠和平均，利用可学习的路由（Router）机制提取紧凑的周期位置上下文向量 $C_{pos}$。
*   **自适应相位门（Adaptive PhaseGate）**：
    *   **带符号修正**：利用 $tanh$ 函数生成可正可负的修正值 $\Delta$，对跨分支门控进行方向性调整。
    *   **自适应信任机制**：通过一个专门的 MLP 学习信任分数 $T \in [0, 1]$，在样本、变量和通道级别动态控制周期修正信号的强度。
*   **算法流程**：输入序列 $\rightarrow$ 可逆实例归一化（RevIN） $\rightarrow$ 线性嵌入 $\rightarrow$ 快速傅里叶变换（rFFT） $\rightarrow$ 双分支 RWKV 编码 $\rightarrow$ **CrossBranchGate 交互与 PhaseGate 修正** $\rightarrow$ 逆傅里叶变换（iFFT） $\rightarrow$ 投影输出。

### 3. 实验设计
*   **数据集**：使用了 7 个标准基准数据集：ETTh1, ETTh2, ETTm1, ETTm2, Weather, Exchange, ILI。
*   **预测设置**：输入长度固定（如 96），预测步长涵盖 96, 192, 336, 720（ILI 为 24-60）。
*   **对比方法（Baselines）**：
    *   **线性/轻量级**：DLinear, RWKV-TS, S-Mamba, FRWKV。
    *   **Transformer 家族**：iTransformer, PatchTST, Informer, Autoformer, FEDformer。
    *   **大模型/预训练类**：Time-LLM, TimeCMA, Chronos-2, UniTime, T3Time。
    *   **频域/相位相关**：FreTS, PhaseFormer, TimesNet。

### 4. 资源与算力
*   **硬件**：实验在一台配备 **8 台 NVIDIA GeForce RTX 3090 GPU** 的服务器上运行。
*   **训练细节**：使用 AdamW 优化器，采用余弦退火学习率调度（初始 $1 \times 10^{-4}$），Batch Size 为 32（ILI 为 16）。
*   **效率表现**：FRWKV+ 相比基础 FRWKV 仅增加了约 0.01M 的参数量，内存占用几乎持平，训练耗时略有增加，保持了极高的计算效率。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 28 个“数据集-步长”组合。
*   **充分性与公平性**：
    *   采用了**严格的 16 种子匹配协议**进行家族内部消融实验，确保了统计意义上的客观性。
    *   进行了组件级消融（如去除 PPCE、仅保留正向修正、固定信任值等），验证了各模块的必要性。
    *   对比了外部最新（2024-2026年）的强基准模型，实验设计较为严谨。

### 6. 论文的主要结论与发现
*   **性能优势**：FRWKV+ 在 28 个设置中获得了最多的 MSE 胜出覆盖率，尤其在 **ETTh2** 数据集上表现出显著的领先地位。
*   **机制有效性**：自适应信任机制至关重要，它允许模型在周期信号不可靠时退化回简单的分支交互，从而保证了鲁棒性。
*   **区间特性**：周期位置修正并非万能，在 Weather 等噪声较大或周期性不明显的场景下，原始的 FRWKV 或简单的跨分支交互可能更优。

### 7. 优点（亮点）
*   **设计精巧**：将周期性信息作为“修正项”而非“主信号”，通过自适应信任分数解决了频域建模中信息可靠性的难题。
*   **高效性**：在不破坏线性复杂度的前提下，增强了复数频谱的表达能力。
*   **实验严谨**：通过 16 种子匹配实验和详细的区间分析（Regime Analysis），清晰地界定了方法的适用边界。

### 8. 不足与局限
*   **指标差异**：虽然在 MSE 上表现优异，但在 MAE 指标上，原始 FRWKV 在某些设置下依然具有更强的竞争力。
*   **数据集覆盖**：未包含 Traffic 等超高维数据集，这些数据集对跨变量交互和内存压力的挑战可能更大。
*   **隐式估计**：目前的信任机制是隐式学习的，缺乏一个显式的、基于数据统计特性的周期可靠性评估器。

（完）
