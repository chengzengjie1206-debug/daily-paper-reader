---
title: "ITGPT: Generative Pretraining on Irregular Timeseries"
title_zh: ITGPT：不规则时间序列上的生成式预训练
authors: "Antoine Honoré, Ming Xiao"
date: 2026-05-15
pdf: "https://arxiv.org/pdf/2605.16069v1"
tags: ["query:ts"]
score: 10.0
evidence: 针对不规则和缺失时间序列的生成式预训练
tldr: ITGPT是一个针对不规则采样多模态时间序列设计的生成式预训练模型。针对医疗和预测性维护中数据不规则、缺失及标注成本高的问题，该模型结合了自监督学习（SSL）和类GPT的生成式目标，无需重采样或插值即可处理复杂数据。实验证明其在TIHM和CompX数据集上达到SOTA，且在少样本场景下表现优异，有效提升了非结构化数据的利用率。
selection_source: fresh_fetch
motivation: 传统时间序列模型在处理不规则采样、多模态及缺失数据时存在局限，且难以充分利用大规模无标注数据。
method: 提出一种基于注意力机制的ITGPT架构，通过自监督学习和类GPT的生成式预训练目标，直接处理原始不规则时间序列。
result: 在医疗和预测性维护任务中，ITGPT无需重采样或数据插补即可达到SOTA性能，并在标注稀缺时显著优于纯监督方法。
conclusion: ITGPT证明了生成式预训练在处理大规模非结构化时间序列数据中的有效性，为实际推理任务提供了灵活高效的方案。
---

## 摘要
时间序列回归模型通常难以利用大规模的有标签多模态数据，特别是在数据采样不规则或包含缺失值的情况下。这在医疗保健和预测性维护等领域非常常见，在这些领域中，数据通常来自不可靠的来源，且标注需要专家知识或昂贵的设备。基于 Transformer 的大语言模型已证明通过自监督学习 (SSL) 和生成式预训练 (GPT) 框架在文本等结构化数据上非常有效。然而，此类模型缺乏高效处理不规则采样多模态时间序列数据的灵活性。在本文中，我们介绍了 ITGPT，这是一种基于注意力的架构，旨在通过允许使用 SSL 损失和类 GPT 目标进行训练，来处理多模态、不规则采样的时间序列。我们在 TIHM 数据集的医疗保健任务和 CompX 数据集的预测性维护任务上评估了其性能。我们的结果表明，ITGPT 在不需要重采样、特征融合或显式数据插补的情况下，实现了最先进的性能。此外，当标签稀缺时，ITGPT 通过 SSL 和 GPT 训练有效地利用了无标签数据，表现优于纯监督方法。这代表了在实际推理任务中高效利用大规模非结构化时间序列数据集迈出的重要一步。

## Abstract
Timeseries regression models often struggle to leverage large volumes of labeled multimodal data, particularly when the data are irregularly sampled or contain missing values. This is common in domains like healthcare and predictive maintenance, where data are collected from unreliable sources, and labeling requires expert knowledge or costly equipments. Transformer-based large language models have proven effective on structured data such as text through self-supervised learning (SSL) and generative pretraining (GPT) frameworks. However, such models lack the flexibility to efficiently process irregularly sampled multimodal timeseries data. In this paper, we introduce ITGPT, an attention-based architecture designed for handling multimodal, irregularly sampled timeseries by allowing training with both SSL losses and GPT-like objectives. We evaluate its performance on a healthcare task with the TIHM dataset, and a predictive maintenance task with the CompX dataset. Our results demonstrate that ITGPT achieves state-of-the-art performance without requiring resampling, feature fusion or explicit data imputation. Furthermore, when labels are scarce, ITGPT effectively leverages unlabeled data through SSL and GPT training, outperforming the purely supervised approach. This represents an important step towards efficiently using large and unstructured timeseries datasets for practical inference tasks.

---

## 论文详细总结（自动生成）

这是一份关于论文《ITGPT: Generative Pretraining on Irregular Timeseries》的结构化总结：

### 1. 核心问题与研究背景
*   **核心问题**：在医疗保健和预测性维护等实际应用中，多模态时间序列数据通常具有**采样频率不一致（不规则采样）**、**数据缺失**以及**标注成本极高**的特点。
*   **研究背景**：虽然基于 Transformer 的大语言模型（LLM）通过生成式预训练（GPT）和自监督学习（SSL）在结构化文本数据上取得了巨大成功，但现有模型在处理非结构化、不规则采样的多模态时间序列时缺乏灵活性，通常依赖于计算昂贵或过于简单的重采样、插值和特征融合方法。

### 2. 方法论
ITGPT 扩展了作者之前的 ITNet 模型，旨在实现类 GPT 的预训练目标：
*   **核心思想**：通过“锚点（Anchor）”机制将不同频率的模态对齐。模型由编码器（Encoder）和解码器（Decoder）成对组成，可以堆叠形成深度网络。
*   **关键技术细节**：
    *   **编码器**：接收多模态不规则数据，利用**因果交叉注意力机制（Causal Cross-Attention）**将其映射到一个预定义的统一时间线（锚点数据）。
    *   **解码器**：以锚点数据为输入，将其重新映射回原始的各模态时间线，从而实现输入空间的重构。
    *   **位置编码（PE）**：使用正弦/余弦函数将标量时间戳投影到高维空间，使模型能够处理连续时间差。
    *   **训练目标**：支持多种模式，包括纯监督学习（交叉熵损失 CE）、自监督学习（预测下一时刻输入数据的均方误差损失 MSE）以及先 GPT 预训练再 CE 微调的模式。

### 3. 实验设计
*   **数据集**：
    1.  **TIHM 数据集**：痴呆症患者远程医疗监控数据，包含室内活动（红外、门传感器）和生理指标（血压、体温等），属于极度稀疏且不规则的数据。
    2.  **SCANIA CompX 数据集**：重型卡车的真实世界预测性维护数据集，包含 15 种模态（直方图变量、数值计数器、车辆规格等），样本量达 2.8 万辆车。
*   **对比基准与方法**：
    *   对比了纯监督学习（CE）、自监督辅助学习（CE+SSL）和生成式预训练后微调（GPT → CE）。
    *   在 TIHM 上对比了原始论文中的线性插值+线性模型方法。
    *   在 CompX 上进行了详尽的架构搜索，包括不同深度（1-7 层）、不同丢弃率（0-30%）和不同混合层（线性、MLP）。

### 4. 资源与算力
*   **算力资源**：论文提到计算得到了 **Berzelius 资源**的支持（由瑞典国家超级计算机中心提供）。
*   **具体配置**：文中**未明确说明**具体的 GPU 型号、数量或总训练时长。但提到使用了 ADAM 优化器，学习率为 $5 \times 10^{-4}$，Batch Size 为 64，训练轮数为 20 轮。

### 5. 实验数量与充分性
*   **实验规模**：
    *   针对 CompX 数据集进行了 5 折交叉验证。
    *   进行了多组消融实验：改变模型深度（L=1 到 7）、测试不同复杂度的混合层（Linear vs MLP）、调整 Dropout 比例。
    *   **少样本实验**：专门测试了在仅有 0.1% 到 1% 标签数据情况下的模型表现。
*   **充分性评价**：实验设计较为充分且客观。通过在两个完全不同领域（医疗 vs 工业）的数据集上验证，证明了模型的通用性。少样本实验有力地支持了其“生成式预训练”的价值。

### 6. 主要结论与发现
*   **SOTA 性能**：ITGPT 在无需重采样、特征融合或显式数据插补的情况下，在 CompX 任务上达到了最先进的性能（AUPRC 约 0.44）。
*   **少样本优势**：在标签稀缺的情况下，CE+SSL 和 GPT → CE 模式显著优于纯监督学习。特别是 CE+SSL 模式，在标签极少时表现最稳定。
*   **架构特性**：增加模型深度可以提高容量，但必须配合适当的 Dropout（如 10%-20%）以防止过拟合；MLP 混合层优于线性混合层。

### 7. 优点
*   **端到端处理**：直接处理原始不规则时间序列，避免了预处理带来的信息损失或人工偏差。
*   **灵活性**：架构允许处理任意数量和维度的模态，且能自然地利用无标签数据进行预训练。
*   **鲁棒性**：在极度失真和不规则的真实世界数据（如 CompX）上表现稳健。

### 8. 不足与局限
*   **计算复杂度**：虽然模型灵活，但注意力机制在处理极长序列时可能存在计算瓶颈（作者在结论中也提到了未来需要优化 CUDA 内核）。
*   **性能差距**：在 TIHM 数据集上，虽然无需插值，但其 Recall 和 Specificity 略低于原始论文中使用专家经验和线性插值的方法，说明在极小规模数据集上，深度学习的优势尚未完全抵消先验知识的缺失。
*   **可解释性**：作为深度注意力模型，其决策过程的透明度不如传统的统计模型，这在医疗领域是一个潜在限制。

（完）
