---
title: "MuteBench: Modality Unavailability Tolerance Evaluation for Incomplete Multimodal Fusion"
title_zh: MuteBench：不完整多模态融合的模态不可用容忍度评估
authors: "Wugeng Zheng, Ziwen Kan, Tianlong Chen, Chen Chen, Song Wang"
date: 2026-05-13
pdf: "https://arxiv.org/pdf/2605.15235v1"
tags: ["query:ts"]
score: 8.0
evidence: 评估模态内缺失数据模式下的融合架构
tldr: 针对临床AI中多模态生理数据传感器失效（全模态缺失或模态内片段缺失）的普遍问题，本文提出了MuteBench基准测试。该基准涵盖9个临床领域数据集和6种融合架构，通过对12.5万个样本的评估，揭示了架构类型是鲁棒性的关键决定因素，并探讨了课程模态丢弃和扩散归补等策略的有效性，为构建鲁棒的多模态融合方法提供了指导。
selection_source: fresh_fetch
motivation: 临床场景中多模态传感器常发生全模态或局部数据缺失，但现有研究缺乏在不同缺失模式下对多种融合架构的系统性评估。
method: 构建了包含9个数据集和6种架构的MuteBench基准，模拟两种缺失模式并评估了课程学习及扩散模型补全的效果。
result: 研究发现架构类型是鲁棒性的核心预测指标，通道独立模型在模态缺失下表现优异，而扩散补全能有效缓解模态内缺失带来的性能下降。
conclusion: MuteBench为选择和设计能够应对现实世界数据缺失挑战的鲁棒多模态融合方法提供了重要的实践指南。
---

## 摘要
多模态生理数据为从重症监护室到可穿戴设备的临床人工智能系统提供动力，但在实践中传感器经常发生故障。两种故障模式较为常见：模态缺失（即整个通道缺失）和模态内缺失（即连续的时间段丢失）。目前尚无基准测试能够在多种临床数据集上，在受控的严重程度下，针对这两种故障模式评估多种融合架构。我们提出了 MuteBench，这是一个涵盖 7 个临床领域的 9 个数据集、6 种融合架构和 2 种缺失数据模式的基准，包含超过 125,000 个样本。通过该基准测试，我们发现架构家族是鲁棒性最强的预测指标，其影响超过了参数量。通道独立模型能很好地容忍模态缺失，但对模态内缺失较为敏感，尤其是在短序列上。课程模态丢弃（Curriculum modality dropout）仅在训练中使用的最大丢弃率范围内提供可靠的保护。我们还发现，通道数量、序列长度和模态对齐共同决定了哪种故障模式构成的威胁更大。最后，一项 PTB-XL 案例研究表明，基于扩散的插补可以改善模态内缺失下的下游分类，对于专家路由对损坏输入最敏感的模型，其收益最大，尽管跨数据集的更广泛验证仍是一个开放的研究方向。MuteBench 为从业者在选择现有架构和指导未来鲁棒多模态融合方法的设计方面提供了具体的指导。

## Abstract
Multimodal physiological data powers clinical AI systems from intensive care units to wearable devices, but sensors routinely fail in practice. Two failure modes are common: modality missing, where an entire channel is absent, and within-modality missing, where a contiguous time segment is lost. No existing benchmark evaluates multiple fusion architectures under both failure modes at controlled severity levels across diverse clinical datasets. We present MuteBench, a benchmark covering 9 datasets from 7 clinical domains, 6 fusion architectures, and 2 missing-data modes over 125,000 samples. Through this benchmark, we find that architecture family is the strongest predictor of robustness, outweighing parameter count. Channel-independent models tolerate modality missing well but can be sensitive to within-modality missing, especially on short sequences. Curriculum modality dropout protects reliably only up to the maximum dropout rate used in training. We also find that channel count, sequence length, and modality alignment jointly determine which failure mode poses the greater threat. Finally, a PTB-XL case study suggests that diffusion-based imputation can improve downstream classification under within-modality missing, with the largest gains for models whose expert routing is most sensitive to corrupted inputs, though broader validation across datasets remains an open direction. MuteBench provides practitioners with concrete guidance for both selecting existing architectures and informing the design of future robust multimodal fusion methods.

---

## 论文详细总结（自动生成）

这是一份关于论文《MuteBench: Modality Unavailability Tolerance Evaluation for Incomplete Multimodal Fusion》的结构化深入总结：

### 1. 论文的核心问题与整体含义
*   **研究动机**：在临床 AI 应用（如 ICU 监测、可穿戴设备）中，多模态生理数据（ECG、EEG、PPG 等）至关重要。然而，现实中传感器经常失效（如导联脱落、设备失联、传输错误），导致数据不完整。
*   **核心问题**：现有的多模态模型大多假设数据完整，且缺乏一个统一的基准来评估不同融合架构在**全模态缺失**（整个通道丢失）和**模态内缺失**（连续时间段丢失）这两种现实故障模式下的鲁棒性。
*   **整体含义**：本文提出了 **MuteBench**，这是首个系统性评估临床多模态融合模型对数据缺失容忍度的基准测试，旨在为构建鲁棒的临床 AI 系统提供架构选择和设计指南。

### 2. 论文提出的方法论
*   **核心思想**：通过构建一个包含多样化临床数据集、多种代表性融合架构以及受控缺失模式的评估框架，揭示架构设计与鲁棒性之间的内在联系。
*   **关键技术细节**：
    *   **缺失模式模拟**：
        1.  **模态缺失（Modality Missing）**：以概率 $p$ 独立丢弃整个通道（模拟传感器完全失效）。
        2.  **模态内缺失（Within-modality Missing）**：在活动通道内掩盖 5%-10% 的连续时间块，总比例为 $b$（模拟瞬时干扰或伪影）。
    *   **架构分类**：将 6 种模型分为三类：
        1.  **通道独立模型（Channel-Independent）**：独立编码各通道，通过平均池化聚合。
        2.  **共享-特定分解模型（Shared-Specific）**：将特征分解为模态共有和模态特有部分。
        3.  **混合专家融合模型（MoE-Fusion）**：使用动态路由机制处理不同模态组合。
    *   **统一评估协议**：开发了框架无关的缺失库，确保所有模型在相同的确定性掩码下进行测试，消除数据管道差异。

### 3. 实验设计
*   **数据集（Benchmark）**：涵盖 9 个数据集，跨越 7 个临床领域（ICU、心脏病学、神经科学等），总计超过 12.5 万个样本。
    *   **类型 1（同质对齐）**：如 12 导联 ECG（PTB-XL, Chapman）。
    *   **类型 2（异质对齐）**：如 EEG 与呼吸信号（Sleep-EDF, PPG-DaLiA）。
    *   **类型 3（异质非对齐）**：如时间序列与静态元数据混合（MIMIC-IV, Challenge-2012）。
*   **对比方法**：评估了 6 种代表性架构：CLIMB、MIRA（通道独立）；ShaSpec（共享-特定）；Flex-MoE、FuseMoE、Maestro（MoE 融合）。
*   **评估指标**：主要使用 Macro-AUROC，辅助使用 Macro-F1。

### 4. 资源与算力
*   **算力设备**：主要使用 **NVIDIA B200 GPU**（178.4 GiB VRAM），部分早期探索使用 NVIDIA L4。
*   **实验规模**：总计进行了 **810 次独立运行**（6 模型 × 9 数据集 × 3 随机种子 × 5 缺失配置）。
*   **训练时长**：整个基准测试的运行耗时约 **1 个月**（墙钟时间）。

### 5. 实验数量与充分性
*   **实验数量**：非常充分。每个配置均在 3 个独立随机种子下重复，涵盖了从二分类、多分类到多标签预测的多种任务。
*   **客观性与公平性**：
    *   使用了统一的预处理、损失函数和检查点选择标准（基于验证集最高 AUROC）。
    *   通过确定性掩码确保模型间输入完全一致。
    *   涵盖了不同通道数（2 到 78）和序列长度（48 到 3000 步）的极端情况。

### 6. 主要结论与发现
*   **架构家族是关键**：架构类型比参数量更能决定鲁棒性。
*   **通道独立模型最稳健**：在**全模态缺失**下表现最好，因为其独立编码机制防止了缺失通道污染其他表示。
*   **共享-特定模型擅长处理局部缺失**：在同质信号（如 ECG）的**模态内缺失**下表现优异。
*   **课程学习的局限性**：课程模态丢弃（Curriculum Dropout）仅在训练涵盖的缺失率范围内有效，超出该范围性能会迅速崩溃。
*   **扩散模型补全的潜力**：在 PTB-XL 案例中，基于扩散的插补能显著提升模态内缺失下的分类性能，但对全模态缺失几乎无效。

### 7. 优点
*   **全面性**：首次同时考虑了两种缺失模式、多种架构家族和广泛的临床领域。
*   **实用性**：为从业者提供了基于数据集特征（通道数、序列长度、对齐类型）的架构选择指南。
*   **标准化**：开源了框架无关的缺失模拟库，有助于未来研究的公平对比。

### 8. 不足与局限
*   **缺失模拟的简化**：目前的“模态缺失”是在通道级别进行的，对于 12 导联 ECG 等数据集，丢弃一个导联并不等同于丢失整个传感器源。
*   **插补实验范围有限**：扩散模型插补仅在 PTB-XL 数据集上进行了验证，其在异质或非对齐数据集上的泛化性尚待研究。
*   **缺失机制单一**：主要关注完全随机缺失（MCAR），未深入探讨非随机缺失（MNAR）等更复杂的临床场景。

（完）
