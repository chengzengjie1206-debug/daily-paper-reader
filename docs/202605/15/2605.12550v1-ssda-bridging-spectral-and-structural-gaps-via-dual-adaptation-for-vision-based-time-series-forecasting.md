---
title: "SSDA: Bridging Spectral and Structural Gaps via Dual Adaptation for Vision-Based Time Series Forecasting"
title_zh: SSDA：通过双重自适应弥合频谱与结构差距，用于基于视觉的时间序列预测
authors: "Mingrui Zhang, Hanchen Yang, Wengen Li, Xudong Jiang, Yichao Zhang, Jihong Guan, Shuigeng Zhou"
date: 2026-05-10
pdf: "https://arxiv.org/pdf/2605.12550v1"
tags: ["query:ts"]
score: 8.0
evidence: 基于视觉的时间序列预测
tldr: 本研究针对基于视觉大模型（LVM）的时间序列预测中存在的频谱和结构差异问题，提出了双重适配网络SSDA。通过频谱幅度对齐器（SMA）调整图像频谱分布以接近自然图像，并利用结构引导低秩适配（SG-LoRA）注入位置感知的时间编码，有效弥合了1D序列转2D图像带来的信息断裂与伪空间关联。实验证明，SSDA在多个基准数据集上显著优于现有的LVM和LLM预测模型，充分释放了视觉预训练模型的迁移潜力。
selection_source: fresh_fetch
motivation: 现有的视觉预测方法忽略了渲染的时间序列图像与自然图像在频谱分布和空间结构上的显著差异，限制了预训练模型的知识迁移效果。
method: 提出SSDA框架，通过SMA模块在数据层面进行频谱对齐，并利用SG-LoRA在模型层面注入时间位置编码和低秩适配。
result: 在七个真实世界数据集上的全量和少样本实验中，SSDA的预测性能均一致优于基于LVM和LLM的强基准模型。
conclusion: 通过在数据和模型层面进行双重适配，可以有效弥合视觉与时间序列间的模态鸿沟，显著提升视觉大模型在预测任务中的表现。
---

## 摘要
大视觉模型（LVMs）最近被证明是出人意料有效的时间序列预测器，只需将时间数据渲染为图像即可。然而，这一成功建立在一个很大程度上未经检验的前提之上：渲染的时间序列图像与自然图像足够接近，使得预训练模型中的知识能够有效迁移。我们认为仍然存在两个差距，即频谱差距和结构差距，这从根本上限制了 LVM 在时间序列预测方面的潜力。在频谱方面，我们系统地揭示了渲染的时间序列图像表现出比 LVM 预训练识别的自然图像明显更浅的功率谱。在结构方面，将一维时间序列重塑为二维网格会产生虚假的空间邻接关系，同时切断了真实的时间连续性，从而误导了预训练 LVM 的空间归纳偏置。为了弥合这些差距，我们提出了 SSDA，这是一个双分支网络，通过频谱和结构自适应来释放 LVM 在时间序列预测中的全部潜力。在数据层面，频谱幅度对齐器（SMA）应用二维快速傅里叶变换（2D FFT）有选择地增强幅度谱，使其趋向自然图像统计特性，同时保留相位。在模型层面，结构引导的低秩自适应（SG-LoRA）将位置感知的时间编码注入到 patch 嵌入中，并通过低秩更新来调整注意力机制。这两个分支进一步自适应融合以产生最终预测。在七个真实世界基准测试上的广泛实验表明，SSDA 在全样本和少样本设置下均始终优于强大的基于 LVM 和 LLM 的基准模型。代码已公开在 https://anonymous.4open.science/r/SSDA-8C5B。

## Abstract
Large vision models (LVMs) have recently proven to be surprisingly effective time series forecasters, simply by rendering temporal data as images. This success, how ever, rests on a largely unexamined premise: the rendered time series images are sufficiently close to natural images for knowledge in pre-trained models to transfer effectively. We argue that two gaps still remain, i.e., spectral and structural gaps, fundamentally limiting the potential of LVMs for time series forecasting. Spectrally, we systematically reveal that rendered time series images exhibit a markedly shallower power spectrum than the natural images LVMs are pre-trained to recognize. Structurally, reshaping 1D temporal sequences into 2D grids fabricates spurious spatial adjacencies while severing genuine temporal continuities, misleading the spatial inductive biases of pre-trained LVMs. To bridge these gaps, we propose SSDA, a dual-branch network that spectrally and structurally adapts to unlock the full potential of LVMs for time series forecasting. At the data level, a Spectral Magnitude Aligner (SMA) applies 2D FFT to selectively enhance the magnitude spectrum toward natural-image statistics while preserving phase. At the model level, a Structural-Guided Low-Rank Adaptation (SG-LoRA) injects position-aware temporal encodings into patch embeddings and adapts at tention via low-rank updates. The two branches are further adaptively fused to produce the final forecast. Extensive experiments on seven real-world benchmarks demonstrate that SSDA consistently outperforms strong LVM- and LLM-based baselines under both full-shot and few-shot settings. Code is publicly available at https://anonymous.4open.science/r/SSDA-8C5B.

---

## 论文详细总结（自动生成）

这篇论文针对基于视觉大模型（LVM）的时间序列预测任务，提出了名为 **SSDA** 的双重自适应框架。以下是对该论文的深度结构化总结：

### 1. 核心问题与整体含义（研究动机）
*   **研究背景**：近期研究发现，将一维时间序列渲染为二维图像并输入预训练的视觉大模型（如 MAE）能取得极佳的预测效果。
*   **核心问题**：论文指出，现有的“视觉预测”方法忽略了渲染图像与自然图像之间的**模态鸿沟（Modality Gap）**，主要体现在两个维度：
    *   **频谱差距（Spectral Gap）**：通过功率谱斜率（PSS）分析发现，时间序列图像的频谱分布比自然图像更“浅”（衰减更慢），不符合 LVM 预训练时遵循的 $1/f^\alpha$ 统计规律。
    *   **结构差距（Structural Gap）**：将 1D 序列折叠成 2D 图像会产生“虚假的空间邻接”（原本遥远的点在图像上相邻）并“切断真实的时间连续性”（原本相邻的点在图像上变远），误导了 LVM 的空间归纳偏置。

### 2. 方法论：SSDA 双分支网络
SSDA 通过在数据层和模型层进行双重适配来弥合上述鸿沟：
*   **频谱分支（数据层适配）—— 频谱幅度对齐器 (SMA)**：
    *   利用 **2D 快速傅里叶变换 (FFT)** 将图像分解为幅度和相位。
    *   使用轻量级卷积模块有选择地增强幅度谱，使其统计特性向自然图像靠拢，同时**保留相位信息**以维持原始的时间结构。
    *   处理后的图像输入冻结的 MAE 编码器，最大化利用视觉预训练知识。
*   **结构分支（模型层适配）—— 结构引导低秩适配 (SG-LoRA)**：
    *   **时间定位适配器 (TGA)**：将 2D Patch 映射回原始 1D 时间索引，注入正弦时间位置编码，并通过可学习的门控机制融合到 Patch 嵌入中。
    *   **LoRA 适配**：在注意力层注入低秩矩阵进行微调，使模型能够识别被图像化破坏的真实时间顺序。
*   **自适应融合**：通过一个可学习的标量权重 $\beta$ 动态平衡两个分支的输出，生成最终预测。

### 3. 实验设计
*   **数据集**：使用了 7 个主流的真实世界基准数据集，包括 ETT 系列（ETTh1, ETTh2, ETTm1, ETTm2）、Weather、Traffic 和 Electricity。
*   **对比方法（11 个基准模型）**：
    *   **基于 LVM**：VisionTS, DMMV, Time-VLM。
    *   **基于 LLM**：Time-LLM, TimeCMA, GPT4TS。
    *   **经典/深度模型**：PatchTST, TimesNet, FEDformer, Informer, DLinear。
*   **实验场景**：涵盖了全量样本（Full-shot）和少样本（Few-shot，5% 和 10% 比例）两种设置。

### 4. 资源与算力
*   **硬件环境**：使用了工作站配备的 **8 台 NVIDIA GeForce RTX 4090 GPU (24GB)**。
*   **软件环境**：Python 3.10, PyTorch 2.2.2, CUDA 12.1。
*   **训练细节**：Batch size 为 32，学习率为 2e-6，训练 10 个 epoch，采用 Adam 优化器。

### 5. 实验数量与充分性
*   **实验规模**：在 7 个数据集、4 种预测长度（96, 192, 336, 720）下进行了详尽测试。
*   **消融实验**：针对 SMA、SG-LoRA、TGA 等核心组件进行了剥离测试，验证了各部分的必要性。
*   **充分性评价**：实验设计非常充分。不仅有精度对比，还包含了超参数敏感性分析（如回顾窗口、周期性参数、隐藏层维度）、效率分析（参数量 vs. 内存 vs. 精度）以及可视化验证（展示了结构分支如何恢复时间连续性）。

### 6. 主要结论与发现
*   **性能领先**：SSDA 在全量样本测试中获得了 48 项第一，显著优于现有的视觉和语言大模型方案。
*   **少样本优势**：在数据极度匮乏（5% 训练集）的情况下，SSDA 表现出极强的鲁棒性，证明了预训练视觉知识的有效迁移。
*   **鸿沟验证**：通过 PSS 分析证实 SMA 确实缩小了频谱差距；通过可视化证实 SG-LoRA 能够抑制虚假空间关联，恢复垂直的时间模式。

### 7. 优点（亮点）
*   **理论深刻**：首次系统性地定义并量化了时间序列图像化过程中的“频谱”和“结构”模态鸿沟。
*   **设计精巧**：SMA 模块“保相位、调幅度”的设计非常符合信号处理直觉；SG-LoRA 成功解决了图像重塑带来的拓扑扭曲。
*   **效率平衡**：在保持高性能的同时，参数量（约 1.5M）和内存占用远低于基于 LLM 的方案。

### 8. 不足与局限
*   **周期性依赖**：图像渲染高度依赖预设的“周期性（Periodicity）”参数，对于周期性不明显的非平稳序列，效果可能受限。
*   **计算开销**：虽然使用了 LoRA，但双分支结构（一个冻结、一个微调）在推理时仍需运行两次编码过程，相比单分支模型计算量有所增加。
*   **应用限制**：目前主要针对长时预测（LTSF），在极短时预测或实时流数据上的表现尚未探讨。

（完）
