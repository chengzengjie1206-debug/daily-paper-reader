---
title: "SeesawNet: Towards Non-stationary Time Series Forecasting with Balanced Modeling of Common and Specific Dependencies"
title_zh: SeesawNet：通过平衡建模通用与特定依赖关系实现非平稳时间序列预测
authors: "Hao Li, Lu Zhang, Liu Chong, Yankai Chen, Pengyang Wang, Yingjie Zhou"
date: 2026-05-14
pdf: "https://arxiv.org/pdf/2605.14551v1"
tags: ["query:ts"]
score: 9.0
evidence: 非平稳多变量时间序列预测
tldr: 针对非平稳多元时间序列预测中实例归一化（IN）导致特定结构信息过度平滑的问题，本文提出 SeesawNet。该架构通过自适应平稳-非平稳注意力机制（ASNA），在时间和通道维度上动态平衡通用依赖与实例特定依赖的建模，有效解决了分布偏移与异构性保留之间的矛盾，在多个基准数据集上达到了领先水平。
selection_source: fresh_fetch
motivation: 现有的实例归一化方法在减少分布偏移的同时，容易抹除对建模时间与通道异构性至关重要的实例特定结构信息。
method: 提出 SeesawNet 架构，核心是 ASNA 机制，它分别从归一化和原始序列中提取通用与特定依赖，并根据非平稳性进行自适应融合。
result: 在多个真实世界基准数据集上的广泛实验表明，SeesawNet 的预测性能一致优于现有的最先进方法。
conclusion: SeesawNet 通过协同建模时间和通道关系，成功实现了非平稳时间序列中通用与特定依赖的动态平衡。
---

## 摘要
实例归一化（IN）被广泛应用于非平稳多变量时间序列预测，以减少分布偏移并突出样本间的通用模式。然而，IN 可能会过度平滑实例特定的结构信息，而这些信息对于建模时间与跨通道的异质性至关重要。尽管先前的方法进一步抑制了分布差异或尝试恢复时间特定的依赖关系，但它们往往忽略了一个核心矛盾：如何根据每个实例的非平稳结构，自适应地建模通用和实例特定的依赖关系。为了解决这一困境，我们提出了 SeesawNet，这是一种在时间轴和通道维度上动态平衡通用与实例特定依赖关系建模的统一架构。其核心是自适应平稳-非平稳注意力机制（ASNA），它从归一化序列中捕获通用依赖关系，从原始序列中捕获特定依赖关系，并根据实例级的非平稳性对二者进行自适应融合。基于 ASNA，SeesawNet 交替进行专门的时间和通道关系建模，以共同捕获远程和跨变量依赖关系。在多个真实世界基准测试上的广泛实验表明，SeesawNet 始终优于现有最先进的方法。

## Abstract
Instance normalization (IN) is widely used in non-stationary multivariate time series forecasting to reduce distribution shifts and highlight common patterns across samples. However, IN can over-smooth instance-specific structural information that is essential for modeling temporal and cross-channel heterogeneity. While prior methods further suppress distribution discrepancies or attempt to recover temporal specific dependencies, they often ignore a central tension: how to adaptively model common and instance-specific dependency based on each instance's non-stationary structures. To address this dilemma, we propose SeesawNet, a unified architecture that dynamically balances common and instance-specific dependency modeling in both temporal and channel dimensions. At its core is Adaptive Stationary-Nonstationary Attention (ASNA), which captures common dependencies from normalized sequences and specific dependencies from raw sequences, and adaptively fuses them according to instance-level non-stationarity. Built upon ASNA, SeesawNet alternates dedicated temporal and channel relationship modeling to jointly capture long-range and cross-variable dependencies. Extensive experiments on multiple real-world benchmarks demonstrate that SeesawNet consistently outperforms state-of-the-art methods.

---

## 论文详细总结（自动生成）

这是一份关于论文《SeesawNet: Towards Non-stationary Time Series Forecasting with Balanced Modeling of Common and Specific Dependencies》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：在非平稳多变量时间序列预测中，如何平衡“通用模式学习”与“实例特定结构保留”之间的矛盾。
*   **研究背景**：
    *   **实例归一化（IN）的副作用**：虽然 IN（如 RevIN）能有效减少分布偏移，但它会过度平滑掉实例特有的趋势、局部波动以及跨通道的异构性（即“过度平稳化”）。
    *   **现有方法的不足**：现有研究要么侧重于进一步消除非平稳性，要么尝试在归一化后恢复时间依赖，但缺乏一种根据实例自身的非平稳程度，自适应地在时间和通道两个维度上协调通用与特定依赖关系的机制。

### 2. 方法论
SeesawNet 采用双路径架构，核心思想是通过“跷跷板”式的平衡机制动态整合不同来源的信息。
*   **核心组件：自适应平稳-非平稳注意力（ASNA）**：
    *   **平稳分支**：在归一化序列上计算注意力，捕捉样本间可迁移的通用规律（如周期性）。
    *   **非平稳分支**：在原始序列上计算注意力，保留实例特定的结构线索（如特定趋势）。
    *   **自适应门控机制**：利用 Sigmoid 函数生成门控张量 $G$，根据实例的非平稳特性动态融合上述两个分支的注意力分数。
*   **整体架构流程**：
    1.  **双路径输入**：同时输入归一化序列（Stationary）和原始序列（Non-stationary）。
    2.  **补丁依赖学习层（PDL）**：利用 ASNA 建模每个通道内部的时间补丁依赖。
    3.  **通道关系学习层（CRL）**：在时间下采样后，利用 ASNA 建模不同变量间的跨通道依赖。
    4.  **预测与反归一化**：展平特征后通过线性头预测，最后还原量纲。

### 3. 实验设计
*   **数据集**：涵盖 9 个真实世界基准数据集，包括 ETT (h1, h2, m1, m2)、Exchange、Weather、ILI、Solar-energy 和 ECL。
*   **对比方法（Benchmark）**：
    *   **归一化改进类**：DDN, SAN。
    *   **归一化后依赖恢复类**：U-Mixer, NS-Transformer, TimeBridge。
    *   **先进预测模型**：PDF, iTransformer, PatchTST。
*   **评价指标**：均方误差（MSE）和平均绝对误差（MAE）。

### 4. 资源与算力
*   **硬件设备**：使用了 **2 台 NVIDIA RTX 4090 GPU (24GB)**。
*   **训练细节**：采用 Adam 优化器，学习率在 {1e-3, 2e-4, 1e-4} 中搜索，使用了混合时频损失函数 Fredf 以提高稳定性。
*   **效率说明**：ASNA 引入了常数倍的计算开销（注意力计算从 $O(N^2)$ 变为 $O(2N^2)$），但通过时间下采样技术，在实际运行中保持了与 iTransformer 等模型相当的推理延迟。

### 5. 实验数量与充分性
*   **实验规模**：在 9 个数据集、4 种预测长度（共 72 种设置）下进行了测试，每种设置运行 3 次取平均值。
*   **消融实验**：针对平稳分支、非平稳分支、门控机制、时间建模层、通道建模层分别进行了剔除实验，验证了各组件的必要性。
*   **泛化性测试**：将 ASNA 模块直接替换到 PatchTST 和 iTransformer 的骨干网络中，均取得了显著的性能提升。
*   **客观性**：实验覆盖了不同频率、不同变量规模的数据，对比了近年来最先进的各类模型，实验设计较为公平、客观。

### 6. 主要结论与发现
*   **性能领先**：SeesawNet 在 72 种设置中的 51 种达到了最优，显著优于现有的非平稳处理方法。
*   **平衡的重要性**：实验证明，单纯依赖归一化或单纯依赖原始数据都无法达到最优，自适应门控能根据样本偏离分布中心的程度自动调整权重。
*   **多维度协同**：在时间和通道两个维度上同时进行平衡建模，比单一维度的改进效果更好。

### 7. 优点与亮点
*   **自适应性强**：ASNA 模块不是静态的特征堆叠，而是根据输入实例的非平稳性动态调整。
*   **通用性高**：ASNA 作为一个解耦的注意力模块，可以轻松集成到其他基于 Transformer 的预测模型中。
*   **视角全面**：首次明确讨论并解决了归一化对“跨通道依赖”建模的负面影响。

### 8. 不足与局限
*   **计算开销**：虽然是常数倍增长，但双路径设计确实增加了内存占用和计算量，在超大规模变量场景下可能存在压力。
*   **超参数敏感性**：补丁大小（Patch size）和下采样比例等参数对不同数据集的影响可能较大，需要精细调优。
*   **理论深度**：虽然实验效果显著，但对于“非平稳性如何具体量化并指导门控权重”的理论解释仍有进一步挖掘的空间。

（完）
