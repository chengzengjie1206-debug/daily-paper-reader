---
title: "McCast: Memory-Guided Latent Drift Correction for Long-Horizon Precipitation Nowcasting"
title_zh: McCast：面向长时效降水临近预报的记忆引导潜变量漂移修正
authors: "Penghui Wen, Yu Luo, Lintao Wang, Mengwei He, Patrick Filippi, Thomas Francis Bishop, Zhiyong Wang"
date: 2026-05-13
pdf: "https://arxiv.org/pdf/2605.13197v1"
tags: ["query:ts"]
score: 8.0
evidence: 长时程降水临近预报的潜变量漂移修正
tldr: 针对降水临近预报中自回归模型在长时预测时容易产生误差累积和轨迹偏移的问题，本文提出了McCast。该方法引入了漂移校正记忆库（DCBank），通过时间组织的记忆主动校正潜在状态的演化。McCast包含校正潜变量提取和记忆检索两个阶段，能有效减少预测偏移，在SEVIR和MeteoNet数据集上实现了长时预报的SOTA性能。
selection_source: fresh_fetch
motivation: 现有的自回归降水预报方法在长时预测中存在严重的误差累积和物理轨迹偏移问题，且缺乏主动校正机制。
method: 提出McCast方法，利用漂移校正记忆库（DCBank）通过两阶段过程来主动校正潜在演化轨迹，包括提取初始校正并结合历史记忆进行精细化。
result: 在SEVIR和MeteoNet两个基准数据集上的实验表明，McCast在长时预报场景下取得了最先进的性能。
conclusion: 通过显式校正潜在状态的演化而非仅优化单步精度，McCast显著提升了长时降水预报的时间一致性和可靠性。
---

## 摘要
现有的降水临近预报方法通常采用自回归方案，即通过先前的输出来预测未来状态。然而，这种方法在长程推演过程中会累积误差，导致预测结果偏离物理上合理的演变轨迹。尽管已有研究尝试通过提高单步预测精度来缓解这一问题，但它们在很大程度上忽略了气象系统的全局时间演变，且缺乏在推演过程中主动修正漂移的机制。为了解决这一问题，我们提出了 McCast，一种用于降水临近预报的记忆引导潜变量漂移修正方法。McCast 并非将记忆视为用于被动调节的无序潜状态字典，而是利用按时间组织的记忆来主动修正自回归潜变量的演变。具体而言，McCast 引入了一个漂移修正记忆库（DCBank），用于显式估计时间一致的漂移修正量以校准发散轨迹。DCBank 的漂移修正分为两个阶段：首先，修正潜变量提取器根据当前预测和参考潜状态预测初始修正量；随后，修正感知记忆检索模块利用按时间组织的历史记忆对初始修正量进行细化。通过显式修正潜变量演变而非仅提高单步预测精度，McCast 能够产生时间连贯性更强且更可靠的长时效预测。在 SEVIR 和 MeteoNet 两个广泛使用的基准数据集上的实验表明，McCast 达到了最先进的性能，尤其是在具有挑战性的长时效预测场景中。

## Abstract
Existing precipitation nowcasting methods typically adopt an autoregressive formulation, where future states are predicted from previous outputs. However, such an approach accumulates errors over long rollouts, causing forecasts to drift away from physically plausible evolution trajectories. Although various studies have attempted to alleviate this problem by improving step-wise prediction accuracy, they largely neglect the global temporal evolution of meteorological systems and lack mechanisms to actively correct drift during rollouts. To address this issue, we propose McCast, a memory-guided latent drift correction method for precipitation nowcasting. Rather than treating memory as an unordered dictionary of latent states for passive conditioning, McCast leverages temporally organized memory to actively correct autoregressive latent evolution. Specifically, McCast introduces a Drift-Corrective Memory Bank (DCBank) that explicitly estimates the temporally consistent drift corrections to calibrate the divergent trajectory. DCBank performs drift correction in two stages: a Corrective Latent Extractor first predicts an initial correction from the current prediction and a reference latent state, and a Correction-Aware Memory Retrieval module then refines the initial correction using temporally organized historical memory. By explicitly correcting latent evolution, instead of improving step-wise prediction accuracy only, McCast produces more temporally coherent and reliable long-horizon forecasts. Experiments on two widely used benchmarks, SEVIR and MeteoNet, show that McCast achieves state-of-the-art performance, particularly in challenging long-horizon forecasting scenarios.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **McCast** 的新型降水临近预报方法，旨在解决长时效预报中的误差累积和物理轨迹偏移问题。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：传统的降水临近预报多采用**自回归（Autoregressive）**架构，即用前一时刻的预测作为下一时刻的输入。这种模式会导致误差随时间推移不断累积，产生“降水漂移（Precipitation Drift）”现象，表现为空间结构模糊、运动不一致以及长时效预报的物理连贯性丧失。
*   **研究背景**：现有方法大多致力于提高单步预测的精度，但忽略了气象系统的全局时间演化规律，且缺乏在推演过程中主动修正偏差的机制。

### 2. 方法论：核心思想与技术细节
McCast 的核心思想是将历史记忆从“被动条件”转变为“主动修正”机制。
*   **核心架构**：基于编码器-解码器框架，引入了**漂移修正记忆库（DCBank）**。
*   **关键技术细节**：
    1.  **修正潜变量提取器（CLE）**：通过比较当前预测的先验潜状态与参考潜状态（通常是上一时刻的记忆），计算“原始漂移残差”和“上下文感知差异”，通过门控机制融合得到初始修正量。
    2.  **修正感知记忆检索（CAMR）**：利用按时间组织的历史记忆进行精细化修正。检索标准包含两部分：**内容相关性**（语义匹配）和**漂移一致性**（确保修正方向符合历史演化趋势）。
    3.  **主动修正机制**：不同于简单的特征融合，McCast 将估计的漂移项通过残差连接和门控机制直接作用于潜状态，从而校准预测轨迹。
*   **主干网络**：采用了预训练的地球系统大模型 **Aurora** 作为骨干，并通过 **LoRA** 技术进行高效微调。

### 3. 实验设计
*   **数据集**：
    *   **SEVIR**：美国风暴事件图像数据集，包含 NEXRAD 雷达数据。
    *   **MeteoNet**：法国气象局发布的开放天气数据集。
*   **实验设置**：使用 5 帧历史观测（25 分钟）预测未来 20 帧（100 分钟）。
*   **对比方法（Benchmark）**：对比了 12 种 SOTA 方法，包括确定性模型（SimVP, AlphaPre）、概率/扩散模型（DiffCast, NowcastNet, PercpCast）以及时空 Transformer（Earthformer）等。
*   **评估指标**：CSI（关键成功指数，多个降水阈值）、HSS（海德克技术得分）、LPIPS（感知相似度）和 SSIM（结构相似度）。

### 4. 资源与算力
*   **硬件**：训练使用了 **3 台 NVIDIA A6000 GPU**（每台 48GB 显存）；评估使用了 **1 台 NVIDIA RTX 4090 GPU**。
*   **模型规模**：虽然主干 Aurora 有 13 亿参数，但通过 LoRA 微调，可训练参数仅约 **10.24M**。
*   **效率**：在 RTX 4090 上，生成 20 帧预测仅需 **0.20 秒/样本**，计算量为 **3.80 TFLOPs**，具有较高的推理效率。

### 5. 实验数量与充分性
*   **实验规模**：论文在两个大型基准数据集上进行了详尽的对比测试。
*   **消融实验**：非常充分，分别验证了 DCBank 整体、CLE 提取器、CAMR 检索模块以及“主动修正 vs 被动条件”的有效性。
*   **扩展性测试**：将 DCBank 插入到其他模型（如 SimVP）中，验证了该插件式模块的通用性；还测试了在极短输入（仅 2 帧）下的鲁棒性。
*   **客观性**：实验遵循了领域内标准的评估协议，对比方法涵盖了近年来顶级会议的所有主流路线，结果具有较强的说服力。

### 6. 主要结论与发现
*   **性能领先**：McCast 在所有主要指标上均优于现有方法，尤其在**高强度降水**和**长时效（60-100 分钟）**场景下优势显著。
*   **修正有效性**：显式地修正潜变量演化轨迹比单纯提高单步精度更能维持长期的空间结构和运动连贯性。
*   **记忆的作用**：带有“漂移一致性”的记忆检索能有效防止模型在长程推演中陷入物理上不可行的状态。

### 7. 优点（亮点）
*   **范式创新**：提出了从“被动调节”到“主动修正”的记忆利用新范式。
*   **物理启发**：通过漂移一致性建模，间接引入了对气象演化连续性的约束。
*   **高效适配**：成功将气象大模型（Aurora）应用于高分辨率临近预报任务，并证明了 LoRA 微调的有效性。
*   **即插即用**：DCBank 模块具有良好的通用性，可集成至其他时空预测模型中。

### 8. 不足与局限
*   **物理约束缺失**：虽然引入了记忆引导，但仍属于纯数据驱动方法，尚未显式强制执行质量守恒或平流动力学等硬性物理约束。
*   **时效限制**：研究集中在 100 分钟内的临近预报，对于更长时效（如 6 小时以上）的预报效果尚待验证。
*   **数据依赖**：修正效果高度依赖于历史记忆库的质量，在极端罕见气象事件（记忆库中缺乏类似演化模式）下的表现可能受限。

（完）
