---
title: "SurF: A Generative Model for Multivariate Irregular Time Series Forecasting"
title_zh: SurF：一种用于多元不规则时间序列预测的生成模型
authors: "Mohammad R. Rezaei, Tejas Balaji, Rahul G. Krishnan"
date: 2026-05-13
pdf: "https://arxiv.org/pdf/2605.14069v1"
tags: ["query:ts"]
score: 10.0
evidence: 多变量不规则时间序列预测的生成模型
tldr: 针对不规则采样多变量事件流建模难的问题，本文提出SurF生成模型。该模型利用时间重标度定理（TRT）在事件序列与独立同分布指数噪声间建立可学习的双射，并结合高效的累积强度参数化方案与Transformer编码器。实验表明，SurF在多个基准数据集上达到SOTA，并在跨数据集预训练中展现出强大的泛化能力，是迈向异步事件流基础模型的重要一步。
selection_source: fresh_fetch
motivation: 传统分词方法难以处理跨度巨大的时间间隔，而神经点过程模型受限于窗口级数值积分的计算瓶颈。
method: 利用时间重标度定理构建事件序列与指数噪声间的双射，并设计了三种高效的累积强度参数化方法及Transformer预训练架构。
result: 在六个真实基准数据集上，SurF在多个任务中取得最佳RMSE，且在留一法跨数据集测试中表现优于绝大多数经典和神经自回归基准。
conclusion: SurF为异步事件流提供了一种高效、可扩展的生成建模方案，证明了在异构事件流数据上构建基础模型的可行性。
---

## 摘要
不规则采样的多元事件流仍然是生成建模中一个极具挑战性的模态：当事件间隔跨越多个数量级时，基于分词（tokenization）的方法会失效，而神经时间点过程则受限于窗口级的数值求积（numerical quadrature）。我们（i）提出了 SurF，这是一种生成模型，它利用时间重标度定理（TRT）作为事件序列与独立同分布（i.i.d.）单位速率指数噪声之间的可学习双射，使得单个模型能够在异构事件流数据集上进行训练；（ii）提出了三种累积强度的有效参数化方法，可扩展至长序列；以及（iii）设计了一个基于 Transformer 的编码器用于多数据集预训练。在六个真实世界基准测试中，SurF 在 Earthquake、Retweet 和 Taobao 数据集上实现了已报道的最佳时间均方根误差（RMSE），而在其余三个数据集上，其表现与最强的专用模型相比仅在试验级噪声范围内。在严格的留一法（leave-one-out）协议下，留出的检查点在 5/6 的数据集上击败了所有经典和神经自回归基线，并在 Amazon 和 Earthquake 上击败了所有基线，这是迈向异步事件流基础模型的第一步。

## Abstract
Irregularly sampled multivariate event streams remain a stubbornly difficult modality for generative modeling: tokenization-based approaches break down when inter-event intervals vary by orders of magnitude, and neural temporal point processes are bottlenecked by window-level numerical quadrature. We (i) propose SurF, a generative model that uses the Time Rescaling Theorem (TRT) as a learnable bijection between event sequences and i.i.d.\ unit-rate exponential noise, enabling a single model to be trained across heterogeneous event-stream datasets; (ii) three efficient parameterizations of the cumulative intensity that scale to long sequences; and (iii) a Transformer-based encoder for multi-dataset pretraining. On six real-world benchmarks, SurF achieves the best reported time RMSE on Earthquake, Retweet, and Taobao, and is within trial-level noise of the strongest specialist on the remaining three. Under a strict leave-one-out protocol, the held-out checkpoint beats every classical and neural-autoregressive baseline on 5/6 datasets and beats every baseline on Amazon and Earthquake, an initial step toward foundation models over asynchronous event streams.

---

## 论文详细总结（自动生成）

这是一份关于论文《SurF: A Generative Model for Multivariate Irregular Time Series Forecasting》的结构化深度分析报告：

---

### 1. 核心问题与整体含义（研究动机和背景）
论文针对的是**不规则采样的多元事件流（Multivariate Irregular Event Streams）**的生成建模问题。这类数据（如临床记录、金融订单流、地震序列）具有异步性、频率多变且观测时间本身包含语义信息的特点。
*   **研究动机**：现有的生成模型在处理此类数据时存在两大瓶颈：
    1.  **分词方案失效**：当事件间隔跨越多个数量级时，传统的离散化分词方法难以捕捉精确的时间动态。
    2.  **计算瓶颈**：神经时间点过程（Neural TPPs）通常依赖于窗口级的数值求积（Numerical Quadrature）来计算似然函数，计算成本随序列长度增加而剧增。
*   **核心目标**：建立一个高效、可扩展且具备跨数据集泛化能力（零样本预测）的异步事件流生成模型。

### 2. 方法论：核心思想与技术细节
SurF 的核心思想是将**时间重标度定理（Time Rescaling Theorem, TRT）**从一种评估工具转化为一个**可学习的双射流（Bijective Flow）**。

*   **核心思想**：TRT 指出，如果累积强度函数 $\Lambda(t)$ 是正确的，那么重标度后的事件间隔 $\Delta z_i$ 应服从独立同分布的单位速率指数分布 $Exp(1)$。SurF 利用这一特性，在观测到的事件序列与规范的指数噪声之间建立映射。
*   **关键技术细节**：
    1.  **双射映射**：前向映射 $F_\lambda$ 将事件时间扭曲为 $Exp(1)$ 噪声（用于训练）；逆向映射 $R_\lambda$ 将噪声还原为事件时间（用于采样生成）。
    2.  **三种参数化方案**：
        *   **SurF-MoE**：指数混合模型，具有闭式解，计算最快，适用于单调衰减强度。
        *   **SurF-CSB**：累积 Softplus 基函数，支持非单调强度，是正强度的通用近似器。
        *   **SurF-GLQ**：基于高斯-勒让德求积的 MLP，通过每间隔（Per-interval）求积代替窗口级求积，计算成本与时间跨度 $T$ 无关。
    3.  **Transformer 编码器**：使用 Transformer 捕捉历史依赖，并生成参数化累积强度所需的隐藏表示。
    4.  **损失函数**：结合了 SurF 似然损失（MLE）、类型预测交叉熵损失（$L_{type}$）和预测 RMSE 辅助损失（$L_{fcst}$）。
    5.  **采样算法**：使用**受保护的牛顿法（Safeguarded Newton Method）**对累积强度函数进行求逆，实现高效的时间生成。

### 3. 实验设计
*   **数据集**：涵盖了 6 个真实世界基准，时间跨度从秒级到天级：
    *   Taobao（电商）、Taxi（交通）、Retweet（社交媒体）、StackOverflow（问答社区）、Amazon（评论）、Earthquake（地震）。
*   **对比方法（Baselines）**：
    *   经典模型：MHP（Hawkes 过程）。
    *   神经 TPP：RMTPP, NHP, SAHP, THP, AttNHP, ODETPP, FullyNN, IFTPP, DTPP, NJDTPP, GRUwE。
    *   时间序列基础模型：TimesFM（231M 参数）。
*   **实验协议**：包括联合训练（Joint）、微调（Fine-tuned）以及严格的**留一法跨数据集评估（Leave-one-out Zero-shot）**。

### 4. 资源与算力
*   **硬件**：实验使用了 **NVIDIA H100 GPU**。
*   **效率对比**：
    *   在 Taxi 数据集上，SurF-MoE 的训练速度比 NJDTPP 快约 **9 倍**，推理速度快约 **17 倍**。
    *   SurF-MoE 的参数量比 TimesFM 小 125-165 倍，但推理速度更快且精度更高。
*   **超参数**：Batch size 为 512，使用了 Optuna 进行 100 次随机超参数搜索。

### 5. 实验数量与充分性
*   **实验规模**：论文在 6 个异构数据集上进行了详尽测试，总计处理了约 130 万个事件。
*   **充分性**：
    *   **消融实验**：对 Conformer 模块、损失调度方案、求积点数量 $Q$ 等进行了深入消融。
    *   **校准评估**：通过 K-S 检验（Kolmogorov-Smirnov test）验证了时间重标度后的残差是否符合 $Exp(1)$ 分布，证明了概率密度的准确性。
    *   **稳定性测试**：进行了多步预测（Multi-horizon）实验，验证了自回归生成的稳定性。
*   **客观性**：采用了严格的留一法协议来测试零样本泛化能力，这在神经 TPP 领域是首次报道，具有很高的客观价值。

### 6. 主要结论与发现
1.  **性能领先**：SurF 在 Earthquake、Retweet 和 Taobao 上刷新了时间预测的 RMSE 记录。
2.  **零样本能力**：预训练的 SurF 检查点在未见数据集上的表现优于大多数专门针对该数据集训练的基线模型，证明了 TRT 提供的规范目标有助于跨域特征学习。
3.  **计算优势**：通过消除窗口级数值积分，SurF 解决了长序列建模的计算瓶颈，且在推理时通过牛顿法求逆表现出极高的效率。
4.  **模型通用性**：单一模型架构配合不同的参数化头部，可以适应从快速衰减到复杂波动的各种强度模式。

### 7. 优点
*   **理论优雅**：将经典的 TRT 定理重新解释为生成流模型，理论支撑扎实。
*   **高效性**：参数化方案巧妙避开了昂贵的数值积分，使得模型能够扩展到超长序列。
*   **泛化性**：展示了在异步事件流上构建“基础模型”的可能性，打破了以往 TPP 模型只能单数据集训练的局限。
*   **灵活性**：提供了三种不同的强度参数化方案，用户可根据计算资源和表达能力需求进行权衡。

### 8. 不足与局限
*   **正强度假设**：理论要求强度函数必须严格大于零，虽然通过设置极小的底数（$\lambda_{floor}$）解决，但在处理具有绝对“死区时间”（如市场关闭）的进程时可能存在微小偏差。
*   **MoE 的局限性**：最快的 MoE 变体仅能表示单调衰减的强度，对于具有延迟激发或复杂波动的模式，必须切换到计算稍慢的 CSB 或 GLQ 变体。
*   **预训练规模**：虽然在 6 个数据集上表现良好，但相比视觉或语言领域，其预训练数据的多样性和规模仍有提升空间，尚未达到真正意义上的“大模型”数据量级。

---
（完）
