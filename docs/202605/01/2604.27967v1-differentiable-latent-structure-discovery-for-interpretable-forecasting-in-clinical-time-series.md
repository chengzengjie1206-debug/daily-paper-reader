---
title: Differentiable latent structure discovery for interpretable forecasting in clinical time series
authors: "Ivan Lerner, Jean Feydy, Alexandre Kalimouttou, Anita Burgun, Francis Bach"
date: 2026-04-30
pdf: "https://arxiv.org/pdf/2604.27967v1"
tags: ["query:ts"]
score: 10.0
evidence: 基于不规则电子健康档案的预测
tldr: 针对电子健康档案中不规则临床时间序列的预测难题，本文提出StructGP模型。该模型结合了多任务高斯过程与可微分结构学习，能够自动发现变量间的稀疏有向无环图（DAG）依赖关系。通过引入潜在路径（LP-StructGP）捕捉跨患者的演变模式，模型在保持不确定性量化的同时，显著提升了预测精度和可解释性，在MIMIC-IV和PhysioNet数据集上表现优异。
selection_source: fresh_fetch
motivation: 现有的临床时间序列预测方法往往在数据插值或模型可解释性之间进行权衡，难以同时实现精准预测、不确定性量化和变量间依赖关系的自动发现。
method: 提出StructGP及其扩展版LP-StructGP，利用过程卷积和可微分约束学习变量间的稀疏DAG结构，并结合潜在路径机制捕捉患者间的共性演变模式。
result: 在模拟实验中成功恢复真实图结构，在MIMIC-IV和PhysioNet真实数据集上，其预测精度和校准度均优于独立任务基准及图神经网络模型。
conclusion: 结合潜在路径的结构化过程卷积为不规则临床时间序列提供了一种可解释、可扩展且校准良好的预测方案。
---

## Abstract
Background: Timely, uncertainty-aware forecasting from irregular electronic health records (EHR) can support critical-care decisions, yet most approaches either impute to a grid or sacrifice interpretability. We introduce StructGP, a continuous-time multi-task Gaussian process that couples process convolutions with differentiable structure learning to uncover a sparse, ordered directed acyclic graph (DAG) of inter-variable dependencies while preserving principled uncertainty. We further propose LP-StructGP, which augments StructGP with latent pathways-shared, temporally shifted trajectories inferred via subject-specific coupling filters and a softmax gating mechanism-to capture cross-patient progression patterns. Both models are trained under sparsity and acyclicity constraints (augmented Lagrangian, Adam) using scalable low-rank updates. Results: In simulations, the approach reliably recovers ground-truth graphs (Structural Hamming Distance approaching 0 as cohorts grow) and pathway assignments (high Adjusted Rand Index). On a MIMIC-IV septic shock cohort (n=1,008; norepinephrine, creatinine, mean arterial pressure), StructGP improves short-horizon (6 h) forecasting over independent-task baselines (average RMSE 0.68 [95%CI: 0.63--0.74] vs. 0.88 [0.83-0.94]) and, with 15 additional inputs, markedly outperforms unstructured kernels (0.63 [0.58-0.69] vs. 3.02 [2.85-3.18]) with superior calibration (coverage 0.96 vs. 0.84). On the PhysioNet Challenge (12k patients, 41 variables), StructGP attains competitive accuracy (MAE 3.72e-2) relative to a state-of-the-art graph neural model while maintaining calibrated uncertainty. Conclusion: These results show that structured process convolutions with latent pathways deliver interpretable, scalable, and well-calibrated forecasting for irregular clinical time series.

---

## 论文详细总结（自动生成）

这是一份关于论文《Differentiable latent structure discovery for interpretable forecasting in clinical time series》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
临床决策依赖于对患者未来状态的准确预测。然而，电子健康档案（EHR）数据具有**高度不规则性**（采样频率不一）、**异质性**和**稀疏性**。现有的预测方法通常要么将数据插值到固定网格（引入偏差），要么使用缺乏可解释性和不确定性量化的黑盒模型（如深度学习）。
本研究旨在开发一种**连续时间多任务高斯过程（MTGP）模型**，既能直接处理不规则采样，又能自动发现变量间具有因果启发意义的依赖结构（DAG），并提供可靠的预测不确定性估计。

### 2. 方法论
论文提出了两种核心模型：
*   **StructGP（结构化高斯过程）：**
    *   **核心思想：** 利用**过程卷积（Process Convolutions）**构建协方差函数。将多任务输出视为潜在白噪声过程通过稀疏、下三角矩阵滤波器（ impulse response functions）卷积的结果。
    *   **结构学习：** 引入**可微分结构学习框架（类似 NOTEARS）**，通过带有无环约束（Acyclicity constraint）的连续优化来学习变量间的有向无环图（DAG）结构。
    *   **内部标准化：** 引入标准化步骤，使学习到的依赖强度不受变量原始量纲的影响。
*   **LP-StructGP（潜在路径结构化高斯过程）：**
    *   **核心思想：** 在 StructGP 基础上增加一层“潜在路径”，捕捉跨患者的共同演变模式。
    *   **关键技术：** 使用 **Softmax 门控机制**将患者概率性地分配到不同的潜在轨迹，并引入**时间偏移参数**来处理不同患者疾病进展速度的差异。
*   **算法实现：** 采用增强拉格朗日法（Augmented Lagrangian）处理约束优化，并利用 **Hilbert 空间高斯过程（HSGP）** 进行低 rank 近似，结合 Woodbury 矩阵恒等式实现大规模数据的高效训练。

### 3. 实验设计
*   **数据集：**
    1.  **模拟数据集：** 用于验证模型恢复真实图结构和潜在路径分配的能力。
    2.  **MIMIC-IV 败血症休克队列：** 包含 1,008 名患者，重点预测去甲肾上腺素（NE）、肌酐（Cr）和平均动脉压（MAP）。
    3.  **PhysioNet Challenge 数据集：** 包含约 12,000 名患者，涉及 41 个临床变量。
*   **Benchmark 与对比方法：**
    *   **基准：** 独立任务高斯过程（Independent-task GP）、无结构核函数模型。
    *   **SOTA 对比：** **GraFITi**（目前最先进的图神经网络模型）、NeuralFlow、Latent-ODE、CrossFormer 等。

### 4. 资源与算力
*   **硬件：** 使用了单张 **NVIDIA A40 GPU**。
*   **效率：** 在处理 PhysioNet 数据集（约 526 万个观测点、41 个任务）时，模型在 **45 分钟内**完成了训练（包括 100 次网格搜索迭代和多次约束优化步骤）。这展示了该方法在处理大规模临床数据时的卓越扩展性。

### 5. 实验数量与充分性
*   **实验规模：** 论文涵盖了从受控模拟到大规模真实世界数据的多维度评估。
*   **消融与变体：** 测试了不同任务维度（3 任务 vs 18 任务）、不同预测时界（6 小时短程 vs 7 天长程）以及多种模型变体（如加入滞后特征、任务特定噪声等）。
*   **客观性：** 实验采用了自助法（Bootstrap）计算 95% 置信区间，并对比了预测精度（RMSE/MAE）和校准度（Coverage），评估维度全面且客观。

### 6 主要结论与发现
*   **结构恢复：** 在模拟实验中，随着样本量增加，模型能以接近 0 的结构汉明距离（SHD）完美恢复真实图结构。
*   **预测性能：** 在 MIMIC-IV 任务中，StructGP 显著优于独立任务基准；在 18 个任务的高维设定下，结构化约束对防止模型崩溃至关重要。
*   **长程预测：** LP-StructGP 通过捕捉潜在路径，在长程预测（如 6 天）中的表现优于标准 StructGP，且校准度更高。
*   **对比 SOTA：** 在 PhysioNet 挑战赛中，StructGP 的精度与最先进的图神经网络（GraFITi）相当，但在**高置信度预测区域**表现更优，且能提供校准良好的不确定性估计。

### 7. 优点
*   **可解释性：** 自动学习变量间的 DAG 结构，能够揭示临床变量间的相互影响（如 MAP 与去甲肾上腺素的调节关系）。
*   **连续时间建模：** 天然支持不规则采样，无需预先进行数据插值或对齐。
*   **不确定性量化：** 作为高斯过程模型，它能提供原则性的预测方差，这对医疗决策中的风险评估至关重要。
*   **扩展性：** 通过低 rank 近似解决了 GP 的 $O(n^3)$ 复杂度难题，使其可应用于万级患者规模。

### 8. 不足与局限
*   **高斯似然假设：** 目前模型假设观测噪声服从高斯分布，对于离散状态（如是否插管、死亡率）或高度非高斯的数据建模能力有限。
*   **潜在路径的扩展性：** LP-StructGP 的特征维度随路径数量、任务数和基函数数量乘积增长，在极高维任务中可能面临计算瓶颈。
*   **因果解释限制：** 虽然学习的是 DAG，但由于允许不同任务有不同的时间尺度，模型捕捉的是“总效应”而非纯粹的直接因果效应，解释时需谨慎。

（完）
