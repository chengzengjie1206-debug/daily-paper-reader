---
title: "ABC: Any-Subset Autoregression via Non-Markovian Diffusion Bridges in Continuous Time and Space"
title_zh: ABC：通过连续时空中的非马尔可夫扩散桥实现任意子集自回归
authors: "Gabe Guo, Thanawat Sornwanee, Lutong Hao, Elon Litman, Stefano Ermon, Jose Blanchet"
date: 2026-04-30
pdf: "https://arxiv.org/pdf/2604.27443v1"
tags: ["query:ts"]
score: 8.0
evidence: 以不规则采样时间步等任意状态子集为条件
tldr: 针对连续时空随机过程（如视频、天气）生成中扩散模型存在的结构相似性缺失、物理时间不敏感及任意子集条件约束难等问题，本文提出ABC模型。该模型通过非马尔可夫扩散桥在连续时空中构建自回归过程，利用SDE模拟真实物理演化，支持对历史或未来任意状态子集的路径依赖条件生成，显著提升了生成动力学的物理合理性。
selection_source: fresh_fetch
motivation: 现有扩散模型在处理连续时空过程时，难以捕捉物理时间邻近状态间的结构相似性，且无法灵活处理任意观测子集的条件约束。
method: 提出一种基于非马尔可夫扩散桥的连续SDE建模方法，通过路径空间测度变换实现对任意状态子集的路径依赖条件生成，并扩展了去噪分数匹配算法。
result: 在视频生成和天气预报等多个领域的实验表明，ABC模型在生成质量和物理动力学一致性上均优于现有竞争方法。
conclusion: ABC模型通过将扩散过程与物理时间对齐，为连续时空随机过程的灵活条件生成提供了一种高效且具有理论保障的新框架。
---

## 摘要
生成以部分观测（如首帧和末帧）为条件的连续时间、连续空间随机过程（如视频、天气预报）是一项基础性挑战。现有方法（如扩散模型）存在关键局限性：(1) 从噪声到数据的演化过程无法捕捉物理时间上接近的状态之间的结构相似性，且在低步长机制下积分不稳定；(2) 注入的随机噪声对物理过程流逝的时间不敏感，导致动力学过程不正确；(3) 它们忽略了对任意状态子集（如不规则采样的时刻、未来观测）的条件化。我们提出了 ABC：通过连续时空中的非马尔可夫扩散桥实现的任意子集自回归模型。关键在于，我们使用一个连续的随机微分方程（SDE）对该过程进行建模，其时间变量和中间状态追踪真实时间和过程状态。这具有可证明的优势：(1) 生成未来状态的起点是已经接近的前一状态，而非无信息的噪声；(2) 随机噪声的注入随物理流逝时间而缩放，从而促进具有相似时间相邻状态的物理合理动力学。我们通过路径空间上的测度变换推导出 SDE 动力学，从而产生另一个优势：(3) 对状态历史和/或未来的任意子集进行路径依赖的条件化。为了学习这些动力学，我们推导出了去噪分数匹配的路径和时间依赖扩展。我们的实验表明，ABC 在包括视频生成和天气预报在内的多个领域优于竞争方法。

## Abstract
Generating continuous-time, continuous-space stochastic processes (e.g., videos, weather forecasts) conditioned on partial observations (e.g., first and last frames) is a fundamental challenge. Existing approaches, (e.g., diffusion models), suffer from key limitations: (1) noise-to-data evolution fails to capture structural similarity between states close in physical time and has unstable integration in low-step regimes; (2) random noise injected is insensitive to the physical process's time elapsed, resulting in incorrect dynamics; (3) they overlook conditioning on arbitrary subsets of states (e.g., irregularly sampled timesteps, future observations). We propose ABC: Any-Subset Autoregressive Models via Non-Markovian Diffusion Bridges in Continuous Time and Space. Crucially, we model the process with one continual SDE whose time variable and intermediate states track the real time and process states. This has provable advantages: (1) the starting point for generating future states is the already-close previous state, rather than uninformative noise; (2) random noise injection scales with physical time elapsed, encouraging physically plausible dynamics with similar time-adjacent states. We derive SDE dynamics via changes-of-measure on path space, yielding another advantage: (3) path-dependent conditioning on arbitrary subsets of the state history and/or future. To learn these dynamics, we derive a path- and time-dependent extension of denoising score matching. Our experiments show ABC's superiority to competing methods on multiple domains, including video generation and weather forecasting.

---

## 论文详细总结（自动生成）

这是一份关于论文《ABC: Any-Subset Autoregression via Non-Markovian Diffusion Bridges in Continuous Time and Space》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
论文关注的是**连续时间、连续空间随机过程**（如视频、天气预报、金融走势）的条件生成问题。
*   **现有局限**：传统的扩散模型（如 DDPM）通常采用“噪声到数据”的演化模式，忽略了物理时间上相邻状态间的结构相似性，且对物理时间的流逝不敏感，导致生成的动力学过程不符合物理规律（如视频闪烁）。
*   **条件约束难**：现有方法难以处理不规则采样的数据，或难以同时以过去和未来的观测（非因果条件）为约束进行灵活填充。
*   **研究动机**：提出一种能够追踪真实物理时间、支持任意观测子集作为条件、且具备物理合理动力学的生成框架。

### 2. 方法论：核心思想与关键技术
论文提出了 **ABC** 模型，其核心是将扩散过程与物理时间轴对齐，并引入非马尔可夫性质。
*   **核心思想**：使用一个连续的随机微分方程（SDE）来模拟数据生成，SDE 的时间变量直接对应物理时间。生成过程不再是从纯噪声开始，而是从已知的观测状态出发，通过“数据到数据”的扩散桥进行演化。
*   **关键技术细节**：
    *   **Doob h-transform（测度变换）**：通过路径空间的测度变换，将一个数据无关的基过程转化为能够匹配数据有限维分布的生成过程。
    *   **非马尔可夫分数匹配**：推导出了路径依赖（Path-dependent）的去噪分数匹配目标函数。这意味着模型在 $t$ 时刻的漂移项（Drift）不仅取决于当前状态，还取决于之前所有的观测历史。
    *   **任意子集自回归（Any-Subset AR）**：通过交叉注意力机制（Cross-Attention），模型可以接收任意数量、任意时间点（包括未来）的观测作为输入，实现了连续时间下的任意顺序生成。
    *   **时间自适应波动率**：SDE 中的噪声注入量随物理时间间隔 $\Delta t$ 缩放，确保了时间邻近状态的平滑过渡。

### 3. 实验设计
*   **数据集/场景**：
    1.  **视频生成**：CelebV-HQ（人脸视频，32帧）、Sky-Timelapse（天空延时，64帧）。
    2.  **天气预报**：SEVIR VIL（雷达回波序列）。
    3.  **玩具实验**：布朗运动路径模拟，用于验证动力学正确性。
*   **Benchmark 与对比方法**：
    *   **DDBM / PFI**：基于固定波动率计划的条件扩散桥模型（代表了忽略物理时间缩放的基线）。
    *   **DDPM / SMLD**：传统的“噪声到数据”扩散模型。
*   **评估指标**：FVD（视频一致性）、FID（单帧质量）、MAE/RMSE（天气预测误差）、CSI（关键成功指数）。

### 4. 资源与算力
论文明确记录了实验的算力消耗：
*   **硬件**：主要使用 4 块 NVIDIA A100 80GB GPU 进行基础模型训练；消融实验中的中型模型使用了 8 块 A100。
*   **训练时长**：每个标准训练任务大约耗时 **36 小时**。
*   **模型规模**：基础模型约 202M 参数（基于 DiT-B 架构），中型模型约 594M 参数。

### 5. 实验数量与充分性
*   **实验规模**：论文在两个不同风格的视频数据集和一个专业气象数据集上进行了验证，涵盖了非因果填充（Infilling）和因果预测（Forecasting）多种任务。
*   **消融实验**：
    *   对比了有无“布朗桥漂移项修正”的效果。
    *   测试了不同波动率系数 $\sigma$ 的敏感性。
    *   进行了路径依赖条件的消融（全路径 vs. 马尔可夫条件）。
    *   探讨了模型参数规模（Scaling）对性能的影响。
*   **评价**：实验设计较为充分且客观。通过控制变量法（相同的架构、相同的训练步数），公平地展示了 ABC 在处理物理时间缩放方面的理论优势。

### 6. 主要结论与发现
*   **时间敏感性至关重要**：证明了如果生成 SDE 的波动率不随物理时间缩放，模型将无法捕捉正确的路径测度，导致生成轨迹在物理上不可信。
*   **数据到数据优于噪声到数据**：ABC 在 FVD 和 FID 指标上显著优于传统的噪声初始化模型，尤其在低采样步数下表现更稳定。
*   **非马尔可夫优势**：引入路径历史信息能显著提升长序列生成的连贯性，优于仅依赖当前状态的马尔可夫基线。
*   **灵活性**：ABC 能够自然地处理不规则采样的时间序列，无需对模型架构做特殊修改。

### 7. 优点（亮点）
*   **理论严谨**：基于随机分析和测度变换提供了坚实的数学证明，统一了扩散模型与自回归模型。
*   **物理一致性**：通过将 SDE 时间与物理时间对齐，解决了视频生成中常见的闪烁和动力学扭曲问题。
*   **通用性强**：支持任意子集条件，使其在缺失值填补、视频插帧、非均匀采样预测等实际应用中极具潜力。

### 8. 不足与局限
*   **计算开销**：由于需要关注整个路径历史，随着序列长度增加，交叉注意力的计算复杂度会增加（尽管文中使用了 FlashAttention 缓解）。
*   **采样速度**：虽然比传统扩散模型更稳定，但仍依赖于 SDE 数值求解器，采样速度慢于单步生成模型（如 GAN 或变分自回归）。
*   **超参数依赖**：波动率参数 $\sigma$ 的选择对生成质量有一定影响，目前仍需针对不同数据集进行一定程度的调优。

（完）
