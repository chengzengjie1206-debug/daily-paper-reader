---
title: "ITS-Mina: A Harris Hawks Optimization-Based All-MLP Framework with Iterative Refinement and External Attention for Multivariate Time Series Forecasting"
title_zh: ITS-Mina：一种基于哈里斯鹰优化、具有迭代细化和外部注意力的全 MLP 框架，用于多变量时间序列预测
authors: "Pourya Zamanvaziri, Amirhossein Sadr, Aida Pakniyat, Dara Rahmati"
date: 2026-04-30
pdf: "https://arxiv.org/pdf/2604.27981v1"
tags: ["query:ts"]
score: 9.0
evidence: 多变量时间序列预测的全MLP框架
tldr: 本文提出ITS-Mina，一种基于全MLP架构的多元时间序列预测框架。该模型通过迭代细化机制深化计算能力，利用外部注意力模块以线性复杂度捕捉全局依赖，并引入哈里斯鹰优化算法自动调节Dropout率。在六个基准数据集上的实验表明，ITS-Mina在保持低计算成本的同时，性能达到SOTA或极具竞争力的水平。
selection_source: fresh_fetch
motivation: 针对Transformer架构计算成本高的问题，探索如何通过改进MLP模型在降低开销的同时提升对复杂时间序列依赖的建模能力。
method: 集成迭代细化残差混合器、基于学习存储单元的外部注意力机制以及用于自动超参数调优的哈里斯鹰优化算法。
result: 在六个广泛使用的基准数据集上，ITS-Mina在多个预测步长下均优于或媲美11种主流基线模型。
conclusion: ITS-Mina证明了结合迭代细化与自适应正则化的全MLP框架是实现高效且高精度多元时间序列预测的有效方案。
---

## 摘要
多变量时间序列预测在金融分析、能源管理和交通规划等众多实际应用中发挥着关键作用。虽然基于 Transformer 的架构在该任务中广受欢迎，但最近的研究表明，更简单的基于 MLP 的模型能够以显著降低的计算成本实现具有竞争力甚至更优越的性能。在本文中，我们提出了 ITS-Mina，这是一种用于多变量时间序列预测的新型全 MLP 框架，它集成了三项关键创新：(1) 一种迭代细化机制，通过重复应用共享参数的残差混合器堆栈来逐步增强时间表示，在不增加不同参数数量的情况下有效加深了模型的计算能力；(2) 一个外部注意力模块，用可学习的记忆单元取代传统的自注意力，以线性计算复杂度捕捉跨样本的全局依赖关系；(3) 一种用于自动调整 Dropout 率的哈里斯鹰优化（HHO）算法，实现了针对每个数据集量身定制的自适应正则化。在六个广泛使用的基准数据集上进行的广泛实验表明，与 11 个基准模型相比，ITS-Mina 在多个预测周期内均实现了最先进或极具竞争力的性能。

## Abstract
Multivariate time series forecasting plays a pivotal role in numerous real-world applications, including financial analysis, energy management, and traffic planning. While Transformer-based architectures have gained popularity for this task, recent studies reveal that simpler MLP-based models can achieve competitive or superior performance with significantly reduced computational cost. In this paper, we propose ITS-Mina, a novel all-MLP framework for multivariate time series forecasting that integrates three key innovations: (1) an iterative refinement mechanism that progressively enhances temporal representations by repeatedly applying a shared-parameter residual mixer stack, effectively deepening the model's computational capacity without multiplying the number of distinct parameters; (2) an external attention module that replaces traditional self-attention with learnable memory units, capturing cross-sample global dependencies at linear computational complexity; and (3) a Harris Hawks Optimization (HHO) algorithm for automatic dropout rate tuning, enabling adaptive regularization tailored to each dataset. Extensive experiments on six widely-used benchmark datasets demonstrate that ITS-Mina achieves state-of-the-art or highly competitive performance compared to eleven baseline models across multiple forecasting horizons.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **ITS-Mina** 的新型全 MLP（多层感知机）框架，旨在解决多变量时间序列预测（MTSF）中的效率与精度平衡问题。以下是对该论文的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：尽管 Transformer 架构在时间序列预测中占据主导地位，但其二次方计算复杂度和高资源消耗限制了其应用。虽然最近的 MLP 模型（如 DLinear, TSMixer）显示出极具竞争力的性能，但它们仍存在三个局限：
    1.  **缺乏迭代能力**：通常采用单次前向传播，难以通过逐步细化来处理复杂模式。
    2.  **缺乏全局上下文**：仅依赖局部特征交互，无法捕捉跨样本的全局分布规律。
    3.  **超参数调优困难**：关键正则化参数（如 Dropout 率）通常依赖耗时的手动网格搜索。
*   **整体含义**：论文试图证明，通过引入迭代细化、线性复杂度的外部注意力和启发式优化算法，全 MLP 架构可以在保持轻量级的同时，达到甚至超越最先进的 Transformer 模型的预测精度。

### 2. 论文提出的方法论：ITS-Mina 框架
ITS-Mina 包含四个主要阶段和一种自动优化机制：
*   **核心思想**：利用参数共享的迭代循环加深网络“逻辑深度”，并结合外部记忆单元捕捉全局特征。
*   **关键技术细节**：
    1.  **迭代细化（Iterative Refinement）**：引入一个深度为 $M$ 的残差混合器堆栈（Mixer Stack），通过**权重共享**的方式重复应用 $N$ 次。这在不增加参数量的前提下，通过循环迭代增强了模型对时间表示的提取能力。
    2.  **外部注意力（External Attention）**：用两个可学习的记忆矩阵（$E$ 和 $V$）取代自注意力。它计算输入与外部存储单元的关联，复杂度仅为 $O(LCS)$（$L$ 为序列长度，$S$ 为存储单元大小），实现了线性复杂度并能捕捉数据集级别的全局模式。
    3.  **哈里斯鹰优化（HHO）**：这是一种受鹰群捕食行为启发的元启发式算法。论文将其用于**自动搜索最优 Dropout 率**，通过模拟“逃逸能量”在探索和开发之间取得平衡，从而实现自适应正则化。
    4.  **混合架构**：结合了实例归一化（Instance Normalization）、时间混合（Time-mixing）和特征混合（Feature-mixing）层。

### 3. 实验设计
*   **数据集**：使用了 6 个广泛认可的基准数据集：Traffic（交通）、Electricity（电力）以及 ETT 系列（ETTh1, ETTh2, ETTm1, ETTm2）。
*   **预测步长（Horizons）**：涵盖了 96, 192, 336, 720 四个不同的预测长度。
*   **对比方法（Baselines）**：共对比了 11 种模型，包括：
    *   **Transformer 类**：Informer, Autoformer, FEDformer, Crossformer, iTransformer, PatchTST。
    *   **MLP 类**：DLinear, CI-TSMixer, PatchMixer。
    *   **卷积/其他类**：TimesNet, MICN。

### 4. 资源与算力
*   **硬件设备**：实验在 **NVIDIA RTX 3090 GPU** 上进行。
*   **训练细节**：使用了 Adam 优化器，最大训练轮数为 100 轮，并设有早停机制（Patience=5）。
*   **算力说明**：虽然未详细列出每个模型的总训练时长，但论文强调了其 MLP 架构和外部注意力机制具有较低的计算复杂度，且 HHO 优化过程每组参数仅需训练 10 个 epoch 即可评估适应度。

### 5. 实验数量与充分性
*   **实验规模**：在 6 个数据集、4 个预测步长下进行了全面测试，共计 24 组核心实验。
*   **评估指标**：采用了均方误差（MSE）和平均绝对误差（MAE）。
*   **充分性与公平性**：
    *   对比了近年来几乎所有主流的 SOTA 模型。
    *   使用了 Optuna 框架进行结构超参数搜索，并用 HHO 优化 Dropout，确保了 ITS-Mina 处于最优状态。
    *   基准模型的结果多引用自原始论文或使用官方推荐参数复现，保证了对比的客观性。

### 6. 主要结论与发现
*   **性能领先**：ITS-Mina 在绝大多数数据集和预测步长组合中均取得了**最优（SOTA）的 MSE 结果**。
*   **MAE 表现**：在 21/24 的实验设置中排名第一，仅在 ETTm2 的部分步长下略逊于 MICN。
*   **架构优势**：实验证明，即使不使用复杂的自注意力机制，通过迭代共享参数和外部记忆，MLP 也能有效处理长程依赖和多变量相关性。
*   **自动化有效性**：HHO 算法能够为不同数据集找到最合适的 Dropout 率，显著提升了模型的泛化能力。

### 7. 优点（亮点）
*   **参数高效性**：通过权重共享的迭代机制，在不爆炸式增加参数量的情况下加深了模型。
*   **线性复杂度**：外部注意力解决了长序列预测中的计算瓶颈。
*   **自适应正则化**：引入启发式算法（HHO）解决超参数调优痛点，具有较强的工程实用价值。
*   **鲁棒性**：在不同维度（从 7 个特征到 862 个特征）的数据集上表现均非常稳定。

### 8. 不足与局限
*   **迭代开销**：虽然参数量没增加，但 $N$ 次迭代会增加前向传播的计算时间（推理延迟）。
*   **静态迭代次数**：目前的迭代次数 $N$ 是固定的，缺乏根据输入复杂度动态调整（如自适应停止）的机制。
*   **辅助信息利用**：目前尚未探讨如何整合静态特征（如地理位置）或未来已知协变量（如节假日）。
*   **应用范围**：实验主要集中在长时预测，尚未在异常检测或分类等其他时间序列任务中验证。

（完）
