---
title: "NPMixer: Hierarchical Neighboring Patch Mixing for Time Series Forecasting"
title_zh: NPMixer：用于时间序列预测的分层相邻补丁混合
authors: "Jung Min Choi, Vijaya Krishna Yalavarthi, Lars Schmidt-Thieme"
date: 2026-05-08
pdf: "https://arxiv.org/pdf/2605.07476v1"
tags: ["query:ts"]
score: 8.0
evidence: 分层混合的多变量时间序列预测
tldr: 针对多变量时间序列预测中局部动态和全局依赖建模的挑战，本文提出NPMixer。该模型采用分层架构，通过可学习平稳小波变换（LSWT）自适应分解趋势与细节，并利用邻域混合块（NMB）在非重叠补丁上通过多层感知机（MLP）捕捉多尺度时间依赖。实验证明其在多个基准数据集上显著优于现有最先进模型。
selection_source: fresh_fetch
motivation: 旨在解决多变量时间序列预测中复杂的局部时间动态和跨变量全局依赖难以同时有效建模的问题。
method: 提出一种包含可学习小波变换的分层架构，通过邻域混合块和通道混合编码器分别捕捉多尺度时间模式与变量间相关性。
result: "在七个基准数据集的28个实验设置中，NPMixer在20个设置（71.4%）的均方误差（MSE）指标上达到了最优性能。"
conclusion: NPMixer通过结合自适应信号分解与分层补丁混合机制，为多变量时间序列预测提供了一种高效且性能卓越的解决方案。
---

## 摘要
由于局部时间动态的复杂性以及多个变量之间的全局依赖关系，多元时间序列预测仍然是一项挑战。在本文中，我们提出了 Neighboring Patching Mixer (NPMixer)，这是一种分层架构，其特点是具有可学习的平稳小波变换 (Learnable Stationary Wavelet Transform)，能够以数据依赖的方式自适应地学习滤波器系数，从而将信号分解为趋势和细节分量。我们的框架引入了一个相邻混合器块 (Neighboring Mixer Block)，通过在一系列非重叠补丁 (patches) 上运行的分层 MLP 层来捕捉局部时间动态。具体而言，该混合器块利用 MLP 学习这些补丁内部和之间的时序模式，从而扩大感受野以捕捉多尺度依赖关系。通道混合编码器 (Channel-Mixing Encoder) 被应用于高频分量，以学习通道间的相关性，同时保持底层全局趋势的稳定性。在七个基准数据集上的广泛实验表明，NPMixer 一致地优于现有最先进的模型，在 28 个评估的 MSE 实验设置中有 20 个（71.4%）取得了更好的性能。

## Abstract
Multivariate time series forecasting remains a challenge due to the complexity of local temporal dynamics and global dependencies across multiple variables.   In this paper, we propose \textbf{N}eighboring \textbf{P}atching \textbf{Mixer} (\textbf{NPMixer}), a hierarchical architecture featuring a Learnable Stationary Wavelet Transform that adaptively learns filter coefficients to decompose signals into trend and detail components in a data-dependent manner.   Our framework introduces a Neighboring Mixer Block that captures local temporal dynamics through a series of hierarchical MLP layers operating on non-overlapping patches.   Specifically, the mixer block utilizes MLPs to learn temporal patterns within and across these patches, expanding the receptive field to capture multi-scale dependencies.   A Channel-Mixing Encoder is applied to high-frequency components to learn channel correlations while preserving the stability of the underlying global trend.   Extensive experiments on seven benchmark datasets demonstrate that NPMixer consistently outperforms state-of-the-art models, achieving better performance in 20 out of 28 ($71.4\%$) evaluated experimental setups for MSE.

---

## 论文详细总结（自动生成）

### NPMixer: 用于时间序列预测的分层相邻补丁混合论文总结

#### 1. 核心问题与研究动机
论文针对多元时间序列（MTS）预测中的两大核心挑战：**复杂的局部时间动态**和**跨变量的全局依赖关系**。
*   **研究背景**：现有的 Patching（补丁）技术多采用重叠滑动窗口，虽然能捕捉局部语义，但带来了巨大的计算开销和冗余信息。
*   **核心问题**：如何在降低计算冗余的同时，有效地在不同尺度上捕捉时间序列的局部细节与全局趋势，并处理多变量间的相关性。

#### 2. 方法论
NPMixer 提出了一种结合频域分解与时域分层混合的架构，主要包含以下三个核心组件：
*   **可学习平稳小波变换 (LSWT)**：
    *   不同于固定基函数的传统小波，LSWT 将滤波器系数参数化为可学习张量，使模型能根据数据自适应地将信号分解为**趋势（低频）**和**细节（高频）**分量。
    *   采用平稳小波变换（SWT）避免了下采样，保持了序列长度一致性和平移不变性。
*   **通道混合编码器 (Channel-Mixing Encoder)**：
    *   仅应用于高频细节分量。借鉴 iTransformer 的思想，将每个变量的整个序列视为一个 Token，利用自注意力机制建模变量间的相关性。
*   **相邻混合器块 (Neighboring Mixer Block, NMB)**：
    *   **非重叠补丁化**：将序列划分为不重叠的补丁。
    *   **分层配对策略**：采用“分组-混合-溶解（Group-Mix-Dissolve）”策略。在每一层中，相邻的补丁或块成对通过 MLP 进行混合，递归地扩大感受野，从局部细节逐步过渡到全局依赖。
    *   **非对称更新**：设计了方向性偏置的残差连接，以符合时间序列预测的因果/时序逻辑。

#### 3. 实验设计
*   **数据集**：使用了 7 个标准基准数据集，涵盖电力（ETTh1, ETTh2, ETTm1, ETTm2, ECL）、天气（Weather）和交通（Traffic）领域。
*   **预测任务**：长程时间序列预测，预测步长 $H \in \{96, 192, 336, 720\}$。
*   **对比方法 (Baselines)**：对比了 6 种最先进的模型，包括 SimpleTM、TimeXer、CrossGNN、iTransformer、PatchTST 和 TimeMixer。
*   **评估指标**：均方误差 (MSE) 和平均绝对误差 (MAE)。

#### 4. 资源与算力
*   **算力说明**：论文**未明确列出**具体的 GPU 型号、数量或总训练时长。
*   **效率分析**：文中提到模型针对现代 GPU 的并行执行进行了优化。在 ETTm1 数据集上，尽管参数量比 SimpleTM 多 39%，但训练速度快了约 **2.7 倍**（0.1518 vs 0.4210 s/iter），证明了非重叠补丁和 MLP 架构的高效性。
*   **超参优化**：使用了 Optuna 框架进行自动化的超参数搜索。

#### 5. 实验数量与充分性
*   **实验规模**：在 7 个数据集、4 个预测步长下进行了共 **28 组**标准实验。
*   **消融实验**：针对 LSWT、可学习性、相邻混合器、通道混合编码器分别做了移除测试，验证了各组件的必要性。
*   **回顾窗口分析**：测试了不同输入长度 $L \in \{96, 192, 336, 512\}$ 对模型的影响。
*   **充分性评价**：实验设计涵盖了多维度对比，使用了多个随机种子取平均值，并进行了回顾窗口调优以确保公平性，实验结果具有较高的客观性和说服力。

#### 6. 主要结论与发现
*   **性能领先**：NPMixer 在 20/28 (71.4%) 的 MSE 测试设置中达到最优，显著优于现有 SOTA 模型。
*   **分层混合的有效性**：递归的补丁聚合能比密集的滑动窗口更高效地捕捉多尺度动态。
*   **频域分解的作用**：LSWT 提供的趋势与细节分离有助于模型在不同频率分支上采用专门的处理策略（如仅对细节分支进行通道混合）。
*   **长窗口优势**：随着回顾窗口 $L$ 的增加，NPMixer 的性能提升明显，表明其分层结构能很好地利用长历史信息。

#### 7. 优点
*   **高效性**：采用非重叠补丁和分层 MLP，计算速度快，适合大规模部署。
*   **自适应性**：可学习的小波滤波器能根据不同领域的数据特性自动调整分解基准。
*   **结构简洁**：相比复杂的 Transformer 变体，基于 MLP 的分层混合逻辑清晰且易于实现。

#### 8. 不足与局限
*   **长度限制**：分层聚合依赖于指数级的块大小（$2^{k-1}P$），对输入长度有一定约束，通常需要补零（Zero-padding），这可能在序列边缘引入分布偏移。
*   **超参敏感**：模型性能对初始补丁大小 $P$ 较为敏感，若 $P$ 与数据的潜在周期不匹配，可能影响局部语义提取。
*   **静态推理**：虽然小波滤波器在训练时是可学习的，但在推理阶段是静态的，可能难以应对非平稳数据的概念漂移。

（完）
