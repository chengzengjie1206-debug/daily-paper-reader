---
title: "CastFlow: Learning Role-Specialized Agentic Workflows for Time Series Forecasting"
title_zh: CastFlow：学习用于时间序列预测的角色专业化智能体工作流
authors: "Bokai Pan, Mingyue Cheng, Zhiding Liu, Shuo Yu, Xiaoyu Tao, Yuchong Wu, Qi Liu, Defu Lian, Enhong Chen"
date: 2026-04-30
pdf: "https://arxiv.org/pdf/2604.27840v2"
tags: ["query:ts"]
score: 8.0
evidence: 使用大语言模型的动态代理预测框架
tldr: CastFlow是一个针对时间序列预测的动态智能体工作流框架。针对现有LLM预测方法在模式提取和上下文获取方面的局限性，它通过规划、行动、预测和反思四个阶段，结合记忆模块和多视图工具包，实现了迭代式预测优化。该框架采用角色专业化设计，结合通用推理与微调后的领域特定LLM，并通过SFT和RLVR两阶段训练，显著提升了预测的准确性和适应性。
selection_source: fresh_fetch
motivation: 现有的基于LLM的时间序列预测方法多采用静态生成范式，限制了复杂时间模式的提取和预测结果的迭代优化。
method: 提出了CastFlow框架，通过智能体工作流整合规划、行动、预测与反思，并利用角色专业化设计结合通用LLM推理与领域特定LLM的数值预测。
result: 在多个数据集上的广泛实验表明，CastFlow在整体预测性能上优于现有的强基准模型。
conclusion: CastFlow通过引入动态智能体工作流和角色专业化设计，为实现更具适应性和准确性的时间序列预测提供了有效路径。
---

## 摘要
最近，大语言模型（LLMs）在时间序列预测中展现出巨大潜力。然而，大多数现有的基于 LLM 的预测方法仍遵循静态生成范式，即通过单次传递直接将历史观测值映射为未来值。在这种范式下，预测受到有限的时间模式提取、单轮上下文特征获取、单次预测生成以及缺乏集成预测支持的限制。为了解决这些局限性，本研究提出了 CastFlow，这是一个动态智能体预测框架，支持多视图时间模式提取、多轮上下文特征获取、迭代预测细化以及结合集成预测的预测。首先，CastFlow 将预测过程组织为规划、行动、预测和反思，建立了一个智能体工作流。其次，该工作流由一个检索先前经验的记忆模块和一个构建诊断证据并提供可靠集成预测基线的多元视图工具包提供支持。第三，CastFlow 采用了角色专业化设计，将通用推理与专业数值预测相结合。在这种设计下，冻结的 LLM 保留通用推理能力，而经过微调的领域特定 LLM 则基于集成预测基线（而非从零开始）执行证据引导的数值预测。为了优化微调后的领域特定 LLM，我们进一步开发了一种结合监督微调（SFT）和带可验证奖励的强化学习（RLVR）的两阶段面向工作流的训练方法。为了评估 CastFlow 的有效性，我们在多种数据集上进行了广泛实验，结果表明其在与强基线的对比中取得了优异的整体结果。我们希望这项工作能为实现更具适应性和准确性的时间序列预测迈出一步。

## Abstract
Recently, large language models (LLMs) have shown great promise in time series forecasting. However, most existing LLM-based forecasting methods still follow a static generative paradigm that directly maps historical observations to future values in a single pass. Under this paradigm, forecasting is constrained by limited temporal pattern extraction, single-round acquisition of contextual features, one-shot forecast generation, and lack of support from ensemble forecasts. To address these limitations, in this work, we propose CastFlow, a dynamic agentic forecasting framework that enables multi-view temporal pattern extraction, multi-round contextual features acquisition, iterative forecast refinement, and forecasting with ensemble forecasts. First, CastFlow organizes the forecasting process into planning, action, forecasting, and reflection, establishing an agentic workflow. Second, this workflow is supported by a memory module that retrieves prior experience and a multi-view toolkit that constructs diagnostic evidence and provides a reliable ensemble forecast baseline. Third, CastFlow adopts a role-specialized design that combines general-purpose reasoning with specialized numerical forecasting. Under this design, a frozen LLM preserves general-purpose reasoning, while a fine-tuned domain-specific LLM performs evidence-guided numerical forecasting based on the ensemble forecast baseline, rather than from scratch. To optimize a fine-tuned domain-specific LLM, we further develop a two-stage workflow-oriented training that combines supervised fine-tuning (SFT) and reinforcement learning with verifiable rewards (RLVR). To evaluate the effectiveness of CastFlow, we conduct extensive experiments on diverse datasets and show that it achieves superior overall results against strong baselines. We hope that this work can serve as a step toward more adaptive and accurate time series forecasting.

---

## 论文详细总结（自动生成）

这是一份关于论文《CastFlow: Learning Role-Specialized Agentic Workflows for Time Series Forecasting》的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的基于大语言模型（LLM）的时间序列预测方法大多遵循“静态生成范式”，即单次将历史观测映射为未来值。
*   **研究动机**：
    *   **能力受限**：静态范式在复杂模式提取、多轮上下文获取、迭代细化以及利用集成预测方面存在局限。
    *   **精度与推理的矛盾**：通用 LLM 擅长推理但数值精度不足，而微调模型虽提升了精度却往往损害了通用推理和泛化能力。
    *   **缺乏动态性**：预测过程缺乏像人类专家那样的“规划-执行-反思”循环。

### 2. 方法论：CastFlow 框架
CastFlow 将预测任务重新定义为一个由工作流驱动的动态决策过程，核心思想是**角色专业化（Role-Specialization）**。

*   **核心架构**：
    1.  **规划（Planning）**：使用**冻结的通用 LLM**（如 Grok 4）根据历史数据和检索到的经验制定工具调用计划。
    2.  **行动（Action）**：调用**多视图工具包**。该工具包包含：
        *   *基础锚定器*：通过检索历史相似模式构建集成预测基线（Ensemble Baseline）。
        *   *统计与频谱分析器*：提取均值、方差、频谱熵等宏观特征。
        *   *动态监测器*：识别趋势、拐点及多变量相关性。
        *   *残差诊断器*：分析基线模型的系统性偏差。
    3.  **预测（Forecasting）**：使用**微调后的领域特定 LLM**（如 Qwen3-4B）。它不从零生成数值，而是基于集成基线，结合工具包提供的证据进行“证据引导的修正”。
    4.  **反思（Reflection）**：由冻结 LLM 检查输出格式和逻辑一致性，不合格则触发重试。
*   **关键技术**：
    *   **记忆模块**：存储并检索成功的预测轨迹和策略。
    *   **两阶段训练**：
        *   *SFT（监督微调）*：对齐推理信号与数值预测。
        *   *RLVR（带可验证奖励的强化学习）*：利用 GRPO 算法，根据预测精度（MSE/MAE）和格式正确性提供奖励，优化预测策略。

### 3. 实验设计
*   **数据集**：涵盖 10 个真实世界数据集，包括电力市场（BE, DE, FR, NP, PJM）、电网（ETTh, ETTm）、可再生能源（风能 WP, 太阳能 SP）和径流预测（MOPEX）。
*   **对比方法（21 种基准）**：
    *   统计模型：Prophet, ARIMA。
    *   机器学习：XGBoost, LightGBM。
    *   深度学习：Autoformer, PatchTST, iTransformer, ConvTimeNet 等。
    *   时序大模型：Chronos, TimesFM, Sundial。
    *   基于 LLM/智能体的方法：Time-LLM, Time-R1, AlphaCast 等。
*   **评价指标**：均方误差（MSE）和平均绝对误差（MAE）。

### 4. 资源与算力
*   **硬件**：使用了 **2 台 NVIDIA A800 GPU**。
*   **模型配置**：
    *   推理/规划：Grok 4（冻结）。
    *   数值预测：Qwen3-4B（可训练）。
*   **训练时长**：SFT 阶段进行 1 个 epoch 的跨域联合训练；RLVR 阶段进行 3 个 epoch 的训练。

### 5. 实验数量与充分性
*   **实验规模**：在 10 个不同领域、不同频率（15min 到 1day）的数据集上进行了测试，涵盖了短期和长期预测场景。
*   **消融实验**：非常充分。分别验证了工具包、记忆模块、反思机制、不同工具类别、不同训练阶段（SFT vs RLVR）以及不同底座模型（1.7B 到 8B）对结果的影响。
*   **客观性**：采用了严格的 7:1:2 比例划分数据集，并确保所有对比方法在相同的输入信息（如协变量）下运行，评价过程较为公正。

### 6. 主要结论与发现
*   **性能领先**：CastFlow 在绝大多数数据集上优于所有基准模型，尤其在长期预测和复杂波动场景下表现突出。
*   **角色专业化的有效性**：将“通用推理”与“数值修正”分离，既保留了逻辑能力，又突破了 LLM 的数值精度瓶颈。
*   **强化学习的作用**：RLVR 阶段显著提升了模型利用诊断证据进行精准修正的能力，效果远超单纯的 SFT。
*   **基线修正优于从零生成**：基于集成基线进行微调，比直接让 LLM 生成数值更加稳定且准确。

### 7. 优点与亮点
*   **范式创新**：从“单次映射”转向“动态工作流”，引入了类似人类专家的诊断修正逻辑。
*   **工具集成**：多视图工具包将时序分析的专业知识（如频谱分析、残差诊断）有机整合进 LLM 流程。
*   **可解释性**：通过推理链（Reasoning Trace），用户可以清晰看到模型为何对预测值进行了上调或下调。
*   **训练策略**：结合了 GRPO 强化学习，解决了时序预测中奖励难以量化和格式易崩溃的问题。

### 8. 不足与局限
*   **推理成本**：由于涉及多轮 LLM 调用、工具执行和迭代反思，其推理延迟和计算开销显著高于传统的端到端深度学习模型。
*   **局部权衡**：在某些特定数据集（如 PJM）上，由于跨域联合训练的泛化压力，表现略逊于针对该领域专门优化的模型。
*   **工具依赖**：预测精度高度依赖于工具包输出的质量，若工具提取的特征存在噪声，可能会干扰预测决策。

（完）
