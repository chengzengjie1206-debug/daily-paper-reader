---
title: "MSMixer: Learned Multi-Scale Temporal Mixing with Complementary Linear Shortcut for Long-Term Time Series Forecasting"
title_zh: MSMixer：用于长期时间序列预测的带有互补线性捷径的可学习多尺度时间混合模型
authors: Ahmed Cherif
date: 2026-05-04
pdf: "https://arxiv.org/pdf/2605.02689v1"
tags: ["query:ts"]
score: 10.0
evidence: 用于长期时间序列预测的多尺度时间混合
tldr: 针对长期时间序列预测中单一分辨率难以捕捉多尺度模式的问题，本文提出MSMixer。这是一种轻量级多尺度MLP架构，通过三个并行分支处理不同下采样因子，并结合可学习的门控机制和DLinear残差连接。在保持低参数量和线性复杂度的同时，MSMixer在多个基准数据集上显著优于现有轻量级模型，并能与Transformer模型媲美。
selection_source: fresh_fetch
motivation: 现有的轻量级MLP模型通常在单一时间分辨率下运行，难以同时捕捉快速波动、中程周期和长期宏观趋势。
method: 提出一种包含三个并行多尺度分支、可学习Softmax门控权重以及DLinear互补快捷连接的通道独立型MLP架构。
result: 在ETT基准测试中，MSMixer以仅112K的参数量实现了最低的平均MSE（0.357），优于DLinear和NLinear，且在性能上可与参数量大5倍的PatchTST竞争。
conclusion: MSMixer证明了通过多尺度特征融合与线性快捷连接的结合，轻量级模型也能在长期时间序列预测中达到顶尖性能。
---

## 摘要
长期时间序列预测要求模型能够从固定的回顾窗口中同时捕捉快速振荡、中程周期性和缓慢演变的宏观趋势。现有的轻量级基于 MLP 的模型通常在单一时间分辨率上运行，限制了它们显式建模多尺度模式的能力。我们提出了 MSMixer，这是一种通道独立的多尺度 MLP 架构，通过三项互补创新解决了这一局限性：(i) 在下采样因子为 {1x, 4x, 16x} 处设有三个带有独立 MLP 块的并行尺度分支；(ii) 一个可学习的 softmax 门控，用于动态加权分支输出；(iii) 一个提供全窗口趋势和季节性上下文的 DLinear 互补捷径。在 H=96 时，MSMixer 仅包含 112K 个参数，并以 O(T) 的复杂度运行。在四个 ETT 基准测试集上，采用标准的时间顺序划分和三个随机种子进行评估，MSMixer 在轻量级模型中实现了最低的平均均方误差 (MSE, 0.357)，优于 DLinear (0.386, -7.4%) 和 NLinear (0.365, -2.1%)，在 16 种配置中胜出 12 种。与文献中的五个基于 Transformer 的基准模型相比，MSMixer 在 16 种配置中的 9 种里实现了最佳或次佳的 MSE，同时参数量比 PatchTST 少 5 倍。消融实验和敏感性分析证实了多尺度分支和 DLinear 捷径的互补贡献。

## Abstract
Long-term time series forecasting requires models that simultaneously capture rapid oscillations, medium-range periodicities, and slowly evolving macro-trends from a fixed look-back window. Existing lightweight MLP-based models typically operate on a single temporal resolution, limiting their ability to explicitly model patterns at multiple scales. We propose MSMixer, a channel-independent multi-scale MLP architecture that addresses this limitation through three complementary innovations: (i) three parallel scale branches at down-sample factors {1x, 4x, 16x} with independent MLP blocks, (ii) a learnable softmax gate that dynamically weighs branch outputs, and (iii) a DLinear complementary shortcut that provides full-window trend and seasonality context. MSMixer contains only 112K parameters at H=96 and runs at O(T) complexity. Evaluated on four ETT benchmarks with standard chronological splits and three random seeds, MSMixer achieves the lowest average MSE (0.357) among lightweight models, outperforming DLinear (0.386, -7.4%) and NLinear (0.365, -2.1%), winning 12 of 16 configurations. Against five Transformer-based baselines from the literature, MSMixer achieves best or second-best MSE in 9 of 16 configurations while using 5x fewer parameters than PatchTST. Ablation and sensitivity analyses confirm the complementary contributions of the multi-scale branches and the DLinear shortcut.

---

## 论文详细总结（自动生成）

以下是对论文《MSMixer: Learned Multi-Scale Temporal Mixing with Complementary Linear Shortcut for Long-Term Time Series Forecasting》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
长期时间序列预测（LTSF）的核心挑战在于如何从有限的回顾窗口中同时捕捉**多尺度模式**，包括快速的随机波动、中程的周期性以及缓慢演变的宏观趋势。
*   **现有局限：** 现有的轻量级模型（如 DLinear, NLinear）虽然计算效率高，但通常在单一时间分辨率下运行，难以显式建模多尺度特征。
*   **研究目标：** 提出一种既能保持 MLP 架构的轻量级与线性复杂度（$O(T)$），又能通过多尺度分支和动态融合机制提升预测精度的模型。

### 2. 论文提出的方法论：MSMixer
MSMixer 采用通道独立（Channel-Independent）策略，其核心架构由三个关键组件组成：
*   **并行多尺度分支（Parallel Multi-Scale Branches）：**
    *   模型设置了三个并行分支，分别对应不同的下采样因子：1x（原始分辨率）、4x 和 16x。
    *   每个分支通过平均池化进行下采样，随后进入独立的 MLP 块处理特定频率的特征。
*   **可学习的门控机制（Learnable Softmax Gating）：**
    *   为了融合不同尺度的输出，模型引入了一个可学习的权重向量，通过 Softmax 归一化后对三个分支的预测结果进行加权求和。这允许模型根据数据特征自动学习各尺度的重要性。
*   **DLinear 互补捷径（Complementary Shortcut）：**
    *   在多尺度分支之外，并联了一个 DLinear 模块作为残差连接。DLinear 通过移动平均将序列分解为趋势和季节性分量，为模型提供全窗口的全局上下文信息，弥补下采样可能带来的信息损失。

### 3. 实验设计
*   **数据集：** 使用了电力变压器数据集（ETT）的四个子集：ETTh1, ETTh2, ETTm1, ETTm2。
*   **实验场景：** 长期预测任务，回顾窗口 $L=96$，预测长度 $H \in \{96, 192, 336, 720\}$。
*   **Benchmark 与对比方法：**
    *   **轻量级模型：** DLinear, NLinear。
    *   **Transformer 类模型：** PatchTST, iTransformer, Crossformer, LTSF-Linear, FEDformer。
*   **评估指标：** 均方误差（MSE）和平均绝对误差（MAE），采用标准的时间顺序划分，并使用三个随机种子取平均值以确保稳定性。

### 4. 资源与算力
*   **算力说明：** 论文中**未明确说明**具体的 GPU 型号、数量或详细的训练时长。
*   **模型规模：** 论文强调了其轻量性，在 $H=96$ 时参数量仅为 **112K**，比 PatchTST 少约 5 倍。

### 5. 实验数量与充分性
*   **实验规模：** 涵盖了 4 个数据集、4 种预测长度，共 16 种实验配置。
*   **充分性：** 
    *   每项实验均运行了 3 个随机种子，增加了结果的可信度。
    *   进行了**消融实验**，分别验证了多尺度分支、门控机制和 DLinear 捷径对性能的贡献。
    *   进行了**敏感性分析**，探讨了不同下采样因子组合对模型效果的影响。
*   **客观性：** 对比了当前最先进的 Transformer 模型，证明了轻量级模型在特定场景下具有竞争力的表现。

### 6. 主要结论与发现
*   **性能领先：** MSMixer 在轻量级模型中表现最优，平均 MSE 为 0.357，比 DLinear 提升了 7.4%，比 NLinear 提升了 2.1%。
*   **媲美复杂模型：** 尽管参数量极小，MSMixer 在 16 种配置中的 9 种里达到了与 PatchTST 等重型 Transformer 模型相当甚至更好的水平。
*   **多尺度的重要性：** 实验证明，单一尺度往往不足以覆盖复杂的时间动态，多尺度融合能显著降低预测误差。

### 7. 优点
*   **高效性：** 保持了线性计算复杂度 $O(T)$，适合资源受限的实时预测场景。
*   **架构创新：** 巧妙地结合了多尺度 MLP 分支与 DLinear 的分解能力，通过门控机制实现了特征的动态选择。
*   **参数经济：** 以极小的参数代价实现了高精度的预测，证明了“模型深度和复杂度并非 LTSF 的唯一出路”。

### 8. 不足与局限
*   **数据集覆盖面：** 实验主要集中在 ETT 系列数据集上，缺乏在交通、天气或金融等其他领域数据集上的广泛验证。
*   **超参数敏感性：** 下采样因子的选择（如 4x, 16x）可能是启发式的，对于不同特性的序列，可能需要手动调整这些因子以达到最优效果。
*   **通道独立性限制：** 采用通道独立策略意味着模型无法显式建模变量之间的相关性（Multivariate correlation），这在某些强耦合的多变量预测任务中可能是个短板。

（完）
