---
title: Missingness-aware Data Imputation via AI-powered Bayesian Generative Modeling
title_zh: 基于人工智能驱动的贝叶斯生成建模的缺失感知数据插补
authors: Qiao Liu
date: 2026-05-03
pdf: "https://arxiv.org/pdf/2605.01676v1"
tags: ["query:ts"]
score: 9.0
evidence: 通过贝叶斯生成模型进行缺失感知的数据插补
tldr: 针对现代数据科学中缺失数据填补的挑战，本文提出MissBGM。该方法结合了神经网络的灵活性与贝叶斯推理的严谨性，通过显式建模数据生成和缺失机制，不仅提供点估计，还能量化填补的不确定性。研究开发了随机优化框架并证明了收敛性，实验表明其在多种设置下优于传统及深度学习方法，是可扩展的原则性解决方案。
selection_source: fresh_fetch
motivation: 现有的缺失数据填补方法往往只关注点估计或隐含处理缺失机制，缺乏对不确定性的有效量化。
method: 提出MissBGM框架，利用贝叶斯生成模型显式联合建模数据生成与缺失过程，并通过交替更新的随机优化算法进行求解。
result: 实验结果显示，MissBGM在填补精度和不确定性评估方面均显著优于传统填补器及最新的神经网络模型。
conclusion: MissBGM为现代缺失数据填补提供了一个兼具统计严谨性、表达灵活性和良好扩展性的原则性解决方案。
---

## 摘要
缺失数据插补仍然是现代数据科学中的一个基本挑战，特别是在不确定性量化至关重要的情况下。在这项工作中，我们提出了 MissBGM，这是一种通过贝叶斯生成建模实现的人工智能驱动的缺失数据插补方法，它将神经网络的表达灵活性与贝叶斯推理的统计严谨性结合起来。与通常关注点估计或隐式处理缺失机制的现有方法不同，MissBGM 显式且联合地对数据生成机制和缺失机制进行建模，为插补提供有原则的后验不确定性，而非单一的点估计。我们开发了一个随机优化框架，在缺失值、模型参数和隐变量之间进行交替更新，直至收敛。我们的理论分析表明，在温和的假设下，MissBGM 对缺失值的估计具有一致的收敛性。在实验方面，我们证明了 MissBGM 在广泛的实验设置中，其性能优于传统的插补器和最近基于神经网络的方法。这些结果确立了 MissBGM 作为现代缺失数据插补的一种有原则且可扩展的解决方案。MissBGM 的代码已在 https://github.com/liuq-lab/MissBGM 开源。

## Abstract
Missing data imputation remains a fundamental challenge in modern data science, especially when uncertainty quantification is essential. In this work, we propose MissBGM, an AI-powered missing data imputation method via Bayesian generative modeling that bridges the expressive flexibility of neural networks with the statistical rigor of Bayesian inference. Unlike existing methods that often focus on point estimates or treat the missingness mechanism implicitly, MissBGM explicitly and jointly models the data-generating and missingness mechanisms, providing principled posterior uncertainty over imputations rather than a single point estimate. We develop a stochastic optimization framework with alternating updates among missing values, model parameters, and latent variables until convergence. Our theoretical analysis shows that estimates of missing values from MissBGM converge consistently under mild assumptions. Empirically, we demonstrate that MissBGM achieves superior performance over traditional imputers and recent neural network-based methods across extensive experimental settings. These results establish MissBGM as a principled and scalable solution for modern missing data imputation. The code for MissBGM is open sourced at https://github.com/liuq-lab/MissBGM.

---

## 论文详细总结（自动生成）

这是一份关于论文《Missingness-aware Data Imputation via AI-powered Bayesian Generative Modeling》的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
缺失数据插补是数据科学的基础挑战。现有方法存在两极分化：
*   **传统统计方法**（如 MICE, EM）：具有统计严谨性，能提供不确定性量化，但在处理高维、非线性数据时扩展性差，且依赖过于简单的缺失机制假设。
*   **深度学习方法**（如 GAIN, TabCSDI）：利用神经网络的灵活性提高了插补精度，但大多侧重于“点估计”，对缺失机制（尤其是非随机缺失 MNAR）的处理往往是隐式或启发式的，缺乏严谨的后验不确定性量化。
*   **研究动机**：开发一种既能捕捉复杂非线性依赖，又能显式建模缺失机制，并提供原则性不确定性量化的可扩展框架。

### 2. 论文提出的方法论：MissBGM
MissBGM 结合了深度生成模型与贝叶斯推理，核心思想是**联合建模数据生成过程和缺失机制**。
*   **核心模型**：
    *   **数据生成模型 $p_\theta(X|Z)$**：使用神经网络参数化均值和方差，将隐变量 $Z$ 映射到原始数据空间。
    *   **缺失机制模型 $p_\phi(R|X)$**：显式建模观测掩码 $R$ 如何依赖于完整数据 $X$（包括观测值和缺失值），从而能够处理非随机缺失（MNAR）。
*   **关键技术细节**：
    *   **交替随机优化算法**：在训练阶段，交替更新隐变量 $Z$、缺失值 $X_{mis}$ 以及全局模型参数 $\theta, \phi$。
    *   **EGM 预训练初始化**：引入编码生成建模（EGM）阶段，通过编码器-生成器对为模型参数和隐空间提供稳定的“热启动”。
    *   **后验推理与不确定性量化**：训练完成后，固定全局参数，使用 **HMC-within-Gibbs 采样器** 对每个样本的缺失值进行后验采样，从而计算点估计（后验均值）和置信区间。
    *   **温度系数 $\beta$**：引入超参数平衡数据项和缺失项的权重。

### 3. 实验设计
*   **数据集**：
    *   **合成数据集**：自掩码高斯模型（提供闭式解作为 Ground Truth）。
    *   **真实数据集**：Wine（小规模）、Breast Cancer（生物医学）、Concrete（工程）、Gisette（高维，5000个特征）。
*   **缺失场景**：重点测试了 **MNAR（非随机缺失）** 机制，即缺失概率与数值本身相关。
*   **对比方法（Benchmark）**：
    *   传统/非参数：Mean, OT (Optimal Transport), ICE, Miceforest。
    *   深度学习/生成式 AI：GAIN, ForestDiffusion, TabCSDI。
*   **评估指标**：RMSE（精度）、标准差 RMSE、预测区间宽度、PCC/SCC 相关系数（不确定性质量）。

### 4. 资源与算力
*   **硬件环境**：使用了 Linux 高性能计算集群，配备 **NVIDIA RTX 5000 Ada GPU**，双路 CPU（共48核），300GB 内存。
*   **训练时长**：
    *   EGM 初始化通常在 1 分钟内完成。
    *   MissBGM 的运行速度显著快于基于扩散模型的方法（如 TabCSDI 需要 48 小时，而 MissBGM 在同等设置下仅需数十分钟）。
    *   相比 Miceforest 等迭代方法，MissBGM 在高维数据上表现出更好的计算效率。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在合成数据上测试了 4 种样本量（0.5k 到 10k）和 3 种缺失率（0.3, 0.5, 0.7）的组合。
    *   在 4 个不同维度和领域的真实数据集上进行了 10 次重复实验并报告了标准差。
    *   **消融实验**：针对温度系数 $\beta$ 的影响以及是否使用贝叶斯神经网络（BNN）进行了专项研究。
*   **充分性评价**：实验设计覆盖了从低维到极高维（5000维）的场景，对比了当前最先进的扩散模型和传统森林模型，实验结果具有统计显著性，评估维度全面（精度+不确定性）。

### 6. 主要结论与发现
*   **精度领先**：MissBGM 在大多数合成和真实数据集上的 RMSE 均优于现有方法，尤其是在高维数据（Gisette）上优势明显。
*   **不确定性量化更准**：相比 GAIN 和 TabCSDI 容易出现的预测分布坍缩（区间过窄），MissBGM 提供的预测区间与真实误差的相关性更高，估计的方差更接近理论真实值。
*   **鲁棒性**：对缺失率和特征维度的增加表现出较强的抗干扰能力。
*   **理论一致性**：数学证明了在温和条件下，MissBGM 的估计值能够收敛到伪真实的插补目标。

### 7. 优点
*   **统计严谨性**：通过显式建模 $p(R|X)$，解决了深度学习插补器在 MNAR 机制下理论支撑不足的问题。
*   **高效初始化**：EGM 预训练有效避免了深度潜在变量模型常见的局部最优问题。
*   **高维扩展性**：算法设计支持小批量（mini-batch）更新，能够处理数千维度的特征，而许多传统方法（如 Miceforest）在此时会失效。
*   **开源支持**：提供了完整的代码库，易于复现。

### 8. 不足与局限
*   **计算开销**：虽然比扩散模型快，但由于涉及 MCMC 采样，其推理阶段（尤其是生成大量后验样本时）仍比简单的点估计方法（如 Mean 或单次 GAN 生成）耗时。
*   **超参数敏感性**：虽然论文称其鲁棒，但温度系数 $\beta$ 和隐变量维度 $d$ 的选择仍可能影响特定数据集的表现。
*   **模型假设**：尽管使用了神经网络，但底层仍基于高斯潜在变量假设，对于极端非高斯或高度离散的数据分布，可能需要更复杂的先验设计。

（完）
