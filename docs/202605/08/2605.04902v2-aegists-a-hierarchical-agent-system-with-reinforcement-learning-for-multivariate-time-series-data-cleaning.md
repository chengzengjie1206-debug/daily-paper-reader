---
title: "AegisTS: A Hierarchical Agent System with Reinforcement Learning for Multivariate Time Series Data Cleaning"
title_zh: AegisTS：一种基于强化学习的层次化智能体系统，用于多变量时间序列数据清洗
authors: "Yuhan Shi, Yuanyuan Yao, Lu Chen, Mourad Khayati, Tianyi Li"
date: 2026-05-06
pdf: "https://arxiv.org/pdf/2605.04902v2"
tags: ["query:ts"]
score: 9.0
evidence: 清理含有缺失值的多变量时间序列
tldr: AegisTS是一个针对多变量时间序列（MTS）数据清洗的层次化强化学习智能体系统。针对现实中多种数据质量问题（如缺失值、异常值等）共存且缺乏真实标签的挑战，该系统通过高层智能体确定处理顺序，底层智能体选择最优清洗模型，并结合双阶段奖励机制优化清洗流程。实验证明其在清洗质量和下游任务表现上均显著优于现有方法。
selection_source: fresh_fetch
motivation: 现有的多变量时间序列清洗方法通常只能处理单一质量问题，且高度依赖难以获取的真实数据或领域规则。
method: 提出一种层次化智能体架构，利用强化学习同步优化数据质量问题的处理顺序与清洗模型选择，并采用双阶段奖励机制引导优化。
result: "实验结果显示，AegisTS在数据清洗质量上提升了高达96%，在下游分析任务性能上提升了27%。"
conclusion: AegisTS通过自动化和层次化的强化学习策略，为复杂多变量时间序列数据的多重质量问题提供了一种高效且无需真实标签的清洗方案。
---

## 摘要
多变量时间序列（MTS）经常受到并发质量问题的影响，例如缺失值、离群值和约束违规，这些问题严重损害了下游分析。现有的清洗方法仅能修复有限的一组此类问题，因此不适用于多种质量问题同时出现的场景。此外，这些方法通常依赖于地面真值（ground truth）数据或特定领域的规则，而这两者在实际应用中往往难以获取。在本文中，我们介绍了 AegisTS，这是一个采用强化学习设计的智能体系统，旨在清洗 MTS 中的多种数据质量问题。我们将清洗过程建模为一个联合优化问题，同时处理质量问题的处理顺序和清洗模型的选择，从而在庞大的可能清洗流水线空间中进行高效导航。我们的框架依赖于层次化智能体架构，其中高层智能体确定处理数据质量问题的顺序，而底层智能体为每个问题识别最合适的清洗方法。为了引导智能体走向最优清洗流水线，我们提出了一种双阶段奖励机制，将上游（清洗）和下游性能耦合起来，从而在不依赖地面真值的情况下实现有效优化。实验结果表明，AegisTS 一贯优于现有方法，在数据清洗质量上提升高达 96%，在下游性能上提升达 27%。

## Abstract
Multivariate time series (MTS) are frequently affected by co-occurring quality issues, such as missing values, outliers, and constraint violations, which significantly undermine downstream analytics. Existing cleaning approaches fix only a limited set of such issues, making them ill-suited for scenarios where multiple quality problems arise simultaneously. Furthermore, these methods commonly depend on the availability of ground truth data or domain-specific rules, both of which are rarely accessible in real-world applications.   In this paper, we introduce \sys, an agent system with reinforcement learning designed to clean multiple data quality issues in MTS. We cast the cleaning process as a joint optimization problem that simultaneously handles quality issue order and cleaning model selection, allowing efficient navigation of the large space of possible cleaning pipelines. Our framework relies on a hierarchical agent architecture, where a high-level agent determines the order in which data quality issues should be processed, while a low-level agent identifies the most suitable cleaning method for each issue. To guide the agent toward an optimal cleaning pipeline, we propose a dual-stage reward mechanism that couples upstream (cleaning) and downstream performance, enabling effective optimization without relying on ground truth. Our experimental results show that \sys consistently outperforms existing methods, achieving up to 96\% improvement in data cleaning quality and 27\% improvement in downstream performance.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **AegisTS** 的层次化强化学习系统，专门用于解决多变量时间序列（MTS）中的多种并发数据质量问题。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **现实挑战**：现实中的多变量时间序列数据通常同时存在缺失值、离群值、重复值和约束违规等多种问题。
*   **现有局限**：
    1.  **功能单一**：现有清洗方法通常只针对某一类特定问题（如仅针对缺失值或仅针对离群值）。
    2.  **顺序敏感**：清洗操作的先后顺序（如先补全缺失值还是先剔除离群值）会显著影响最终效果，而手动确定最优顺序在海量特征下几乎不可能。
    3.  **缺乏真值**：实际应用中往往没有“干净”的地面真值（Ground Truth）来指导清洗过程。
*   **研究目标**：构建一个自动化的、无需真值引导的系统，能够根据下游任务需求自动选择和排序清洗算子。

### 2. 方法论
AegisTS 将清洗过程建模为一个**层次化强化学习（HRL）**任务：
*   **层次化智能体架构**：
    *   **高层智能体（High-Level Agent）**：负责战略决策，决定当前应该处理哪种类型的质量问题（缺失、离群、约束违规或终止）。
    *   **底层智能体（Low-Level Agent）**：负责战术执行，从包含 48 个算子的库中选择最适合当前问题的具体清洗算法。
*   **数据质量检测器**：
    *   **约束挖掘**：利用中位数绝对偏差（MAD）和特征残差剪枝，直接从“脏数据”中挖掘时间与变量间的结构约束。
    *   **异常检测选择**：使用元学习方法（FMMS）为特定数据集匹配最合适的异常检测模型。
*   **双阶段奖励机制（核心创新）**：
    *   **密集奖励（Dense Reward）**：在每一步操作后，利用**轻量级代理模型**（如 DLinear）的性能增益和数据质量指标（平滑度、修改距离等）提供即时反馈。
    *   **稀疏奖励（Sparse Reward）**：在整个清洗流水线完成后，利用**高复杂度目标模型**（如 LSTM、InceptionTime）的最终表现提供全局反馈。
*   **搜索空间优化**：引入“依赖感知搜索剪枝”，例如在发现空值时强制优先执行填充操作，将搜索复杂度从指数级降低到线性。

### 3. 实验设计
*   **数据集**：涵盖四个领域：电力（ETTh1）、重型设备监控（IDF_OilTemp）、手语识别（Libras）、智能手表动作识别（Handwriting）。
*   **下游任务**：预测（Forecasting）、分类（Classification）、聚类（Clustering）。
*   **对比方法（Benchmarks）**：
    *   **EDITOR**：最新的多分辨率 MTS 清洗方法。
    *   **Clean4TSDB**：基于约束的时间序列数据库清洗系统。
    *   **DiffPrep**：针对表格数据的可微分预处理流水线搜索方法。
    *   **Sampling**：随机采样算子并组合的基准。
*   **评价指标**：上游指标（F1, NMSE, RRA）和下游任务指标（NRMSE, CC, Macro F1, ROC AUC, Silhouette Score 等）。

### 4. 资源与算力
*   **硬件环境**：Linux 工作站，配置为 **Intel Core i9-10900K CPU**，**128 GB RAM**，以及单块 **NVIDIA RTX 3090 GPU (24 GB VRAM)**。
*   **训练时长**：文中提供了不同数据集的运行时间对比。例如，在 ETTh1 数据集上训练加推理总耗时约 2016 秒（约 33 分钟），在更复杂的 Handwriting 数据集上约需 6 小时。

### 5. 实验数量与充分性
*   **实验规模**：在 4 个数据集上针对 3 种不同任务进行了全面测试。
*   **消融实验**：对比了“单智能体 vs 层次化智能体”、“有无下游任务引导”、“有无质量指标引导”等变体，验证了各组件的必要性。
*   **泛化性测试**：进行了**跨数据集迁移实验**（如在 ETTh1 上训练的智能体直接用于清洗 IDF_OilTemp），证明了系统学习到的是通用的清洗策略而非简单的过拟合。
*   **客观性**：实验涵盖了从简单预测到复杂分类的多种场景，对比了当前最先进的（SOTA）基准，实验设计较为客观公平。

### 6. 主要结论与发现
*   **性能领先**：AegisTS 在所有任务中均优于基准方法，数据清洗质量（NMSE）提升高达 96%，下游任务性能提升最高达 27%。
*   **顺序的重要性**：实验证实了清洗顺序对结果有显著影响，AegisTS 能够发现比人工经验更优的清洗序列。
*   **无需真值的有效性**：通过双阶段奖励机制，系统在完全没有干净数据参考的情况下，依然能通过下游任务的反馈找到高质量的清洗路径。

### 7. 优点
*   **端到端自动化**：实现了从问题检测、算子选择到顺序优化的全流程自动化。
*   **层次化设计**：有效解决了强化学习在巨大动作空间下的收敛难题。
*   **任务驱动**：清洗目标直接与下游分析任务挂钩，确保了清洗后的数据具有更高的实用价值。
*   **可扩展性**：算子库是可扩展的，可以轻松集成新的清洗算法。

### 8. 不足与局限
*   **训练开销**：虽然推理很快，但初始训练过程（尤其是涉及高复杂度目标模型时）仍需一定的计算资源和时间。
*   **算子依赖**：系统的上限受限于预定义的算子库（目前为 48 个），如果库中缺乏针对特定极端噪声的算子，效果可能受限。
*   **静态环境假设**：目前主要针对离线数据集，虽然文中提到未来将扩展到流数据，但目前在实时动态变化的数据分布下的表现尚待验证。

（完）
