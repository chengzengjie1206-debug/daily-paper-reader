---
title: "AirQualityBench: A Realistic Evaluation Benchmark for Global Air Quality Forecasting"
title_zh: AirQualityBench：全球空气质量预测的真实评估基准
authors: "Xing Xu, Xu Wang, Yudong Zhang, Huilin Zhao, Zhengyang Zhou, Yang Wang"
date: 2026-05-07
pdf: "https://arxiv.org/pdf/2605.05854v1"
tags: ["query:ts"]
score: 9.0
evidence: 具有结构化缺失和原始观测掩码的预测基准
tldr: 针对现有空气质量预测模型多在理想化数据集上评估的问题，本文推出了AirQualityBench。这是一个涵盖全球3720个站点、六种主要污染物、跨度为2021-2025年的真实基准测试。它保留了原始的缺失数据掩码，要求模型在物理浓度尺度上进行预测，旨在评估模型在处理全球碎片化监测流时的真实性能，为开发可扩展且具备物理可解释性的预测模型提供重要测试平台。
selection_source: fresh_fetch
motivation: 现有的空气质量预测评估通常使用经过预处理和归一化的理想化数据集，忽略了现实监测网络中全球覆盖不均和数据结构性缺失等复杂挑战。
method: 提出了一个包含全球3720个站点、六种污染物、保留原始缺失值掩码且基于物理浓度尺度评估的全球多污染物预测基准。
result: 实验表明，在理想化数据集上表现优异的时空模型在面对全球碎片化的真实监测数据时，其性能往往无法有效迁移。
conclusion: AirQualityBench为开发更具鲁棒性、能够感知掩码且具备物理可解释性的全球空气质量预测模型提供了一个真实的评估基准。
---

## 摘要
空气质量预测模型通常在区域性、经过预处理和归一化的数据集上进行评估，其中缺失的观测值会被移除或人工补全。此类协议虽然简化了比较，但掩盖了主导真实监测网络的实际情况：全球覆盖不均、结构性缺失、异构污染物尺度以及部署成本。我们推出了 AirQualityBench，这是一个全球多污染物基准，旨在评估这些真实条件下的预测模型。该基准包含 2021-2025 年间 3,720 个监测站的逐小时观测数据，涵盖六种主要污染物，并保留了提供商原始的观测掩码。AirQualityBench 并非插补出一个密集的数据张量，而是将缺失性视为预测问题的一部分，并在逆变换回物理浓度尺度后，报告有效未来观测值的误差。在这一统一协议下对代表性时空模型的评估表明，在经过清理的数据集上的强劲表现并不能可靠地迁移到全球、碎片化的监测流中。因此，AirQualityBench 为可扩展、掩码感知且具有物理可解释性的空气质量预测提供了一个真实的测试平台。所有基准数据、代码、评估脚本和基准实现均可在 GitHub 上获取。

## Abstract
Air-quality forecasting models are commonly evaluated on regional, preprocessed, and normalized datasets, where missing observations are removed or artificially completed. Such protocols simplify comparison but hide the conditions that dominate real monitoring networks: uneven global coverage, structured missingness, heterogeneous pollutant scales, and deployment cost. We introduce \textbf{AirQualityBench}, a global multi-pollutant benchmark designed to evaluate forecasting models under these realistic conditions. The benchmark contains hourly observations from 3,720 monitoring stations over 2021--2025, covers six major pollutants, and preserves provider-native observation masks. Rather than imputing a dense data tensor, AirQualityBench exposes missingness as part of the forecasting problem and reports errors on valid future observations after inverse transformation to physical concentration scales. Evaluating representative spatio-temporal models under this unified protocol shows that strong performance on sanitized datasets does not reliably transfer to global, fragmented monitoring streams. AirQualityBench therefore serves as a realistic testbed for scalable, mask-aware, and physically interpretable air-quality forecasting. All benchmark data, code, evaluation scripts, and baseline implementations are available at \href{https://github.com/Star-Learning/AirQualityBench}{GitHub}.

---

## 论文详细总结（自动生成）

以下是对论文《AirQualityBench: A Realistic Evaluation Benchmark for Global Air Quality Forecasting》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：现有的空气质量预测模型评估通常在区域性、经过人工清理（如插值补全缺失值）且归一化的数据集上进行。这种“实验室环境”忽略了真实监测网络中的四大挑战：**全球覆盖不均、结构性数据缺失（非随机缺失）、污染物尺度异构以及实际部署的计算成本**。
*   **整体含义**：论文指出，在理想化数据集上表现优异的模型往往难以迁移到真实的碎片化监测流中。为此，作者提出了 **AirQualityBench**，旨在提供一个全球尺度、保留原始缺失特征、基于物理浓度单位评估的真实基准测试平台，以推动可扩展且具鲁棒性的时空预测模型研究。

### 2. 方法论：核心思想与关键技术
*   **核心思想**：提倡“掩码感知（Mask-aware）”和“物理尺度（Physical-scale）”评估。不进行人工数据插补，而是将缺失性作为预测任务的一部分。
*   **关键技术细节**：
    *   **数据构建**：从 OpenAQ 平台获取 2021-2025 年全球 3,720 个常驻站点的逐小时数据，涵盖 PM2.5、PM10、O3、NO2、SO2 和 CO 六种污染物。
    *   **任务定义**：输入历史观测张量 $X$ 和对应的二值掩码张量 $M$（1表示有效观测，0表示缺失），要求模型直接从碎片化历史中预测未来轨迹。
    *   **球面图构建**：考虑到全球尺度，放弃平面距离，采用 **Haversine 公式**计算站点间的球面距离，构建 $k$-最近邻（k-NN）图作为空间先验。
    *   **评估协议**：模型内部可进行归一化，但最终误差计算必须逆变换回**原始物理单位**（如 $\mu g/m^3$），且仅在掩码标记为“有效”的未来观测点上计算 MAE、RMSE 等指标。

### 3. 实验设计
*   **数据集与场景**：AirQualityBench 全球数据集。按时间顺序划分：2021-2023 年为训练集，2024 年为验证集，2025 年为测试集（涵盖完整的年度季节循环）。
*   **对比方法**：对比了 12 种代表性时空预测模型，涵盖四大类：
    *   **基于图的模型**：DCRNN, AGCRN。
    *   **时间卷积模型**：STGCN, GWN。
    *   **注意力/Transformer模型**：ASTGCN, STTN, PDFormer。
    *   **效率/解耦模型**：MAGE, BiST, D2STGNN, IGSTGNN, DSTAGNN。
*   **Benchmark 协议**：所有模型使用相同的球面图、相同的掩码协议和相同的物理尺度评估脚本。

### 4. 资源与算力
*   **算力说明**：论文详细记录了各模型的**推理延迟（ms）**、**显存占用（MB）**和**参数量（M）**。
*   **具体数值**：例如，D2STGNN 显存占用最高（约 16.5GB），而 BiST 非常轻量（仅 31.5MB）。
*   **硬件细节**：文中提到了“统一的分析流水线（profiling pipeline）”，但未明确给出具体的 GPU 型号（如 A100 或 V100）及总训练时长。

### 5. 实验数量与充分性
*   **实验规模**：
    *   主实验涵盖 12 种模型在 4 个预测步长（6h, 12h, 18h, 24h）下的表现。
    *   提供了 6 种污染物各自的详细误差分析。
    *   包含 $k$-NN 图连通性（$k$ 值）的消融实验。
    *   进行了 5 组不同随机种子的鲁棒性测试。
*   **充分性与公平性**：实验设计较为充分。通过统一的“真实掩码”协议，强制所有模型在相同的数据残缺条件下竞争，避免了因预处理手段不同导致的性能偏差，具有较高的客观性和公平性。

### 6. 主要结论与发现
*   **性能迁移失效**：在清理过的区域数据集上表现强的模型，在全球碎片化数据上排名会发生剧变。
*   **模型偏好**：具有**解耦时空动力学**的模型（如 D2STGNN）和**稳定的时间骨干**（如 GWN）表现最稳健；而过于复杂的注意力机制在处理非均匀全球图和高稀疏度数据时，往往难以学习到可靠的依赖关系。
*   **精度-效率权衡**：高精度通常伴随着高昂的显存和延迟代价，轻量化模型在处理真实缺失数据时精度损失显著。

### 7. 优点
*   **真实性极高**：保留了原始的缺失模式（MNAR），而非假设随机缺失，贴近实际部署环境。
*   **物理可解释性**：坚持在物理浓度尺度上报告误差，使结果对环境监管和毒理学研究具有实际意义。
*   **全球视野**：突破了单城市或单区域的限制，考虑了地球曲率对长距离污染物传输的影响。

### 8. 不足与局限
*   **地理分布不均**：监测站高度集中在亚洲、欧洲和北美，非洲和南美站点稀少且缺失率高，可能导致模型在这些地区的泛化能力受限。
*   **单位统一性挑战**：虽然保留了原始单位，但不同提供商对气态污染物（尤其是 CO）的单位约定可能存在细微差异，未来版本需更严格的单位审计。
*   **任务范围**：目前仅关注监督式预测，尚未涵盖不确定性估计或因果干预分析。

（完）
