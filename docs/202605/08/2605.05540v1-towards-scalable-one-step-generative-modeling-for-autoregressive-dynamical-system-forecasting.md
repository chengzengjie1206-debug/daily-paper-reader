---
title: Towards Scalable One-Step Generative Modeling for Autoregressive Dynamical System Forecasting
title_zh: 迈向自回归动力系统预测的可扩展单步生成建模
authors: "Tianyue Yang, Xiao Xue"
date: 2026-05-07
pdf: "https://arxiv.org/pdf/2605.05540v1"
tags: ["query:ts"]
score: 8.0
evidence: 自回归预测的长期不变时空一致性
tldr: 针对高维物理动力学模拟中神经算子易漂移且生成模型推理慢的问题，本文提出 MeLISA。这是一种基于像素空间 MeanFlow 的无潜变量自回归生成代理模型，通过单步评估实现快速预测。结合窗口一致性目标和时间增量一致性损失，MeLISA 在保持推理速度的同时，显著提升了长程滚动的统计准确性，并在高分辨率湍流基准测试中表现优异。
selection_source: fresh_fetch
motivation: 现有的物理动力学代理模型在长期预测中容易产生漂移，且基于扩散的生成模型推理效率较低。
method: 提出 MeLISA 模型，利用 MeanFlow 构建单步随机转移核，并结合窗口一致性与时间增量一致性损失来稳定长程预测。
result: 在 Kolmogorov 流和湍流通道流测试中，MeLISA 在短期精度和长期统计特性上均优于神经算子，且推理速度极快。
conclusion: MeLISA 证明了单步生成模型在兼顾物理模拟效率与长程统计一致性方面的巨大潜力，并具备良好的参数扩展性。
---

## 摘要
高维物理动力学的快速代理建模不仅需要较低的短期误差：有用的模型必须在高效外推的同时，保持长轨迹的统计结构。神经算子（Neural operators）提供了低成本的自回归预测，但在湍流状态下可能会发生漂移；而滚动扩散（rolling diffusion）和潜变量生成代理模型虽然可以表示随机转移，但代价是需要多步去噪、噪声调度设计或辅助压缩模型。我们提出了 MeanFlow 长期不变时空一致性自回归模型（MeLISA），这是一种基于像素空间 MeanFlow 构建的无潜变量自回归生成代理模型。MeLISA 定义了一个分块随机转移核，通过单次模型评估即可生成每个预测块，从而在推理时避免了潜变量编码器和迭代扩散求解器。为了稳定长时程外推，MeLISA 结合了窗口一致性 MeanFlow 目标（从部分观测的时间窗口学习条件时空生成）和时间增量一致性损失（约束多滞后有限增量并针对时间相关结构）。我们在两个高分辨率基准测试上评估了采用紧凑型 UNet 和可扩展 DiT 骨干网络的 MeLISA：$256 \times 256$ 的扩展二维 Kolmogorov 流和 $192 \times 192$ 的湍流槽道流切片。MeLISA 在短期预测精度和长时程统计指标（包括能谱、湍动能和与混合速率相关的动力学）方面均优于神经算子基准，同时实现了与神经算子相当、甚至在某些情况下更快的推理速度。参数量为 3.7-5.7M 的紧凑变体已经展现出强大的参数效率，而 DiT 变体则提供了扩展至 1.5 亿参数的可扩展路径。总的来说，MeLISA 兼顾了外推效率和长时程统计精度。

## Abstract
Fast surrogate modeling for high-dimensional physical dynamics requires more than low short-term error: useful models must roll out efficiently while preserving the statistical structure of long trajectories. Neural operators provide inexpensive autoregressive forecasts but can drift in turbulent regimes, whereas rolling diffusion and latent generative surrogates can represent stochastic transitions at the cost of multi-step denoising, noise-schedule design, or auxiliary compression models. We propose MeanFlow Long-term Invariant Spatiotemporal Consistency Autoregressive Models (MeLISA), a latent-free autoregressive generative surrogate built on pixel-space MeanFlow. MeLISA defines a blockwise stochastic transition kernel that generates each forecast block with a single model evaluation, avoiding latent encoders and iterative diffusion solvers at inference time. To stabilize long-horizon rollouts, MeLISA combines a Window-Consistency MeanFlow objective that learns conditional spatiotemporal generation from partially observed temporal windows with a Time Increment Consistency loss that constrains multi-lag finite increments and targets temporal-correlation structure. We evaluate MeLISA with compact UNet and scalable DiT backbones on two high-resolution benchmarks, extended 2D Kolmogorov flow at $256 \times 256$ and turbulent channel-flow slice at $192 \times 192$. MeLISA outperforms neural-operator baselines on short-term forecasting accuracy and long-horizon statistical metrics, including energy spectra, turbulent kinetic energy, and mixing-rate-related dynamics, while achieving inference speeds comparable to, and in some cases faster than, neural operators. Compact 3.7-5.7M-parameter variants already deliver strong parameter efficiency, and DiT variants provide a scalable path up to 150M parameters. Overall, MeLISA benefits both rollout efficiency and long-horizon statistical accuracy.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **MeLISA** 的新型生成式代理模型，旨在解决高维动力系统（如流体力学）长程预测中的效率与准确性平衡问题。以下是对该论文的深度结构化总结：

### 1. 核心问题与整体含义（研究动机）
*   **研究背景**：模拟复杂的非线性动力系统（如湍流）通常依赖高成本的数值求解（DNS）。数据驱动的代理模型中，**神经算子**（如 FNO）虽然推理快，但在长程自回归预测中容易产生误差累积和物理统计特性的漂移；**扩散模型**虽然能捕捉随机转移，但推理时需要多步迭代去噪，计算开销巨大。
*   **核心问题**：如何构建一个既能保持神经算子级别的推理速度（单步生成），又能像生成模型一样准确捕捉长程物理统计特性（如能谱、动能）的自回归代理模型？

### 2. 方法论：MeLISA 模型
MeLISA 基于 **Pixel MeanFlow (p-MF)** 框架，核心思想是将预测建模为从基础分布到数据分布的单步运输过程。
*   **窗口一致性 MeanFlow (WinC-MF)**：
    *   将单帧生成扩展为窗口条件生成。
    *   **关键技术**：在训练时对时间窗口内的帧进行随机掩码（Masking），模型需要根据部分观测的上下文预测完整的时空块。这迫使模型学习帧间的时空相干性，而非简单的复制。
*   **时间增量一致性 (TIC) 损失**：
    *   **核心公式思想**：不仅约束单帧的像素重构误差，还显式惩罚预测轨迹与真实轨迹在不同时间滞后（Lags）下的增量差异（$\Delta x = x_{t+w} - x_t$）。
    *   **物理意义**：TIC 损失约束了二阶时间结构，有助于恢复长程时空相关性和混合行为，这是单纯的像素重构损失无法实现的。
*   **算法流程**：
    *   **训练**：结合 WinC-MF 目标和 TIC 正则项，在像素空间直接训练。
    *   **推理**：采用 **1-NFE（单次函数评估）** 策略。给定上下文窗口，通过一次模型前向计算即可生成未来的一个时间块，实现极速自回归外推。

### 3. 实验设计
*   **数据集/场景**：
    1.  **2D Kolmogorov Flow ($256 \times 256$)**：受迫二维流体，具有复杂的混沌动力学。
    2.  **Turbulent Channel Flow ($192 \times 192$)**：3D 湍流槽道流的 2D 切片，具有强烈的非马尔可夫效应。
*   **Benchmark 与对比方法**：
    *   **确定性基准**：傅里叶神经算子 (FNO)、U 形神经算子 (UNO)、局部 FNO (Local-FNO)。
    *   **骨干网络变体**：MeLISA-$\Upsilon$（基于 UNet，3.7M-5.7M 参数）和 MeLISA-$\Delta$（基于 DiT，10M-150M 参数）。
*   **评估指标**：短期指标（相对 $L_2$ 误差、SSIM）；长期统计指标（能谱 PSDD、湍动能 TKED、混合速率 MRD）。

### 4. 资源与算力
*   **训练算力**：使用了 **32 个 GH200 节点**（每个节点 4 张 GPU），训练时长约为 **4 小时**。作者指出这大约相当于在 4 张 GH200 上训练 5 天。
*   **推理算力**：在单张 **NVIDIA RTX 4090** 上进行测试。MeLISA 的推理速度与神经算子相当，甚至在某些配置下更快（例如生成 320 帧仅需 6-10 秒）。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从 3M 到 150M 不同参数规模的模型，验证了可扩展性。
*   **消融实验**：详细探讨了 TIC 损失的作用（证明无 TIC 会导致预测向均值塌陷）以及上下文帧数对预测的影响。
*   **稳定性测试**：进行了长达 **9998 帧** 的极端外推测试（Stress Test），证明了模型在远超训练长度时的稳定性。
*   **客观性**：对比了多种主流神经算子，并使用了物理学界公认的统计指标（能谱等），实验设计较为严谨、客观。

### 6. 主要结论与发现
*   **性能优越**：MeLISA 在短期精度上匹配或超过了神经算子，在长期物理统计特性（如高频能谱恢复）上显著优于所有基准。
*   **推理高效**：通过单步生成（1-NFE），MeLISA 彻底解决了传统扩散模型推理慢的痛点，实现了与确定性模型同级别的速度。
*   **物理一致性**：TIC 损失是保持长程物理特性的关键，能有效防止模型在长程滚动中丢失高频细节。

### 7. 优点
*   **无需潜变量**：直接在像素空间操作，避免了 VAE 带来的信息损失和训练复杂性。
*   **单步生成**：无需复杂的噪声调度或多步 ODE/SDE 求解器，推理效率极高。
*   **参数效率与扩展性**：小模型（3.7M）表现已足够强劲，大模型（150M）展示了良好的 Scaling Law 潜力。

### 8. 不足与局限
*   **维度限制**：目前实验主要集中在 2D 场景，尚未在全 3D 高分辨率湍流数据上验证。
*   **数据依赖**：作为生成模型，其性能高度依赖于训练数据的覆盖范围，对于极度稀缺数据的场景可能受限。
*   **非马尔可夫性处理**：虽然在通道流切片上表现良好，但对于具有极长记忆效应的系统，其固定窗口的建模方式可能仍存在挑战。

（完）
