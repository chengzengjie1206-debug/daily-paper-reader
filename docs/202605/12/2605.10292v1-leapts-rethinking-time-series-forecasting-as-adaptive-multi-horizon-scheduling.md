---
title: "LeapTS: Rethinking Time Series Forecasting as Adaptive Multi-Horizon Scheduling"
title_zh: LeapTS：将时间序列预测重新构想为自适应多步长调度
authors: "Sheng Pan, Ming Jin, Bo Du, Shirui Pan"
date: 2026-05-11
pdf: "https://arxiv.org/pdf/2605.10292v1"
tags: ["query:ts"]
score: 9.0
evidence: 时间序列预测的自适应多周期调度
tldr: LeapTS将时间序列预测重新定义为动态多步调度过程，打破了传统模型固定映射的局限。该框架通过分层控制器动态选择预测尺度和步进长度，并结合神经常微分方程（NCDE）驱动连续状态演化。实验表明，LeapTS在提升预测精度的同时显著加快了推理速度，并能自主适应非平稳动态。
selection_source: fresh_fetch
motivation: 传统预测模型将历史到目标的映射视为固定过程，导致未来时间点间的时序解耦，难以适应演化的上下文。
method: 提出LeapTS框架，利用分层控制器动态调度预测步长，并结合神经常微分方程实现连续时间状态演化。
result: "在多个数据集上性能提升至少7.4%，推理速度比主流Transformer模型快2.6至5.3倍。"
conclusion: LeapTS通过将预测转化为自适应调度过程，有效捕捉了非平稳时序动态，实现了精度与效率的双重提升。
---

## 摘要
时间序列预测是许多现实应用中的重要工具，支持资源优化和决策制定等任务。尽管架构上取得了显著进展，但大多数现代模型仍将预测任务视为从历史到目标预测范围的固定映射。这导致了未来时间点之间的时序解耦，并限制了模型在预测过程中适应不断演变的上下文的能力。在这项工作中，我们提出了 LeapTS，这是一个将时间序列预测重新表述为预测范围内动态调度过程的新颖框架。具体而言，LeapTS 通过以下方式将预测过程组织为多级决策：(1) 使用分层控制器在每一步动态选择最佳预测尺度和推进长度；(2) 使用由神经常微分方程（Neural Controlled Differential Equations）驱动的连续时间状态演化。在此过程中，受控更新机制显式地将不规则的时序动力学与离散调度反馈耦合在一起。在真实世界和合成数据集上的广泛评估表明，LeapTS 将整体预测性能提高了至少 7.4%，同时与代表性的基于 Transformer 的模型相比，实现了 2.6 倍至 5.3 倍的推理加速。此外，通过显式追踪调度轨迹，我们揭示了模型如何自主调整其预测行为以捕捉非平稳动力学。

## Abstract
Time series forecasting serves as an essential tool for many real-world applications, supporting tasks such as resource optimization and decision-making. Despite significant architectural advancements, most modern models still treat forecasting task as a fixed mapping from history to target horizons. This induces temporal decoupling across future time points and limits the model's ability to adapt to the evolving context as forecasting progresses. In this work, we present LeapTS, a novel framework that reformulates time series forecasting as a dynamic scheduling process over the prediction horizon. Specifically, LeapTS organizes the forecasting process into multi-level decisions using: (1) the hierarchical controller to dynamically select the optimal prediction scale and advancement length at each step, and (2) continuous-time state evolution driven by neural controlled differential equations. Within this process, the controlled update mechanism explicitly couples the irregular temporal dynamics with discrete scheduling feedback. Extensive evaluations on both real-world and synthetic datasets demonstrate that LeapTS improves overall forecasting performance by at least 7.4% while achieving a 2.6$\times$ to 5.3$\times$ inference speedup over representative Transformer-based models. Furthermore, by explicitly tracing the scheduling trajectories, we reveal how the model autonomously adapts its forecasting behavior to capture non-stationary dynamics.

---

## 论文详细总结（自动生成）

这篇论文提出了 **LeapTS**，一种将时间序列预测重新定义为“自适应多步长调度”的新型框架，旨在解决传统预测范式在处理非平稳动力学时的僵化问题。以下是对该论文的深度结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：传统的多步预测主要有两种范式：**递归式（Recursive）**（逐步预测，易产生误差累积且计算开销大）和**直接式（Direct）**（一次性映射全周期，忽略了未来时间点间的时序依赖且缺乏上下文适应性）。
*   **核心问题**：如何让模型根据预测过程中不断演变的上下文动态调整预测行为？
*   **整体含义**：LeapTS 放弃了单纯追求复杂编码器的路线，转而关注“预测策略”的动态化，通过模拟人类“先粗后细、动态跳跃”的决策过程，实现了精度与效率的双重提升。

### 2. 方法论
LeapTS 的核心思想是将预测视为一个**粗到细的动态调度过程**，主要包含以下关键技术：
*   **分层控制器（H-Controller）**：
    *   **高层调度**：利用 Gumbel-Softmax 机制在“短、中、长”三个尺度中选择当前步的预测范围。
    *   **底层调度**：在选定尺度内，回归预测具体的推进长度（Advancement Length）。
*   **状态演化与更新机制**：
    *   **步内摘要更新**：将当前步生成的预测片段压缩为反馈向量，指导下一步决策。
    *   **步间连续演化**：引入**神经常微分方程（NCDE）**。它将调度步长视为不规则的时间采样，通过控制信号驱动隐藏状态在连续时间域内演化，从而捕捉复杂的时序过渡。
*   **双分支融合**：模型结合了一个全局粗略预测器（提供基准）和一个自适应调度分支（提供精细修正），最终通过学习到的门控机制进行融合。

### 3. 实验设计
*   **数据集**：
    *   **真实世界**：涵盖 13 个主流数据集，包括能源（ETT, Electricity）、天气、交通（PEMS）、金融（Exchange, SP500）、医疗（ILI, COVID）和网页流量（Wiki）。
    *   **单变量基准**：M4 竞赛数据集（包含 10 万条序列）。
    *   **合成数据**：设计了三种场景（高维异构、因果驱动、隐变量驱动）以测试模型在极端非平稳环境下的鲁棒性。
*   **对比方法（Benchmarks）**：包括 iTransformer, PatchTST, TimeMixer, TSMixer, DLinear, TimesNet, Amplifier 等近年来最先进的（SOTA）模型。

### 4. 资源与算力
*   **硬件环境**：实验在多块 **NVIDIA A100 80GB GPU** 上运行。
*   **算力表现**：论文强调了效率优势，LeapTS 在推理速度上比代表性的 Transformer 模型快 **2.6 倍至 5.3 倍**，且在训练时内存占用更低，参数量更少（见论文图 6）。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了极其详尽的测试，包括多周期预测（18 到 720 步）、单变量/多变量全覆盖、长短期对比。
*   **消融实验**：针对调度分支、CDE 单元（对比 RNN/LSTM）、分层机制、变量聚类数量等核心组件均做了消融分析。
*   **客观性**：所有实验重复 5 次并报告了置信区间（90%/95%/99%），且在合成数据上对基线模型进行了更广泛的超参数搜索，确保了对比的公平性。

### 6. 主要结论与发现
*   **性能领先**：LeapTS 在真实数据集上的 MAE 提升至少 **7.4%**，在 M4 数据集上也取得了最优的 OWA 指标。
*   **调度规律**：通过可视化发现，模型在**高波动区域**倾向于选择“短步长、高频率”的调度策略，而在**平稳区域**则倾向于“长步长、低频率”的跳跃。
*   **连续性优势**：NCDE 驱动的状态演化显著优于离散的 RNN 或 LSTM 单元，证明了连续时间建模在处理不规则调度步长时的必要性。

### 7. 优点
*   **范式创新**：打破了“历史-目标”固定映射的思维定式，引入了闭环反馈的调度思想。
*   **可解释性（可追踪性）**：通过记录调度轨迹，LeapTS 不再是完全的黑盒，用户可以直观看到模型在哪些时间点选择了“跳跃”或“细嗅”。
*   **高效率**：在保持高精度的同时，大幅降低了长序列预测的计算复杂度。

### 8. 不足与局限
*   **领域局限性**：目前模型仍需在特定数据集上从头训练，模型规模相对较小，尚未展现出类似大语言模型的通用泛化能力。
*   **零样本能力**：论文提到未来需要通过大规模预训练来构建通用的调度模型，以实现无需微调的零样本预测。
*   **复杂度平衡**：虽然推理快，但分层控制器和 NCDE 的引入增加了系统实现的复杂性，对开发者理解模型内部机理有一定门槛。

（完）
