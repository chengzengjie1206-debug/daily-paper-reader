---
title: "ABC: Any-Subset Autoregression via Non-Markovian Diffusion Bridges in Continuous Time and Space"
title_zh: ABC：通过连续时空中的非马尔可夫扩散桥实现任意子集自回归
authors: "Gabe Guo, Thanawat Sornwanee, Lutong Hao, Elon Litman, Stefano Ermon, Jose Blanchet"
date: 2026-04-30
pdf: "https://arxiv.org/pdf/2604.27443v2"
tags: ["query:ts"]
score: 8.0
evidence: 基于任意子集和不规则时间步的条件生成
tldr: 针对连续时间与空间随机过程（如视频、天气）的生成难题，本文提出ABC模型。该模型利用非马尔可夫扩散桥构建连续SDE，使生成过程的时间与状态与物理过程同步。相比传统扩散模型，ABC能从已有状态而非纯噪声开始生成，且噪声注入随物理时间缩放，支持对任意历史或未来子集的条件生成。实验证明其在视频生成和天气预报任务中优于现有方法。
selection_source: fresh_fetch
motivation: 现有扩散模型在处理连续物理过程时，存在噪声演化与物理时间脱节、难以处理任意子集条件约束等局限。
method: 提出一种基于非马尔可夫扩散桥的连续SDE建模方法，通过路径空间测度变换实现路径依赖的条件生成，并扩展了去噪分数匹配算法。
result: 在视频生成和天气预报等多个领域的实验中，ABC模型在生成质量和物理一致性上均显著优于竞争方法。
conclusion: ABC模型通过将扩散过程与物理时间对齐，为连续时空随机过程的任意子集自回归生成提供了一种高效且物理上更合理的新框架。
---

## 摘要
基于部分观测（如首尾帧）生成连续时间、连续空间的随机过程（如视频、天气预报）是一项基础性挑战。现有方法（如扩散模型）存在关键局限性：(1) 从噪声到数据的演化过程无法捕捉物理时间上接近的状态之间的结构相似性，且在低步长机制下积分不稳定；(2) 注入的随机噪声对物理过程流逝的时间不敏感，导致动力学过程不正确；(3) 它们忽视了对任意状态子集（如不规则采样的时刻、未来观测值）的条件约束。我们提出了 ABC：通过连续时空中的非马尔可夫扩散桥实现的任意子集自回归模型。关键在于，我们使用一个连续的随机微分方程（SDE）对该过程进行建模，其时间变量和中间状态追踪真实的时间和过程状态。这具有可证明的优势：(1) 生成未来状态的起点是已经接近的前一状态，而非无信息的噪声；(2) 随机噪声的注入随物理时间的流逝而缩放，从而促进产生具有相似时间相邻状态的物理上合理的动力学。我们通过路径空间上的测度变换推导出 SDE 动力学，从而产生另一个优势：(3) 对状态历史和/或未来的任意子集进行路径依赖的条件约束。为了学习这些动力学，我们推导出了去噪得分匹配的一种路径和时间依赖的扩展。我们的实验表明，ABC 在包括视频生成和天气预报在内的多个领域均优于竞争方法。

## Abstract
Generating continuous-time, continuous-space stochastic processes (e.g., videos, weather forecasts) conditioned on partial observations (e.g., first and last frames) is a fundamental challenge. Existing approaches, (e.g., diffusion models), suffer from key limitations: (1) noise-to-data evolution fails to capture structural similarity between states close in physical time and has unstable integration in low-step regimes; (2) random noise injected is insensitive to the physical process's time elapsed, resulting in incorrect dynamics; (3) they overlook conditioning on arbitrary subsets of states (e.g., irregularly sampled timesteps, future observations). We propose ABC: Any-Subset Autoregressive Models via Non-Markovian Diffusion Bridges in Continuous Time and Space. Crucially, we model the process with one continual SDE whose time variable and intermediate states track the real time and process states. This has provable advantages: (1) the starting point for generating future states is the already-close previous state, rather than uninformative noise; (2) random noise injection scales with physical time elapsed, encouraging physically plausible dynamics with similar time-adjacent states. We derive SDE dynamics via changes-of-measure on path space, yielding another advantage: (3) path-dependent conditioning on arbitrary subsets of the state history and/or future. To learn these dynamics, we derive a path- and time-dependent extension of denoising score matching. Our experiments show ABC's superiority to competing methods on multiple domains, including video generation and weather forecasting.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **ABC**（Any-Subset Autoregression via Non-Markovian Diffusion Bridges）的新型生成模型框架，旨在解决连续时间与空间随机过程（如视频、天气预报）的建模难题。

以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **研究背景**：生成视频或天气预报等连续过程时，通常需要基于部分观测值（如首帧、末帧或不规则采样的中间帧）进行“补全”。
*   **现有局限**：
    1.  **状态不连续**：传统扩散模型从无信息的噪声映射到数据，忽略了物理时间上相邻状态的极高相似性，导致生成过程在物理上不合理。
    2.  **时间尺度缺失**：现有模型通常使用固定的辅助时间（0到1）进行迭代，而不考虑物理时间的跨度。例如，生成间隔1秒和间隔1小时的帧，注入的噪声量往往相同，这违反了物理规律。
    3.  **条件约束僵化**：难以处理任意子集的条件约束（如非因果的未来观测、不规则的时间采样）。
*   **核心目标**：建立一个统一的 SDE（随机微分方程）模型，使其生成时间与物理时间同步，并能灵活处理任意观测子集。

### 2. 方法论
*   **核心思想**：将生成过程建模为一个**连续的 SDE**，其时间变量和中间状态直接对应真实的物理时间和过程状态。
*   **关键技术细节**：
    1.  **数据到数据的桥接**：不同于“噪声到数据”的扩散，ABC 实现了“数据到数据”的演化。生成起点是前一个已知状态，而非高斯噪声。
    2.  **时间自适应波动率（Time-Adaptive Volatility）**：证明了随机噪声的注入应随物理时间跨度缩放。时间跨度越短，注入噪声越小，确保了状态的连续性。
    3.  **Doob h-变换与测度变换**：利用随机分析理论，通过改变路径空间的测度，推导出能够匹配数据有限维分布的漂移项（Drift）。
    4.  **非马尔可夫得分匹配**：推导出一种路径依赖的去噪得分匹配目标函数，使模型能够根据整个观测历史（甚至未来）来预测当前的演化趋势。
    5.  **任意子集自回归**：通过交叉注意力机制（Cross-Attention Transformer），模型可以同时接收过去和未来的观测点作为输入，实现非因果的条件生成。

### 3. 实验设计
*   **数据集**：
    *   **视频生成**：CelebV-HQ（人脸视频）、Sky Timelapse（天空延时摄影）。
    *   **天气预报**：SEVIR VIL（雷达回波图）。
*   **对比方法（Baselines）**：
    *   **条件扩散桥（DDBM/PFI）**：代表固定波动率的桥接模型。
    *   **噪声到数据扩散（DDPM/SMLD）**：代表传统的扩散模型。
*   **评估指标**：
    *   视频：FVD（时间一致性）、FID（单帧质量）。
    *   天气：MAE、RMSE、CSI（关键成功指数）等气象标准指标。
*   **场景设置**：包括因果生成（预测未来）和非因果生成（给定首尾帧进行插值）。

### 4. 资源与算力
*   **硬件使用**：使用了 **NVIDIA A100 (80GB) GPU**。
*   **训练规模**：
    *   基础模型（202M 参数）：在 4 台 A100 上训练约 **36 小时**（24万步）。
    *   中型模型（594M 参数）：在 8 台 A100 上进行扩展实验。
*   **推理设置**：测试了 250 步和 500 步的 SDE 积分步数。

### 5. 实验数量与充分性
*   **实验组数**：论文进行了多维度的实验，包括不同数据集的性能对比、波动率超参数敏感性分析、条件约束方式（因果 vs 非因果）的消融实验、以及模型参数规模的扩展性实验。
*   **充分性与公平性**：
    *   实验覆盖了视频和天气两个截然不同的领域，验证了通用性。
    *   **受控对比**：所有对比方法使用相同的 Transformer 架构和训练设置，仅改变 SDE 动力学逻辑，确保了对比的客观性和公平性。
    *   提供了定性（视频样本）和定量（指标表格）的双重证据。

### 6. 主要结论与发现
1.  **物理一致性**：ABC 模型在时间一致性（FVD）上显著优于传统扩散模型，生成的视频更平滑，无闪烁。
2.  **时间缩放的重要性**：实验证明，让噪声随物理时间缩放是捕捉真实动态的关键；固定噪声计划会导致轨迹偏离物理真实。
3.  **数据起点优势**：从已有数据点开始生成（Bridge）比从噪声开始生成在数值积分上更稳定，且在低步长下表现更好。
4.  **灵活性**：ABC 成功实现了在不规则采样和非因果约束下的高质量生成，证明了其作为“任意子集自回归”模型的有效性。

### 7. 优点与亮点
*   **理论严谨**：基于测度变换和 Doob h-变换提供了坚实的数学证明，将扩散模型与自回归模型在连续空间统一起来。
*   **物理启发式偏置**：引入了“时间相邻则状态相似”的归纳偏置，这比纯数据驱动的噪声映射更符合现实世界逻辑。
*   **多功能性**：一个模型即可处理预测、插值、修复等多种任务，且支持任意时间分辨率。

### 8. 不足与局限
*   **计算开销**：由于需要对整个路径历史进行注意力计算，随着序列长度增加，Transformer 的计算复杂度会显著上升。
*   **硬约束挑战**：在非因果生成中，为了精确命中未来的观测点，有时需要引入额外的“Brownian Bridge”修正项（Hack），这在数学优雅性上略有妥协。
*   **应用限制**：目前主要在潜在空间（Latent Space）进行实验，对于极长序列或极高分辨率的直接生成，显存压力依然存在。

（完）
