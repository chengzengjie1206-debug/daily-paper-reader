---
title: "PAMNet: Cycle-aware Phase-Amplitude Modulation Network for Multivariate Time Series Forecasting"
title_zh: PAMNet：面向多元时间序列预测的周期感知相位-幅度调制网络
authors: "Yingbo Zhou, Yutong Ye, Zhiwei Ling, Shuhao Li, Rui Qian, Jian Xiong, Li Sun, Dejing Dou"
date: 2026-05-01
pdf: "https://arxiv.org/pdf/2605.02938v1"
tags: ["query:ts"]
score: 9.0
evidence: 最先进的多元时间序列预测
tldr: PAMNet是一种针对多元时间序列预测的新型网络，旨在解决现有方法在处理周期性模式时计算开销大或忽略相位-振幅耦合的问题。该方法通过双分支调制器将周期模式显式分解为相位和振幅组件，利用循环嵌入捕捉相位偏移，并建模强度变化以适应方差波动。在12个真实数据集上的实验证明，该方法通过显式的相位-振幅解耦机制实现了SOTA性能，为时间序列周期建模提供了新视角。
selection_source: fresh_fetch
motivation: 现有时间序列预测方法在建模周期性时，要么计算复杂度过高，要么忽略了相位与振幅之间的内在耦合关系。
method: 提出PAMNet，通过双分支调制器显式分解相位（捕捉均值偏移）与振幅（建模强度变化），并利用轻量化元素级融合进行交互。
result: 在12个真实世界数据集上的广泛实验表明，PAMNet达到了当前最先进的预测性能。
conclusion: 显式的相位-振幅解耦机制能更有效地捕捉时间序列的周期性特征，为循环模式建模提供了一种高效且准确的新方案。
---

## 摘要
可靠的周期性模式是准确进行多元时间序列预测的基础。然而，现有方法要么通过具有高计算开销的复杂模型架构（如 Transformer）隐式提取周期性，要么在显式建模周期性成分时忽略了内在的相位-幅度耦合。为了解决这些问题，我们提出了一种新颖的周期感知相位-幅度调制网络（PAMNet），它将周期性模式显式分解为互补的相位和幅度成分。其核心创新在于双分支调制器，具有用于相位定位和幅度调制的专用可学习嵌入。相位分支采用循环嵌入来捕捉与相位相关的均值偏移，而幅度分支则对强度变化进行建模以适应方差的变化。具有逐元素融合功能的轻量级调制器有效地结合了这些成分，能够在无需复杂注意力机制的情况下显式建模它们的相互作用。在 12 个真实世界数据集上的广泛实验表明，我们的方法通过其新颖的相位-幅度解耦机制实现了最先进的性能，为时间序列预测中的周期性建模提供了新的视角。

## Abstract
Reliable periodic patterns serve as a fundamental basis for accurate multivariate time series forecasting. However, existing methods either implicitly extract periodicity through complex model architectures (e.g., Transformers) with high computational overhead or overlook the intrinsic phase-amplitude coupling when modeling periodic components explicitly. To address these issues, we propose a novel Cycle-aware Phase-Amplitude Modulation Network (PAMNet) that explicitly decomposes periodic patterns into complementary phase and amplitude components. The core innovation lies in its dual-branch modulator, featuring dedicated learnable embeddings for phase positioning and amplitude modulation. The phase branch employs cyclical embeddings to capture phase-dependent mean shifts, while the amplitude branch models intensity variations to adapt to changes in variance. A lightweight modulator with element-wise fusion efficiently combines these components, enabling explicit modeling of their interactions without complex attention mechanisms. Extensive experiments on twelve real-world datasets demonstrate that our method achieves state-of-the-art performance through its novel phase-amplitude decoupling mechanism, offering a new perspective for cyclical modeling in time series forecasting.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **PAMNet** 的新型多元时间序列预测框架，旨在通过显式解耦周期性模式中的“相位”与“幅度”来提升预测精度。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：如何在多元时间序列（MTS）中更有效地建模周期性模式。
*   **研究背景**：
    *   **现有方法的局限**：Transformer 等隐式方法计算开销大且缺乏可解释性；而现有的显式周期建模方法（如季节性分解）通常将周期视为整体，忽略了**相位（事件发生的时间点）**与**幅度（事件的强度）**之间的复杂耦合关系。
    *   **周期非平稳性**：现实数据中，即使相位对齐，其幅度也会因外部因素（如节假日、天气）而剧烈波动。论文通过可视化证明了均值（相位相关）和标准差（幅度相关）是独立变化且相互关联的。

### 2. 方法论（PAMNet）
PAMNet 的核心思想是将预测过程视为一种**自适应信号调制**过程。
*   **核心架构**：
    *   **输入编码**：使用倒置嵌入（Inverted Embedding）将时间点转化为变量特征。
    *   **周期索引（Cyclical Index）**：根据时间戳计算当前处于周期内的位置 $t$（如一天中的第几小时）。
    *   **双载波嵌入（Dual Carrier Embeddings）**：
        *   **相位载波 $E_P(t)$**：学习周期内不同位置的均值偏移，负责“定位”。
        *   **幅度载波 $E_A(t)$**：学习不同变量在特定位置的强度变化，负责“缩放”。
    *   **并行调制器（Parallel Modulator）**：
        *   采用类似幅度调制（AM）的机制，通过**Hadamard 积（逐元素相乘）**实现特征门控。
        *   公式化表示为：$M(E_X, S) = W_3(Dropout(SiLU(W_1 E_X) \odot W_2 S))$，其中 $S$ 为相位或幅度载波。
    *   **融合与预测**：将相位调制和幅度调制的特征相加融合，通过轻量级 MLP 输出预测结果。

### 3. 实验设计
*   **数据集**：涵盖 12 个真实世界数据集，包括 ETT 系列（4个）、电力（ECL）、交通（Traffic）、天气（Weather）、太阳能（Solar）以及 PEMS 系列（4个）。
*   **预测任务**：长短期预测，预测步长包括 $\{96, 192, 336, 720\}$（PEMS 为 $\{12, 24, 48, 96\}$）。
*   **对比方法（Benchmark）**：对比了 9 种最先进的方法，包括 TQNet (2025), FilterTS (2025), iTransformer (2024), PatchTST (2023), TimesNet (2023) 等。

### 4. 资源与算力
*   **硬件环境**：单张 **NVIDIA GeForce RTX 4090 (24GB)** GPU。
*   **效率表现**：论文明确对比了训练速度和内存占用。在 ETTm1 数据集上，PAMNet 的内存占用与 CycleNet 相当，但训练速度更快；在 Traffic 等大规模数据集上，其效率远高于 iTransformer 和 PatchTST 等复杂架构。

### 5. 实验数量与充分性
*   **实验规模**：进行了大规模的基准测试（24 组指标对比），PAMNet 在 20 组中获得第一。
*   **消融实验**：验证了相位/幅度组件、激活函数、损失函数以及调制器结构的必要性。
*   **泛化性测试**：
    *   **输入长度敏感性**：在不同回顾窗口长度下保持稳定。
    *   **缺失值鲁棒性**：在 10%-30% 数据缺失情况下，性能下降幅度远小于基准模型。
    *   **外生变量预测**：在多对一预测任务中表现优异。
*   **充分性评价**：实验设计非常全面，涵盖了准确性、效率、鲁棒性和可解释性分析，结果具有较强的说服力。

### 6. 主要结论与发现
*   **解耦的有效性**：显式地将周期信号分解为相位和幅度，比将其视为单一成分（Monolithic）更符合物理直觉且预测更准。
*   **SOTA 性能**：PAMNet 在多个领域（能源、交通、天气等）均刷新了预测精度记录。
*   **轻量化优势**：无需复杂的注意力机制，仅通过线性复杂度的调制操作即可捕捉复杂的周期动态。

### 7. 优点（亮点）
*   **物理启发式设计**：借鉴信号处理中的调制理论，为深度学习模型引入了强大的归纳偏置。
*   **高效率与低显存**：适合处理大规模、高维度的多元时间序列。
*   **强解释性**：通过 t-SNE 可视化证明了模型确实学习到了具有区分性的相位和幅度特征。
*   **鲁棒性**：对数据缺失和噪声具有极强的抵抗力。

### 8. 不足与局限
*   **周期长度依赖**：模型需要预先定义周期长度 $c$（如 24 小时），虽然实验证明其对 $c$ 的微小偏差不敏感，但在处理周期未知或随时间漂移的数据时可能受限。
*   **单尺度建模**：目前主要针对单一主导周期进行调制，对于存在多个嵌套周期（如同时存在日周期和周周期）的复杂交互建模尚不显式。
*   **未来方向**：论文提到未来需要探索自适应周期检测和跨频率交互建模。

（完）
