---
title: "Predicting missing values: A good idea?"
title_zh: 预测缺失值：这是一个好主意吗？
authors: Stef van Buuren
date: 2026-05-05
pdf: "https://arxiv.org/pdf/2605.03733v1"
tags: ["query:ts"]
score: 9.0
evidence: 缺失值填补与偏差减少
tldr: 本研究探讨了在缺失值填补中最小化均方误差（MSE）导致的系统性偏差问题。虽然预测性填补能提供准确的点估计，但会降低数据变异性，从而影响方差、相关性等下游分析。研究提出通过引入与MSE成比例的随机噪声（随机填补）来消除这些偏差。通过模拟实验和对missForest等主流工具的评估，证明了随机填补在保持数据自然变异性和确保分析有效性方面的必要性。
selection_source: fresh_fetch
motivation: 传统的缺失值填补方法往往追求最小化均方误差，但这会因忽略数据变异性而导致下游统计分析产生系统性偏差。
method: 对比了最小化MSE的预测性填补与引入随机噪声的随机填补方法，并在多元正态分布及多种主流填补工具上进行了验证。
result: 预测性方法虽然点估计准确，但会显著扭曲数据分布，而随机填补方法能有效保留数据的自然变异性并提供无偏估计。
conclusion: 均方误差不足以衡量填补质量，在填补过程中引入随机噪声是防止分析偏差、确保下游研究有效性的关键。
---

## 摘要
最小化均方误差（MSE）是机器学习中的一个核心目标，常用于填补缺失值。虽然这种方法能提供准确的点估计，但它会在下游分析中引入系统性偏差。这些偏差会影响方差、流行率、相关性、斜率和解释方差等关键参数。根本原因在于，针对 MSE 优化的填补值是平均值，这降低了数据中的自然变异性。本文证明，在填补值中加入噪声可以有效消除这些偏差。所需的噪声水平与 MSE 成正比。在多元正态分布的简单示例中，我们比较了两种方法：最小化 MSE 的预测性填补（predictive imputation）和引入随机噪声的随机填补（stochastic imputation）。仿真结果表明，预测性方法会系统性地引入偏差，而随机填补方法则保留了数据的自然变异性并产生无偏估计。我们还评估了三种流行的填补工具——missForest、softImpute 和 mice，并观察到预测性方法中存在一致的偏差。这些发现强调，MSE 并不是衡量填补质量的充分指标，因为它优先考虑准确性而非变异性。在填补方法中加入噪声对于防止偏差和确保下游分析的有效性至关重要，这突显了随机方法在处理不完整数据时的重要性。

## Abstract
Minimizing the Mean Squared Error (MSE) is a key objective in machine learning and is commonly used for imputing missing values. While this approach provides accurate point estimates, it introduces systematic biases in downstream analyses. These biases affect key parameters such as variance, prevalence, correlation, slope, and explained variance. The root cause is that imputed values optimized for MSE are averages, which reduce the natural variability in the data.   This paper demonstrates that adding noise to imputed values can effectively eliminate these biases. The required noise level is proportional to the MSE. Using a toy example in a multivariate normal setting, we compare two methods: predictive imputation, which minimizes MSE, and stochastic imputation, which incorporates random noise. Simulation results show that predictive methods systematically introduce bias, while stochastic methods preserve the data's natural variability and produce unbiased estimates.   We also evaluate three popular imputation tools -- missForest, softImpute, and mice -- and observe consistent biases in predictive methods. These findings highlight that MSE is an inadequate measure of imputation quality, as it prioritizes accuracy over variability. Incorporating noise into imputation methods is essential to prevent biases and ensure valid downstream analyses, underscoring the importance of stochastic approaches for handling incomplete data.

---

## 论文详细总结（自动生成）

以下是对论文《Predicting missing values: A good idea?》的结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：在机器学习和数据科学中，最小化均方误差（MSE）被视为填补缺失值的“金标准”。然而，这种追求“准确性”的做法在统计学视角下存在严重缺陷。
*   **核心问题**：论文探讨了为什么以 MSE 为目标的预测性填补会导致下游数据分析（如方差计算、相关性分析、回归建模）产生系统性偏差。
*   **整体含义**：填补缺失值的目的不应是“还原”真实值，而应是“保持数据的统计特性”。论文挑战了机器学习界过度依赖 MSE 评估填补质量的现状。

### 2. 方法论
*   **核心思想**：对比“预测性填补（Predictive Imputation）”与“随机填补（Stochastic Imputation）”。
*   **关键技术细节**：
    *   **预测性填补 (predict)**：直接使用模型预测的条件均值 $\hat{y}_i = f(X; \hat{\beta})$ 填补。其目标是最小化 MSE，但会导致填补值向均值回归，人为压缩了数据的变异性。
    *   **随机填补 (draw)**：在预测值的基础上增加一个随机噪声项 $\dot{y}_i = \hat{y}_i + \epsilon_i$，其中 $\epsilon_i$ 是从残差分布中抽取的随机数。
*   **算法流程**：
    1.  利用观测数据估计回归系数 $\hat{\beta}$ 和残差方差 $\hat{\sigma}^2$。
    2.  对于缺失值，计算预测值。
    3.  **关键区别**：预测法直接填入预测值；随机法则生成一个均值为 0、方差为 $\hat{\sigma}^2$ 的噪声并叠加到预测值上。

### 3. 实验设计
*   **数据集/场景**：构建了两个合成总体（$N=10^6$），分别代表“高信号”（$R^2=0.8$）和“低信号”（$R^2=0.2$）场景。
*   **缺失机制**：对比了完全随机缺失（MCAR）和随机缺失（MAR，缺失概率与预测变量相关）。
*   **Benchmark**：以原始完整数据（Ground Truth）作为基准。
*   **对比方法**：
    *   内部对比：`predict` vs `draw`。
    *   主流工具对比：`missForest`（基于随机森林）、`softImpute`（基于矩阵分解）、`mice`（基于链式方程的随机填补）。
*   **评估指标**：均值、标准差、90分位数比例、相关系数、回归斜率、解释方差（$R^2$）以及 MSE。

### 4. 资源与算力
*   论文未明确提到使用高性能计算资源或 GPU。
*   实验基于 R 语言环境完成，样本量为 1000，重复 200 次模拟。由于模型较为简单（线性回归、随机森林等），此类实验通常在普通桌面级 CPU 上即可在短时间内完成。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 2 种信号强度、2 种缺失机制、5 种填补方法，每组实验重复 200 次。
*   **充分性**：实验设计非常系统，通过控制变量法清晰地展示了 MSE 与统计偏差之间的因果关系。虽然使用的是合成数据（多元正态分布），但对于证明统计学原理而言，这种“玩具模型”是充分且客观的，因为它排除了复杂噪声的干扰。

### 6. 主要结论与发现
*   **MSE 的误导性**：预测性方法（如 `missForest`）虽然 MSE 较低，但会显著低估方差，人为提高相关性和回归斜率，导致下游分析得出错误结论。
*   **噪声的必要性**：在填补值中加入与 MSE 成比例的噪声（随机填补），虽然会使 MSE 增加约一倍，但能有效消除下游分析的偏差，还原数据的真实分布。
*   **工具表现**：`mice` 由于采用了随机框架，表现最稳健；`missForest` 表现出明显的预测性偏差；`softImpute` 在低信号场景下表现不稳定。

### 7. 优点
*   **观点犀利**：直击机器学习领域在缺失值处理上的盲点，即“高精度填补不等于高质量分析”。
*   **理论与实践结合**：不仅有数学推导，还对工业界常用的软件包进行了实测，具有很强的实操指导意义。
*   **透明度高**：附录提供了完整的 R 代码，便于研究者复现和验证。

### 8. 不足与局限
*   **数据分布单一**：实验主要基于正态分布假设，未深入探讨非线性关系或重尾分布下的表现。
*   **单次填补局限**：论文主要讨论单次填补（Single Imputation），虽然提到了多次填补（Multiple Imputation），但未对其在复杂 ML 模型中的应用做详细展开。
*   **应用场景限制**：在某些纯预测任务（如仅关注预测准确率，不关注解释性）中，最小化 MSE 依然有效，论文的结论主要适用于需要进行统计推断和关系建模的场景。

（完）
