---
title: U-STS-LLM A Unified Spatio-Temporal Steered Large Language Model for Traffic Prediction and Imputation
title_zh: U-STS-LLM：一种用于流量预测和插补的统一时空引导大语言模型
authors: "Yichen Zhang, Jun Li"
date: 2026-05-12
pdf: "https://arxiv.org/pdf/2605.11735v1"
tags: ["query:ts"]
score: 10.0
evidence: 交通预测与缺失值填补的统一模型
tldr: 本研究针对蜂窝网络流量分析中的预测与填补任务，提出了统一框架U-STS-LLM。该模型通过创新的动态时空注意力偏差生成器，将功能图与节点状态结合，显式引导大语言模型的注意力机制。结合LoRA微调和门控自适应融合，U-STS-LLM在保持参数效率的同时，实现了对时空结构的深度理解，有效解决了传统模型泛化性差和LLM结构引导不足的问题，为非语言领域应用大模型提供了新思路。
selection_source: fresh_fetch
motivation: 旨在解决传统时空图神经网络泛化性受限以及大语言模型在处理非语言时空数据时缺乏结构引导和训练不稳定的挑战。
method: 引入动态时空注意力偏差生成器，并结合部分冻结的骨干网络、LoRA微调及门控自适应融合机制，实现统一的多任务学习。
result: 在真实世界数据集上，该模型在长程预测和高缺失率数据填补方面均刷新了最优性能记录，并展现出卓越的训练效率与稳定性。
conclusion: U-STS-LLM证明了通过引入显式时空引导，大语言模型能有效统一并解决结构化非语言领域的复杂时空数据建模挑战。
---

## 摘要
现代蜂窝网络的高效运行取决于对时空流量数据的准确分析。掌握这些模式对于核心网络功能至关重要，主要包括预测未来负载以预防拥塞，以及插补因传感器故障或传输错误导致的缺失值以确保数据连续性。尽管预测和插补密切相关，但历史上它们作为独立的分支领域发展。主流范式时空图神经网络（STGNNs）虽然有效，但通常具有专用性、计算密集且泛化能力有限。与此同时，适配预训练大语言模型（LLMs）为序列建模提供了一种强大的替代方案，但现有方法提供的结构引导较弱，导致收敛不稳定且过于关注预测。为了弥补这些差距，我们提出了 U-STS-LLM，这是一个基于时空引导 LLM 的统一框架。我们的核心创新是一个动态时空注意力偏差生成器，它将持久的功能图与瞬态节点状态相结合，以显式引导 LLM 的注意力。结合通过低秩自适应（LoRA）微调的部分冻结主干网络和门控自适应融合机制，该模型实现了稳定且参数高效的适配。在统一的多任务目标下训练，U-STS-LLM 学习到了整体的数据表示。在真实蜂窝数据集上的广泛实验表明，U-STS-LLM 在长周期预测和高缺失率插补方面均达到了新的最先进性能，同时保持了卓越的训练效率和稳定性，为在结构化、非语言领域利用基础模型提供了新的蓝图。

## Abstract
The efficient operation of modern cellular networks hinges on the accurate analysis of spatio-temporal traffic data. Mastering these patterns is essential for core network functions, chiefly forecasting future load to pre-empt congestion and imputing missing values caused by sensor failures or transmission errors to ensure data continuity. While deeply connected, forecasting and imputation have historically evolved as separate sub-fields. The dominant paradigm, Spatio-Temporal Graph Neural Networks (STGNNs), while effective, are often specialized, computationally intensive, and exhibit limited generalization. Concurrently, adapting large pre-trained language models (LLMs) offers a powerful alternative for sequence modeling, yet existing approaches provide weak structural guidance, leading to unstable convergence and a narrow focus on forecasting. To bridge these gaps, we propose U-STS-LLM, a unified framework built on a spatio-temporally steered LLM. Our core innovation is a Dynamic Spatio-Temporal Attention Bias Generator that synthesizes a persistent functional graph with transient nodal states to explicitly steer the LLM's attention. Coupled with a partially frozen backbone tuned via Low-Rank Adaptation (LoRA) and a Gated Adaptive Fusion mechanism, the model achieves stable, parameter-efficient adaptation. Trained under a unified multi-task objective, U-STS-LLM learns a holistic data representation. Extensive experiments on real-world cellular datasets demonstrate that U-STS-LLM establishes new state-of-the-art performance in both long-horizon forecasting and high-missing-rate imputation, while maintaining remarkable training efficiency and stability, offering a novel blueprint for harnessing foundation models in structured, non-linguistic domains.

---

## 论文详细总结（自动生成）

这是一份关于论文《U-STS-LLM: A Unified Spatio-Temporal Steered Large Language Model for Traffic Prediction and Imputation》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：如何在统一的框架下同时解决蜂窝网络流量数据的**长程预测（Forecasting）**与**高缺失率插补（Imputation）**问题。
*   **研究动机**：
    *   **传统模型局限**：现有的时空图神经网络（STGNNs）通常针对特定任务设计，计算开销大且泛化能力弱。
    *   **LLM 适配挑战**：虽然大语言模型（LLMs）在序列建模上表现强大，但在处理非语言的时空数据时，缺乏显式的结构引导，导致训练不稳定、收敛慢，且大多仅关注预测任务。
    *   **任务关联性**：预测和插补本质上都依赖于对底层时空动态规律的理解，将两者统一建模有助于学习更稳健的数据流形表示。

### 2. 方法论：核心思想与关键技术
U-STS-LLM 的核心思想是**“时空引导”**，即通过显式的结构偏差来约束和导向 LLM 的注意力机制。
*   **动态时空注意力偏差生成器（Dynamic Spatio-Temporal Attention Bias Generator）**：
    *   利用历史流量剖面的相似性构建**静态功能图**。
    *   结合当前输入的瞬态节点状态生成**动态因子**。
    *   通过两者合成注意力偏差矩阵 $\tilde{B}$，直接注入 LLM 的注意力层，引导模型关注具有物理或功能关联的节点。
*   **部分冻结主干与 LoRA 微调（PFSTA）**：
    *   采用预训练的 GPT-2 作为骨干，冻结底层以保留通用的序列推理能力。
    *   仅对顶层模块进行微调，并引入 **LoRA（低秩自适应）** 技术，极大地减少了可训练参数量。
*   **门控自适应融合（Gated Adaptive Fusion）**：
    *   并行生成一个基于 GRU/ARGRU 的**初步引导信号（Guidance Signal）**。
    *   将 LLM 提取的深层上下文特征与该初步信号进行门控融合，使模型只需学习残差修正，增强了训练稳定性。
*   **统一多任务优化**：使用加权损失函数同时监督预测和插补任务，强制模型学习通用的时空特征。

### 3. 实验设计
*   **数据集**：
    *   **Milan（米兰）电信数据集**：用于主要的训练、验证和测试。
    *   **Trento（特伦托）电信数据集**：专门用于 **Zero-shot（零样本）跨区域迁移实验**。
*   **任务场景**：
    *   **预测**：基于过去 48 小时数据预测未来 24 小时流量。
    *   **插补**：在 70%-80% 的极高缺失率下恢复原始数据。
*   **对比方法（Benchmarks）**：
    *   **基础模型**：LSTM, ConvLSTM, DLinear。
    *   **时空/时间序列前沿模型**：TimesNet, STGCN。
    *   **LLM 适配模型**：GPT4ST, GATGPT, ST-LLM, ST-LLM+。
    *   **专用插补模型**：BRITS, SAITS。

### 4. 资源与算力
*   **硬件环境**：使用单张 **NVIDIA RTX A6000 GPU**。
*   **软件环境**：Python 3.8, PyTorch 2.4.1, CUDA 11.8。
*   **训练细节**：采用了 AdamW 优化器、线性预热（Warm-up）及余弦退火学习率调度策略。文中强调了该模型具有较低的推理延迟和极高的参数效率（可训练参数远少于同类 LLM 方法）。

### 5. 实验数量与充分性
*   **实验规模**：
    *   涵盖了 5 种不同的电信业务类型（SMS-IN/OUT, Call-IN/OUT, Internet）。
    *   进行了详尽的**消融实验**，分别验证了时空嵌入（STE）、引导信号（GS）和图嵌入（GE）的必要性。
    *   包含**效率分析**（参数量 vs. 性能 vs. 延迟）。
    *   包含**可视化分析**（注意力热力图、UMAP 特征空间投影）。
*   **充分性评价**：实验设计非常充分。特别是引入了跨城市的 **Zero-shot 实验**，这在时空数据挖掘论文中属于高标准要求，有力证明了模型的泛化能力而非简单的过拟合。

### 6. 主要结论与发现
*   **性能领先**：在预测任务上，MAE 和 RMSE 平均提升约 **23.8%**；在插补任务上，平均提升约 **12%**。
*   **泛化性极强**：在 Zero-shot 实验中，U-STS-LLM 的领先优势扩大到了 **35%-40%**，说明该模型捕捉到了跨地域的通用时空演化规律。
*   **表征解耦**：UMAP 可视化显示，模型在共享参数的同时，能为预测和插补任务生成既有共性（周期性）又有特性（任务空间分离）的特征表示。
*   **效率优势**：通过部分冻结和 LoRA，模型在保持大模型性能的同时，推理速度接近轻量级模型。

### 7. 优点与亮点
*   **统一性**：成功将两个长期孤立的任务（预测与插补）整合进一个参数高效的框架。
*   **显式引导机制**：动态注意力偏差的设计巧妙地解决了 LLM 难以理解空间拓扑结构的痛点。
*   **无需物理坐标**：通过流量剖面相似性构建功能图，使模型不依赖于具体的经纬度信息，增强了可移植性。
*   **稳定性**：门控融合机制有效缓解了微调大模型时常见的收敛不稳定问题。

### 8. 不足与局限
*   **领域局限性**：实验主要集中在电信流量数据上，虽然证明了跨城市泛化，但尚未在交通流、气象预测等其他典型时空领域进行验证。
*   **对预训练模型的依赖**：模型性能高度依赖于 GPT-2 的预训练质量，对于更长序列或更复杂维度的扩展性（如使用更大的 Llama 模型）有待进一步探讨。
*   **超参数敏感性**：多任务学习中的权重系数 $\alpha$ 以及 LoRA 的秩 $r$ 可能需要针对不同数据集进行精细调优。

（完）
