---
title: Variational Smoothing and Inference for SDEs from Sparse Data with Dynamic Neural Flows
title_zh: 基于动态神经流的稀疏数据随机微分方程变分平滑与推断
authors: "Yu Wang, Arnab Ganguly"
date: 2026-05-07
pdf: "https://arxiv.org/pdf/2605.05606v1"
tags: ["query:ts"]
score: 8.0
evidence: 利用动态神经流从稀疏数据中推断SDE
tldr: 针对稀疏噪声观测下的随机微分方程（SDE）参数推断和状态平滑难题，本文提出了一种基于动态神经流的新型变分平滑方法。该方法通过神经网络学习满足Kolmogorov后向方程及观测诱导跳变条件的条件得分函数，从而构建后向时间得分诱导的后验SDE。通过推导基于似然的证据下界（ELBO），实现了状态平滑与参数估计的联合优化，在非线性系统中展现出优于传统MCMC方法的扩展性和稳定性。
selection_source: fresh_fetch
motivation: 传统的SDE平滑方法在处理稀疏观测数据时面临路径退化和扩展性差的问题。
method: 利用神经网络学习满足偏微分方程和离散贝叶斯更新跳变条件的条件后向得分函数，并结合变分EM框架进行参数估计。
result: 在非线性系统实验中，该方法仅需极少量观测即可实现准确且稳定的推断，且扩展性显著优于传统MCMC方法。
conclusion: 本研究通过将连续时间动力学与离散观测更新相结合，为复杂随机系统的状态推断和参数学习提供了一种高效的变分推断方案。
---

## 摘要
随机微分方程 (SDE) 为部分观测系统中的时间动力学建模提供了一个灵活的框架。一个核心任务是从数据中校准此类模型，这需要从稀疏、有噪声的观测中推断潜在轨迹和参数。针对该问题的经典平滑方法通常受限于路径退化和较差的可扩展性。在这项工作中，我们开发了一种新方法，该方法基于后验 SDE 的表征，即条件逆向时间得分（conditional backward-in-time score），该得分被定义为一个函数的梯度，该函数求解在观测时刻具有乘性更新的 Kolmogorov 后向方程。我们使用神经网络学习该条件得分，训练网络以同时满足控制偏微分方程 (PDE) 和观测诱导的跳跃条件，从而将连续时间动力学与离散贝叶斯更新相结合。由此产生的得分诱导出一个具有相同扩散系数但漂移项经过修正的后验 SDE，从而实现了高效的后验轨迹采样。我们进一步推导了一个基于似然的目标函数来学习 SDE 参数，为联合状态平滑和参数估计提供了一个证据下界 (ELBO)。这产生了一个变分 EM 风格的过程，其中优化神经条件得分以逼近平滑分布，随后使用来自诱导后验的样本对 SDE 参数进行最大化步骤。在非线性系统上的实验表明，在观测极少的情况下也能实现准确且稳定的推断，与经典的 MCMC 方法相比，展现出显著提高的可扩展性。

## Abstract
Stochastic differential equations (SDEs) provide a flexible framework for modeling temporal dynamics in partially observed systems. A central task is to calibrate such models from data, which requires inferring latent trajectories and parameters from sparse, noisy observations. Classical smoothing methods for this problem are often limited by path degeneracy and poor scalability. In this work, we developed a novel method based on characterization of the posterior SDE in terms of conditional backward-in-time score defined as the gradient of a function solving a Kolmogorov backward equation with multiplicative updates at observation times. We learn this conditional score using neural networks trained to satisfy both the governing PDE and the observation-induced jump conditions, thereby integrating continuous-time dynamics with discrete Bayesian updates. The resulting score induces a posterior SDE with the same diffusion coefficient but a modified drift, enabling efficient posterior trajectory sampling. We further derive a likelihood-based objective for learning the SDE parameters, yielding an evidence lower bound (ELBO) for joint state smoothing and parameter estimation. This leads to a variational EM-style procedure, where the neural conditional score is optimized to approximate the smoothing distribution, followed by a maximization step over the SDE parameters using samples from the induced posterior. Experiments on nonlinear systems demonstrate accurate and stable inference with a very few observations demonstrating significant improved scalability compared to classical MCMC methods.

---

## 论文详细总结（自动生成）

这篇论文提出了一种名为“动态神经流”（Dynamic Neural Flows）的方法，旨在解决从稀疏、有噪声的观测数据中推断随机微分方程（SDE）的潜在轨迹（平滑）和模型参数（推断）的难题。

以下是对该论文的结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：SDE 是建模不确定性动态系统的核心工具，但在实际应用中，系统状态往往只能通过离散、稀疏且含有噪声的观测获得。
*   **核心挑战**：传统的平滑方法（如 MCMC 或粒子滤波）在观测稀疏或系统高度非线性时，面临“路径退化”（path degeneracy）、混合效率低以及在大规模离散化下可扩展性差的问题。
*   **整体含义**：本文试图通过一种连续时间的变分推断框架，避免对潜在路径进行高维离散化，从而实现高效、稳定的联合状态平滑与参数估计。

### 2. 方法论
*   **核心思想**：将平滑分布（后验分布）表征为一个新的扩散过程（后验 SDE）。该后验 SDE 的扩散系数与原 SDE 相同，但其漂移项（Drift）增加了一个由“条件逆向时间得分”（conditional backward-in-time score）构成的修正项。
*   **关键技术细节**：
    *   **得分表征**：该得分定义为函数 $h = \ln w$ 的空间梯度，其中 $w$ 满足 Kolmogorov 后向方程，并在观测时刻遵循乘性更新（对应贝叶斯更新）。
    *   **神经近似**：使用一系列神经网络（每个观测间隔一个）来近似 $h$。通过物理信息神经网络（PINN）的思想，训练网络使其满足区间内的偏微分方程（PDE）残差以及观测时刻的跳跃条件（Jump Conditions）。
    *   **变分 EM 算法**：
        *   **E 步**：固定 SDE 参数，训练神经网络以逼近后验得分，从而定义一个可采样的后验 SDE。
        *   **M 步**：利用重参数化技巧从后验 SDE 中采样轨迹，通过最大化证据下界（ELBO）来更新 SDE 的模型参数。
*   **公式逻辑**：后验漂移项 $\bar{b} = b + a \nabla_x h$，其中 $h$ 通过最小化包含 PDE 残差和观测跳跃约束的损失函数 $L_{smooth}$ 来学习。

### 3. 实验设计
*   **实验场景**：
    1.  **2D Michaelis-Menten 模型**：生化反应系统，具有高度非线性。
    2.  **4D 环状耦合双阱系统（Ring-Coupled Double Well）**：多稳态非线性系统，用于测试高维扩展性。
    3.  **1D 验证实验**：使用几何布朗运动（GBM）和双阱模型，将其与有限差分法（FDM）得到的 PDE 精确解进行对比。
*   **Benchmark 与对比方法**：主要对比了经典的 **MCMC（马尔可夫链蒙特卡洛）** 方法。
*   **数据设置**：仅使用 5 个等间距的稀疏观测点，并加入高斯噪声。

### 4. 资源与算力
*   **算力设备**：文中提到该方法可以在标准计算后端运行，如 **Apple MPS** 或 **NVIDIA CUDA**。
*   **训练时长**：
    *   初始的 PINN 训练（学习得分函数）大约需要 **20 分钟**（采用学习率衰减策略）。
    *   随后的 EM 迭代通过“热启动”（warm-starting）网络参数，每步仅需 **1 分钟或更短**。
*   **网络架构**：使用了 5 层隐藏层、每层 70 个神经元的统一架构，激活函数为 tanh。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从 1D 到 4D 的多个动力学系统，包括了参数收敛曲线、轨迹重建对比以及与数值精确解的误差分析。
*   **充分性评价**：实验设计较为充分。通过 1D 实验验证了 PDE 求解的数值准确性，通过 2D 和 4D 实验验证了在复杂非线性系统下的鲁棒性。特别是在“极稀疏观测”（仅 5 个点）的极端条件下进行测试，客观地展示了该方法相对于 MCMC 的优势。

### 6. 主要结论与发现
*   **高精度与稳定性**：该方法能从极少量的观测中准确推断出 SDE 的真实参数，且收敛过程比 MCMC 更平滑。
*   **轨迹重建**：学习到的后验 SDE 能够生成与观测数据高度一致且符合物理动力学的潜在轨迹。
*   **可扩展性**：相比于随离散步长减小而性能下降的 MCMC，该方法直接在路径空间操作，对时间分辨率不敏感，展现了更好的扩展潜力。

### 7. 优点
*   **理论优雅**：利用后验 SDE 的漂移项修正理论，将贝叶斯推断转化为 PDE 求解问题。
*   **无需高维离散化**：避免了传统方法中将路径离散化为高维向量所带来的计算负担。
*   **物理一致性**：通过 PINN 损失函数强制模型遵循物理规律（SDE 结构）。

### 8. 不足与局限
*   **维度诅咒风险**：虽然在 4D 系统表现良好，但对于更高维（如几十维）的系统，求解 PDE 的神经网络可能会面临维度灾难。
*   **局部最优**：作为 EM 算法，其性能依赖于初始化，在非凸参数空间中可能陷入局部最优（文中建议通过多次初始化缓解）。
*   **超参数敏感性**：PINN 的训练通常对损失函数中各项的权重平衡较为敏感，可能需要精细调参。

（完）
