---
title: "MILM: Large Language Models for Multimodal Irregular Time Series with Informative Sampling"
title_zh: MILM：面向具有信息采样特征的多模态不规则时间序列的大语言模型
authors: "Hsing-Huan Chung, Shijun Li, Yoav Wald, Xing Han, Suchi Saria, Joydeep Ghosh"
date: 2026-05-13
pdf: "https://arxiv.org/pdf/2605.13711v1"
tags: ["query:ts"]
score: 9.0
evidence: 使用LLM处理多模态不规则时间序列
tldr: 针对医疗电子健康档案等包含异步、不规则采样数值与文本的多模态不规则时间序列（MITS），本文提出MILM模型。该模型将MITS表示为XML格式的时间三元组，并采用两阶段微调策略：先学习采样模式，再结合观测值进行联合建模。实验证明，MILM在多个数据集上表现优异，能有效利用采样频率和通道信息提升预测性能。
selection_source: fresh_fetch
motivation: 多模态不规则时间序列中的采样时间和通道模式包含重要的预测信号，但现有方法难以有效整合这些异构的数值与文本信息。
method: 将MITS表示为XML格式的三元组，并采用先训练采样模式、后训练完整数据的两阶段策略（MILM-2S）对大语言模型进行微调。
result: MILM-2S在多个EHR数据集上取得了最佳性能，特别是在数值缺失的评估场景下，其表现显著优于单阶段模型。
conclusion: 通过结构化表示和两阶段微调，大语言模型能够有效利用不规则采样模式和多模态内容来提升时间序列分类任务的准确性。
---

## 摘要
多模态不规则时间序列 (MITS) 由来自异构数值和文本通道的异步且不规则采样的观测值组成。例如，在医疗保健领域，患者的电子健康记录 (EHR) 包含不规则的化验测量值和临床笔记。观测值的不规则时间安排和通道模式与数值及文本内容一样，都携带了预测信号。鉴于大语言模型 (LLMs) 拥有跨越文本和数值领域的广泛预训练知识，它们是处理此类异构数据的理想选择。我们提出了 MILM（多模态不规则时间序列语言模型），它将 MITS 表示为可扩展标记语言 (XML) 格式的时间排序三元组，并通过两阶段策略微调 LLM 以进行 MITS 分类。第一阶段在数值被遮蔽的 MITS 上进行训练，仅根据采样模式进行预测；第二阶段在完整的 MITS 上进行训练，以联合建模采样模式和观测值。我们的两阶段模型 (MILM-2S) 及其单阶段对应模型 (MILM-Direct) 在多个 EHR 数据集上分别取得了最佳和次佳的平均性能。进一步的数值遮蔽评估证实，采样模式携带预测信号，且 MILM-2S 学会了利用这些信号。在我们引入的“数值待定”评估中（即预测时某些数值不可用），与标准评估相比，MILM-2S 领先 MILM-Direct 的幅度更大。对于 MILM-2S，将数值待定观测值的时间和通道作为额外的采样信息予以保留，进一步提高了院内死亡率预测的准确性。

## Abstract
Multimodal irregular time series (MITS) consist of asynchronous and irregularly sampled observations from heterogeneous numerical and textual channels. In healthcare, for example, patients' electronic health records (EHR) include irregular lab measurements and clinical notes. The irregular timing and channel patterns of observations carry predictive signal alongside the numerical values and textual content. LLMs are natural candidates for processing such heterogeneous data, given their extensive pretrained knowledge spanning textual and numerical domains. We introduce MILM (Multimodal Irregular time series Language Model), which represents MITS as time-ordered triplets in Extensible Markup Language (XML) format and fine-tunes an LLM through a two-stage strategy for MITS classification. The first stage trains on value-redacted MITS to predict from sampling patterns alone, and the second stage trains on full MITS to jointly model sampling patterns and observed values. Our two-stage model (MILM-2S) and its single-stage counterpart (MILM-Direct) achieve the best and second-best average performance on multiple EHR datasets. Further value redaction evaluations confirm that sampling patterns carry predictive signal and that MILM-2S learns to exploit them. In the value pending evaluation we introduce, where some values are unavailable at prediction time, MILM-2S outperforms MILM-Direct by a larger margin compared to standard evaluation. For MILM-2S, preserving the time and channel of value-pending observations as additional sampling information further improves in-hospital mortality prediction.

---

## 论文详细总结（自动生成）

这是一份关于论文《MILM: Large Language Models for Multimodal Irregular Time Series with Informative Sampling》的结构化总结：

### 1. 核心问题与整体含义
论文关注的是**多模态不规则时间序列（MITS）**的分类问题。在医疗（如EHR数据）、金融等领域，数据往往以异步、不规则频率采样，且包含数值（如化验结果）和文本（如临床笔记）两种模态。
*   **研究动机**：现有的MITS模型多依赖复杂的融合机制，而基于LLM的时间序列方法通常只处理纯数值数据。
*   **核心发现**：采样模式（即“何时”测量以及“测量了什么通道”）本身就携带了关于预测目标的“信息采样（Informative Sampling）”信号（例如：化验频率增加通常暗示病情恶化）。论文旨在探索LLM是否能同时利用这些采样模式和观测值内容。

### 2. 方法论
论文提出了 **MILM (Multimodal Irregular time series Language Model)** 框架，核心步骤如下：
*   **XML三元组表示**：将每个观测点序列化为包含 `<time>`（时间）、`<channel>`（通道/指标名称）和 `<value>`（数值或文本内容）的XML格式字符串。这种方式保留了LLM预训练时的语义理解能力，且XML标签明确了结构边界。
*   **时间排序序列化**：将所有通道的观测值按全局时间顺序排列，确保因果结构被LLM的注意力机制捕获。
*   **两阶段微调策略（MILM-2S）**：
    1.  **第一阶段（数值遮蔽微调）**：移除所有 `<value>` 标签中的内容，强制模型仅根据采样的时间和通道模式进行预测，从而学习采样过程的表征。
    2.  **第二阶段（全数据微调）**：在第一阶段基础上，使用包含数值和文本的完整数据继续微调，实现采样模式与观测值的联合建模。

### 3. 实验设计
*   **数据集**：使用了两个主流公共医疗数据库：**MIMIC-IV** 和 **eICU**。
*   **任务场景**：针对每个数据库分别设置了“院内死亡率预测（IHM）”和“住院时长预测（LOS）”两个任务，共4个基准数据集。
*   **对比方法（Baselines）**：
    *   **非LLM模型**：GRU-D, mTAND。
    *   **多模态专用模型**：UTDE, FuseMoE。
    *   **LLM时间序列模型**：VITAL, ISTS-PLM。
    *   **零样本对比**：直接使用原始 Qwen3-4B 模型。
*   **特殊评估**：引入了“数值待定（Value Pending）”评估，模拟现实中采样已完成但结果尚未录入数据库的场景。

### 4. 资源与算力
*   **硬件**：MILM模型微调使用了 **NVIDIA H200 (96GB)** GPU；基线模型运行在 **RTX 3090 (24GB)** 上。
*   **训练时长**：
    *   MILM 第一阶段训练约 200-300 分钟。
    *   MILM 第二阶段训练约 160-310 分钟。
*   **模型规模**：基于 **Qwen3-4B-Instruct**，使用 QLoRA 技术进行高效微调。

### 5. 实验数量与充分性
*   **实验规模**：在4个数据集上进行了全面对比，每个实验运行5次并报告均值和标准差。
*   **消融与验证**：
    *   对比了单阶段（MILM-Direct）与两阶段（MILM-2S）的效果。
    *   进行了“数值遮蔽评估”，验证了模型确实学到了采样模式的信号。
    *   进行了“数值待定评估”，验证了模型在信息缺失情况下的鲁棒性。
*   **充分性评价**：实验设计较为客观、公平，涵盖了从传统RNN到最新LLM-based方法的广泛对比，且针对医疗场景的特性（如storetime延迟）做了专门的现实模拟。

### 6. 主要结论与发现
*   **性能领先**：MILM-2S 在所有数据集和指标上均取得了最佳平均排名（Avg Rank 1.1），MILM-Direct 位居第二。
*   **采样模式有效性**：实验证明采样模式确实包含预测信号，且通过第一阶段的预训练，模型能显著提升对这些信号的利用率。
*   **处理缺失值的优势**：在“数值待定”场景下，MILM-2S 表现出比单阶段模型更强的泛化能力，能够通过“已知采样但未知数值”的信息提升预测精度。

### 7. 优点
*   **统一表征**：通过XML格式巧妙地将数值、文本和时间结构统一在LLM的自然语言空间内，无需复杂的模态对齐模块。
*   **两阶段策略**：针对“信息采样”特征设计的微调策略非常直观且有效，解决了数值信号可能掩盖采样模式信号的问题。
*   **现实意义**：提出的“数值待定”评估非常贴合临床实际，具有较高的应用参考价值。

### 8. 不足与局限
*   **计算成本**：相比于轻量级的传统模型（如GRU-D），基于LLM的微调和推理需要极高的算力资源（H200等）。
*   **模态限制**：目前仅处理了数值和文本，尚未扩展到医学影像（如X光片）等视觉模态。
*   **数据偏差**：实验仅限于美国EHR数据集，不同国家的医疗采样习惯（采样政策）可能存在差异，模型的跨国泛化性有待验证。
*   **上下文长度限制**：对于极长的时间序列，LLM的上下文窗口可能成为瓶颈。

（完）
