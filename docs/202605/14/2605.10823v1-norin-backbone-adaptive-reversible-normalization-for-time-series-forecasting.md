---
title: "NoRIN: Backbone-Adaptive Reversible Normalization for Time-Series Forecasting"
title_zh: NoRIN：面向时间序列预测的骨干网络自适应可逆归一化
authors: "Shun Zhang, Yuyang Xiao"
date: 2026-05-11
pdf: "https://arxiv.org/pdf/2605.10823v1"
tags: ["query:ts"]
score: 8.0
evidence: 最先进的预测归一化技术
tldr: 针对时间序列预测中现有可逆归一化方法（如RevIN）仅能进行线性变换、无法处理重尾和偏态分布的问题，本文提出NoRIN。NoRIN利用Johnson SU变换引入非线性形状参数，并通过解耦优化策略（分位数拟合与贝叶斯优化）解决了梯度下降导致的参数退化问题。实验证明，不同模型架构对数据分布形状有不同偏好，NoRIN能显著提升多种主流预测模型的性能。
selection_source: fresh_fetch
motivation: 现有的线性可逆归一化方法无法修正时间序列中的重尾和偏态分布，限制了高容量模型对复杂分布的建模能力。
method: 提出基于Johnson SU变换的非线性归一化方法NoRIN，并采用分位数初始化与贝叶斯优化相结合的策略来独立优化形状参数。
result: 在90种实验配置下，NoRIN找到的最优形状参数显著优于线性变换，且表现出明显的模型架构依赖性。
conclusion: 通过非线性变换和解耦优化，NoRIN能为不同架构的预测模型提供更适配的数据分布，有效提升了时间序列预测的准确性。
---

## 摘要
可逆实例归一化 (RevIN) 及其后续方法 (Dish-TS, SAN, FAN) 已成为时间序列预测的事实标准插件，然而它们应用于每个数据点的映射是严格仿射的，$x \mapsto ax+b$，因此无法重塑底层分布——重尾特征依然存在，偏度也未得到修正。我们提出了 NoRIN，一种基于 arcsinh 形式 Johnson $S_U$ 变换的非线性可逆归一化，具有两个控制尾部特征和偏度的形状参数 $(\delta,\varepsilon)$；RevIN 所使用的线性 $Z$-score 仅在 $\delta\to \infty$ 的极限情况下得以恢复。通过梯度下降将 $(\delta,\varepsilon)$ 与骨干网络联合训练，会在几个 epoch 内可靠地将它们推向这一线性极限——我们将这一现象称为退化问题 (degeneration problem)：预测损失在局部对形状不敏感，且高容量的骨干网络会补偿其输入的任何单调重参数化。NoRIN 通过将形状选择与梯度训练解耦来避免退化：$(\delta,\varepsilon)$ 通过闭式 Slifker-Shapiro 分位数拟合进行初始化，并根据验证集目标通过贝叶斯优化进行精炼，而内部训练循环与标准的 RevIN 式训练一致。在 6 个代表性骨干网络、5 个真实世界数据集和 3 个预测跨度（共 90 种配置）的实验中，解耦的形状优化恢复出的 $(\delta^\star,\varepsilon^\star)$ 系统性地远离线性极限，且其取值随骨干网络的不同而变化。这从经验上支持了本文的核心论点：不同的骨干网络确实需要不同的归一化参数才能达到其最佳性能。

## Abstract
Reversible instance normalization (RevIN) and its successors (Dish-TS, SAN, FAN) have become the de facto plug-in for time-series forecasting, yet the map they apply to each data point is strictly affine, $x \mapsto ax+b$, so they cannot reshape the underlying distribution -- heavy tails remain heavy and skewness remains uncorrected. We propose NoRIN, a non-linear reversible normalization based on the arcsinh-form Johnson $S_U$ transform with two shape parameters $(δ,\varepsilon)$ that control tailedness and skewness; the linear $Z$-score used by RevIN is recovered only in the limit $δ\to \infty$. Training $(δ,\varepsilon)$ jointly with the backbone via gradient descent reliably pushes them toward this linear limit within a few epochs -- a phenomenon we name the degeneration problem: the forecasting loss is locally indifferent to shape, and the high-capacity backbone compensates for any monotone reparameterization of its input. NoRIN escapes the degeneration by decoupling shape selection from gradient training: $(δ,\varepsilon)$ are initialized by a closed-form Slifker-Shapiro quantile fit and refined by Bayesian optimization on the validation objective, while the inner training loop is identical to standard RevIN-style training. Across six representative backbones x five real-world datasets x three prediction horizons (90 configurations), decoupled shape optimization recovers $(δ^\star,\varepsilon^\star)$ that sit systematically far from the linear limit, with values that vary in a backbone-dependent way. This empirically supports the central thesis: different backbones genuinely require different normalization parameters to reach their best performance.

---

## 论文详细总结（自动生成）

这是一份关于论文《NoRIN: Backbone-Adaptive Reversible Normalization for Time-Series Forecasting》的结构化深度总结：

### 1. 论文的核心问题与整体含义
*   **研究动机**：在深度时间序列预测中，可逆实例归一化（如 RevIN）已成为处理非平稳性的标准配置。然而，现有方法（RevIN, Dish-TS, SAN, FAN 等）本质上都是**仿射变换**（$x \mapsto ax + b$），只能调整均值和方差，无法改变数据的分布形状。这意味着现实数据中的**重尾（Heavy tails）**和**偏态（Skewness）**特征会原封不动地进入模型，限制了预测精度。
*   **核心问题**：如何引入非线性变换来重塑分布，并解决非线性参数在端到端梯度训练中容易“退化”回线性状态的难题。

### 2. 论文提出的方法论
*   **核心思想**：引入基于 **Johnson $S_U$ 变换**的非线性可逆归一化（NoRIN），通过非线性映射将复杂分布转化为近似高斯分布，并采用**解耦优化策略**防止参数退化。
*   **关键技术细节**：
    *   **Johnson $S_U$ 变换**：使用 $Z = \varepsilon + \delta \operatorname{arcsinh}(\frac{X - \lambda}{\xi})$ 公式。其中 $\lambda$ 和 $\xi$ 是位置和缩放参数（采用中位数和 MAD 等鲁棒统计量），而 $\delta$ 和 $\varepsilon$ 是控制尾部厚度和偏度的形状参数。
    *   **退化问题（Degeneration Problem）**：作者发现，若将形状参数与骨干网络联合训练，梯度下降会迅速将 $\delta$ 推向无穷大，使变换退化为线性 $Z$-score。这是因为高容量模型倾向于通过内部权重补偿输入的变化，寻找最易优化的“高斯角落”。
    *   **解耦形状优化（Decoupled Shape Optimization）**：
        1.  **初始化**：利用闭式 Slifker–Shapiro 分位数拟合法快速估计初始形状参数。
        2.  **外部优化**：将形状参数视为超参数，利用贝叶斯优化（基于 Optuna 的 TPE 或 GP 采样器）在验证集上寻找最优解。
        3.  **内部训练**：在形状参数固定的情况下，按常规方式训练预测模型。

### 3. 实验设计
*   **数据集**：涵盖 5 个真实世界主流基准数据集：Exchange, ETTh1, ETTh2, ETTm1, ETTm2。
*   **骨干网络（Backbones）**：选择了 6 种具有代表性的模型：Informer, PatchTST, iTransformer, DLinear, TimesNet, FEDformer。
*   **对比方法（Baselines）**：
    *   无归一化（None）
    *   线性可逆归一化：RevIN, SAN, Dish-TS, DeStat。
*   **实验配置**：针对每个模型和数据集，测试了 3 个预测跨度（90, 336, 720），共计 90 种实验配置。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号和单次训练时长。
*   **实验规模推算**：文中提到共进行了 1,620 次完整的训练运行（90 种配置 × 6 种归一化方法 × 3 个随机种子）。此外，每个 NoRIN 配置还涉及 60-100 次贝叶斯优化试验。这表明该研究具有相当大的计算工作量。

### 5. 实验数量与充分性
*   **实验规模**：进行了 90 种配置下的对比实验，并附带了详尽的消融实验、敏感性分析（针对学习率、Batch Size、模型维度等）以及重复性验证。
*   **充分性与客观性**：
    *   使用了 **Wilcoxon 符号秩检验**进行统计显著性分析（$p < 10^{-14}$），证明了改进的普遍性。
    *   通过 10 个不同随机种子的实验验证了结果的稳定性（变异系数低于 1%）。
    *   实验覆盖了从线性模型到 Transformer 架构的多种骨干网络，结果具有较强的说服力和客观性。

### 6. 论文的主要结论与发现
*   **性能提升**：NoRIN 在所有测试配置中显著优于所有线性归一化基准。
*   **骨干网络偏好**：不同的模型架构对数据分布形状有显著不同的偏好。例如，基于注意力的模型（如 Informer）偏好较大的 $\delta$（较轻的尾部压缩），而线性模型（DLinear）和 PatchTST 则偏好极小的 $\delta$（强力的尾部压缩）。
*   **形状参数的重要性**：最优形状参数系统性地远离线性极限，证明了非线性重塑分布的必要性。

### 7. 优点（亮点）
*   **打破线性瓶颈**：首次指出并解决了可逆归一化中长期存在的“仅限仿射变换”的局限性。
*   **诊断“退化问题”**：深刻分析了为什么简单的非线性扩展在端到端训练中会失效，并给出了有效的解耦优化方案。
*   **即插即用**：NoRIN 作为一个模块，可以无缝集成到任何现有的时间序列预测模型中，且不改变模型内部结构。

### 8. 不足与局限
*   **计算开销**：由于引入了外部贝叶斯优化循环，NoRIN 的离线超参数搜索成本高于传统的 RevIN。
*   **搜索空间限制**：目前的形状参数搜索范围（如 $\varepsilon \in [-1, 1]$）在某些极端通道上可能仍显不足，存在边界触碰现象。
*   **场景覆盖**：虽然在主流长程预测任务上表现优异，但尚未在极度非平稳或事件驱动的稀疏序列（如金融高频数据、交通突发事件）上进行验证。

（完）
