---
title: A Hierarchical Agent System with Reinforcement Learning for Multivariate Time Series Data Cleaning
title_zh: 一种用于多变量时间序列数据清洗的强化学习分层智能体系统
authors: "Yuhan Shi, Yuanyuan Yao, Lu Chen, Mourad Khayati, Tianyi Li"
date: 2026-05-06
pdf: "https://arxiv.org/pdf/2605.04902v1"
tags: ["query:ts"]
score: 9.0
evidence: 清洗多变量时间序列中的缺失值
tldr: 针对多变量时间序列中缺失值、异常值和约束违规等多种质量问题并存的挑战，本文提出了一种基于强化学习的分层智能体系统。该系统将清洗过程建模为处理顺序与模型选择的联合优化问题，通过高层智能体决策顺序、底层智能体选择算法，并结合无需地面真值的双阶段奖励机制，实现了高效的自动化数据修复。实验表明，该方法在清洗质量和下游任务表现上均显著优于现有方案。
selection_source: fresh_fetch
motivation: 现有的时间序列清洗方法通常只能处理单一类型的质量问题，且高度依赖在现实中难以获取的地面真值或领域规则。
method: 采用分层强化学习架构，由高层智能体确定质量问题的处理顺序，底层智能体选择最合适的清洗模型，并利用双阶段奖励机制进行优化。
result: "实验结果显示，该系统在数据清洗质量上提升了高达96%，同时在下游分析任务的性能上改进了27%。"
conclusion: 该分层智能体系统为复杂多变量时间序列的自动化清洗提供了一种高效、通用且无需地面真值的解决方案。
---

## 摘要
多变量时间序列（MTS）经常受到并发质量问题的影响，如缺失值、离群值和约束违规，这些问题严重损害了下游分析。现有的清洗方法仅能修复有限的一组此类问题，因此不适用于多种质量问题同时出现的场景。此外，这些方法通常依赖于地面真值（ground truth）数据或特定领域的规则，而这两者在实际应用中往往难以获取。在本文中，我们介绍了 \sys，这是一个采用强化学习的智能体系统，旨在清洗 MTS 中的多种数据质量问题。我们将清洗过程建模为一个联合优化问题，同时处理质量问题的处理顺序和清洗模型的选择，从而在庞大的候选清洗流水线空间中进行高效搜索。我们的框架依赖于一种分层智能体架构，其中高层智能体决定处理数据质量问题的顺序，而底层智能体则为每个问题识别最合适的清洗方法。为了引导智能体找到最优清洗流水线，我们提出了一种双阶段奖励机制，将上游（清洗）和下游性能耦合起来，从而在不依赖地面真值的情况下实现有效优化。我们的实验结果表明，\sys 一贯优于现有方法，在数据清洗质量上提升了高达 96%，在下游性能上提升了 27%。

## Abstract
Multivariate time series (MTS) are frequently affected by co-occurring quality issues, such as missing values, outliers, and constraint violations, which significantly undermine downstream analytics. Existing cleaning approaches fix only a limited set of such issues, making them ill-suited for scenarios where multiple quality problems arise simultaneously. Furthermore, these methods commonly depend on the availability of ground truth data or domain-specific rules, both of which are rarely accessible in real-world applications.   In this paper, we introduce \sys, an agent system with reinforcement learning designed to clean multiple data quality issues in MTS. We cast the cleaning process as a joint optimization problem that simultaneously handles quality issue order and cleaning model selection, allowing efficient navigation of the large space of possible cleaning pipelines. Our framework relies on a hierarchical agent architecture, where a high-level agent determines the order in which data quality issues should be processed, while a low-level agent identifies the most suitable cleaning method for each issue. To guide the agent toward an optimal cleaning pipeline, we propose a dual-stage reward mechanism that couples upstream (cleaning) and downstream performance, enabling effective optimization without relying on ground truth. Our experimental results show that \sys consistently outperforms existing methods, achieving up to 96\% improvement in data cleaning quality and 27\% improvement in downstream performance.

---

## 论文详细总结（自动生成）

以下是对论文《AegisTS: A Hierarchical Agent System with Reinforcement Learning for Multivariate Time Series Data Cleaning》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：多变量时间序列（MTS）在现实中常面临多种质量问题并存（如缺失值、离群值、重复值和约束违规），而现有清洗方法通常只针对单一问题，且清洗顺序（Pipeline）对最终效果影响巨大。
*   **研究动机**：
    1.  **组合爆炸**：在众多清洗算子和可能的执行顺序中寻找最优流水线是一个指数级复杂度的搜索问题。
    2.  **缺乏地面真值（Ground Truth）**：现实场景中很难获得干净的原始数据来评估清洗效果。
    3.  **依赖性挑战**：简单的算子堆叠可能破坏时间序列内在的特征关联和时间依赖性。

### 2. 方法论
AegisTS 将数据清洗建模为一个**分层强化学习（Hierarchical RL）**过程，核心组件包括：
*   **数据质量检测器**：
    *   **约束挖掘**：利用 MAD（中值绝对偏差）和多项式回归从脏数据中鲁棒地提取时间约束（列向）和跨变量约束（行向）。
    *   **异常检测模型选择**：使用 FMMS 自动为当前数据集匹配最适合的异常检测模型。
*   **分层智能体架构**：
    *   **高层智能体（High-Level）**：负责战略决策，决定当前应优先处理哪种质量问题（缺失、异常、约束或终止）。
    *   **底层智能体（Low-Level）**：负责战术执行，从对应的算子库（共包含 48 个专业清洗算子）中选择具体的算法。
*   **双阶段奖励机制**：
    *   **密集奖励（Dense Reward）**：由轻量级代理模型提供，结合数据质量指标（如平滑度、修改距离、问题减少率）给予即时反馈。
    *   **稀疏奖励（Sparse Reward）**：在流水线完成后，由复杂的下游目标模型提供最终性能增益反馈。
*   **搜索优化**：提出“依赖感知搜索剪枝”，例如发现空值时强制优先执行填充，以缩小搜索空间。

### 3. 实验设计
*   **数据集**：涵盖电力（ETTh1）、设备监控（IDF_OilTemp）、手语识别（Libras）和手写识别（Handwriting）。
*   **下游任务场景**：预测（Forecasting）、分类（Classification）和聚类（Clustering）。
*   **Benchmark 与对比方法**：
    *   **专业清洗工具**：EDITOR、Clean4TSDB。
    *   **流水线生成方法**：DiffPrep（适配后的版本）、随机采样（Sampling）。
*   **评估指标**：上游指标（F1、NMSE、RRA）和下游任务指标（NRMSE、CC、Macro F1、ROC AUC、Silhouette Score 等）。

### 4. 资源与算力
*   **硬件环境**：Intel Core i9-10900K CPU，128 GB RAM，**NVIDIA RTX 3090 GPU (24 GB VRAM)**。
*   **运行耗时**：论文详细记录了不同任务的训练+推理总耗时。例如，在 ETTh1 数据集上约为 2016 秒，在 Handwriting 数据集上约为 21674 秒。相比单智能体（Single Agent）方案，分层架构使效率提升了 1.9 到 2.8 倍。

### 5. 实验数量与充分性
*   **实验规模**：在 4 个数据集上针对 3 种不同下游任务进行了全面测试。
*   **消融实验**：验证了分层架构、下游任务引导、质量指标反馈等组件的必要性。
*   **泛化性测试**：进行了**跨数据集迁移实验**（如在 ETTh1 上训练的智能体直接用于清洗 IDF_OilTemp），证明了智能体学到的是通用的清洗策略而非简单的数值拟合。
*   **参数敏感性**：对奖励函数中的 7 个超参数进行了详细的敏感性分析。
*   **评价**：实验设计全面、客观，通过跨任务和跨数据集的对比，充分验证了系统的鲁棒性。

### 6. 主要结论
*   **性能领先**：AegisTS 在所有任务中均优于基准方法，数据清洗质量（NMSE）提升最高达 96%，下游任务性能提升最高达 27%。
*   **顺序至关重要**：实验证实了不同的清洗顺序会导致截然不同的下游结果，自动化搜索是必要的。
*   **无需真值可行性**：通过结合数据内在约束和下游任务反馈，可以在没有干净参考数据的情况下训练出高效的清洗策略。

### 7. 优点
*   **端到端自动化**：首次实现了针对 MTS 多种质量问题的自动化流水线构建。
*   **分层解耦**：将复杂的联合优化分解为“问题诊断”和“算子选择”，显著降低了搜索难度。
*   **任务驱动**：清洗过程直接服务于下游分析目标，避免了“为了清洗而清洗”导致的特征丢失。
*   **可扩展性**：算子库是可扩展的，可以轻松集成新的清洗算法。

### 8. 不足与局限
*   **冷启动开销**：虽然推理很快，但针对新任务的初始训练仍需一定时间（尤其是涉及复杂下游模型时）。
*   **算子依赖性**：系统的上限受限于底层算子库的质量，如果所有候选算子在特定极端分布下都失效，智能体也无法产生好的结果。
*   **应用限制**：目前主要针对离线批处理数据，对于极高频率的实时流数据清洗，其决策延迟可能仍需进一步优化。

（完）
