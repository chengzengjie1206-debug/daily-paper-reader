---
title: Prediction of Magnetic Flux Evolution During Solar Active Region Emergence using Long Short-Term Memory Networks
title_zh: 基于长短期记忆网络预测太阳活动区浮现过程中的磁通量演化
authors: "Eren Dogan, Spiridon Kasapis, Sarang Patil, Jonas Tirona, John Stefan, Irina Kitiashvili, Mengjia Xu, Alexander Kosovichev"
date: 2026-04-03
pdf: "https://arxiv.org/pdf/2604.03507v1"
tags: ["query:ts"]
score: 9.0
evidence: 使用LSTM和编码器-解码器模型进行一维时间序列预测
tldr: 太阳活动区是空间天气事件的主要驱动力，其早期预测对预报系统至关重要。本研究开发了基于LSTM的机器学习模型，利用连续谱强度和太阳振荡功率图的1D时间序列数据，预测活动区浮现过程中的磁通量演化。通过对比发现，较简单的MagFluxLSTM模型在泛化能力和稳定性上优于复杂的编码器-解码器结构，能够在12小时预测窗口内提前3-10小时预警磁通量的浮现。
selection_source: fresh_fetch
motivation: 为了提高空间天气预报的准确性，需要开发能够提前预测太阳活动区磁通量演化的有效模型。
method: 利用53个活动区的连续谱强度和多频段振荡功率图作为输入，对比了标准LSTM与带有教师强制的编码器-解码器LSTM模型的性能。
result: MagFluxLSTM在超参数筛选中表现更优，且在实验和运行环境下均能提前3-10小时预测磁通量的浮现。
conclusion: 简单的LSTM架构在太阳磁通量预测任务中比复杂的编码器-解码器结构具有更好的泛化能力和训练稳定性。
---

## 摘要
太阳活动区（ARs）是空间天气事件的主要驱动源，因此对其进行早期预测对于业务预报系统至关重要。我们开发了机器学习模型，利用53个活动区及其周围宁静区的一维连续谱强度和太阳振荡功率图时间序列，预测活动区浮现期间的磁通量演化。每个观测变量都在固定的30.66°x30.66°视场内进行采样。这些观测捕捉了每个活动区的时间演化，并作为训练和验证我们的 MagFluxLSTM 和 MagFluxEnc-Dec 模型的输入。MagFluxLSTM 架构实现了一个单阶段的标准长短期记忆（LSTM）网络。MagFluxEnc-Dec 代表一个带有教师强制（teacher forcing）的 LSTM 编码器-解码器。为了测试和评估模型性能，我们使用连续谱强度和振荡功率图（由多普勒速度计算的多个频段）作为输入来预测磁通量。在按验证集导数均方根误差（RMSE）排名的前100个超参数配置中，98% 对应于 MagFluxLSTM，而 MagFluxEnc-Dec 仅占 2%。因此，尽管 MagFluxEnc-Dec 架构具有更高的模型复杂度，但与更简单的 MagFluxLSTM 相比，它对训练集之外的活动区泛化能力较差，且训练稳定性较低。MagFluxLSTM 在实验和业务型设置下，针对5个测试活动区，能够在12小时的预测窗口内提前3-10小时预测磁通量浮现。

## Abstract
Solar active regions (ARs) are the primary drivers of space weather events, making their early prediction crucial for operational forecasting systems. We develop machine learning models capable of predicting the evolution of magnetic flux during AR emergence using 1D time series of the continuum intensity and solar oscillation power maps for 53 active regions and their surrounding quiet-Sun areas. Each observable is sampled over a fixed 30.66°x30.66° field of view. These observations capture the temporal evolution of each active region and serve as inputs for training and validation of our MagFluxLSTM and MagFluxEnc-Dec models. The MagFluxLSTM architecture implements a single-stage standard Long-Short Term Memory (LSTM) network. MagFluxEnc-Dec represents an LSTM encoder-decoder with teacher forcing. To test and evaluate the models' performance, we use the continuum intensity and oscillation power maps (calculated for several frequency bands from Doppler velocity) as input to predict the magnetic flux. Among the top 100 hyperparameter configurations ranked by validation derivative RMSE, 98% correspond to MagFluxLSTM, compared to only 2% for MagFluxEnc-Dec. Thus, although the MagFluxEnc-Dec architecture has higher model complexity, it leads to poorer generalization to ARs outside the training set and less stable training than the simpler MagFluxLSTM, which can predict magnetic flux emergence 3-10 hours in advance within a 12-hour prediction window in both experimental and operational-type settings for the 5 testing active regions.

---

## 论文详细总结（自动生成）

以下是对论文《Prediction of Magnetic Flux Evolution During Solar Active Region Emergence using Long Short-Term Memory Networks》的结构化深入总结：

### 1. 核心问题与整体含义
*   **研究动机**：太阳活动区（AR）是空间天气事件（如耀斑、日冕物质抛射）的源头。现有的预报系统往往在磁场已经显现后才能做出反应。
*   **核心问题**：如何利用磁场浮现前的日震学特征（声学功率变化）和连续谱强度，在磁通量到达光球层之前，提前预测其演化过程。
*   **整体含义**：本研究旨在开发一种基于深度学习的自动化工具，通过分析太阳表面的微小扰动，为空间天气预报提供更长的预警提前量。

### 2. 方法论
*   **核心思想**：将太阳活动区的浮现建模为多变量时间序列预测问题，利用长短期记忆网络（LSTM）捕捉非线性的时间依赖关系。
*   **关键技术细节**：
    *   **输入特征**：包含连续谱强度（$I_c$）和四个不同频率段（2-6 mHz）的声学功率（$P_a$）。
    *   **模型架构**：对比了两种架构：
        1.  **MagFluxLSTM**：标准的堆叠式多层 LSTM，采用单步预测（One-shot）输出未来 12 小时的序列。
        2.  **MagFluxEnc-Dec**：编码器-解码器结构，在训练阶段引入**教师强制（Teacher Forcing）**机制，即以一定概率使用真实值而非预测值作为下一步的输入，以稳定训练。
    *   **混合损失函数**：这是本文的关键创新。公式为 $L_{hybrid} = \alpha L_{mse} + (1 - \alpha) L_{derivative}$。其中 $L_{derivative}$ 惩罚预测序列与真实序列之间一阶差分（即时间导数）的差异。这迫使模型不仅关注数值大小，更关注磁通量爆发性增长的“时刻”。
    *   **数据处理**：使用滑动窗口（110小时输入，预测未来12小时），并对数据进行 Min-Max 归一化（仅基于训练集拟合）。

### 3. 实验设计
*   **数据集**：使用 SolARED 数据集，包含 2010 年至 2023 年间由 SDO/HMI 观测到的 53 个太阳活动区。
*   **场景划分**：41 个 AR 用于训练，7 个用于验证，5 个完全独立的 AR 用于最终测试。
*   **Benchmark 与对比**：
    *   对比了 MagFluxLSTM 与 MagFluxEnc-Dec 两种架构。
    *   对比了混合损失函数与纯均方误差（MSE）损失函数的表现。
    *   以观测到的磁通量实际浮现时刻（定义为导数超过阈值并持续4小时）作为基准。

### 4. 资源与算力
*   **硬件环境**：使用了 NVIDIA GPU 进行 CUDA 加速计算。
*   **软件框架**：PyTorch 2.7.0，并利用 **Ray Tune** 库进行分布式超参数优化。
*   **算力细节**：文中提到使用了新泽西理工学院（NJIT）的高性能计算（HPC）设施，但未明确给出具体的 GPU 型号数量及总训练时长。

### 5. 实验数量与充分性
*   **实验规模**：研究进行了大规模的超参数搜索（Hyperopt 搜索），并对排名前 100 的模型配置进行了统计分析。
*   **充分性**：实验涵盖了从 2010 年到 2023 年跨越一个完整太阳周期的样本，具有较好的时间代表性。
*   **客观性**：通过严格的训练/验证/测试集划分，并采用 ASHA 调度器进行早期停止，确保了模型评估的公正性，避免了过拟合。

### 6. 主要结论与发现
*   **架构优劣**：简单的 **MagFluxLSTM** 显著优于复杂的编码器-解码器结构。在最优的 100 个配置中，98% 属于 MagFluxLSTM。这表明在样本量有限（53个AR）的情况下，参数较少的模型泛化能力更强。
*   **预测能力**：MagFluxLSTM 配合混合损失函数，能够在实验环境下提前 **3-10 小时** 预测出磁通量的显著浮现。
*   **损失函数作用**：引入导数损失项显著提升了模型对磁场爆发起始点的敏感度，减少了预警延迟。

### 7. 优点
*   **物理启发式损失**：通过在损失函数中加入导数项，将物理上的“浮现特征”直接融入模型优化目标。
*   **端到端预测**：直接从易观测的声学功率预测难以提前观测的磁通量演化，具有极强的业务应用潜力。
*   **严谨的验证**：使用了专门的日震学数据集 SolARED，并对业务化场景（考虑数据处理延迟）进行了模拟评估。

### 8. 不足与局限
*   **空间信息缺失**：模型目前将 9x9 网格中的每个 tile 视为独立的时间序列，忽略了活动区内部不同区域之间的空间相关性。
*   **样本量瓶颈**：53 个活动区对于深度学习而言规模仍然较小，这限制了更复杂架构（如 Transformer 或 3D-CNN）的发挥。
*   **业务延迟风险**：在实际业务操作中，数据处理（如多普勒图转声学功率图）存在约 4 小时的固有延迟，这会进一步压缩实际的预警窗口。

（完）
