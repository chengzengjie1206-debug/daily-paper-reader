---
title: Systematic Integration of Digital Twins and Constrained LLMs for Interpretable Cyber-Physical Anomaly Detection
title_zh: 数字孪生与受约束大语言模型的系统集成，用于可解释的信息物理系统异常检测
authors: "Konstantinos E. Kampourakis, Vasileios Gkioulos, Sokratis Katsikas"
date: 2026-04-04
pdf: "https://arxiv.org/pdf/2604.03790v1"
tags: ["query:ts"]
score: 9.0
evidence: 可解释的网络物理异常检测
tldr: 针对工业控制系统（ICS）日益复杂的网络攻击，本文提出一种结合数字孪生（DT）与受限大语言模型（LLM）的混合检测方法。该方法利用DT同步物理过程并提取特征，通过确定性启发式算法识别已知攻击模式，并在不确定时引入受限LLM进行语义推理。在SWaT数据集上的实验表明，该系统能精准定位攻击，具有低延迟和零误报率，显著提升了异常检测的可解释性与可靠性。
selection_source: fresh_fetch
motivation: 传统的异常检测器缺乏将底层行为线索与高层语义解释相结合的能力，难以应对复杂的ICS网络攻击。
method: 提出一种由数字孪生驱动的混合架构，结合确定性启发式算法与受限JSON模式约束的LLM推理，并辅以语义过滤和时间平滑层。
result: 在SWaT场景下的评估显示，该方法实现了精准的攻击定位、极低的检测延迟，且在良性区域达到了零误报。
conclusion: 数字孪生引导的LLM推理为工业控制系统提供了一种可靠且具有高度可解释性的异常检测新途径。
---

## 摘要
针对工业控制系统（ICS）的网络攻击日益复杂且难以识别。检测此类攻击需要将底层行为线索与高层语义解释相结合，而这正是传统异常检测器所缺乏的能力。本文提出了一种数字孪生（DT）驱动的混合检测方法，该方法结合了确定性启发式算法与系统化的受约束大语言模型（LLM）推理，以实现实时事件检测。数字孪生维护了安全水处理（SWaT）过程的同步且特征丰富的表示，并推导出行为描述符。启发式算法识别欺骗、阀门强制、拒绝服务和偏差漂移的特征签名，而 LLM 仅在启发式算法无法判定时被调用。受约束的 JSON 模式和语义合理性过滤器确保了 LLM 输出在物理上的一致性，时间平滑层则稳定了最终的决策信号。对四个典型 SWaT 攻击场景的评估表明，所提出的检测器能够精确定位每个攻击区间，具有较低的检测时间，且在评估的良性区域内实现了零误报（FP）。本地 LLaMA 模型和云端 GPT 模型的结果一致，证明了受约束混合架构的鲁棒性。研究结果强调了数字孪生引导的 LLM 推理作为一种可靠且可解释的 ICS 异常检测方法的潜力。

## Abstract
Cyber attacks targeting Industrial Control Systems (ICS) have become increasingly sophisticated and hard to identify. Detecting such attacks requires integrating low-level behavioral cues with high-level semantic interpretation, a capability that traditional anomaly detectors lack. This paper presents a Digital Twin (DT)-driven hybrid detection approach that combines deterministic heuristics with systematic, constrained Large Language Model (LLM) reasoning to achieve real-time incident detection. The DT maintains a synchronized, feature-enriched representation of the Secure Water Treatment (SWaT) process, deriving behavioral descriptors. Heuristics identify characteristic signatures of spoofing, valve forcing, denial-of-service, and bias drift, while the LLM is invoked only when heuristics abstain. A constrained JSON schema and semantic plausibility filters ensure physically consistent LLM outputs, and a temporal smoothing layer stabilizes the final decision signal. Evaluation on four canonical SWaT attack scenarios shows that the proposed detector precisely localizes each attack interval with low time-to-detect and zero False Positives (FPs) in the evaluated benign region. Results are consistent across both a local LLaMA model and a cloud-based GPT model, demonstrating the robustness of the constrained hybrid architecture. The findings highlight the potential of DT-guided LLM reasoning as a reliable and interpretable approach to ICS anomaly detection.

---

## 论文详细总结（自动生成）

### 论文总结：数字孪生与受约束大语言模型的系统集成，用于可解释的信息物理系统异常检测

#### 1. 核心问题与整体含义（研究动机和背景）
工业控制系统（ICS）如水处理、电力分配等关键基础设施，正面临日益复杂的网络攻击。这些攻击往往巧妙地隐藏在正常的物理过程波动中，传统的异常检测器（如基于统计或机器学习的方法）虽然能发现偏差，但存在两个主要痛点：
*   **缺乏语义解释性**：无法告诉操作员“为什么”这是异常，以及攻击者的意图是什么。
*   **决策不稳定**：难以区分正常的控制操作与恶意的物理篡改。
本文旨在通过集成**数字孪生（Digital Twin, DT）**的实时同步能力与**大语言模型（LLM）**的语义推理能力，构建一个既能精准检测又能提供可解释报告的混合检测框架。

#### 2. 方法论：核心思想与关键技术
该论文提出了一种五层结构的混合检测流水线：
*   **重放与同步引擎**：DT 作为一个虚拟副本，通过 HTTP 服务维护一个 30 秒的滑动窗口，实时同步物理过程的传感器和执行器状态。
*   **行为特征提取**：从窗口数据中推导高阶特征，包括一阶斜率、滚动标准差、幅度范围、平坦度（Freeze Ratio）和执行器切换频率。
*   **确定性启发式检测层**：利用专家知识预设规则。例如，通过 LIT101 水位持续上升且阀门开启的行为识别“欺骗攻击”；通过执行器频繁切换识别“阀门强制攻击”。若启发式规则匹配成功，则直接输出结果，不调用 LLM。
*   **受约束 LLM 推理引擎**：当启发式算法无法判定时调用 LLM。为防止 LLM 幻觉，采用了**严格的 JSON Schema 约束**和**语义合理性过滤器**，强制 LLM 按特定格式输出攻击策略、路径及缓解建议，并剔除物理上不可能的推论。
*   **时间平滑模块**：对检测结果进行多数投票平滑处理，消除瞬时噪声引起的抖动，确保决策信号的稳定性。

#### 3. 实验设计
*   **数据集**：使用了经典的 **SWaT（Secure Water Treatment）** 安全水处理数据集。
*   **实验场景**：涵盖了四种典型的攻击类型：传感器欺骗（Spoofing）、阀门强制（Valve Forcing）、传感器冻结（DoS）和缓慢偏差漂移（Bias Drift）。
*   **基准模型（Benchmark）**：对比了传统的**孤立森林（Isolation Forest, IF）**算法。
*   **LLM 配置**：对比了本地部署的 **LLaMA-3.1-8B** 和云端的 **GPT-4.1-mini**。

#### 4. 资源与算力
*   论文提到了使用了本地 LLaMA-3.1-8B 模型（通过兼容 OpenAI 的推理服务器运行）和 GPT-4.1-mini API。
*   **未明确说明**具体的硬件配置（如 GPU 型号、数量）以及具体的推理耗时或训练时长。由于该方法主要侧重于推理阶段的混合架构，而非从头训练模型，其算力需求主要集中在 LLM 的实时推理上。

#### 5. 实验数量与充分性
*   **实验规模**：对 1400 个时间步进行了详细评估，并额外进行了 10,000 个窗口的稳定性检查。
*   **充分性**：实验覆盖了从突发性攻击（DoS）到隐蔽性攻击（Bias Drift）的不同维度。通过对比本地与云端两种不同规模的 LLM，验证了架构的鲁棒性。
*   **公平性**：使用了统一的评估脚本和相同的系统提示词（Prompts），基准模型 IF 也经过了合理的预训练。

#### 6. 主要结论与发现
*   **高精度与零误报**：混合检测器在所有四个场景中均实现了精准定位，且在良性运行区间内达到了 **0 误报率**，远优于孤立森林（IF 产生了约 19.5% 的误报）。
*   **检测延迟（TTD）极低**：对于偏差漂移和 DoS 攻击，系统在第一个窗口内即可识别（TTD=0），其他攻击也在 1 个窗口内触发。
*   **模型无关性**：LLaMA 和 GPT 在受限架构下的表现几乎一致，说明**系统约束（启发式门控和 Schema 过滤）比 LLM 本身的规模更关键**。
*   **可解释性**：系统能够输出符合 MITRE ATT&CK 框架的 JSON 格式报告，提供攻击路径和缓解措施。

#### 7. 优点
*   **混合架构设计**：通过启发式算法过滤了大部分简单场景，极大地降低了 LLM 的调用频率和幻觉风险。
*   **强约束机制**：引入 JSON Schema 和物理语义过滤，解决了 LLM 在工业严苛环境下输出不可靠的问题。
*   **实时性与稳定性**：结合数字孪生的滑动窗口和时间平滑，使系统具备了处理实时流数据的能力。

#### 8. 不足与局限
*   **泛化性受限**：启发式规则是针对 SWaT 数据集精心调优的，迁移到其他工业领域（如电网）需要重新设计规则。
*   **物理模型缺失**：目前的数字孪生主要基于行为镜像（Behavioral Mirror），尚未集成深度的物理方程建模，可能难以发现极其复杂的物理逻辑攻击。
*   **评估范围较小**：虽然在 4 个典型场景表现优异，但对于 SWaT 数据集中几十种复杂的组合攻击尚未进行全量覆盖测试。

（完）
