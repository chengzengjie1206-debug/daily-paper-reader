---
title: A Family of Open Time-Series Foundation Models for the Radio Access Network
title_zh: 一系列面向无线接入网的开源时间序列基础模型
authors: "Ioannis Panitsas, Leandros Tassiulas"
date: 2026-04-05
pdf: "https://arxiv.org/pdf/2604.04271v1"
tags: ["query:ts"]
score: 9.0
evidence: 统一的时间序列建模多任务学习框架
tldr: 针对无线接入网（RAN）中AI模型碎片化和泛化能力差的问题，本文提出了TimeRAN，一个统一的多任务时间序列基础模型框架。通过在包含5.6亿条测量数据的大规模开源数据集TimeRAN DataPile上进行预训练，该模型能够高效适配异常检测、预测等多种任务。实验证明，TimeRAN在极少微调的情况下达到了SOTA性能，并在5G测试平台验证了其实际运行效率。
selection_source: fresh_fetch
motivation: 现有的RAN智能模型多为特定任务设计，导致模型碎片化、知识共享受限且系统复杂度高。
method: 提出了TimeRAN框架，利用轻量级时间序列基础模型和少量任务特定头，结合大规模开源数据集TimeRAN DataPile进行预训练。
result: 在异常检测、分类、预测和填补等任务中，TimeRAN仅需极少或无需微调即可达到最先进的性能。
conclusion: TimeRAN证明了基础模型在RAN领域的有效性，为构建高效、通用的无线网络智能提供了新路径。
---

## 摘要
无线接入网（RAN）正在演进为一种可编程且解耦的基础设施，越来越依赖 AI 原生算法进行优化和闭环控制。然而，目前的 RAN 智能在很大程度上仍由针对特定功能的任务专用模型构建，导致了模型碎片化、跨任务知识共享受限、泛化能力差以及系统复杂度增加。为了解决这些局限性，我们推出了 TimeRAN，这是一个用于 RAN 时间序列建模的统一多任务学习框架。TimeRAN 利用一个带有少量任务专用头的轻量级时间序列基础模型，来学习可迁移的表示，这些表示可以在有限的监督下高效地适配到各种任务中。为了实现大规模预训练，我们进一步整理并开源了 TimeRAN DataPile，这是迄今为止用于 RAN 分析的最大时间序列语料库，包含超过 35.5 万条时间序列和 5.6 亿次测量，涵盖了多种遥测源、协议层和部署场景。我们在包括异常检测、分类、预测和插值在内的一系列全面的 RAN 分析任务中对 TimeRAN 进行了评估，结果表明，在极少或无需任务专用微调的情况下，它就能达到最先进的性能。最后，我们将 TimeRAN 集成到一个概念验证 5G 测试平台中，并证明它在真实场景中能够以有限的资源需求高效运行。

## Abstract
The Radio Access Network (RAN) is evolving into a programmable and disaggregated infrastructure that increasingly relies on AI-native algorithms for optimization and closed-loop control. However, current RAN intelligence is still largely built from task-specific models tailored to individual functions, resulting in model fragmentation, limited knowledge sharing across tasks, poor generalization, and increased system complexity. To address these limitations, we introduce TimeRAN, a unified multi-task learning framework for time-series modeling in the RAN. TimeRAN leverages a lightweight time-series foundation model with few task-specific heads to learn transferable representations that can be efficiently adapted across diverse tasks with limited supervision. To enable large-scale pretraining, we further curate and open-source TimeRAN DataPile, the largest time-series corpus for RAN analytics to date, comprising over 355K time series and 0.56B measurements across diverse telemetry sources, protocol layers, and deployment scenarios. We evaluate TimeRAN across a comprehensive set of RAN analytics tasks, including anomaly detection, classification, forecasting, and imputation, and show that it achieves state-of-the-art performance with minimal or no task-specific fine-tuning. Finally, we integrate TimeRAN into a proof-of-concept 5G testbed and demonstrate that it operates efficiently with limited resource requirements in real-world scenarios.

---

## 论文详细总结（自动生成）

这篇论文介绍了一个名为 **TimeRAN** 的统一多任务学习框架，旨在为无线接入网（RAN）构建开源的时间序列基础模型。以下是对该论文的深度结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：随着 5G/6G 向 AI-RAN 演进，网络优化愈发依赖 AI。然而，当前的 RAN 智能存在“模型碎片化”问题：每个功能（如异常检测、流量预测）都需要专门设计的模型。这导致了知识共享受限、泛化能力差（换个基站就失效）以及系统维护复杂度极高。
*   **核心目标**：借鉴自然语言处理中的基础模型（Foundation Model）范式，开发一个通用的、轻量级的预训练模型，使其能够通过极少量的微调甚至零样本（Zero-shot）迁移，适配多种 RAN 遥测分析任务。

### 2. 方法论
*   **核心思想**：采用“一个编码器，多个任务头”的架构。通过在大规模未标记的 RAN 遥测数据上进行自监督预训练，学习通用的时间序列表示。
*   **关键技术细节**：
    *   **多通道分片（Multi-channel Patching）**：将多维遥测数据切分为固定长度的时间片（Patches），减少输入序列长度并捕捉局部特征。
    *   **Transformer 编码器**：基于轻量级 Transformer 架构（参考 T5 结构），利用自注意力机制捕捉跨通道的相关性和长程时间依赖。
    *   **自监督预训练**：使用**掩码时间序列建模（Masked Time-Series Modeling）**，随机遮盖 30% 的分片，让模型通过上下文重建原始数据，从而学习鲁棒的特征表示。
    *   **任务特定头**：在共享编码器之上添加轻量级线性层，支持异常检测、分类、预测和填补（Imputation）四类任务。
*   **适配策略**：支持全量微调（FF）、线性探测（LP，仅训练输出层）和零样本推理（0）。

### 3. 实验设计
*   **数据集（TimeRAN DataPile）**：论文贡献了目前最大的 RAN 时间序列语料库，包含 35.5 万条序列、5.6 亿次测量值（约 29GB），涵盖了从物理层到应用层的多种协议遥测。
*   **Benchmark 任务**：
    *   **异常检测**：如干扰检测、网络故障识别。
    *   **分类**：如移动性状态识别、业务类型识别、拥塞检测。
    *   **预测**：如 PRB 利用率预测、吞吐量预测。
    *   **填补**：修复缺失的 KPI 测量值。
*   **对比方法**：
    *   **基础模型**：MOMENT。
    *   **专用 Transformer 模型**：Autoformer, Informer, TimesNet。
    *   **传统深度学习**：1D-CNN, LSTM。
    *   **统计模型**：ARIMA, ETS。

### 4. 资源与算力
*   **硬件环境**：使用了高性能计算平台，配备 AMD Ryzen Threadripper 5975WX 处理器。
*   **GPU 算力**：使用了 **NVIDIA RTX 3090、RTX 4090 和 RTX A6000** 进行训练和推理评估。
*   **模型规模**：提供了三种变体：Small (40M 参数)、Base (125M 参数) 和 Large (385M 参数)。
*   **训练细节**：采用 Adam 优化器，Batch size 为 2048，使用混合精度（bfloat16）和梯度检查点技术以优化内存。具体训练总时长未明确说明，但提到模型仅需训练一个 Epoch 即可收敛。

### 5. 实验数量与充分性
*   **实验规模**：在 20 多个不同的公开和私有数据集上进行了测试，涵盖了 4 大类任务。
*   **充分性**：
    *   **多维度评估**：不仅对比了模型精度，还评估了推理延迟、显存占用和 CPU/GPU 利用率。
    *   **泛化性验证**：专门测试了模型在非 RAN 领域（如 WiFi、蓝牙、LoRa 数据）的零样本表现。
    *   **真实环境验证**：在基于 OAI（OpenAirInterface）的真实 5G 测试床上进行了端到端验证。
*   **客观性**：采用了严格的训练/测试集划分，并对不同模型进行了参数量对齐，确保了对比的公平性。

### 6. 主要结论与发现
*   **性能领先**：TimeRAN 在几乎所有任务中均达到或超过了 SOTA（最先进）水平，尤其是在线性探测模式下表现优异。
*   **高效迁移**：模型展现了极强的跨环境泛化能力，能够直接处理未见过的基站配置和流量模式。
*   **边缘友好**：在 GPU 上的推理延迟通常在 40-120ms 之间，显存占用约 1GB，证明其适合部署在近实时（Near-RT）RIC 或边缘服务器上。

### 7. 优点
*   **开源贡献**：发布了 TimeRAN DataPile 数据集和预训练权重，填补了电信领域缺乏大规模时间序列基础模型的空白。
*   **系统性强**：从数据收集、模型设计到真实 5G 测试床集成，形成了一个完整的闭环。
*   **灵活性高**：统一的架构极大降低了网络运营商维护数十个专用 AI 模型的负担。

### 8. 不足与局限
*   **实时性限制**：目前主要针对高层 RAN 功能（秒级/毫秒级），对于物理层（微秒级）的极高实时性任务（如波束赋形控制）尚未验证。
*   **阈值依赖**：在异常检测任务中，如何自动设定最优的异常判定阈值仍需进一步研究。
*   **微调技术限制**：论文提到 LoRA 等参数高效微调技术在时间序列任务上效果不如线性探测，这反映出时间序列基础模型的微调机制可能与 NLP 领域存在差异。

（完）
