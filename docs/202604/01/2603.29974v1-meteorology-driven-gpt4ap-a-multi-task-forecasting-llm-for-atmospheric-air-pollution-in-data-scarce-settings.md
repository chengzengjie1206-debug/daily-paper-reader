---
title: "Meteorology-Driven GPT4AP: A Multi-Task Forecasting LLM for Atmospheric Air Pollution in Data-Scarce Settings"
authors: "Prasanjit Dey, Soumyabrata Dev, Bianca Schoen-Phelan"
date: 2026-03-31
pdf: "https://arxiv.org/pdf/2603.29974v1"
tags: ["query:ts"]
score: 9.0
evidence: 利用大语言模型进行空气污染长期预测
tldr: 针对数据稀缺地区的空气污染预测难题，本文提出GPT4AP框架。该模型基于预训练GPT-2，利用rsLoRA技术进行参数高效微调，仅调整位置和输出模块。在少样本和零样本跨站点迁移实验中，GPT4AP表现优于DLinear等传统模型，证明了其在有限监督和领域偏移下的强大泛化能力与数据效率，为环境监测提供了有力支持。
selection_source: fresh_fetch
motivation: 传统数据驱动模型在观测数据稀缺的地区泛化能力有限，难以实现准确的空气污染预测。
method: 基于预训练GPT-2骨干网络，结合高斯秩稳定低秩自适应（rsLoRA）技术，构建了一个参数高效的多任务预测框架。
result: 在少样本和零样本跨站迁移设置下，GPT4AP的预测精度显著优于现有基准模型，且在长期预测中保持竞争力。
conclusion: GPT4AP证明了预训练大语言模型在处理受限监督和领域偏移的空气质量预测任务中具有极高的稳健性和数据效率。
---

## Abstract
Accurate forecasting of air pollution is important for environmental monitoring and policy support, yet data-driven models often suffer from limited generalization in regions with sparse observations. This paper presents Meteorology-Driven GPT for Air Pollution (GPT4AP), a parameter-efficient multi-task forecasting framework based on a pre-trained GPT-2 backbone and Gaussian rank-stabilized low-rank adaptation (rsLoRA). The model freezes the self-attention and feed-forward layers and adapts lightweight positional and output modules, substantially reducing the number of trainable parameters. GPT4AP is evaluated on six real-world air quality monitoring datasets under few-shot, zero-shot, and long-term forecasting settings. In the few-shot regime using 10% of the training data, GPT4AP achieves an average MSE/MAE of 0.686/0.442, outperforming DLinear (0.728/0.530) and ETSformer (0.734/0.505). In zero-shot cross-station transfer, the proposed model attains an average MSE/MAE of 0.529/0.403, demonstrating improved generalization compared with existing baselines. In long-term forecasting with full training data, GPT4AP remains competitive, achieving an average MAE of 0.429, while specialized time-series models show slightly lower errors. These results indicate that GPT4AP provides a data-efficient forecasting approach that performs robustly under limited supervision and domain shift, while maintaining competitive accuracy in data-rich settings.

---

## 论文详细总结（自动生成）

以下是对论文《Meteorology-Driven GPT4AP: A Multi-Task Forecasting LLM for Atmospheric Air Pollution in Data-Scarce Settings》的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究背景**：空气污染（如PM2.5）受气候变化和气象条件的复杂影响。虽然深度学习在空气质量预测中取得了进展，但现有模型通常是“数据密集型”的，依赖大规模、高质量的数据集。
*   **核心问题**：在观测站点稀疏、历史数据有限（数据稀缺环境）或需要跨区域迁移（领域偏移）的情况下，传统模型的泛化能力和预测精度显著下降。
*   **研究目的**：探索如何利用预训练大语言模型（LLM）的强大表征能力，通过参数高效的微调技术，实现跨站点、多任务且对数据量依赖较低的空气污染预测。

### 2. 方法论：核心思想与关键技术
*   **核心思想**：提出 **GPT4AP** 框架，将空气污染预测视为时间序列预测任务，利用预训练的 **GPT-2** 作为骨干网络，通过冻结核心层并引入轻量级适配器来捕捉气象与污染物之间的复杂关系。
*   **关键技术细节**：
    *   **输入表示与分块（Patching）**：将6种污染物和4种气象变量组合成多变量序列，采用滑动窗口分块并线性投影为时间特征Token。
    *   **冻结骨干网络**：完全冻结GPT-2的自注意力层和前馈网络层，以保留预训练的通用知识并防止在小样本上过拟合。
    *   **高斯秩稳定低秩自适应（rsLoRA）**：这是本文的核心改进。在位置编码模块和输出预测头中引入rsLoRA。相比标准LoRA，它引入了秩相关的缩放因子 $\beta_r = \alpha / \sqrt{r}$，能够稳定不同秩下的训练动力学。
    *   **多任务预测头**：使用一个轻量级MLP作为预测头，通过均值池化汇总Token信息，直接输出多步预测结果（Direct Multi-horizon Forecasting）。

### 3. 实验设计
*   **数据集**：使用中国北京的6个空气质量监测站（奥体中心、东四、顺义、天坛、海淀万柳、万寿西宫）的真实数据，涵盖2013-2017年的逐小时观测。
*   **实验场景**：
    1.  **少样本预测（Few-shot）**：仅使用10%的训练数据。
    2.  **零样本预测（Zero-shot）**：在一个站点训练，直接在另一个未见过的站点测试（跨站迁移）。
    3.  **长期预测（Long-term）**：使用全量数据进行24、36、48、60小时的预测。
*   **基准模型（Benchmarks）**：对比了 DLinear, ETSformer, FiLM, Informer, Pyraformer 和标准 Transformer。

### 4. 资源与算力
*   **硬件环境**：实验在 **NVIDIA A100 GPU** 上运行。
*   **软件框架**：基于 PyTorch 1.13.1 实现。
*   **参数效率**：通过rsLoRA，可训练参数仅占总参数量的 **0.309%**（在秩 $r=32$ 时）。
*   **训练细节**：使用 Adam 优化器，学习率 $1 \times 10^{-4}$，训练10个 epoch，并设有早停机制。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了6个不同地理位置的站点，每个站点进行了4种不同预测长度的测试。
*   **消融实验**：专门针对 rsLoRA 的秩（Rank）进行了灵敏度分析（$r \in \{4, 8, 16, 32, 64\}$），验证了性能随秩增加而饱和的趋势。
*   **充分性评价**：实验设计非常充分，通过少样本和零样本实验直接回应了“数据稀缺”这一核心痛点，对比方法均为近年来时序预测领域的顶尖模型，实验结果具有客观性和说服力。

### 6. 主要结论与发现
*   **少样本领先**：在仅用10%数据时，GPT4AP在MSE和MAE上均显著优于所有基准模型，证明了LLM先验知识在数据匮乏时的价值。
*   **零样本泛化强**：在跨站点迁移中，GPT4AP表现出极强的鲁棒性，MSE提升幅度在某些路径下超过20%。
*   **长期预测稳健**：在全量数据下，虽然专门的时序模型（如Informer）在某些指标上略优，但GPT4AP仍保持了极具竞争力的精度，且参数量更少。
*   **最优权衡**：秩 $r=32$ 是精度与计算成本的最佳平衡点，能达到峰值性能的99.9%。

### 7. 优点
*   **数据效率极高**：解决了环境监测中新设站点或欠发达地区数据不足的预测难题。
*   **参数高效**：通过冻结骨干网络和rsLoRA，极大地降低了微调LLM的算力门槛。
*   **气象驱动**：有机结合了气象因子和污染物历史，符合大气科学的物理逻辑。
*   **统一框架**：一个模型架构即可同时处理少样本、零样本和长期预测任务。

### 8. 不足与局限
*   **数据丰富时的优势不明显**：在数据量非常充足的长期预测任务中，其表现略逊于专门设计的时序架构（如Informer）。
*   **地理局限性**：目前仅在中国北京的站点进行了验证，跨国界、跨气候带的泛化能力尚待进一步测试。
*   **黑盒特性**：尽管引入了气象因子，但LLM内部如何处理这些物理变量的解释性仍显不足。
*   **单一污染物焦点**：主要针对PM2.5进行预测，未充分探讨多污染物联合预测的相互影响。

（完）
