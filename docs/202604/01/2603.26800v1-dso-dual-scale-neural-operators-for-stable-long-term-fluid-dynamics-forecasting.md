---
title: "DSO: Dual-Scale Neural Operators for Stable Long-term Fluid Dynamics Forecasting"
title_zh: DSO：用于稳定长期流体动力学预测的双尺度神经算子
authors: "Huanshuo Dong, Hao Wu, Hong Wang, Qin-Yi Zhang, Zhezheng Hao"
date: 2026-03-26
pdf: "https://arxiv.org/pdf/2603.26800v1"
tags: ["query:ts"]
score: 9.0
evidence: 稳定的长期流体动力学预测
tldr: "针对流体动力学长期预测中神经算子存在的局部细节模糊和全局趋势偏差问题，本文提出了双尺度神经算子（DSO）。该方法通过深度可分离卷积提取局部特征，并利用MLP-Mixer进行全局聚合，实现了局部与全局信息处理的解耦。实验表明，DSO在湍流基准测试中显著提升了预测精度和长期稳定性，误差较现有模型降低了88%以上。"
selection_source: fresh_fetch
motivation: 现有神经算子因统一处理演化特性不同的局部与全局信息，导致在长期流体预测中出现细节模糊和轨迹偏移。
method: 提出DSO架构，利用深度可分离卷积捕捉细粒度局部特征，并结合MLP-Mixer进行长程全局信息聚合，实现双尺度解耦。
result: "在湍流基准测试中，DSO达到了SOTA性能，相比现有神经算子将预测误差降低了88%以上，并保持了极高的长期稳定性。"
conclusion: 显式解耦局部与全局尺度的信息处理是解决神经算子在复杂物理系统长期模拟中精度与稳定性问题的有效途径。
---

## 摘要
长期流体动力学预测是科学与工程领域中一个至关重要的问题。尽管神经算子已成为建模由偏微分方程（PDEs）支配的系统的极具前景的范式，但它们往往在长期稳定性和精度方面面临挑战。我们在现有架构中识别出两种基本的失效模式：（1）局部细节模糊，即涡核和陡峭梯度等细尺度结构被逐渐平滑；（2）全局趋势偏差，即在长时间推演过程中，整体运动轨迹偏离真实值。我们认为，这些失效的原因在于现有的神经算子统一处理局部和全局信息，而忽略了它们在物理系统中固有的不同演化特性。为了弥补这一差距，我们提出了双尺度神经算子（DSO），它将信息处理显式解耦为两个互补模块：用于细粒度局部特征提取的深度可分离卷积，以及用于远程全局聚合的 MLP-Mixer。通过对涡旋动力学的数值实验，我们证明了近处扰动主要影响局部涡旋结构，而远处扰动则影响全局运动趋势，这为我们的设计选择提供了实证支持。在湍流基准测试上的广泛实验表明，DSO 在保持稳健的长期稳定性的同时，达到了最先进的精度，与现有的神经算子相比，预测误差降低了 88% 以上。

## Abstract
Long-term fluid dynamics forecasting is a critically important problem in science and engineering. While neural operators have emerged as a promising paradigm for modeling systems governed by partial differential equations (PDEs), they often struggle with long-term stability and precision. We identify two fundamental failure modes in existing architectures: (1) local detail blurring, where fine-scale structures such as vortex cores and sharp gradients are progressively smoothed, and (2) global trend deviation, where the overall motion trajectory drifts from the ground truth during extended rollouts. We argue that these failures arise because existing neural operators treat local and global information processing uniformly, despite their inherently different evolution characteristics in physical systems. To bridge this gap, we propose the Dual-Scale Neural Operator (DSO), which explicitly decouples information processing into two complementary modules: depthwise separable convolutions for fine-grained local feature extraction and an MLP-Mixer for long-range global aggregation. Through numerical experiments on vortex dynamics, we demonstrate that nearby perturbations primarily affect local vortex structure while distant perturbations influence global motion trends, providing empirical validation for our design choice. Extensive experiments on turbulent flow benchmarks show that DSO achieves state-of-the-art accuracy while maintaining robust long-term stability, reducing prediction error by over 88% compared to existing neural operators.

---

## 论文详细总结（自动生成）

这是一份关于论文《DSO: Dual-Scale Neural Operators for Stable Long-term Fluid Dynamics Forecasting》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的神经算子（如 FNO, CNO 等）在进行长期流体动力学预测时，容易出现两种失效模式：
    1.  **局部细节模糊**：细尺度结构（如涡核、陡峭梯度）随时间推移被平滑，类似于数值扩散。
    2.  **全局趋势偏差**：整体运动轨迹（如涡旋平移、旋转）逐渐偏离真实值，产生相位误差。
*   **研究动机**：作者通过数值实验发现，流体系统中的局部交互（近距离涡旋）主要影响细微结构变形，而全局交互（远距离压力耦合）主要影响运动趋势。现有模型统一处理这两种尺度，导致无法兼顾精度与稳定性。

### 2. 方法论：双尺度神经算子 (DSO)
*   **核心思想**：显式解耦局部和全局信息处理路径，建立一个物理启发式的双路径架构。
*   **关键技术细节**：
    *   **整体架构**：采用“编码器-双尺度转换器-解码器”结构。编码器负责降采样提取特征，解码器负责重建，中间通过跳跃连接（Skip Connections）保留浅层特征。
    *   **双尺度转换器 (Dual-Scale Translator)**：由 $N_t$ 个堆叠的块组成，每个块包含两个核心路径：
        1.  **局部路径 (Local Pathway)**：使用**深度可分离卷积 (Depthwise Separable Convolution)**。利用其有限的感受野捕捉细粒度特征和梯度结构，防止数值扩散。
        2.  **全局路径 (Global Pathway)**：使用 **MLP-Mixer**。通过空间混合（Spatial Mixing）和通道混合（Channel Mixing）实现全域信息聚合，捕捉长程依赖，维持全局运动趋势。
    *   **算法流程**：输入连续的时间步序列，通过编码器进入转换器，先进行局部卷积处理，再进行全局 MLP 混合，最后由解码器输出预测的下一时刻状态，并以自回归（Autoregressive）方式进行长期推演。

### 3. 实验设计
*   **数据集/场景**：
    1.  **NS-Forced**：受迫纳维-斯托克斯湍流，具有持续的外部能量输入，评估模型在混沌动态下的长期预测能力。
    2.  **NS-Decaying**：衰减湍流，无外部驱动，评估模型对能量耗散和复杂结构演化的捕捉。
*   **对比方法 (Baselines)**：涵盖了神经算子（FNO, UNO, CNO, LSM）、计算机视觉模型（ResNet, U-Net, Swin Transformer）以及时空预测模型（ConvLSTM, SimVP, PastNet）共 10 种主流方法。
*   **评估指标**：均方误差 (MSE)、结构相似性 (SSIM)、梯度误差以及散度误差（用于验证物理一致性）。

### 4. 资源与算力
*   **硬件环境**：使用了 **8 台 NVIDIA A100 GPU (40GB)**。
*   **软件框架**：PyTorch 1.8.1，采用分布式数据并行 (DDP) 模式加速训练。
*   **训练细节**：Batch size 为 20，总计训练 500 个 epoch，初始学习率为 0.001。推理过程在单张 A100 上完成。

### 5. 实验数量与充分性
*   **实验规模**：论文在两个具有代表性的湍流基准数据集上进行了详尽测试。
*   **消融实验**：专门设计了“去掉卷积模块”和“去掉 MLP-Mixer 模块”的对比实验，验证了双尺度解耦的必要性。
*   **充分性评价**：实验设计较为充分。不仅对比了 MSE，还引入了物理相关的梯度和散度指标，并展示了长达 99 个时间步的自回归推演可视化，客观地证明了模型在极长预测周期下的稳定性。

### 6. 主要结论与发现
*   **性能领先**：DSO 在所有基准测试中均达到 SOTA。在 NS-Decaying 数据集上，其全步长平均误差比次优模型降低了 **88% 以上**。
*   **长期稳定性**：在其他模型（如 UNO）出现数值崩溃（NaN）或严重模糊时，DSO 仍能保持清晰的涡旋结构和正确的运动轨迹。
*   **物理一致性**：DSO 在散度误差和梯度捕捉上表现优异，证明其预测结果更符合流体力学的质量守恒和变形特性。

### 7. 优点
*   **物理启发式设计**：通过涡旋动力学实验动机出发，将物理直觉（局部 vs 全局）转化为架构设计，具有较强的解释性。
*   **高效解耦**：结合了卷积的局部提取能力和 MLP-Mixer 的全局聚合能力，有效解决了长期预测中的扩散和漂移问题。
*   **鲁棒性强**：在自回归推演中表现出极强的抗误差累积能力。

### 8. 不足与局限
*   **维度限制**：目前实验主要集中在 2D 流体模拟，尚未在更复杂的 3D 湍流或非周期性边界条件下进行验证。
*   **计算开销**：虽然使用了深度可分离卷积，但堆叠多个双尺度块以及 MLP-Mixer 的全连接特性在处理超高分辨率数据时可能面临显存压力。
*   **应用范围**：论文主要针对不可压缩流体，对于可压缩流体或带有激波的复杂物理场，该架构的适用性有待进一步探索。

（完）
