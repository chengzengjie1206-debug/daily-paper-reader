---
title: "One-for-All: A Lightweight Stabilized and Parameter-Efficient Pre-trained LLM for Time Series Forecasting"
authors: "Prasanjit Dey, Soumyabrata Dev, Bianca Schoen-Phelan"
date: 2026-03-31
pdf: "https://arxiv.org/pdf/2603.29756v1"
tags: ["query:ts"]
score: 10.0
evidence: 多变量时间序列预测的参数高效微调
tldr: "本研究针对预训练大语言模型（LLM）在多变量时间序列分析中计算和内存开销巨大的挑战，提出了One-for-All框架。该框架引入了高斯秩稳定低秩适配器（rsLoRA），通过在位置嵌入和输出层注入极少量可训练参数并冻结自注意力权重，实现了参数高效的微调。One-for-All在显著降低参数量（减少98.3%）和内存占用（缩小168-1776倍）的同时，保持了与SOTA模型相当的预测精度，为边缘设备部署提供了可能。"
selection_source: fresh_fetch
motivation: 预训练大语言模型在处理多变量时间序列时面临极高的计算资源和内存需求，限制了其在资源受限环境下的应用。
method: 提出One-for-All框架，利用具有数学稳定性保障的rsLoRA技术，在冻结LLM主体的基础上仅对位置嵌入和输出层进行低秩适配。
result: 在多个基准数据集上实现了与SOTA模型相当的精度，且参数效率提升了5.5至21倍，内存占用降低了三个数量级。
conclusion: 该研究证明了通过秩稳定适配器可以极低成本地迁移LLM能力，实现高效、稳定且适用于边缘设备的时间序列预测。
---

## Abstract
We address the challenge of adapting pre-trained Large Language Models (LLMs) for multivariate time-series analysis, where their deployment is often hindered by prohibitive computational and memory demands. Our solution, One-for-All, introduces Gaussian Rank-Stabilized Low-Rank Adapters (rsLoRA) to enable parameter-efficient fine-tuning of frozen LLMs. While inspired by LoRA, rsLoRA introduces a mathematically grounded rank-stabilization mechanism that enables provable gradient stability at low ranks a novel contribution absent in prior PEFT methods. Our framework injects trainable rank decomposition matrices (rank 16) into positional embeddings and output layers, while keeping self-attention weights fixed. This design reduces trainable parameters by 6.8$\times$ (vs. TimesNet), 21$\times$ (vs. GPT4TS), and 11.8$\times$ (vs. TIME-LLM), while achieving a 168-1,776$\times$ smaller memory footprint (2.2MiB vs. 340MiB-4.18GiB in SOTA models). Rigorous evaluation across six time-series tasks demonstrates that One-for-All achieves state-of-the-art efficiency-accuracy trade-offs: 5.5$\times$ higher parameter efficiency (MSE=5.50) than TimesNet and 21$\times$ better than GPT4TS, while matching their forecasting accuracy (MSE=0.33). The framework's stability is validated through consistent performance across diverse horizons (96-720 steps) and datasets (ETT, Weather, M3, M4), with 98.3% fewer parameters than conventional transformers. These advances enable deployment on edge devices for healthcare, finance, and environmental monitoring without compromising performance.

---

## 论文详细总结（自动生成）

以下是对论文《One-for-All: A Lightweight Stabilized and Parameter-Efficient Pre-trained LLM for Time Series Forecasting》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：如何将预训练大语言模型（LLM）高效地适配到多变量时间序列分析任务中，同时克服其巨大的计算开销和内存需求。
*   **研究背景**：
    *   LLM在NLP领域表现卓越，但在时间序列领域面临数据稀缺（时间序列数据集通常远小于10GB）、存储成本高（全量微调产生巨大参数量）以及时间序列特有的非平稳性、不规则采样和多尺度依赖等挑战。
    *   现有的参数高效微调（PEFT）方法在适配时间序列时，往往无法兼顾推理延迟、模型深度和预测精度。

### 2. 方法论：核心思想与关键技术
*   **核心思想**：提出 **One-for-All** 框架，通过冻结LLM主干网络，仅在位置嵌入和输出层引入一种改进的低秩适配器——**高斯秩稳定低秩适配器（rsLoRA）**。
*   **关键技术细节**：
    *   **rsLoRA（Rank-Stabilized LoRA）**：这是本文的核心创新。不同于标准LoRA使用固定缩放因子，rsLoRA引入了基于秩 $r$ 的动态缩放因子 $\beta_r = \alpha / \sqrt{r}$。
    *   **数学稳定性证明**：论文通过定理1证明了当 $\beta_r \propto 1/\sqrt{r}$ 时，适配器在秩 $r \to \infty$ 时仍能保持梯度的数学稳定性，解决了低秩微调中的梯度不稳定问题。
    *   **架构设计**：冻结GPT-2的自注意力权重，仅在位置嵌入和输出层注入秩为16的可训练矩阵。
    *   **预处理流程**：采用实例归一化（Z-score）处理非平稳性，并使用分块（Patching）技术将原始序列转化为Token，以提高计算效率并捕捉局部趋势。

### 3. 实验设计
*   **任务场景**：涵盖长期预测、少样本预测（5%、10%数据）、零样本预测、短期预测、分类和异常检测六大任务。
*   **数据集**：
    *   预测：ETT系列（ETTh1/2, ETTm1/2）、Weather、M3、M4。
    *   分类：UEA多元时间序列分类存档（如Japanese Vowels, SCP1等）。
    *   异常检测：SMD, MSL, SMAP, SWaT, PSM。
*   **对比方法（Benchmark）**：
    *   **LLM类**：GPT4TS, TIME-LLM, TEST, TEMPO。
    *   **Transformer类**：FEDformer, Autoformer, Informer, Reformer, Non-Stationary Transformer。
    *   **其他SOTA**：TimesNet, ETSformer, LightTS。

### 4. 资源与算力
*   **硬件环境**：实验使用了 **NVIDIA A100 80GB GPU**。
*   **训练细节**：基于PyTorch实现，默认使用GPT-2作为主干网络。虽然未详细列出总训练时长，但提到大多数任务仅需训练10个Epoch，且由于可训练参数极少，训练速度显著快于全量微调模型。

### 5. 实验数量与充分性
*   **实验规模**：非常充分。论文在6种不同任务、10多个主流数据集上进行了测试，涵盖了从96到720步的多个预测长度。
*   **消融实验**：针对秩（Rank）从2到1024进行了详尽的性能-资源权衡分析，验证了Rank 16是最佳平衡点。
*   **公平性**：所有基准模型均采用与TimesNet一致的配置，实验重复三次取平均值，确保了结果的客观性。

### 6. 主要结论与发现
*   **极致效率**：One-for-All仅需 **0.55M** 可训练参数，比GPT4TS减少21倍，比TIME-LLM减少11.8倍。
*   **内存优势**：内存占用仅为 **2.2MiB**，比TIME-LLM（约4GiB）小1776倍，极易部署于边缘设备。
*   **精度持平**：在参数量大幅减少的情况下，其预测精度（MSE=0.33）与参数量巨大的GPT4TS持平，甚至在某些任务上优于TimesNet。
*   **稳定性**：rsLoRA确保了模型在不同预测长度和不同秩设定下的表现极其稳定，精度在Rank 16处即达到饱和。

### 7. 优点（亮点）
*   **理论支撑**：不仅是工程实现，还提供了关于秩稳定性的数学证明（定理1），为PEFT在时间序列上的应用提供了理论依据。
*   **统一架构**：一个仅2.2MiB的模型即可处理预测、分类、异常检测等多种异构任务，实现了真正的“One-for-All”。
*   **边缘友好**：极小的内存需求解决了LLM落地工业/医疗边缘监测设备的痛点。

### 8. 不足与局限
*   **零样本迁移限制**：在某些特定的跨领域迁移（如M4 $\to$ M3）中，其表现虽具竞争力，但仍略逊于参数量更大的GPT4TS。
*   **主干网络单一**：实验主要集中在GPT-2上，虽然理论上适用于所有LLM，但未展示在更大规模模型（如Llama-3）上的扩展性表现。
*   **异常检测召回率**：在某些特定数据集（如SMAP）上的召回率（Recall）仍有提升空间，相比专门设计的异常检测模型尚存差距。

（完）
