---
title: Stable Fine-Time-Step Long-Horizon Turbulence Prediction with a Multi-Stepsize Mixture-of-Experts Neural Operator
title_zh: 基于多步长混合专家神经算子的稳定小时间步长长时程湍流预测
authors: "Guanyu Pan, Huiyu Yang, Yunpeng Wang, Zikun Xu, Jianchun Wang, Nianyu Yi"
date: 2026-04-14
pdf: "https://arxiv.org/pdf/2604.12794v1"
tags: ["query:ts"]
score: 8.0
evidence: 湍流的稳定长周期自回归预测
tldr: 针对三维湍流预测中细时间步长导致的长程自回归预测不稳定和误差累积问题，本文提出了一种基于隐式分解Transformer（IFactFormer）的多步长混合专家（Ms-MoE）神经算子。该模型通过时间步路由机制动态激活特定尺度的专家模块，实现了在极细时间分辨率下的稳定长程预测。在强迫齐次各向同性湍流和槽道流上的实验表明，该方法在保持物理统计特性一致性的同时，显著提升了长程预测的稳定性。
selection_source: fresh_fetch
motivation: 传统的神经算子在细时间步长下进行长程湍流预测时，容易因时间冗余和长路径展开导致严重的误差累积和数值不稳定。
method: 提出一种多步长混合专家（Ms-MoE）架构，结合IFactFormer骨干网络，利用时间步路由器根据请求步长动态激活特定专家和共享专家。
result: 在比以往研究细20倍的时间分辨率下，该模型在齐次各向同性湍流和槽道流任务中均实现了更稳定的长程预测和更准确的长期统计特性。
conclusion: Ms-MoE-IFactFormer 证明了通过多尺度专家建模可以有效解决细时间步长下的稳定性挑战，为复杂湍流的高精度数据驱动模拟提供了新方案。
---

## 摘要
神经算子越来越多地被用作湍流时间推进预测的数据驱动代理模型。然而，长时程自回归预测对误差累积和预测间隔的选择非常敏感。过小的时间增量可能会增加时间冗余并延长推演过程，从而降低神经算子在湍流预测中的稳定性。本研究追求一个统一的目标：在精细的时间分辨率下实现三维湍流的稳定长时程自回归预测。我们提出了一种基于隐式分解 Transformer (IFactFormer) 骨干网络的多步长混合专家 (Ms-MoE) 神经算子。该模型以请求的相对步幅为条件，并使用时间步路由来激活尺度特定的路由专家和共享专家，从而构建出一种能够表示一系列步幅参数化时间推进算子的单一架构。我们在受迫均匀各向同性湍流 (HIT) 和湍流槽道流上评估了该方法，使用了滤波直接数值模拟数据集。相对于以往研究中使用的采样间隔，我们构建了时间分辨率提高达 20 倍的训练数据集，并通过定性时间切片对比和长时间平均统计特性报告了长时程自回归推演结果。Ms-MoE-IFactFormer 在 HIT 和湍流槽道流上均实现了更稳定的长时程推演，并改善了与长时间平均统计特性的一致性，这表明其在更复杂的湍流中实现精细时间分辨率下的稳定时间推进具有潜力。

## Abstract
Neural operators have been increasingly used as data-driven surrogates for time-marching predictions of turbulent flows. However, long-horizon autoregressive prediction is sensitive to error accumulation and the choice of prediction interval. Excessively small time increments may increase temporal redundancy and lengthen rollouts, which can degrade the stability of neural operators in turbulence forecasting. This work pursues a unified objective: stable long-horizon autoregressive prediction at fine temporal resolution for three-dimensional turbulence. We propose a multi-stepsize mixture-of-experts (Ms-MoE) neural operator built on an implicit factorized Transformer (IFactFormer) backbone. The model conditions on a requested relative stride and uses a time-step router to activate scale-specific routed experts together with a shared expert, yielding a single architecture that represents a family of stride-parameterized time-advancement operators. We evaluate the approach on forced homogeneous isotropic turbulence (HIT) and turbulent channel flow using filtered direct numerical simulation datasets. Relative to sampling intervals used in previous studies, we construct training datasets with up to 20 times finer temporal resolution and report long-horizon autoregressive rollouts using qualitative time-slice comparisons and long-time-averaged statistics. Ms-MoE-IFactFormer yields more stable long-horizon rollouts and improved agreement with long-time-averaged statistics on both HIT and turbulent channel flow, suggesting potential for stable time-marching at fine temporal resolution in more complex turbulent flows.

---

## 论文详细总结（自动生成）

## 论文总结：基于多步长混合专家神经算子的稳定小时间步长长时程湍流预测

### 1. 核心问题与研究背景
*   **研究动机**：在利用数据驱动的神经算子（如 FNO, Transformer）进行湍流模拟时，存在一个“时间步长悖论”。较小的时间步长（Fine Time-step）虽然能提供更高的时空分辨率，但会导致长时程自回归推演（Autoregressive Rollout）的步数剧增，从而加剧误差累积（Exposure Bias），导致模型在物理演化中迅速崩溃或产生数值不稳定。
*   **核心问题**：如何在保持极细时间分辨率（比现有研究细 20 倍）的同时，确保三维湍流长时程预测的数值稳定性和物理统计特性的一致性。

### 2. 方法论
论文提出了一种名为 **Ms-MoE-IFactFormer** 的新型神经算子架构：
*   **核心思想**：将时间推进建模为一系列受步长（Stride）参数化的算子族，利用混合专家（Mixture-of-Experts, MoE）机制，根据请求的预测步长动态激活不同的神经元子集。
*   **关键技术细节**：
    *   **骨干网络**：采用隐式分解 Transformer（IFactFormer），通过轴向分解注意力机制降低 3D 计算复杂度，并利用隐式残差迭代增强稳定性。
    *   **时间步路由器（Time-step Router）**：引入对数尺度的路由机制。将步长 $s$ 映射到对数空间，通过高斯核函数计算权重，动态选择专家。
    *   **专家结构**：包含一个**共享专家**（处理通用动力学特征）和多个**尺度特定路由专家**（处理特定时间尺度的演化特征）。
    *   **步长索引校正器**：在专家融合后，使用轻量级的步长索引 MLP 进行微调，以捕捉特定步长下的残余效应。
*   **算法流程**：输入初始流场窗口 $\rightarrow$ 提升至隐空间 $\rightarrow$ 路由器根据步长 $s$ 分配权重 $\rightarrow$ 共享与路由专家并行计算 $\rightarrow$ 融合与校正 $\rightarrow$ 投影回物理空间输出预测场。

### 3. 实验设计
*   **数据集/场景**：
    1.  **强迫齐次各向同性湍流 (HIT)**：$Re_\lambda \approx 100$，周期性边界。
    2.  **湍流槽道流 (Channel Flow)**：$Re_\tau \approx 180$，包含壁面非均匀性和各向异性。
*   **Benchmark**：以滤波直接数值模拟（fDNS）数据为真值。
*   **对比方法**：
    *   **神经算子**：傅里叶神经算子 (FNO)、固定步长的 IFactFormer。
    *   **传统物理模型 (LES)**：动态 Smagorinsky 模型 (DSM)、壁面适应局部涡粘模型 (WALE)。
*   **时间分辨率**：测试了 $\Delta T_{50}$（50倍 DNS 步长）和极细的 $\Delta T_{10}$（10倍 DNS 步长）。

### 4. 资源与算力
*   **硬件**：单张 NVIDIA Tesla V100 (32 GB) GPU。
*   **训练时长**：所有模型统一设定为 **48 小时**训练预算，采用基于时间的学习率衰减策略。
*   **模型规模**：Ms-MoE 模型参数量约为 1.4M 至 2.2M，峰值显存占用约 27GB-29GB。
*   **训练开销**：由于 Ms-MoE 需要在多个步长上进行联合监督，其单轮训练时间显著高于固定步长模型（如在 $\Delta T_{10}$ 下，单轮耗时约为 IFactFormer 的 30 倍）。

### 5. 实验数量与充分性
*   **实验组数**：针对两种主流湍流模型，分别在两个细时间步长下进行了长达 2000-4000 步的自回归推演。
*   **消融实验**：对 MoE 的超参数（专家数量 $K$、路由宽度 $\sigma$、Top-p 阈值）进行了敏感性分析。
*   **客观性**：实验采用了统一的训练预算（48h）和相同的正则化手段（如添加 2% 高斯噪声），对比了能谱、PDF、壁面剖面等多种物理统计量，评价维度全面且客观。

### 6. 主要结论与发现
*   **稳定性提升**：在极细步长下，FNO 往往会产生 NaN（数值崩溃），IFactFormer 会出现明显的结构抹平或条纹伪影，而 Ms-MoE 能够保持数千步的稳定演化。
*   **统计一致性**：Ms-MoE 在能谱分布、速度增量 PDF 以及壁面剖面统计（如 Reynolds 剪切应力）上与 fDNS 最为接近，优于传统的 DSM 和 WALE 模型。
*   **多尺度优势**：通过多步长联合训练，模型学习到了不同时间尺度下的演化规律，有效缓解了单一细步长训练带来的冗余和误差累积问题。

### 7. 优点
*   **架构创新**：首次将 MoE 机制引入神经算子的时间步长自适应建模，解决了细步长下的稳定性痛点。
*   **物理保真度高**：不仅关注单步精度，更强调长程推演中的统计特性保持。
*   **参数效率**：利用轴向分解和参数共享，在保持较低参数量（约 2M）的情况下实现了强大的表征能力。

### 8. 不足与局限
*   **训练成本**：多步长监督显著增加了训练时的计算负担，收敛所需的时间成本较高。
*   **几何限制**：目前实验基于结构化网格（笛卡尔坐标），对于复杂几何边界的适用性尚待验证。
*   **外推风险**：虽然在训练步长范围内表现优异，但对于超出训练范围（$T_{max}$ 之外）的步长预测能力未做深入探讨。

（完）
