---
title: On Neural Scaling Laws for Weather Emulation through Continual Training
title_zh: 论通过持续训练实现天气模拟的神经缩放法则
authors: "Shashank Subramanian, Alexander Kiefer, Arnur Nigmetov, Amir Gholami, Dmitriy Morozov, Michael W. Mahoney"
date: 2026-03-26
pdf: "https://arxiv.org/pdf/2603.25687v1"
tags: ["query:ts"]
score: 9.0
evidence: 天气预测模型的神经缩放法则
tldr: 本研究探讨了气象模拟中的神经缩放定律，旨在为科学机器学习构建基础模型。研究采用可扩展的 Swin Transformer 架构，并引入带有周期性冷却阶段的持续训练策略。实验证明，该方法遵循可预测的缩放趋势，且在性能上优于传统的余弦学习率调度。通过构建 IsoFLOP 曲线，研究确定了计算最优的训练方案，为气象预报模型的资源分配和性能极限预测提供了重要参考。
selection_source: fresh_fetch
motivation: 旨在探索气象预报模型在模型规模、数据量和计算资源之间的缩放规律，以优化科学机器学习基础模型的构建。
method: 采用极简且可扩展的 Swin Transformer 架构，并结合恒定学习率与周期性冷却的持续训练策略进行高效实验。
result: 发现模型遵循可预测的缩放趋势，且冷却阶段能显著提升多步预测精度和频谱清晰度，表现优于标准调度算法。
conclusion: 神经缩放定律是确定计算最优训练方案和预测气象模拟性能极限的重要诊断工具。
---

## 摘要
神经缩放法则（Neural scaling laws）在某些领域可以根据模型、数据和计算规模预测大型神经网络的性能，是自然语言处理和计算机视觉领域构建基础模型的基石。我们研究了科学机器学习（Scientific Machine Learning）中的神经缩放，重点关注天气预报模型。为了在尽可能简单的设置下分析缩放行为，我们采用了一种极简、可扩展且通用的 Swin Transformer 架构，并使用带有恒定学习率和周期性冷却（cooldowns）的持续训练作为一种高效的训练策略。我们表明，以这种极简方式训练的模型遵循可预测的缩放趋势，甚至优于标准的余弦学习率调度。冷却阶段可以被重新利用以提高下游性能，例如，通过频谱损失调整实现更长预测时效下的准确多步推演（multi-step rollouts）以及更清晰的预测。我们还系统地探索了各种计算预算下的多种模型和数据集规模，以构建 IsoFLOP 曲线，并确定了计算最优的训练方案。将这些趋势外推到更大规模揭示了潜在的性能极限，证明了神经缩放可以作为高效资源分配的重要诊断工具。我们开源了代码以确保可复现性。

## Abstract
Neural scaling laws, which in some domains can predict the performance of large neural networks as a function of model, data, and compute scale, are the cornerstone of building foundation models in Natural Language Processing and Computer Vision. We study neural scaling in Scientific Machine Learning, focusing on models for weather forecasting. To analyze scaling behavior in as simple a setting as possible, we adopt a minimal, scalable, general-purpose Swin Transformer architecture, and we use continual training with constant learning rates and periodic cooldowns as an efficient training strategy. We show that models trained in this minimalist way follow predictable scaling trends and even outperform standard cosine learning rate schedules. Cooldown phases can be re-purposed to improve downstream performance, e.g., enabling accurate multi-step rollouts over longer forecast horizons as well as sharper predictions through spectral loss adjustments. We also systematically explore a wide range of model and dataset sizes under various compute budgets to construct IsoFLOP curves, and we identify compute-optimal training regimes. Extrapolating these trends to larger scales highlights potential performance limits, demonstrating that neural scaling can serve as an important diagnostic for efficient resource allocation. We open-source our code for reproducibility.

---

## 论文详细总结（自动生成）

### 论文总结：论通过持续训练实现天气模拟的神经缩放法则

#### 1. 核心问题与整体含义（研究动机和背景）
论文探讨了**科学机器学习（SciML）**领域，特别是**全球天气预报**中的**神经缩放法则（Neural Scaling Laws）**。
*   **研究动机**：在 NLP 和 CV 领域，缩放法则已成为构建基础模型的基石，但在 SciML 领域，研究往往集中在复杂的定制架构上。作者试图验证“苦涩的教训”（The Bitter Lesson），即长期来看，利用大规模算力和数据的通用算法往往胜过精心设计的领域专家知识。
*   **核心问题**：简单的通用架构在天气预报中是否遵循可预测的缩放趋势？如何高效地确定模型大小、数据量与计算预算之间的最优平衡（即计算最优训练）？

#### 2. 方法论：核心思想与技术细节
作者提出了一套极简且高效的实验框架：
*   **极简架构**：采用标准的 **Swin Transformer** 作为骨干网络，去除了特定领域的复杂修改（如分层结构、相对位置偏差等），仅保留窗口自注意力机制，以减少架构干扰。
*   **持续训练与冷却策略（Constant LR + Cooldown）**：
    *   不同于传统的余弦学习率调度（需为每个计算预算从头训练），作者采用**恒定学习率**进行持续训练。
    *   在达到特定计算预算时，进行短暂的（总迭代次数的 5%）**冷却阶段（Cooldown）**，将学习率降至零。
    *   实验证明，这种方法不仅比余弦调度性能更好，且允许从同一检查点继续训练以探索更高预算，极大地节省了算力。
*   **下游任务对齐**：冷却阶段被重新利用，通过引入**多步自回归（AR）损失**或**频谱调整损失（AMSE）**，在不重新进行大规模预训练的情况下，优化模型在长时效预报和高频特征捕捉上的表现。
*   **分布式并行**：针对高分辨率天气数据（ERA5 0.25°），实现了 **2D 空间并行（Spatial Parallelism）** 结合数据并行，解决了单卡显存无法容纳高分辨率中间激活值的问题。

#### 3. 实验设计
*   **数据集**：使用 **ERA5** 再分析数据集（1979-2022年），空间分辨率 0.25°（721x1440 栅格），包含 71 个大气变量。
*   **Benchmark**：对比了金标准数值天气预报系统 **HRES** 和最先进的深度学习模型 **GraphCast**。
*   **实验设置**：
    *   模型规模：从 3M 到 456M 参数，外推至 1.3B。
    *   计算预算：跨越两个数量级（6E17 至 6E19 FLOPs）。
    *   评估指标：面积加权均方根误差（RMSE）和功率谱密度（PSD）。

#### 4. 资源与算力
*   **硬件**：使用 NVIDIA **A100 GPU**（40GB 和 80GB 版本）。
*   **算力规模**：
    *   小模型使用 16 张 GPU 进行数据并行。
    *   大模型（如 456M）使用 64 张 GPU，结合 2x2 空间并行。
    *   1.3B 参数模型在 64 张 80GB A100 上训练。
*   **计算量**：实验覆盖了从 6E17 到 6E19 FLOPs 的多个预算等级，最终外推实验达到了 **2.25E21 FLOPs**。

#### 5. 实验数量与充分性
*   **实验规模**：作者训练了十余种不同规模的模型，并针对每个计算预算构建了 **IsoFLOP 曲线**（等计算量曲线），通过抛物线拟合寻找每个预算下的最优参数量和样本数。
*   **充分性**：实验设计非常系统，涵盖了从模型架构对比、学习率调度消融、到下游任务对齐的完整闭环。通过 IsoFLOP 曲线确定的缩放指数（$N^* \propto C^{0.59}$，$S^* \propto C^{0.41}$）具有较强的实证支撑。
*   **客观性**：作者不仅展示了成功的缩放趋势，还诚实地报告了 1.3B 模型在超大规模下的**性能饱和与过拟合**现象。

#### 6. 主要结论与发现
*   **缩放的可预测性**：天气模拟模型遵循清晰的幂律缩放法则。
*   **策略优越性**：恒定学习率+冷却阶段的策略在性能上优于标准的余弦调度，且更具灵活性。
*   **计算最优性**：确定了在给定算力下，模型规模与训练数据量（包括多轮迭代产生的伪样本）的最优配比。
*   **性能极限**：当模型扩展到 1.3B 参数且训练超过 13 个 epoch 时，性能出现饱和。这表明在当前数据分辨率和规模下，单纯增加模型参数可能面临边际效应递减，提示未来需增加数据多样性或分辨率。

#### 7. 优点
*   **方法论创新**：将 NLP 中的持续训练和冷却策略成功引入 SciML，显著降低了研究缩放法则的门槛。
*   **解耦设计**：通过冷却阶段实现预训练与下游任务对齐的解耦，避免了为不同损失函数重复进行大规模预训练。
*   **工程实现**：开源了支持空间并行的代码，解决了高分辨率科学数据训练的痛点。

#### 8. 不足与局限
*   **确定性限制**：目前仅关注确定性预报，未涉及天气预报中至关重要的概率预报（如扩散模型或集合预报）。
*   **架构单一**：主要基于 Swin Transformer，虽然证明了其有效性，但未对比神经算子（Neural Operators）或图神经网络（GNN）在缩放过程中的表现差异。
*   **数据瓶颈**：外推实验显示的饱和现象暗示了 ERA5 数据集规模可能不足以支撑更大规模模型的持续增益，且未探讨 Patch Size（补丁大小）对缩放的影响。

（完）
