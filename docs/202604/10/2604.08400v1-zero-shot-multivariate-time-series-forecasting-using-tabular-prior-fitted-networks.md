---
title: Zero-shot Multivariate Time Series Forecasting Using Tabular Prior Fitted Networks
title_zh: 使用表格先验拟合网络的零样本多变量时间序列预测
authors: "Mayuka Jayawardhana, Nihal Sharma, Kazem Meidani, Bayan Bruss, Tom Goldstein, Doron Bergman"
date: 2026-04-09
pdf: "https://arxiv.org/pdf/2604.08400v1"
tags: ["query:ts"]
score: 9.0
evidence: 使用TabPFN进行多变量时间序列预测和数据插补
tldr: 本研究针对多变量时间序列预测，提出了一种利用表格基础模型（如TabPFN）的新框架。传统方法常将多变量序列拆分为独立的单变量子问题，忽略了通道间的相互作用。本文通过将多变量预测重构为一系列标量回归问题，使表格模型能够以零样本方式处理多变量交互。实验表明，该方法在TabPFN-TS骨干网络上表现优异，超越了现有的表格预测方法。
selection_source: fresh_fetch
motivation: 现有的表格基础模型在处理时间序列时通常忽略了多变量间的相互作用，限制了其在复杂场景下的预测能力。
method: 将多变量时间序列预测任务重构为一系列标量回归问题，从而利用具备回归能力的表格基础模型进行零样本预测。
result: 在TabPFN-TS骨干网络上的实验结果显示，该方法在多变量预测任务中优于当前的先进表格模型。
conclusion: 该框架成功扩展了表格基础模型在多变量时间序列领域的应用，证明了通过回归重构捕获通道间依赖关系的有效性。
---

## 摘要
表格基础模型，特别是像 TabPFN 这样的先验数据拟合网络（Prior-data Fitted Networks），已在从数据插补到标签预测的多种表格数据任务中脱颖而出，成为领先的竞争者，超越了树模型在历史上的成功。这引发了对其在时间序列数据预测中适用性的研究，因为时间序列预测可以被表述为一个表格问题。虽然最近在这方面的工作展示了积极的结果，但大多数工作将多变量时间序列问题的处理局限于几个独立的单变量时间序列预测子问题，从而忽略了通道间的相互作用。为了克服这一局限性，我们引入了一个使用表格基础模型进行多变量时间序列预测的通用框架。我们通过将多变量时间序列预测问题重新表述为一系列标量回归问题来实现这一点，这些问题随后可以由任何具有回归能力的表格基础模型以零样本方式解决。我们展示了使用 TabPFN-TS 作为骨干网络的方法结果，并将其性能与当前最先进的表格方法进行了比较。

## Abstract
Tabular foundation models, particularly Prior-data Fitted Networks like TabPFN have emerged as the leading contender in a myriad of tasks ranging from data imputation to label prediction on the tabular data format surpassing the historical successes of tree-based models. This has led to investigations on their applicability to forecasting time series data which can be formulated as a tabular problem. While recent work to this end has displayed positive results, most works have limited their treatment of multivariate time series problems to several independent univariate time series forecasting subproblems, thus ignoring any inter-channel interactions. Overcoming this limitation, we introduce a generally applicable framework for multivariate time series forecasting using tabular foundation models. We achieve this by recasting the multivariate time series forecasting problem as a series of scalar regression problems which can then be solved zero-shot by any tabular foundation model with regression capabilities. We present results of our method using the TabPFN-TS backbone and compare performance with the current state of the art tabular methods.

---

## 论文详细总结（自动生成）

以下是对论文《Zero-shot Multivariate Time Series Forecasting Using Tabular Prior Fitted Networks》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：如何利用现有的表格基础模型（如 TabPFN）有效地处理**多变量时间序列（MTS）**预测，并捕获变量间的相互依赖关系（跨通道交互）。
*   **研究背景**：
    *   表格基础模型（PFNs）在处理表格数据方面表现优异，近期研究（如 TabPFN-TS）将其应用于单变量时间序列预测。
    *   现有的多变量预测方法通常采用“通道独立性（Channel Independence, CI）”策略，将多变量问题拆分为多个独立的单变量问题，这忽略了变量间的空间相关性。
    *   直接训练支持多变量输出的基础模型成本极高且面临工程挑战。

### 2. 论文提出的方法论
*   **核心思想**：将多变量时间序列预测重构为一个**标量回归问题**，通过“展开（Rolling out）”数据结构，使表格模型能够以零样本（Zero-shot）方式理解跨通道信息。
*   **关键技术细节**：
    *   **数据重构（Serialization）**：将多变量向量扁平化。在每个时间步 $t$，不再是单行包含所有变量，而是将其拆分为多行。
    *   **特征增强**：为每一行引入一个**通道指示符（Channel Indicator, $\eta$）**作为分类特征，特征向量包含：时间戳 $t$、通道索引 $\eta$ 和对应的数值 $u$。
    *   **回归目标**：模型预测给定通道在特定时间点的标量值。
    *   **独立标准化**：在输入模型前，对每个通道独立进行 Z-score 标准化，预测后再进行逆变换，以处理不同变量间量纲差异巨大的问题。
*   **算法流程**：该方法（TabPFN-TS-MV）无需修改 TabPFN 的架构或进行重新训练，直接利用其上下文学习（In-Context Learning）能力进行推理。

### 3. 实验设计
*   **数据集**：使用 **Gift-Eval** 时间序列基准测试，涵盖 23 个数据集、14.4 万条时间序列、1.77 亿个数据点，涉及金融、气象、工程等 7 个领域。
*   **Benchmark 任务**：包含 97 个不同的基准任务，覆盖短、中、长期预测。
*   **对比方法**：
    *   **TabPFN-TS**：基于通道独立（CI）策略的基准。
    *   **SOTA 时间序列模型**：包括 Chronos-2、TempoPFN、TimePFN 等。
*   **评估指标**：平均绝对缩放误差（MASE，衡量点预测精度）和加权分位数损失（WQL，衡量概率预测精度）。

### 4. 资源与算力
*   **算力说明**：论文**未明确列出**具体的 GPU 型号、数量或训练时长。
*   **计算成本分析**：作者指出，该方法的主要代价是**计算成本增加**。由于将多变量数据“展开”，输入模型的上下文长度（Context Length）会随变量数量线性增长，这受限于基础模型（如 TabPFN v2）的最大上下文窗口限制。

### 5. 实验数量与充分性
*   **实验规模**：在 23 个多变量数据集上进行了广泛测试，并针对 97 个任务进行了详细对比。
*   **消融实验**：研究了不同的分布偏移缓解策略（如一阶差分 vs. 通道标准化），证明了标准化的有效性。
*   **充分性评价**：实验设计较为充分，涵盖了多种频率和领域的数据。通过与多种 SOTA 模型对比，验证了其在多变量场景下的竞争力。

### 6. 论文的主要结论与发现
*   **性能提升**：在 60% 的多变量数据集上，TabPFN-TS-MV 的 MASE 优于通道独立的 TabPFN-TS。
*   **跨通道建模的价值**：显式建模通道间依赖关系在多数情况下能提高预测精度。
*   **标准化至关重要**：通道独立的 Z-score 标准化能显著提升性能，而一阶差分反而会降低预测效果。
*   **概率预测挑战**：虽然点预测（MASE）表现优异，但在概率预测（WQL）上，该方法相比 CI 策略仍存在一定差距，可能需要更好的校准策略。

### 7. 优点
*   **零样本能力**：无需针对特定数据集进行微调或重新训练，直接利用现有的表格基础模型。
*   **模型无关性**：该框架具有通用性，可以应用于任何具备回归能力的表格基础模型。
*   **简单高效**：通过简单的特征工程（引入通道指示符）解决了复杂的多变量交互建模问题。

### 8. 不足与局限
*   **上下文长度瓶颈**：由于数据展开导致输入序列变长，在处理变量极多（如 Jena-Weather 数据集有 21 个变量）的长序列时，会迅速触及模型的上下文长度上限。
*   **概率预测精度**：在 WQL 指标上表现略逊于 CI 方法，表明在处理多变量联合分布的概率估计时仍有优化空间。
*   **推理开销**：相比 CI 方法，由于序列变长，推理时的内存和计算开销显著增加。

（完）
