---
title: The CTLNet for Shanghai Composite Index Prediction
title_zh: 用于上证指数预测的 CTLNet
authors: Haibin Jiao
date: 2026-04-18
pdf: "https://arxiv.org/pdf/2604.16835v1"
tags: ["query:ts"]
score: 8.0
evidence: 使用 CNN-Transformer-LSTM 进行多变量时间序列预测
tldr: 上证指数预测是金融研究的热点。本文针对多元时间序列预测，结合CNN、Transformer和LSTM的优势，提出了一种名为CTLNet的混合深度学习模型。该模型利用CNN提取局部特征，Transformer处理长序列依赖和多元相关性，LSTM捕捉时序动态。实验结果表明，CTLNet在预测上证指数方面优于现有的主流基准模型，为金融市场预测提供了有效工具。
selection_source: fresh_fetch
motivation: 旨在利用深度学习技术解决上证指数预测中长序列依赖和多元数据相关性的挑战。
method: 提出了一种结合卷积神经网络（CNN）、Transformer编码器和长短期记忆网络（LSTM）的混合模型CTLNet。
result: 对比实验显示，所提出的CTLNet模型在预测性能上显著优于现有的最先进基准模型。
conclusion: CTLNet通过融合多种架构的优势，有效提升了上证指数预测的准确性，具有较高的学术和应用价值。
---

## 摘要
上证指数预测已成为许多投资者和学术研究人员关注的热点问题。深度学习模型被广泛应用于多元时间序列预测，包括循环神经网络（RNN）、卷积神经网络（CNN）和 Transformer。具体而言，Transformer 编码器凭借其独特的注意力机制和并行处理能力，已成为时间序列预测中的重要工具，在处理长序列依赖和多元数据相关性方面具有优势。借鉴各种模型的优点，我们提出了 CNN-Transformer-LSTM 网络（CTLNet）。本文探讨了 CTLNet 在上证指数预测中的应用，对比实验表明，所提出的模型优于目前最先进的基准模型。

## Abstract
Shanghai Composite Index prediction has become a hot issue for many investors and academic researchers. Deep learning models are widely applied in multivariate time series forecasting, including recurrent neural networks (RNN), convolutional neural networks (CNN), and transformers. Specifically, the Transformer encoder, with its unique attention mechanism and parallel processing capabilities, has become an important tool in time series prediction, and has an advantage in dealing with long sequence dependencies and multivariate data correlations. Drawing on the strengths of various models, we propose the CNN-Transformer-LSTM Networks (CTLNet). This paper explores the application of CTLNet for Shanghai Composite Index prediction and the comparative experiments show that the proposed model outperforms state-of-the-art baselines.

---

## 论文详细总结（自动生成）

以下是对论文《The CTLNet for Shanghai Composite Index Prediction》的结构化深入总结：

### 1. 论文的核心问题与整体含义
*   **研究背景**：上证指数（SSE Composite Index）作为中国证券市场的核心指标，其预测对投资者和研究者至关重要。然而，金融时间序列具有高波动性、复杂模式以及信息稀疏性（单个时间点包含信息有限）等挑战。
*   **核心问题**：如何结合不同深度学习架构的优势，同时捕捉时间序列的局部特征、全局交互信息以及长期的时序依赖关系，以提高多变量时间序列预测的准确性。

### 2. 论文提出的方法论：CTLNet
CTLNet 是一个融合了 CNN、Transformer 和 LSTM 的混合模型，其核心流程如下：
*   **CNN 模块（局部特征提取）**：使用一维卷积（1D CNN）作为滑动窗口，将相邻的时间点聚合为“时间节点”（Time Nodes）。这一步旨在增强局部表示，缓解信息稀疏问题，并将原始序列转化为节点嵌入序列。
*   **Transformer 编码器模块（全局特征提取）**：在节点嵌入中加入位置编码（Positional Encoding），通过多头自注意力机制（MSA）促进时间节点之间的全面交互。它充当全局特征提取器，丰富每个节点的上下文信息。
*   **LSTM 模块（时序依赖捕捉）**：采用双向 LSTM（Bi-LSTM）处理经过 Transformer 增强的节点序列。LSTM 负责捕捉节点间的长期时序演变规律，应对金融市场中的趋势和噪声。
*   **预测输出**：最后通过输出层进行回归预测，使用均方误差（MSE）作为损失函数进行监督学习。

### 3. 实验设计
*   **数据集**：使用了两个真实世界数据集：**上证综合指数（SSE Composite Index）**和**上证50指数（SSE 50 Index）**。
*   **数据规格**：采样频率为 5 分钟，包含开盘价、收盘价、最高价、最低价和成交量等 6 个维度。样本量约为 11,616 条。
*   **实验设置**：采用滑动窗口法，利用前 5 个时间点的数据预测第 6 个时间点的值（单步预测）。
*   **基准模型（Benchmark）**：对比了 LSTM、CNN-LSTM、TCLNet（将 Transformer 置于最前端的变体）以及 PatchTST。
*   **评价指标**：平均绝对误差（MAE）和决定系数（$R^2$）。

### 4. 资源与算力
*   **训练时长**：论文记录了训练时间。在 SSE Composite 数据集上，CTLNet 的训练时间约为 **97.58 秒**，LSTM 约为 38.23 秒，CNN-LSTM 约为 37.17 秒。
*   **算力设备**：文中**未明确说明**具体的 GPU 型号、数量或 CPU 配置。
*   **参数量**：CTLNet 的参数量为 102,153，高于其他对比模型。

### 5. 实验数量与充分性
*   **实验规模**：在 2 个数据集上进行了性能对比，并进行了一组消融实验（对比了模块顺序对结果的影响，如 TCLNet）。
*   **充分性评价**：
    *   **优点**：通过对比不同架构组合（如 CNN-LSTM 与 CTLNet），证明了 Transformer 模块的增益；通过 TCLNet 的对比证明了“先局部再全局”处理顺序的有效性。
    *   **不足**：实验仅限于单步预测（预测下一个 5 分钟），未测试长程预测能力；数据集仅限于中国市场，缺乏对其他国际指数的泛化性验证。

### 6. 论文的主要结论与发现
*   **CTLNet 表现最优**：在两个数据集上，CTLNet 的 MAE 最低且 $R^2$ 最高（均在 0.97 以上），显著优于单一模型或简单的混合模型。
*   **模块顺序至关重要**：实验发现，如果将 Transformer 放在 CNN 之前（如 TCLNet），效果会变差。这表明原始时间点包含较多噪声，先通过 CNN 提取局部模式后再进行全局交互更为有效。
*   **CNN 的压缩作用**：CNN 不仅能提取特征，还能压缩序列长度，从而提高后续模块的处理效率。

### 7. 优点
*   **架构设计合理**：有机结合了 CNN（局部）、Transformer（全局交互）和 LSTM（时序趋势）的专长，逻辑清晰。
*   **解决稀疏性**：通过将时间点转化为“时间节点”的概念，借鉴了 Vision Transformer (ViT) 的 Patch 思想，有效处理了金融数据的稀疏性。
*   **模型简洁**：尽管是混合模型，但其原理易于解释，且在小规模参数下即可实现高精度。

### 8. 不足与局限
*   **硬件描述缺失**：缺乏实验环境的具体硬件信息，难以评估能效比。
*   **预测窗口过短**：5 分钟频率的单步预测在实际交易中可能受交易成本影响较大，缺乏多步预测（Long-term forecasting）的实验。
*   **超参数敏感性**：论文提到 CNN 的滑动窗口大小对结果影响显著，但未给出详细的超参数搜索过程或敏感性分析。
*   **基准模型较老**：虽然对比了 PatchTST，但未对比最新的时间序列模型（如 iTransformer 或 TimesNet 等）。

（完）
