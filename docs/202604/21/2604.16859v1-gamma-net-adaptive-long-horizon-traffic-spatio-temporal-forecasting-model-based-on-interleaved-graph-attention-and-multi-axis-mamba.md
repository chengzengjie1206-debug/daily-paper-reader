---
title: "GAMMA-Net: Adaptive Long-Horizon Traffic Spatio-Temporal Forecasting Model based on Interleaved Graph Attention and Multi-Axis Mamba"
authors: "Dongyi He, Yuanquan Gao, Bin Jiang, He Yan"
date: 2026-04-18
pdf: "https://arxiv.org/pdf/2604.16859v1"
tags: ["query:ts"]
score: 9.0
evidence: 长周期交通时空预测
tldr: 结合图注意力网络与Mamba模型，实现自适应的长周期交通时空预测。
selection_source: fresh_fetch
motivation: 长周期交通时空预测。
method: 方法与实现细节请参考摘要与正文。
result: 结果与对比结论请参考摘要与正文。
conclusion: 总体而言，该工作在所述任务上展示了有效性，并提供了可复用的思路或工具。
---

## Abstract
Accurate traffic forecasting is crucial for intelligent transportation systems, supporting effective traffic management, congestion reduction, and informed urban planning. However, traditional models often fail to adequately capture the intricate spatio-temporal dependencies present in traffic data. To overcome these limitations, we introduce GAMMA-Net, a novel approach that integrates Graph Attention Networks (GAT) with multi-axis Selective State Space Models (Mamba). The GAT component uses a self-attention mechanism to dynamically adjust the influence of nodes within the traffic network, enabling adaptive spatial dependency modeling based on real-time conditions. Simultaneously, the Mamba module efficiently models long-term temporal and spatial dynamics without the heavy computational cost of conventional recurrent architectures. Extensive experiments on several benchmark traffic datasets, including METR-LA, PEMS-BAY, PEMS03, PEMS04, PEMS07, and PEMS08, show that GAMMA-Net consistently outperforms existing state-of-the-art models across different prediction horizons, achieving up to a 16.25% reduction in Mean Absolute Error (MAE) compared to baseline models. Ablation studies highlight the critical contributions of both the spatial and temporal components, emphasizing their complementary role in improving prediction accuracy. In conclusion, the GAMMA-Net model sets a new standard in traffic forecasting, offering a powerful tool for next-generation traffic management and urban planning. The code for this study is available at https://github.com/hdy6438/GAMMA-Net

---

## 论文详细总结（自动生成）

这是一份关于论文 **《GAMMA-Net: Adaptive Long-Horizon Traffic Spatio-Temporal Forecasting Model based on Interleaved Graph Attention and Multi-Axis Mamba》** 的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
交通预测是智能交通系统（ITS）的核心，但面临两大挑战：
*   **时空依赖的复杂性**：交通数据具有高度非线性、非平稳性以及长程空间耦合特征。
*   **计算效率与适应性的权衡**：
    *   **Transformer 类模型**：虽能捕捉长程依赖，但自注意力机制的二次计算复杂度（$O(L^2)$）限制了实时性。
    *   **RNN/GNN 类模型**：存在梯度消失、并行化差或图结构静态（无法适应路网动态变化）的问题。
    *   **现有 Mamba 变体**：虽然实现了线性复杂度，但在处理稀疏传感器或动态路网时，空间编码能力较弱。
**研究动机**：提出一种既能保持线性计算效率，又能动态适应空间拓扑变化的长周期预测模型。

### 2. 方法论：核心思想与关键技术
**GAMMA-Net** 的核心思想是**交替集成（Interleaved）**图注意力网络（GAT）与多轴选择性状态空间模型（Mamba）。

*   **核心架构流程**：
    1.  **嵌入层（Embedding Layer）**：融合原始流量特征、周期性特征（周、日）以及时空自适应嵌入。
    2.  **时间 GAT-Mamba 层**：
        *   **GAT**：基于当前节点特征动态重加权物理路网图，生成拓扑感知特征。
        *   **Temporal Mamba**：沿时间轴进行扫描，以线性复杂度提取长程序列信息。
    3.  **空间 GAT-Mamba 层**：
        *   **GAT**：利用时间层更新后的特征再次修正图注意力，捕捉动态演化的空间关系（如拥堵扩散）。
        *   **Spatial Mamba**：沿空间轴扫描，实现高效的空间信息混合。
    4.  **回归层**：通过全连接层输出最终预测结果。

*   **关键技术细节**：
    *   **动态图建模**：不依赖固定权重，通过 GAT 随时间步动态调整节点间影响力。
    *   **SVD 稳定性分析**：提出使用奇异值分解（SVD）分析 Mamba 的状态转移矩阵 $A$，通过奇异值分布验证系统稳定性并解释时空模块的差异。

### 3. 实验设计
*   **数据集**：使用了 6 个主流交通基准数据集：**METR-LA、PEMS-BAY、PEMS03、PEMS04、PEMS07、PEMS08**。
*   **Benchmark（对比方法）**：
    *   **传统/经典 GNN**：DCRNN, STGCN, GWNet, AGCRN, MTGNN。
    *   **Transformer 变体**：GMAN, PDFormer, STAEformer。
    *   **最新 Mamba 变体**：ST-MambaSync, STGM。
    *   **其他**：STID, ST-Norm, GTS。
*   **评估指标**：MAE、RMSE、MAPE。

### 4. 资源与算力
*   **硬件**：单张 **NVIDIA GeForce RTX 4090 GPU**。
*   **软件**：PyTorch 2.1.1。
*   **训练细节**：Batch size 为 16，学习率 1e-3，使用 Adam 优化器和早停机制（Early-stopping）。
*   **收敛情况**：文中给出了各数据集的最佳 Epoch 数（如 METR-LA 为 21，PEMS08 为 47）。

### 5. 实验数量与充分性
*   **实验规模**：在 6 个数据集上进行了全方位的性能对比，涵盖了短、中、长三个预测维度（15/30/60分钟）。
*   **消融实验**：设计了四组对比（无 GAT、无时间模块、无空间模块、两者皆无），验证了各组件的协同作用。
*   **可视化分析**：
    *   **SVD 可视化**：解释了空间模块受少数主导因素驱动，而时间模块捕捉更广谱的依赖。
    *   **社区检测**：通过 Louvain 算法展示了模型学习到的空间地理聚类和时间功能聚类。
    *   **预测曲线**：展示了模型在传感器故障（零值数据）下的鲁棒性。
*   **评价**：实验设计非常充分且客观，通过多维度的可视化增强了深度学习模型的可解释性。

### 6. 主要结论与发现
*   **性能领先**：GAMMA-Net 在所有数据集上均达到 SOTA，相比基准模型，MAE 最高降低了 **16.25%**。
*   **长程能力**：在长周期预测（60分钟）中，GAMMA-Net 的领先优势比短周期更明显，证明了 Mamba 架构在长序列处理上的优越性。
*   **鲁棒性**：预测可视化表明，模型能有效“修复”传感器故障产生的异常零值，利用时空上下文维持平滑预测。
*   **稳定性**：SVD 分析证实所有奇异值均小于 1，保证了长序列推理的数值稳定性。

### 7. 优点（亮点）
*   **交替式设计**：将 GAT 的动态空间建模与 Mamba 的高效长程建模交替结合，解决了“时间模式改变如何影响空间权重”的问题。
*   **线性效率**：避免了 Transformer 的二次方开销，更适合实时 ITS 部署。
*   **深度可解释性**：引入 SVD 和社区检测技术，将黑盒模型的内部状态（转移矩阵、注意力权重）转化为可理解的物理意义。

### 8. 不足与局限
*   **初始图依赖**：虽然 GAT 可以调整权重，但模型仍基于物理连接构建初始图，对于完全缺失先验拓扑的场景（如虚拟传感器网络）适应性未被验证。
*   **超参数敏感性**：模型包含多层交替结构，Embedding 维度和层数等超参数对性能影响较大，文中未详细讨论不同硬件环境下的推理延迟对比。
*   **应用限制**：主要针对交通流量预测，对于极端突发事件（如重大交通事故导致的长期封路）的零样本适应能力仍有待观察。

（完）
