---
title: "TriTS: Time Series Forecasting from a Multimodal Perspective"
title_zh: TriTS：从多模态视角进行时间序列预测
authors: Xiang Ao
date: 2026-04-17
pdf: "https://arxiv.org/pdf/2604.16748v1"
tags: ["query:ts"]
score: 10.0
evidence: 利用跨模态解耦进行长期时间序列预测
tldr: 提出 TriTS 框架，将时间序列投影到多个空间以提升长期预测性能。
selection_source: fresh_fetch
motivation: 利用跨模态解耦进行长期时间序列预测。
method: 方法与实现细节请参考摘要与正文。
result: 结果与对比结论请参考摘要与正文。
conclusion: 总体而言，该工作在所述任务上展示了有效性，并提供了可复用的思路或工具。
---

## 摘要
时间序列预测在金融、能源、交通和气象等关键领域发挥着至关重要的作用。然而，长期时间序列预测（LTSF）仍然是一个重大挑战，因为现实世界的信号包含高度纠缠的时间动态，很难从纯一维（1D）视角完全捕捉。为了打破这一表示瓶颈，我们提出了 TriTS，这是一种新颖的跨模态解耦框架，它将一维时间序列投影到正交的时间、频率和二维（2D）视觉空间中。为了在不产生 Vision Transformer (ViT) 高昂的 $O(N^2)$ 计算开销的情况下，无缝衔接一维到二维的模态鸿沟，我们引入了周期感知重塑（Period-Aware Reshaping）策略，并结合了 Visual Mamba (Vim)。该方法在保持线性计算复杂度的同时，将跨周期依赖关系高效地建模为全局视觉纹理。作为补充，我们为频率模态设计了多分辨率小波混合（MR-WM）模块，该模块将非平稳信号显式解耦为趋势和噪声分量，以实现细粒度的时频定位。最后，在时域中保留了一个流式线性分支，以确保数值稳定性。通过动态融合这三种互补的表示，TriTS 能够有效地适应多样化的数据语境。在多个基准数据集上的广泛实验表明，TriTS 达到了最先进（SOTA）的性能，通过大幅减少参数量和推理延迟，从根本上超越了现有的基于视觉的预测模型。

## Abstract
Time series forecasting plays a pivotal role in critical sectors such as finance, energy, transportation, and meteorology. However, Long-term Time Series Forecasting (LTSF) remains a significant challenge because real-world signals contain highly entangled temporal dynamics that are difficult to fully capture from a purely 1D perspective. To break this representation bottleneck, we propose TriTS, a novel cross-modal disentanglement framework that projects 1D time series into orthogonal time, frequency, and 2D-vision spaces.To seamlessly bridge the 1D-to-2D modality gap without the prohibitive $O(N^2)$ computational overhead of Vision Transformers (ViTs), we introduce a Period-Aware Reshaping strategy and incorporate Visual Mamba (Vim). This approach efficiently models cross-period dependencies as global visual textures while maintaining linear computational complexity. Complementing this, we design a Multi-Resolution Wavelet Mixing (MR-WM) module for the frequency modality, which explicitly decouples non-stationary signals into trend and noise components to achieve fine-grained time-frequency localization. Finally, a streaming linear branch is retained in the time domain to anchor numerical stability. By dynamically fusing these three complementary representations, TriTS effectively adapts to diverse data contexts. Extensive experiments across multiple benchmark datasets demonstrate that TriTS achieves state-of-the-art (SOTA) performance, fundamentally outperforming existing vision-based forecasters by drastically reducing both parameter count and inference latency.

---

## 论文详细总结（自动生成）

这篇论文介绍了 **TriTS**，一个从多模态视角出发的长期时间序列预测框架。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：现实世界的时间序列信号包含高度纠缠的动态（趋势、季节性、非平稳噪声），仅从一维（1D）时域视角难以完全捕捉其复杂特征。
*   **核心挑战**：
    1.  **计算瓶颈**：现有的将时间序列转化为图像并使用 Vision Transformer (ViT) 的方法，其计算复杂度随序列长度呈平方增长（$O(N^2)$）。
    2.  **表示局限**：传统的频域方法（如 FFT）使用全局基函数，难以定位非平稳信号中的瞬态突变。
*   **整体含义**：TriTS 旨在通过将 1D 信号投影到**时间、频率、2D 视觉**三个正交空间，打破单一模态的表示瓶颈，并在保持线性计算复杂度的前提下提升预测精度。

### 2. 方法论
TriTS 采用跨模态解耦框架，包含三个并行分支及一个动态融合模块：
*   **时域分支（Time-domain Branch）**：
    *   使用**指数移动平均（EMA）**提取趋势，相比简单移动平均（SMA）能更灵敏地响应近期变化且无需填充。
    *   通过流式线性层（Streaming Linear Layer）建立数值锚点，确保预测的稳定性。
*   **频域分支（Frequency-domain Branch）**：
    *   引入**多分辨率小波混合（MR-WM）**模块。利用离散小波变换（DWT）将信号分解为不同分辨率的近似（趋势）和细节（噪声）分量。
    *   每个分辨率分支独立处理，避免高频噪声污染低频趋势，并解决频率泄漏问题。
*   **视觉分支（Vision-domain Branch）**：
    *   **周期感知重塑**：根据自相关函数确定的主周期 $P$，将 1D 序列折叠为 2D “时间图像”。
    *   **Visual Mamba (Vim)**：引入双向选择性状态空间模型（Bi-SSM）替代 ViT。利用其选择性扫描机制捕捉全局纹理，同时将计算复杂度降低至线性（$O(N)$）。
*   **自适应尺度感知融合（Adaptive Scale-Aware Fusion）**：
    *   设计轻量级门控网络，根据输入数据的特征动态生成三个分支的置信度权重，实现加权融合。

### 3. 实验设计
*   **数据集**：使用了 7 个主流公开基准数据集，涵盖电力（ETTh1/h2, ETTm1/m2, ECL）、交通（Traffic）和气象（Weather）领域。
*   **对比方法（Benchmarks）**：对比了 8 种 SOTA 模型，包括：
    *   **视觉类**：VisionTS, VisionTS++。
    *   **频率类**：WPMixer, FDNet。
    *   **Transformer 类**：iTransformer, PatchTST。
    *   **线性/混合类**：TimeMixer, DLinear。
*   **评价指标**：均方误差（MSE）和平均绝对误差（MAE）。

### 4. 资源与算力
*   **硬件环境**：实验在一台配备单张 **NVIDIA GeForce A800 80G GPU** 的服务器上运行。
*   **效率表现**：论文明确指出 TriTS 相比 VisionTS 大幅减少了参数量和推理延迟。例如，在处理长序列时，TriTS 的内存占用显著低于基于 ViT 的模型。

### 5. 实验数量与充分性
*   **实验规模**：在 7 个数据集上针对 4 种预测长度（96, 192, 336, 720）进行了详尽测试。
*   **消融实验**：分别验证了移除时域、频域、视觉分支以及门控融合模块后的性能变化，证明了各组件的必要性。
*   **可视化分析**：展示了不同数据集（如 Weather vs. ETTh1）在融合时各模态权重的差异，解释了模型的决策逻辑。
*   **充分性评价**：实验设计涵盖了精度对比、效率对比和内部组件验证，对比基线涵盖了近年最先进的模型，实验结果具有较强的说服力和客观性。

### 6. 主要结论与发现
*   **性能领先**：TriTS 在大多数数据集和预测长度上均达到了 SOTA 性能，尤其在长序列预测中表现优异。
*   **模态互补**：时域分支提供数值稳定性，频域分支解析周期与噪声，视觉分支捕捉长程结构纹理，三者结合优于任何单一模态。
*   **效率突破**：通过引入 Visual Mamba，成功解决了视觉模态在长序列处理中的计算开销问题，实现了精度与效率的平衡。

### 7. 优点
*   **创新性强**：首次将 Visual Mamba 引入多模态时间序列预测，解决了 1D 到 2D 转换后的计算复杂度问题。
*   **解耦彻底**：利用小波变换和多分支结构，在物理意义上实现了趋势与噪声的有效分离。
*   **轻量高效**：相比于依赖大型预训练视觉模型（如 MAE-Base）的方法，TriTS 使用更小的骨干网络（Vim-Tiny）即可获得更优结果。

### 8. 不足与局限
*   **周期依赖性**：视觉分支的重塑依赖于准确的周期检测（自相关函数），对于周期性极弱或周期随时间剧烈变化的信号，效果可能受限。
*   **架构复杂性**：三分支并行结构虽然提升了性能，但也增加了模型调参和部署的复杂性。
*   **不规则数据**：论文未讨论该框架在处理具有缺失值或非均匀采样频率的不规则时间序列时的表现。

（完）
