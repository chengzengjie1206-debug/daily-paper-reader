---
title: PDE-regularized Dynamics-informed Diffusion with Uncertainty-aware Filtering for Long-Horizon Dynamics
title_zh: 基于PDE正则化动力学感知扩散模型与不确定性感知滤波的长程动力学预测
authors: "Min Young Baeg, Yoon-Yeong Kim"
date: 2026-04-10
pdf: "https://arxiv.org/pdf/2604.09058v1"
tags: ["query:ts"]
score: 9.0
evidence: 稳定的长期预测和长时程动力学
tldr: 针对长程时空预测中累积误差和物理一致性缺失的问题，本文提出PDYffusion框架。该方法将扩散模型的概率建模能力与物理规律相结合，通过PDE正则化插值器确保中间状态符合物理定律，并利用无迹卡尔曼滤波（UKF）预测器显式建模不确定性以抑制误差累积。实验证明，该方法在保持物理一致性的同时，显著提升了长程预测的准确性和稳定性。
selection_source: fresh_fetch
motivation: 现有的扩散模型在长程时空预测中往往忽略物理规律，且难以有效处理迭代预测中的误差累积和噪声放大问题。
method: 提出由PDE正则化插值器和基于UKF的预测器组成的扩散框架，分别用于强制物理一致性和进行不确定性感知预测。
result: 在多个动力学数据集上的实验表明，该方法在CRPS、MSE和SSR指标上均优于现有模型，实现了精度与不确定性的平衡。
conclusion: PDYffusion通过融合物理约束与滤波机制，为稳定且物理一致的长程动力学预测提供了一种鲁棒的解决方案。
---

## 摘要
由于累积误差、噪声放大以及现有模型缺乏物理一致性，长程时空预测仍然是一个具有挑战性的问题。虽然扩散模型为建模不确定性提供了一个概率框架，但传统方法通常依赖于均方误差目标函数，且无法捕捉受物理定律支配的底层动力学。在这项工作中，我们提出了 PDYffusion，这是一个动力学感知的扩散框架，它集成了基于 PDE 的正则化和不确定性感知预测，以实现稳定的长期预测。该方法由两个关键组件组成：一个 PDE 正则化插值器和一个基于 UKF 的预测器。插值器引入了微分算子以强制执行物理一致的中间状态，而预测器则利用无迹卡尔曼滤波（UKF）来显式建模不确定性，并减轻迭代预测过程中的误差累积。我们提供了理论分析，证明所提出的插值器满足 PDE 约束的光滑性质，并且预测器在所提出的损失函数下具有收敛性。在多个动力学数据集上的广泛实验表明，PDYffusion 在 CRPS 和 MSE 方面取得了优异的性能，同时保持了由 SSR 衡量的稳定不确定性行为。我们进一步分析了预测准确性与不确定性之间的内在权衡，表明我们的方法为长程预测提供了一个平衡且鲁棒的解决方案。

## Abstract
Long-horizon spatiotemporal prediction remains a challenging problem due to cumulative errors, noise amplification, and the lack of physical consistency in existing models. While diffusion models provide a probabilistic framework for modeling uncertainty, conventional approaches often rely on mean squared error objectives and fail to capture the underlying dynamics governed by physical laws. In this work, we propose PDYffusion, a dynamics-informed diffusion framework that integrates PDE-based regularization and uncertainty-aware forecasting for stable long-term prediction. The proposed method consists of two key components: a PDE-regularized interpolator and a UKF-based forecaster. The interpolator incorporates a differential operator to enforce physically consistent intermediate states, while the forecaster leverages the Unscented Kalman Filter to explicitly model uncertainty and mitigate error accumulation during iterative prediction. We provide theoretical analyses showing that the proposed interpolator satisfies PDE-constrained smoothness properties, and that the forecaster converges under the proposed loss formulation. Extensive experiments on multiple dynamical datasets demonstrate that PDYffusion achieves superior performance in terms of CRPS and MSE, while maintaining stable uncertainty behavior measured by SSR. We further analyze the inherent trade-off between prediction accuracy and uncertainty, showing that our method provides a balanced and robust solution for long-horizon forecasting.

---

## 论文详细总结（自动生成）

以下是对论文《PDE-regularized Dynamics-informed Diffusion with Uncertainty-aware Filtering for Long-Horizon Dynamics》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
长程时空动力学预测（如气象、流体、物理模拟）面临三大挑战：**累积误差、噪声放大以及物理一致性的缺失**。
*   **研究动机**：现有的深度学习模型（如 RNN、LSTM）在长程预测中容易产生漂移；生成式模型（如 GAN、扩散模型）虽然能建模不确定性，但大多基于纯数据驱动的均方误差（MSE）目标，忽略了底层物理定律（PDE），导致预测结果在物理上不可行。
*   **核心目标**：提出一种名为 **PDYffusion** 的框架，将扩散模型的概率建模能力与偏微分方程（PDE）约束及贝叶斯滤波（UKF）相结合，以实现稳定、物理一致的长程预测。

### 2. 方法论：核心思想与关键技术
PDYffusion 将预测过程分解为两个互补阶段：
*   **PDE 正则化插值器 (PDE-regularized Interpolator)**：
    *   **核心思想**：在扩散模型的正向/插值过程中，不仅要求重建准确，还要求中间状态满足物理约束。
    *   **技术细节**：引入了一个基于微分算子的正则化项（源自 Matérn 协方差结构的 SPDE 形式）。损失函数在 MSE 基础上增加了 PDE 约束项，强制插值轨迹符合特定的平滑性和物理演化规律。
*   **基于 UKF 的预测器 (UKF-based Forecaster)**：
    *   **核心思想**：将迭代预测视为一个非线性状态估计问题，利用无迹卡尔曼滤波（UKF）显式追踪不确定性。
    *   **技术细节**：预测器不再只输出单一状态，而是通过确定性采样的“西格玛点（Sigma Points）”传播均值和协方差。损失函数采用负对数似然（NLL），包含马氏距离项（惩罚预测偏差）和对数行列式项（正则化不确定性范围），从而抑制闭环预测中的误差累积。

### 3. 实验设计
*   **数据集/场景**：
    1.  **SST (Sea Surface Temperature)**：真实世界的海面温度数据（复杂、多噪）。
    2.  **Navier-Stokes**：不可压缩流体动力学模拟。
    3.  **Spring-mesh**：弹性变形动力学。
    4.  **Wave**：振荡波传播。
*   **Benchmark 与对比方法**：
    *   **基础扩散模型**：DDPM, MCVD。
    *   **动力学感知模型**：DYffusion（本文的直接基准）。
    *   **不确定性基准**：基于扰动（Perturbation）和 Dropout 的变体。
*   **评估指标**：CRPS（概率校准度）、MSE（点预测精度）、SSR（谱稳定性和动力学一致性）。

### 4. 资源与算力
*   **算力说明**：论文**未明确列出**具体的 GPU 型号、数量或总训练时长。
*   **效率体现**：在实验结果表中列出了各方法的推理时间（Time [s]）。PDYffusion 的推理速度慢于基础的 DYffusion（因为 UKF 需要处理多个西格玛点），但远快于传统的扩散模型（如 MCVD）。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从合成物理模拟到真实复杂系统的 4 类数据集，每项实验均基于 5 次独立运行取平均值。
*   **消融与分析**：
    *   **边界条件分析**：对比了 Dirichlet、Neumann 和周期性边界条件对模型性能的影响。
    *   **超参数调节**：对 PDE 正则化强度 $\lambda$ 进行了敏感性分析。
    *   **权衡分析**：深入探讨了预测精度（MSE）与动力学稳定性（SSR）之间的内在权衡。
*   **充分性评价**：实验设计较为全面，不仅验证了精度提升，还通过理论证明（收敛性分析）和定性可视化（波形轨迹）支撑了方法的有效性，实验结果客观且具有说服力。

### 6. 主要结论与发现
*   **性能优越性**：PDYffusion 在多数数据集（尤其是 Navier-Stokes 和 Wave）上的 CRPS 和 MSE 均达到最优。
*   **物理一致性**：引入 PDE 正则化显著提升了预测轨迹的物理合理性，减少了长程预测中的虚假振荡。
*   **误差抑制**：UKF 机制能有效校准预测不确定性，防止模型在多步迭代中陷入非物理区域。
*   **数据集差异**：在噪声极大的真实 SST 数据上，MCVD 表现较好，说明 PDYffusion 的物理偏置在高度随机的系统中可能受限。

### 7. 优点
*   **理论与实践结合**：不仅提出了算法，还提供了插值器误差界限和预测器收敛性的数学证明。
*   **显式不确定性建模**：相比于普通扩散模型的隐式采样，UKF 提供了更具解释性的不确定性传播机制。
*   **物理启发式设计**：将经典的 PDE 采样理论融入深度学习框架，增强了模型的归纳偏置。

### 8. 不足与局限
*   **计算开销**：由于 UKF 需要传播多个西格玛点，其计算复杂度随状态维度增加，推理时间约为基础模型的 2-4 倍。
*   **超参数敏感性**：PDE 正则化项的权重 $\lambda$ 需要针对不同数据集仔细调优，过大可能导致欠拟合。
*   **随机动力学支持不足**：目前主要针对确定性 PDE 约束，对于本质随机的系统（如金融随机微分方程 SPDE），其表现和适应性尚待进一步验证。

（完）
