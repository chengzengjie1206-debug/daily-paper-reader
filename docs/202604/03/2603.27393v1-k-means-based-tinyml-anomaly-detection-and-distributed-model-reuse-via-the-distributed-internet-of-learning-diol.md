---
title: K-Means Based TinyML Anomaly Detection and Distributed Model Reuse via the Distributed Internet of Learning (DIoL)
title_zh: 基于 K-Means 的 TinyML 异常检测以及通过分布式学习物联网 (DIoL) 实现的分布式模型复用
authors: "Abdulrahman Albaiz, Fathi Amsaad"
date: 2026-03-28
pdf: "https://arxiv.org/pdf/2603.27393v1"
tags: ["query:ts"]
score: 9.0
evidence: 用于家电行为的K-Means异常检测
tldr: 本研究针对资源受限的微控制器（MCU），提出了一种基于K-Means的轻量级异常检测模型，并引入了分布式学习互联网（DIoL）框架。该框架允许在单个MCU上训练模型并将其导出为可移植的文本表示，从而在其他设备上直接复用，实现“一次训练，到处共享”（TOSE）。通过冰箱功耗监测实验证明，该方法在保持检测性能的同时，显著降低了大规模TinyML部署的重训练成本。
selection_source: fresh_fetch
motivation: 旨在解决资源受限的嵌入式设备在部署TinyML模型时，重复训练成本高且难以跨设备共享模型的问题。
method: 提出基于K-Means的异常检测算法，并开发DIoL工作流，通过文本化模型表示实现跨MCU的模型分发与复用。
result: 实验表明，DIoL框架在保持异常检测准确性的同时，解析开销极低，且推理运行时间与本地训练模型完全一致。
conclusion: 该研究为大规模、低成本的分布式TinyML部署提供了一种可行且高效的“一次训练，到处共享”解决方案。
---

## 摘要
本文提出了一种轻量级 K-Means 异常检测模型和分布式模型共享工作流，专为资源受限的微控制器 (MCU) 设计。该系统利用小型冰箱电器的真实功率测量数据，在设备端进行特征提取、聚类和阈值估计，以识别电器的异常行为。为了避免在每个设备上重新训练模型，我们引入了分布式学习物联网 (DIoL)，它允许将一个 MCU 上训练的模型导出为便携式文本表示，并直接在其他设备上复用。一个双设备原型通过真实电器案例研究证明了“一次训练，到处共享” (TOSE) 方法的可行性，其中设备 A 负责训练模型，设备 B 在不进行重新训练的情况下执行推理。实验结果表明，独立运行与基于 DIoL 的运行在异常检测行为上保持一致，解析开销可以忽略不计，且推理运行时间完全相同。所提出的框架为大规模嵌入式设备群实现了可扩展、低成本的 TinyML 部署。

## Abstract
This paper presents a lightweight K-Means anomaly detection model and a distributed model-sharing workflow designed for resource-constrained microcontrollers (MCUs). Using real power measurements from a mini-fridge appliance, the system performs on-device feature extraction, clustering, and threshold estimation to identify abnormal appliance behavior. To avoid retraining models on every device, we introduce the Distributed Internet of Learning (DIoL), which enables a model trained on one MCU to be exported as a portable, text-based representation and reused directly on other devices. A two-device prototype demonstrates the feasibility of the "Train Once, Share Everywhere" (TOSE) approach using a real-world appliance case study, where Device A trains the model and Device B performs inference without retraining. Experimental results show consistent anomaly detection behavior, negligible parsing overhead, and identical inference runtimes between standalone and DIoL-based operation. The proposed framework enables scalable, low-cost TinyML deployment across fleets of embedded devices.

---

## 论文详细总结（自动生成）

这是一份关于论文《K-Means Based TinyML Anomaly Detection and Distributed Model Reuse via the Distributed Internet of Learning (DIoL)》的结构化总结：

### 1. 核心问题与研究动机
*   **研究背景**：在物联网（IoT）环境中，利用微控制器（MCU）对家电功耗进行异常检测（如识别机械故障或不安全运行）具有重要意义。
*   **核心问题**：
    1.  **资源受限**：在 MCU 上进行端到端训练计算量大且耗能。
    2.  **缺乏标准**：目前缺乏在不同 MCU 之间存储和复用 TinyML 模型的标准化机制，导致相同设备往往需要重复训练。
    3.  **通信瓶颈**：现有的联邦学习或分布式学习通常依赖服务器和高带宽，不适合低功耗、离线的嵌入式环境。
*   **研究目的**：提出一种轻量级框架，实现“一次训练，到处共享”（TOSE），降低大规模部署 TinyML 的成本。

### 2. 方法论
*   **核心思想**：结合轻量级 K-Means 聚类算法与分布式学习物联网（DIoL）工作流，通过文本化模型文件实现跨设备复用。
*   **关键技术细节**：
    *   **特征提取**：从电流波形中提取 5 个关键特征：均方根（RMS）、滚动平均值、标准差、RMS 斜率、压缩机开启持续时间。
    *   **K-Means 聚类**：设置 $k=3$（对应压缩机关闭、稳定运行、瞬态/高负载三种状态）。使用 Lloyd 迭代优化，并在 MCU 上实现确定性内存分配。
    *   **异常判定**：计算特征向量到最近质心的欧氏距离，若超过基于训练集 95 百分位数设定的阈值，则判定为异常。
    *   **DIoL 工作流**：训练完成后，MCU 将质心坐标、归一化统计数据（均值/标准差）和阈值序列化为 `MODEL.TXT` 存储在 microSD 卡中。其他设备可直接读取该文件进行推理，跳过训练阶段。

### 3. 实验设计
*   **实验场景**：以小型冰箱（Mini-fridge）为案例，监测其 14 天的真实功率数据。
*   **数据集**：包含 43,285 条特征记录，其中前 20%（约 8,600 条）用于初始稳定期的模型训练，其余用于推理测试。
*   **Benchmark（基准）**：对比了作者之前开发的基于 **Z-Score** 的异常检测方法。
*   **对比实验**：
    *   **独立运行 vs. DIoL 复用**：对比设备 A（本地训练+推理）与设备 B（加载 A 的模型进行推理）的性能一致性。
    *   **算法对比**：对比 K-Means 与 Z-Score 在检测行为、运行时间和内存占用上的差异。

### 4. 资源与算力
*   **硬件平台**：STM32F446RE 微控制器（基于 ARM Cortex-M4 内核），配备 ACS712 电流传感器和 microSD 卡模块。
*   **算力消耗**：
    *   **训练时长**：K-Means 训练（针对 8,600 条记录）耗时约 **59,328 毫秒**（约 59 秒）。
    *   **推理时长**：处理 43,285 条记录耗时约 **58,518 毫秒**（约 1.35 毫秒/条）。
    *   **内存/存储**：运行在 sub-100 KB 的内存限制内，不使用动态内存分配。
*   **GPU 使用**：未涉及 GPU，完全在低功耗 MCU 上完成。

### 5. 实验数量与充分性
*   **实验规模**：进行了为期 14 天的连续监测，处理了超过 4 万条记录。
*   **实验设计**：通过双设备原型验证了 DIoL 的可行性。
*   **充分性评价**：实验在单一电器（冰箱）上验证了端到端的闭环过程，证明了模型迁移的等效性。虽然样本种类（仅一种电器）较少，但对于证明“模型复用”这一概念验证（PoC）而言是充分且客观的。

### 6. 主要结论与发现
*   **性能一致性**：通过 DIoL 复用的模型（设备 B）与原始训练模型（设备 A）在异常检测结果上完全一致，实现了 100% 的注入异常召回率。
*   **开销极低**：DIoL 的模型解析开销几乎可以忽略不计，推理速度与本地训练模型相同。
*   **优越性**：相比 Z-Score，K-Means 提供了更平滑的决策边界和多变量处理能力，且运行效率相当。

### 7. 优点
*   **实用性强**：解决了 TinyML 部署中“重复训练”的痛点，提出的 `MODEL.TXT` 格式简单易读，适合资源极度受限的设备。
*   **低功耗设计**：通过离线、基于存储卡的模型共享，摆脱了对云端和持续网络连接的依赖。
*   **确定性**：算法实现考虑了嵌入式约束，避免了动态内存分配，保证了系统的稳定性。

### 8. 不足与局限
*   **实验覆盖面有限**：仅在一种电器（冰箱）上进行了测试，尚未验证在功耗模式更复杂的电器（如洗衣机）上的表现。
*   **安全性缺失**：目前的 DIoL 框架尚未包含模型文件的完整性校验、加密或身份验证机制，容易受到恶意模型替换攻击。
*   **扩展性挑战**：虽然提出了 DIoL 概念，但目前依赖物理介质（microSD 卡）传输模型，在大规模自动化部署方面仍有优化空间（如通过 BLE 或 Zigbee 传输）。

（完）
