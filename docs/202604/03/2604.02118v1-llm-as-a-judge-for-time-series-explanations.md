---
title: LLM-as-a-Judge for Time Series Explanations
title_zh: LLM 作为时间序列解释的评判者
authors: "Preetham Sivalingam, Murari Mandal, Saurabh Deshpande, Dhruv Kumar"
date: 2026-04-02
pdf: "https://arxiv.org/pdf/2604.02118v1"
tags: ["query:ts"]
score: 9.0
evidence: LLM作为评判者评估时间序列解释
tldr: 本研究针对时间序列解释缺乏有效评估手段的问题，提出了“LLM-as-a-Judge”框架。该方法无需参考答案，通过LLM对解释的模式识别、数值准确性和忠实度进行三元评分。研究构建了包含350个案例的合成基准，涵盖七种查询类型。实验揭示了生成与评估之间的不对称性：尽管模型在某些复杂模式生成上表现不佳，但在评估和排序解释方面表现出较高的稳定性和可靠性，证明了LLM作为时间序列推理评估器的潜力。
selection_source: fresh_fetch
motivation: 传统评估方法依赖参考文本或仅限数值分析，无法直接验证自由文本解释与底层时间序列数据之间的一致性。
method: 提出一种无参考评估框架，利用LLM作为裁判对解释进行多维度评分，并建立了一个包含多种查询类型的合成基准数据集。
result: 实验显示生成任务受模式影响波动巨大（准确率0.00至0.96），而评估任务在模型生成错误时仍能保持较高的排序和评分准确性。
conclusion: LLM在时间序列领域展现出作为可靠评估器的潜力，能够有效验证基于数据的推理过程，弥补了生成能力的不足。
---

## 摘要
评估基于时间序列数据的大语言模型（LLM）生成的自然语言解释的事实准确性仍然是一个开放性挑战。尽管现代模型能够生成数值信号的文本解释，但现有的评估方法存在局限性：基于参考的相似度指标和一致性检查模型需要标准答案（ground truth）解释，而传统的时间序列方法仅针对数值进行操作，无法评估自由形式的文本推理。因此，目前尚不存在一种通用方法，能够在没有预定义参考或特定任务规则的情况下，直接验证解释是否忠实于底层时间序列数据。我们研究了在无参考设置下，大语言模型作为时间序列解释的生成器和评估者的表现。在这种设置中，给定时间序列、问题和候选解释，评估者根据模式识别、数值准确性和回答忠实度分配三元正确性标签，从而实现原则性的评分和比较。为了支持这一研究，我们构建了一个包含 350 个时间序列案例的合成基准，涵盖七种查询类型，每种类型都配有正确、部分正确和错误的解释。我们在四个任务上评估了模型：解释生成、相对排序、独立评分和多异常检测。结果显示出明显的不对称性：生成过程高度依赖于模式，并在某些查询类型上表现出系统性失败（例如，“季节性下降”和“波动性偏移”的准确率在 0.00 到 0.12 之间，而“结构性断裂”的准确率则在 0.94 到 0.96 之间），而评估过程则更为稳定，即使模型自身的输出不正确，它们也能正确地对解释进行排序和评分。这些发现证明了基于数据驱动的 LLM 对时间序列解释进行评估的可行性，并突显了它们作为时间序列领域数据驱动推理可靠评估者的潜力。

## Abstract
Evaluating factual correctness of LLM generated natural language explanations grounded in time series data remains an open challenge. Although modern models generate textual interpretations of numerical signals, existing evaluation methods are limited: reference based similarity metrics and consistency checking models require ground truth explanations, while traditional time series methods operate purely on numerical values and cannot assess free form textual reasoning. Thus, no general purpose method exists to directly verify whether an explanation is faithful to underlying time series data without predefined references or task specific rules. We study large language models as both generators and evaluators of time series explanations in a reference free setting, where given a time series, question, and candidate explanation, the evaluator assigns a ternary correctness label based on pattern identification, numeric accuracy, and answer faithfulness, enabling principled scoring and comparison. To support this, we construct a synthetic benchmark of 350 time series cases across seven query types, each paired with correct, partially correct, and incorrect explanations. We evaluate models across four tasks: explanation generation, relative ranking, independent scoring, and multi anomaly detection. Results show a clear asymmetry: generation is highly pattern dependent and exhibits systematic failures on certain query types, with accuracies ranging from 0.00 to 0.12 for Seasonal Drop and Volatility Shift, to 0.94 to 0.96 for Structural Break, while evaluation is more stable, with models correctly ranking and scoring explanations even when their own outputs are incorrect. These findings demonstrate feasibility of data grounded LLM based evaluation for time series explanations and highlight their potential as reliable evaluators of data grounded reasoning in the time series domain.

---

## 论文详细总结（自动生成）

以下是对论文《LLM-as-a-Judge for Time Series Explanations》的结构化深度总结：

### 1. 核心问题与整体含义
*   **研究动机**：随着 LLM 被广泛用于金融、医疗等领域的时间序列数据解释，如何验证这些“自然语言解释”的**事实准确性**（即文本是否忠实于底层数值数据）成为关键挑战。
*   **核心矛盾**：
    *   **传统文本指标**（如 BLEU、ROUGE）依赖参考答案，且仅关注语义相似度，无法识别数值错误。
    *   **传统时间序列分析**仅处理数值，无法理解自由格式的文本推理。
    *   **现有 LLM 评判者**多用于纯文本任务，在“数值驱动”的推理评估方面缺乏通用框架。
*   **研究目标**：探索 LLM 在无参考（Reference-free）设置下，直接根据原始数值数据评估时间序列解释的可行性。

### 2. 方法论
*   **核心思想**：采用**准则引导（Rubric-guided）**的“LLM 作为评判者”框架。将评估任务建模为基于数值证据的逻辑推理过程，而非简单的文本匹配。
*   **评估流程**：
    1.  **输入**：单变量时间序列 $T$、关于该序列的自然语言问题 $q$、待评估的候选解释 $e$。
    2.  **三元分类标签**：评判者输出 $y \in \{0, 1, 2\}$。其中 0 代表错误，1 代表部分正确（存在数值偏差），2 代表完全正确。
    3.  **多维度准则**：提示词引导模型关注数据忠实度（模式识别）、数值准确性（指标/索引）、问题相关性、逻辑连贯性以及是否存在幻觉（无中生有的声明）。
*   **关键技术细节**：模型被明确要求**直接验证原始数据**，不得盲信解释中提到的数值。

### 3. 实验设计
*   **数据集（TSQueryBench）**：
    *   **合成基准**：包含 350 个时间序列实例，涵盖 7 种查询类型（线性尖峰、季节性下降、结构性断裂、多指标一致性、相对极值、均值偏移、波动性偏移）。
    *   **序列长度**：分布在 100、200、300 和 500 个时间步。
    *   **候选解释**：为每个实例人工/规则构造了正确、部分正确和错误三种解释，共 1050 个评估样本。
*   **评估任务**：
    1.  **解释生成**：直接生成解释并由人工/模型评分。
    2.  **相对排序**：从三个候选解释中选出最佳者。
    3.  **独立评分**：对单个解释进行三元分类。
    4.  **多异常检测**：直接从数值中识别所有异常点及其变化率。
*   **对比方法**：评估了三个主流开源模型：**Qwen-3 8B**、**LLaMA-3 8B** 和 **Gemma-2 9B IT**。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量或总训练/推理时长。
*   **实验设置**：实验是在 **Zero-shot（零样本）** 设置下进行的，不涉及模型微调，主要消耗在于推理阶段。输出被限制为结构化 JSON 格式以方便解析。

### 5. 实验数量与充分性
*   **实验规模**：
    *   涵盖了 7 种不同的时间序列模式，每种模式 50 个案例。
    *   跨越了 4 种不同的序列长度，验证了模型处理长序列的能力。
    *   进行了 4 类核心任务的交叉验证。
*   **充分性评价**：实验设计较为**充分且系统**。通过对比生成与评估的表现，揭示了模型能力的解耦。然而，由于主要使用合成数据，其在真实世界复杂噪声数据下的鲁棒性仍有待进一步验证。

### 6. 主要结论与发现
*   **生成与评估的不对称性**：这是最重要的发现。模型在**生成**某些复杂模式（如波动性偏移）的解释时表现极差（准确率接近 0），但在**评估**和**排序**这些解释时却表现出极高的准确性和稳定性。
*   **模式依赖性**：所有模型在处理“结构性断裂”时表现优异，但在处理“波动性偏移”和“季节性下降”等高阶统计属性时普遍挣扎。
*   **评估的稳定性**：相比于开放式生成，结构化提示词引导下的评估任务受序列长度影响较小，表现更稳健。
*   **异常检测倾向**：在多异常检测任务中，模型倾向于“过度敏感”，即召回率高但精确率低（容易将微小波动误判为异常）。

### 7. 优点
*   **填补空白**：提出了首个针对时间序列解释的无参考、数据驱动的 LLM 评估框架。
*   **任务设计巧妙**：通过“排序”和“评分”任务证明了 LLM 可以作为比生成器更可靠的“裁判”。
*   **细粒度评估**：引入“部分正确”标签，能够有效捕捉 LLM 常见的数值微小幻觉问题。

### 8. 不足与局限
*   **数据局限性**：完全依赖合成数据，可能无法完全模拟金融或医疗等真实场景中的复杂非平稳特征。
*   **模型规模限制**：实验仅限于 8B-9B 规模的模型，未探讨更大规模模型（如 70B+）或闭源模型（如 GPT-4）在这一特定任务上的表现。
*   **统计推理能力不足**：实验表明 LLM 在处理方差变化（波动性）等深层统计特征时仍存在显著短板。
*   **Token 限制**：在处理 500 步以上的长序列时，部分模型（如 Gemma-2）出现了无法处理输入的问题。

（完）
