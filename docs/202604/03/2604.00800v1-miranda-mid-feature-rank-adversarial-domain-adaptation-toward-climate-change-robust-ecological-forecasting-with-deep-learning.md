---
title: "MIRANDA: MId-feature RANk-adversarial Domain Adaptation toward climate change-robust ecological forecasting with deep learning"
title_zh: MIRANDA：面向气候变化鲁棒的深度学习生态预测的中层特征秩对抗领域自适应
authors: "Yuchang Jiang, Jan Dirk Wegner, Vivien Sainte Fare Garnot"
date: 2026-04-01
pdf: "https://arxiv.org/pdf/2604.00800v1"
tags: ["query:ts"]
score: 9.0
evidence: 基于气象时间序列的深度学习生态预测
tldr: 针对气候变化导致的植物物候预测分布偏移问题，本文提出MIRANDA方法。传统深度学习模型在应对气候变暖引发的协变量和标签偏移时表现不如机械模型。MIRANDA通过在中间特征层引入基于排名的对抗性正则化，强制模型学习年份不变的气象表示。在长达70年的树木物候数据集上，该方法显著提升了模型对气候变化的鲁棒性，缩小了与机械模型的性能差距。
selection_source: fresh_fetch
motivation: 深度学习在植物物候预测中虽表现优异，但在气候变化导致的非平稳数据分布下，其泛化能力和鲁棒性弱于传统的机械模型。
method: 提出MIRANDA框架，通过在中间特征层应用基于排名的对抗任务来学习年份不变的特征表示，以应对连续的时间域偏移。
result: 在包含5种树木、跨越70年的大规模物候数据集上，MIRANDA有效缓解了分布偏移带来的性能下降。
conclusion: MIRANDA证明了通过中间特征的秩对抗域自适应可以增强生态预测模型的鲁棒性，为气候变化背景下的深度学习应用提供了新思路。
---

## 摘要
植物物候建模旨在根据气象时间序列预测季节性阶段（如展叶或开花）的时间。可靠的预测对于预判生态系统对气候变化的响应至关重要。虽然物候建模传统上依赖于机制模型，但深度学习方法作为灵活的数据驱动替代方案，近年来被提出并通常表现出更优越的性能。然而，当气候变化引起数据分布偏移时，机制模型往往优于深度网络。领域自适应（DA）技术可能有助于解决这一局限性。然而，与标准的领域自适应设置不同，气候变化诱发了域的时间连续性，并涉及协变量偏移和标签偏移，表现为气温记录升高和春季开始提前。为了应对这一挑战，我们提出了中层特征秩对抗领域自适应（MIRANDA）。传统的对抗方法在最终的潜在表示上强制执行域不变性，这种方法并未明确解决标签偏移问题，而我们将对抗正则化应用于中间特征。此外，我们采用基于秩的目标函数，而非二元域分类目标，从而在学习到的气象表示中强制执行年份不变性。在一个跨越 70 年、包含 5 个树种 67,800 次物候观测的国家级数据集上，我们证明了与传统的领域自适应方法不同，MIRANDA 提高了对气候分布偏移的鲁棒性，并缩小了与机制模型之间的性能差距。

## Abstract
Plant phenology modelling aims to predict the timing of seasonal phases, such as leaf-out or flowering, from meteorological time series. Reliable predictions are crucial for anticipating ecosystem responses to climate change. While phenology modelling has traditionally relied on mechanistic approaches, deep learning methods have recently been proposed as flexible, data-driven alternatives with often superior performance. However, mechanistic models tend to outperform deep networks when data distribution shifts are induced by climate change. Domain Adaptation (DA) techniques could help address this limitation. Yet, unlike standard DA settings, climate change induces a temporal continuum of domains and involves both a covariate and label shift, with warmer records and earlier start of spring. To tackle this challenge, we introduce Mid-feature Rank-adversarial Domain Adaptation (MIRANDA). Whereas conventional adversarial methods enforce domain invariance on final latent representations, an approach that does not explicitly address label shift, we apply adversarial regularization to intermediate features. Moreover, instead of a binary domain-classification objective, we employ a rank-based objective that enforces year-invariance in the learned meteorological representations. On a country-scale dataset spanning 70 years and comprising 67,800 phenological observations of 5 tree species, we demonstrate that, unlike conventional DA approaches, MIRANDA improves robustness to climatic distribution shifts and narrows the performance gap with mechanistic models.

---

## 论文详细总结（自动生成）

以下是对论文《MIRANDA: MId-feature RANk-adversarial Domain Adaptation toward climate change-robust ecological forecasting with deep learning》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：深度学习（DL）模型在植物物候预测（如预测展叶、开花日期）中虽表现优异，但在气候变化导致的“分布偏移”下鲁棒性不足。
*   **研究背景**：气候变化不仅改变了气象输入（协变量偏移），还导致物候事件普遍提前（标签偏移）。传统的机制模型（基于生理假设）在极端气候下比数据驱动的深度学习模型更稳健。
*   **研究目标**：开发一种无监督领域自适应（UDA）方法，使深度学习模型能够利用未标记的未来气象数据，提高在气候变化背景下的外推能力和预测鲁棒性。

### 2. 方法论
MIRANDA 的核心思想是针对气候变化的连续性和标签偏移特性，对模型进行定制化改造：
*   **中层特征对齐（Mid-feature Alignment）**：不同于传统方法在最终输出层对齐特征（这会抹除预测所需的标签偏移），MIRANDA 在 Transformer 的中间层施加对抗正则化，允许后续层保留领域特定的变化。
*   **秩对抗目标（Rank-based Adversarial Objective）**：放弃二元域分类（源域 vs 目标域），引入 **Rank-N-Contrast 损失**。该损失利用年份信息作为引导，强制模型学习“年份不变”的表示，使时间上接近的样本在特征空间更相似，从而捕捉气候变化的连续趋势。
*   **混合层归一化（Hybrid Layer Normalization, hLN）**：在推理阶段，使用源域的运行统计数据来归一化目标域特征。这确保了特征缩放的一致性，同时允许目标域的潜在表示具有不同的范围，以适应物候日期的偏移。
*   **算法流程**：输入气象序列 -> 线性嵌入 -> 第一层 Transformer -> **中层特征提取并进行秩对抗训练（梯度反转层）** -> 第二层 Transformer（含 hLN） -> 线性解码器输出预测日期。

### 3. 实验设计
*   **数据集**：瑞士物候网络（SPN），包含 70 年（1951-2022）、175 个站点、5 种树木的 67,800 条观测记录。
*   **场景设置（三种偏移强度）**：
    1.  **按年代划分**：历史数据训练，近期数据测试。
    2.  **按气温划分**：在较冷年份训练，在最热的 15% 年份测试（模拟变暖）。
    3.  **按海拔划分**：低海拔训练，高海拔测试（最严峻的分布偏移）。
*   **对比方法（Benchmark）**：
    *   **深度学习基准**：Vanilla PhenoFormer。
    *   **领域自适应方法**：DANN、ADDA、CORAL、DANL、AdaBN。
    *   **机制模型**：经典的 M1 过程模型（传统生态学标杆）。

### 4. 资源与算力
*   **硬件**：使用了一块 **NVIDIA GeForce RTX 3080 GPU**。
*   **训练时长**：MIRANDA 训练约需 **26 分钟**。相比之下，机制模型 M1 需要约 150 分钟（因为需要为每个物种单独训练）。
*   **超参数**：Batch size 为 16，学习率为 $10^{-4}$，最多训练 300 个 epoch。

### 5. 实验数量与充分性
*   **实验规模**：针对 3 种数据划分方案、5 个物种分别进行了实验，每组实验重复 10 次并取均值和标准差。
*   **消融实验**：详细评估了中层特征对齐、秩对抗损失（$L_{rank}$）和混合归一化（hLN）各自的贡献。
*   **充分性评价**：实验设计非常充分且具有针对性。通过海拔划分模拟极端偏移，客观地展示了传统 DA 方法在标签偏移面前的失效，验证了 MIRANDA 的必要性。

### 6. 主要结论与发现
*   **性能领先**：在所有深度学习方法中，MIRANDA 表现最优，尤其在极端偏移（海拔划分）下，其 $R^2$ 显著高于其他 DA 方法（其他方法甚至出现负值）。
*   **缩小差距**：MIRANDA 极大地缩小了深度学习与机制模型（M1）之间的鲁棒性差距，在某些指标上已非常接近。
*   **中层对齐的必要性**：消融实验证明，在中间层而非顶层进行对齐是处理标签偏移的关键。
*   **稳定性**：MIRANDA 在不同物种和运行次数间的标准差较小，表现出比机制模型更高的预测稳定性。

### 7. 优点
*   **创新性对齐策略**：敏锐地捕捉到气候预测中“连续域”和“标签偏移”的特殊性，提出的秩对抗和中层对齐具有很强的物理/生态学解释力。
*   **高效性**：单模型支持多物种预测，训练速度远快于需要逐一校准的机制模型。
*   **通用性**：虽然针对物候学，但其处理连续时间偏移的思想可推广至其他受气候变化影响的回归任务。

### 8. 不足与局限
*   **外推极限**：尽管有所改进，但在最极端的分布偏移下，其绝对性能（$R^2=0.22$）仍有提升空间，尚未完全超越机制模型。
*   **物种覆盖**：实验仅涵盖了 5 种主要树种，对于更广泛的生态系统（如草本植物或农作物）的适用性尚待验证。
*   **依赖目标域数据**：作为无监督自适应方法，它仍需要获取目标域（未来）的气象驱动数据才能进行对齐训练。

（完）
