---
title: "DySCo: Dynamic Semantic Compression for Effective Long-term Time Series Forecasting"
title_zh: DySCo：用于有效长程时间序列预测的动态语义压缩
authors: "Xiang Ao, Yinyu Tan, Mengru Chen"
date: 2026-04-01
pdf: "https://arxiv.org/pdf/2604.01261v1"
tags: ["query:ts"]
score: 10.0
evidence: 用于有效长期时序预测的动态语义压缩
tldr: 针对长程时间序列预测中增加回溯窗口带来的噪声和计算冗余问题，本文提出DySCo框架。该框架通过熵引导动态采样（EGDS）识别关键片段，利用层级频率增强分解（HFED）保留异常与趋势细节，并结合跨尺度交互混合器（CSIM）融合全局与局部信息。DySCo作为通用插件，在降低计算成本的同时显著提升了主流模型捕捉长程相关性的能力。
selection_source: fresh_fetch
motivation: 传统长窗口预测方法易引入无关噪声和计算冗余，难以有效捕捉复杂的长期依赖关系。
method: 提出包含熵引导动态采样、层级频率增强分解和跨尺度交互混合器的动态语义压缩框架。
result: 实验证明DySCo能作为即插即用模块，在减少计算开销的同时显著增强主流模型的长程预测性能。
conclusion: DySCo通过动态压缩冗余信息并保留关键语义，为高效且准确的长程时间序列预测提供了新方案。
---

## 摘要
时间序列预测 (TSF) 在金融、气象和能源等领域至关重要。虽然从理论上讲，延长回顾窗口可以提供更丰富的历史上下文，但在实践中，这往往会引入无关噪声和计算冗余，阻碍模型有效捕捉复杂的长程依赖关系。为了应对这些挑战，我们提出了一个动态语义压缩 (DySCo) 框架。与依赖固定启发式方法的传统方法不同，DySCo 引入了一种熵引导动态采样 (EGDS) 机制，能够自主识别并保留高熵片段，同时压缩冗余趋势。此外，我们结合了层级频率增强分解 (HFED) 策略，将高频异常与低频模式分离，确保在稀疏采样过程中保留关键细节。最后，设计了一个跨尺度交互混合器 (CSIM)，用于动态融合全局上下文与局部表示，取代了简单的线性聚合。实验结果表明，DySCo 作为一个通用的即插即用模块，在降低计算成本的同时，显著增强了主流模型捕捉长程相关性的能力。

## Abstract
Time series forecasting (TSF) is critical across domains such as finance, meteorology, and energy. While extending the lookback window theoretically provides richer historical context, in practice, it often introduces irrelevant noise and computational redundancy, preventing models from effectively capturing complex long-term dependencies. To address these challenges, we propose a Dynamic Semantic Compression (DySCo) framework. Unlike traditional methods that rely on fixed heuristics, DySCo introduces an Entropy-Guided Dynamic Sampling (EGDS) mechanism to autonomously identify and retain high-entropy segments while compressing redundant trends. Furthermore, we incorporate a Hierarchical Frequency-Enhanced Decomposition (HFED) strategy to separate high-frequency anomalies from low-frequency patterns, ensuring that critical details are preserved during sparse sampling. Finally, a Cross-Scale Interaction Mixer(CSIM) is designed to dynamically fuse global contexts with local representations, replacing simple linear aggregation. Experimental results demonstrate that DySCo serves as a universal plug-and-play module, significantly enhancing the ability of mainstream models to capture long-term correlations with reduced computational cost.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **DySCo（Dynamic Semantic Compression，动态语义压缩）** 的框架，旨在解决长程时间序列预测（TSF）中“回顾窗口越长，噪声和冗余越多”的悖论。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：在长程时间序列预测中，理论上增加历史回顾窗口（Lookback Window）能提供更多上下文，但实际上往往会导致预测精度下降。这是因为长序列引入了大量无关噪声和计算冗余，且远距离历史与当前的相关性减弱。
*   **研究动机**：盲目截断历史会丢失长周期规律（如周、月趋势）。因此，挑战在于如何从长序列中有效提取高价值信号，同时压缩冗余信息。

### 2. 方法论：DySCo 框架
DySCo 采用了一种可学习的范式来提炼关键依赖关系，包含三个核心组件：
*   **层级频率增强分解 (HFED)**：
    *   构建多粒度金字塔，将原始序列分解为不同尺度的表示。
    *   对长窗口应用低通滤波器（移动平均）以提取全局趋势并滤除高频噪声；对短窗口保留高频细节。
*   **熵引导动态采样 (EGDS)**：
    *   **核心思想**：不依赖固定的启发式规则（如“越旧的数据越不重要”），而是通过一个可学习的 MLP 评分器评估片段的“信息熵”（语义重要性）。
    *   **动态核计算**：采样密度由“距离衰减”和“语义调制”共同决定。如果远距离片段包含关键异常或复杂模式（高熵），模型会动态减小池化核，进行密集采样以保留细节。
*   **跨尺度交互混合器 (CSIM)**：
    *   取代简单的线性加权，使用门控机制（Gating Network）根据当前上下文动态调整不同尺度预测结果的权重，融合全局稳定性和局部灵敏度。

### 3. 实验设计
*   **数据集**：涵盖 7 个主流基准数据集，包括 ETT 系列（m1, m2, h1, h2）、Electricity、Traffic 和 Weather，涉及电力、交通、气象等多个领域。
*   **基准模型 (Baselines)**：对比了四种代表性架构：PatchTST、iTransformer、TimeMixer 和 Linear。
*   **对比策略**：
    *   将 DySCo 作为插件集成到上述模型中。
    *   对比了基础模型在固定长窗口（2440）下的表现，以及它们在各自最优调优窗口下的最佳表现。
    *   对比了不同的采样策略（均匀采样、随机采样、Top-k 频率采样）。

### 4. 资源与算力
*   **硬件**：实验在一台单卡 **NVIDIA 2080Ti GPU** 上完成。
*   **效率提升**：
    *   **参数量**：在长窗口（L=2440）下，DySCo 相比基础 Linear 模型减少了约 **58.7%** 的参数。
    *   **计算开销**：对于 Transformer 类模型（如 PatchTST），DySCo 将注意力机制的计算复杂度降低了约 **94.3%**。
    *   **显存与时间**：实验图表显示，DySCo 在显存占用和每轮训练时间上均显著优于处理全量长序列的基础模型。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了多维度测试，包括 7 个数据集上的主实验、针对三个核心组件的消融实验、四种采样策略的对比实验，以及对回顾窗口长度（从 720 到 5000）的敏感性分析。
*   **公平性**：作者采用了一种保守且严谨的评估协议，即 DySCo 使用固定的长窗口，而基准模型则通过广泛的超参数搜索寻找其各自的最优窗口进行对比。这种“以己之短比人之长”的策略证明了 DySCo 的稳健性。

### 6. 主要结论与发现
*   **性能提升**：DySCo 显著增强了现有模型捕捉长程相关性的能力，即使基础模型已经调至最优窗口，集成 DySCo 后的表现依然更优。
*   **效率突破**：通过将长序列压缩为 20% 采样率的稀疏表示，DySCo 成功解决了长上下文预测中的内存瓶颈问题。
*   **语义敏感性**：可视化结果证实，EGDS 能够准确识别并保留历史数据中的关键异常点和趋势转折点。

### 7. 优点与亮点
*   **即插即用**：作为一个通用模块，DySCo 可以轻松集成到线性、Transformer 或混合架构中。
*   **动态性**：打破了传统采样方法的僵化规则，实现了基于内容重要性的自适应压缩。
*   **理论支撑**：提供了详细的复杂度分析，证明了其在处理超长序列时的亚线性扩展优势。

### 8. 不足与局限
*   **采样率固定**：虽然采样位置是动态的，但整体采样率（如 20%）似乎是预设的超参数，未来可探索完全自适应的压缩率。
*   **极端场景**：在短周期模式占绝对主导的数据集（如 Electricity）上，DySCo 带来的精度提升相对有限（虽然效率提升依然显著）。
*   **未来验证**：论文主要在标准 TSF 数据集上验证，对于具有极高非平稳性或突发性极强的金融高频数据，其语义评分器的稳定性仍需进一步验证。

（完）
