---
title: Generative Modeling under Non-Monotonic MAR Missingness via Approximate Wasserstein Gradient Flows
title_zh: 基于近似 Wasserstein 梯度流的非单调 MAR 缺失下的生成式建模
authors: "Gitte Kremling, Jeffrey Näf, Johannes Lederer"
date: 2026-04-06
pdf: "https://arxiv.org/pdf/2604.04567v1"
tags: ["query:ts"]
score: 9.0
evidence: 从随机缺失数据中生成完整数据集的迭代方法
tldr: 针对数据科学中普遍存在的非单调随机缺失（MAR）问题，本文提出了 FLOWGEM。这是一种基于 Wasserstein 梯度流的生成式建模方法，通过最小化观测数据与生成样本之间的 KL 散度，利用粒子演化和局部线性密度比估计来迭代生成完整数据集。该方法在处理复杂缺失模式时具有理论严谨性，并在多项基准测试中达到了最先进的性能，为缺失数据插补提供了新思路。
selection_source: fresh_fetch
motivation: 现有的缺失值处理方法在应对非单调随机缺失时缺乏严谨的非参数化理论支持，且难以保证分布恢复的准确性。
method: 提出 FLOWGEM 方法，通过离散化的 Wasserstein 梯度流粒子演化，结合局部线性密度比估计，迭代地将初始粒子群推向目标分布。
result: 仿真实验和真实数据基准测试表明，FLOWGEM 在包括非单调 MAR 在内的多种设定下均达到了最先进的性能水平。
conclusion: FLOWGEM 为处理复杂缺失数据提供了一个兼具理论严谨性和实践高效性的生成式方案，有效缩小了理论与实证性能之间的差距。
---

## 摘要
数据科学中缺失值的普遍存在对后续分析构成了重大风险。尽管已有大量研究，但处理一般非单调缺失的有原则的非参数方法仍然稀缺。相反，通常使用权宜的插补方法，而这些方法是否能恢复正确的分布仍不明确。在本文中，我们提出了 FLOWGEM，这是一种从具有随机缺失（MAR）值的数据集中生成完整数据集的有原则的迭代方法。受忽略极大似然估计器收敛结果的启发，我们的方法最小化了观测数据分布与不同缺失模式下生成样本分布之间的期望 Kullback-Leibler (KL) 散度。为了最小化 KL 散度，我们采用了相应 Wasserstein 梯度流的离散化粒子演化，其中速度场使用密度比的局部线性估计器进行近似。这种构造产生了一种数据生成方案，该方案迭代地将初始粒子系综向目标分布传输。仿真研究和真实数据基准测试表明，FLOWGEM 在一系列设置中均达到了最先进的性能，包括具有挑战性的非单调 MAR 机制。总之，这些结果使 FLOWGEM 成为现有插补方法的一种有原则且实用的替代方案，也是缩小理论严谨性与经验性能之间差距的决定性一步。

## Abstract
The prevalence of missing values in data science poses a substantial risk to any further analyses. Despite a wealth of research, principled nonparametric methods to deal with general non-monotone missingness are still scarce. Instead, ad-hoc imputation methods are often used, for which it remains unclear whether the correct distribution can be recovered. In this paper, we propose FLOWGEM, a principled iterative method for generating a complete dataset from a dataset with values Missing at Random (MAR). Motivated by convergence results of the ignoring maximum likelihood estimator, our approach minimizes the expected Kullback-Leibler (KL) divergence between the observed data distribution and the distribution of the generated sample over different missingness patterns. To minimize the KL divergence, we employ a discretized particle evolution of the corresponding Wasserstein Gradient Flow, where the velocity field is approximated using a local linear estimator of the density ratio. This construction yields a data generation scheme that iteratively transports an initial particle ensemble toward the target distribution. Simulation studies and real-data benchmarks demonstrate that FLOWGEM achieves state-of-the-art performance across a range of settings, including the challenging case of non-monotonic MAR mechanisms. Together, these results position FLOWGEM as a principled and practical alternative to existing imputation methods, and a decisive step towards closing the gap between theoretical rigor and empirical performance.

---

## 论文详细总结（自动生成）

以下是对论文《Generative Modeling under Non-Monotonic MAR Missingness via Approximate Wasserstein Gradient Flows》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：如何在数据存在“随机缺失”（Missing at Random, MAR），特别是复杂的**非单调缺失**（Non-monotonic missingness）情况下，恢复原始数据的真实分布并生成完整的样本。
*   **研究动机**：
    *   **理论与实践的脱节**：现有的插补方法（如 MICE, GAIN）在实证上表现良好但缺乏理论保证；而有理论支撑的方法（如基于高斯混合的模型）在处理复杂真实数据时表现不佳。
    *   **非单调缺失的挑战**：在非单调 MAR 机制下，观测到的数据分布在不同缺失模式之间可能存在偏移，导致传统的 M-估计器失效。
    *   **生成式需求**：数据分析通常关注分布特征而非单个缺失值，因此生成一个符合原始分布的完整数据集比单纯“填补”空格更具统计意义。

### 2. 方法论：FLOWGEM
*   **核心思想**：将数据生成过程建模为一个**Wasserstein 梯度流（WGF）**。通过迭代演化一组初始粒子，使其分布逐渐逼近目标分布。
*   **关键技术细节**：
    *   **目标函数**：受极大似然估计启发，最小化观测数据分布与生成样本分布之间的**期望 KL 散度**（加权平均所有缺失模式）。
    *   **粒子演化**：使用前向欧拉法离散化常微分方程（ODE），通过速度场引导粒子移动。
    *   **速度场近似**：利用**局部线性估计器**来近似密度比的梯度。在每个时间步，通过解决一个局部加权的线性方程组来确定粒子的移动方向。
    *   **线性求解器**：针对 KL 散度，优化问题简化为线性方程组求解，避免了复杂的神经网络训练或梯度下降，提高了计算效率和数值稳定性。

### 3. 实验设计
*   **数据集/场景**：
    *   **仿真实验**：设计了一个具有挑战性的 3 维非单调 MAR 场景（均匀分布和高斯分布），专门用于测试方法处理复杂缺失机制的能力。
    *   **真实数据**：选取了 10 个来自 UCI 和 OpenML 的真实数据集（如 scm1d, parkinsons, concrete 等），维度从 6 维到 272 维不等，样本量从 215 到 4901 不等。
*   **Benchmark 与对比方法**：
    *   对比了 7 种主流方法：**MICE**（经典统计）、**GAIN**（GAN 架构）、**Hyperimpute**（自动机器学习）、**MIRI**（整流流）、**Bayes**（非参数贝叶斯）、**MissDiff**（扩散模型）和 **NewImp**（梯度流插补）。
    *   **评估指标**：能量距离（Energy Distance，衡量分布相似度）和分位数估计准确度。

### 4. 资源与算力
*   **硬件环境**：实验使用了 **Google Colaboratory Pro** 平台，配备 **TPU v6e-1** 加速器。
*   **软件环境**：Python 3.11。
*   **训练时长**：文中未详细列出每个实验的总时长，但提供了仿真实验的平均运行时间（FLOWGEM 约为 22.85 秒，显著快于 MIRI 和 Bayes 方法的 1600-2000 秒）。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 2 组仿真场景（每组 20 次重复实验）和 10 个不同规模、不同维度的真实数据集。
*   **充分性与公平性**：
    *   实验覆盖了从低维到高维、从小样本到大样本的多种情况。
    *   **超参数统一**：在所有实验中，FLOWGEM 使用了相同的步长、迭代次数和启发式带宽选择，证明了其鲁棒性。
    *   **对比公正**：使用了标准的 `mice` R 包生成 MAR 缺失，并采用了标准化的能量距离作为评估指标。

### 6. 主要结论与发现
*   **性能领先**：FLOWGEM 在几乎所有真实数据集上的能量距离均为最低或次低，显著优于基于深度学习的方法（如 GAIN, MIRI）。
*   **理论验证**：证明了在 MAR 假设和正性条件下，最小化期望 KL 散度确实能唯一恢复真实分布。
*   **非单调处理能力**：在专门设计的非单调 MAR 仿真中，FLOWGEM 在分位数估计和分布恢复上均表现出最强的准确性。

### 7. 优点
*   **理论严谨**：建立了从群体一致性到渐近误差界的完整理论框架。
*   **无需调参**：相比神经网络方法，FLOWGEM 只有三个超参数（步长、时间步、带宽），且对启发式设置不敏感。
*   **计算高效**：通过局部线性近似将优化转化为线性方程求解，计算速度快，且易于并行化。
*   **隐私友好**：作为生成式方法，它产生的是新样本而非直接填充原始数据，适用于隐私敏感场景。

### 8. 不足与局限
*   **数据类型限制**：目前主要针对**连续型变量**设计，尚未直接扩展到分类数据或混合类型数据。
*   **高维扩展性**：虽然在 272 维上表现良好，但局部线性估计在高维空间可能面临“维度灾难”，计算复杂度随维度增加。
*   **带宽依赖**：尽管有启发式方法，但带宽 $\sigma$ 的选择对结果仍有一定影响，未来可能需要更自动化的选择机制。

（完）
