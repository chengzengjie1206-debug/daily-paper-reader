---
title: Channel-wise Retrieval for Multivariate Time Series Forecasting
title_zh: 多变量时间序列预测的通道级检索
authors: "Junhyeok Kang, Jun Seo, Soyeon Park, Sangjun Han, Seohui Bae, Hyeokjun Choe, Soonyoung Lee"
date: 2026-04-07
pdf: "https://arxiv.org/pdf/2604.05543v1"
tags: ["query:ts"]
score: 8.0
evidence: 利用通道检索处理长程依赖的多变量时序预测
tldr: 针对多元时间序列预测中长程依赖捕捉难及变量间异质性被忽视的问题，本文提出CRAFT框架。该框架采用通道独立检索策略，通过时域稀疏关系图初步筛选和频域谱相似度精确排序的两阶段流程，有效提取各变量特有的历史参考信息。实验证明CRAFT在多个基准数据集上超越了现有最先进模型，兼具高精度与推理效率。
selection_source: fresh_fetch
motivation: 现有的检索增强预测方法采用通道无关策略，忽视了多元时间序列中不同变量间显著的异质性和不同的周期特性。
method: 提出CRAFT框架，利用时域稀疏图进行候选剪枝，并结合频域谱相似度对各通道独立进行历史片段检索与排序。
result: 在七个公开基准数据集上的实验结果显示，CRAFT的预测准确度优于当前最先进的基线模型。
conclusion: 通道级检索能更精准地捕捉变量特有的长程依赖，是提升多元时间序列预测性能的有效途径。
---

## 摘要
多变量时间序列预测由于固定的回看窗口，往往难以捕捉长程依赖关系。检索增强预测通过从存储器中检索历史片段来解决这一问题，但现有方法依赖于通道无关策略，即对所有变量应用相同的参考片段。这忽略了变量间的异质性，即不同通道表现出截然不同的周期性和频谱特征。我们提出了 CRAFT（通道级检索增强预测），这是一个为每个通道独立执行检索的新型框架。为了确保效率，CRAFT 采用了两阶段流水线：在时域构建稀疏关系图以剪枝无关候选，并在频域利用频谱相似性对参考片段进行排序，从而在抑制噪声的同时强调主要的周期性成分。在七个公开基准数据集上的实验表明，CRAFT 优于最先进的预测基准模型，在具备实用推理效率的同时实现了卓越的准确性。

## Abstract
Multivariate time series forecasting often struggles to capture long-range dependencies due to fixed lookback windows. Retrieval-augmented forecasting addresses this by retrieving historical segments from memory, but existing approaches rely on a channel-agnostic strategy that applies the same references to all variables. This neglects inter-variable heterogeneity, where different channels exhibit distinct periodicities and spectral profiles. We propose CRAFT (Channel-wise retrieval-augmented forecasting), a novel framework that performs retrieval independently for each channel. To ensure efficiency, CRAFT adopts a two-stage pipeline: a sparse relation graph constructed in the time domain prunes irrelevant candidates, and spectral similarity in the frequency domain ranks references, emphasizing dominant periodic components while suppressing noise. Experiments on seven public benchmarks demonstrate that CRAFT outperforms state-of-the-art forecasting baselines, achieving superior accuracy with practical inference efficiency.

---

## 论文详细总结（自动生成）

### 论文总结：多变量时间序列预测的通道级检索 (CRAFT)

#### 1. 核心问题与研究动机
*   **核心问题**：多变量时间序列（MTS）预测中，传统模型受限于固定的“回看窗口”，难以捕捉长程依赖关系。
*   **研究动机**：现有的检索增强（RAG）预测方法通常采用“通道无关”策略，即所有变量共享同一个检索到的历史片段。然而，MTS 中的不同变量（通道）往往具有不同的周期性、趋势和频谱特征（异质性）。强制所有通道使用相同的参考信息会导致检索不匹配，进而限制预测精度。

#### 2. 核心方法论
CRAFT 框架的核心思想是**为每个通道独立执行检索**，并采用两阶段流水线确保效率：
*   **知识库构建与稀疏关系图**：
    *   预计算变量间的余弦相似度，为每个变量构建一个包含 $M$ 个最相关邻居的“稀疏关系图”。
    *   这步操作将搜索空间从全量变量剪枝为局部候选集，避免了计算复杂度的二次方增长。
*   **两阶段检索流水线**：
    1.  **时域剪枝**：利用预计算的稀疏图限制每个通道的候选检索范围。
    2.  **频域排序**：对候选片段进行快速傅里叶变换（FFT），仅保留低频成分。通过计算查询序列与候选序列之间的**频谱相似度**（复数内积）进行排序。这种方法能突出主要的周期性模式并抑制高频噪声。
*   **预测集成**：
    *   检索到的 $R$ 个历史片段经过线性映射后取平均，生成“检索预测值”。
    *   最终预测结果由基础预测器（如 MLP）的直接输出与检索预测值通过加性修正（$\hat{Y} = \hat{Y}_{direct} + \alpha \cdot \hat{Y}_{retrieval}$）得到。

#### 3. 实验设计
*   **数据集**：使用了 7 个标准公开基准数据集，包括 ETT (ETTh1, ETTh2, ETTm1, ETTm2)、Electricity (ECL)、Traffic 和 Weather。
*   **预测场景**：长程时间序列预测，预测步长 $H \in \{96, 192, 336, 720\}$。
*   **对比方法（Benchmark）**：对比了 8 种最先进的模型，包括 RAFT (ICML 2025)、TimeMixer (ICLR 2024)、PatchTST、TimesNet、MICN、DLinear、Stationary 和 Autoformer。

#### 4. 资源与算力
*   **硬件**：实验在单张 **NVIDIA RTX 3090 GPU** 上完成。
*   **软件**：基于 PyTorch 2.0.1 框架。
*   **效率**：推理时间约为 **0.03 秒/batch**（Batch Size 为 32），相当于每秒可处理超过 1000 个时间序列实例，证明了该方法在实时服务中的可行性。

#### 5. 实验数量与充分性
*   **实验规模**：在 7 个数据集上分别针对 4 种预测长度进行了测试，并报告了 MSE 和 MAE 的平均值。
*   **消融与分析**：
    *   进行了关于候选邻居数量 $M$ 的敏感性分析。
    *   提供了检索结果的可视化分析，展示了检索片段与真实未来值的匹配程度。
*   **充分性评价**：实验覆盖了主流的数据集和最新的基线模型，对比维度客观，且通过效率分析证明了通道级检索在增加计算量后的实用性。

#### 6. 主要结论与发现
*   **性能领先**：CRAFT 在所有 7 个基准数据集上的平均性能均优于现有 SOTA 模型，显著降低了预测误差。
*   **异质性重要性**：实验证明，允许每个通道独立检索其特有的历史参考信息，比统一检索效果更好。
*   **效率平衡**：通过稀疏图和频域计算，CRAFT 成功将通道级检索的复杂度控制在与变量数量成线性的范围内，实现了精度与速度的平衡。

#### 7. 优点
*   **创新性**：首次在检索增强预测中引入通道级（Channel-wise）独立检索，解决了变量异质性问题。
*   **设计巧妙**：结合时域稀疏化和频域相似度计算，既保证了检索的语义相关性，又过滤了噪声。
*   **通用性**：作为一种加性修正模块，CRAFT 可以无缝集成到现有的各种预测模型中，无需改变其核心架构。

#### 8. 不足与局限
*   **超参数依赖**：模型性能可能受到权重系数 $\alpha$、候选数量 $M$ 以及频率保留数 $F$ 的影响，不同数据集可能需要单独调优。
*   **存储开销**：虽然检索速度快，但需要维护一个包含所有训练数据的历史知识库，对于超大规模数据集，内存/显存占用可能成为瓶颈。
*   **基础模型单一**：文中主要展示了与简单 MLP 结合的效果，在更复杂的 Transformer 类模型上的增益幅度有待进一步验证。

（完）
