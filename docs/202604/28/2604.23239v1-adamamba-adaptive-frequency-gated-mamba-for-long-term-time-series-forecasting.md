---
title: "AdaMamba: Adaptive Frequency-Gated Mamba for Long-Term Time Series Forecasting"
authors: "Xudong Jiang, Mingshan Loo, Hanchen Yang, Wengen Li, Mingrui Zhang, Yichao Zhang, Jihong Guan, Shuigeng Zhou"
date: 2026-04-25
pdf: "https://arxiv.org/pdf/2604.23239v1"
tags: ["query:ts"]
score: 10.0
evidence: 用于长期时间序列预测的自适应 Mamba 模型
tldr: AdaMamba 针对长期时间序列预测（LTSF）中跨域异构性和复杂长程依赖的挑战，提出了一种自适应频率门控 Mamba 框架。该方法通过交互式补丁编码捕获变量间动态，并引入自适应频率门控状态空间模块，将频率分析内生化于 Mamba 的更新过程中。实验证明，AdaMamba 在多个基准数据集上均优于现有最先进方法，实现了高预测精度与计算效率的平衡。
selection_source: fresh_fetch
motivation: 现有的频率域预测方法往往假设跨域同质性，难以处理现实世界时间序列中复杂的跨域异构性和动态周期模式。
method: 提出 AdaMamba 框架，利用交互式补丁编码捕获变量交互，并通过自适应频率门控状态空间模块实现输入相关的频率分析与时频统一遗忘门。
result: 在七个公共 LTSF 基准和两个特定领域数据集上的实验表明，AdaMamba 的预测精度一致优于当前最先进的方法。
conclusion: AdaMamba 有效结合了频率域分析与长程依赖学习，为处理具有复杂异构性的长期时间序列预测提供了高效且准确的解决方案。
---

## Abstract
Accurate long-term time series forecasting (LTSF) requires the capture of complex long-range dependencies and dynamic periodic patterns. Recent advances in frequency-domain analysis offer a global perspective for uncovering temporal characteristics. However, real-world time series often exhibit pronounced cross-domain heterogeneity where variables that appear synchronized in the time domain can differ substantially in the frequency domain. Existing frequency-based LTSF methods often rely on implicit assumptions of cross-domain homogeneity, which limits their ability to adapt to such intricate variability. To effectively integrate frequency-domain analysis with temporal dependency learning, we propose AdaMamba, a novel framework that endogenizes adaptive and context-aware frequency analysis within the Mamba state-space update process. Specifically, AdaMamba introduces an interactive patch encoding module to capture inter-variable interaction dynamics. Then, we develop an adaptive frequency-gated state-space module that generates input-dependent frequency bases, and generalizes the conventional temporal forgetting gate into a unified time-frequency forgetting gate. This allows dynamic calibration of state transitions based on learned frequency-domain importance, while preserving Mamba's capability in modeling long-range dependencies. Extensive experiments on seven public LTSF benchmarks and two domain-specific datasets demonstrate that AdaMamba consistently outperforms state-of-the-art methods in forecasting accu racy while maintaining competitive computational efficiency. The code of AdaMamba is available at https://github.com/XDjiang25/AdaMamba.

---

## 论文详细总结（自动生成）

以下是对论文《AdaMamba: Adaptive Frequency-Gated Mamba for Long-Term Time Series Forecasting》的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
长期时间序列预测（LTSF）的核心挑战在于捕获复杂且长程的时间依赖关系以及动态的周期性模式。
*   **研究动机**：尽管频域分析能提供全局视角，但现实世界的数据存在显著的**跨域异构性（Cross-domain Heterogeneity）**。即在时域中看起来同步的变量，在频域中可能具有截然不同的功率谱密度（PSD）。
*   **现有缺陷**：现有的频域方法通常假设“跨域同质性”，依赖静态频率假设或硬编码的启发式设计（如预定义的周期长度或随机采样频率），难以适应复杂的动态变化。
*   **核心目标**：提出 AdaMamba 框架，旨在将自适应、上下文感知的频域分析内生化（Endogenize）到 Mamba 状态空间模型（SSM）的更新过程中，实现时频统一建模。

### 2. 方法论
AdaMamba 主要由两个核心模块组成：
*   **交互式补丁编码模块（Interactive Patch Encoding）**：
    *   **交互编码**：利用一维卷积（Conv1D）捕获多变量间的交互相关性，并通过可学习参数自适应融合原始输入。
    *   **补丁嵌入**：采用通道独立策略，通过多尺度补丁划分（不同长度的 Patch）来捕获不同分辨率的局部变化和全局趋势。
*   **自适应频率门控状态空间模块（Adaptive Frequency-Gated State-Space Module, AFSSM）**：
    *   **动态频率自适应**：不使用固定频率，而是通过轻量级适配器（Adapter）根据输入特征生成上下文相关的频率偏移量（$\Delta \omega$），从而获得自适应频率基。
    *   **频域门控状态更新**：
        *   将 Mamba 的状态方程扩展到复数域，分别处理实部和虚部。
        *   **时频统一遗忘门**：将传统的时域遗忘门扩展为时域与频域的张量积，实现基于频域重要性的动态状态转移校准。
        *   **振幅输出门控**：研究发现相位（Phase）对噪声敏感且优化不稳定，因此仅利用振幅（Amplitude）进行输出映射，并通过门控机制筛选任务相关的周期信息。

### 3. 实验设计
*   **数据集**：涵盖 7 个公共基准数据集（ETTh1, ETTh2, ETTm1, ETTm2, Weather, ILI, Exchange Rate）和 2 个特定领域数据集（KnowAir-v2 空气质量、NOAA 海表温度 SST）。
*   **对比方法（Benchmarks）**：
    *   **Mamba 类**：Affirm, S_Mamba, STDMamba, TimeMachine。
    *   **频域/深度学习类**：FreTS, PatchTST, iTransformer, ModernTCN。
*   **评价指标**：均方误差（MSE）和平均绝对误差（MAE）。

### 4. 资源与算力
*   **硬件环境**：实验在单张 **NVIDIA RTX 4090 GPU** 上运行。
*   **训练细节**：Batch Size 设置为 24。
*   **效率分析**：论文提供了运行时间与 MSE 的对比图，证明 AdaMamba 在保持高精度的同时，计算开销（训练时间/迭代）与同类 Mamba 模型相当，且具有线性计算复杂度 $O(M)$。

### 5. 实验数量与充分性
*   **实验规模**：在 9 个数据集上进行了广泛测试，涵盖了不同的预测长度（如 96 到 720 步）。
*   **消融实验**：设计了四种变体（Case I-IV），分别验证了交互补丁编码和自适应频率门控模块的有效性。
*   **敏感性分析**：对超参数（层数、隐藏维度、学习率、多尺度补丁配置）进行了详尽的敏感性测试（例如对 63 种补丁组合进行了遍历）。
*   **充分性评价**：实验设计非常充分，不仅对比了最新的 SOTA 模型，还通过可视化手段（预测曲线、频率分析图）深入解释了模型行为，结果具有较强的说服力和客观性。

### 6. 主要结论与发现
*   **性能领先**：AdaMamba 在所有数据集上均表现优异，特别是在 ILI 数据集上，MSE 降低了 **11.47%**。
*   **时频融合的优越性**：将频率分析直接嵌入 SSM 更新过程（内生化），比简单的外部特征耦合更有效。
*   **振幅 vs 相位**：实验证明，在序列预测中，振幅分量比相位分量更稳健，相位分量容易导致梯度不稳定和过拟合。
*   **动态频率的必要性**：动态调整频率基比固定频率基能更好地处理跨域异构性。

### 7. 优点
*   **架构创新**：首次将自适应频域分析与 Mamba 的选择性状态空间机制深度融合，而非简单的模块堆叠。
*   **处理异构性**：通过上下文相关的频率偏移，有效解决了时频域特征不一致的问题。
*   **高效性**：保留了 Mamba 的线性复杂度优势，适合处理长序列数据。
*   **多尺度感知**：交互式补丁编码增强了模型对多变量相关性和多分辨率时间模式的提取能力。

### 8. 不足与局限
*   **复杂度常数项**：虽然渐进复杂度是线性的，但由于引入了频域张量运算和门控机制，其实际计算常数项（Constant Factor）可能略高于原始 Mamba。
*   **应用范围限制**：目前主要针对长期预测任务，尚未在时间序列分类、异常检测或大规模预训练任务中验证其通用性。
*   **相位信息的舍弃**：虽然论文解释了相位不稳定的原因，但完全舍弃相位可能在某些对时间偏移极度敏感的特定任务中丢失信息。

（完）
