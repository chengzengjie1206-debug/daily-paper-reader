---
title: Conditional Imputation for Within-Modality Missingness in Multi-Modal Federated Learning
title_zh: 多模态联邦学习中模态内缺失的条件插补
authors: "Wugeng Zheng, Ziwen Kan, Katie Wang, Chen Chen, Song Wang"
date: 2026-04-25
pdf: "https://arxiv.org/pdf/2604.23112v1"
tags: ["query:ts"]
score: 9.0
evidence: 缺失时间分量的条件填补
tldr: 针对多模态联邦学习中常见的模态内数据缺失问题，本文提出CondI框架。该框架利用条件扩散模型显式补全缺失的原始数据，通过两阶段训练流程（数据补全与特征提取优化）恢复完整的语义结构。在三个临床数据集上的实验证明，CondI在处理严重数据不完整性方面具有显著优势，显著提升了模型在临床应用中的鲁棒性。
selection_source: fresh_fetch
motivation: 现有多模态联邦学习方法在处理模态内缺失数据时，往往无法准确恢复数据的真实分布，导致下游任务性能受限。
method: 提出CondI框架，利用条件扩散模型结合多模态上下文进行显式数据补全，并分阶段优化模态提取器与联合嵌入空间。
result: 在PTB-XL、SLEEP-EDF和MIMIC-IV三个临床数据集上的实验表明，CondI取得了与当前最先进基准相当的性能。
conclusion: 通过显式数据补全确保模型在完整语义结构上运行，能显著增强多模态联邦学习系统应对严重数据缺失的能力。
---

## 摘要
多模态联邦学习（MMFL）能够实现隐私保护的协作训练，但在实际临床应用中，由于传感器间歇性或不规则采样，经常面临模态内数据缺失的问题。现有方法通过架构对齐或缺失嵌入来隐式表示未观测数据，通常无法恢复真实分布，导致性能欠佳。我们提出了 CondI，这是一个利用条件扩散模型显式处理此类缺失问题的联邦框架。CondI 采用两阶段训练流水线：首先，利用可用的多模态上下文和条件嵌入来插补未观测的时间分量；其次，优化模态特定的提取器和联合嵌入空间。在推理过程中，插补后的原始数据通过训练好的提取器生成鲁棒特征，为下游任务提供整体表示。显式数据插补确保模型在完整的语义结构上运行，显著增强了对严重数据不完整性的鲁棒性。在三个临床数据集（PTB-XL、SLEEP-EDF、MIMIC-IV）上的实验表明，CondI 取得了与最先进基准模型相当的结果。代码：https://github.com/ZhengWugeng/CondI

## Abstract
Multimodal Federated Learning (MMFL) enables privacy-preserving collaborative training, but real-world clinical applications often suffer from within-modality missingness caused by sensor intermittency or irregular sampling. Existing methods implicitly represent unobserved data via architectural alignment or missing embeddings, often failing to recover the true distribution and yielding sub-optimal performance. We propose CondI, a federated framework explicitly addressing this missingness using conditional diffusion models. CondI employs a two-phase training pipeline: first, imputing unobserved temporal components using available multimodal context and conditional embeddings; second, optimizing modality-specific extractors and joint embedding spaces. During inference, imputed raw data pass through trained extractors to generate robust features, providing a holistic representation for downstream tasks. Explicit data imputation ensures models operate on complete semantic structures, significantly enhancing resilience against severe data incompleteness. Experiments on three clinical datasets (PTB-XL, SLEEP-EDF, MIMIC-IV) demonstrate CondI achieves comparable results to state-of-the-art baselines. Code: https://github.com/ZhengWugeng/CondI

---

## 论文详细总结（自动生成）

这篇论文针对多模态联邦学习（MMFL）中一个关键但常被忽视的问题——**模态内缺失（Within-Modality Missingness）**，提出了名为 **CondI** 的条件插补框架。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：在现实临床场景（如 ICU 监测）中，多模态数据不仅存在整个模态缺失的情况，更常见的是由于传感器间歇性脱落或采样频率不一致导致的“模态内部分时间段缺失”。
*   **研究动机**：现有的多模态联邦学习方法大多采用“隐式”处理方式（如学习一个代表缺失的嵌入向量或进行特征对齐），这无法恢复数据的真实分布，在缺失严重时会导致语义断裂和性能大幅下降。作者认为，应该通过“显式”生成缺失数据来保持语义的连续性和跨模态的一致性。

### 2. 方法论：CondI 框架
CondI 采用两阶段训练流水线，结合了条件扩散模型（Conditional Diffusion Models）和联邦学习机制：
*   **核心思想**：利用可用的模态作为条件，通过扩散模型显式地“还原”缺失的原始信号，再进行下游任务学习。
*   **关键技术细节**：
    *   **可学习嵌入空间 ($W$)**：引入了三种嵌入：$W_{mod}$（模态特征）、$W_{ins}$（实例语义）和 $W_{cond}$（跨模态条件先验）。
    *   **阶段 A：条件估计（Conditional Estimation）**：
        *   利用混合专家模型（MoE）提取观测到的上下文特征。
        *   通过 $W_{cond}$ 建立跨模态关联规则（例如：血压下降通常伴随心率上升）。
        *   使用条件扩散模型，在已知观测数据和跨模态条件的引导下，通过去噪过程生成缺失的时间序列分量。
    *   **阶段 B：任务优化（Task Optimization）**：
        *   在本地优化特征提取器和分类器。
        *   **推理阶段**：先用阶段 A 的模型补全数据，再将“完整”的数据输入提取器，确保模型处理的是连续的语义结构。
    *   **联邦聚合**：服务器通过 FedAvg 算法聚合各客户端的模型参数和全局共享的嵌入矩阵（$W_{mod}, W_{cond}$）。

### 3. 实验设计
*   **数据集**：
    1.  **PTB-XL**：大型心电图（ECG）数据集，12 导联视为 12 个模态。
    2.  **SLEEP-EDF**：睡眠阶段评分数据集，包含 EEG、EOG、EMG 等 5 个模态。
    3.  **MIMIC-IV**：重症监护数据集，用于预测住院死亡率（IHM）和住院时长（LOS）。
*   **Benchmark 与对比方法**：
    *   对比了 **FedAvg**（基础联邦学习）、**FedMM**（多模态联邦学习基准）以及 **PEPSY**（目前处理缺失数据的最先进 MMFL 方法）。
*   **缺失模拟**：设置了样本级缺失率 ($p_s$) 和模态内缺失率 ($p_w$)，涵盖从轻微（0.2）到严重（0.8）的四种组合场景。

### 4. 资源与算力
*   **算力说明**：论文中**未明确说明**具体的 GPU 型号、数量或训练总时长。
*   **代码资源**：作者提供了 GitHub 开源链接，便于社区复现和评估其计算开销。

### 5. 实验数量与充分性
*   **实验规模**：在 3 个不同类型的临床数据集上进行了测试，涵盖了分类和回归预测任务。
*   **消融实验**：专门设计了消融实验验证“插补模块”和“条件嵌入 $W_{cond}$”的必要性。
*   **特征重建分析**：通过 L2 距离和余弦相似度分析了插补后的特征与原始完整特征的接近程度，证明了模型确实恢复了真实分布。
*   **评价**：实验设计较为充分且客观，通过多维度（准确率、F1、AUROC、特征距离）验证了方法的有效性。

### 6. 主要结论与发现
*   **显式优于隐式**：显式插补原始数据比隐式的“缺失嵌入”更能抵抗严重的数据缺失。
*   **鲁棒性强**：在缺失率高达 80% 的极端情况下，CondI 的性能下降远小于基准方法，甚至在某些指标上保持稳定。
*   **跨客户端知识共享**：通过联邦学习聚合 $W_{cond}$，客户端可以从其他拥有完整模态的机构学习到缺失模态的关联规则。

### 7. 优点与亮点
*   **解决细粒度缺失**：首次在联邦学习框架下深入探讨“模态内时间序列缺失”这一实际痛点。
*   **生成式增强**：将扩散模型引入联邦学习插补任务，相比传统的 GAN 或 VAE，生成的信号质量更高、分布更真实。
*   **解耦设计**：两阶段训练流程使得插补任务和分类任务既相互独立又互为补充，提高了系统的灵活性。

### 8. 不足与局限
*   **计算开销**：扩散模型在推理时需要多次迭代去噪，这会显著增加客户端的计算负担和推理延迟，论文未对此进行深入讨论。
*   **低缺失场景优势不明显**：在 SLEEP-EDF 数据集的低缺失率实验中，CondI 的表现略逊于 PEPSY，说明在数据相对完整时，复杂的生成模型可能引入不必要的噪声。
*   **隐私风险探讨不足**：虽然使用了联邦学习，但生成模型（扩散模型）是否会通过生成的原始数据泄露训练集的成员隐私，文中未做安全性分析。

（完）
