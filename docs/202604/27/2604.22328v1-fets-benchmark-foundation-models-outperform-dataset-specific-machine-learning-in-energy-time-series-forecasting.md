---
title: "FETS Benchmark: Foundation Models Outperform Dataset-specific Machine Learning in Energy Time Series Forecasting"
title_zh: FETS 基准：基础模型在能源时间序列预测中优于特定于数据集的机器学习
authors: "Marco Obermeier, Marco Pruckner, Florian Haselbeck, Andreas Zeiselmair"
date: 2026-04-24
pdf: "https://arxiv.org/pdf/2604.22328v1"
tags: ["query:ts"]
score: 8.0
evidence: 能源时间序列预测的基础模型
tldr: 针对能源时间序列预测中传统机器学习模型难以扩展且依赖特定数据集的问题，本文提出了FETS基准测试。通过对54个数据集、9个类别的深入分析，研究对比了基础模型与传统机器学习方法的性能。结果表明，基础模型（尤其是结合协变量的模型）在各种场景下均优于针对特定数据集优化的传统模型，展现了其在能源领域作为可扩展、通用预测方案的巨大潜力。
selection_source: fresh_fetch
motivation: 传统的能源预测方法高度依赖特定数据集的训练，导致模型开发维护成本高且难以在大规模场景下扩展。
method: 提出了FETS基准，涵盖54个数据集，并对比了基础模型与针对特定数据集优化的传统机器学习模型在不同预测设置下的表现。
result: 基础模型在所有数据类别中均优于传统机器学习方法，且性能与频谱熵强相关，在高度聚合的数据上表现更佳。
conclusion: 基础模型是能源领域极具潜力的通用预测方案，特别适用于数据受限或对隐私敏感的应用场景。
---

## 摘要
在向气候中和能源系统转型的驱动下，准确的能源时间序列预测对于规划和运行至关重要。然而，这在很大程度上仍是一项特定于数据集的任务，需要全面的训练数据，限制了可扩展性，并导致高昂的模型开发和维护成本。最近，旨在通过广泛预训练学习可泛化模式的基础模型在多项预测任务中表现出了卓越的性能。尽管它们取得了成功并具有解决能源预测挑战的巨大潜力，但它们在该领域的应用在很大程度上仍未得到探索。我们通过提出能源时间序列预测中的基础模型（FETS）基准来填补这一空白。我们（1）从利益相关者、属性和数据类别三个主要维度提供了能源预测用例的结构化概述；（2）在典型利益相关者利益的指导下，收集并分析了跨 9 个数据类别的 54 个数据集；（3）在不同的预测设置下，将基础模型与经典的机器学习方法进行了基准测试。尽管后者在训练期间已经看到了完整的历史目标数据，但基础模型在所有设置和数据类别中始终优于特定于数据集优化的机器学习方法。特别是，融合协变量信息的基础模型表现最为出色。进一步的分析揭示了预测性能与谱熵之间的强相关性、超过一定上下文长度后的性能饱和，以及在国家负荷、区域供热和电网数据等更高聚合水平上性能的提升。总之，我们的研究结果强调了基础模型作为能源领域可扩展且可泛化的预测解决方案的巨大潜力，特别是在数据受限和隐私敏感的环境中。

## Abstract
Driven by the transition towards a climate-neutral energy system, accurate energy time series forecasting is critical for planning and operation. Yet, it remains largely a dataset-specific task, requiring comprehensive training data, limiting scalability, and resulting in high model development and maintenance effort. Recently, foundation models that aim to learn generalizable patterns via extensive pretraining have shown superior performance in multiple prediction tasks. Despite their success and strong potential to address challenges in energy forecasting, their application in this domain remains largely unexplored. We address this gap by presenting the Foundation Models in Energy Time Series Forecasting (FETS) benchmark. We (1) provide a structured overview of energy forecasting use cases along three main dimensions: stakeholders, attributes, and data categories; (2) collect and analyze 54 datasets across 9 data categories, guided by typical stakeholder interests; (3) benchmark foundation models against classical machine learning approaches across different forecasting settings. Foundation models consistently outperform dataset-specific optimized machine learning approaches across all settings and data categories, despite the latter having seen the full historic target data during training. In particular, covariate-informed foundation models achieve the strongest performance. Further analysis reveals a strong correlation between predictive performance and spectral entropy, performance saturation beyond a certain context length, and improved performance at higher aggregation levels such as national load, district heating, and power grid data. Overall, our findings highlight the strong potential of foundation models as scalable and generalizable forecasting solutions for the energy domain, particularly in data-constrained and privacy-sensitive settings.

---

## 论文详细总结（自动生成）

这是一份关于论文《FETS Benchmark: Foundation Models Outperform Dataset-specific Machine Learning in Energy Time Series Forecasting》的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：传统的能源时间序列预测（如电力负荷、可再生能源出力、价格预测）通常依赖于“特定数据集”的机器学习模型。这种方式需要大量的历史数据进行训练，且模型开发和维护成本高，难以在大规模、多场景下扩展。
*   **研究背景**：随着能源系统向气候中和转型，预测需求激增。与此同时，时间序列基础模型（Foundation Models, FMs）通过在大规模通用数据上预训练，展现出了强大的泛化能力。
*   **整体含义**：本文旨在评估这些通用的基础模型在能源这一特定领域是否能超越针对特定任务优化的传统机器学习模型，并为此提出了 **FETS（能源时间序列基础模型）基准测试**。

### 2. 论文提出的方法论
*   **核心思想**：构建一个多维度的能源预测评估框架，对比“预训练-零样本/微调”的基础模型与“从头训练”的传统机器学习模型。
*   **关键技术细节**：
    *   **FETS 框架**：从利益相关者（如电网运营商、产消者）、属性（如预测粒度、地平线）和数据类别（9大类）三个维度对能源预测用例进行结构化分类。
    *   **基础模型选择**：选取了当前主流的时间序列基础模型，包括 **Chronos**（基于语言模型架构）、**MOIRAI**（多补丁大小预测器）、**Lag-Llama**（基于 Llama 架构）和 **TimesFM**（Google 开发的时间序列基础模型）。
    *   **协变量融合**：研究了如何将外部信息（如天气、时间特征）整合进基础模型中，以增强预测精度。

### 3. 实验设计
*   **数据集与场景**：收集并分析了 **54 个数据集**，涵盖 **9 个类别**：国家负荷、区域供热、电网数据、光伏（PV）、风能、电动汽车（EV）、工业负荷、商业负荷和住宅负荷。
*   **Benchmark 对比方法**：
    *   **传统机器学习（针对每个数据集单独优化）**：XGBoost、LightGBM、多层感知机（MLP）和线性回归（Linear Regression）。
    *   **基础模型**：在未见过的能源数据上进行零样本（Zero-shot）推理。
*   **评估指标**：采用平均绝对误差（MAE）、均方根误差（RMSE）和对称平均绝对百分比误差（sMAPE）。

### 4. 资源与算力
*   **算力说明**：论文中**未明确给出**具体的 GPU 型号、数量或总训练时长。
*   **推论**：由于基础模型主要处于推理阶段（Zero-shot），而传统 ML 模型是在 54 个数据集上分别训练，实验的计算重点在于大规模的推理评估和传统模型的超参数优化。

### 5. 实验数量与充分性
*   **实验规模**：涵盖 54 个数据集，跨越了从个体住宅到国家层面的不同聚合程度，实验规模非常庞大。
*   **充分性与公平性**：
    *   对比非常严苛：基础模型在“零样本”情况下与“看过完整历史数据”的传统 ML 模型竞争。
    *   包含了消融实验：分析了上下文长度（Context Length）对性能的影响，以及预测性能与数据复杂度（谱熵）之间的关系。
    *   实验设计考虑了能源数据的特殊性（如季节性、天气依赖性），具有较高的客观性。

### 6. 主要结论与发现
*   **基础模型全面胜出**：在所有 9 个数据类别中，基础模型（尤其是结合了协变量的模型）的性能始终优于针对特定数据集优化的传统机器学习方法。
*   **协变量的重要性**：加入时间特征和天气等协变量后，基础模型的预测精度显著提升。
*   **可预测性相关性**：预测性能与数据的**谱熵（Spectral Entropy）**强相关，即数据越规律（熵越低），基础模型表现越好。
*   **聚合效应**：在国家负荷、区域供热等高聚合水平的数据上，基础模型的表现优于低聚合（如单个家庭）的数据。
*   **性能饱和**：当上下文长度达到一定程度后，基础模型的性能提升会趋于平缓。

### 7. 优点（亮点）
*   **系统性强**：首次为能源领域建立了如此全面、多维度的基础模型基准测试。
*   **实用价值高**：证明了在数据稀缺或隐私敏感（无法获取大量历史数据）的场景下，直接使用基础模型是可行且高效的。
*   **跨领域验证**：验证了在通用时间序列上训练的模型可以成功迁移到高度专业化的能源领域。

### 8. 不足与局限
*   **数据泄露风险**：虽然论文尽力排除，但无法完全保证基础模型的预训练数据中未包含某些公开的能源数据集。
*   **推理成本**：虽然省去了训练时间，但大型基础模型的推理计算开销通常高于轻量级的 XGBoost 等模型。
*   **实时性挑战**：对于需要极高频率更新的能源预测场景，基础模型的部署复杂性可能是一个障碍。
*   **黑盒性质**：相比于线性模型，基础模型的决策过程缺乏可解释性，这在某些能源监管场景下可能是个问题。

（完）
