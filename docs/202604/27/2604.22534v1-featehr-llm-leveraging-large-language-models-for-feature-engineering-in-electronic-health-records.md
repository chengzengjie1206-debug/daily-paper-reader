---
title: "FeatEHR-LLM: Leveraging Large Language Models for Feature Engineering in Electronic Health Records"
title_zh: FeatEHR-LLM：利用大语言模型进行电子健康档案的特征工程
authors: "Hojjat Karami, David Atienza, Jean-Philippe Thiran, Anisoara Ionescu"
date: 2026-04-24
pdf: "https://arxiv.org/pdf/2604.22534v1"
tags: ["query:ts"]
score: 9.0
evidence: 不规则采样临床时间序列的特征工程
tldr: 针对电子健康档案（EHR）中临床时间序列的不规则采样和稀疏性挑战，本文提出 FeatEHR-LLM 框架。该框架利用大语言模型（LLM）结合数据集模式和任务描述，通过工具增强生成机制自动产生可执行的特征提取代码。在保护隐私的同时，它能生成具有临床意义的特征，在多个 ICU 数据集的临床预测任务中表现优于现有基准。
selection_source: fresh_fetch
motivation: 传统的 EHR 特征工程方法难以处理不规则采样和稀疏数据，且缺乏临床领域知识。
method: 利用 LLM 基于数据集模式生成处理不规则时间序列的代码，并通过工具增强和迭代验证优化特征提取。
result: "在 4 个 ICU 数据集的 8 项预测任务中，该方法在 7 项任务上取得了最高 AUROC，提升幅度高达 6%。"
conclusion: FeatEHR-LLM 证明了 LLM 在自动化临床特征工程中的潜力，能有效处理复杂的医疗时间序列数据并提升预测性能。
---

## 摘要
电子健康档案（EHR）的特征工程因临床时间序列固有的不规则观察间隔、多变的测量频率以及结构性稀疏而变得复杂。现有的自动化方法要么缺乏临床领域意识，要么假设输入是干净且定期采样的，这限制了它们在真实世界 EHR 数据中的适用性。我们提出了 FeatEHR-LLM，这是一个利用大语言模型（LLMs）从不规则采样的 EHR 时间序列中生成具有临床意义的表格特征的框架。为了限制患者隐私泄露，LLM 仅在数据集模式（schemas）和任务描述上运行，而非原始患者记录。一种工具增强生成机制为 LLM 配备了用于查询不规则时间数据的专门例程，使其能够生成可执行的特征提取代码，从而显式地处理不均匀的观察模式和信息性稀疏。FeatEHR-LLM 通过迭代的验证在环（validation-in-the-loop）流水线支持单变量和多变量特征生成。在四个 ICU 数据集的八个临床预测任务上进行的评估表明，我们的框架在 8 个任务中的 7 个上实现了最高的平均 AUROC，相比强基准模型提升了高达 6 个百分点。代码可在 github.com/hojjatkarami/FeatEHR-LLM 获取。

## Abstract
Feature engineering for Electronic Health Records (EHR) is complicated by irregular observation intervals, variable measurement frequencies, and structural sparsity inherent to clinical time series. Existing automated methods either lack clinical domain awareness or assume clean, regularly sampled inputs, limiting their applicability to real-world EHR data. We present \textbf{FeatEHR-LLM}, a framework that leverages Large Language Models (LLMs) to generate clinically meaningful tabular features from irregularly sampled EHR time series. To limit patient privacy exposure, the LLM operates exclusively on dataset schemas and task descriptions rather than raw patient records. A tool-augmented generation mechanism equips the LLM with specialized routines for querying irregular temporal data, enabling it to produce executable feature-extraction code that explicitly handles uneven observation patterns and informative sparsity. FeatEHR-LLM supports both univariate and multivariate feature generation through an iterative, validation-in-the-loop pipeline. Evaluated on eight clinical prediction tasks across four ICU datasets, our framework achieves the highest mean AUROC on 7 out of 8 tasks, with improvements of up to 6 percentage points over strong baselines. Code is available at github.com/hojjatkarami/FeatEHR-LLM.

---

## 论文详细总结（自动生成）

这是一份关于论文 **《FeatEHR-LLM: Leveraging Large Language Models for Feature Engineering in Electronic Health Records》** 的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心挑战**：电子健康档案（EHR）中的临床时间序列数据具有高度的**不规则性**（采样间隔不一）、**多变性**（测量频率随病情变化）和**结构性稀疏**（测量缺失往往具有临床含义，而非随机丢失）。
*   **现有方法局限**：
    *   **传统自动化方法**（如 tsfresh）：缺乏临床领域知识，且通常假设数据是规则采样的。
    *   **深度学习模型**：虽然能处理序列，但往往是“黑盒”，且对数据量和算力要求极高。
    *   **现有 LLM 特征工程**：大多局限于静态表格数据，难以直接处理长序列、不规则的临床时间数据，且直接将原始数据输入 LLM 会面临严重的隐私风险和上下文窗口限制。
*   **研究目标**：开发一个既能利用 LLM 临床知识，又能处理不规则时间序列，同时保护患者隐私的自动化特征工程框架。

### 2. 方法论：核心思想与关键技术
FeatEHR-LLM 的核心思想是**“LLM 编写代码，本地执行提取”**。
*   **隐私保护机制**：LLM 不接触任何原始患者记录，仅接收数据集的元数据（Schema，如变量名、单位、统计摘要）和临床任务描述。
*   **工具增强生成（Tool-Augmented Generation）**：为 LLM 提供专门设计的 Python 工具函数库（如 `get_all_measurements`），使其能编写出能够正确处理不规则时间戳、处理空列表和异常值的鲁棒代码。
*   **双阶段特征生成流程**：
    1.  **单变量特征工程**：针对每个临床变量（如心率、肌酐），LLM 生成多个独立的特征提取函数（如计算“尿量波动的平均绝对变化”）。
    2.  **多变量特征工程**：LLM 首先提出具有临床意义的问题（如“患者呼吸补偿能力如何？”），识别相关变量组，然后编写跨变量的逻辑代码（如结合 pH、PaCO2 和呼吸频率判断酸碱平衡）。
*   **迭代验证在环**：采用多轮生成（Multiple Rounds）和每轮多样本（B=5）策略，通过本地语法检查和运行测试过滤无效代码，最终将生成的特征输入 LightGBM 等树模型。

### 3. 实验设计
*   **数据集与场景**：涵盖 4 个主流 ICU 数据集，共 8 项预测任务：
    *   **P12 (PhysioNet 2012)**：院内死亡率预测。
    *   **P19 (PhysioNet 2019)**：败血症早期检测。
    *   **MIMIC-III**：48 小时院内死亡率。
    *   **eICU-CRD**：包括死亡率、急性呼吸衰竭（ARF）和休克（Shock）在 12h/48h 窗口的预测。
*   **Benchmark 与对比方法**：
    *   **基础基准**：Baseline (均值/标准差等简单统计量)。
    *   **传统自动化**：tsfresh (时间序列特征提取)、OpenFE (表格特征工程)。
    *   **LLM 相关方法**：CAAFE、FeatLLM (均适配于 EHR 场景)。
    *   **直接推理**：Zeroshot LLM (直接让 LLM 给风险评分)。

### 4. 资源与算力
*   **LLM 后端**：主要使用 **Gemini-2.0-Flash**（平衡成本与性能），消融实验中对比了 **GPT-4o-mini**。
*   **本地计算**：下游预测模型使用 LightGBM、XGBoost 和 CatBoost。
*   **算力说明**：文中提到使用了 EPFL IC Cluster 的计算资源进行模型训练和特征提取。虽然未列出具体的 GPU 型号和训练总时长，但强调了该方法在推理阶段比直接将患者数据输入 LLM 的方法（Zeroshot）效率高得多，因为 LLM 只参与一次性的代码生成。

### 5. 实验数量与充分性
*   **实验规模**：实验覆盖了从 1.2 万到 14.5 万不等的样本量，具有很强的统计说服力。
*   **充分性**：
    *   **消融实验**：分别验证了单变量/多变量模块的贡献、生成轮数的影响、以及候选特征数量（B 值）的作用。
    *   **泛化性测试**：在不同数据规模（10% 到 100%）下测试，证明了在小样本下依然有效。
    *   **客观性**：使用了 1000 次分层 Bootstrap 采样来估计 95% 置信区间，确保了结果的稳健性和公平性。

### 6. 主要结论与发现
*   **性能领先**：FeatEHR-LLM 在 8 个任务中的 **7 个** 达到了最高 AUROC。在 P19 和 MIMIC-III 上分别比基准提升了 **6%** 和 **3.1%**。
*   **优于直接推理**：相比于直接让 LLM 进行 Zero-shot 预测，该方法生成的特征配合树模型表现大幅领先（例如 P19 任务中 AUROC 92.7 vs 62.2）。
*   **模块贡献**：消融实验显示，**单变量特征生成**对性能的贡献略大于多变量模块，但两者结合效果最佳。
*   **冗余性是有益的**：生成多个相似但阈值略有不同的特征（如不同的心率阈值报警）有助于树模型发现更细致的数据分区。

### 7. 优点
*   **临床语义注入**：将 LLM 的医学先验知识转化为显式的 Python 代码，弥补了纯数学变换方法的盲目性。
*   **处理不规则性**：通过工具增强机制，优雅地解决了 EHR 数据中时间戳不齐和采样频率不一致的顽疾。
*   **高解释性**：生成的特征是可读的代码逻辑，临床医生可以审计和验证，而非不可解释的向量。
*   **隐私与效率**：元数据驱动的模式避免了敏感数据上传云端，且大幅降低了长序列处理的 Token 成本。

### 8. 不足与局限
*   **幻觉风险**：LLM 可能会生成看似合理但医学上错误或存在安全隐患的特征逻辑，部署前仍需人工审核。
*   **缺失机制建模缺失**：框架主要处理“观测不规则性”，但并未显式区分缺失数据的类型（如 MCAR/MAR/MNAR），也未推断临床医生“不测量”背后的动机。
*   **计算开销**：虽然推理快，但在特征生成阶段需要多次调用 LLM API，且在本地执行大量生成的 Python 脚本会带来一定的预处理计算负担。
*   **模态限制**：目前仅专注于数值型时间序列和静态协变量，尚未整合临床文本（如医生笔记）或医学影像数据。

（完）
