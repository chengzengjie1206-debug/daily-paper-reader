---
title: Adapting Time Series Foundation Models through Data Mixtures
title_zh: 通过数据混合适配时间序列基础模型
authors: "Thomas L. Lee, Edoardo M. Ponti, Amos Storkey"
date: 2026-03-03
pdf: "https://arxiv.org/pdf/2603.02840v1"
tags: ["query:ts"]
score: 9.0
evidence: 调整时间序列基础模型以进行零样本预测
tldr: 针对时间序列基础模型（TSFM）在处理新领域数据时零样本预测性能下降的问题，本文提出了 MixFT 方法。该方法通过贝叶斯混合模型将数据集重新划分为更具同质性的子域，并针对每个子域分别微调 LoRA 模块。相比于传统的全量微调或按数据集微调，MixFT 能更好地捕捉数据内部的分布差异，显著提升了模型在零样本预测任务中的适应能力和准确性。
selection_source: fresh_fetch
motivation: 传统按数据集微调的方法忽略了单个数据集中可能存在多种分布（子域）的情况，导致模型难以精准捕捉特定分布特征。
method: 提出 MixFT，利用贝叶斯混合模型将数据重新划分为同质子域，并为每个子域独立微调专门的 LoRA 模块。
result: 实验结果表明，MixFT 在零样本预测任务上的表现优于单模块微调和按数据集微调的方法。
conclusion: 通过重新划分数据以代表子域，可以更有效地使时间序列基础模型专业化，从而提升其在未知领域的预测性能。
---

## 摘要
时间序列基础模型（TSFMs）在零样本预测中变得越来越流行。然而，对于预训练集未完全覆盖的新时间序列领域，其性能可能会受到影响。因此，当从业者关注一个新领域并拥有一组相关数据集时，问题随之而来：如何最好地微调 TSFM 以提高零样本预测性能？解决此类问题的典型方法是在所有数据集上或分别在每个数据集上微调 LoRA 模块。通过为不同的时间序列上下文选择每个数据集模块的不同组合，在每个数据集上微调独立模块可以使 TSFM 专门化于不同类型的数据分布。然而，我们发现使用基于数据集的模块可能并非最优，因为一个时间序列数据集可能包含来自多种分布类型（即子领域）的数据。这可能是由于分布偏移或时间序列的不同维度具有不同的分布。因此，我们提出了 MixFT，它使用贝叶斯混合模型将数据重新划分为最能代表数据中存在的子领域的集合，并分别在这些集合上进行微调。这种数据的重新划分确保了每个集合更加同质，从而产生专注于特定子领域的微调模块。我们的实验表明，MixFT 的表现优于基于数据集的方法以及在所有数据上微调单个模块的方法。这表明，通过重新划分数据以代表子领域，我们可以更好地使 TSFM 专门化，从而提高零样本预测性能。

## Abstract
Time series foundation models (TSFMs) have become increasingly popular for zero-shot forecasting. However, for a new time series domain not fully covered by the pretraining set, performance can suffer. Therefore, when a practitioner cares about a new domain and has access to a set of related datasets, the question arises: how best to fine-tune a TSFM to improve zero-shot forecasting? A typical approach to this type of problem is to fine-tune a LoRA module on all datasets or separately on each dataset. Tuning a separate module on each dataset allows for the specialisation of the TSFM to different types of data distribution, by selecting differing combinations of per-dataset modules for different time series contexts. However, we find that, using per-dataset modules might not be optimal, since a time series dataset can contain data from several types of distributions, i.e. sub-domains. This can be due to the distribution shifting or having differing distributions for different dimensions of the time series. Hence, we propose MixFT which re-divides the data using Bayesian mixtures into sets that best represent the sub-domains present in the data, and fine-tunes separately on each of these sets. This re-division of the data ensures that each set is more homogeneous, leading to fine-tuned modules focused on specific sub-domains. Our experiments show that MixFT performs better than per-dataset methods and when fine-tuning a single module on all the data. This suggests that by re-partitioning the data to represent sub-domains we can better specialise TSFMs to improve zero-shot forecasting.

---

## 论文详细总结（自动生成）

这是一份关于论文《Adapting Time Series Foundation Models through Data Mixtures》的结构化深入总结：

### 1. 核心问题与整体含义
*   **研究动机**：时间序列基础模型（TSFM）虽然在零样本（Zero-shot）预测上表现出色，但面对预训练集未覆盖的新领域时性能会大幅下降。
*   **核心问题**：现有的微调方法（如在所有数据上训练单个 LoRA 或按数据集边界训练多个 LoRA）忽略了**数据集内部的异质性**。一个数据集中可能包含多种分布（子域），例如不同的季节性模式或突发性波动。
*   **整体含义**：本文认为“数据集标签”并非划分数据的最优标准。通过识别并利用数据中的潜在“子域”（Sub-domains），可以训练出更专业、泛化能力更强的微调模块。

### 2. 方法论：MixFT
MixFT 的核心思想是利用概率混合模型重新划分微调数据，并为每个识别出的子域训练专门的 LoRA 专家模块。
*   **第一阶段：子域识别（贝叶斯混合）**
    1.  使用 TSFM 的编码器将所有微调数据映射到嵌入空间（Embedding Space）。
    2.  在嵌入空间上拟合一个**贝叶斯高斯混合模型（Bayesian GMM）**。
    3.  根据后验预测分布，将数据点重新分配到 $K$ 个最可能的子域集合中，而非遵循原始的数据集边界。
*   **第二阶段：专家微调**
    1.  为每个子域集合独立训练一个 LoRA 模块。
    2.  为了防止过拟合，引入了 MixUp 数据增强和经验回放（Experience Replay，即加入部分预训练数据共同训练）。
*   **第三阶段：零样本推理**
    1.  给定一个新的时间序列上下文，首先通过贝叶斯 GMM 识别其所属的子域。
    2.  调用该子域对应的特定 LoRA 模块进行预测。

### 3. 实验设计
*   **数据集**：使用了来自 Cloud 和 Gift-Eval 基准的挑战性数据集。
    *   **微调集**：CloudD3/D4、BizITObs-Service、BitBrains、M4-Hourly/Weekly。
    *   **评估集**：CloudD1/D2、BizITObs-App、US-Births、M4-Daily/Monthly/Quarterly、ETTh2、ETTm2。
*   **基础模型（TSFMs）**：Chronos Bolt (Small) 和 Moirai-1.1-R (Small)。
*   **对比方法**：
    *   **Base**：不进行微调。
    *   **Shared**：在所有微调数据上训练单个 LoRA。
    *   **Per-Dataset 方法**：包括 $\mu$-Datasets（权重平均）、Arrow-Dataset（动态路由）、Poly（端到端重要性加权）和 MBC（基于聚类的数据集聚合）。
*   **评价指标**：平均绝对比例误差（MASE）。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量或总训练时长。
*   **训练细节**：提到了使用 AdamW 优化器，学习率为 0.00005，Batch Size 为 256。LoRA 参数设置为 $r=2, \alpha=16$。
*   **开销分析**：作者指出 MixFT 的训练时间与 MBC 方法大致相当，但在推理时由于需要先进行子域分类，会增加少量的计算延迟。

### 5. 实验数量与充分性
*   **实验规模**：在 2 个主流 TSFM 架构上针对 10 个不同的评估数据集进行了测试。
*   **消融实验**：非常充分。包括了对混合组件数量 $K$ 的选择、推理时路由机制（硬分配 vs 软分配）、以及不同概率模型（K-means vs 主题模型 vs 贝叶斯 GMM）的对比。
*   **客观性**：实验设计考虑了“相关但不相同”的领域迁移场景，符合实际应用需求；通过平均排名（Average Rank）来汇总结果，减少了单一数据集偏差的影响。

### 6. 主要结论与发现
*   **性能领先**：MixFT 在平均排名和预测准确度上均优于所有基准方法（包括复杂的路由方法如 Arrow）。
*   **子域价值**：实验证明，即使在同一个数据集中，MixFT 也能识别出不同的子域（如周期性模式），这种细粒度的划分比粗糙的数据集划分更能提升模型专业化程度。
*   **微调难度**：在 TSFM 上，许多传统的微调方法甚至不如不微调（Base），这凸显了 TSFM 微调中防止分布偏移和过拟合的挑战性。

### 7. 优点
*   **架构无关性**：MixFT 是一种通用的微调框架，可以应用于任何基于 Transformer 的时间序列模型。
*   **解决异质性**：通过贝叶斯混合模型自动处理多变量时间序列中的维度差异和时间偏移，无需人工标注子域。
*   **理论支撑**：利用概率模型直接对齐微调和推理阶段的模块选择，比后验（Post-hoc）路由更具一致性。

### 8. 不足与局限
*   **子域一致性假设**：MixFT 假设微调集和评估集之间存在共享的潜在子域。如果评估数据属于完全未见的全新分布，该方法可能失效。
*   **存储开销**：需要存储 $K$ 个 LoRA 模块，虽然 LoRA 本身很轻量，但在 $K$ 值较大时仍有一定内存压力。
*   **超参数敏感性**：虽然论文提供了选择 $K$ 的验证方法，但在实际应用中，确定最优的子域数量仍需要额外的计算资源进行验证。

（完）
