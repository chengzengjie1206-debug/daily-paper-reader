---
title: "Timer-S1: A Billion-Scale Time Series Foundation Model with Serial Scaling"
title_zh: Timer-S1：具有序列缩放能力的十亿级时间序列基础模型
authors: "Yong Liu, Xingjian Su, Shiyu Wang, Haoran Zhang, Haixuan Liu, Yuxuan Wang, Zhou Ye, Yang Xiang, Jianmin Wang, Mingsheng Long"
date: 2026-03-05
pdf: "https://arxiv.org/pdf/2603.04791v1"
tags: ["query:ts"]
score: 10.0
evidence: 用于长期预测的十亿级时间序列基础模型
tldr: Timer-S1是一个拥有83亿参数的时间序列基础模型，旨在解决现有预训练模型的扩展性瓶颈。它采用了创新的串行扩展（Serial Scaling）策略，结合了稀疏混合专家（MoE）架构与串行令牌预测（STP）目标，并在包含一万亿时间点的TimeBench数据集上进行训练。通过后训练阶段的长上下文扩展，该模型在GIFT-Eval基准测试中取得了SOTA性能，显著提升了长短期预测的准确性与效率。
selection_source: fresh_fetch
motivation: 针对现有预训练时间序列模型在模型架构、数据规模和训练流程上存在的扩展性瓶颈问题。
method: 引入了基于TimeMoE和串行令牌预测（STP）的架构，并利用一万亿规模的TimeBench数据集进行预训练及长上下文扩展。
result: 在GIFT-Eval大规模评测中，Timer-S1在MASE和CRPS指标上均达到了预训练模型的最高水平。
conclusion: 该研究通过串行扩展范式成功构建了超大规模时间序列基础模型，为该领域的进一步研究提供了强有力的基准。
---

## 摘要
我们介绍了 Timer-S1，这是一个强大的混合专家（MoE）时间序列基础模型，拥有 83 亿总参数，每个 token 激活 7.5 亿参数，上下文长度为 11.5K。为了克服现有预训练时间序列基础模型中的可扩展性瓶颈，我们在模型架构、数据集和训练流水线三个维度上进行了序列缩放（Serial Scaling）。Timer-S1 集成了稀疏 TimeMoE 模块和通用的 TimeSTP 模块，用于序列 Token 预测（STP），这是一种遵循预测序列特性的通用训练目标。该范式引入了序列计算以改进长期预测，同时避免了标准下文预测（next-token prediction）中昂贵的滚动式推理和显著的误差累积。为了追求高质量且无偏的训练数据集，我们构建了包含一万亿个时间点的语料库 TimeBench，并应用细致的数据增强来减轻预测偏差。我们还开创了后训练阶段，包括持续预训练和长上下文扩展，以增强短期和长上下文性能。在大规模 GIFT-Eval 排行榜的评估中，Timer-S1 实现了最先进的预测性能，作为预训练模型获得了最佳的 MASE 和 CRPS 分数。Timer-S1 将被发布以促进进一步的研究。

## Abstract
We introduce Timer-S1, a strong Mixture-of-Experts (MoE) time series foundation model with 8.3B total parameters, 0.75B activated parameters for each token, and a context length of 11.5K. To overcome the scalability bottleneck in existing pre-trained time series foundation models, we perform Serial Scaling in three dimensions: model architecture, dataset, and training pipeline. Timer-S1 integrates sparse TimeMoE blocks and generic TimeSTP blocks for Serial-Token Prediction (STP), a generic training objective that adheres to the serial nature of forecasting. The proposed paradigm introduces serial computations to improve long-term predictions while avoiding costly rolling-style inference and pronounced error accumulation in the standard next-token prediction. Pursuing a high-quality and unbiased training dataset, we curate TimeBench, a corpus with one trillion time points, and apply meticulous data augmentation to mitigate predictive bias. We further pioneer a post-training stage, including continued pre-training and long-context extension, to enhance short-term and long-context performance. Evaluated on the large-scale GIFT-Eval leaderboard, Timer-S1 achieves state-of-the-art forecasting performance, attaining the best MASE and CRPS scores as a pre-trained model. Timer-S1 will be released to facilitate further research.

---

## 论文详细总结（自动生成）

这篇论文介绍了 **Timer-S1**，一个拥有 83 亿参数的时间序列基础模型。以下是对该论文的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：现有的时间序列基础模型在扩展（Scaling）到超大规模时面临瓶颈。
*   **研究动机**：
    *   **预测的序列本质**：长期预测本质上是序列化的（未来的每一步都依赖于之前的估计），但现有的“并行预测”模型缺乏足够的序列计算能力，而“自回归（AR）”模型虽然符合序列特性，但在长程推理时计算开销巨大且误差累积严重。
    *   **数据异质性**：不同领域的时间序列在频率、形状和随机性上差异巨大，难以用统一模型捕捉。
    *   **扩展性挑战**：如何在增加模型参数和数据规模的同时，既保持推理效率，又提升长期预测的准确性。

### 2. 方法论：核心思想与关键技术
Timer-S1 提出了**序列缩放（Serial Scaling）**范式，从三个维度进行优化：
*   **核心架构：TimeMoE + TimeSTP**
    *   **TimeMoE（混合专家模块）**：采用稀疏 MoE 结构（总 32 个专家，每个 token 激活 2 个），以 8.3B 的总参数量实现仅 0.75B 的激活计算量，有效处理跨域数据的异质性。
    *   **TimeSTP（序列 Token 预测）**：这是本文的核心创新。不同于传统的下文预测（NTP）或多 Token 并行预测（MTP），TimeSTP 模块在推理时被保留。它通过一系列堆叠的块，每一层在前一层的基础上引入额外的序列计算来预测更远的未来。这使得模型在单次前向传播中即可完成多步预测，同时具备自回归模型的序列推理深度。
*   **数据缩放：TimeBench 语料库**
    *   构建了包含 **1 万亿（1 Trillion）**时间点的大规模数据集，涵盖金融、物联网、气象、医疗及合成数据。
    *   **数据增强**：引入重采样（Resampling）和数值翻转（Value-Flipping）来减轻模型对特定频率或趋势的预测偏差。
*   **训练流水线**：
    *   **预训练**：在 TimeBench 上进行密集监督学习。
    *   **后训练（Post-training）**：包括**持续预训练（CPT）**（使用加权 STP 目标强化短期预测）和**长上下文扩展（LCE）**（利用 RoPE 将上下文从 2880 扩展至 11520）。

### 3. 实验设计
*   **数据集/场景**：主要在 **GIFT-Eval** 排行榜上进行评估，包含 24 个数据集、14.4 万个序列、1.77 亿个数据点。
*   **Benchmark**：涵盖了点预测（MASE 指标）和概率预测（CRPS 指标）。
*   **对比方法**：
    *   **基础模型**：Chronos-2, TimesFM-2.5, TiRex, Moirai2, Sundial (Timer-3), Toto-1.0 等。
    *   **传统/统计方法**：AutoARIMA, AutoETS, Seasonal Naive 等。

### 4. 资源与算力
*   **算力说明**：论文提到使用了 **VeOmni** 分布式训练框架，支持十亿级参数跨设备扩展。
*   **技术细节**：采用 **BF16** 精度训练。
*   **明确性**：论文**未明确列出**具体的 GPU 型号（如 H100/A100）、具体数量以及总训练时长（GPU Hours）。仅提到数据存储约为 4TB。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在 GIFT-Eval 全集上进行了大规模零样本（Zero-shot）测试。
    *   **消融实验**：对比了 STP 与 NTP、MTP 的性能差异；分析了 TimeMoE 和 TimeSTP 块数量对性能的影响（Scaling Law 分析）。
    *   **针对性分析**：对数据增强的效果、预训练的必要性、长上下文扩展的效果进行了专项实验。
*   **充分性与客观性**：实验设计较为全面，涵盖了从短程到长程的多种预测场景，并使用了业界公认的第三方排行榜（GIFT-Eval）进行对比，结果具有较高的客观性和说服力。

### 6. 主要结论与发现
*   **SOTA 性能**：Timer-S1 在 GIFT-Eval 上取得了预训练模型中的最佳 MASE（0.693）和 CRPS（0.485）分数。
*   **序列计算的价值**：实验证明，增加序列计算深度（TimeSTP）对中长期预测的提升显著优于单纯的并行预测。
*   **缩放定律**：模型性能随参数量（TimeMoE 层数）和序列计算深度（TimeSTP 层数）的增加而持续提升，验证了时间序列模型也存在 Scaling Law。
*   **后训练的重要性**：持续预训练和长上下文扩展能显著进一步压低误差。

### 7. 优点
*   **架构创新**：TimeSTP 巧妙地平衡了自回归模型的“深度推理”和并行模型的“推理效率”，解决了长程预测的误差累积问题。
*   **数据规模**：1 万亿时间点的 TimeBench 是目前公开报道中规模最大的时间序列数据集之一。
*   **系统性优化**：不仅关注模型，还从数据增强、多阶段训练、长上下文等多个维度系统性地提升了基础模型的可用性。

### 8. 不足与局限
*   **外生变量缺失**：目前模型主要关注单变量上下文模式，**未原生支持外生协变量（Exogenous Covariates）**，这在某些实际应用场景（如受天气影响的电力负荷预测）中可能受限。
*   **单变量局限**：虽然通过单序列格式处理多变量，但缺乏对多变量间复杂显式相关性的建模。
*   **算力透明度**：缺乏具体的算力消耗数据，使得其他研究者难以评估复现该模型的成本。

（完）
