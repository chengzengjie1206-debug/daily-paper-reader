---
title: Dynamic Multi-period Experts for Online Time Series Forecasting
title_zh: 用于在线时间序列预测的动态多周期专家模型
authors: "Seungha Hong, Sukang Chae, Suyeon Kim, Sanghwan Jang, Hwanjo Yu"
date: 2026-03-10
pdf: "https://arxiv.org/pdf/2603.09062v1"
tags: ["query:ts"]
score: 10.0
evidence: 具有概念漂移自适应能力的在线时序预测
tldr: 针对在线时间序列预测中的概念漂移问题，本文提出DynaME框架。该方法将概念漂移细分为循环漂移和突发漂移，分别采用动态拟合历史周期模式的专家委员会和处理高不确定性场景的稳定通用专家来应对。实验证明，DynaME在多个基准数据集上显著优于现有基准，能有效适应复杂的动态环境。
selection_source: fresh_fetch
motivation: 现有的在线时间序列预测方法通常将概念漂移视为单一现象，忽视了循环模式与全新模式之间的差异。
method: 提出DynaME框架，通过动态专家委员会捕捉历史周期性规律以应对循环漂移，并利用通用专家处理高不确定性的突发漂移。
result: 在多个基准数据集和骨干网络上的广泛实验表明，DynaME在处理两种类型的概念漂移方面均表现出色，性能显著超过现有模型。
conclusion: DynaME通过区分并针对性处理循环与突发漂移，为在线时间序列预测提供了一种更具鲁棒性和适应性的解决方案。
---

## 摘要
在线时间序列预测 (OTSF) 要求模型能够持续适应概念漂移 (concept drift)。然而，现有方法通常将概念漂移视为一种单一的现象。为了解决这一局限性，我们首先通过将概念漂移分为两种不同的类型来对其进行重新定义：循环漂移 (Recurring Drift)，即先前见过的模式再次出现；以及突发漂移 (Emergent Drift)，即出现全新的模式。随后，我们提出了 DynaME (Dynamic Multi-period Experts)，这是一种旨在有效应对这种漂移双重性质的新型混合框架。针对循环漂移，DynaME 采用了一个由专门专家组成的委员会，这些专家在每个时间步动态拟合最相关的历史周期模式。针对突发漂移，该框架会检测高不确定性场景，并将依赖转向一个稳定的通用专家。在多个基准数据集和骨干网络上的广泛实验表明，DynaME 能够有效适应这两种概念漂移，并显著优于现有的基准方法。

## Abstract
Online Time Series Forecasting (OTSF) requires models to continuously adapt to concept drift. However, existing methods often treat concept drift as a monolithic phenomenon. To address this limitation, we first redefine concept drift by categorizing it into two distinct types: Recurring Drift, where previously seen patterns reappear, and Emergent Drift, where entirely new patterns emerge. We then propose DynaME (Dynamic Multi-period Experts), a novel hybrid framework designed to effectively address this dual nature of drift. For Recurring Drift, DynaME employs a committee of specialized experts that are dynamically fitted to the most relevant historical periodic patterns at each time step. For Emergent Drift, the framework detects high-uncertainty scenarios and shifts reliance to a stable, general expert. Extensive experiments on several benchmark datasets and backbones demonstrate that DynaME effectively adapts to both concept drifts and significantly outperforms existing baselines.

---

## 论文详细总结（自动生成）

以下是对论文《Dynamic Multi-period Experts for Online Time Series Forecasting》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：在线时间序列预测（OTSF）中如何有效应对**概念漂移（Concept Drift）**。
*   **研究背景**：传统离线训练无法适应持续到达的数据流。现有 OTSF 方法通常将概念漂移视为单一现象，并过度依赖“近期优先（Recency-bias）”的自适应策略。
*   **研究动机**：作者发现“近期优先”策略在处理**循环模式**时效果不佳（例如：本周五的交通模式与上周五更相似，而非昨天）。因此，论文将概念漂移重新定义为两类：
    *   **循环漂移（Recurring Drift）**：先前出现过的模式再次出现。
    *   **突发漂移（Emergent Drift）**：由于突发事件（如疫情、新基建）产生的全新模式。

### 2. 论文提出的方法论：DynaME 框架
DynaME（Dynamic Multi-period Experts）采用混合架构，结合了稳定的骨干网络和高度自适应的专家委员会。
*   **动态周期分析**：利用快速傅里叶变换（FFT）从历史窗口中识别当前最显著的 $k$ 个周期频率，并将其转化为具体的周期长度（如日、周）。
*   **非参数化专家自适应**：
    *   为每个识别出的周期分配一个专门专家。
    *   **核心技术**：为了避免梯度下降的高额计算开销，专家通过**岭回归（Ridge Regression）的对偶形式**进行在线拟合。这使得计算复杂度从特征维度的立方 $O(D^3)$ 降低到样本数量的立方 $O(n^3)$，实现了极速的每步更新。
*   **动态门控网络与安全机制**：
    *   **门控网络**：基于当前输入的特征表示，动态分配各专家（包括一个通用的参数化专家和多个专门的非参数化专家）的权重。
    *   **危险信号（Danger Signal）**：通过监测近期预测误差（MSE）的突发尖峰来识别“突发漂移”。当检测到危险时，系统会自动增加对“通用专家”的依赖，将其作为缓冲，直到专门专家学习到新模式。

### 3. 实验设计
*   **数据集**：使用了 5 个主流 TSF 基准数据集：ETTh2、ETTm1、Traffic（交通）、ECL（电力）、Weather（天气）。
*   **骨干网络（Backbones）**：为了验证通用性，在 PatchTST、iTransformer 和 xPatch 三种 SOTA 模型上进行了测试。
*   **对比方法（Baselines）**：
    *   基础方法：GD（全模型梯度下降微调）。
    *   先进 OTSF 框架：DSOF、PROCEED、SOLID。
*   **实验设置**：严格遵循在线学习协议，考虑了反馈延迟（Feedback Delay），确保无信息泄露。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号或数量。
*   **效率分析**：作者提供了详细的**推理时间（Wall-clock time）**对比。结果显示，采用对偶形式优化的 DynaME-dual 比原始形式快 2.5~3 倍，其运行速度与 PROCEED 等先进基准相当，证明了其在实时场景下的可行性。

### 5. 实验数量与充分性
*   **实验规模**：
    *   涵盖了 5 个数据集、3 种骨干网络、3 种预测长度（24, 48, 96）。
    *   进行了详尽的**消融实验**，验证了动态门控、危险信号机制、动态周期选择的有效性。
    *   进行了**超参数敏感性分析**（专家数量 $k$、样本量 $n$）。
*   **充分性评价**：实验设计非常充分且客观。通过在不同架构的骨干网络上取得一致的提升，证明了该框架的鲁棒性和公平性。

### 6. 主要结论与发现
*   **性能领先**：DynaME 在所有测试场景下均达到了 SOTA 性能，显著优于现有的 recency-focused（关注近期）方法。
*   **周期性的动态性**：实验证实，数据的主导周期并非固定，而是随时间变化的，DynaME 的动态选择机制抓住了这一特性。
*   **稳定性**：危险信号机制能有效抑制突发漂移带来的误差尖峰，使模型在环境剧变时更稳健。

### 7. 优点
*   **分类治理**：首次明确区分循环漂移和突发漂移，并给出针对性方案。
*   **计算高效**：巧妙利用岭回归对偶解，解决了在线学习中每步更新的计算瓶颈。
*   **模型无关（Plug-in）**：作为一个通用的增强框架，可以轻松集成到任何现有的深度学习预测模型中。
*   **参数高效**：除了骨干网络，仅增加了极少量的门控网络参数（约 0.1M）。

### 8. 不足与局限
*   **历史缓冲区依赖**：虽然周期选择是动态的，但仍依赖于预设的历史缓冲区大小 $M$，若漂移周期超过 $M$，可能无法捕捉。
*   **超参数敏感性**：实验显示专家数量 $k$ 和拟合样本量 $n$ 对性能有一定影响，在不同数据集上可能需要微调。
*   **应用限制**：主要针对具有一定周期性特征的时间序列，对于完全随机或无规律波动的序列，专家委员会的优势可能不明显。

（完）
