---
title: "GCGNet: Graph-Consistent Generative Network for Time Series Forecasting with Exogenous Variables"
title_zh: GCGNet：用于带外生变量时间序列预测的图一致性生成网络
authors: "Zhengyu Li, Xiangfei Qiu, Yuhan Zhu, Xingjian Wu, Jilin Hu, Chenjuan Guo, Bin Yang"
date: 2026-03-09
pdf: "https://arxiv.org/pdf/2603.08032v1"
tags: ["query:ts"]
score: 9.0
evidence: 利用外部变量和通道相关性进行预测
tldr: 针对含外生变量的时间序列预测中时空关联建模不充分及对噪声敏感的问题，本文提出了一种图一致性生成网络 GCGNet。该模型通过变分生成器生成初步预测，利用图结构对齐器确保生成关联与真实关联的一致性以提升鲁棒性，并结合图精炼器进一步优化预测精度。在12个真实数据集上的实验证明，GCGNet 在处理复杂时空依赖方面具有显著优势。
selection_source: fresh_fetch
motivation: 现有预测方法多采用两步策略分离建模时间与通道关联，限制了对联合相关性的捕捉能力且易受噪声干扰。
method: 提出由变分生成器、图结构对齐器和图精炼器组成的生成式框架，通过图结构一致性引导模型学习鲁棒的关联特征。
result: 在12个真实世界数据集上的对比实验显示，GCGNet 的预测性能全面超越了现有的最先进基准模型。
conclusion: GCGNet 通过整合图一致性建模与生成式架构，有效解决了外生变量预测中的关联建模与鲁棒性难题。
---

## 摘要
外生变量为预测未来内生变量提供了宝贵的补充信息。带外生变量的预测需要同时考虑过去到未来的依赖关系（即时间相关性）以及外生变量对内生变量的影响（即通道相关性）。当未来外生变量已知时，这一点至关重要，因为它们可能直接影响未来的内生变量。许多针对带外生变量时间序列预测的方法已被提出，重点在于建模时间相关性和通道相关性。然而，其中大多数方法采用两步策略，分别建模时间相关性和通道相关性，这限制了它们捕捉跨时间和通道的联合相关性的能力。此外，在现实场景中，时间序列经常受到各种形式噪声的影响，这凸显了此类相关性建模中鲁棒性的关键重要性。为了解决这些局限性，我们提出了 GCGNet，一种用于带外生变量时间序列预测的图一致性生成网络。具体而言，GCGNet 首先采用变分生成器（Variational Generator）来产生粗略预测。随后，图结构对齐器（Graph Structure Aligner）通过评估生成的预测与真实相关性之间的一致性来进一步引导模型，其中相关性被表示为图，且对噪声具有鲁棒性。最后，提出了一种图精炼器（Graph Refiner）来优化预测，以防止退化并提高准确性。在 12 个真实世界数据集上的广泛实验表明，GCGNet 优于最先进的基准模型。

## Abstract
Exogenous variables offer valuable supplementary information for predicting future endogenous variables. Forecasting with exogenous variables needs to consider both past-to-future dependencies (i.e., temporal correlations) and the influence of exogenous variables on endogenous variables (i.e., channel correlations). This is pivotal when future exogenous variables are available, because they may directly affect the future endogenous variables. Many methods have been proposed for time series forecasting with exogenous variables, focusing on modeling temporal and channel correlations. However, most of them use a two-step strategy, modeling temporal and channel correlations separately, which limits their ability to capture joint correlations across time and channels. Furthermore, in real-world scenarios, time series are frequently affected by various forms of noises, underscoring the critical importance of robustness in such correlations modeling. To address these limitations, we propose GCGNet, a Graph-Consistent Generative Network for time series forecasting with exogenous variables. Specifically, GCGNet first employs a Variational Generator to produce coarse predictions. A Graph Structure Aligner then further guides it by evaluating the consistency between the generated and true correlations, where the correlations are represented as graphs, and are robust to noises. Finally, a Graph Refiner is proposed to refine the predictions to prevent degeneration and improve accuracy. Extensive experiments on 12 real-world datasets demonstrate that GCGNet outperforms state-of-the-art baselines.

---

## 论文详细总结（自动生成）

### GCGNet：用于带外生变量时间序列预测的图一致性生成网络

#### 1. 核心问题与整体含义（研究动机和背景）
在现实世界的时间序列预测中，除了目标变量（内生变量）的历史数据外，通常还存在相关的外生变量（如天气预报、电网负荷预测等）。有效的预测需要同时捕捉**时间相关性**（历史对未来的影响）和**通道相关性**（外生变量对内生变量的影响）。
*   **现有挑战**：大多数现有方法采用“两步走”策略（先建模时间再建模通道，或反之），这会导致两种相关性之间产生干扰，限制了捕捉联合相关性的能力。此外，传感器故障或记录错误带来的**噪声**会干扰模型学习真实的关联。
*   **研究动机**：提出一种能够**联合建模**时间与通道相关性，并对噪声具有**鲁棒性**的生成式框架。

#### 2. 方法论：核心思想与关键技术
GCGNet 提出了一个包含三个核心模块的生成式架构：
*   **变分生成器 (Variational Generator)**：基于 VAE（变分自编码器）架构，利用历史内生和外生变量生成未来的粗略预测（$\tilde{Y}$）。该模块能够捕捉数据的分布不确定性。
*   **图结构对齐器 (Graph Structure Aligner)**：
    *   **核心思想**：通过“图一致性”引导生成过程。
    *   **技术细节**：将序列切片（Patching）后，利用 **Graph VAE** 分别提取真实序列和生成序列的联合相关性图结构（邻接矩阵 $A$ 和 $\hat{A}$）。
    *   **对齐损失**：通过最小化 $A$ 与 $\hat{A}$ 之间的 $L_1$ 距离，强制生成器产生的序列在结构特征上与真实数据保持一致。
*   **图精炼器 (Graph Refiner)**：
    *   利用学习到的邻接矩阵 $\hat{A}$ 作为指导，通过多层**图卷积网络 (GCN)** 对初始预测进行精炼。
    *   **防退化设计**：通过将图结构直接用于最终预测，迫使 Graph VAE 产生有意义的关联矩阵，防止生成模型出现输出单一的退化问题。

#### 3. 实验设计
*   **数据集**：涵盖 12 个真实世界数据集，包括电力市场（NP, PJM, BE, FR, DE）、能源（Energy）、水利（Colbun, Rapel）和风电（Sdwpf 系列）。
*   **对比基准 (Baselines)**：
    *   **原生支持外生变量的方法**：TimeXer, TFT, TiDE。
    *   **适配后的先进模型**：DUET, CrossLinear, Amplifier, TimeKAN, xPatch, PatchTST（通过增加 MLP Fusion 模块使其能够利用未来外生变量）。
*   **实验场景**：包括短期预测、长期预测、未来外生变量不可用场景、以及外生变量部分缺失（0 填充或随机噪声填充）的鲁棒性测试。

#### 4. 资源与算力
*   **硬件环境**：实验运行在 **NVIDIA Tesla-A800 GPU** 上。
*   **软件环境**：Python 3.8, PyTorch 框架。
*   **说明**：论文未明确给出具体的训练时长或显存占用峰值，但提到代码已开源以供复现。

#### 5. 实验数量与充分性
*   **实验规模**：在 12 个数据集上进行了详尽的测试，涵盖了 MSE 和 MAE 两个指标。
*   **消融实验**：针对变分生成器、对齐损失、Graph VAE 和图精炼器分别做了拆解实验，验证了各组件的必要性。
*   **公平性与客观性**：
    *   对不支持外生变量的模型统一使用了 MLP Fusion 模块，确保了输入信息的一致性。
    *   针对测试集处理，禁用了可能导致误差偏差的 `drop_last` 操作。
    *   实验涵盖了未来外生变量“已知”与“未知”两种实际情况，评估非常全面。

#### 6. 主要结论与发现
*   **性能领先**：GCGNet 在绝大多数数据集和预测步长上均取得了最优（Red）或次优（Blue）的成绩，显著优于传统的“两步走”模型。
*   **联合建模的优势**：可视化分析证明，GCGNet 能同时捕捉周期性规律和外生变量的瞬时影响，而 PatchTST 等模型容易忽略外生信息。
*   **鲁棒性强**：在数据缺失率高达 50% 的情况下，GCGNet 的性能下降幅度远小于基准模型，证明了生成式架构和图对齐机制的抗噪能力。
*   **灵活性**：即使在未来外生变量不可用的情况下，模型依然能通过自身生成的预测值维持高精度。

#### 7. 优点（亮点）
*   **架构创新**：将图神经网络的结构学习与变分生成模型结合，解决了时间/通道相关性耦合建模的难题。
*   **鲁棒性设计**：通过图结构对齐而非简单的点对点重建，使模型对局部噪声和异常值不敏感。
*   **实用性高**：设计了可选的未来外生变量生成分支，使其能适应多种现实数据环境。

#### 8. 不足与局限
*   **计算复杂度**：引入了多个 VAE 模块和 GCN 结构，相比于纯线性模型（如 CrossLinear），其参数量和推理延迟可能会更高（文中未详细对比推理速度）。
*   **超参数敏感性**：实验显示 Patch 维度、VAE 潜空间维度和 GCN 层数对结果有一定影响，在不同领域的数据集上可能需要精细调优。
*   **图结构的稠密性**：虽然使用了 Sparsify（稀疏化）策略，但在处理极大规模变量（成千上万个通道）时，图运算的内存开销可能成为瓶颈。

（完）
