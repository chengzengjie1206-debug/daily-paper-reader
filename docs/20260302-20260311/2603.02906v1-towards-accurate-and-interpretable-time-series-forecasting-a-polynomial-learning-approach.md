---
title: "Towards Accurate and Interpretable Time-series Forecasting: A Polynomial Learning Approach"
title_zh: 迈向准确且可解释的时间序列预测：一种多项式学习方法
authors: "Bo Liu, Shao-Bo Lin, Changmiao Wang, Xiaotong Liu"
date: 2026-03-03
pdf: "https://arxiv.org/pdf/2603.02906v1"
tags: ["query:ts"]
score: 10.0
evidence: 用于时间序列预测的可解释多项式学习
tldr: 该研究针对时间序列预测中准确性与可解释性难以兼得的问题，提出了一种可解释多项式学习（IPL）方法。通过多项式表示显式建模原始特征及其任意阶交互，IPL在保留时间依赖性的同时提供了特征级可解释性。实验证明，该方法在比特币价格和天线数据预测中表现优异，不仅提高了预测精度，还为预警机制提供了更简单高效的支持。
selection_source: fresh_fetch
motivation: 现有时间序列预测方法在建模时间依赖性、提供特征级可解释性以及平衡精度与透明度方面存在局限。
method: 提出IPL方法，通过多项式结构显式建模特征及其高阶交互，并允许通过调整多项式阶数灵活权衡精度与可解释性。
result: 在模拟数据、比特币价格和现场天线数据上的实验表明，IPL在保持高精度的同时，其可解释性优于主流解释方法。
conclusion: IPL为时间序列预测提供了一种兼顾准确性和透明度的新方案，特别适用于需要高效预警机制的工业应用场景。
---

## 摘要
时间序列预测能够实现预警，并推动资产性能管理从传统的计划性维护转向预测性维护。然而，预测方法缺乏可解释性削弱了用户的信任，并增加了开发人员调试的难度。因此，可解释的时间序列预测引起了越来越多的研究关注。尽管如此，现有方法仍存在一些局限性，包括对时间依赖性建模不足、缺乏支持预警的特征级可解释性，以及难以同时实现准确性和可解释性。本文提出了可解释多项式学习（IPL）方法，该方法通过多项式表示显式地对原始特征及其任意阶交互进行建模，从而将可解释性集成到模型结构中。这种设计保留了时间依赖性，提供了特征级的可解释性，并通过调整多项式阶数在预测准确性和可解释性之间提供了灵活的权衡。我们在模拟数据和比特币价格数据上对 IPL 进行了评估，结果表明，与广泛使用的可解释性方法相比，它在实现高预测准确性的同时具有更优越的可解释性。在现场采集的天线数据上的实验进一步证明，IPL 能够产生更简单、更高效的预警机制。

## Abstract
Time series forecasting enables early warning and has driven asset performance management from traditional planned maintenance to predictive maintenance. However, the lack of interpretability in forecasting methods undermines users' trust and complicates debugging for developers. Consequently, interpretable time-series forecasting has attracted increasing research attention. Nevertheless, existing methods suffer from several limitations, including insufficient modeling of temporal dependencies, lack of feature-level interpretability to support early warning, and difficulty in simultaneously achieving the accuracy and interpretability. This paper proposes the interpretable polynomial learning (IPL) method, which integrates interpretability into the model structure by explicitly modeling original features and their interactions of arbitrary order through polynomial representations. This design preserves temporal dependencies, provides feature-level interpretability, and offers a flexible trade-off between prediction accuracy and interpretability by adjusting the polynomial degree. We evaluate IPL on simulated and Bitcoin price data, showing that it achieves high prediction accuracy with superior interpretability compared with widely used explainability methods. Experiments on field-collected antenna data further demonstrate that IPL yields simpler and more efficient early warning mechanisms.

---

## 论文详细总结（自动生成）

这是一份关于论文《Towards Accurate and Interpretable Time-series Forecasting: A Polynomial Learning Approach》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
论文聚焦于**时间序列预测中的“准确性-可解释性”权衡难题**。在工业预测性维护（如天线故障预警）和金融监控等高风险领域，模型不仅需要高精度的预测，还需要**特征级（Feature-level）的可解释性**，以指导实际干预。
*   **现有局限**：深度学习模型（如Transformer、RNN）虽准确但属于“黑盒”；传统模型（如ARIMAX）虽可解释但难以捕捉复杂的非线性交互；事后解释方法（如SHAP、LIME）往往破坏时间序列的连续性，导致解释不可靠。
*   **研究目标**：提出一种既能显式建模时间依赖和特征交互，又能灵活调节精度与透明度的预测方法。

### 2. 论文提出的方法论：可解释多项式学习（IPL）
*   **核心思想**：将可解释性直接嵌入模型结构中。通过**多项式表示**显式地对原始特征及其任意阶的交互项进行建模。
*   **关键技术细节**：
    *   **多项式建模**：模型函数被表示为输入特征的单项式加权和。例如，权重 $w_t^{(i)}$ 代表特定时间点特征的重要性，而 $w_{t1,t2}^{(i1,i2)}$ 则量化了跨时间、跨特征的交互影响。
    *   **时间依赖集成**：通过引入滞后变量（Lagged variables，即过去时刻的输入和标签）作为多项式的输入，原生支持时间序列的动态特性。
    *   **优化算法**：采用**交替方向乘子法（ADMM）**求解目标函数。该算法支持多种损失函数（如平方损失、Hinge损失、Logistic损失），具有良好的收敛性和计算效率。
    *   **稀疏性处理**：引入解释性阈值 $I$，仅保留贡献最大的多项式项，从而生成简洁、易读的预测规则。

### 3. 实验设计
*   **数据集与场景**：
    1.  **模拟数据**：用于验证模型在已知真实函数的情况下，识别关键特征和交互项的准确性。
    2.  **比特币历史价格**：金融时间序列分类任务，预测价格涨跌趋势。
    3.  **现场采集天线数据**：来自中国电子科技集团第39研究所的真实工业数据，用于构建故障预警机制。
*   **对比方法（Benchmarks）**：
    *   **内在可解释模型**：ARIMAX。
    *   **事后解释方法**：SHAP（基于深度神经网络 DNN）和 LIME（基于核岭回归 KRR）。
    *   **评估指标**：MSE/AUC、特征重合率、排名相似度、计算效率（运行时间）以及扰动分析（Perturbation Analysis）。

### 4. 资源与算力
*   **硬件环境**：主要实验在配备 **Intel Core i5 2GHz 处理器**的个人电脑（PC）上完成。
*   **算力细节**：
    *   **IPL、ARIMAX、SHAP** 均在 CPU 上运行。
    *   **LIME** 实验由于计算量巨大，使用了 **GPU 加速**（未指明具体型号）。
*   **效率对比**：在模拟实验中，IPL 的运行时间约为 1.81 秒（CPU），而 LIME 即使在 GPU 加速下也需要约 32.5 分钟，IPL 的效率优势极其显著。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了回归（平方损失）和分类（Hinge损失）两类任务。
*   **充分性**：
    *   每组实验均进行了 **10 次独立试验**并取平均值，保证了结果的统计意义。
    *   进行了**扰动分析**，通过人为干扰特征观察预测性能下降程度，客观验证了特征重要性的真实性。
    *   **消融研究**：分析了不同滞后阶数（Lag parameters）和多项式阶数（Degree）对性能的影响。
*   **客观性**：实验对比了不同维度的输入（原始特征 vs 增加冗余特征），验证了模型排除干扰项的能力，设计较为严谨。

### 6. 主要结论与发现
*   **精度与解释性的双赢**：IPL 在预测精度上显著优于 ARIMAX，在特征识别的准确性上优于 SHAP 和 LIME。
*   **特征交互的重要性**：实验证明，特征之间的交互（如天线转速与电流比的乘积）往往比单一特征更能反映系统状态（如机械功率异常）。
*   **灵活的权衡**：通过调整多项式阶数 $s$，用户可以根据需求在“极简规则”和“高精模型”之间切换。
*   **工业实用性**：在天线预警任务中，IPL 仅需 2 个核心特征交互项即可构建出准确率超过 98% 的预警规则，且能实现 100% 的异常召回率。

### 7. 优点（亮点）
*   **结构化透明**：不同于事后解释，IPL 的权重直接对应物理含义，不存在解释偏差。
*   **计算极其高效**：ADMM 算法配合多项式核，使其在处理中等规模数据时比主流解释工具快几个数量级。
*   **原生时间序列支持**：通过多项式结构保留了时间自相关性，解决了 LIME/SHAP 独立扰动时间点导致的解释失效问题。

### 8. 不足与局限
*   **维度灾难风险**：虽然论文提到 $n$ 通常小于 $m$，但当特征维度 $d$ 和多项式阶数 $s$ 同时增大时，候选单项式的数量会呈组合爆炸式增长，可能限制其在超高维数据上的应用。
*   **基准对比范围**：实验主要对比了传统方法和通用解释工具，未与最新的可解释深度学习模型（如 Temporal Fusion Transformer, TFT）进行直接的精度对比。
*   **超参数依赖**：多项式阶数 $s$ 和滞后参数 $L_x, L_y$ 仍需根据经验或交叉验证确定，缺乏自动化的参数搜索机制。

（完）
